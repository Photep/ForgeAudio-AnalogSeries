---
phase: 27-notion-manual
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/install.md
  - docs/changelog.md
  - docs/license-credits.md
  - tests/check_docs.sh
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "D-02: Install, changelog, and license/credits each exist as their own section file under docs/"
    - "Install section documents BOTH paths: VCV Library subscribe AND manual .vcvplugin install"
    - "D-09: Changelog is structured by shipped milestone (v1.0 → v1.4) derived from milestone history; no invented CHANGELOG"
    - "Pitfall 5: Changelog uses milestone names, states manifest version 2.0.0 once as context, never relabels milestones as 2.x"
    - "License/credits summarizes and links existing LICENSE (GPL-3.0), NOTICES, res/fonts/OFL.txt — does not re-author license text"
    - "D-06: A brand-name denylist check (tests/check_docs.sh) exists and returns zero hits across docs/"
  artifacts:
    - path: "docs/install.md"
      provides: "VCV Library + manual .vcvplugin install steps"
      contains: ".vcvplugin"
    - path: "docs/changelog.md"
      provides: "Milestone-structured changelog v1.0–v1.4"
      contains: "v1.0"
    - path: "docs/license-credits.md"
      provides: "GPL-3.0 summary + third-party notices links"
      contains: "GPL-3.0"
    - path: "tests/check_docs.sh"
      provides: "Brand-name denylist grep + docs fact-check asserts (D-06 / Pitfall 4)"
  key_links:
    - from: "docs/license-credits.md"
      to: "../LICENSE, ../NOTICES, ../res/fonts/OFL.txt"
      via: "relative links to existing files"
      pattern: "LICENSE"
    - from: "tests/check_docs.sh"
      to: "docs/"
      via: "grep denylist over docs/*.md"
      pattern: "grep"
---

<objective>
Author the install, changelog, and license/credits sections, and add `tests/check_docs.sh` — the brand-name denylist + docs fact-check that enforces D-06/Pitfall 4 and closes the Wave 0 validation gap.

Purpose: Completes the remaining table-stakes reference sections (DOC-02) that draw on milestone history (D-09) and existing legal files, and stands up the automatable guard the whole phase's validation relies on.
Output: Three section files + one executable check script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-notion-manual/27-RESEARCH.md
@.planning/phases/27-notion-manual/27-VALIDATION.md
@.planning/phases/27-notion-manual/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author install + license/credits sections</name>
  <files>docs/install.md, docs/license-credits.md</files>
  <read_first>
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Docs Structure — license source material; install both paths)
    - .planning/phases/27-notion-manual/27-UI-SPEC.md (§Copywriting Contract — Primary CTA, Error state troubleshooting line)
    - LICENSE (GPL-3.0 header), NOTICES (third-party inventory), res/fonts/OFL.txt (JetBrains Mono OFL)
    - plugin.json (slug ForgeAudio-AnalogSeries, module slug ForgeAnalogLFO, version 2.0.0)
  </read_first>
  <action>
    Create `docs/install.md`: `#` H1 + a `##` "VCV Library" primary path ("Add ForgeAnalogLFO from the VCV Library" — subscribe in the Library, sync in Rack) and a `##` "Manual install" secondary path (download the `.vcvplugin`, place in the Rack user plugins folder, relaunch Rack). Include the troubleshooting line "Module not appearing? Fully quit and relaunch Rack, then re-check the plugin list." Terse (D-05).
    Create `docs/license-credits.md`: `#` H1; state the plugin is licensed GPL-3.0-or-later and link the repo-root `LICENSE` via a relative path (`../LICENSE`); a `##` Third-party notices subsection that summarizes the shipped third-party assets (JetBrains Mono under the SIL OFL; panel SVG font outlines Chakra Petch / Bebas Neue confirmed-OFL per Phase 25) and links `../NOTICES` and `../res/fonts/OFL.txt`. Do NOT duplicate/re-author the full license text — summarize + link (Don't Hand-Roll).
  </action>
  <verify>
    <automated>grep -qF ".vcvplugin" docs/install.md &amp;&amp; grep -qi "VCV Library" docs/install.md &amp;&amp; grep -qF "GPL-3.0" docs/license-credits.md &amp;&amp; grep -qF "LICENSE" docs/license-credits.md &amp;&amp; grep -qi "OFL" docs/license-credits.md</automated>
  </verify>
  <acceptance_criteria>
    - `install.md` documents both the VCV Library path and the manual `.vcvplugin` path, plus the relaunch troubleshooting line.
    - `license-credits.md` states GPL-3.0-or-later, links `../LICENSE`, `../NOTICES`, `../res/fonts/OFL.txt`, and does NOT paste the full license body.
  </acceptance_criteria>
  <done>Install (both paths) and license/credits (summary + relative links to existing legal files) sections exist.</done>
</task>

<task type="auto">
  <name>Task 2: Author milestone-structured changelog</name>
  <files>docs/changelog.md</files>
  <read_first>
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Changelog Derivation — the v1.0–v1.4 milestone table, Pitfall 5)
    - .planning/ROADMAP.md (milestone list)
    - .planning/milestones/v1.0-ROADMAP.md … v1.3-ROADMAP.md (shipped milestone content)
    - plugin.json (version 2.0.0)
  </read_first>
  <action>
    Create `docs/changelog.md`: `#` H1 + one `##` entry per shipped milestone using the milestone NAMES from RESEARCH §Changelog Derivation — "v1.0 Analog Series LFO", "v1.1 Clock Sync", "v1.2 Deep Analog", "v1.3 Forge Noir", "v1.4 Tempered" — each with its headline feature bullets from that table. State exactly once, as context, that the plugin's manifest version is `2.0.0` (MAJOR = Rack 2). Pitfall 5: NEVER relabel milestones as "2.x" and never present the manifest version as a milestone. v1.4 is in-progress; note it covers release hardening + this manual + public source release.
  </action>
  <verify>
    <automated>for m in "v1.0" "v1.1" "v1.2" "v1.3" "v1.4"; do grep -qF "$m" docs/changelog.md || exit 1; done &amp;&amp; grep -qF "2.0.0" docs/changelog.md &amp;&amp; ! grep -qE '"?2\.[1-4]\b' docs/changelog.md</automated>
  </verify>
  <acceptance_criteria>
    - `changelog.md` has a milestone-named entry for each of v1.0–v1.4 with feature headlines.
    - Manifest version `2.0.0` appears exactly as context; no milestone is relabeled "2.1/2.2/2.3/2.4".
  </acceptance_criteria>
  <done>Changelog structured by milestone v1.0→v1.4 with the 2.0.0 manifest-version note stated once.</done>
</task>

<task type="auto">
  <name>Task 3: Add docs fact-check + brand-name denylist script</name>
  <files>tests/check_docs.sh</files>
  <read_first>
    - .planning/phases/27-notion-manual/27-VALIDATION.md (§Per-Task Verification Map, §Wave 0 Requirements)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Validation Architecture — denylist + fact greps)
    - src/AnalogLFO.cpp (RATIO_LABELS, SWING_MENU_LABELS — the oracle values)
  </read_first>
  <action>
    Create `tests/check_docs.sh` (executable, `#!/usr/bin/env bash`, `set -euo pipefail`): (1) a brand-name denylist grep — case-insensitive search over `docs/` for Moog, Minimoog, Roland, Juno, SH-101, Prophet, Oberheim, Korg — that FAILS (non-zero exit) on any hit (D-06 / Pitfall 4); (2) file-existence asserts for `docs/index.md` and each **wave-1** section file (`engine-concept.md`, `io-reference.md`, `context-menu.md`, `clock-sync.md`, `install.md`, `changelog.md`, `license-credits.md`) — do NOT require `docs/panel.md` here (it is authored in 27-04, wave 2, and validated by that plan's checkpoint), so the script returns PASS at wave 1; (3) fact asserts that the 6 Swing labels and the ratio labels /16, /1.5, x1.5, x16 and the CV token `5V` appear in `docs/`. Print a clear PASS/FAIL summary line. Make the script the phase's single automatable doc gate. `chmod +x` the file.
  </action>
  <verify>
    <automated>test -x tests/check_docs.sh &amp;&amp; grep -qiE "moog|roland|korg|prophet|oberheim|juno" tests/check_docs.sh &amp;&amp; bash -n tests/check_docs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `tests/check_docs.sh` is executable and passes `bash -n` syntax check.
    - It greps the full trademarked-name denylist over `docs/` and exits non-zero on any hit.
    - It asserts existence of `docs/index.md` + the 7 wave-1 section files (not `docs/panel.md`) and the Swing/ratio/CV fact tokens.
    - Running it after Plans 01/02 authoring returns PASS (zero denylist hits; no panel.md dependency).
  </acceptance_criteria>
  <done>Executable denylist + fact-check script exists and enforces D-06 / DOC-02 facts automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public reader (Phase 28) | Committed docs + legal attributions become publicly readable at the public flip |
| shipped third-party assets → GPL release | License/attribution correctness gates a clean public GPL release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Tampering (legal/IP) | docs/*.md prose | mitigate | `tests/check_docs.sh` denylist grep fails on any trademarked synth brand name across docs/ (D-06 / Pitfall 4) |
| T-27-03 | Repudiation (attribution) | license-credits.md | mitigate | Summarize + link the authoritative LICENSE / NOTICES / OFL.txt rather than re-authoring; keeps attribution correct and single-sourced |
| T-27-04 | Information disclosure | changelog/install prose | mitigate | Public-facing feature/install facts only; no internal paths, secrets, or PII committed |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs (docs authoring) — N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `docs/install.md`, `docs/changelog.md`, `docs/license-credits.md`, `tests/check_docs.sh` all exist.
- `tests/check_docs.sh` runs and returns PASS with zero denylist hits after authoring.
- Changelog uses milestone names, states 2.0.0 once, no 2.x relabeling.
</verification>

<success_criteria>
Install (both paths), milestone changelog (v1.0–v1.4), and license/credits (summary + links) sections exist; the executable denylist/fact-check gate passes.
</success_criteria>

<output>
Create `.planning/phases/27-notion-manual/27-02-SUMMARY.md` when done.
</output>

---
phase: 27-notion-manual
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin.json
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "D-04: plugin.json gains a top-level manualUrl field pointing at the docs landing, and keeps pluginUrl/authorUrl/sourceUrl"
    - "plugin.json remains valid JSON and version stays 2.0.0 (never edited — Pitfall 5)"
    - "D-01: ROADMAP §Phase 27 + REQUIREMENTS DOC-01 wording updated from Notion → GitHub Markdown under docs/ with an index.md hub"
    - "D-07: ROADMAP success criteria + REQUIREMENTS DOC-02 drop the patch-examples table-stakes item"
    - "D-04: DOC-03 wording updated from 'linked from pluginUrl' → 'linked from plugin.json manualUrl; public reachability completes with the Phase 28 repo-public flip'"
    - "Sequencing: no live public-URL verification is attempted this phase (repo is private until Phase 28)"
  artifacts:
    - path: "plugin.json"
      provides: "Manifest with manualUrl added"
      contains: "manualUrl"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 27 reconciled to GitHub-Markdown pivot"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOC-01/02/03 + Out-of-Scope reconciled"
  key_links:
    - from: "plugin.json"
      to: "docs/index.md (public repo blob URL)"
      via: "manualUrl field"
      pattern: "manualUrl"
---

<objective>
Add the `manualUrl` manifest link (D-04) and apply the ROADMAP/REQUIREMENTS reconciliation edits that record the Notion→GitHub pivot (D-01), the dropped patch-examples item (D-07), and the manualUrl link mechanism (D-04).

Purpose: Satisfies DOC-03's manifest-link requirement and brings the source-of-truth planning docs in line with the locked decisions so downstream verification checks the right things. Independent of the docs-authoring plans.
Output: Updated `plugin.json`, `ROADMAP.md`, `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-notion-manual/27-RESEARCH.md
@.planning/phases/27-notion-manual/27-CONTEXT.md
@plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manualUrl to plugin.json</name>
  <files>plugin.json</files>
  <read_first>
    - plugin.json (current manifest — slug ForgeAudio-AnalogSeries, version 2.0.0, existing authorUrl/pluginUrl/sourceUrl all → https://github.com/Photep/ForgeAudio-AnalogSeries)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§VCV Manifest Conventions — manualUrl is a documented plugin-level field)
    - .planning/phases/27-notion-manual/27-CONTEXT.md (D-04)
  </read_first>
  <action>
    Add a top-level `"manualUrl"` key to `plugin.json` with value `https://github.com/Photep/ForgeAudio-AnalogSeries/blob/main/docs/index.md` (owner Photep, repo ForgeAudio-AnalogSeries, branch main confirmed from the existing URLs + git remote). Place it alongside the existing `authorUrl`/`pluginUrl`/`sourceUrl` and KEEP all three (D-04 — manualUrl coexists, does not replace them). Do NOT change `version` (stays `2.0.0` — Pitfall 5), `slug`, `minRackVersion`, tags, or any other field. Optionally (Claude's discretion, A2) also add `"changelogUrl"` → `https://github.com/Photep/ForgeAudio-AnalogSeries/blob/main/docs/changelog.md`. Keep valid JSON (comma placement, 2-space indent matching the file).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('plugin.json')); assert d['manualUrl']=='https://github.com/Photep/ForgeAudio-AnalogSeries/blob/main/docs/index.md', d.get('manualUrl'); assert d['version']=='2.0.0'; assert d['pluginUrl'] and d['sourceUrl'] and d['authorUrl']; print('OK', d['manualUrl'])"</automated>
  </verify>
  <acceptance_criteria>
    - `plugin.json` parses as valid JSON.
    - `manualUrl` == `https://github.com/Photep/ForgeAudio-AnalogSeries/blob/main/docs/index.md`.
    - `version` is still `2.0.0`; `authorUrl`/`pluginUrl`/`sourceUrl` are all still present.
  </acceptance_criteria>
  <done>plugin.json has manualUrl pointing at the docs hub, all prior URLs retained, version unchanged, JSON valid.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP §Phase 27</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (§"Phase 27: Notion Manual" checklist line ~84 + Phase Details header ~174, Goal ~175, Success Criteria, parallel note ~201)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Roadmap / Requirements Reconciliation — exact edits)
    - .planning/phases/27-notion-manual/27-CONTEXT.md (D-01, D-04, D-07)
  </read_first>
  <action>
    Edit ROADMAP §Phase 27 per RESEARCH §Reconciliation: retitle "Phase 27: Notion Manual" → "Phase 27: User Manual (GitHub Markdown)" in both the milestone checklist line and the Phase Details header. Rewrite the Goal from "lives in Notion" → "lives as GitHub Markdown under `docs/` in the (public) repo, linked from `plugin.json` `manualUrl`". Rewrite Success Criteria: #1 "new top-level Notion page … subpage-per-section" → "`docs/` directory with per-section `.md` files + a `docs/index.md` hub"; #2 REMOVE "patch examples" from the table-stakes list (D-07), keep all other sections; #3 "shared to web … linked from `pluginUrl`/`manualUrl`" → "committed `docs/`; `manualUrl` added to `plugin.json`; public reachability completes with Phase 28". Drop "Notion" from the parallel-drafting note (cosmetic). Do NOT touch other phases.
  </action>
  <verify>
    <automated>grep -q "GitHub Markdown" .planning/ROADMAP.md &amp;&amp; grep -q "docs/index.md" .planning/ROADMAP.md &amp;&amp; ! sed -n '/### Phase 27/,/### Phase 28/p' .planning/ROADMAP.md | grep -qi "patch example"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP §Phase 27 title/goal/criteria reference GitHub Markdown under `docs/` + `docs/index.md` hub + `manualUrl`, not Notion.
    - The Phase 27 success-criteria list no longer contains "patch examples".
    - No edits leak into other phases' sections.
  </acceptance_criteria>
  <done>ROADMAP §Phase 27 reconciled to the GitHub-Markdown pivot, patch-examples removed, manualUrl mechanism stated.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS DOC-01/02/03 + Out of Scope</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DOC-01 line 57, DOC-02 line 58, DOC-03 line 59, Out of Scope "Custom Notion site" line 88)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Roadmap / Requirements Reconciliation — exact DOC-01/02/03 replacement wording)
    - .planning/phases/27-notion-manual/27-CONTEXT.md (D-01, D-04, D-07)
  </read_first>
  <action>
    Apply the RESEARCH §Reconciliation edits verbatim-style: DOC-01 → "authored as GitHub Markdown under `docs/` with a per-section file per table-stakes section and a `docs/index.md` hub (subpage-per-section preserved)"; DOC-02 → remove "patch examples" from the table-stakes list, keep all other sections; DOC-03 → "linked from `plugin.json` `manualUrl`; public reachability completes with the Phase 28 repo-public flip". Retarget the Out-of-Scope row "Custom Notion site / custom domain hosting" → "GitHub Pages site (MkDocs/Docusaurus) — deferred to a future docs revision; `docs/` is authored Pages-ready". Do NOT flip the DOC-01/02/03 status checkboxes (verify-work owns completion). Leave the requirements→phase status table entries as Phase 27.
  </action>
  <verify>
    <automated>grep -q "GitHub Markdown under" .planning/REQUIREMENTS.md &amp;&amp; grep -q "manualUrl" .planning/REQUIREMENTS.md &amp;&amp; ! grep -qi "published in Notion" .planning/REQUIREMENTS.md &amp;&amp; ! grep -q "Custom Notion site" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - DOC-01 references GitHub Markdown under `docs/` + `docs/index.md` hub; no "published in Notion" wording remains.
    - DOC-02 no longer lists "patch examples"; DOC-03 references `manualUrl` + Phase 28 reachability.
    - Out-of-Scope row retargeted to the deferred GitHub Pages site; DOC status checkboxes NOT flipped.
  </acceptance_criteria>
  <done>REQUIREMENTS DOC-01/02/03 + Out-of-Scope reconciled to the locked decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin.json → VCV Library / Rack UI | The manifest is consumed by the VCV Library and Rack; a malformed manifest breaks plugin load |
| manualUrl → public reader (Phase 28) | The link resolves publicly only after the Phase 28 repo-public flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering | plugin.json (manifest integrity) | mitigate | `python3 -c "json.load(...)"` validates JSON post-edit; version 2.0.0 asserted unchanged; existing URLs asserted retained |
| T-27-06 | Spoofing (link target) | manualUrl value | mitigate | manualUrl owner/repo/branch matched against the existing verified pluginUrl/sourceUrl (Photep/ForgeAudio-AnalogSeries, main) |
| T-27-07 | Information disclosure | manualUrl reachability | accept | Link 404s for the public until Phase 28 by design (sequencing constraint) — not verified live this phase |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs (manifest string + doc edits only) — N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `plugin.json` is valid JSON with `manualUrl` set to the docs-hub blob URL; version 2.0.0; prior URLs intact.
- ROADMAP §Phase 27 and REQUIREMENTS DOC-01/02/03 reflect the GitHub pivot, dropped patch-examples, and manualUrl link.
- No live public-URL check attempted (deferred to Phase 28).
</verification>

<success_criteria>
manualUrl added to a valid plugin.json (version untouched), and ROADMAP/REQUIREMENTS reconciled to D-01/D-04/D-07.
</success_criteria>

<output>
Create `.planning/phases/27-notion-manual/27-03-SUMMARY.md` when done.
</output>

---
phase: 27-notion-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/engine-concept.md
  - docs/io-reference.md
  - docs/context-menu.md
  - docs/clock-sync.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "D-01/D-02: Manual exists as GitHub Markdown under docs/ with a docs/index.md hub linking one file per table-stakes section (NOT Notion)"
    - "D-03: All cross-links use relative paths including the .md extension; no absolute repo URLs"
    - "D-05: Terse reference voice — facts stated in tables, no tutorial hand-holding"
    - "D-06: Engine-concept describes Character with generic vocabulary only; zero trademarked synth brand names"
    - "D-07: No patch-examples section exists"
    - "I/O reference CV ranges are transcribed verbatim from src/AnalogLFO.cpp (bipolar ±5V, 5V/unit; FM exponential; CLK/RESET Schmitt 1.0V/0.1V; OUTPUT ±5V)"
    - "Context-menu section lists exactly the 6 Swing options and no invented options"
    - "Clock/sync section documents the FREE/ACQUIRING/LOCKED FSM and the 15-entry ratio table (x1 = index 7)"
  artifacts:
    - path: "docs/index.md"
      provides: "Manual hub: H1 title + link list to every section file"
      contains: "ForgeAnalogLFO"
    - path: "docs/engine-concept.md"
      provides: "3-axis engine concept (morph/character/drift), generic vocabulary"
    - path: "docs/io-reference.md"
      provides: "7 inputs / 1 output with verified CV ranges"
      contains: "5V"
    - path: "docs/context-menu.md"
      provides: "Swing submenu (6 options)"
      contains: "Straight 50%"
    - path: "docs/clock-sync.md"
      provides: "FREE/ACQUIRING/LOCKED FSM + 15-entry ratio table"
      contains: "ACQUIRING"
  key_links:
    - from: "docs/index.md"
      to: "docs/io-reference.md, docs/context-menu.md, docs/clock-sync.md, docs/engine-concept.md"
      via: "relative .md links"
      pattern: "\\]\\(io-reference\\.md\\)"
---

<objective>
Create the `docs/` manual scaffold and author the four code-fact reference sections that need no screenshot: the hub (`index.md`), engine concept, I/O reference, context-menu, and clock/sync.

Purpose: Establishes the per-section GitHub-Markdown structure (D-01/D-02) that satisfies DOC-01, and transcribes the module's control/I-O/clock facts verbatim from source (DOC-02), the highest-risk drift surface (Pitfall 2).
Output: `docs/index.md` + four section files, all cross-linked with relative `.md` links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-notion-manual/27-RESEARCH.md
@.planning/phases/27-notion-manual/27-UI-SPEC.md
@.planning/phases/27-notion-manual/27-CONTEXT.md
@src/AnalogLFO.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs hub + engine-concept section</name>
  <files>docs/index.md, docs/engine-concept.md</files>
  <read_first>
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Docs Structure, §3-axis engine concept, §Section Set)
    - .planning/phases/27-notion-manual/27-UI-SPEC.md (§Copywriting Contract, §Docs Presentation Conventions)
    - .planning/phases/27-notion-manual/27-CONTEXT.md (D-01, D-02, D-03, D-05, D-06, D-07)
    - .planning/PROJECT.md (§Context — 18HP Forge Noir engine summary)
  </read_first>
  <action>
    Create `docs/index.md` as the manual hub: one `#` H1 "ForgeAnalogLFO — User Manual"; a one-line intro "Analog-modeled LFO for VCV Rack 2. Choose a section below." (D-05 terse); then a bulleted link list to every section file using relative paths WITH the `.md` extension — Engine Concept (engine-concept.md), Annotated Panel (panel.md), I/O Reference (io-reference.md), Context Menu (context-menu.md), Clock &amp; Sync (clock-sync.md), Install (install.md), Changelog (changelog.md), License &amp; Credits (license-credits.md). Do NOT include a patch-examples link (D-07).
    Create `docs/engine-concept.md`: `#` H1 title, then three `##` subsections — Morph (waveform sweep sine→triangle→saw→square→narrow pulse, PWM folded into morph, even 20%-per-shape), Character (crossfade clean/digital→analog per shape using GENERIC vocabulary only — "warm analog triangle", "vintage transistor saw", "classic PWM square"; x² progressive curve, 0.5 knob ≈ 25% effect), Drift (multi-timescale Ornstein-Uhlenbeck pitch drift + phase jitter + DC wander + thermal slew scaled by one knob; authority reduced in clocked mode). D-06: NO trademarked synth brand names (Moog/Minimoog/Roland/Juno/SH-101/Prophet/Oberheim/Korg). Terse reference voice per D-05.
  </action>
  <verify>
    <automated>test -f docs/index.md &amp;&amp; test -f docs/engine-concept.md &amp;&amp; grep -q "engine-concept.md" docs/index.md &amp;&amp; grep -q "io-reference.md" docs/index.md &amp;&amp; ! grep -qiE "moog|roland|juno|sh-101|prophet|oberheim|korg" docs/engine-concept.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/index.md` and `docs/engine-concept.md` exist.
    - `index.md` links to all 8 sections via relative `.md` paths; contains NO link matching `patch`.
    - `engine-concept.md` has three `##` axis subsections (Morph, Character, Drift).
    - Brand-name denylist grep over `engine-concept.md` returns zero hits.
  </acceptance_criteria>
  <done>Hub file links every non-patch section by relative .md path; engine-concept documents all three axes in generic vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Author I/O reference + context-menu sections</name>
  <files>docs/io-reference.md, docs/context-menu.md</files>
  <read_first>
    - src/AnalogLFO.cpp (configInput/configOutput labels L206-216; process() CV math; SWING_MENU_LABELS L83-86)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§The 7 Inputs + CV ranges, §The 1 Output, §Swing submenu)
    - .planning/phases/27-notion-manual/27-UI-SPEC.md (§Docs Presentation Conventions — every fact in a table)
  </read_first>
  <action>
    Create `docs/io-reference.md`: `#` H1 + a GitHub pipe table of the 7 inputs (Morph CV, Character CV, Drift CV, Clock, Reset, Phase Offset CV, FM) with signal type and range/behavior transcribed verbatim from src/AnalogLFO.cpp — CV convention `value = knob + trim × (CV / 5V)` clamped to [0,1], so bipolar ±5V at 5V/unit (full-scale); FM is exponential `freq ×= 2^(CV × FMdepth × depthScale)` with depthScale 0.6 free-running / 0.5 clocked; CLK/RESET are Schmitt-trigger gates rising above 1.0V / arming below 0.1V; RESET has 1ms blanking. Add a second table for the 1 output: LFO = bipolar ±5V (`5.f × sample`), 3ms cosine anti-click crossfade on phase reset. State the CV-convention formula once above the table.
    Create `docs/context-menu.md`: `#` H1 + one table listing exactly the 6 Swing submenu options — Straight 50%, Light 54%, Medium 58%, Triplet 66%, Heavy 71%, Max 75% — with fractions 0.50/0.54/0.58/0.66/0.71/0.75; note swing is active only in clocked mode (free-running stores 0.5, no warp) and persists as `swingIndex` in patch JSON. Do NOT invent any other menu options — Swing is the only context-menu item.
  </action>
  <verify>
    <automated>grep -q "5V" docs/io-reference.md &amp;&amp; grep -qE "1\.0V|1\.0 V" docs/io-reference.md &amp;&amp; grep -qE "0\.1V|0\.1 V" docs/io-reference.md &amp;&amp; for s in "Straight 50%" "Light 54%" "Medium 58%" "Triplet 66%" "Heavy 71%" "Max 75%"; do grep -qF "$s" docs/context-menu.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `io-reference.md` documents all 7 inputs + 1 output; contains `±5V`/`5V` CV convention, Schmitt `1.0V`/`0.1V`, and FM `depthScale` 0.5/0.6.
    - `context-menu.md` contains all 6 exact Swing labels above and no additional menu option headings.
    - Every fact lives in a table (D-05), not prose.
  </acceptance_criteria>
  <done>I/O and context-menu facts match src/AnalogLFO.cpp verbatim; Swing table has exactly 6 rows.</done>
</task>

<task type="auto">
  <name>Task 3: Author clock/sync section (FSM + ratio table)</name>
  <files>docs/clock-sync.md</files>
  <read_first>
    - src/AnalogLFO.cpp (RATIO_LABELS L67-70, RATIO_TABLE L49-64)
    - src/dsp/ClockTracker.hpp (FREE/ACQUIRING/LOCKED transitions, Schmitt, EMA, outlier rejection)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Clock/sync behavior, §The 15-entry RATIO_TABLE)
  </read_first>
  <action>
    Create `docs/clock-sync.md`: `#` H1 + a `##` FSM section describing FREE (no clock; Rate knob = free Hz 0.01–20; Hz readout), ACQUIRING (entered on connect+first edge; phase resets; SYNC badge blinks 2 Hz; locks via fast-track when 2nd edge is 0.8×–1.2× last period, or after ≥4 consistent edges; EMA α=0.3), LOCKED (ratio+BPM shown; per-edge SYNC flash; outliers >3× or <⅓× smoothed period discarded EXCEPT 3 consecutive outliers → drop to ACQUIRING and re-learn, BUG-01 fix), TIMEOUT→FREE (`max(1.0s, min(3×period, 5.0s))`), and instant Disconnect→FREE with cached period. Add a `##` ratio-table section: a 15-row pipe table (index, label, multiplier) transcribed verbatim — /16 (idx0) … x1 (idx7) … x16 (idx14) per RATIO_LABELS/RATIO_TABLE; state effective LFO freq = `(1/smoothedPeriod) × RATIO_TABLE[idx]` and that division-aware phase reset uses `shouldReset(ratioIdx, beatCount)` with the 3ms crossfade. Terse tables (D-05).
  </action>
  <verify>
    <automated>grep -q "ACQUIRING" docs/clock-sync.md &amp;&amp; grep -q "LOCKED" docs/clock-sync.md &amp;&amp; for r in "/16" "/1.5" "x1.5" "x16"; do grep -qF "$r" docs/clock-sync.md || exit 1; done &amp;&amp; test $(grep -cE '^\| *(/|x)[0-9]' docs/clock-sync.md) -ge 15</automated>
  </verify>
  <acceptance_criteria>
    - `clock-sync.md` names all three states FREE/ACQUIRING/LOCKED and the fast-track 0.8×–1.2× + ≥4-edge lock conditions.
    - Ratio table contains all 15 labels including `/16`, `/1.5`, `x1.5`, `x16`; x1 documented as index 7.
    - Outlier rejection (>3×/<⅓×) and the 3-consecutive-outlier re-acquire are documented.
  </acceptance_criteria>
  <done>Clock/sync FSM and 15-entry ratio table match ClockTracker.hpp and RATIO_TABLE verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public reader (Phase 28) | Committed Markdown becomes publicly readable once the repo flips public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information disclosure | docs/*.md prose | mitigate | Docs contain only public control/behavior facts transcribed from source; no secrets, tokens, or PII authored (grep review before commit) |
| T-27-02 | Tampering (legal/IP) | engine-concept.md Character prose | mitigate | Generic-vocabulary-only Character descriptors; brand-name denylist grep in Task 1 verify (full denylist enforced by Plan 02) |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (docs authoring only) — N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- All five files exist under `docs/`.
- Facts in io-reference/context-menu/clock-sync match src/AnalogLFO.cpp (CV ±5V/5V-per-unit, 6 Swing options, 15 ratios).
- `index.md` links every section by relative `.md` path; no patch-examples link.
- Brand-name denylist grep over these files returns zero hits.
</verification>

<success_criteria>
docs/ scaffold + hub + four code-fact sections exist, cross-linked with relative .md links, terse voice, zero trademarked brand names, all facts verbatim from source.
</success_criteria>

<output>
Create `.planning/phases/27-notion-manual/27-01-SUMMARY.md` when done.
</output>

---
phase: 24-dsp-extraction-display-refactors
plan: 04
type: execute
wave: 4
depends_on: [24-02, 24-03]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [CLEAN-03, CLEAN-04, CLEAN-05, CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "in-Rack visual feel is confirmed identical pre/post for animations, fades, and pill symmetry"
    - "getLastFrameDuration() is probed once and reads ~1/refreshRate (Assumption A1 confirmed), not a sub-ms render time"
    - "no audio glitches/CPU regression after moving the fill to the GUI thread"
    - "the UAT result (pass or issues) is logged to STATE.md before the phase closes"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "logged manual UAT outcome for Phase 24"
      contains: "Phase 24"
  key_links:
    - from: "manual UAT"
      to: ".planning/STATE.md"
      via: "human-gated visual sign-off recorded per D-06"
      pattern: "UAT"
---

<objective>
Human-gated verification of the deterministically-untestable parts of Phase 24 (D-06: "test what's deterministic, human-gate the visual"). The headless harness (24-01) already proves fill purity + dt/decay; this plan confirms the in-Rack *feel*: that animations are frame-rate-independent yet identical at 60fps, the ratio/BPM pills fade symmetrically with the SYNC badge, the display renders unchanged after dead-code removal, and moving the fill off the audio thread introduced no glitch. It also closes the one MEDIUM-confidence research assumption (A1) with a live probe of `getLastFrameDuration()`.

Purpose: the phase's hard success bar is "looks and feels exactly the same, just frame-rate-independent and off the audio thread" — only a human can sign that off.
Output: UAT outcome logged to STATE.md; phase ready for /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dsp-extraction-display-refactors/24-VALIDATION.md
@.planning/phases/24-dsp-extraction-display-refactors/24-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: In-Rack UAT — animation feel, pill fade symmetry, audio-thread relief, getLastFrameDuration probe</name>
  <what-built>
    Phase 24 moved the display-buffer fill off the audio thread behind a seqlock (CLEAN-05), converted all five animations to clamped wall-clock dt (CLEAN-04), made the ratio/BPM pills fade symmetrically with the SYNC badge (CLEAN-03), and removed dead code / the unreachable isStill (CLEAN-01/02). The plugin has been rebuilt against ../Rack-SDK and installed (stale-install flush per the project's vcv_build_install_workflow memory).
  </what-built>
  <how-to-verify>
    Build + install the CURRENT binary and flush any stale install first (per project memory `vcv_build_install_workflow.md`); confirm install hashes match the fresh build before auditing. Then in VCV Rack:
    1. ANIMATION FEEL (CLEAN-04): add the Analog LFO. Watch the breathe glow (~5s cycle), the SYNC ACQUIRING blink (~2Hz), the scanline scroll, and the SYNC badge per-edge flash decay. They must feel IDENTICAL to the pre-refactor build at 60fps — no faster/slower, no stutter. Then force a window stall (drag/resize, or tab away and back): on return there must be NO multi-second animation "jump" (the dt clamp caps it) and NO first-frame pop when the module is added.
    2. FRAME-RATE INDEPENDENCE (CLEAN-04 / A1): if a high-refresh display (120/144Hz) is available, confirm the breathe cycle is still ~5s (not faster). Probe `getLastFrameDuration()` ONCE (temporary log or debugger) and confirm it reads ~0.0167 at 60Hz / ~0.0069 at 144Hz — i.e. the inter-frame interval, NOT a sub-millisecond render time. If it reads sub-ms render-only time, STOP and report (fallback: derive dt from getFrameTime() deltas — research Pitfall 2 / A1).
    3. PILL FADE SYMMETRY (CLEAN-03): patch a clock into CLK, let SYNC LOCK (ratio + BPM pills visible), then DISCONNECT the clock. The ratio pill, BPM stack, and SYNC badge must all fade out together over ~200ms — NO instant pop of the ratio/BPM pills ahead of the badge. Reconnect and confirm they fade back in together.
    4. AUDIO-THREAD RELIEF (CLEAN-05): sweep morph/character knobs while listening to the output and watching the preview trace — the preview must track exactly as before with no audio glitch/zipper and no CPU-meter regression vs the pre-refactor build.
    5. DISPLAY UNCHANGED (CLEAN-01/02): confirm the waveform display, bypass dim, and all overlays render identically to before (dead-code removal must be visually invisible).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass (and log the getLastFrameDuration probe value), or describe the specific issue (e.g. "ratio pill still pops", "breathe runs fast at 144Hz", "getLastFrameDuration reads sub-ms").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Log the UAT outcome to STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (Accumulated Context > Decisions / Last activity sections — match the existing logging style)
    - .planning/phases/24-dsp-extraction-display-refactors/24-VALIDATION.md (Manual-Only Verifications table — the behaviors being signed off)
  </read_first>
  <action>
    After the checkpoint is approved, append a Phase 24 UAT entry to STATE.md in the established style: record the date, that the manual in-Rack UAT passed (or the issues found), the probed `getLastFrameDuration()` value confirming Assumption A1 (inter-frame interval, not render time), and a one-line confirmation that CLEAN-01..05 are visually/behaviorally verified feel-identical. Update `last_activity` / `Last activity` and the Session Continuity "Stopped at" to reflect Phase 24 UAT logged. Do not flip any roadmap checkboxes here — /gsd:verify-work owns phase completion.
  </action>
  <acceptance_criteria>
    - `grep -c "Phase 24" .planning/STATE.md` >= 1 and the new entry names the UAT outcome + the getLastFrameDuration probe value.
    - STATE.md `Last activity` (or `last_activity`) reflects the Phase 24 UAT.
    - No ROADMAP.md checkbox changes in this task (verify-work owns that).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Phase 24" .planning/STATE.md && grep -qi "UAT" .planning/STATE.md && grep -qi "getLastFrameDuration" .planning/STATE.md</automated>
    <human-check>STATE.md contains a dated Phase 24 UAT entry with the probe value and the pass/issues outcome.</human-check>
  </verify>
  <done>The Phase 24 manual UAT result and the getLastFrameDuration probe value are recorded in STATE.md; the phase is ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is human verification + a docs append; it introduces no code, no cross-thread data, no I/O beyond editing STATE.md, and no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Repudiation (unverified visual regression ships) | manual UAT sign-off | mitigate | Blocking human-verify checkpoint; the seqlock/dt/feel claims that headless tests cannot cover are explicitly auditioned and the outcome (incl. the A1 probe) is logged to STATE.md before the phase closes. |
| T-24-04-02 | (external) | — | accept | No network/auth/persistence/untrusted-input surface; ASVS V2-V6 not applicable. No package installs → no supply-chain row. |
</threat_model>

<verification>
- The blocking checkpoint is explicitly approved by the operator (or issues are filed for gap closure).
- STATE.md carries the dated Phase 24 UAT outcome + the getLastFrameDuration probe value (Assumption A1 closed).
- `make test` is green and the plugin builds against ../Rack-SDK (carried from 24-01..24-03).
</verification>

<success_criteria>
A human has confirmed in-Rack that animations are frame-rate-independent yet feel-identical at 60fps, the ratio/BPM pills fade symmetrically with the SYNC badge, the fill move introduced no audio glitch or CPU regression, and the display is otherwise unchanged — with the outcome and the getLastFrameDuration probe logged to STATE.md.
</success_criteria>

<output>
Create `.planning/phases/24-dsp-extraction-display-refactors/24-04-SUMMARY.md` when done.
</output>

---
phase: 28-publish-submit
plan: 02
type: execute
wave: 1
depends_on: [28-01]
files_modified: []
autonomous: false
requirements: [PUB-01]
user_setup: []

must_haves:
  truths:
    - "A fresh --mirror clone of the remote shows no trial-font object path in ANY ref (grep empty, exit 1)"
    - "Known trial-font blobs 031e8db and 3533f3e are ABSENT from remote history"
    - "The repo becomes public ONLY after the purge re-verify returned CLEAN"
    - "sourceUrl repo and manualUrl (raw docs/index.md) are reachable ANONYMOUSLY with HTTP 200"
  artifacts: []
  key_links:
    - from: "purge re-verify CLEAN result"
      to: "public visibility flip"
      via: "flip gated on the empty-grep + absent-OID output"
      pattern: "gh repo edit .* --visibility public"
    - from: "public remote"
      to: "manualUrl"
      via: "unauthenticated curl 200"
      pattern: "curl -sI"
---

<objective>
Perform the single most consequential action in the milestone: flip the repo from
PRIVATE to PUBLIC — but ONLY after a fresh-clone HARD-STOP re-verification proves the
trial-font purge (Phase 25 / IP-02) still holds on the exact remote state about to be
exposed, including the new v2.0.0 tag. Then confirm the repo and manual are genuinely
reachable without credentials.

Purpose: PUB-01 exists to guarantee history is verified clean at the moment of
exposure. Verifying AFTER the tag/push (plan 28-01) but BEFORE the flip means no
branch or tag can carry a purged blob when the repo goes public. Flipping first would
expose unverified history — the exact failure this gate prevents (RESEARCH Pitfall 4).

Output: repo visibility == PUBLIC (gated on CLEAN); anonymous 200 on sourceUrl and manualUrl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-publish-submit/28-RESEARCH.md

<facts>
- Remote: https://github.com/Photep/ForgeAudio-AnalogSeries.git (PRIVATE at plan time).
- The two trial-font blob OIDs purged in Phase 25 are 031e8db and 3533f3e
  (STATE.md 25-03 blocker entry). Their ABSENCE is a belt-and-braces check even under
  a renamed path.
- The `gh` visibility flag is CONFIRMED (2026-07-10): `gh repo edit --help` requires
  `--accept-visibility-change-consequences` whenever `--visibility` is used (resolves A2).
- IRREVERSIBILITY: you can flip back to private, but any content served while public
  may already have been cloned/cached/forked. Treat the flip as a one-way door.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HARD-STOP pre-flight — fresh mirror-clone purge re-verify</name>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (The Public-Flip Safety Gate, Step 3); .planning/STATE.md (25-03 blocker OIDs)</read_first>
  <action>
    Into a fresh `mktemp -d`, run `git clone --mirror` of the remote (mirror is required
    so `rev-list --all` sees EVERY ref — every branch and the new v2.0.0 tag — a plain
    clone would miss non-default refs). In the mirror run the primary check
    `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` — it MUST print
    nothing (grep exit 1 = CLEAN). Then the belt-and-braces check: for each OID 031e8db
    and 3533f3e, `git cat-file -e <oid>` MUST fail (blob absent). If EITHER check finds
    a match, this is a HARD STOP: do NOT proceed to the flip; escalate to a re-purge
    (out of scope for this phase) and abort. Print an explicit CLEAN / DIRTY verdict the
    operator will read at the Task 2 gate. Do not filter the grep so a header comment can
    self-satisfy it — grep the raw `rev-list --objects` path stream only.
  </action>
  <acceptance_criteria>
    `git -C <mirror> rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` prints
    nothing and returns exit 1 (CLEAN);
    `git -C <mirror> cat-file -e 031e8db` returns non-zero AND `... cat-file -e 3533f3e`
    returns non-zero (both blobs ABSENT);
    the task emits the literal verdict `CLEAN — safe to flip` on success.
  </acceptance_criteria>
  <verify>
    <automated>d=$(mktemp -d); git clone --mirror https://github.com/Photep/ForgeAudio-AnalogSeries.git "$d/m" >/dev/null 2>&1; git -C "$d/m" rev-list --all --objects | grep -iE 'Barell|FoundationLogo' && echo DIRTY-ABORT || echo CLEAN</automated>
  </verify>
  <done>Fresh mirror shows no trial-font path in any ref and both known blob OIDs absent; CLEAN verdict emitted. On DIRTY, the plan halts before Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Flip repository to PUBLIC (IRREVERSIBLE)</name>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (Step 4 — flip to public)</read_first>
  <what-built>Task 1 produced a CLEAN verdict from a fresh mirror clone: no trial-font paths in any ref, blobs 031e8db and 3533f3e absent. The flip command is confirmed as `gh repo edit Photep/ForgeAudio-AnalogSeries --visibility public --accept-visibility-change-consequences`.</what-built>
  <action>After the operator confirms Task 1's CLEAN verdict, run `gh repo edit Photep/ForgeAudio-AnalogSeries --visibility public --accept-visibility-change-consequences`, then confirm visibility == PUBLIC. Do NOT run if Task 1 returned DIRTY.</action>
  <how-to-verify>
    1. Read Task 1's output and confirm it says `CLEAN — safe to flip` (empty grep, both OIDs absent). If it says DIRTY, STOP — do not flip.
    2. Confirm you accept that this is a one-way door: content served while public may be cloned/forked/cached even if later re-privatized.
    3. On go, run: `gh repo edit Photep/ForgeAudio-AnalogSeries --visibility public --accept-visibility-change-consequences`.
    4. Confirm: `gh repo view Photep/ForgeAudio-AnalogSeries --json visibility --jq .visibility` prints `PUBLIC`.
  </how-to-verify>
  <acceptance_criteria>`gh repo view --json visibility --jq .visibility` == `PUBLIC`, AND the operator confirmed the Task 1 CLEAN verdict immediately beforehand.</acceptance_criteria>
  <resume-signal>Type "approved" after the flip returns PUBLIC, or describe the DIRTY/abort reason</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Confirm genuine anonymous public reachability</name>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (Step 5 — confirm true public reachability)</read_first>
  <action>
    Prove the repo is PUBLIC to an unauthenticated caller (an authed gh/git clone would
    succeed even while private — a false positive; RESEARCH Pitfall 5). Use unauthenticated
    curl for the repo page and the raw manual, and a token-free clone: `curl -sI` the repo
    URL and the raw manualUrl (`raw.githubusercontent.com/Photep/ForgeAudio-AnalogSeries/main/docs/index.md`);
    both MUST return 200. Optionally `GIT_TERMINAL_PROMPT=0 git clone` into a temp dir to
    prove anonymous clone works. This also closes DOC-03 (manual publicly reachable).
  </action>
  <acceptance_criteria>
    `curl -sI -o /dev/null -w '%{http_code}' https://github.com/Photep/ForgeAudio-AnalogSeries` == `200`;
    `curl -sI -o /dev/null -w '%{http_code}' https://raw.githubusercontent.com/Photep/ForgeAudio-AnalogSeries/main/docs/index.md` == `200`;
    `GIT_TERMINAL_PROMPT=0 git clone https://github.com/Photep/ForgeAudio-AnalogSeries.git <tmp>` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>curl -sI -o /dev/null -w 'manual:%{http_code}\n' https://raw.githubusercontent.com/Photep/ForgeAudio-AnalogSeries/main/docs/index.md | grep -q 'manual:200' && echo REACHABLE</automated>
  </verify>
  <done>Unauthenticated curl returns 200 for both the repo and the raw manual; anonymous clone succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PRIVATE remote → PUBLIC internet | The flip crosses this boundary once, irreversibly; all history becomes world-readable |
| authed token view → anonymous view | Reachability must be proven WITHOUT credentials to avoid a false-positive "it's public" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Information Disclosure | trial-font blob surviving in history at flip time (IP-02) | mitigate | Task 1 fresh `--mirror` clone `rev-list --all --objects` grep HARD STOP + OID-absence check; flip (Task 2) is strictly gated on the CLEAN verdict |
| T-28-05 | Information Disclosure | any other secret/token committed anywhere in history | accept | Milestone scoped IP to fonts; the same mirror `rev-list` is the last-chance sweep. No secrets found; low residual risk for an open-source GPL plugin repo |
| T-28-06 | Elevation of Privilege / Exposure | irreversible over-exposure (flip cannot un-clone) | mitigate | Human go/no-go checkpoint (Task 2) immediately after the CLEAN verdict; one-way-door acknowledged in the gate |
| T-28-07 | Spoofing | false-positive "public" from an authed clone | mitigate | Task 3 verifies with unauthenticated `curl` (200) + token-free clone, never the authed token |
</threat_model>

<verification>
- Task 1 mirror grep empty (exit 1) AND both OIDs absent before any flip.
- `gh repo view --json visibility --jq .visibility` == `PUBLIC` after the gated flip.
- Anonymous `curl -sI` returns 200 for repo URL and raw manualUrl.
</verification>

<success_criteria>
The repo is PUBLIC, flipped only after a CLEAN fresh-mirror re-verify; sourceUrl and
manualUrl return 200 to an unauthenticated client.
</success_criteria>

<output>
Create `.planning/phases/28-publish-submit/28-02-SUMMARY.md` when done — include the
CLEAN verdict transcript and the post-flip reachability HTTP codes.
</output>

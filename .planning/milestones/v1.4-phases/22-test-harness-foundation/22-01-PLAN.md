---
phase: 22-test-harness-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .gitignore
  - tests/doctest.h
  - tests/main.cpp
  - tests/test_smoke.cpp
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "`make test` compiles and runs a standalone doctest binary that exits 0 (D-01: doctest single-header framework, zero external deps)"
    - "`make`, `make dist`, `make install` behavior is unchanged (test target is purely additive)"
    - "The test target links no libRack and uses no -I$(RACK_DIR)/include"
  artifacts:
    - path: "tests/doctest.h"
      provides: "Vendored single-header doctest framework, version pinned in a top comment"
      contains: "doctest"
    - path: "tests/main.cpp"
      provides: "The ONE translation unit defining DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN"
      contains: "DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN"
    - path: "tests/test_smoke.cpp"
      provides: "A trivial passing test proving the harness runs"
      contains: "TEST_CASE"
    - path: "Makefile"
      provides: "Additive `make test` recipe appended below the plugin.mk include"
      contains: "TEST_BIN"
  key_links:
    - from: "Makefile test target"
      to: "tests/*.cpp + src/dsp/*.hpp"
      via: "$(CXX) -std=c++17 -Isrc -Itests -ffp-contract=off, NO -I$(RACK_DIR)/include"
      pattern: "build-test"
---

<objective>
Stand up the Rack-free doctest harness skeleton so the DSP core can be moved into it in later plans. This is Constraint A from research: the harness must EXIST before any DSP extraction or refactor.

Purpose: TEST-01 — a `make test` target builds and runs a standalone doctest binary without disturbing the existing plugin build. This is the load-bearing first step of the v1.4 safety net.
Output: A vendored `tests/doctest.h`, a single doctest impl TU (`tests/main.cpp`), a trivial passing smoke test, an additive `make test` recipe in the Makefile, and `build-test/` added to `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-test-harness-foundation/22-RESEARCH.md
@Makefile
@.gitignore

<notes>
- Project build workflow (memory `vcv_build_install_workflow.md`): relative `../Rack-SDK`, no worktrees, stale-install flush. The `make test` target MUST NOT perturb `make` / `make dist` / `make install` and MUST NOT link libRack or add `-I$(RACK_DIR)/include`.
- The existing Makefile is 14 lines and ends with `include $(RACK_DIR)/plugin.mk` (line 14). `plugin.mk` defines no `test` target, so appending a `TEST_`-namespaced block below line 14 is non-perturbing.
- doctest is header-only and vendored (no registry install, no submodule). Per RESEARCH.md Package Legitimacy Audit: download from the official `doctest/doctest` GitHub release tag and commit the single header; pin the version in a top-of-file comment. No checkpoint needed (single inspectable header from a well-known project).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor doctest.h and create the single-impl bootstrap TU</name>
  <files>tests/doctest.h, tests/main.cpp</files>
  <read_first>
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L87-106 (vendor instructions + Package Legitimacy Audit) and L463-469 (the single-impl-TU bootstrap)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L215-221 (main.cpp / doctest.h — no analog, template sources)
  </read_first>
  <action>
    Download the single-header doctest into `tests/doctest.h` from the official release:
    `curl -L https://raw.githubusercontent.com/doctest/doctest/v2.4.11/doctest/doctest.h -o tests/doctest.h`
    (Confirm v2.4.11 is the latest 2.4.x tag at implementation time; any 2.4.x is acceptable.) Prepend a provenance comment line at the very top of the file recording the source URL and the pinned version (e.g. `// doctest 2.4.11 — vendored from https://github.com/doctest/doctest/releases/tag/v2.4.11`). Do NOT add a submodule.
    Create `tests/main.cpp` as the ONLY translation unit that defines `DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN` then `#include "doctest.h"`. This provides `main()`. No other test TU may define that macro (Pattern 3 / anti-pattern: defining the impl macro in >1 TU produces duplicate-symbol link errors).
  </action>
  <verify>
    <automated>test -f tests/doctest.h && head -1 tests/doctest.h | grep -qi doctest && grep -q 'DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN' tests/main.cpp && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/doctest.h` exists; line 1 is a provenance comment naming the source URL + pinned version.
    - `tests/main.cpp` contains exactly one `#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN` followed by `#include "doctest.h"`.
    - No git submodule was added (`.gitmodules` unchanged / absent).
  </acceptance_criteria>
  <done>doctest.h vendored with pinned-version provenance comment; main.cpp is the sole impl TU.</done>
</task>

<task type="auto">
  <name>Task 2: Append the additive `make test` recipe and gitignore build-test/</name>
  <files>Makefile, .gitignore</files>
  <read_first>
    - Makefile (all 14 lines — the additive block goes BELOW line 14 `include $(RACK_DIR)/plugin.mk`)
    - .gitignore (existing build-artifacts section)
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L398-420 (the exact make test recipe) and L250-254 (Pitfall 1: zero rack/ include path)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L245-253 (Makefile/.gitignore extend instructions)
  </read_first>
  <action>
    Append the `make test` block from RESEARCH.md L398-420 BELOW the existing `include $(RACK_DIR)/plugin.mk` line (do not rewrite the file). Use `TEST_`-namespaced variables: `TEST_DIR := tests`, `TEST_BIN := build-test/test`, `TEST_SOURCES := $(wildcard $(TEST_DIR)/*.cpp)`, `TEST_HEADERS := $(wildcard src/dsp/*.hpp) $(wildcard $(TEST_DIR)/*.hpp)`, and `TEST_CXXFLAGS := -std=c++17 -O2 -g -Isrc -I$(TEST_DIR) -Wall -Wextra -ffp-contract=off`. CRITICAL: NO `-I$(RACK_DIR)/include`, NO `-ffast-math` (cross-platform bit stability, Pitfall 6). Add a `.PHONY: test` target that depends on `$(TEST_BIN)` and runs `./$(TEST_BIN)`; the `$(TEST_BIN)` rule `mkdir -p build-test` then `$(CXX) $(TEST_CXXFLAGS) $(TEST_SOURCES) -o $@`. `src/dsp/*.hpp` does not exist yet — `$(wildcard ...)` resolves to empty harmlessly until later plans create them.
    Add `build-test/` to `.gitignore` under the build-artifacts section. Do NOT ignore `tests/golden/*.f32` (those are tracked regression baselines, added in a later plan).
  </action>
  <verify>
    <automated>make test && make 2>&1 | tail -2 && grep -q 'build-test/' .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `make test` compiles `tests/*.cpp` and runs `build-test/test`, exiting 0 (smoke test from Task 3 passes).
    - `make` (the plugin build) still succeeds and produces the plugin binary unchanged — the test block is purely additive.
    - `grep -q 'I.*RACK_DIR.*include' Makefile` returns nothing in the appended TEST_ block (no rack include path on the test target).
    - `.gitignore` contains `build-test/`; it does NOT contain a `tests/golden` ignore rule.
  </acceptance_criteria>
  <done>`make test` and `make` both succeed; build-test/ ignored; test target is Rack-free and additive.</done>
</task>

<task type="auto">
  <name>Task 3: Add a trivial passing smoke test</name>
  <files>tests/test_smoke.cpp</files>
  <read_first>
    - tests/main.cpp (the impl TU — test_smoke.cpp must NOT redefine the impl macro)
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L211-214 (Pattern 3 — non-impl test TUs just `#include "doctest.h"`)
  </read_first>
  <action>
    Create `tests/test_smoke.cpp` with `#include "doctest.h"` (NO impl macro — only main.cpp defines it) and a single `TEST_CASE("harness smoke: 1+1==2")` that `CHECK(1 + 1 == 2)`. This proves the toolchain compiles, links a second TU against the impl TU, discovers, and runs a test. It is intentionally trivial; real invariant/golden tests land in later plans.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E 'test cases:.*passed|SUCCESS|All tests passed' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_smoke.cpp` contains `#include "doctest.h"` and exactly one `TEST_CASE`, and does NOT define `DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN`.
    - `make test` reports the smoke test passing (doctest summary shows >=1 test case passed, exit 0).
    - Linking two TUs (main.cpp + test_smoke.cpp) produces no duplicate-symbol error.
  </acceptance_criteria>
  <done>`make test` runs green with at least one passing test case across two translation units.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is build-only, offline tooling. No network at runtime, no auth, no untrusted runtime input surface. The single boundary worth noting: third-party code entering the build via the vendored `tests/doctest.h`.

| Boundary | Description |
|----------|-------------|
| upstream doctest source → repo build | A single third-party header is committed and compiled into the test binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-SC | Tampering | vendored `tests/doctest.h` (supply chain) | mitigate | Download from the official `doctest/doctest` GitHub release tag only; pin the version in a top-of-file provenance comment (source URL + version). Single inspectable header from a well-known project — no registry/opaque-dependency surface (RESEARCH.md Package Legitimacy Audit). No package-manager install occurs. |
| T-22-01 | Tampering | Makefile test target | accept | Build-only tooling, no runtime input surface. The additive target cannot affect the shipped plugin (verified: `make` unchanged). No high/medium threat. |

No high-severity threats. ASVS L1 block-threshold (high) is not reached — this is a local, offline C++ build-tooling plan with no network, auth, or untrusted runtime input.
</threat_model>

<verification>
- `make test` exits 0 and the doctest summary shows the smoke test passing.
- `make` still builds the plugin unchanged (additive proof).
- `tests/doctest.h` carries a pinned-version provenance comment; no submodule added.
- The test target uses no `-I$(RACK_DIR)/include` and links no libRack.
</verification>

<success_criteria>
- TEST-01 satisfied: a standalone `make test` doctest binary builds and runs green without touching or breaking the existing plugin build.
- The harness skeleton exists and is ready for the DSP core to be moved into it in Plan 02+.
</success_criteria>

<output>
Create `.planning/phases/22-test-harness-foundation/22-01-SUMMARY.md` when done.
</output>

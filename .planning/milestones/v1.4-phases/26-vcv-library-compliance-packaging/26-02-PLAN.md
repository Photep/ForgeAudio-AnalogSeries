---
phase: 26-vcv-library-compliance-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dsp/MathConst.hpp
  - src/dsp/Waveshape.hpp
  - src/dsp/DriftEngine.hpp
  - src/dsp/LfoCore.hpp
  - src/AnalogLFO.cpp
  - tests/test_extraction.cpp
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "No M_PI token remains anywhere under src/ or tests/"
    - "A rack-free forge::kPi constexpr exists and is included by every file that used M_PI"
    - "make test stays bit-exact green against the existing drift-on goldens on macOS (kPi is bit-identical to M_PI)"
    - "D-06: The direct-g++ compile has no undeclared-M_PI error (the Windows CI failure root cause is removed)"
    - "D-05: This removes the Windows leg's root cause of the Rack-free doctest matrix (ubuntu/macos/windows) being red — CI stays Rack-free (fetches no Rack SDK)"
  artifacts:
    - path: "src/dsp/MathConst.hpp"
      provides: "Rack-free forge::kPi constexpr replacing the non-standard M_PI macro"
      contains: "inline constexpr double kPi = 3.14159265358979323846"
  key_links:
    - from: "src/dsp/Waveshape.hpp / DriftEngine.hpp / LfoCore.hpp / AnalogLFO.cpp / tests/test_extraction.cpp"
      to: "src/dsp/MathConst.hpp"
      via: "#include \"dsp/MathConst.hpp\" + forge::kPi usage"
      pattern: "forge::kPi"
---

<objective>
Remove the non-standard `M_PI` macro from the DSP core and its consumers, replacing all 22 uses across
5 files with a shared rack-free `forge::kPi` constexpr (D-06). This eliminates the Windows CI test-leg
compile failure (`M_PI` undeclared under direct g++ without `_USE_MATH_DEFINES`) without any build-flag
reliance or Rack dependency.

Purpose: One of the two root causes keeping the 3-OS CI matrix red (TEST-06). The kPi literal is chosen
to be bit-for-bit IEEE-754 identical to `M_PI` (`0x400921FB54442D18`) so the golden fixtures are NOT
perturbed — this is a portability refactor with ZERO shipped-audio change.
Output: `src/dsp/MathConst.hpp` plus 5 mechanically-edited files, `make test` still bit-exact green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vcv-library-compliance-packaging/26-CONTEXT.md
@.planning/phases/26-vcv-library-compliance-packaging/26-RESEARCH.md
@.planning/phases/26-vcv-library-compliance-packaging/26-PATTERNS.md

<interfaces>
<!-- M_PI occurrence map (22 sites, 5 files) — from PATTERNS.md §M_PI→forge::kPi and grep -->
src/dsp/Waveshape.hpp   : 4 sites (lines 40, 72, 75, 92)   include block L17-18 (<cmath>/<algorithm>)
src/dsp/DriftEngine.hpp : 5 sites (lines 66, 70, 74, 78, 83) include block L25-27
                          NOTE: this file ALSO holds the D-07 std::normal_distribution spread landmine
                          (L46, L98) — do NOT touch that; the kPi edit is orthogonal (owned by plan 26-03).
src/dsp/LfoCore.hpp     : 1 site  (line 229)                include block L22-24
src/AnalogLFO.cpp       : 11 sites (lines 421-426, 981, 1043-1075) include block L1-10; already includes "dsp/LfoCore.hpp"
tests/test_extraction.cpp: 1 site (line 164)                already resolves dsp/ via -Isrc

Two site shapes only (scaffolding UNCHANGED — only the token M_PI changes):
  float-cast scaling : `2.f * (float)M_PI`   ->  `2.f * (float)forge::kPi`
  bare-double angle  : `-0.75 * M_PI`        ->  `-0.75 * forge::kPi`

Header hygiene (every src/dsp/*.hpp): `#pragma once`, `// src/dsp/<Name>.hpp` banner,
`namespace forge { ... }`, ZERO Rack-SDK includes. Use `inline constexpr` (C++17, ODR-safe).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/dsp/MathConst.hpp with forge::kPi</name>
  <files>src/dsp/MathConst.hpp</files>
  <read_first>
    - src/dsp/Swing.hpp (the exact rack-free constexpr-only header skeleton to copy — pragma once + banner + namespace forge)
    - .planning/phases/26-vcv-library-compliance-packaging/26-PATTERNS.md (§MathConst.hpp — concrete content + convention notes, lines 27-69)
    - .planning/phases/26-vcv-library-compliance-packaging/26-CONTEXT.md (D-06 — exact literal and bit-identity rationale)
  </read_first>
  <action>
    Create src/dsp/MathConst.hpp following the src/dsp/Swing.hpp skeleton: `#pragma once`, a
    `// src/dsp/MathConst.hpp` banner, the "ZERO Rack-SDK includes" hygiene comment, and a
    `namespace forge { ... }` wrapper. Inside the namespace declare exactly
    `inline constexpr double kPi = 3.14159265358979323846;` with a comment noting it is bit-for-bit
    IEEE-754 identical to cmath's pi macro (hex identity `0x400921FB54442D18`) so golden fixtures are
    unperturbed. CRITICAL: the header comment MUST NOT contain the literal cmath pi macro token — Task 2's
    grep gate (`grep -rn M_PI src/ tests/` must return zero) scans this same header, so refer to the value
    only via the hex identity `0x400921FB54442D18` and the phrase "cmath's pi macro", never the raw macro
    name. Copy the reworded comment wording verbatim from PATTERNS.md §MathConst.hpp concrete content.
    Include NOTHING (no <cmath>, no Rack headers) — this is a pure constant header.
  </action>
  <verify>
    <automated>grep -q 'inline constexpr double kPi = 3.14159265358979323846' src/dsp/MathConst.hpp && ! grep -q '#include' src/dsp/MathConst.hpp && ! grep -q 'M_PI' src/dsp/MathConst.hpp</automated>
  </verify>
  <acceptance_criteria>
    - src/dsp/MathConst.hpp exists, opens with `#pragma once`, wraps content in `namespace forge`
    - Declares `inline constexpr double kPi = 3.14159265358979323846;` (exact literal)
    - Contains ZERO `#include` directives (rack-free, dependency-free)
    - Contains ZERO `M_PI` tokens (comment refers to the value via the hex identity + "cmath's pi macro"), so Task 2's grep gate stays absolute
  </acceptance_criteria>
  <done>The kPi constexpr header exists, rack-free and ODR-safe, with no M_PI token in its comment, matching the src/dsp/*.hpp convention.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 22 M_PI uses with forge::kPi across 5 files</name>
  <files>src/dsp/Waveshape.hpp, src/dsp/DriftEngine.hpp, src/dsp/LfoCore.hpp, src/AnalogLFO.cpp, tests/test_extraction.cpp</files>
  <read_first>
    - src/dsp/Waveshape.hpp, src/dsp/DriftEngine.hpp, src/dsp/LfoCore.hpp, src/AnalogLFO.cpp, tests/test_extraction.cpp (the 5 files being edited)
    - src/dsp/MathConst.hpp (the header just created — the include target)
    - .planning/phases/26-vcv-library-compliance-packaging/26-PATTERNS.md (§M_PI→forge::kPi — per-file site list + include-block locations, lines 73-120)
  </read_first>
  <action>
    In each of the 5 files add `#include "dsp/MathConst.hpp"` to the existing include block, then replace
    every `M_PI` token with `forge::kPi`, leaving ALL surrounding scaffolding (`(float)`, `2.f *`,
    `-0.75 *`, comments) byte-identical — only the token changes. Sites: Waveshape.hpp (40,72,75,92),
    DriftEngine.hpp (66,70,74,78,83), LfoCore.hpp (229), AnalogLFO.cpp (421-426, 981, 1043-1075, 11 sites),
    test_extraction.cpp (164). In DriftEngine.hpp do NOT touch the std::normal_distribution at L46/L98 —
    that is the plan 26-03 landmine, orthogonal to this edit. After editing, `grep -rn M_PI src/ tests/`
    MUST return zero hits (MathConst.hpp already contains no M_PI token, so this invariant is absolute).
    Rebuild and run `make test` on macOS — it MUST stay bit-exact green against the
    existing freerun_*.f32 goldens (kPi rounds to the same double as M_PI, so no golden is perturbed).
  </action>
  <verify>
    <automated>test "$(grep -rc 'M_PI' src/ tests/ | grep -v ':0' | wc -l | tr -d ' ')" = "0" && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn M_PI src/ tests/` returns zero hits (no M_PI token anywhere, MathConst.hpp included)
    - Each of the 5 files `#include "dsp/MathConst.hpp"` and uses `forge::kPi`
    - `make test` exits 0 on macOS with the existing drift-on goldens still bit-exact (no golden regeneration)
    - The std::normal_distribution lines in DriftEngine.hpp (L46/L98) are unchanged
  </acceptance_criteria>
  <done>All 22 M_PI uses are forge::kPi; grep M_PI is empty; make test is bit-exact green; no DSP behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DSP source → both plugin build and Rack-free test build | A shared constexpr header is compiled by two independent toolchains (SDK plugin build + direct-g++ CI test leg) |
| local commit → GitHub Actions CI | Pushed source triggers the 3-OS matrix under the default runner token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | golden-fixture integrity (kPi vs M_PI numeric drift) | mitigate | kPi literal chosen bit-for-bit IEEE-754 identical to M_PI (`0x400921FB54442D18`); acceptance gate requires `make test` bit-exact green against existing goldens — any perturbation fails the build |
| T-26-CI | Elevation of Privilege | GitHub Actions workflow token scope | accept | Rack-free CI only checks out, builds, and runs the doctest binary; references no secrets and needs no elevated permissions — default read-only GITHUB_TOKEN suffices |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; toolchain is OS/SDK-provided — no supply-chain surface, no slopcheck required |
</threat_model>

<verification>
- `grep -rn M_PI src/ tests/` returns empty
- `make test` exits 0 on macOS, bit-exact against existing freerun_*.f32 goldens
- src/dsp/MathConst.hpp is rack-free (no #include) and declares the exact kPi literal
</verification>

<success_criteria>
The non-standard M_PI macro is gone tree-wide, replaced by a rack-free forge::kPi constexpr; the Windows
CI compile root cause is removed; the DSP output is bit-identical (goldens unperturbed). Contributes the
D-06 half of TEST-06.
</success_criteria>

<output>
Create `.planning/phases/26-vcv-library-compliance-packaging/26-02-SUMMARY.md` when done.
</output>
</output>

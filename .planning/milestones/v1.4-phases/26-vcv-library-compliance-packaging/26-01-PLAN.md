---
phase: 26-vcv-library-compliance-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin.json
autonomous: true
requirements: [PKG-02, PKG-03]
must_haves:
  truths:
    - "D-01: plugin.json authorUrl, pluginUrl, sourceUrl are all populated with the GitHub repo URL"
    - "D-01: All three manifest URLs return HTTP 200 (reachable)"
    - "D-02: minRackVersion is 2.0.0 and no Rack 2.1-2.6-only API is relied on"
    - "D-03: version stays 2.0.0; Makefile VERSION derives to 2.0.0 via jq"
    - "D-04: Both module tags are on the VCV whitelist; slug/module-slug are charset-valid and unchanged; no trademarked strings in the manifest"
  artifacts:
    - path: "plugin.json"
      provides: "Submission-ready manifest with populated URLs and lowered minRackVersion"
      contains: "https://github.com/Photep/ForgeAudio-AnalogSeries"
  key_links:
    - from: "plugin.json version"
      to: "Makefile VERSION"
      via: "plugin.mk:6 jq -r .version derivation"
      pattern: "jq -r .version plugin.json"
---

<objective>
Validate and finalize `plugin.json` for VCV Library submission (PKG-02, PKG-03). Populate the three
manifest URLs, lower `minRackVersion` to the widest-compatible floor, and confirm every field-level
submission rule (version rule, VERSION derivation, tag whitelist, slug charset/immutability, no
trademarked strings, brand/slug collision).

Purpose: The VCV Library builds from the manifest; every field must be correct and permanent before
submission. This is the cheap, no-build workstream — do it first.
Output: An edited, validated `plugin.json` whose URLs are reachable and whose fields are submission-locked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vcv-library-compliance-packaging/26-CONTEXT.md
@.planning/phases/26-vcv-library-compliance-packaging/26-RESEARCH.md
@.planning/phases/26-vcv-library-compliance-packaging/26-PATTERNS.md

<interfaces>
<!-- Current plugin.json state (executor edits fields in place — do NOT restructure). -->
plugin.json current values:
  "slug": "ForgeAudio-AnalogSeries"        (D-04 FINAL — do not change)
  "version": "2.0.0"                        (D-03 — keep; MAJOR = Rack major = 2)
  "minRackVersion": "2.6.0"                 (D-02 — lower to "2.0.0")
  "authorUrl": ""                           (D-01 — populate)
  "pluginUrl": ""                           (D-01 — populate)
  "sourceUrl": ""                           (D-01 — populate)
  modules[0].slug: "ForgeAnalogLFO"         (D-04 FINAL — do not change)
  modules[0].tags: ["Low-frequency oscillator", "Waveshaper"]  (both whitelist-valid)

Makefile VERSION is NOT a Makefile line — it is derived in ../Rack-SDK/plugin.mk:6 via
`VERSION := $(shell jq -r .version plugin.json)`. VERIFY the derivation; do NOT add a Makefile VERSION line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate manifest URLs and lower minRackVersion</name>
  <files>plugin.json</files>
  <read_first>
    - plugin.json (the manifest being edited)
    - .planning/phases/26-vcv-library-compliance-packaging/26-CONTEXT.md (D-01, D-02, D-03, D-04 locked values)
    - .planning/phases/26-vcv-library-compliance-packaging/26-PATTERNS.md (§plugin.json — field-level edit spec, lines 257-271)
  </read_first>
  <action>
    Edit plugin.json in place. Set authorUrl, pluginUrl, and sourceUrl all to the exact string
    `https://github.com/Photep/ForgeAudio-AnalogSeries` (per D-01 — VCV permits identical URLs).
    Change minRackVersion from `2.6.0` to `2.0.0` (per D-02). Leave slug (`ForgeAudio-AnalogSeries`),
    version (`2.0.0`), module slug (`ForgeAnalogLFO`), module name (`Analog LFO`), and both tags
    UNCHANGED — they are final per D-03/D-04. Do NOT reformat or reorder unrelated fields. After the
    edit, confirm the file is still valid JSON.
  </action>
  <verify>
    <automated>jq -e '.authorUrl == "https://github.com/Photep/ForgeAudio-AnalogSeries" and .pluginUrl == "https://github.com/Photep/ForgeAudio-AnalogSeries" and .sourceUrl == "https://github.com/Photep/ForgeAudio-AnalogSeries" and .minRackVersion == "2.0.0" and .version == "2.0.0" and .slug == "ForgeAudio-AnalogSeries"' plugin.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq . plugin.json` parses without error (valid JSON)
    - The three URL fields all equal `https://github.com/Photep/ForgeAudio-AnalogSeries`
    - `jq -r .minRackVersion plugin.json` prints `2.0.0`
    - `jq -r .version plugin.json` still prints `2.0.0`; slug and module slug unchanged
  </acceptance_criteria>
  <done>plugin.json has populated URLs and minRackVersion 2.0.0; version/slug untouched; file is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Field-by-field manifest validation (PKG-03) + URL reachability (PKG-02)</name>
  <files>plugin.json</files>
  <read_first>
    - plugin.json (validated state after Task 1)
    - Makefile (confirm no VERSION line; VERSION is jq-derived in plugin.mk)
    - .planning/phases/26-vcv-library-compliance-packaging/26-RESEARCH.md (§Common Pitfalls 2/3, §phase_requirements PKG-03 — tag whitelist, slug charset, minRackVersion enforcement, VERSION auto-derivation)
    - src/AnalogLFO.cpp (Rack API usage — for the 2.1-2.6-only symbol grep)
  </read_first>
  <action>
    Run five field-level validations against the edited plugin.json and record the outcomes in the
    SUMMARY:
    (1) minRackVersion floor (D-02): grep src/ for Rack API symbols and confirm the only
    Rack-version-sensitive symbols are the Rack-2.0-era `getLastFrameDuration` and
    `createIndexSubmenuItem` (their presence is expected and fine). If any 2.1-2.6-only symbol surfaces,
    raise the floor and note it; otherwise 2.0.0 stands.
    (2) VERSION derivation (D-03): confirm `jq -r .version plugin.json` yields `2.0.0` and that
    ../Rack-SDK/plugin.mk derives VERSION from it (there is NO Makefile VERSION line to edit — verify only).
    (3) Tag whitelist: confirm both tags `Low-frequency oscillator` and `Waveshaper` are the canonical
    VCV whitelist spellings.
    (4) Slug/brand collision (D-04): check the plugin slug `ForgeAudio-AnalogSeries` and module slug
    `ForgeAnalogLFO` against the live VCV Library plugin list; flag ONLY on a real conflict. Confirm both
    slugs use only the allowed charset `[A-Za-z0-9_-]`.
    (5) No trademarked strings: scan name/brand/description/module fields for trademarked synth/brand names.
    Then verify PKG-02 reachability: each of the three manifest URLs returns HTTP 200.
  </action>
  <verify>
    <automated>test "$(jq -r .version plugin.json)" = "2.0.0" && jq -r '.slug, .modules[0].slug' plugin.json | grep -Eqx '[A-Za-z0-9_-]+' && grep -REn 'getLastFrameDuration|createIndexSubmenuItem' src/ >/dev/null && curl -sSfI https://github.com/Photep/ForgeAudio-AnalogSeries -o /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r .version plugin.json` == `2.0.0` and no Makefile VERSION line was added (VERSION stays jq-derived)
    - Grep confirms the Rack-version-sensitive symbols in src/ are the Rack-2.0-era `getLastFrameDuration` and `createIndexSubmenuItem` (their presence is expected); no 2.1-2.6-only API is relied on, so the minRackVersion 2.0.0 floor holds — documented in the SUMMARY
    - Both tags confirmed as canonical VCV whitelist spellings; both slugs pass the `[A-Za-z0-9_-]` charset check
    - Brand/slug collision check run against the live library; result (no-conflict or flagged) recorded in the SUMMARY
    - `curl -sSfI` on the three (identical) manifest URLs returns HTTP 200
  </acceptance_criteria>
  <done>All five PKG-03 field validations pass (or are documented), URLs are reachable, and the SUMMARY records each outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin.json → VCV Library build server | Hand-authored manifest consumed by an external build/submission pipeline |
| repo string fields → published metadata | Manifest name/brand/description become public, permanent identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | plugin.json module name/tags/description | mitigate | Field-by-field PKG-03 review; tags validated against VCV whitelist; scan for trademarked synth/brand strings (VCV Ethics Guidelines — no brand cloning) |
| T-26-02 | Spoofing | manifest URLs | mitigate | All three URLs point to the operator-owned GitHub repo; reachability confirmed via `curl -sSfI` HTTP 200 |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; all tooling is OS-provided (jq/curl) or SDK-provided — no supply-chain surface, no slopcheck required |
</threat_model>

<verification>
- `jq . plugin.json` is valid; the three URLs are populated and reachable (HTTP 200)
- minRackVersion 2.0.0 confirmed against a src/ API grep; version 2.0.0 unchanged; VERSION derivation verified (no Makefile edit)
- Tags whitelist-valid, slugs charset-valid and collision-checked, no trademarked strings
</verification>

<success_criteria>
plugin.json is submission-ready: URLs populated + reachable (PKG-02); version/slug/tags/minRackVersion
all validated field-by-field with no trademarked strings (PKG-03).
</success_criteria>

<output>
Create `.planning/phases/26-vcv-library-compliance-packaging/26-01-SUMMARY.md` when done.
</output>
</output>

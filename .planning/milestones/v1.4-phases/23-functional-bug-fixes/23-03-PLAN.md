---
phase: 23-functional-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dsp_units.cpp
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "Waveshape output ranges are covered across the full morph x character x phase grid"
    - "The clock ratio/alignment table (RATIO_TABLE + shouldReset cadence) is covered at all 15 ratios"
    - "Swing math (swingPhaseMultiplier + SWING_FRACTIONS edges) is covered, including the isClocked=false gate == 1.0"
    - "Consecutive-outlier clock recovery is covered (lives in test_dsp_stateful.cpp via plan 23-01)"
  artifacts:
    - path: "tests/test_dsp_units.cpp"
      provides: "Extended TEST-03 coverage: waveshape range grid, full ratio/alignment cadence, swing-math edges"
      contains: "TEST-03"
  key_links:
    - from: "tests/test_dsp_units.cpp"
      to: "forge::shouldReset / forge::swingPhaseMultiplier / Waveshape"
      via: "direct pure-function CHECK asserts"
      pattern: "forge::shouldReset|swingPhaseMultiplier"
---

<objective>
Fix TEST-03: extend the unit suite so it covers waveshaping output ranges, the clock
ratio/alignment table, consecutive-outlier clock recovery, and swing math. Phase 22 left the
Waveshape coverage in `test_dsp_units.cpp` as a SCAFFOLD (a morph×character grid + segment-
boundary identity); this plan widens it to the full grid and adds the full per-ratio alignment
cadence and swing-math edge cases.

Note on scope split: TEST-03's "consecutive-outlier clock recovery" coverage is delivered by the
BUG-01 cases in `test_dsp_stateful.cpp` (plan 23-01) — that is the correct home for stateful FSM
tests. This plan covers the THREE pure/table behaviors that live in `test_dsp_units.cpp`. The
ratio/alignment cadence asserted here is the CURRENT behavior at the 13 unchanged ratios; the two
audition-gated cells (idx 6 /1.5, idx 8 x1.5) are pinned by plan 23-05's regression after the
audition decision, NOT here.

Purpose: SC5 — TEST-03 coverage across waveshape ranges, ratio/alignment table, and swing math.
Output: extended `test_dsp_units.cpp`, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-functional-bug-fixes/23-RESEARCH.md
@.planning/phases/23-functional-bug-fixes/23-PATTERNS.md

<interfaces>
<!-- Pure functions/tables under test. Extracted from the live core. -->

src/dsp/RatioTable.hpp:
  static constexpr float RATIO_TABLE[15];        // /16../2, /1.5, x1, x1.5, x2..x16
  static constexpr const char* RATIO_LABELS[15];
  inline bool shouldReset(int ratioIdx, int beatCount);   // CURRENT: ratios<1 reset every round(1/ratio) beats; >=1 every beat; idx<0 always

src/dsp/Swing.hpp:
  static constexpr float SWING_FRACTIONS[6];     // index 0 = Straight = 0.5
  inline double swingPhaseMultiplier(double phase, float swingFrac, bool isClocked);  // free-run (isClocked=false) returns 1.0

src/dsp/Waveshape.hpp: the morphed-wave shape function the scaffold at test_dsp_units.cpp L101-124
  already exercises (nested morph×character×phase grid; pre-scale bounds [-1.15,1.15]).
  NOTE: strict post-scale ±5V bounds live in test_invariants.cpp — do NOT duplicate them here.

tests/test_dsp_units.cpp existing patterns to extend:
  L101-124 Waveshape range-sweep grid; L169-185 `shouldReset` table block (CHECK_FALSE(...n-1)/CHECK(...n)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen waveshape output-range coverage + swing-math edges</name>
  <files>tests/test_dsp_units.cpp</files>
  <read_first>
    - tests/test_dsp_units.cpp (L1-30 header; L101-124 Waveshape grid scaffold; the Swing block — grep `swingPhaseMultiplier`)
    - .planning/phases/23-functional-bug-fixes/23-PATTERNS.md ("tests/test_dsp_units.cpp (MOD)" L138-154 — extend the range-sweep + shouldReset/swing patterns; bounds note: pre-scale here, post-scale ±5V in test_invariants.cpp)
    - .planning/phases/23-functional-bug-fixes/23-RESEARCH.md (Wave 0 Gaps L400 — full waveshape grid, swing-math edges; Float-comparison discipline)
    - src/dsp/Swing.hpp (SWING_FRACTIONS table + swingPhaseMultiplier semantics)
  </read_first>
  <action>Extend the Waveshape range-sweep (L101-124) to the FULL grid: sweep morph and character across their full [0,1] ranges at a fine step and phase across [0,1), asserting each output stays within the documented pre-scale bounds (`v >= lo && v <= hi`, the scaffold's [-1.15,1.15] band). Do NOT duplicate the strict post-scale ±5V invariant (it lives in test_invariants.cpp). Add a swing-math TEST_CASE covering: `swingPhaseMultiplier(phase, swingFrac, /*isClocked*/false) == doctest::Approx(1.0)` for several phases and every SWING_FRACTIONS[0..5] (the free-run gate); and the clocked warp at the Straight edge (`SWING_FRACTIONS[0]==0.5f` ⇒ multiplier ≈ 1.0 across the cycle) plus a Heavy-swing value showing the multiplier deviates from 1.0 within a beat. Use `doctest::Approx`/inequality bands for all float compares (never == on warp output).</action>
  <verify>
    <automated>make test 2>&1 | tail -6 ; grep -c "swingPhaseMultiplier" tests/test_dsp_units.cpp</automated>
  </verify>
  <done>test_dsp_units.cpp asserts waveshape outputs stay in-band across the full morph×character×phase grid and covers swing math (free-run gate == 1.0 for all 6 fractions; Straight and a Heavy clocked case); `make test` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Full ratio/alignment-table coverage (13 unchanged ratios)</name>
  <files>tests/test_dsp_units.cpp</files>
  <read_first>
    - tests/test_dsp_units.cpp (L169-185 existing `shouldReset` table block — the CHECK_FALSE(...n-1)/CHECK(...n) boundary style)
    - .planning/phases/23-functional-bug-fixes/23-PATTERNS.md (L144-154 — extend the shouldReset block with per-ratio cadence cases)
    - src/dsp/RatioTable.hpp (RATIO_TABLE + the current `shouldReset` divisor math at L53-61)
  </read_first>
  <action>Extend the `shouldReset` table coverage to assert the CURRENT reset cadence at all 15 indices using the boundary style: for each division ratio (idx 0-5) assert no reset before `round(1/RATIO_TABLE[idx])` beats and a reset exactly at it (e.g. idx0 /16: CHECK_FALSE at 15, CHECK at 16; idx5 /2: CHECK_FALSE at 1, CHECK at 2); for each multiply ratio (idx 7, 9-14, x1..x16 integer) assert reset every beat; assert idx<0 always resets. Do NOT assert idx 6 (/1.5) or idx 8 (x1.5) cadence here — those are audition-gated and pinned by plan 23-05 after the STATE.md decision; asserting them now would conflict with whatever the audition decides. Add a comment marking idx 6 / idx 8 as "audition-gated — pinned in test_regression.cpp (plan 23-05)".</action>
  <verify>
    <automated>make test 2>&1 | tail -6 ; grep -c "shouldReset" tests/test_dsp_units.cpp</automated>
  </verify>
  <done>The 13 non-audition-gated ratios have full boundary-cadence assertions over `shouldReset`; idx 6 and idx 8 are explicitly excluded with a comment pointing to plan 23-05; `make test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | TEST-03 is test-only coverage over pure DSP functions. No untrusted input, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-NA | — | test_dsp_units.cpp | accept | No new threats (test-only, pure DSP). Adds coverage; changes no shipped code path. |
</threat_model>

<verification>
- `make test` exits 0 with the new waveshape-grid, swing-math, and ratio-cadence cases green.
- `make RACK_DIR=../Rack-SDK` still builds the plugin (wave-merge check).
- idx 6 / idx 8 cadence is NOT asserted in this TU (left to the audition-gated plan 23-05).
</verification>

<success_criteria>
TEST-03's pure/table coverage is delivered: waveshape output ranges (full grid), the
ratio/alignment table (13 unchanged ratios), and swing math (free-run gate + clocked edges).
Combined with plan 23-01's consecutive-outlier recovery coverage in test_dsp_stateful.cpp and
plan 23-05's idx6/idx8 cadence pin, TEST-03 is fully satisfied at phase close.
</success_criteria>

<output>
Create `.planning/phases/23-functional-bug-fixes/23-03-SUMMARY.md` when done. Note that the
idx 6 / idx 8 cadence is deferred to plan 23-05 (audition-gated) and that consecutive-outlier
recovery coverage is in plan 23-01.
</output>

---
phase: 23-functional-bug-fixes
plan: 05
type: execute
wave: 3
depends_on: ["23-04"]
files_modified:
  - src/dsp/RatioTable.hpp
  - tests/test_regression.cpp
autonomous: true
requirements: [BUG-02, TEST-05]
must_haves:
  truths:
    - "The BUG-02 regression EXPECTED[15] array is set from the logged STATE.md audition decision"
    - "shouldReset resets exactly on the per-ratio boundary and never mid-cycle, for all 15 ratios, per the decided cadence"
    - "If adopt-table: idx 6 (/1.5) resets every 3 beats and idx 8 (x1.5) every 2 — no mid-cycle truncation; the 13 other ratios are unchanged"
    - "If keep-current: shouldReset is unchanged and the test pins the current cadence"
    - "BUG-02 regression goes RED on the pre-change header (adopt-table) and GREEN after (TEST-05)"
  artifacts:
    - path: "tests/test_regression.cpp"
      provides: "BUG-02 ratio-alignment cadence regression reading the decided EXPECTED[15]"
      contains: "BUG-02"
    - path: "src/dsp/RatioTable.hpp"
      provides: "BEATS_PER_ALIGN[15] table swap (only if audition adopted the table)"
      contains: "BEATS_PER_ALIGN"
  key_links:
    - from: ".planning/STATE.md ### Decisions audition entry"
      to: "tests/test_regression.cpp EXPECTED[15] + RatioTable.hpp edit decision"
      via: "the logged keep-current | adopt-table decision selects the cadence pinned/applied"
      pattern: "DECISION: (keep-current|adopt-table)"
    - from: "src/dsp/ClockTracker.hpp L172"
      to: "forge::shouldReset"
      via: "single-home delegation (CR-03) — table swap lives only in RatioTable.hpp"
      pattern: "forge::shouldReset"
---

<objective>
Fix BUG-02: apply the x1.5/÷1.5 alignment change per the logged audition decision and pin it with
a deterministic reset-cadence regression. This plan is GATED — it depends on plan 23-04 having
written a `- [Phase 23]: x1.5/÷1.5 audition — DECISION: ...` entry to STATE.md `### Decisions`.
The executor reads that entry first and branches:

- **adopt-table:** add `static constexpr int BEATS_PER_ALIGN[15]` to RatioTable.hpp and rewrite
  `shouldReset` to read it uniformly (the `< 1.f` guard goes away; non-integer ratios x1.5/÷1.5
  are handled too). This is a TWO-CELL behavioral change — only idx 6 (/1.5: 2→3) and idx 8
  (x1.5: 1→2) differ; the other 13 ratios are bit-identical. The signature `bool shouldReset(int,int)`
  is FROZEN (ClockTracker.hpp:172 delegates to it — CR-03). Goldens are free-run only, `shouldReset`
  is unreached on them → NO golden regeneration.
- **keep-current:** leave RatioTable.hpp unchanged; the regression pins the CURRENT cadence.

The regression's `EXPECTED[15]` array is the single named constant set per the decision, so the
test proves *whatever was decided* is what `shouldReset` does — valid under EITHER outcome.

Purpose: SC4 — x1.5/÷1.5 align per the auditioned decision, pinned by a deterministic regression.
Output: (conditionally) edited RatioTable.hpp; extended test_regression.cpp; red→green if adopt-table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-functional-bug-fixes/23-RESEARCH.md
@.planning/phases/23-functional-bug-fixes/23-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live core. -->

src/dsp/RatioTable.hpp — current shouldReset (L53-61):
  inline bool shouldReset(int ratioIdx, int beatCount) {
    bool reset = true;
    if (ratioIdx >= 0 && RATIO_TABLE[ratioIdx] < 1.f) {
      // FUTURE (P23): replace with `int divisor = BEATS_PER_ALIGN[ratioIdx];`
      int divisor = (int)std::round(1.f / RATIO_TABLE[ratioIdx]);
      reset = (beatCount >= divisor);
    }
    return reset;
  }
  Post-fix (adopt-table) shape: add BEATS_PER_ALIGN next to RATIO_TABLE and read it uniformly:
    static constexpr int BEATS_PER_ALIGN[15] =
      { 16, 8, 6, 4, 3, 2,  3,  1,  2,  1, 1, 1, 1, 1, 1 };
      //  /16 /8 /6 /4 /3 /2 /1.5 x1 x1.5 x2 x3 x4 x6 x8 x16
    inline bool shouldReset(int ratioIdx, int beatCount) {
      if (ratioIdx < 0) return true;                  // unlocked: every beat (unchanged)
      return beatCount >= BEATS_PER_ALIGN[ratioIdx];
    }
  ONLY idx 6 (2→3) and idx 8 (1→2) change vs current; all 13 others identical. Signature FROZEN.

  CURRENT cadence (keep-current EXPECTED[15]):  {16,8,6,4,3,2, 2, 1, 1, 1,1,1,1,1,1}
  ADOPT-TABLE cadence (adopt EXPECTED[15]):     {16,8,6,4,3,2, 3, 1, 2, 1,1,1,1,1,1}
  (Differences are ONLY at idx 6 and idx 8.)

tests/test_regression.cpp (already exists from plan 23-02; ADD the BUG-02 case beside the BUG-04 one):
  already includes "doctest.h" and "dsp/PatchParse.hpp"; ADD `#include "dsp/RatioTable.hpp"`.

.planning/STATE.md ### Decisions: read the `- [Phase 23]: x1.5/÷1.5 audition — DECISION: ...` line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the audition decision and write the BUG-02 cadence regression (RED if adopt-table)</name>
  <files>tests/test_regression.cpp</files>
  <read_first>
    - .planning/STATE.md (### Decisions — find the `- [Phase 23]: x1.5/÷1.5 audition — DECISION:` line; if it is ABSENT, STOP: the audition gate (plan 23-04) has not run and this plan must not proceed)
    - tests/test_regression.cpp (current contents — the BUG-04 case + TU boilerplate from plan 23-02; do not disturb it)
    - .planning/phases/23-functional-bug-fixes/23-PATTERNS.md ("BUG-02 TEST_CASE" L97-110 — the boundary-sweep over EXPECTED[15], gated by the audition)
    - .planning/phases/23-functional-bug-fixes/23-RESEARCH.md (Code Examples L258-273; Pitfall 7 L250 — test must stay valid under EITHER outcome)
  </read_first>
  <behavior>
    - Test "BUG-02: ratio alignment cadence (per logged audition decision)":
      a `static const int EXPECTED[15]` set per the STATE.md decision —
        adopt-table → {16,8,6,4,3,2, 3, 1, 2, 1,1,1,1,1,1}
        keep-current → {16,8,6,4,3,2, 2, 1, 1, 1,1,1,1,1,1}
      for each idx 0..14: for b in 1..period-1 CHECK_FALSE(forge::shouldReset(idx,b)) (no mid-cycle reset);
      CHECK(forge::shouldReset(idx, period)) (resets exactly on the boundary).
  </behavior>
  <action>Add `#include "dsp/RatioTable.hpp"` to tests/test_regression.cpp. Add the BUG-02 TEST_CASE with the `EXPECTED[15]` array filled from the logged STATE.md decision (use the adopt-table array if the decision is adopt-table, the keep-current array if keep-current). Add an in-file comment quoting the STATE.md decision line so the chosen array is traceable. Then run `make test`: if the decision is adopt-table, the new case must FAIL against the un-swapped RatioTable.hpp (idx 6/idx 8 are the RED) — record it; if the decision is keep-current, the case passes immediately (no source change needed — note this in the SUMMARY). Do NOT edit RatioTable.hpp in this task.</action>
  <verify>
    <automated>grep -nE "\[Phase 23\]: x1.5.*DECISION" .planning/STATE.md ; make test 2>&1 | tail -15</automated>
  </verify>
  <done>test_regression.cpp contains the BUG-02 cadence case with EXPECTED[15] matching the logged decision and a comment quoting it; under adopt-table the case is RED here (idx6/idx8), under keep-current it is green.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the BEATS_PER_ALIGN swap (only if adopt-table) and confirm GREEN</name>
  <files>src/dsp/RatioTable.hpp</files>
  <read_first>
    - src/dsp/RatioTable.hpp (L18-61 — RATIO_TABLE declaration site + the `// FUTURE (P23)` marker at L56 naming the exact slot)
    - .planning/phases/23-functional-bug-fixes/23-PATTERNS.md ("src/dsp/RatioTable.hpp (MOD)" L158-186 — two-cell change, frozen signature, no golden regen)
    - .planning/phases/23-functional-bug-fixes/23-RESEARCH.md (Pattern 3 L145-160 — verified table; Anti-Patterns L163-166 — do NOT patch ClockTracker, do NOT regen goldens)
  </read_first>
  <action>If the logged decision is **keep-current**: make NO change to RatioTable.hpp — the regression already passes; record "keep-current: no source change" in the SUMMARY and skip to verify. If the logged decision is **adopt-table**: add `static constexpr int BEATS_PER_ALIGN[15] = {16,8,6,4,3,2,3,1,2,1,1,1,1,1,1};` next to RATIO_TABLE (with the index banner comment), and rewrite `shouldReset` to: `if (ratioIdx < 0) return true; return beatCount >= BEATS_PER_ALIGN[ratioIdx];` — removing the `RATIO_TABLE[ratioIdx] < 1.f` guard and the `round(1/ratio)` math (the `<cmath>` include may remain). Keep the signature `bool shouldReset(int, int)` FROZEN. Do NOT touch ClockTracker.hpp (it delegates via L172 — CR-03 single home). Do NOT regenerate goldens (free-run goldens never reach shouldReset). Run `make test` — all green, including the BUG-02 case that was red in Task 1 and the 13-ratio cadence assertions in plan 23-03.</action>
  <verify>
    <automated>make test 2>&1 | tail -6 ; grep -c "BEATS_PER_ALIGN" src/dsp/RatioTable.hpp</automated>
  </verify>
  <done>`make test` exits 0. Under adopt-table: RatioTable.hpp declares BEATS_PER_ALIGN[15], shouldReset reads it uniformly, idx 6 resets every 3 and idx 8 every 2 (the BUG-02 case green where it was red), the 13 other ratios unchanged, ClockTracker.hpp untouched, goldens not regenerated. Under keep-current: RatioTable.hpp is unchanged and all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | BUG-02 is an internal ratio-table lookup change. No untrusted input; the cadence is driven by the clock FSM only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-NA | — | RatioTable.hpp shouldReset | accept | No new threats (internal DSP / test-only). The change is a constexpr table lookup; the index is clamped upstream in ClockTracker (clampi 0..14) before reaching shouldReset. |
</threat_model>

<verification>
- The STATE.md `[Phase 23]: x1.5 ... DECISION:` line exists (gate from plan 23-04) — this plan reads it.
- `make test` exits 0; under adopt-table the BUG-02 case is green where it was red pre-swap.
- `make RACK_DIR=../Rack-SDK` still builds the plugin (wave-merge check).
- Goldens NOT regenerated (free-run goldens unaffected); ClockTracker.hpp NOT modified.
</verification>

<success_criteria>
SC4 satisfied: x1.5/÷1.5 align per the auditioned decision (no mid-cycle truncation if the table
was adopted; preserved behavior if kept), pinned by a deterministic regression reading the decided
EXPECTED[15]. TEST-05 satisfied for BUG-02 under adopt-table (red→green); under keep-current the
regression pins the preserved cadence.
</success_criteria>

<output>
Create `.planning/phases/23-functional-bug-fixes/23-05-SUMMARY.md` when done. Quote the STATE.md
decision line, state which branch (adopt-table / keep-current) was taken, and cite the BUG-02
red→green (or "keep-current: pinned, no source change"). Confirm goldens were not regenerated and
ClockTracker.hpp was not touched.
</output>

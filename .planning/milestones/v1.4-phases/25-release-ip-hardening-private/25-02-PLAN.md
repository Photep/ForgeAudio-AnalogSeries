---
phase: 25-release-ip-hardening-private
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [IP-03]

must_haves:
  truths:
    - "res/AnalogLFO.svg contains zero <text>/<tspan>/<glyph>/<font>/@font-face nodes and no <use href> pulling a glyph symbol — it is path-only, so no font program ships in the SVG"
    - "A human has confirmed the FORGE/AUDIO and ANALOG LFO wordmark outlines derive from OFL fonts (Bebas Neue / Chakra Petch), NOT the trial FoundationLogo"
    - "The provenance decision (confirmed-OFL vs needs-regeneration) is recorded in the SUMMARY and STATE.md"
  artifacts:
    - path: ".planning/phases/25-release-ip-hardening-private/25-02-SUMMARY.md"
      provides: "Recorded human provenance decision for IP-03 (Assumption A1 closed)"
      contains: "provenance"
  key_links:
    - from: "res/AnalogLFO.svg outlines"
      to: "OFL Bebas Neue / Chakra Petch source fonts"
      via: "human visual confirmation against known OFL letterforms"
      pattern: "A1"
---

<objective>
Confirm the one residual IP risk for public GPL release: that the FORGE/AUDIO and "ANALOG LFO" wordmark vector outlines baked into res/AnalogLFO.svg were drawn from OFL fonts (Bebas Neue / Chakra Petch), NOT from the trial FoundationLogo font being purged in 25-03. Path geometry carries no font metadata, so this cannot be determined by any tool — it is a human judgment (Assumption A1, research's defining HIGH-stakes open question).

Purpose: If the wordmark outlines came from the trial font, the shipped panel art derives from a non-redistributable trial face and must be regenerated from confirmed-OFL fonts BEFORE the history purge. This gate must resolve before 25-03 runs.
Output: A recorded provenance decision (confirmed-acceptable, or needs-regeneration) closing IP-03 / Assumption A1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-release-ip-hardening-private/25-RESEARCH.md
@.planning/phases/25-release-ip-hardening-private/25-VALIDATION.md
@$HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/project_design_language.md

<facts>
Verified live (2026-06-30): res/AnalogLFO.svg contains 120 <path> elements and ZERO <text>/<tspan>/<glyph>/<font>/@font-face/base64 nodes — no font program ships in the SVG. OFL 1.1 explicitly permits embedding/outline conversion, and the embedded outlines are NOT subject to OFL redistribution restrictions, so Bebas Neue + Chakra Petch outlines are safe under GPL-3.0. The ONLY residual risk is whether any wordmark outline was instead drawn from the trial FoundationLogo font (named as the brand face in the design-language memory). This is undeterminable from path data — human gate required (A1).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated SVG font-data census (prove no font program ships, gather evidence for the human gate)</name>
  <files>res/AnalogLFO.svg (read-only census — no edits)</files>
  <read_first>
    - res/AnalogLFO.svg (the shipped panel art under review)
    - .planning/phases/25-release-ip-hardening-private/25-RESEARCH.md (Pitfall 4; Assumption A1/A3; State of the Art OFL outline finding)
  </read_first>
  <action>
    Run a census of res/AnalogLFO.svg to confirm it embeds no font program: grep for `<text`, `<tspan`, `<glyph`, `<font`, `@font-face`, `base64`, and `<use ` (to rule out A3 — a <use href> pulling in a glyph symbol). Count the `<path` elements. Also census any sibling panel SVGs under res/ and res/components/ for the same nodes. Produce a short evidence summary (node counts) for the human reviewer in Task 2 — this task makes NO file changes to the SVG.
  </action>
  <verify>
    <automated>grep -ciE '<text|<tspan|<glyph|<font|@font-face|base64' res/AnalogLFO.svg</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '<text|<tspan|<glyph|<font|@font-face|base64' res/AnalogLFO.svg` returns 0
    - `grep -c '<use ' res/AnalogLFO.svg` returns 0 (no glyph symbol pulled in — A3 ruled out)
    - `grep -c '<path' res/AnalogLFO.svg` returns a positive count (~120) — art is vector outlines
    - Evidence summary (node counts for AnalogLFO.svg + any sibling panel SVGs) prepared for the Task 2 human gate
  </acceptance_criteria>
  <done>Automated census confirms no font program is embedded in any shipped SVG; the only open question is the human-judgment outline provenance, with evidence prepared for the gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human provenance gate — confirm wordmark outlines are OFL, not the trial FoundationLogo (IP-03 / Assumption A1)</name>
  <files>res/AnalogLFO.svg (read-only review — no edits in this plan)</files>
  <action>Present the Task 1 census evidence to the operator and obtain the OFL-vs-trial provenance decision; make NO file changes. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    An automated census (Task 1) proved res/AnalogLFO.svg ships zero font programs (0 text/glyph/font/@font-face/use nodes, ~120 vector paths). What a tool CANNOT determine: whether the FORGE/AUDIO + ANALOG LFO wordmark outlines were drawn from the OFL fonts (Bebas Neue / Chakra Petch) or from the trial FoundationLogo font. This is Assumption A1 — the phase's defining IP risk.
  </what-built>
  <how-to-verify>
    1. Open res/AnalogLFO.svg in a vector editor (or view the rendered panel) and locate the FORGE/AUDIO wordmark and the "ANALOG LFO" lettering.
    2. Compare the letterforms against known OFL Bebas Neue (the documented fallback brand face) and Chakra Petch specimens — checking distinctive glyph shapes (the F, A, G, and the condensed caps proportions).
    3. Decide: were these outlines generated from the OFL fonts (Bebas Neue / Chakra Petch), or from the trial FoundationLogo?
    4. If you CANNOT positively confirm OFL origin, the answer is "needs-regeneration": the wordmark paths must be re-exported from a confirmed-OFL font before the purge — that becomes a follow-up plan, and 25-03 should NOT proceed until it is resolved.
  </how-to-verify>
  <resume-signal>Reply "confirmed-OFL" (outlines derive from Bebas Neue / Chakra Petch — IP-03 clear) OR "needs-regeneration" (describe which wordmark must be re-exported before 25-03 can run).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped panel art → public GPL consumers | res/AnalogLFO.svg is redistributed under GPL-3.0; its embedded outlines must derive from license-compatible fonts |
| design intent (trial font) → shipped outlines | the brand face documented in design notes is the trial FoundationLogo, which must NOT be the source of any shipped outline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Information Disclosure | res/AnalogLFO.svg wordmark outlines | mitigate | Human gate (Task 2) confirms outlines derive from OFL fonts; if unconfirmed, blocks 25-03 until paths are regenerated from a confirmed-OFL font |
| T-25-11 | Tampering / hidden font data | <use href> glyph symbols | mitigate | Task 1 greps for `<use ` and font/glyph nodes (A3) — proves no font program ships, only Bézier paths |
| T-25-12 | Repudiation | provenance decision | mitigate | Decision (confirmed-OFL vs needs-regeneration) recorded in SUMMARY + STATE.md so the Phase 28 public-flip gate has an auditable record |
</threat_model>

<verification>
- Automated: res/AnalogLFO.svg has 0 font/glyph/text/@font-face/use nodes and a positive path count
- Human (IP-03 / A1): operator confirms wordmark outline provenance is OFL-acceptable, decision recorded
</verification>

<success_criteria>
- No font program ships in any panel SVG (automated census)
- Human provenance decision recorded: either "confirmed-OFL" (IP-03 satisfied, 25-03 may proceed) or "needs-regeneration" (25-03 blocked pending a follow-up re-export plan)
</success_criteria>

<output>
Create `.planning/phases/25-release-ip-hardening-private/25-02-SUMMARY.md` when done — record the human provenance decision verbatim and mirror it into STATE.md.
</output>

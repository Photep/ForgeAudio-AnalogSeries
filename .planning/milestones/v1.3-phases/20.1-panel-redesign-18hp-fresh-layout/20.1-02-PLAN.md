---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - res/AnalogLFO-fresh.svg
  - res/AnalogLFO.svg
  - res/AnalogLFO-redesign.svg
  - res/AnalogLFO-fresh-v1.svg
autonomous: true
requirements: [PANEL-08]
must_haves:
  truths:
    - "The production panel res/AnalogLFO.svg is the 18HP fresh art (viewBox 91.44x128.5) with all double-rendered metal knob bodies stripped"
    - "Dead artwork SVGs (redesign, fresh-v1) no longer exist in res/"
  artifacts:
    - path: "res/AnalogLFO.svg"
      provides: "18HP bezel-stripped production panel artwork"
      contains: "viewBox=\"0 0 91.44 128.5\""
  key_links:
    - from: "src/AnalogLFO.cpp setPanel call (line 1526)"
      to: "res/AnalogLFO.svg"
      via: "asset::plugin(... \"res/AnalogLFO.svg\")"
      pattern: "res/AnalogLFO\\.svg"
---

<objective>
Strip the double-rendered static metal knob bodies from `res/AnalogLFO-fresh.svg` (D-01), promote the stripped file to `res/AnalogLFO.svg` overwriting the old 14HP art (D-03), and delete the two dead artwork SVGs (D-04). This makes the production panel the 18HP fresh art using the widget-owned-knob pattern, from which VCV Rack auto-derives 18HP via the viewBox (D-05, PANEL-08).

Purpose: The fresh SVG currently draws full metal knob graphics at all 10 knob anchors. Left in place they double-render under the widget-owned `_bg.svg` knobs (and the hero panel circle r=11.00 is LARGER than the widget r about 9.0, so a hard metal ring rings out past the live knob). Stripping them matches the proven 14HP convention where the panel draws zero knob bodies.
Output: `res/AnalogLFO.svg` = bezel-stripped 18HP art; `res/AnalogLFO-redesign.svg` and `res/AnalogLFO-fresh-v1.svg` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md
@res/AnalogLFO-fresh.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip metal knob bodies from res/AnalogLFO-fresh.svg (D-01)</name>
  <files>res/AnalogLFO-fresh.svg</files>
  <read_first>
    - res/AnalogLFO-fresh.svg (the file being edited — read the full file first so the exact element runs at the target line ranges are visible in current state; line numbers in the strip table are approximate and MUST be confirmed against actual element content before deleting)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (Common Pitfalls > Pitfall 1 — the exact KEEP/REMOVE strip table verified line-by-line)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md (Panel artwork strip rule D-01 — CRITICAL)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md (res/AnalogLFO-fresh.svg knob-body strip — the widget-owns-the-knob rule and prior-art analog)
  </read_first>
  <action>
    Remove, for each of the 5 knobs, the metal body circle (url(#metal) / url(#metalHero)), the inner-ring circle, the indicator path, and the center dot. Verified target element runs in the fresh SVG: MORPH hero at lines 62-65 (r=11.00 url(#metalHero) body, r=6.82 inner ring, indicator path, r=0.8 center dot); CHARACTER at 79-82; DRIFT at 96-99; RATE at 113-116; PHASE at 130-133. Also remove the 5 trimpot body+indicator pairs at lines 140+149, 153+162, 166+175, 179+188, 192+201 (each is an r=2.9 url(#metal) body circle plus its d4a017 gold indicator path).

    KEEP everything else — do NOT touch: display chrome / screen / corner brackets (about lines 36-44), the placeholder waveform polylines (about 39-40, covered by the opaque live display), all hero anchor scaffolding and scallop ticks (about 45-60), every label path, all jack rings including the OUTPUT ember accent rings, the divider, the 4 bolt circles, the rails, and the brand wordmark/rune. Per RESEARCH discretion: KEEP the soft under-knob shadow circles (about 61, 78, 95, 112, 129) and the scallop tick marks — they read as recessed sockets under the live widget knobs. Removing the metal body / inner-ring / indicator / center-dot is the MANDATORY part; keeping shadows+ticks is the recommended discretionary choice.

    Confirm each element by its attributes (fill="url(#metal)" / "url(#metalHero)", the indicator path's d4a017 gold stroke/fill, the small center-dot radius) before deleting — line numbers shift as you delete, so delete from the BOTTOM of the file upward (PHASE first, MORPH last; trimpots before knobs) so earlier line numbers stay valid, OR re-locate each element by attribute after each deletion. Do NOT change the svg width/height/viewBox header (must stay 91.44x128.5). Do NOT edit res/AnalogLFO.svg in this task — promotion is Task 2.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && C1=$(grep -c 'url(#metalHero)' res/AnalogLFO-fresh.svg); C2=$(grep -c 'url(#metal)' res/AnalogLFO-fresh.svg); grep -q 'viewBox="0 0 91.44 128.5"' res/AnalogLFO-fresh.svg && grep -q 'url(#jack)' res/AnalogLFO-fresh.svg && grep -q 'url(#screen)' res/AnalogLFO-fresh.svg && [ "$C1" = 0 ] && [ "$C2" = 0 ] && echo STRIP_OK</automated>
  </verify>
  <done>res/AnalogLFO-fresh.svg contains zero url(#metal) and zero url(#metalHero) references (all knob/trimpot metal bodies stripped), still contains url(#jack) and url(#screen) (jacks + display chrome kept), and the viewBox is unchanged at "0 0 91.44 128.5". Manual visual confirmation deferred to Plan 05's in-Rack gate (no metal ring rings out past any live knob).</done>
</task>

<task type="auto">
  <name>Task 2: Promote fresh.svg to AnalogLFO.svg and delete dead artwork (D-03, D-04)</name>
  <files>res/AnalogLFO.svg, res/AnalogLFO-redesign.svg, res/AnalogLFO-fresh-v1.svg</files>
  <read_first>
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (Runtime State Inventory + State of the Art — fresh-v1.svg is byte-identical-size to fresh.svg; confirm fresh.svg is the live one before deleting v1; D-04 dead-file list)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md (res/AnalogLFO.svg promotion + dead-SVG cleanup — the setPanel string stays unchanged)
  </read_first>
  <action>
    After Task 1's strip is complete, overwrite the production panel: copy the stripped `res/AnalogLFO-fresh.svg` to `res/AnalogLFO.svg` (overwrite the old 35.4KB 14HP file). Do NOT edit `src/AnalogLFO.cpp` — the `setPanel(createPanel(... "res/AnalogLFO.svg"))` string at line 1526 stays exactly as-is; only the file content changes (D-03). Do NOT add or edit any width/HP field in plugin.json — Rack derives 18HP from the SVG viewBox 91.44mm (D-05).

    Then delete the two dead artwork files: `res/AnalogLFO-redesign.svg` and `res/AnalogLFO-fresh-v1.svg` (D-04). Keep `res/AnalogLFO-fresh.svg` itself if desired as the source-of-strip, OR remove it too once promoted (it is no longer referenced by code) — leaving it is harmless; the production path is `res/AnalogLFO.svg`. Use `git rm` for the deletions so the removal is staged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'viewBox="0 0 91.44 128.5"' res/AnalogLFO.svg && C1=$(grep -c 'url(#metalHero)' res/AnalogLFO.svg); C2=$(grep -c 'url(#metal)' res/AnalogLFO.svg); [ "$C1" = 0 ] && [ "$C2" = 0 ] && [ ! -f res/AnalogLFO-redesign.svg ] && [ ! -f res/AnalogLFO-fresh-v1.svg ] && grep -q 'res/AnalogLFO.svg' src/AnalogLFO.cpp && echo PROMOTE_OK</automated>
  </verify>
  <done>res/AnalogLFO.svg is the 18HP stripped art (viewBox 91.44x128.5, zero url(#metal)/url(#metalHero)). res/AnalogLFO-redesign.svg and res/AnalogLFO-fresh-v1.svg no longer exist. src/AnalogLFO.cpp still references "res/AnalogLFO.svg" (setPanel string untouched). plugin.json has no width field added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits and moves static SVG asset files for an offline desktop audio-plugin faceplate. No input parsing, no network, no auth, no secrets, no user data, no executable code path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-02 | — | Static panel SVG artwork (strip + promote + delete) | accept | No applicable ASVS category. Per RESEARCH.md Security Domain, editing static SVG art on an offline faceplate introduces no threat surface — the SVG is rendered, never parsed for control flow, and carries no scripts (NanoVG/nanosvg renders shapes only). |

No package installs (T-20.1-SC not applicable — zero npm/pip/cargo/registry packages this phase per RESEARCH.md Package Legitimacy Audit).
</threat_model>

<verification>
- res/AnalogLFO.svg is the stripped 18HP art (viewBox 91.44x128.5, no url(#metal)/url(#metalHero), retains url(#jack) + url(#screen)).
- res/AnalogLFO-redesign.svg and res/AnalogLFO-fresh-v1.svg deleted.
- setPanel string in src/AnalogLFO.cpp unchanged; no plugin.json width field.
- Final in-Rack confirmation (no double-rendered knob bodies, no over-strip of display glass) happens in Plan 05's visual gate.
</verification>

<success_criteria>
The production panel artwork is the 18HP fresh art with no metal knob bodies, the old 14HP and dead SVGs are gone, and Rack will auto-derive 18HP from the unchanged setPanel path (PANEL-08).
</success_criteria>

<output>
Create `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-02-SUMMARY.md` when done. Record which elements were stripped (and whether soft shadows/scallop ticks were kept), and confirm the dead-file deletions.
</output>

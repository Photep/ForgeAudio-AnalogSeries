---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [PANEL-11]
must_haves:
  truths:
    - "A pre-change baseline screenshot of the current 14HP Analog LFO display exists as the Phase-20 regression reference"
  artifacts:
    - path: ".planning/phases/20.1-panel-redesign-18hp-fresh-layout/baseline-14hp-100.png"
      provides: "Pre-change display content reference at 100% zoom"
    - path: ".planning/phases/20.1-panel-redesign-18hp-fresh-layout/baseline-14hp-200.png"
      provides: "Pre-change display content reference at 200% zoom"
  key_links: []
---

<objective>
Capture a pre-change baseline screenshot of the CURRENT 14HP Analog LFO panel (specifically the live waveform display content) in VCV Rack, so the Phase-20 no-regression diff in the final visual gate (PANEL-11, success criterion 6) is a concrete pixel reference rather than a from-memory judgment.

Purpose: Without a baseline, "no regression to Phase 20 display content" is unverifiable — the executor cannot compare ember waveform, scanlines, breathing glow, and corner brackets against anything. This is the Wave 0 requirement from VALIDATION.md.
Output: Two screenshots committed into the phase directory (100% and 200% zoom) showing the current display with a running waveform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture current 14HP display baseline screenshots</name>
  <read_first>
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-VALIDATION.md (Wave 0 Requirements section — the baseline screenshot requirement and its purpose)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (§Build & verify mechanics — make install + Rack zoom + screencapture commands; §Validation Architecture for what Phase 20 content to capture)
  </read_first>
  <what-built>
    Nothing is built yet — this is a pure baseline capture. The current installed plugin is the unmodified 14HP panel. Before any code or SVG edit, the current display state is photographed so the later regression diff is concrete.
  </what-built>
  <how-to-verify>
    1. Ensure the current (unmodified) plugin is the one loaded: from repo root run `make install RACK_DIR=../Rack-SDK` if you are unsure the installed copy is current, otherwise skip — do NOT edit any source/SVG first.
    2. Launch VCV Rack 2 and add the "Analog LFO" module.
    3. Let the LFO run free (no clock patched) so the ember waveform trace, comet trail, phase dot, scanlines, breathing border glow, and corner brackets are all visibly animating in the display.
    4. Set zoom to 100% (View menu or Ctrl-scroll). Capture the module with macOS `screencapture -R` or Cmd-Shift-4 and save to `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/baseline-14hp-100.png`.
    5. Set zoom to 200%. Capture again and save to `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/baseline-14hp-200.png`.
    6. Optionally patch a clock into CLK to also capture the clocked state (SYNC badge, BPM stack, ratio/swing pills) — these are part of the Phase 20 content set that must not regress.
    7. Confirm both PNGs exist in the phase directory and clearly show the live display content (ember trace + pills + glow).
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/baseline-14hp-100.png` exists and shows the current Analog LFO display with a live ember waveform.
    - File `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/baseline-14hp-200.png` exists and shows the same at 200% zoom.
    - The captures were taken BEFORE any edit to `src/AnalogLFO.cpp` or `res/*.svg` (this plan modifies no source files).
    - The display content visible includes: ember waveform trace, phase dot, scanlines, breathing border glow, corner brackets (the Phase 20 polish set named in success criterion 6).
  </acceptance_criteria>
  <resume-signal>Type "approved" once both baseline PNGs are saved in the phase directory, or describe any issue capturing them.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan captures screenshots of a locally-running desktop audio plugin. No inputs are parsed, no network, no auth, no secrets, no file I/O beyond writing two PNG image files into the project's own planning directory.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-01 | — | Offline VCV Rack faceplate (this plan: screenshot capture only) | accept | No applicable ASVS category. Per RESEARCH.md §Security Domain, this is an offline desktop audio-plugin faceplate with no input parsing, network, auth, secrets, or user data. Capturing screenshots introduces no threat surface. |

No package installs (`T-20.1-SC` not applicable — zero npm/pip/cargo/registry packages in this phase per RESEARCH.md Package Legitimacy Audit).
</threat_model>

<verification>
Both baseline PNGs exist in the phase directory and visibly show the current 14HP display content. No source files were modified by this plan.
</verification>

<success_criteria>
A concrete Phase-20 display content reference (100% + 200% screenshots) exists before any panel edit begins, enabling a real regression diff in Plan 05's visual gate.
</success_criteria>

<output>
Create `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-01-SUMMARY.md` when done. Note the exact PNG paths captured and whether the clocked state was also captured.
</output>

---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 02
subsystem: ui
tags: [svg, panel, vcv-rack, nanosvg, faceplate, forge-noir]

# Dependency graph
requires:
  - phase: 20.1-01
    provides: baseline 14HP panel screenshots for regression diff
provides:
  - "res/AnalogLFO.svg is the 18HP fresh art (viewBox 91.44x128.5) with all double-rendered metal knob bodies stripped"
  - "Widget-owned-knob convention established for the 18HP panel (panel draws zero knob bodies)"
  - "Dead artwork SVGs (redesign, fresh-v1) removed from res/"
affects: [20.1-03, 20.1-04, 20.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Widget owns the knob: panel SVG draws only sockets/chrome, never the metal body/indicator"
    - "VCV Rack derives module HP from SVG viewBox width (no manifest width field)"

key-files:
  created:
    - res/AnalogLFO-fresh.svg
  modified:
    - res/AnalogLFO.svg

key-decisions:
  - "Kept soft under-knob shadow circles and scallop ticks (D-01 discretion) — they read as recessed sockets under the live widget knobs"
  - "Deleted dead SVGs via filesystem rm (they were untracked, nothing to git rm)"

patterns-established:
  - "Strip rule D-01: remove url(#metal)/url(#metalHero) body + inner ring + indicator + center dot for every knob/trimpot; one source of truth per control (the widget _bg.svg)"

requirements-completed: [PANEL-08]

# Metrics
duration: 4min
completed: 2026-06-12
---

# Phase 20.1 Plan 02: Strip + Promote 18HP Fresh Panel Art Summary

**Stripped all 5 knob + 5 trimpot double-rendered metal bodies from the fresh SVG and promoted it to the production `res/AnalogLFO.svg`, making the 18HP panel (viewBox 91.44x128.5) use the widget-owned-knob convention.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-06-12T00:43:02Z
- **Completed:** 2026-06-12T00:48:00Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 overwritten) + 2 deleted

## Accomplishments
- Removed every `url(#metalHero)`/`url(#metal)` knob body, inner ring, gold/orange indicator, and center dot for MORPH (hero) + CHARACTER/DRIFT/RATE/PHASE (secondary) + 5 trimpots.
- Kept the soft under-knob shadow circles and scallop tick marks so each anchor still reads as a recessed socket under the live widget knob.
- Promoted the stripped `res/AnalogLFO-fresh.svg` → `res/AnalogLFO.svg`, overwriting the old 35.4KB 14HP art with the 18HP bezel-stripped art.
- Deleted the two dead artwork files `res/AnalogLFO-redesign.svg` and `res/AnalogLFO-fresh-v1.svg`.
- Left `setPanel(... "res/AnalogLFO.svg")` in `src/AnalogLFO.cpp` untouched and added no `plugin.json` width field — Rack auto-derives 18HP from the viewBox.

## What Was Stripped vs Kept

**REMOVED (mandatory, D-01):**
- MORPH hero: `r=11.00 url(#metalHero)` body, `r=6.82` inner ring, indicator path, `r=0.8` center dot.
- CHARACTER / DRIFT / RATE / PHASE: `r=6.20 url(#metal)` body, `r=3.84` inner ring, indicator path, `r=0.5` center dot (each).
- 5 trimpots: `r=2.9 url(#metal)` body + the `#d4a017` gold indicator path (each).

**KEPT (recommended discretionary, D-01):**
- Soft under-knob shadow circles (`r=11.60` hero, `r=6.80` secondary) — recessed-socket read.
- Scallop tick marks around every knob/trimpot.
- All jack rings (8 `url(#jack)` — incl. OUTPUT ember accent rings), display chrome/screen/brackets, label paths, divider, 4 bolt circles, rails, brand wordmark/rune, and the placeholder waveform polylines (covered by the opaque live display).

Note: the gradient *definitions* `id="metal"` / `id="metalHero"` remain in `<defs>` (harmless, no `url(#...)` usage references them anymore).

## Task Commits

Each task was committed atomically:

1. **Task 1: Strip metal knob bodies from res/AnalogLFO-fresh.svg (D-01)** - `3862d93` (feat)
2. **Task 2: Promote fresh.svg to AnalogLFO.svg and delete dead artwork (D-03, D-04)** - `6263945` (feat)

## Files Created/Modified
- `res/AnalogLFO-fresh.svg` - Created/committed in stripped form (source-of-strip, kept per plan as harmless).
- `res/AnalogLFO.svg` - Overwritten with the stripped 18HP art (was the old 14HP 35.4KB file).
- `res/AnalogLFO-redesign.svg` - Deleted (dead artwork, was untracked).
- `res/AnalogLFO-fresh-v1.svg` - Deleted (dead artwork, byte-size superseded by fresh.svg, was untracked).

## Verification
- Task 1 `STRIP_OK`: zero `url(#metalHero)`, zero `url(#metal)` usages; retains `url(#jack)` + `url(#screen)`; viewBox unchanged `0 0 91.44 128.5`. Gold trimpot indicators remaining = 0; jack rings = 8.
- Task 2 `PROMOTE_OK`: `res/AnalogLFO.svg` is the 18HP stripped art (viewBox + zero metal); the two dead SVGs no longer exist; `src/AnalogLFO.cpp` still references `res/AnalogLFO.svg`; `plugin.json` has no width field.
- Manual in-Rack visual confirmation (no metal ring rings out past any live knob, no over-strip of display glass) is deferred to Plan 05's 100%/200% zoom gate per D-08.

## Decisions Made
- **Kept soft shadows + scallop ticks** (D-01 discretion): they read as a recessed socket beneath the live widget knob and match the Forge Noir look; only the metal body/inner-ring/indicator/center-dot were mandatory removals.
- **Dead-file deletion via `rm`** not `git rm`: both dead SVGs were untracked (never in the index), so filesystem removal is the correct, fully-staged-by-default action.

## Deviations from Plan
None - plan executed exactly as written. (The plan suggested `git rm` for the dead SVGs; since they were untracked, plain `rm` was the equivalent correct action — no index entry existed to remove.)

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Production panel art is the 18HP bezel-stripped fresh art; ready for Plan 03 (ctor anchor swap + RATE/PHASE type upgrade) and Plan 04 (display box resize/re-tune).
- No double-rendered knob bodies remain to collide with widget-owned knobs.
- Final visual gate (alignment + no-regression at 100%/200% zoom) remains for Plan 05.

## Self-Check: PASSED

- FOUND: res/AnalogLFO.svg, res/AnalogLFO-fresh.svg, 20.1-02-SUMMARY.md
- CONFIRMED-GONE: res/AnalogLFO-redesign.svg, res/AnalogLFO-fresh-v1.svg
- FOUND commits: 3862d93, 6263945

---
*Phase: 20.1-panel-redesign-18hp-fresh-layout*
*Completed: 2026-06-12*

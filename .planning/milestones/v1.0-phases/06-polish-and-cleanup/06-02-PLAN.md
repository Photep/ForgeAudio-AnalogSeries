---
phase: 06-polish-and-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Drift dot instability is clearly visible at high drift levels"
    - "Drift dot instability responds to patched CV signal, not just knob"
    - "Bottom row trimpots sit above their corresponding jacks with visible connection lines"
    - "Output jack has visual distinction from input jacks (amber accent)"
    - "Module loads and functions correctly (no crashes, no rendering artifacts)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 6 changes in VCV Rack.

Purpose: Confirm that drift visual tuning, CV response, bottom row restructure, and output distinction all look correct in the running module. These are visual/interactive changes that can only be verified by loading the module in VCV Rack.
Output: User-confirmed visual verification of all 4 tech debt items.
</objective>

<execution_context>
@/Users/mrcbrown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrcbrown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-and-cleanup/06-CONTEXT.md
@.planning/phases/06-polish-and-cleanup/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and install plugin</name>
  <files></files>
  <action>
Run `make install` to build the plugin and install it for VCV Rack. Verify the build succeeds without errors or warnings. This prepares the module for visual inspection.
  </action>
  <verify>
    <automated>cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series" && make install 2>&1 | tail -10</automated>
  </verify>
  <done>`make install` succeeds. Plugin dylib and vcvplugin are built and installed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 6 changes in VCV Rack</name>
  <action>User visually verifies all Phase 6 changes in VCV Rack.</action>
  <verify>User confirms all 5 verification items pass.</verify>
  <done>User types "approved" confirming all visual changes are correct.</done>
  <what-built>All Phase 6 polish changes: amplified drift dot visuals (5x trail jitter + halo), CV-responsive drift visuals, two-row bottom section (trimpots above jacks with connection lines), output jack amber accent ring, and documentation fixes.</what-built>
  <how-to-verify>
    Launch VCV Rack and add the Forge Audio Analog LFO module. Verify these 5 items:

    1. **Bottom row layout**: Confirm three trimpots sit in an upper row with three CV jacks directly below them in a lower row. Thin amber connection lines should be visible between each trimpot-jack pair. The output jack (rightmost in the bottom row) should have a subtle amber accent ring distinguishing it from the input jacks.

    2. **Drift dot at high drift (knob only)**: Turn the Drift knob to maximum. Observe the phase dot on the waveform display. The trail should show visible Y-axis jitter and the halo should pulse/breathe noticeably. This should "sell the alive feeling" -- clearly present but not distracting.

    3. **Drift dot responds to CV**: Turn the Drift knob to zero. Connect an LFO or constant voltage source to the Drift CV input. Turn the Drift CV attenuator (trimpot above the DCV jack) up. The drift dot instability should appear and scale with the CV signal. This confirms the visual reads the combined CV-modulated value, not just the knob.

    4. **Drift dot at zero drift**: With both Drift knob at zero and no CV patched, the dot should be clean and stable with no jitter or halo pulsing (existing breathe animation is fine).

    5. **General check**: Module loads without crashes, waveform display renders correctly, all knobs and jacks function, morph/character/rate still work as expected. No visual artifacts or component overlap in the bottom section.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 items look good, or describe any issues that need adjustment (e.g., "drift visual too aggressive", "connection lines not visible enough", "output accent too subtle")</resume-signal>
</task>

</tasks>

<verification>
User visually confirms all 4 tech debt items are resolved in VCV Rack:
1. Drift dot instability is clearly perceptible at high drift levels
2. Drift dot responds to CV-modulated drift value
3. Bottom row uses standard two-row Eurorack convention
4. Output jack is visually distinct from input jacks
</verification>

<success_criteria>
- User types "approved" confirming all visual changes look correct
- No crashes or rendering artifacts reported
- Module functions correctly with all three knobs and CV inputs
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-cleanup/06-02-SUMMARY.md`
</output>

---
phase: 25-release-ip-hardening-private
plan: 04
type: execute
wave: 2
depends_on: [25-02]
files_modified:
  - res/AnalogLFO.svg
autonomous: false
requirements: [IP-03]

must_haves:
  truths:
    - "The FORGE/AUDIO and ANALOG LFO wordmark outlines in res/AnalogLFO.svg are re-exported from confirmed-OFL fonts (Bebas Neue and/or Chakra Petch) obtained from Google Fonts / the upstream OFL source"
    - "The source OFL font files used for the re-export are recorded by name + version + OFL source URL in the SUMMARY (provenance is now provable by construction, not by eye)"
    - "res/AnalogLFO.svg still contains zero <text>/<tspan>/<glyph>/<font>/@font-face/base64 nodes and zero <use href> (it stays path-only — no font program ships)"
    - "The regenerated panel renders with the wordmark in the same position, size, and fill as before (visual parity); the custom forge-rune glyph between FORGE and AUDIO is preserved (it is original art, not font-derived)"
    - "A human has visually accepted the regenerated wordmark AND the provenance decision is recorded as confirmed-OFL, closing IP-03 and unblocking 25-03"
  artifacts:
    - path: "res/AnalogLFO.svg"
      provides: "Panel art whose wordmark outlines provably derive from OFL fonts"
      contains: "<path"
    - path: ".planning/phases/25-release-ip-hardening-private/25-04-SUMMARY.md"
      provides: "Recorded OFL font sources + confirmed-OFL provenance decision (IP-03 closed)"
      contains: "confirmed-OFL"
  key_links:
    - from: "OFL Bebas Neue / Chakra Petch font files"
      to: "res/AnalogLFO.svg wordmark <path> outlines"
      via: "text-to-outline export, suspect FoundationLogo geometry replaced"
      pattern: "confirmed-OFL"
    - from: "res/AnalogLFO.svg (regenerated)"
      to: "25-03 push-first → history purge"
      via: "IP-03 gate now returns confirmed-OFL"
      pattern: "25-03"
---

<objective>
Re-export the FORGE/AUDIO and "ANALOG LFO" wordmark vector outlines in res/AnalogLFO.svg from confirmed-OFL fonts (Bebas Neue and/or Chakra Petch), replacing any path geometry that may have been drawn from the trial FoundationLogo font. After this lands, the wordmark provenance is provable BY CONSTRUCTION (the outlines come from a known OFL source), so the IP-03 gate flips from needs-regeneration (25-02) to confirmed-OFL — the precondition 25-03 requires.

Purpose: Close the one residual IP risk for the public GPL release. 25-02 could not confirm by eye that the shipped wordmark outlines derive from OFL fonts, and the design-language memory names the trial FoundationLogo as the brand face. Regenerating the lettering from a known-OFL font removes the ambiguity entirely.
Output: A regenerated res/AnalogLFO.svg (path-only, visual parity, custom forge-rune preserved); recorded OFL font sources; a confirmed-OFL provenance decision that unblocks the irreversible 25-03 history purge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-release-ip-hardening-private/25-RESEARCH.md
@.planning/phases/25-release-ip-hardening-private/25-02-SUMMARY.md
@$HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/project_design_language.md

<facts>
Verified live (2026-07-01):
- res/AnalogLFO.svg is 177 lines: 115 anonymous `<path>` elements, ZERO `<g>` groups, ZERO `id=` on paths, ZERO `<text>/<tspan>/<glyph>/<font>/@font-face/base64` nodes, ZERO `<use>`. The strings "FORGE"/"AUDIO"/"ANALOG"/"LFO" appear nowhere as text — the wordmark is pure unlabeled Bezier geometry, so it must be located visually/by coordinate, not by selector.
- viewBox is `0 0 91.44 128.5` (1 HP-multiple Eurorack panel in mm).
- 25-02 decision (recorded): needs-regeneration — provenance of the wordmark outlines could NOT be confirmed as OFL.
- Design-language memory documents FoundationLogo (the trial font, purged in 25-01 / to-be-purged in 25-03) as the intended brand face — the root of the risk.
- OFL candidates per RESEARCH/Assumption A1: Bebas Neue (OFL 1.1) and Chakra Petch (OFL 1.1), both available from Google Fonts. The forge-rune glyph between FORGE and AUDIO is original art (no font), and must be kept as-is.
- res/fonts/OFL.txt (the SIL OFL 1.1 text) already shipped via 25-01; this plan adds NO new shipped font program — only static outlines.

PRECONDITION: this plan resolves IP-03 to confirmed-OFL. It MUST land + commit before 25-03 runs (25-03 depends_on now includes 25-04).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify the wordmark paths and obtain the OFL source fonts</name>
  <files>res/AnalogLFO.svg (read-only census in this task), scratch dir for font downloads</files>
  <read_first>
    - res/AnalogLFO.svg (locate the FORGE/AUDIO header lettering and the "ANALOG LFO" lettering by coordinate band; note their x/y, scale, and fill so the replacement matches)
    - .planning/phases/25-release-ip-hardening-private/25-RESEARCH.md (Assumption A1 OFL candidates; State of the Art OFL outline finding)
    - $HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/project_design_language.md (wordmark layout, forge-rune position, FORGE/AUDIO arrangement)
  </read_first>
  <action>
    Render res/AnalogLFO.svg (e.g. rsvg-convert / resvg / a headless browser) to a reference PNG and visually identify which `<path>` elements form (a) the "FORGE", (b) the "AUDIO", and (c) the "ANALOG LFO" lettering — recording their bounding boxes, fill colors, and the position of the custom forge-rune glyph (which must NOT be replaced). Then obtain the OFL source fonts: download Bebas Neue and Chakra Petch from Google Fonts (or the upstream OFL repos), and record each font's family, the exact file, the version, and the OFL source URL. Verify each downloaded font ships under SIL OFL 1.1 (check its OFL.txt / LICENSE). Place fonts in a scratch dir — do NOT commit the font binaries into the repo (only outlines ship).
  </action>
  <verify>
    <automated>grep -cE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg</automated>
  </verify>
  <acceptance_criteria>
    - A reference render of the current panel exists and the FORGE / AUDIO / ANALOG LFO wordmark path groups are identified (bounding boxes + fills recorded)
    - The forge-rune glyph is identified and explicitly marked KEEP (original art, not font-derived)
    - Bebas Neue and Chakra Petch OFL font files are obtained; family + version + OFL source URL + OFL-1.1 confirmation recorded for the SUMMARY
    - `grep -cE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg` returns 0 (baseline confirmed before edits)
  </acceptance_criteria>
  <done>Wordmark path groups located with their geometry/fills, forge-rune marked keep, and confirmed-OFL source fonts in hand with recorded provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the wordmark outlines from the OFL fonts and splice into res/AnalogLFO.svg</name>
  <files>res/AnalogLFO.svg</files>
  <read_first>
    - res/AnalogLFO.svg (the path bands identified in Task 1)
    - .planning/phases/25-release-ip-hardening-private/25-VALIDATION.md (visual-parity expectations)
  </read_first>
  <action>
    For each wordmark string (FORGE, AUDIO, ANALOG LFO), set the text in the matching OFL font (FORGE/AUDIO + ANALOG LFO in their intended faces per the design language — typically the condensed display face Bebas Neue for the big wordmark and Chakra Petch where used), convert text→outline (deterministic tool: fonttools/FontForge, Inkscape `--actions=...export-text-to-path`, or `hb-view`/`text-to-svg` producing `<path d=...>`). Scale/position each regenerated outline group to match the bounding boxes and fills recorded in Task 1 (preserve placement, tracking, and color exactly — this is a like-for-like swap, not a redesign). Replace ONLY the suspect wordmark `<path>` elements; keep the forge-rune glyph and all non-wordmark art (knobs, jacks, display, rails, bolts) byte-untouched. Keep the SVG path-only — emit `<path>` geometry, never `<text>`/`<glyph>`/`<use>`. Re-flatten so no `font-family` attribute or `<defs>` font reference remains.
  </action>
  <verify>
    <automated>grep -cE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg` returns 0 (still path-only — no font program introduced)
    - `grep -c '<path' res/AnalogLFO.svg` returns a positive count; non-wordmark art (knobs/jacks/display/rails/bolts) is unchanged in a diff
    - The forge-rune glyph path is unchanged (still original art)
    - viewBox `0 0 91.44 128.5` unchanged; the SVG is still well-formed (parses)
  </acceptance_criteria>
  <done>The wordmark outlines are now OFL-font-derived path geometry spliced in at the original placement/scale/fill, with the rest of the panel and the forge-rune untouched and the SVG still font-program-free.</done>
</task>

<task type="auto">
  <name>Task 3: Visual-parity render check + font-node census (regression gate)</name>
  <files>res/AnalogLFO.svg (verification only)</files>
  <read_first>
    - res/AnalogLFO.svg (regenerated)
    - $HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/vcv_build_install_workflow.md (build/install + stale-flush conventions, if an in-Rack check is wanted)
  </read_first>
  <action>
    Render the regenerated res/AnalogLFO.svg to PNG and compare against the Task-1 reference: the FORGE/AUDIO header and ANALOG LFO lettering should read identically in placement and weight, with the forge-rune intact. Re-run the full font-node census (text/tspan/glyph/font/@font-face/base64/use across res/AnalogLFO.svg AND sibling res SVGs) and require 0. Optionally rebuild the plugin against ../Rack-SDK and confirm the panel loads (no SVG parse error). `make test` is unaffected (no code touched) but may be run for hygiene.
  </action>
  <verify>
    <automated>grep -rcE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg res/components/*.svg | awk -F: '{s+=$2} END{print s}'</automated>
  </verify>
  <acceptance_criteria>
    - Side-by-side render shows visual parity (wordmark placement/weight, forge-rune intact, no clipping/overlap)
    - Aggregate font-node census across all res SVGs returns 0
    - If rebuilt: the plugin builds and the panel renders without an SVG error
  </acceptance_criteria>
  <done>The regenerated panel is visually equivalent to the original, still ships zero font programs, and loads cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human acceptance + provenance close — record confirmed-OFL (IP-03), unblock 25-03</name>
  <files>(none — acceptance gate; decision recorded in SUMMARY + STATE.md)</files>
  <action>Present the before/after render and the recorded OFL font sources; obtain visual acceptance of the regenerated wordmark and confirm the provenance is now OFL-by-construction. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    The FORGE/AUDIO + ANALOG LFO wordmark outlines in res/AnalogLFO.svg have been re-exported from confirmed-OFL fonts (Bebas Neue / Chakra Petch, sources recorded), replacing the suspect geometry that may have derived from the trial FoundationLogo. The panel is path-only (0 font programs), visually at parity, with the forge-rune preserved. Provenance is now provable by construction, not by eye.
  </what-built>
  <how-to-verify>
    1. Compare the before/after panel render — the wordmark should read the same; the forge-rune between FORGE and AUDIO is intact.
    2. Confirm the recorded font sources are genuinely OFL 1.1 (Bebas Neue / Chakra Petch from Google Fonts or upstream).
    3. Accept the visual result, or request adjustments (tracking, weight, placement) before closing.
  </how-to-verify>
  <resume-signal>Reply "confirmed-OFL" (wordmark accepted and provenance now OFL — IP-03 closed, 25-03 may proceed) OR "revise" (describe the adjustment needed before acceptance).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFL source font → shipped outline | only license-clean OFL fonts may be the source of the regenerated wordmark geometry |
| regenerated SVG → public GPL consumers | res/AnalogLFO.svg redistributes under GPL-3.0; its outlines must now provably derive from OFL fonts |
| this plan → 25-03 | only a confirmed-OFL outcome here authorizes the irreversible history purge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Tampering / wrong source | regenerated outlines drawn from a non-OFL font | mitigate | Task 1 records font family + version + OFL source URL + OFL-1.1 confirmation; only Bebas Neue / Chakra Petch OFL cuts are used |
| T-25-15 | Information Disclosure | a font program slipping into the SVG | mitigate | Tasks 2-3 re-run the full font-node census and require 0 (path-only, no <text>/<glyph>/<use>) |
| T-25-16 | Tampering | collateral change to non-wordmark art | mitigate | Task 2 replaces ONLY the wordmark paths; a diff confirms knobs/jacks/display/rails/bolts and the forge-rune are untouched |
| T-25-17 | Repudiation | unrecorded provenance | mitigate | Task 4 records confirmed-OFL + the font sources in SUMMARY + STATE.md for the Phase 28 audit trail |
</threat_model>

<verification>
- Path-only: res/AnalogLFO.svg (and siblings) have 0 font/glyph/text/@font-face/use nodes after regeneration
- Provenance: OFL font sources recorded (family + version + OFL URL); decision = confirmed-OFL
- Visual parity: before/after render equivalent, forge-rune intact, non-wordmark art unchanged
- Unblocks 25-03: IP-03 closed confirmed-OFL; 25-03 precondition satisfied
</verification>

<success_criteria>
- Wordmark outlines re-exported from confirmed-OFL fonts; suspect FoundationLogo-derived geometry replaced (IP-03)
- res/AnalogLFO.svg stays path-only (no font program), visually at parity, forge-rune preserved
- OFL font sources + confirmed-OFL decision recorded in SUMMARY + STATE.md
- 25-03 (irreversible history purge) is unblocked — its IP-03 precondition is now satisfied
</success_criteria>

<output>
Create `.planning/phases/25-release-ip-hardening-private/25-04-SUMMARY.md` when done — record the OFL font sources (family/version/URL), the visual-parity result, and the confirmed-OFL provenance decision; mirror the decision into STATE.md.
</output>

---
phase: 25-release-ip-hardening-private
plan: 04
type: execute
wave: 2
depends_on: [25-02]
files_modified:
  - res/AnalogLFO.svg
autonomous: false
requirements: [IP-03]

must_haves:
  truths:
    - "EVERY baked-text run in res/AnalogLFO.svg — the 3 brand-wordmark runs (lines 28 FORGE, 29 AUDIO, 35 ANALOG LFO) PLUS all 15 functional-label runs (lines 63/76/89/102/115/119/128/137/146/155/165/166/169/172/177) — has been triaged and classified as trial-font-derived (FoundationLogo-like) or already-OFL, and that classification + its evidence is recorded in the SUMMARY"
    - "Every run classified trial-font-derived is re-exported from a confirmed-OFL font; runs proven already-OFL (e.g. shipped JetBrains Mono) are left byte-unchanged with the proof recorded — NO trial-FoundationLogo-derived outline ships anywhere in the panel"
    - "The brand wordmark (FORGE/AUDIO + ANALOG LFO) is generated in BOTH Bebas Neue AND Chakra Petch; both are rendered; the operator PICKS the face at the blocking acceptance gate (no OFL face gives pixel parity — provenance is the gate, aesthetic is the human pick)"
    - "Every re-exported run's source OFL font (family + version + OFL source URL + OFL-1.1 confirmation) is recorded in the SUMMARY — provenance is now provable BY CONSTRUCTION, not by eye"
    - "res/AnalogLFO.svg stays path-only: zero <text>/<tspan>/<glyph>/<font>/@font-face/base64 and zero <use>; no font program ships; viewBox 0 0 91.44 128.5 unchanged; SVG still well-formed"
    - "The forge-rune (lines 30-34: 4 circles + diamond polygon at cx=45.72) and all non-text art (knobs, jacks, display, rails, bolts, gradients) are byte-untouched — only text-run lines appear in the git diff"
    - "A human has visually accepted the regenerated panel AND the provenance decision is recorded confirmed-OFL, closing IP-03 and unblocking 25-03"
  artifacts:
    - path: "res/AnalogLFO.svg"
      provides: "Panel art whose every text outline provably derives from a confirmed-OFL font (no trial FoundationLogo geometry)"
      contains: "<path"
    - path: ".planning/phases/25-release-ip-hardening-private/25-04-SUMMARY.md"
      provides: "Per-run triage table + recorded OFL font sources + confirmed-OFL provenance decision (IP-03 closed)"
      contains: "confirmed-OFL"
  key_links:
    - from: "OFL Bebas Neue / Chakra Petch / JetBrains Mono font files"
      to: "res/AnalogLFO.svg text <path> outlines (all trial-derived runs)"
      via: "fontTools SVGPathPen text-to-outline re-export, suspect FoundationLogo geometry replaced"
      pattern: "confirmed-OFL"
    - from: "res/AnalogLFO.svg (regenerated) + 25-04-SUMMARY.md"
      to: "25-03 push-first → history purge"
      via: "IP-03 gate now returns confirmed-OFL (25-03 greps this SUMMARY)"
      pattern: "25-03"
---

<objective>
Triage EVERY baked-text run in res/AnalogLFO.svg and re-export every trial-font-derived run from a confirmed-OFL font, so that NO outline drawn from the trial FoundationLogo face ships anywhere in the public GPL release. There are 18 baked-text runs (verified): the 3-piece brand wordmark (FORGE / AUDIO / ANALOG LFO) plus 15 functional labels (MORPH, the four secondary knob labels, the jack labels, and the footer). 25-02 could not confirm by eye that these outlines derive from OFL fonts; the design-language memory names the trial FoundationLogo as the brand face. After this lands, the provenance of every label is provable BY CONSTRUCTION (each outline comes from a recorded OFL source), so the IP-03 gate flips from needs-regeneration (25-02) to confirmed-OFL — the precondition 25-03 requires.

Purpose: Close the one residual IP risk for the public release. The brand wordmark gets BOTH a Bebas Neue and a Chakra Petch re-cut (operator picks at the gate — no OFL face reproduces the round-heavy original, so confirmed-OFL provenance is the hard requirement and visual character is a human-accepted choice). Functional labels are re-cut from the best-fit OFL face (JetBrains Mono for monospace-style functional text, else Chakra Petch) — legibility/footprint preserved.

Output: A regenerated res/AnalogLFO.svg (path-only, forge-rune + non-text art byte-preserved); a per-run triage table; recorded OFL font sources for every re-exported run; a confirmed-OFL provenance decision that unblocks the irreversible 25-03 history purge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-release-ip-hardening-private/25-04-RESEARCH.md
@.planning/phases/25-release-ip-hardening-private/25-VALIDATION.md
@.planning/phases/25-release-ip-hardening-private/25-02-SUMMARY.md
@$HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/project_design_language.md

<facts>
Verified live (2026-07-01 — see 25-04-RESEARCH.md for full derivation):
- res/AnalogLFO.svg is 178 lines: 115 anonymous `<path>` elements, 0 `<g>`, 0 `id=`, 0 `<text>/<tspan>/<glyph>/<font>/@font-face/base64`, 0 `<use>`. The strings never appear as text — every run is pure Bezier geometry located by transform signature + fill.
- viewBox is `0 0 91.44 128.5` (1 HP-multiple Eurorack panel in mm).
- ALL baked text shares the signature `transform="translate(Tx,Ty) scale(s,-s)"` (fontTools SVGPathPen on a TTF: native quadratic-`Q` curves, y-up flipped by the negative scale). Enumerate with `grep -nE 'scale\([0-9.]+,-[0-9.]+\)' res/AnalogLFO.svg` → 18 runs.
- The 18 runs (line · translate · scale · fill · role — decoded where confident in research):
  - 28 · (9.500,7.600) · 0.001758 · #e8612a · **FORGE** (wordmark, header L)
  - 29 · (64.557,7.600) · 0.001758 · #e8612a · **AUDIO** (wordmark, header R)
  - 35 · (20.648,14.600) · 0.002148 · #ece8e2 · **ANALOG LFO** (wordmark, title)
  - 63 · (39.061,76.200) · 0.001416 · #ece8e2 · **MORPH** (hero knob label)
  - 76 · (10.824,97.400) · 0.000928 · #c8c4be · secondary knob label (decode: ~CHAR)
  - 89 · (32.687,97.400) · 0.000928 · #c8c4be · secondary knob label (decode: ~DRIFT)
  - 102 · (51.468,97.400) · 0.000928 · #c8c4be · secondary knob label (decode: ~RATE)
  - 115 · (68.817,97.400) · 0.000928 · #c8c4be · secondary knob label (decode: ~PHASE)
  - 119 · (5.118,103.900) · 0.000708 · #a39e96 · jack-row label (decode)
  - 128 · (16.517,103.900) · 0.000708 · #a39e96 · jack-row label (decode)
  - 137 · (26.843,103.900) · 0.000708 · #a39e96 · jack-row label (decode)
  - 146 · (37.706,103.900) · 0.000708 · #a39e96 · jack-row label (decode)
  - 155 · (50.178,103.900) · 0.000708 · #a39e96 · jack-row label (decode)
  - 165 · (64.063,103.900) · 0.000708 · #a39e96 · jack-row label (decode)
  - 166 · (60.166,112.500) · 0.000732 · #c8c4be · lower label (decode)
  - 169 · (71.011,112.500) · 0.000732 · #c8c4be · lower label (decode)
  - 172 · (81.547,112.500) · 0.000830 · #ff8a4c · lower label, ember (decode: ~OUT)
  - 177 · (37.165,126.100) · 0.000635 · #e8612a @0.4 · footer run (decode — has ascenders/descenders, likely a different face than the wordmark)
- Forge-rune = SVG lines 30–34 (`<circle cx=45.72 cy=6.30 …>` ×4 + `<polygon points="45.72,3.90 …">`), original art between FORGE and AUDIO. **DO NOT TOUCH (decision #3).**
- Forensic finding (verified): the wordmark is a round heavy geometric (F w/cap = 0.626, round-`Q` O, ~0.23em stems) — matches NEITHER Bebas Neue (F w/cap 0.416, far too condensed) NOR Chakra Petch (chamfered/straight O), i.e. almost certainly the trial FoundationLogo. No OFL face reproduces it; the wordmark's visual character WILL change.
- OFL sources (verified downloaded + OFL-1.1 headers confirmed): Bebas Neue `github.com/google/fonts/raw/main/ofl/bebasneue/BebasNeue-Regular.ttf`; Chakra Petch `…/ofl/chakrapetch/ChakraPetch-{Regular,Bold}.ttf`; JetBrains Mono `…/ofl/jetbrainsmono/…` (the shipped runtime OFL face — candidate "already-OFL" source for monospace functional labels). All UPM 1000, cap 700.
- res/fonts/OFL.txt (SIL OFL 1.1) already shipped via 25-01; this plan adds NO new shipped font program — only static outlines.
- Pipeline is verified on this machine: throwaway Python venv + fontTools 4.63.0 + svgpathtools; SVGPathPen emits the exact quadratic-`Q` `<path d>` dialect the file already uses (nanosvg-compatible). inkscape/rsvg/imagemagick/resvg/cargo are ABSENT — render via the Playwright MCP (decision #4); `.playwright-mcp/` is present.

PRECONDITION CHAIN: this plan resolves IP-03 to confirmed-OFL. It MUST land + commit before 25-03 runs (25-03 depends_on includes 25-04 and greps 25-04-SUMMARY.md for "confirmed-OFL").
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage ALL 18 baked-text runs (trial-derived vs already-OFL), obtain OFL source fonts, render the before reference</name>
  <files>res/AnalogLFO.svg (read-only census + bbox measurement this task); scratch dir under the session scratchpad for fonts + renders + the triage table</files>
  <read_first>
    - res/AnalogLFO.svg (the FULL file — every `scale(s,-s)` run; record each run's line, string, fill, scale, translate)
    - .planning/phases/25-release-ip-hardening-private/25-04-RESEARCH.md (Wordmark Path Map; Font Sourcing; Font Choice Analysis — the F-width/cap + O-construction fingerprint method; Code Examples "Compute a target mm bbox for any existing run"; Package Legitimacy Audit)
    - $HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/project_design_language.md (FoundationLogo is the documented brand face; forge-rune position; wordmark layout/symmetry about x=45.72)
  </read_first>
  <action>
    Stand up the verified pipeline: throwaway Python venv under the scratchpad (`python3 -m venv …`; `pip install fonttools svgpathtools` — do NOT --break-system-packages, do NOT commit anything). Fetch the OFL fonts to scratch ONLY (never commit any .ttf — Pitfall 9): Bebas Neue Regular, Chakra Petch Regular + Bold, and JetBrains Mono, each from `github.com/google/fonts/raw/main/ofl/<family>/…` per the research Font Sourcing table, plus each family's OFL.txt; confirm each header reads "SIL Open Font License, Version 1.1" and record family + version + OFL source URL.

    Render the CURRENT panel to `before.png` via the Playwright MCP (open `file://…/res/AnalogLFO.svg`, viewport at the panel aspect e.g. 366×514, full-element screenshot) — decision #4.

    TRIAGE (decision #1 — this is the scope-defining step): for EACH of the 18 runs, (a) decode its string glyph-by-glyph; (b) fingerprint its SOURCE face using the research method — measure F (or a stem letter) width/cap-height ratio and O construction (round-`Q` vs chamfered/straight) with fontTools BoundsPen/RecordingPen, and compare against the verified reference numbers (FoundationLogo-like ≈ 0.626 round-heavy; Bebas 0.416; Chakra chamfered; JetBrains Mono = monospace, fixed advance); (c) classify each run **trial-font-derived** (FoundationLogo-like) or **already-OFL** (matches a confirmed-OFL face such as the shipped JetBrains Mono); (d) record per run its target geometry for re-export: cap-height in mm (= existing `s` × the run's cap-units, or measured mm bbox via the research svgpathtools snippet), baseline `Ty`, target center-x (or left edge), and `fill`. Mark the forge-rune (lines 30–34) explicitly KEEP — original art, never a font.

    Produce a written triage table (run · line · string · measured fingerprint · classification · target cap_mm / Ty / center_x / fill · disposition re-export|keep) in the scratch dir — it becomes the SUMMARY's provenance record. Runs classified already-OFL are dispositioned KEEP-AS-IS with their proof; every trial-derived run is queued for re-export (functional labels → Task 2; the 3 wordmark runs → Task 3).
  </action>
  <verify>
    <automated>! grep -qE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg && echo 'census 0 (path-only baseline)'</automated>
  </verify>
  <acceptance_criteria>
    - `before.png` exists (Playwright render of the current panel)
    - All 18 runs are enumerated, decoded, and fingerprinted; each carries a classification (trial-derived | already-OFL) with the measured evidence (F w/cap ratio + O construction, or monospace match)
    - The triage table records, for every trial-derived run, its disposition (re-export) and target geometry (cap_mm, Ty, center_x, fill); already-OFL runs are marked KEEP with proof
    - The forge-rune (lines 30–34) is marked KEEP (original art)
    - OFL fonts obtained to scratch with family + version + OFL URL + OFL-1.1 confirmation recorded; no `.ttf` staged in git (`git status` shows none)
    - `grep -cE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg` returns 0 (baseline confirmed before any edit)
  </acceptance_criteria>
  <done>All 18 runs triaged and classified with evidence; OFL source fonts in hand with recorded provenance; before.png captured; the re-export queue (functional + wordmark) and the KEEP list (forge-rune + any already-OFL runs) are fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export EVERY trial-derived FUNCTIONAL label from the best-fit OFL face and splice in</name>
  <files>res/AnalogLFO.svg</files>
  <read_first>
    - res/AnalogLFO.svg (the functional-label run lines flagged trial-derived in Task 1 — i.e. all of 63/76/89/102/115/119/128/137/146/155/165/166/169/172/177 NOT marked already-OFL)
    - .planning/phases/25-release-ip-hardening-private/25-04-RESEARCH.md (Code Examples — `run_path`/`emit` SVGPathPen recipe; Coordinate Mapping `s_new = cap_mm/700`, `Tx = center − s·(bbox_x0+bbox_x1)/2`; Pitfalls 3/4/5/7/8)
    - the Task 1 triage table (per-run cap_mm / Ty / center_x / fill / disposition)
  </read_first>
  <action>
    For EACH functional-label run classified trial-derived in Task 1, regenerate its outline from the **best-fit OFL face** per the legibility/footprint rule (decision #2): use **JetBrains Mono** for monospace-style functional text (it is already the shipped OFL runtime face, so it best preserves a uniform-advance functional look and adds no new family), else **Chakra Petch** where a proportional face reads better — state the chosen face per run in the SUMMARY. Use the research `emit()` recipe: set the run's text in the chosen TTF with fontTools SVGPathPen (native quadratic `Q`), compute `s = cap_mm/700` from the Task-1 target cap-height (NEVER reuse the old `s` — UPM differs, Pitfall 4), keep `Ty` unchanged so the baseline stays put, and compute `Tx` from the inked bbox center to hold the run's center-x (or left edge). Carry the original `fill` verbatim. Keep the glyph `d` in integer font units with the scale in the transform (Pitfall 8 — no float bloat). Emit a bare `<path … fill=… transform="translate(Tx,Ty) scale(s,-s)"/>` — never `<text>/<tspan>/<glyph>/<use>` or a `font-family` attr (Pitfall 3).

    Replace ONLY the flagged functional-label run lines, one line per run, in place. Touch NOTHING else — the forge-rune (30–34), the wordmark runs (28/29/35 — handled in Task 3), any already-OFL run marked KEEP, and all non-text art (knobs/jacks/display/rails/bolts/gradients) stay byte-identical (decision #3, Pitfall 2). Do NOT mix re-cut and original characters within a run. After splicing, re-run the census (must be 0) and an XML well-formed check.
  </action>
  <verify>
    <automated>! grep -qE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg && python3 -c "import xml.dom.minidom; xml.dom.minidom.parse('res/AnalogLFO.svg'); print('well-formed')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg` returns 0 (still path-only)
    - `python3 -c "import xml.dom.minidom; xml.dom.minidom.parse('res/AnalogLFO.svg')"` succeeds (well-formed); viewBox `0 0 91.44 128.5` unchanged
    - `git diff res/AnalogLFO.svg` shows changes ONLY on the functional-label run lines flagged trial-derived — the forge-rune (30–34), wordmark lines (28/29/35), already-OFL KEEP runs, and all non-text art are unchanged
    - `grep -c '<path' res/AnalogLFO.svg` is still a positive count (~115)
    - Each re-exported functional run's chosen OFL face is noted for the SUMMARY
  </acceptance_criteria>
  <done>Every trial-derived functional label is now OFL-font-derived path geometry spliced at the original baseline/center/fill; the wordmark, forge-rune, already-OFL runs, and all art are untouched; the SVG is still font-program-free and well-formed.</done>
</task>

<task type="auto">
  <name>Task 3: Re-export the brand wordmark in BOTH Bebas Neue and Chakra Petch, splice the working default, render both candidates</name>
  <files>res/AnalogLFO.svg; scratch dir (candidate path strings + after renders)</files>
  <read_first>
    - res/AnalogLFO.svg (wordmark runs: line 28 FORGE, line 29 AUDIO, line 35 ANALOG LFO)
    - .planning/phases/25-release-ip-hardening-private/25-04-RESEARCH.md (Wordmark Path Map exact targets; Coordinate Mapping; Code Examples `emit()`; Font Choice Analysis; Pitfalls 1/5/6)
    - .planning/phases/25-release-ip-hardening-private/25-VALIDATION.md (visual-parity expectations; IP-03 acceptance)
  </read_first>
  <action>
    Generate the 3-piece wordmark in BOTH confirmed-OFL faces (decision #2): a **Bebas Neue** version and a **Chakra Petch** version. For each face and each run use the research `emit()` recipe with the verified exact targets — FORGE: fill #e8612a, cap 2.723mm → `s = 2.723/700 = 0.003890`, baseline `Ty=7.600`, center-x 18.50; AUDIO: #e8612a, same s + Ty, center-x 73.18 (mirror of FORGE about panel center 45.72); ANALOG LFO: #ece8e2, cap 3.327mm → `s = 3.327/700 = 0.004753`, `Ty=14.600`, center-x 45.72. Compute each `Tx` from the inked bbox center to preserve the symmetric FORGE↔rune↔AUDIO lockup (Pitfall 5); expose the `tracking` knob in the recipe and tune to approach the original widths (FORGE 17.40 / AUDIO 17.21 / ANALOG LFO 49.92 mm) without forcing parity (Pitfall 6). Save both faces' three `<path>` strings to the scratch dir (e.g. `wordmark-bebas.svgfrag`, `wordmark-chakra.svgfrag`).

    Splice the **working default** — Chakra Petch Regular (research: closest horizontal footprint → least layout disruption pre-gate) — into lines 28/29/35 of res/AnalogLFO.svg, replacing ONLY those three lines. The forge-rune (30–34) and everything else stay byte-identical. Render BOTH candidates with the Playwright MCP to `after-chakra.png` and `after-bebas.png` (same recipe as `before.png`) so the operator can compare both against `before.png` at the gate. Then run the full regression gate: census (must be 0, across res/AnalogLFO.svg AND res/components/*.svg), XML well-formed, and `git diff` scope — confirm only the trial-derived text-run lines from Tasks 2–3 changed and the forge-rune is intact. (Optional belt-and-suspenders: rebuild against ../Rack-SDK per the vcv build workflow and confirm the panel loads with no nanosvg parse error.)
  </action>
  <verify>
    <automated>grep -rcE '<text|<tspan|<glyph|<font|@font-face|base64|<use ' res/AnalogLFO.svg res/components/*.svg | awk -F: '{s+=$2} END{print s}'</automated>
  </verify>
  <acceptance_criteria>
    - Both wordmark candidates exist as scratch path fragments; `after-chakra.png` and `after-bebas.png` rendered alongside `before.png`
    - The working default (Chakra Petch) is spliced at lines 28/29/35 with correct s (0.003890 header / 0.004753 title), Ty (7.600 / 14.600), centers (18.50 / 73.18 / 45.72), and original fills (#e8612a / #ece8e2)
    - Aggregate census across all res SVGs returns 0 (no font program anywhere)
    - SVG well-formed; viewBox unchanged; `git diff res/AnalogLFO.svg` touches ONLY the trial-derived text-run lines (Tasks 2 + 3) — forge-rune (30–34) unchanged in the diff
    - The wordmark reads in the same position/size/fill band as before (placement parity; character intentionally differs per Font Choice Analysis)
  </acceptance_criteria>
  <done>Both Bebas and Chakra wordmark re-cuts are generated and rendered; the Chakra default is spliced; the panel is path-only across all res SVGs, well-formed, with only text-run lines changed and the forge-rune intact — ready for the operator's face pick.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human acceptance + wordmark face pick + provenance close — record confirmed-OFL (IP-03), unblock 25-03</name>
  <files>res/AnalogLFO.svg (only if operator picks the non-default wordmark face — swap the 3 wordmark lines to the chosen scratch fragment); decision recorded in 25-04-SUMMARY.md + STATE.md</files>
  <action>Present before.png vs after-chakra.png vs after-bebas.png, the per-run triage table, and the recorded OFL font sources. Obtain (a) the wordmark FACE pick (Bebas Neue vs Chakra Petch), (b) visual acceptance of the regenerated panel, and (c) confirmation that every trial-derived run is now OFL-by-construction. If the operator picks Bebas (the non-default), swap lines 28/29/35 to the `wordmark-bebas.svgfrag` strings and re-run the census (==0) + well-formed + diff-scope gates before closing. Then write 25-04-SUMMARY.md (triage table + chosen wordmark face + every re-exported run's OFL source family/version/URL + visual-parity result + provenance `confirmed-OFL`) and mirror the decision into STATE.md. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    All 18 baked-text runs in res/AnalogLFO.svg were triaged: each was classified trial-font-derived (FoundationLogo-like) or already-OFL, with measured letterform evidence. Every trial-derived run was re-exported from a confirmed-OFL font — functional labels from the best-fit OFL face (JetBrains Mono / Chakra Petch), and the brand wordmark generated in BOTH Bebas Neue and Chakra Petch for your pick. The panel stays path-only (0 font programs across all res SVGs), the forge-rune (lines 30–34) and all non-text art are byte-untouched, and the SVG is well-formed. Provenance is now provable by construction, not by eye. The working file currently shows the Chakra Petch wordmark default.
  </what-built>
  <how-to-verify>
    1. Compare before.png vs after-chakra.png vs after-bebas.png — the forge-rune between FORGE and AUDIO is intact; functional labels read clearly; the wordmark differs in character (no OFL face matches the round-heavy original — provenance is the requirement, not pixel parity).
    2. Pick the wordmark face: Bebas Neue (documented brand fallback, more condensed) or Chakra Petch (closest footprint, techy/angular).
    3. Review the triage table — confirm every trial-derived run was re-exported and any already-OFL run left as-is is genuinely OFL (e.g. shipped JetBrains Mono).
    4. Confirm the recorded font sources are genuinely OFL 1.1 (Bebas Neue / Chakra Petch / JetBrains Mono from Google Fonts).
    5. Accept the result, or request adjustments (face, tracking, weight, placement) before closing.
  </how-to-verify>
  <resume-signal>Reply "confirmed-OFL: <bebas|chakra>" (wordmark accepted with the named face; every trial-derived outline now OFL-by-construction — IP-03 closed, 25-03 may proceed) OR "revise" (name the face/tracking/placement adjustment needed before acceptance).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFL source font → shipped outline | only license-clean OFL fonts (Bebas Neue / Chakra Petch / JetBrains Mono) may be the source of any regenerated text geometry |
| regenerated SVG → public GPL consumers | res/AnalogLFO.svg redistributes under GPL-3.0; ALL its text outlines must now provably derive from OFL fonts |
| triage classification → re-export queue | a run mis-classified as already-OFL would skip re-export and ship trial geometry |
| this plan → 25-03 | only a confirmed-OFL outcome here (recorded in 25-04-SUMMARY.md) authorizes the irreversible history purge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Tampering / wrong source | a run re-exported from a non-OFL font | mitigate | Task 1 records font family + version + OFL source URL + OFL-1.1 header confirmation; only Bebas Neue / Chakra Petch / JetBrains Mono OFL cuts are used as sources |
| T-25-15 | Information Disclosure | a font program slipping into the SVG | mitigate | Tasks 2–4 re-run the full census across res/AnalogLFO.svg AND res/components/*.svg and require 0 (path-only; no `<text>/<glyph>/<use>/@font-face/base64`) |
| T-25-16 | Tampering | collateral change to the forge-rune or non-text art | mitigate | Tasks 2–3 replace ONLY flagged text-run lines; `git diff` confirms forge-rune (30–34) + knobs/jacks/display/rails/bolts/gradients unchanged |
| T-25-17 | Repudiation | unrecorded provenance | mitigate | Task 4 records the per-run triage table + every re-exported run's OFL source + confirmed-OFL in SUMMARY + STATE.md for the Phase 28 audit trail |
| T-25-18 | Tampering | incomplete triage — a trial-derived run mis-classified already-OFL and left unre-exported | mitigate | Task 1 fingerprints EVERY run with measured evidence (F w/cap + O construction / monospace match); the SUMMARY table makes each keep-decision auditable; the goal is explicit — NO trial-FoundationLogo outline ships anywhere |
| T-25-19 | Tampering | wrong wordmark face shipped after the gate | mitigate | Task 4 splices the operator-named face and re-runs census + well-formed + diff-scope before recording confirmed-OFL |
| T-25-SC | Tampering | fontTools / svgpathtools install + OFL .ttf fetch | mitigate | Packages from PyPI into a throwaway venv (no system pollution, no postinstall); fonts only from `github.com/google/fonts` with OFL-1.1 headers verified; RESEARCH Package Legitimacy Audit verdict = Approved (no [ASSUMED]/[SUS]); no `.ttf` committed |
</threat_model>

<verification>
- Path-only: res/AnalogLFO.svg AND res/components/*.svg have 0 font/glyph/text/@font-face/base64/use nodes after regeneration
- Triage: all 18 runs classified with measured evidence; every trial-derived run re-exported; already-OFL keeps proven
- Provenance: every re-exported run's OFL source recorded (family + version + OFL URL); decision = confirmed-OFL with the chosen wordmark face
- Visual parity: before vs after renders equivalent in placement/fill band; forge-rune intact; only text-run lines changed in the diff
- Unblocks 25-03: IP-03 closed confirmed-OFL in 25-04-SUMMARY.md; 25-03's grep precondition satisfied
</verification>

<success_criteria>
- Every baked-text run triaged; every trial-FoundationLogo-derived run re-exported from a confirmed-OFL font; no trial-derived outline ships anywhere (IP-03)
- Brand wordmark generated in both Bebas Neue and Chakra Petch; operator-picked face shipped
- res/AnalogLFO.svg stays path-only (no font program), placement-parity, forge-rune + non-text art byte-preserved
- Per-run triage table + OFL font sources + confirmed-OFL decision recorded in SUMMARY + STATE.md
- 25-03 (irreversible history purge) is unblocked — its IP-03 precondition (confirmed-OFL in 25-04-SUMMARY.md) is now satisfied
</success_criteria>

<output>
Create `.planning/phases/25-release-ip-hardening-private/25-04-SUMMARY.md` when done — record the per-run triage table (run/line/string/fingerprint/classification/disposition), the chosen wordmark face, every re-exported run's OFL source (family/version/URL), the visual-parity result, and the confirmed-OFL provenance decision; mirror the decision into STATE.md.
</output>
</content>
</invoke>

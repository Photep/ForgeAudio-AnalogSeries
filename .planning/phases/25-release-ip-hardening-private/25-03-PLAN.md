---
phase: 25-release-ip-hardening-private
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02, 25-04]
files_modified: []
autonomous: false
requirements: [IP-02]

must_haves:
  truths:
    - "git-filter-repo is installed and on PATH before any purge step"
    - "The remote is confirmed PRIVATE immediately before any push (gh repo view isPrivate == true)"
    - "Local main is fully pushed to the remote BEFORE any history rewrite — remote == local HEAD (git rev-list origin/main..HEAD == 0)"
    - "Both trial-font blobs are purged from ALL branches and tags via git filter-repo --invert-paths run on a throwaway fresh clone (never the working repo)"
    - "A SECOND independent fresh clone of the remote returns EMPTY for grep -iE 'Barell|FoundationLogo' across --all and --tags"
    - "The two known blob OIDs (031e8db... and 3533f3e...) are unreachable in the rewritten remote"
    - "The local working repo is resynced to the rewritten origin/main with .planning work preserved"
  artifacts: []
  key_links:
    - from: "git filter-repo --invert-paths --path BCBarellTEST-Regular.otf --path FoundationLogo.ttf"
      to: "rewritten remote history (branches + tag v1.3)"
      via: "git push --force --all --tags after re-adding origin"
      pattern: "filter-repo --invert-paths"
    - from: "second fresh clone"
      to: "Phase 28 public-flip hard gate"
      via: "git rev-list --all --objects | grep -iE 'Barell|FoundationLogo' returns empty"
      pattern: "rev-list --all --objects"
---

<objective>
Permanently purge the two trial-font blobs from ALL git history on the private remote and PROVE they are gone via an independent clean-room re-clone (IP-02 — the hard gate Phase 28's public flip depends on). This is the IRREVERSIBLE half of Phase 25.

The load-bearing invariant: local `main` is ~77 commits AHEAD of `origin/main` (parked at v1.3 == 2cf7b59); the trial fonts exist on the remote too (commit e486ce1 is an ancestor) AND inside tag v1.3. The naive "clone remote → filter → force-push" recipe would ERASE all Phase 22-24 work from the remote. The sequence MUST be push-first: push local→remote, verify remote == local, THEN filter-repo on a throwaway clone, THEN force-push --all --tags, THEN verify on a second fresh clone, THEN resync local. The repo stays PRIVATE throughout.

Purpose: Make the remote history legally clean before the Phase 28 public flip.
Output: A rewritten, verified-clean remote; a resynced local repo; a recorded clean-room verification result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-release-ip-hardening-private/25-RESEARCH.md
@.planning/phases/25-release-ip-hardening-private/25-VALIDATION.md

<facts>
Verified live (2026-06-30):
- Remote: https://github.com/Photep/ForgeAudio-AnalogSeries.git (HTTPS, gh-authed as Photep), confirmed PRIVATE (gh repo view isPrivate == true).
- `git rev-list --count origin/main..HEAD` == 77 (divergence GREW from research's 75 — re-measure live before pushing; it may change again).
- Trial-font blob OIDs still reachable in history: 031e8db9c923a6071e910df0105ef245cd63aedb (BCBarellTEST-Regular.otf), 3533f3e4cffbe400f6126f1be4dc50c88cbc4649 (FoundationLogo.ttf). Introduced in e486ce1; present through HEAD AND inside tag v1.3.
- git-filter-repo is NOT installed (`git filter-repo` -> "not a git command"). brew has 2.47.0; fallback `pip3 install git-filter-repo`. git 2.39.5 + python 3.14.2 satisfy minimums.
- filter-repo REMOVES the origin remote by design — it must be re-added with the exact verified URL before force-pushing.
- This working repo is where GSD commits .planning/ docs (Pitfall 2) — after the rewrite the local SHAs diverge and the local repo must be resynced.

PRECONDITION: 25-01's removal+LICENSE+NOTICES commit must be committed locally, and the IP-03 provenance gate must read "confirmed-OFL". NOTE (2026-07-01): 25-02 returned "needs-regeneration", so the wordmark re-export plan 25-04 was added and MUST land + commit + close IP-03 as confirmed-OFL (see 25-04-SUMMARY.md) before this plan runs. Verify 25-04-SUMMARY.md exists and records "confirmed-OFL"; if it does not, STOP.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install filter-repo, confirm private, push-first, and prepare a verified-clean throwaway clone (all reversible — nothing forced to remote yet)</name>
  <files>(none in working tree — operates on a throwaway scratch clone; pushes existing local commits to origin)</files>
  <read_first>
    - .planning/phases/25-release-ip-hardening-private/25-RESEARCH.md (Pattern 1 safe purge sequence steps 0-3; Pitfall 1 the 75-commit cliff; Pitfall 2 self-corruption; Environment Availability)
    - .planning/phases/25-release-ip-hardening-private/25-VALIDATION.md (Manual-Only: push-first invariant; clean-room verification)
    - 25-04-SUMMARY.md (confirm the provenance decision is "confirmed-OFL" before proceeding; 25-02 returned needs-regeneration and 25-04 re-exported the wordmark from OFL fonts)
  </read_first>
  <action>
    Step A — TOOL: install git-filter-repo via `brew install git-filter-repo` (fallback `pip3 install git-filter-repo`), then confirm with `git filter-repo --version`.
    Step B — PRIVACY GATE: run `gh repo view Photep/ForgeAudio-AnalogSeries --json isPrivate` and require isPrivate==true; ABORT if false (premature exposure risk).
    Step C — RE-MEASURE: run `git rev-list --count origin/main..HEAD` and record the live ahead-count (expect ~77; do not assume).
    Step D — PUSH-FIRST (the invariant): with 25-01's removal commit on local main, run `git push origin main` then `git push origin --tags`. Then VERIFY the remote now equals local: `git rev-list --count origin/main..HEAD` must be 0 and `git rev-parse origin/main` must equal `git rev-parse HEAD`. This guarantees the throwaway clone in Step E contains ALL Phase 22-24 work — without this, the purge would erase 77 commits from the remote.
    Step E — PREPARE PURGE (throwaway clone, NOT the working repo): clone the remote fresh into a scratch dir under the session scratchpad (e.g. /tmp scratch), run `git filter-repo --invert-paths --path BCBarellTEST-Regular.otf --path FoundationLogo.ttf` inside it (rewrites ALL commits + tags), then `git remote add origin https://github.com/Photep/ForgeAudio-AnalogSeries.git` (filter-repo dropped it). Verify INSIDE this scratch clone that `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` is empty. DO NOT push yet — leave the verified-clean rewritten history staged in the scratch clone for the Task 2 go/no-go gate.
  </action>
  <verify>
    <automated>git filter-repo --version && gh repo view Photep/ForgeAudio-AnalogSeries --json isPrivate | grep -q true && test "$(git rev-list --count origin/main..HEAD)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `git filter-repo --version` prints a version (>= 2.47.0) — tool on PATH
    - `gh repo view Photep/ForgeAudio-AnalogSeries --json isPrivate` shows `true`
    - After push: `git rev-list --count origin/main..HEAD` == 0 AND `git rev-parse origin/main` == `git rev-parse HEAD` (remote == local, push-first satisfied)
    - In the scratch clone: `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` returns EMPTY (purge prepared and locally verified)
    - The scratch clone has the same commit count as local HEAD minus none (all 77+ commits present pre-filter; commit graph preserved post-filter)
    - NO force-push has occurred yet (remote history unchanged)
  </acceptance_criteria>
  <done>filter-repo installed; remote confirmed private; local fully pushed and remote==local (push-first invariant met); a throwaway clone holds a locally-verified-clean rewritten history, not yet pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Go/no-go gate before the IRREVERSIBLE force-push --all --tags</name>
  <files>(none — authorization gate only)</files>
  <action>Present the push-first + privacy + scratch-clean evidence from Task 1 and obtain explicit authorization (or abort) for the irreversible remote history rewrite. Perform NO push in this task. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    All reversible preparation for the irreversible purge is complete (Task 1): git-filter-repo installed; remote confirmed PRIVATE; local main + tags fully pushed so the remote == local HEAD (push-first invariant met — no commits will be lost); a throwaway clone has been rewritten with filter-repo and locally verified to contain ZERO trial-font blobs. The next step — force-push --all --tags — is IRREVERSIBLE: it rewrites every SHA on the remote, including tag v1.3.
  </what-built>
  <how-to-verify>
    1. Confirm push-first happened: `git rev-list --count origin/main..HEAD` returns 0 (remote already has every local commit — the rewrite will not lose Phase 22-24 work).
    2. Confirm the remote is still PRIVATE: `gh repo view Photep/ForgeAudio-AnalogSeries --json isPrivate` -> true.
    3. Confirm the scratch clone is clean: in the throwaway clone, `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` is empty.
    4. Understand the blast radius: the force-push rewrites all branches AND tags on the remote; the local working repo will need a hard-reset/re-clone afterward (Task 3 handles it).
    5. Authorize (or abort) the irreversible `git push --force --all --tags`.
  </how-to-verify>
  <resume-signal>Reply "force-push approved" to authorize the irreversible rewrite of the private remote, or "abort" with the reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the force-push, verify clean on a second independent clone, resync the local repo (IP-02 hard gate)</name>
  <files>(none in working tree — force-pushes the rewritten scratch clone; resyncs local main to rewritten origin/main)</files>
  <read_first>
    - .planning/phases/25-release-ip-hardening-private/25-RESEARCH.md (Pattern 1 steps 4-5; Code Examples "Verify the purge is complete"; Pitfall 5 stale-cache false pass; Runtime State Inventory "Local repo post-rewrite")
    - .planning/phases/25-release-ip-hardening-private/25-VALIDATION.md (clean-room re-clone is the Phase 28 hard gate)
  </read_first>
  <action>
    Step F — FORCE-PUSH (irreversible, only after the Task 2 gate): from the throwaway scratch clone (with origin re-added to the exact verified URL), run `git push --force --all origin` then `git push --force --tags origin`. This rewrites the remote's branches and tag v1.3, removing the blobs.
    Step G — CLEAN-ROOM VERIFY (the hard gate): clone the remote FRESH into a SECOND, brand-new scratch dir (never reuse the filter-repo clone — Pitfall 5). Inside it run `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` and require EMPTY. Belt-and-suspenders: for each blob OID 031e8db9c923a6071e910df0105ef245cd63aedb and 3533f3e4cffbe400f6126f1be4dc50c88cbc4649, run `git cat-file -e <oid>` and require it to report the object missing (GONE). Also confirm tag v1.3 still exists but no longer carries the fonts (`git ls-tree -r v1.3 | grep -iE 'Barell|FoundationLogo'` empty).
    Step H — RESYNC LOCAL: the working repo's SHAs now diverge from the rewritten remote (this repo holds .planning/ docs). Stash/copy any uncommitted .planning work, then `git fetch origin` and `git reset --hard origin/main` (or re-clone) so local main matches the rewritten remote; re-apply the preserved .planning work. Confirm `git status` shows local in sync with the rewritten origin/main.
  </action>
  <verify>
    <automated>FRESH=$(mktemp -d)/verify && git clone https://github.com/Photep/ForgeAudio-AnalogSeries.git "$FRESH" && cd "$FRESH" && ! (git rev-list --all --objects | grep -iE 'Barell|FoundationLogo')</automated>
  </verify>
  <acceptance_criteria>
    - In a SECOND independent fresh clone: `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` returns EMPTY (exit 1 from grep == PASS) — THE HARD GATE for Phase 28
    - `git cat-file -e 031e8db9c923a6071e910df0105ef245cd63aedb` reports the object MISSING, and likewise for 3533f3e4cffbe400f6126f1be4dc50c88cbc4649
    - `git ls-tree -r v1.3 | grep -iE 'Barell|FoundationLogo'` is EMPTY (tag v1.3 rewritten, fonts gone via --tags)
    - The fresh clone still contains all Phase 22-24 commits (commit count matches local pre-rewrite minus zero) — no work lost
    - Local working repo: `git rev-parse HEAD` == rewritten `origin/main`, `git status` clean/in-sync, preserved .planning work re-applied
    - `gh repo view ... --json isPrivate` is still `true` (repo never left private)
  </acceptance_criteria>
  <done>The remote history is permanently rewritten with both trial fonts purged from all branches and tag v1.3; an independent clean-room clone verifies EMPTY (hard gate for Phase 28); the local repo is resynced to the rewritten remote with .planning work intact; the repo stayed private throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working repo → private remote | force-push rewrites every SHA on the remote; a wrong-target or premature push is destructive |
| throwaway scratch clone → remote | the rewrite happens on a disposable clone, never on the working repo (which holds 77 unpushed-then-pushed commits + GSD .planning docs) |
| private remote → public (Phase 28) | the clean-room verification here is the hard gate that authorizes the eventual public flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Denial / data loss | 77 unpushed local commits (Phase 22-24) | mitigate | Push-first invariant (Task 1 Step D): push local→remote and verify `rev-list origin/main..HEAD == 0` BEFORE any rewrite; the throwaway clone then contains all work |
| T-25-05 | Tampering | wrong-repo / premature force-push | mitigate | filter-repo strips origin by design; re-add the EXACT verified HTTPS URL; blocking human gate (Task 2) authorizes the push only after push-first + privacy + scratch-clean are all confirmed |
| T-25-06 | Tampering | incomplete purge (tag v1.3 left carrying blobs) | mitigate | force-push uses `--all` AND `--tags`; Step G asserts `git ls-tree -r v1.3` is font-free and the blob OIDs are missing |
| T-25-07 | Information Disclosure | premature public exposure of trial fonts | mitigate | gh privacy gate before any push (Task 1 Step B) and re-confirmed at the end of Task 3; repo stays PRIVATE — public flip deferred to Phase 28 behind this verification |
| T-25-08 | Information Disclosure | false-positive verification on a stale/cached clone | mitigate | Verify ONLY on a SECOND brand-new fresh clone of the HTTPS remote (Pitfall 5), never the filter-repo working clone |
| T-25-13 | Information Disclosure | PAT/token leakage in push commands or logs | mitigate | Use the gh-managed HTTPS credential helper (already configured); never embed a PAT in any command or log line |
| T-25-SC | Tampering | git-filter-repo install (brew/PyPI) | mitigate | Install only from Homebrew or PyPI; git-filter-repo is authored by a Git core maintainer (newren) and endorsed in official git docs — RESEARCH Package Legitimacy Audit verdict: Approved (not [ASSUMED]/[SUS], no install checkpoint required) |
</threat_model>

<verification>
- Push-first: `git rev-list --count origin/main..HEAD` == 0 before rewrite
- Privacy: `gh repo view ... --json isPrivate` == true (before and after)
- Hard gate (IP-02): second fresh clone `git rev-list --all --objects | grep -iE 'Barell|FoundationLogo'` EMPTY
- Blob OIDs 031e8db... and 3533f3e... report missing
- Tag v1.3 rewritten and font-free
- Local resynced to rewritten origin/main, .planning work preserved
</verification>

<success_criteria>
- Trial fonts purged from ALL git history (branches + tag v1.3) on the still-private remote (IP-02)
- Clean-room re-clone verification returns EMPTY — the hard gate Phase 28 depends on
- No Phase 22-24 commits lost (push-first preserved them)
- Local working repo resynced to the rewritten remote with .planning docs intact
- Repo remained PRIVATE throughout the entire sequence
</success_criteria>

<output>
Create `.planning/phases/25-release-ip-hardening-private/25-03-SUMMARY.md` when done — record the live ahead-count, the verification grep result, the blob-OID checks, and the local resync method.
</output>

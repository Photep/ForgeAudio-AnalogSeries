---
phase: 25-release-ip-hardening-private
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BCBarellTEST-Regular.otf
  - FoundationLogo.ttf
  - .gitignore
  - LICENSE
  - NOTICES
  - res/fonts/OFL.txt
  - Makefile
autonomous: true
requirements: [IP-01, PKG-01, PKG-04]

must_haves:
  truths:
    - "BCBarellTEST-Regular.otf and FoundationLogo.ttf are absent from the working tree"
    - "Both trial fonts are gitignored (git check-ignore exits 0 for both)"
    - "A GPL-3.0 LICENSE file exists at repo root"
    - "NOTICES and res/fonts/OFL.txt exist and ship inside the make dist artifact"
    - "make test stays green (47/47) — no code regression from the docs/IP changes"
  artifacts:
    - path: "LICENSE"
      provides: "Verbatim GPL-3.0 license body"
      contains: "GNU GENERAL PUBLIC LICENSE"
      min_lines: 600
    - path: "NOTICES"
      provides: "Third-party asset license inventory (JetBrains Mono OFL, Bebas Neue + Chakra Petch outlines OFL, plugin GPL-3.0)"
      contains: "OFL"
    - path: "res/fonts/OFL.txt"
      provides: "SIL OFL 1.1 verbatim text shipped beside JetBrainsMonoNL-Regular.ttf"
      contains: "SIL OPEN FONT LICENSE"
    - path: ".gitignore"
      provides: "Ignore rules for the two trial fonts"
      contains: "FoundationLogo.ttf"
    - path: "Makefile"
      provides: "DISTRIBUTABLES glob that ships NOTICES"
      contains: "NOTICES"
  key_links:
    - from: "Makefile DISTRIBUTABLES"
      to: "dist/*.vcvplugin"
      via: "$(wildcard NOTICES*) glob"
      pattern: "DISTRIBUTABLES \\+= \\$\\(wildcard NOTICES"
    - from: "res/ DISTRIBUTABLES glob"
      to: "dist/*.vcvplugin res/fonts/OFL.txt"
      via: "res tree already in DISTRIBUTABLES"
      pattern: "DISTRIBUTABLES \\+= res"
---

<objective>
Make the working tree legally clean and complete for a public GPL-3.0 release: remove the two trial fonts and gitignore them (IP-01), add the verbatim GPL-3.0 LICENSE (PKG-01), and add a NOTICES inventory plus the SIL OFL 1.1 text shipped beside the bundled JetBrains Mono font (PKG-04). Wire NOTICES into the Makefile so `make dist` actually ships it.

Purpose: This is the reversible, code-tree-only half of Phase 25. It must land and be pushed BEFORE the irreversible history purge (25-03) so the corrected tree is what the purge preserves.
Output: LICENSE, NOTICES, res/fonts/OFL.txt at the correct paths; .gitignore + Makefile updated; trial fonts deleted; build + tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-release-ip-hardening-private/25-RESEARCH.md
@.planning/phases/25-release-ip-hardening-private/25-VALIDATION.md

<facts>
Verified live (2026-06-30):
- BCBarellTEST-Regular.otf (8576 B) and FoundationLogo.ttf (40916 B) are git-tracked at repo root, introduced in commit e486ce1, referenced by NO build/source/SVG file (mockup-only).
- Makefile already has: `DISTRIBUTABLES += res`, `DISTRIBUTABLES += $(wildcard LICENSE*)`, `DISTRIBUTABLES += $(wildcard presets)`. There is NO NOTICES glob — NOTICES will NOT ship unless added.
- plugin.json already declares `"license": "GPL-3.0-or-later"` and `"version": "2.0.0"` — do NOT modify plugin.json in this phase (PKG-02/03 are Phase 26).
- Only shipped runtime font is res/fonts/JetBrainsMonoNL-Regular.ttf (OFL 1.1). No OFL.txt exists yet.
- .gitignore currently has build/dist/OS entries only — no font entries.
- Build/install workflow uses a relative `../Rack-SDK` (per MEMORY.md). `make test` is Rack-free and must stay 47/47 green.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove trial fonts from the working tree and gitignore them (IP-01)</name>
  <files>BCBarellTEST-Regular.otf, FoundationLogo.ttf, .gitignore</files>
  <read_first>
    - BCBarellTEST-Regular.otf, FoundationLogo.ttf (confirm both exist at repo root)
    - .gitignore (current contents — append, do not rewrite)
    - .planning/phases/25-release-ip-hardening-private/25-RESEARCH.md (Code Examples: ".gitignore additions (IP-01)" and "IP-01 removal commit")
  </read_first>
  <action>
    Run `git rm BCBarellTEST-Regular.otf FoundationLogo.ttf` to delete and stage removal of both trial fonts. Append a new block to the END of .gitignore containing exactly the two lines `BCBarellTEST-Regular.otf` and `FoundationLogo.ttf` under a one-line comment marking them as trial/mockup-only fonts purged in Phase 25. Do NOT touch res/fonts/JetBrainsMonoNL-Regular.ttf — it is the legitimate OFL runtime font and must stay. This task is reversible (no history rewrite yet); the purge of these blobs from history happens in 25-03.
  </action>
  <verify>
    <automated>! test -f BCBarellTEST-Regular.otf && ! test -f FoundationLogo.ttf && git check-ignore BCBarellTEST-Regular.otf FoundationLogo.ttf</automated>
  </verify>
  <acceptance_criteria>
    - `test -f BCBarellTEST-Regular.otf` is FALSE and `test -f FoundationLogo.ttf` is FALSE (both removed from working tree)
    - `git check-ignore BCBarellTEST-Regular.otf FoundationLogo.ttf` exits 0 and echoes both names
    - `git status --porcelain` shows both fonts staged as deletions (D)
    - res/fonts/JetBrainsMonoNL-Regular.ttf still exists (untouched)
  </acceptance_criteria>
  <done>Both trial fonts deleted from the working tree, staged for removal, and gitignored; the OFL runtime font is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add GPL-3.0 LICENSE, NOTICES inventory, and OFL.txt; wire NOTICES into DISTRIBUTABLES (PKG-01, PKG-04)</name>
  <files>LICENSE, NOTICES, res/fonts/OFL.txt, Makefile</files>
  <read_first>
    - Makefile (lines 13-15 DISTRIBUTABLES block — add the NOTICES glob immediately after the LICENSE glob; do not disturb the include of $(RACK_DIR)/plugin.mk)
    - res/fonts/ (confirm JetBrainsMonoNL-Regular.ttf is the only font; OFL.txt belongs beside it)
    - plugin.json (confirm license string is GPL-3.0-or-later — informational, do NOT edit)
    - .planning/phases/25-release-ip-hardening-private/25-RESEARCH.md (Don't Hand-Roll table; Pitfall 3 NOTICES-not-shipped; State of the Art OFL outline finding)
  </read_first>
  <action>
    Create LICENSE at repo root containing the VERBATIM FSF GPL-3.0 text (fetch from https://www.gnu.org/licenses/gpl-3.0.txt — do NOT paraphrase or edit; editing voids the grant). Create res/fonts/OFL.txt containing the VERBATIM SIL Open Font License 1.1 text (the same OFL.txt that travels with JetBrains Mono; fetch from the official SIL OFL 1.1 text). Create NOTICES at repo root: a human-readable inventory crediting (1) the plugin code under GPL-3.0-or-later, (2) JetBrains Mono NL (res/fonts/JetBrainsMonoNL-Regular.ttf) under SIL OFL 1.1 with a pointer to res/fonts/OFL.txt, and (3) the FORGE/AUDIO + ANALOG LFO wordmark vector outlines in res/AnalogLFO.svg as OFL-embedded outlines of Bebas Neue (OFL 1.1) and Chakra Petch (OFL 1.1) — note OFL permits outline embedding and the outlines are not subject to OFL redistribution restrictions. Edit the Makefile to add the line `DISTRIBUTABLES += $(wildcard NOTICES*)` immediately after the existing `DISTRIBUTABLES += $(wildcard LICENSE*)` line. OFL.txt ships automatically via the existing `DISTRIBUTABLES += res` glob (it lives under res/fonts/), so no extra Makefile glob is needed for it.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "GNU GENERAL PUBLIC LICENSE" LICENSE && test -f res/fonts/OFL.txt && grep -qi "SIL OPEN FONT LICENSE" res/fonts/OFL.txt && test -f NOTICES && grep -qi "OFL" NOTICES && grep -q 'DISTRIBUTABLES += $(wildcard NOTICES' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` is TRUE and `grep -q "GNU GENERAL PUBLIC LICENSE" LICENSE` matches and `grep -q "Version 3" LICENSE` matches
    - `test -f res/fonts/OFL.txt` is TRUE and `grep -qi "SIL OPEN FONT LICENSE" res/fonts/OFL.txt` matches
    - `test -f NOTICES` is TRUE and NOTICES names JetBrains Mono (OFL), Bebas Neue, Chakra Petch, and GPL-3.0
    - `grep -q 'DISTRIBUTABLES += $(wildcard NOTICES' Makefile` matches, positioned within the DISTRIBUTABLES block
    - plugin.json is unchanged (no diff)
  </acceptance_criteria>
  <done>LICENSE (verbatim GPLv3), NOTICES (full third-party inventory), and res/fonts/OFL.txt (verbatim OFL 1.1) exist; Makefile ships NOTICES via a new wildcard glob.</done>
</task>

<task type="auto">
  <name>Task 3: Build + dist sanity — code untouched, LICENSE/NOTICES/OFL ship, no trial fonts in dist (regression + PKG-01/PKG-04 gate)</name>
  <files>Makefile (no edit — verification only), dist/*.vcvplugin (build output)</files>
  <read_first>
    - Makefile (DISTRIBUTABLES block + the `test` target — confirm `make test` is Rack-free)
    - .planning/phases/25-release-ip-hardening-private/25-VALIDATION.md (Per-Task Verification Map: PKG-01 and PKG-04 dist probes)
    - $HOME/.claude/projects/-Users-mrcbrown-Claude-Software-Forge-Audio-Analog-Series/memory/vcv_build_install_workflow.md (relative ../Rack-SDK build, stale-install flush conventions)
  </read_first>
  <action>
    Run `make test` and confirm it stays green at 47/47 (these are docs/IP changes only — no code touched, so the suite must not regress). Then run `make dist` against the relative ../Rack-SDK to produce the .vcvplugin artifact, and verify the zip contents: LICENSE present, NOTICES present, res/fonts/OFL.txt present, and NEITHER trial font present. If ../Rack-SDK is unavailable in the execution environment, fall back to asserting the source-tree state (LICENSE/NOTICES/OFL.txt exist, Makefile globs them, trial fonts gone) and explicitly record in the SUMMARY that the dist-zip assertion was deferred to the operator's local build — do NOT silently skip it.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `make test` reports all cases passing (47/47, unchanged count) — exit code 0
    - `make dist` succeeds and `unzip -l dist/*.vcvplugin | grep -q LICENSE` matches
    - `unzip -l dist/*.vcvplugin | grep -qi 'OFL\|NOTICES'` matches (NOTICES and OFL.txt shipped)
    - `unzip -l dist/*.vcvplugin | grep -iE 'Barell|FoundationLogo'` returns EMPTY (no trial font in dist)
    - If ../Rack-SDK absent: SUMMARY explicitly records the dist assertion as operator-deferred (not skipped)
  </acceptance_criteria>
  <done>`make test` stays 47/47 green; `make dist` ships LICENSE + NOTICES + OFL.txt and contains no trial fonts (or the dist check is explicitly operator-deferred with source-tree state asserted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → dist artifact | `make dist` selects which files become the shipped, publicly-redistributable `.vcvplugin` |
| repo → public GPL consumers | files committed here will eventually ship under GPL-3.0 to end users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | NOTICES / dist contents | mitigate | NOTICES inventories every shipped third-party asset (JetBrains Mono OFL, Bebas/Chakra outlines); Task 3 asserts the OFL text ships beside the font so attribution travels with the binary |
| T-25-02 | Tampering | LICENSE / OFL.txt body | mitigate | Both texts fetched VERBATIM from authoritative sources (gnu.org GPLv3, SIL OFL 1.1); editing voids the grant — acceptance greps for canonical header strings |
| T-25-09 | Denial / data loss | trial-font deletion | accept | `git rm` is fully reversible until 25-03; the fonts are referenced by no build/source/SVG (verified), so deletion cannot break the build — Task 3 `make test` confirms |
| T-25-10 | Tampering | shipping a non-redistributable font in dist | mitigate | Task 3 greps the dist zip for `Barell|FoundationLogo` and requires EMPTY before the artifact is considered clean |
</threat_model>

<verification>
- IP-01: `! test -f BCBarellTEST-Regular.otf && ! test -f FoundationLogo.ttf && git check-ignore BCBarellTEST-Regular.otf FoundationLogo.ttf`
- PKG-01: `test -f LICENSE && grep -q "GNU GENERAL PUBLIC LICENSE" LICENSE`
- PKG-04: `test -f NOTICES && test -f res/fonts/OFL.txt && grep -q 'DISTRIBUTABLES += $(wildcard NOTICES' Makefile`
- Regression: `make test` 47/47 green
- Dist gate: `unzip -l dist/*.vcvplugin` contains LICENSE + NOTICES + OFL.txt, contains no trial fonts
</verification>

<success_criteria>
- Trial fonts removed from the working tree and gitignored (IP-01)
- GPL-3.0 LICENSE at repo root, shipped by DISTRIBUTABLES (PKG-01)
- NOTICES + res/fonts/OFL.txt present and shipped via `make dist` (PKG-04)
- Plugin builds and `make test` stays 47/47 green (no regression)
- This plan's removal + additions are committed and ready to be pushed to the remote BEFORE 25-03's purge
</success_criteria>

<output>
Create `.planning/phases/25-release-ip-hardening-private/25-01-SUMMARY.md` when done.
</output>

---
phase: 01-plugin-scaffold-and-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - plugin.json
  - .gitignore
  - src/plugin.hpp
  - src/plugin.cpp
  - src/AnalogLFO.cpp
  - res/AnalogLFO.svg
autonomous: true
requirements:
  - INFR-01
  - PANL-01

must_haves:
  truths:
    - "Plugin compiles with `make` on macOS without errors"
    - "`make install` copies plugin to VCV Rack 2 plugins directory"
    - "SVG panel renders at 12HP (60.96mm) width with deep navy background and amber accents"
    - "Module registered as ForgeAnalogLFO with Forge Audio brand in plugin.json"
  artifacts:
    - path: "Makefile"
      provides: "Build configuration pointing to Rack SDK"
      contains: "include $(RACK_DIR)/plugin.mk"
    - path: "plugin.json"
      provides: "Plugin and module metadata"
      contains: "ForgeAnalogLFO"
    - path: "src/plugin.hpp"
      provides: "Shared declarations for plugin instance and model"
      exports: ["pluginInstance", "modelAnalogLFO"]
    - path: "src/plugin.cpp"
      provides: "Plugin init with model registration"
      contains: "addModel(modelAnalogLFO)"
    - path: "src/AnalogLFO.cpp"
      provides: "Module struct with placeholder process(), Widget with panel and component positions"
      contains: "createModel"
    - path: "res/AnalogLFO.svg"
      provides: "12HP branded panel with layout zones and hidden components layer"
      contains: "width=\"60.96mm\""
  key_links:
    - from: "src/plugin.hpp"
      to: "src/plugin.cpp"
      via: "extern declarations matched by definitions"
      pattern: "extern Model\\* modelAnalogLFO"
    - from: "src/plugin.cpp"
      to: "src/AnalogLFO.cpp"
      via: "addModel references model defined in AnalogLFO.cpp"
      pattern: "addModel\\(modelAnalogLFO\\)"
    - from: "src/AnalogLFO.cpp"
      to: "res/AnalogLFO.svg"
      via: "createPanel loads SVG by asset path"
      pattern: "asset::plugin.*AnalogLFO\\.svg"
    - from: "plugin.json"
      to: "src/AnalogLFO.cpp"
      via: "module slug must match createModel string exactly"
      pattern: "ForgeAnalogLFO"
---

<objective>
Create a buildable VCV Rack 2 plugin scaffold with a branded 12HP SVG panel for the Forge Audio Analog LFO module.

Purpose: Establishes the complete build pipeline (Makefile, plugin registration, module skeleton) and the visual foundation (branded SVG panel with the diamond layout) that all future phases build upon. After this plan, the plugin compiles, installs, and loads as a visible module in VCV Rack.

Output: 7 files forming a complete, buildable VCV Rack 2 plugin with branded panel.
</objective>

<execution_context>
@/Users/mrcbrown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrcbrown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffold-and-panel/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin scaffold files</name>
  <files>
    Makefile
    plugin.json
    .gitignore
    src/plugin.hpp
    src/plugin.cpp
    src/AnalogLFO.cpp
  </files>
  <action>
Create the VCV Rack 2 plugin scaffold following the 3-file registration chain pattern validated in the POC at `/Users/mrcbrown/Claude/Software/Forge Audio/LFO/`.

**Makefile:**
```makefile
RACK_DIR ?= ../Rack-SDK

FLAGS +=
CFLAGS +=
CXXFLAGS +=
LDFLAGS +=

SOURCES += $(wildcard src/*.cpp)

DISTRIBUTABLES += res
DISTRIBUTABLES += $(wildcard LICENSE*)
DISTRIBUTABLES += $(wildcard presets)

include $(RACK_DIR)/plugin.mk
```

**plugin.json** — Use these exact values:
- slug: `"ForgeAudio-AnalogSeries"`
- name: `"Forge Audio - Analog Series"`
- version: `"2.0.0"`
- license: `"GPL-3.0-or-later"`
- brand: `"Forge Audio"`
- author: `"Forge Audio"`
- modules array with single entry:
  - slug: `"ForgeAnalogLFO"` (MUST match createModel string exactly)
  - name: `"Analog LFO"`
  - description: `"Sub-audio oscillator with waveform morphing, analog character, and drift modeling"`
  - tags: `["Low-frequency oscillator", "Waveshaper"]`

**.gitignore** — Exclude build artifacts:
```
build/
dep/
dist/
plugin.so
plugin.dylib
plugin.dll
*.d
*.o
.DS_Store
Thumbs.db
```

**src/plugin.hpp:**
```cpp
#pragma once
#include <rack.hpp>
using namespace rack;
extern Plugin* pluginInstance;
extern Model* modelAnalogLFO;
```

**src/plugin.cpp:**
```cpp
#include "plugin.hpp"
Plugin* pluginInstance;
void init(Plugin* p) {
    pluginInstance = p;
    p->addModel(modelAnalogLFO);
}
```

**src/AnalogLFO.cpp** — Module with placeholder enums for ALL controls that will exist in the final module (future phases will implement them). This is NOT a minimal stub -- it declares the complete parameter/input/output set so the widget can place all components on the panel now.

Enum members (use these exact names and order):
- ParamId: MORPH_PARAM, CHARACTER_PARAM, DRIFT_PARAM, RATE_PARAM, OCTAVE_PARAM, PARAMS_LEN
- InputId: MORPH_CV_INPUT, DRIFT_CV_INPUT, INPUTS_LEN
- OutputId: OUTPUT, INV_OUTPUT, OUTPUTS_LEN
- LightId: LIGHTS_LEN

In the constructor, call `config()` with the lengths, then:
- `configParam(MORPH_PARAM, 0.f, 1.f, 0.f, "Morph")` — Morph knob, 0-1 range
- `configParam(CHARACTER_PARAM, 0.f, 1.f, 0.f, "Character")` — Character knob, 0-1 range
- `configParam(DRIFT_PARAM, 0.f, 1.f, 0.f, "Drift")` — Drift knob, 0-1 range
- `configParam(RATE_PARAM, -8.f, 4.f, -3.f, "Rate", " Hz", 2.f, 1.f)` — Exponential rate, default ~0.125Hz
- `configParam(OCTAVE_PARAM, -4.f, 4.f, 0.f, "Octave")` — Octave offset
- `configInput(MORPH_CV_INPUT, "Morph CV")`
- `configInput(DRIFT_CV_INPUT, "Drift CV")`
- `configOutput(OUTPUT, "LFO")`
- `configOutput(INV_OUTPUT, "Inverted LFO")`

The `process()` method should be empty with a comment: `// DSP added in Phase 2`

**AnalogLFOWidget** constructor:
- `setModule(module)`
- `setPanel(createPanel(asset::plugin(pluginInstance, "res/AnalogLFO.svg")))`
- Add 4 corner screws using `ScrewSilver`
- Add all 5 params using `createParamCentered` with `mm2px()`:
  - MORPH_PARAM: `RoundBigBlackKnob` at center (30.48, 52.0)
  - CHARACTER_PARAM: `RoundLargeBlackKnob` at (18.0, 69.0)
  - DRIFT_PARAM: `RoundLargeBlackKnob` at (42.96, 69.0)
  - RATE_PARAM: `RoundBlackKnob` at (18.0, 86.0)
  - OCTAVE_PARAM: `RoundBlackKnob` at (42.96, 86.0)
- Add 2 inputs using `createInputCentered` with `PJ301MPort` and `mm2px()`:
  - MORPH_CV_INPUT at (11.0, 104.0)
  - DRIFT_CV_INPUT at (24.5, 104.0)
- Add 2 outputs using `createOutputCentered` with `PJ301MPort` and `mm2px()`:
  - OUTPUT at (38.0, 104.0)
  - INV_OUTPUT at (51.5, 104.0)

Final line: `Model* modelAnalogLFO = createModel<AnalogLFO, AnalogLFOWidget>("ForgeAnalogLFO");`

CRITICAL: The string `"ForgeAnalogLFO"` in createModel MUST exactly match the module slug in plugin.json. Verify both after writing.
  </action>
  <verify>
Run: `cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series" && grep -c "ForgeAnalogLFO" plugin.json src/AnalogLFO.cpp`
Both files should return 1 match each, confirming slug consistency.
  </verify>
  <done>
All 6 scaffold files exist with correct content. Module slug "ForgeAnalogLFO" matches between plugin.json and AnalogLFO.cpp createModel call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create branded 12HP SVG panel</name>
  <files>
    res/AnalogLFO.svg
  </files>
  <action>
Create the SVG panel at `res/AnalogLFO.svg` with these exact requirements:

**SVG root element:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="60.96mm" height="128.5mm"
     viewBox="0 0 60.96 128.5">
```

All dimensions in millimeters. 12HP = 60.96mm wide, 128.5mm tall (standard 3U Eurorack).

**Visual elements (top to bottom):**

1. **Background:** Full panel rect, fill `#1a1a2e` (deep navy)

2. **Panel border:** Inset rect (0.15mm margins), no fill, stroke `#2d2d4a`, stroke-width 0.3

3. **Top amber accent stripe:** Rect at y=0, full width, 1mm tall, fill `#e8a838`

4. **Brand text "FORGE AUDIO":** Path elements (NOT `<text>`) in muted lavender `#8888aa`, small all-caps, centered at approximately y=6mm. Create simple geometric letterforms from rectangles and lines — each letter approximately 2.5mm tall, 0.4mm stroke width. Letters: F-O-R-G-E (space) A-U-D-I-O.

5. **Module name "ANALOG LFO":** Path elements in bright white-gray `#e0e0e0`, centered at approximately y=12mm. Larger than brand text — each letter approximately 3.5mm tall, 0.5mm stroke width. Letters: A-N-A-L-O-G (space) L-F-O.

6. **Amber decorative line:** Horizontal line under module name at y=14.5mm, from x=10mm to x=51mm, stroke `#e8a838`, stroke-width 0.3

7. **Waveform display area:** Rounded rect from approximately (4.5, 17) to (56.5, 42), fill `#0d0d1a` (slightly darker than panel), rx=1.5. This reserves the generous display space (~25mm tall, nearly full width). Thin amber border: stroke `#e8a838`, stroke-width 0.2, stroke-opacity 0.3.

8. **Control labels** (ALL as path elements, NOT `<text>`):
   - "MORPH" centered at y=46mm, fill `#c0c0d0` (light lavender-gray), letter height ~2mm
   - "CHARACTER" centered-left at x=18mm, y=62.5mm, fill `#c0c0d0`, letter height ~1.8mm
   - "DRIFT" centered-right at x=43mm, y=62.5mm, fill `#c0c0d0`, letter height ~1.8mm
   - "RATE" centered-left at x=18mm, y=79.5mm, fill `#c0c0d0`, letter height ~1.8mm
   - "OCTAVE" centered-right at x=43mm, y=79.5mm, fill `#c0c0d0`, letter height ~1.8mm

9. **Section divider:** Thin amber line at y=94mm, from x=8mm to x=53mm, stroke `#e8a838`, stroke-width 0.15, stroke-opacity 0.5

10. **Jack labels** (as path elements):
    - "MCV" at x=11mm, y=97mm, fill `#8888aa`, letter height ~1.5mm
    - "DCV" at x=24.5mm, y=97mm, fill `#8888aa`, letter height ~1.5mm
    - "OUT" at x=38mm, y=97mm, fill `#8888aa`, letter height ~1.5mm
    - "INV" at x=51.5mm, y=97mm, fill `#8888aa`, letter height ~1.5mm

11. **Bottom brand text "FORGE AUDIO":** Same as top brand, centered at approximately y=116mm, fill `#8888aa`

12. **Bottom amber accent stripe:** Rect at y=127.5, full width, 1mm tall, fill `#e8a838`

**Hidden components layer** (for documentation and designer handoff):
```xml
<g id="components" style="display:none">
  <!-- Params (red circles at center positions) -->
  <circle cx="30.48" cy="52.0" r="7.62" fill="#ff0000"/>   <!-- Morph: RoundBigBlackKnob -->
  <circle cx="18.0" cy="69.0" r="6.1" fill="#ff0000"/>     <!-- Character: RoundLargeBlackKnob -->
  <circle cx="42.96" cy="69.0" r="6.1" fill="#ff0000"/>    <!-- Drift: RoundLargeBlackKnob -->
  <circle cx="18.0" cy="86.0" r="4.8" fill="#ff0000"/>     <!-- Rate: RoundBlackKnob -->
  <circle cx="42.96" cy="86.0" r="4.8" fill="#ff0000"/>    <!-- Octave: RoundBlackKnob -->
  <!-- Inputs (green circles) -->
  <circle cx="11.0" cy="104.0" r="4.0" fill="#00ff00"/>    <!-- Morph CV -->
  <circle cx="24.5" cy="104.0" r="4.0" fill="#00ff00"/>    <!-- Drift CV -->
  <!-- Outputs (blue circles) -->
  <circle cx="38.0" cy="104.0" r="4.0" fill="#0000ff"/>    <!-- Out -->
  <circle cx="51.5" cy="104.0" r="4.0" fill="#0000ff"/>    <!-- Inv Out -->
</g>
```

**CRITICAL nanosvg constraints:**
- NO `<text>` elements anywhere (nanosvg silently ignores them)
- NO CSS `<style>` blocks (use inline attributes only)
- NO `<use>`, `<clipPath>`, `<filter>`, `<image>` elements
- NO opacity on `<g>` groups (set on individual elements)
- All colors as 6-digit hex
- All text rendered as `<path>` elements with geometric letterforms

**Approach for text-as-paths:** Create simple geometric/block letterforms using `<path>` elements with `d` attribute. Each letter is a series of line segments (M, L, H, V commands). The letters do not need to be typographically perfect -- clean geometric block letters that read clearly at panel scale. Alternatively, use `<rect>` and `<line>` combinations grouped per letter. The POC panel at `/Users/mrcbrown/Claude/Software/Forge Audio/LFO/res/LFO.svg` demonstrates this approach -- reference it for the letter construction pattern.

The component positions in the hidden layer MUST match the mm2px coordinates used in AnalogLFO.cpp's widget constructor. Radii should be half the component diameter (RoundBigBlackKnob ~15.24mm dia = r=7.62, RoundLargeBlackKnob ~12.19mm = r=6.1, RoundBlackKnob ~9.60mm = r=4.8, PJ301MPort ~8.03mm = r=4.0).
  </action>
  <verify>
Verify SVG structure:
1. `grep -c '<text' res/AnalogLFO.svg` should return 0 (no text elements)
2. `grep 'width="60.96mm"' res/AnalogLFO.svg` should match (correct dimensions)
3. `grep 'height="128.5mm"' res/AnalogLFO.svg` should match
4. `grep 'id="components"' res/AnalogLFO.svg` should match (hidden layer exists)
5. `grep '#1a1a2e' res/AnalogLFO.svg` should match (navy background)
6. `grep '#e8a838' res/AnalogLFO.svg` should match (amber accents)
  </verify>
  <done>
SVG panel file exists at res/AnalogLFO.svg with correct 12HP dimensions, Forge Audio brand colors, diamond layout zones, all text as path elements, and hidden components layer matching widget positions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build, install, and verify plugin loads</name>
  <files></files>
  <action>
Build and install the plugin, then verify it loads without errors.

**Step 1: Build**
```bash
cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series"
export RACK_DIR="/Users/mrcbrown/Claude/Software/Forge Audio/Rack-SDK"
make
```

If build fails, diagnose and fix:
- Missing include: Check plugin.hpp `#include <rack.hpp>` and RACK_DIR path
- Slug mismatch: Verify "ForgeAnalogLFO" matches in plugin.json and AnalogLFO.cpp
- Syntax errors: Fix C++ issues in the source files

**Step 2: Install**
```bash
make install
```

This copies the built plugin to `~/Library/Application Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/`.

**Step 3: Verify installation**
Check the installed files exist:
```bash
ls -la ~/Library/Application\ Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/
```

Should contain: plugin.dylib (or plugin.so), plugin.json, res/ directory with AnalogLFO.svg.

**Step 4: Check Rack log for errors (dry run)**
If VCV Rack is not running, this step confirms the build artifacts are correct. The actual visual verification happens in Plan 02.

Check the plugin.json is valid JSON:
```bash
python3 -c "import json; json.load(open('plugin.json'))"
```

**If build succeeds:** The plugin is ready for visual verification in VCV Rack (Plan 02).
**If build fails:** Fix the errors and rebuild. Common issues: RACK_DIR path with spaces needs quoting, missing semicolons in C++, wrong include paths.
  </action>
  <verify>
1. `make` completes with exit code 0 (no compilation errors)
2. `make install` completes with exit code 0
3. `ls ~/Library/Application\ Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/plugin.json` exists
4. `python3 -c "import json; json.load(open('plugin.json'))"` exits cleanly
  </verify>
  <done>
Plugin compiles without errors, installs to VCV Rack plugins directory, and plugin.json validates as correct JSON. Build pipeline is verified end-to-end.
  </done>
</task>

</tasks>

<verification>
1. All 7 files exist: Makefile, plugin.json, .gitignore, src/plugin.hpp, src/plugin.cpp, src/AnalogLFO.cpp, res/AnalogLFO.svg
2. `make` compiles without errors
3. `make install` copies plugin to Rack plugins directory
4. Module slug "ForgeAnalogLFO" is consistent between plugin.json and createModel call
5. SVG has no `<text>` elements, correct dimensions (60.96mm x 128.5mm), brand colors present
6. Hidden components layer positions match widget constructor mm2px coordinates
</verification>

<success_criteria>
- Plugin compiles on macOS with `make` (exit code 0, no errors)
- Plugin installs with `make install` to VCV Rack plugins directory
- Module slug "ForgeAnalogLFO" matches between plugin.json and source code
- SVG panel is 12HP (60.96mm x 128.5mm) with navy background, amber accents, all text as paths
- SVG has hidden components layer with positions matching C++ widget code
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffold-and-panel/01-01-SUMMARY.md`
</output>

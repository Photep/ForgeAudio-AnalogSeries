---
phase: 19-forge-noir-panel-custom-components
plan: 04
type: execute
wave: 3
depends_on:
  - 19-03
files_modified: []
autonomous: false
requirements:
  - PANEL-01
  - PANEL-02
  - PANEL-03
  - PANEL-04
  - PANEL-05
  - PANEL-06
  - PANEL-07

must_haves:
  truths:
    - "Module loads in VCV Rack as 14HP with Forge Noir color scheme"
    - "All controls are interactive (knobs rotate, cables attach to jacks)"
    - "Three distinct knob sizes are visible"
    - "Trimpots are visually distinct from knobs (brighter metallic)"
    - "Output jack has ember accent ring; input jacks do not"
    - "Brand text and module name are readable"
    - "Forge emblem is visible but subtle"
  artifacts: []
  key_links: []
---

<objective>
Install the built module into VCV Rack and verify all visual and interactive elements of the Forge Noir panel work correctly. This is the visual verification checkpoint where the user confirms the module renders as designed.

Purpose: SVG rendering in nanosvg can differ from expectations (opacity tuning, gradient rendering, element visibility). This checkpoint catches visual issues before the phase is marked complete.

Output: User approval that the Forge Noir panel renders correctly in VCV Rack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-forge-noir-panel-custom-components/19-UI-SPEC.md
@.planning/phases/19-forge-noir-panel-custom-components/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install module into VCV Rack</name>
  <files></files>
  <read_first>
    .planning/phases/19-forge-noir-panel-custom-components/19-03-SUMMARY.md (confirm build succeeded)
  </read_first>
  <action>
Run `make install` to install the plugin into VCV Rack's local plugin directory. This copies the built .dylib and res/ directory to the Rack plugins folder. Verify the command exits successfully.
  </action>
  <verify>
    <automated>make install 2>&1 | tail -3 && echo "INSTALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - `make install` exits with code 0
  </acceptance_criteria>
  <done>Module installed into VCV Rack plugin directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and interaction verification in VCV Rack</name>
  <files></files>
  <action>
Present the user with the verification checklist below. The user must launch VCV Rack, add the Forge Audio Analog LFO module, and visually confirm all elements of the Forge Noir panel render correctly. Wait for user response.

Verification checklist:
1. Panel is 14HP wide with near-black (#0c0c0c) background
2. Ember gradient accent bars at top and bottom edges
3. "FORGE" (left) and "AUDIO" (right) brand text in ember orange
4. Forge rune glyph visible between FORGE and AUDIO
5. "ANALOG LFO" module name in warm white below brand
6. MORPH knob is largest (hero size), centered
7. CHARACTER and DRIFT knobs medium size, flanking left/right
8. RATE and PHASE knobs smallest (utility size)
9. All 5 trimpots visually brighter than dark knobs
10. 5 CV input jacks below trimpots with NO ember accent ring
11. CLK, RST (input jacks) and OUTPUT (larger jack WITH ember accent ring) at bottom
12. Hex bolt screws at 4 corners
13. Forge emblem faintly visible behind knob section
14. Morph arc (dashed circle) around MORPH knob
15. Knobs rotate with drag, cables attach to jacks, right-click shows parameter menu
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires user visual verification in VCV Rack"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms "approved" or provides list of issues to fix
  </acceptance_criteria>
  <done>User has visually verified the Forge Noir panel in VCV Rack and approved or provided feedback.</done>
</task>

</tasks>

<verification>
- Module loads in VCV Rack without crashes
- All visual elements from the Forge Noir design are present and correctly rendered
- All controls are interactive
- User approves the visual result
</verification>

<success_criteria>
User confirms the Forge Noir panel renders correctly in VCV Rack with all controls interactive, correct visual hierarchy (3 knob sizes, bright trimpots, ember output jack), and all decorative elements visible.
</success_criteria>

<output>
After completion, create `.planning/phases/19-forge-noir-panel-custom-components/19-04-SUMMARY.md`
</output>

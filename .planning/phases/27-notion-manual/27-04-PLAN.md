---
phase: 27-notion-manual
plan: 04
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - docs/panel.md
  - docs/img/panel-raw.png
  - docs/img/panel-annotated.png
  - panel-overlay.html
autonomous: false
requirements: [DOC-02]
must_haves:
  truths:
    - "D-08: The annotated-panel section uses a REAL VCV Rack screenshot of the shipped current-build ForgeAnalogLFO — not the design-mockup PNGs, not the SVG export"
    - "D-08: 19 numbered callouts are baked into the PNG, keyed 1:1 to a legend table that DOUBLES as the control-reference table"
    - "D-03: The annotated PNG lives under docs/img/ and is referenced from panel.md by a relative path"
    - "The screenshot is captured from a current build (built vs installed plugin.dylib shasum matched) per the vcv build/install workflow on the main tree (no worktree)"
    - "Legend table range/behavior values are transcribed verbatim from src/AnalogLFO.cpp (Pitfall 2)"
    - "D-06: No trademarked synth brand names in panel.md prose or legend"
  artifacts:
    - path: "docs/panel.md"
      provides: "Annotated-panel section: image embed + 19-row legend/control-reference table"
      contains: "panel-annotated.png"
    - path: "docs/img/panel-annotated.png"
      provides: "Real Rack screenshot with 19 baked numbered callouts"
  key_links:
    - from: "docs/panel.md"
      to: "docs/img/panel-annotated.png"
      via: "relative image link"
      pattern: "img/panel-annotated\\.png"
    - from: "docs/panel.md legend rows"
      to: "baked callout numbers 1..19"
      via: "1:1 number keying"
      pattern: "^\\| *1[0-9]? "
---

<objective>
Produce the D-08 annotated-panel deliverable: build/install the current module, author `docs/panel.md` with the 19-row legend/control-reference table, then capture a real Rack screenshot and bake numbered callouts keyed to that legend.

Purpose: Completes the one table-stakes section (DOC-02) that requires a live screenshot and human-assisted callout placement — the single non-autonomous step in the phase.
Output: `docs/panel.md` + `docs/img/panel-raw.png` + `docs/img/panel-annotated.png`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-notion-manual/27-RESEARCH.md
@.planning/phases/27-notion-manual/27-UI-SPEC.md
@.planning/phases/27-notion-manual/27-CONTEXT.md
@src/AnalogLFO.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + install current module for an accurate screenshot</name>
  <files>(no repo files — build/install only)</files>
  <read_first>
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Annotated-Panel Capture Procedure step 1, Pitfall 6)
    - .planning/phases/27-notion-manual/27-UI-SPEC.md (§Annotated-Panel Asset Spec — Capture &amp; export)
    - The vcv_build_install_workflow project memory (relative ../Rack-SDK, stale-flush, no worktree)
  </read_first>
  <action>
    On the MAIN tree (NOT a worktree — `../Rack-SDK` must resolve, Pitfall 6): run `make install RACK_DIR=../Rack-SDK`. Confirm the loaded build is current by comparing `shasum` of the repo-built `plugin.dylib` against the installed copy in the Rack user plugins dir; if they differ, stale-flush (rsync `dist/ForgeAudio-AnalogSeries/res/` into the installed `res/`, copy the fresh `plugin.dylib`) so the shipped panel art matches. Report both shasums. This is preparation for Task 3's capture; do not screenshot yet.
  </action>
  <verify>
    <automated>test -f plugin.dylib &amp;&amp; shasum plugin.dylib</automated>
  </verify>
  <acceptance_criteria>
    - `make install RACK_DIR=../Rack-SDK` completes without error on the main tree.
    - Built vs installed `plugin.dylib` shasums are reported and match (current build loaded).
  </acceptance_criteria>
  <done>Current ForgeAnalogLFO build is installed; built/installed dylib shasums match, ready for a faithful screenshot.</done>
</task>

<task type="auto">
  <name>Task 2: Author panel.md with the 19-row legend/control-reference table</name>
  <files>docs/panel.md</files>
  <read_first>
    - src/AnalogLFO.cpp (configParam L199-215 params+ranges, configInput/configOutput L206-216, process() CV math)
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§The 10 Params, §The 7 Inputs, §The 1 Output; §Annotated-Panel callout order 1-19)
    - .planning/phases/27-notion-manual/27-UI-SPEC.md (§Annotated-Panel Asset Spec — legend columns # / Control / Type / Range·Behavior)
  </read_first>
  <action>
    Create `docs/panel.md`: `#` H1 + a relative image embed `![Annotated panel](img/panel-annotated.png)` (file arrives in Task 3) + one GitHub pipe table with columns `# | Control | Type | Range / Behavior`, exactly 19 rows keyed 1:1 to the callout numbers in this order: 1 Display, 2 Morph, 3 Character, 4 Drift, 5 Rate, 6 Phase Offset, 7 Morph-CV trim, 8 Character-CV trim, 9 Drift-CV trim, 10 FM-Depth trim, 11 Phase-Offset-CV trim, 12 Morph-CV jack, 13 Character-CV jack, 14 Drift-CV jack, 15 FM jack, 16 Phase-Offset-CV jack, 17 CLK jack, 18 RESET jack, 19 OUTPUT jack. Transcribe Range/Behavior verbatim from src/AnalogLFO.cpp (Pitfall 2): Morph/Character/Drift 0–1; Rate 0.01–20 Hz free / 15 musical ratios clocked; Phase Offset 0–360°; the four trims are attenuators (Morph/Character/Drift/Phase-Offset CV default 100%, FM Depth default 0%); CV jacks bipolar ±5V at 5V/unit; FM exponential; CLK/RESET Schmitt 1.0V/0.1V; OUTPUT bipolar ±5V. Note this legend doubles as the control-reference table. GENERIC vocabulary only (D-06).
  </action>
  <verify>
    <automated>grep -qF "img/panel-annotated.png" docs/panel.md &amp;&amp; test $(grep -cE '^\| *[0-9]+ ' docs/panel.md) -ge 19 &amp;&amp; grep -qF "OUTPUT" docs/panel.md &amp;&amp; grep -q "5V" docs/panel.md &amp;&amp; ! grep -qiE "moog|roland|juno|prophet|oberheim|korg" docs/panel.md</automated>
  </verify>
  <acceptance_criteria>
    - `panel.md` embeds `img/panel-annotated.png` via a relative path.
    - The legend table has ≥19 numbered rows covering controls 1 Display … 19 OUTPUT jack with verbatim ranges/behavior.
    - Brand-name denylist grep over `panel.md` returns zero hits.
  </acceptance_criteria>
  <done>panel.md holds the relative image embed + a 19-row legend/control-reference table matching src/AnalogLFO.cpp.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Capture Rack screenshot + bake 19 numbered callouts</name>
  <files>docs/img/panel-raw.png, docs/img/panel-annotated.png, panel-overlay.html</files>
  <read_first>
    - .planning/phases/27-notion-manual/27-RESEARCH.md (§Annotated-Panel Capture Procedure steps 2-5, panel geometry note)
    - .planning/phases/27-notion-manual/27-UI-SPEC.md (§Annotated-Panel Asset Spec — badge anatomy, spacing/color/typography for the baked asset)
  </read_first>
  <what-built>
    Task 1 installed the current build; Task 2 authored panel.md with the 19-row legend. This step produces the actual annotated image the legend keys to.
  </what-built>
  <how-to-verify>
    1. Launch VCV Rack 2 (on the main tree's installed build), add the Analog LFO, zoom so the whole panel is crisp.
    2. Capture the panel region at 2×/retina with `screencapture -R<x,y,w,h> docs/img/panel-raw.png` (or ⌘⇧4). Crop tightly to the module — no other windows, file paths, or desktop content in frame (privacy).
    3. Bake the 19 numbered callouts using the HTML-overlay + Playwright method (create `panel-overlay.html` with `panel-raw.png` as a CSS background and absolutely-positioned badge + leader-line divs per the UI-SPEC badge anatomy: circular near-black fill, 4px ember `#e85d26` ring, warm-white `#e8e4e0` JetBrains Mono numeral, 44px badge; leader lines `#e8e4e0`, no arrowhead, terminating 8px from the target). Screenshot the rendered page to `docs/img/panel-annotated.png` (PNG-24, longest edge ~1600px). Preview markup is an acceptable manual fallback.
    4. Confirm each numbered badge points at the correct control per the panel geometry (hero Morph center; secondary row Character/Drift/Rate/Phase-Offset; each trimpot directly above the jack it attenuates) and that all 19 numbers align 1:1 with the panel.md legend rows.
    5. Reply "approved" if every callout is correct and the image is legible after down-scale, or describe misplacements to fix.
  </how-to-verify>
  <verify>
    <automated>test -f docs/img/panel-annotated.png &amp;&amp; test -f docs/img/panel-raw.png</automated>
  </verify>
  <acceptance_criteria>
    - `docs/img/panel-raw.png` is a real Rack screenshot of the shipped module (not a mockup/SVG export).
    - `docs/img/panel-annotated.png` exists with 19 baked numbered callouts keyed 1:1 to the panel.md legend.
    - No unintended windows/paths/PII captured in the screenshot.
    - Human reviewer confirms every callout aligns to the correct control.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe callout placement issues</resume-signal>
  <done>Annotated PNG with 19 correctly-placed numbered callouts is committed under docs/img/ and referenced by panel.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen capture → committed PNG | A screenshot may inadvertently capture other on-screen windows, file paths, or PII |
| repo → public reader (Phase 28) | The committed PNG becomes publicly readable at the public flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Information disclosure | docs/img/panel-raw.png / panel-annotated.png | mitigate | Crop the capture tightly to the module panel; human-verify step explicitly checks no other windows/paths/PII are in frame before commit |
| T-27-02 | Tampering (legal/IP) | panel.md legend prose | mitigate | Generic vocabulary only; brand-name denylist grep (Task 2 verify + Plan 02 script) returns zero hits |
| T-27-09 | Tampering (build integrity) | screenshot source build | mitigate | Built vs installed plugin.dylib shasum matched (Task 1) so the annotated panel reflects the actually-shipped art, not a stale build |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs (build uses existing ../Rack-SDK; Playwright already present) — N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- Current build installed (dylib shasums match).
- `docs/img/panel-annotated.png` and `panel-raw.png` exist; annotated PNG has 19 callouts keyed to the panel.md legend.
- `docs/panel.md` embeds the annotated PNG by relative path; legend facts match src/AnalogLFO.cpp; zero brand-name hits.
</verification>

<success_criteria>
A real-Rack-screenshot annotated panel with 19 numbered callouts, keyed 1:1 to a 19-row legend/control-reference table in panel.md, referenced by a relative image path.
</success_criteria>

<output>
Create `.planning/phases/27-notion-manual/27-04-SUMMARY.md` when done.
</output>

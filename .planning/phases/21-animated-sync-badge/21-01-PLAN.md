---
phase: 21-animated-sync-badge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/AnalogLFO.cpp]
autonomous: false
requirements: [ANIM-01, ANIM-02]

must_haves:
  truths:
    - "While LOCKED, each clock edge re-arms the SYNC badge to a bright white-hot flash distinct from the steady ember badge (ANIM-01)"
    - "The flash decays smoothly via exponential 0.92x-per-frame envelope back to steady ember over ~200ms with no abrupt transition (ANIM-02)"
    - "Brightness at peak comes from a color shift (ember -> white-hot) PLUS glow bloom (larger blur + stronger glow alpha), NOT from alpha alone (D-01)"
    - "The flash fires in LOCKED state ONLY; the ACQUIRING 2Hz blinkPhase branch is byte-for-byte unchanged (D-02)"
    - "The flash peak is clearly visible / punchy and distinct from steady-state at a glance; the exact peak magnitude (white-hot hue, glow blur/alpha) is tuned at the in-Rack acceptance gate, not pinned in code (D-03)"
    - "Audio thread only increments the edge counter; the widget only reads it and owns all envelope state (lock-free, single-writer, no boolean-flag race)"
    - "Scope is the SYNC badge only -- no change to BPM stack, ratio/Hz/swing pills, border glow, scanlines, or any other drawPillText caller"
  artifacts:
    - path: "src/AnalogLFO.cpp"
      provides: "std::atomic<int> displayClockEdge edge counter, LOCKED-guarded increment, widget flash envelope, flash-modulated drawPillText, SYNC LOCKED-branch wiring"
      contains: "std::atomic<int> displayClockEdge"
  key_links:
    - from: "AnalogLFO::process() edge block (~line 533)"
      to: "displayClockEdge atomic"
      via: "fetch_add(1, relaxed) guarded by clockState == LOCKED"
      pattern: "displayClockEdge\\.fetch_add"
    - from: "WaveformDisplay::step() (~line 868)"
      to: "flashIntensity envelope"
      via: "edge-delta detect sets flashIntensity = 1.f, then *= 0.92f decay"
      pattern: "flashIntensity \\*= 0\\.92f"
    - from: "SYNC badge LOCKED branch (~line 1377)"
      to: "drawPillText flash param"
      via: "pass flashIntensity into defaulted float flash param"
      pattern: "float flash = 0\\.f"
---

<objective>
Make the SYNC badge in the waveform display pulse white-hot on each incoming clock edge while LOCKED, decaying back to steady ember via the locked ~0.92x/frame exponential envelope. This satisfies both ANIM-01 (flash on each clock edge) and ANIM-02 (exponential decay).

Purpose: Immediate visual feedback that the module is locked to and responding to each clock beat -- "a spark striking hot metal, cooling back to ember" (on-theme for Forge Noir).

Output: All edits in `src/AnalogLFO.cpp` -- one new atomic, a LOCKED-guarded increment, a widget-owned decay envelope, a flash-modulated `drawPillText`, and the SYNC LOCKED-branch wiring. No new files, no new dependencies.

This is 95% wiring into three existing, verified patterns (lock-free atomic publish, per-frame `step()` envelope, two-pass emissive text). Do NOT redesign -- translate the research into the change set below. Re-grep anchors before editing; line numbers in this plan are current as of planning but the file may shift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/21-animated-sync-badge/21-CONTEXT.md
@.planning/phases/21-animated-sync-badge/21-RESEARCH.md
@.planning/phases/21-animated-sync-badge/21-UI-SPEC.md
@.planning/phases/21-animated-sync-badge/21-VALIDATION.md

@src/AnalogLFO.cpp

<interfaces>
<!-- Verified against src/AnalogLFO.cpp this planning session. Re-grep before editing. -->

Clock state enum (line 40):
  enum ClockState { FREE = 0, ACQUIRING = 1, LOCKED = 2 };

Existing display atomics (lines 110-162), the lock-free DSP->GUI publish pattern to mirror:
  std::atomic<int> displayClockState{0};   // line 128
  // written: displayClockState.store(LOCKED, std::memory_order_relaxed);   (line 483/497)
  // read:    int clockState = module->displayClockState.load(std::memory_order_relaxed);  (line 883/1332)

Edge-detection block (lines 446-534): `if (clockTrigger.process(clkVoltage, 0.1f, 1.0f)) { ... }`.
  ACQUIRING->LOCKED promotion happens at lines 482 and 496 (clockState = LOCKED; displayClockState.store(LOCKED,...)).
  The block closes at line 533. At end-of-block, clockState already reflects this edge's transition.

WaveformDisplay animation state (lines 850-866):
  float breathePhase = 0.f;   // 0.2Hz border glow
  float blinkPhase = 0.f;     // 2Hz SYNC ACQUIRING blink

WaveformDisplay::step() (line 868) -- per-frame, 60fps-assumed convention:
  breathePhase += 2.f * (float)M_PI * 0.2f / 60.f;   // line 870
  blinkPhase   += 2.f * (float)M_PI * 2.f  / 60.f;    // line 874

drawPillText current signature (lines 1161-1162) -- other callers (1226 inside drawBpmStack, 1345, 1352, 1361) plus the SYNC caller at 1377 being modified; a trailing defaulted param keeps every other caller unchanged regardless of count:
  void drawPillText(NVGcontext* vg, int fontHandle, float x, float y,
                    const char* text, float fontSize, int align, float alpha)
  Two-pass emissive render (lines 1188-1196):
    glow pass:  nvgFontBlur(vg, 2.0f);  nvgFillColor(vg, nvgRGBAf(0.91f, 0.365f, 0.149f, alpha * 0.3f));  nvgText(...);
    sharp pass: nvgFontBlur(vg, 0.0f);  nvgFillColor(vg, nvgRGBAf(0.91f, 0.365f, 0.149f, alpha));        nvgText(...);
  Pill fill (line 1180) and stroke (line 1184) stay ember -- D-01 scopes flash to text/glow only.

SYNC badge draw block (lines 1369-1380):
  if (syncFadeAlpha > 0.001f) {
    float effectiveAlpha = syncFadeAlpha;
    if (clockState == AnalogLFO::ACQUIRING) { float blink = 0.5f + 0.5f*std::sin(blinkPhase); effectiveAlpha *= blink; }   // <- leave untouched
    drawPillText(vg, font->handle, rightColX, topY + pillLabelSize, "SYNC", pillLabelSize, NVG_ALIGN_CENTER|NVG_ALIGN_BOTTOM, effectiveAlpha);
  }
  clockState is already loaded at line 1332 and in scope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add LOCKED-gated edge counter (DSP side)</name>
  <read_first>
    - src/AnalogLFO.cpp lines 40 (enum), 110-162 (display atomics), 446-534 (edge-detection block incl. LOCKED promotions at 482/496 and block close at 533)
    - 21-RESEARCH.md: Pattern 1 (lock-free atomic publish), Anti-Patterns ("Boolean flag cleared by widget"), Code Examples (declaring/incrementing the counter), Open Question 1 + Assumption A3 (placement)
    - 21-CONTEXT.md: D-02 (LOCKED-only), Claude's Discretion (data-flow mechanism)
  </read_first>
  <files>src/AnalogLFO.cpp</files>
  <action>
    Declare a new member `std::atomic<int> displayClockEdge{0};` alongside the existing `display*` atomics (next to `displayClockState` near line 128), mirroring that lock-free publish pattern exactly. Add a comment noting it is incremented once per LOCKED clock edge for ANIM-01.

    Inside the existing `if (clockTrigger.process(clkVoltage, 0.1f, 1.0f))` edge block, at the END of the block (after the division-aware phase-reset logic, just before the block closes ~line 533) where `clockState` already reflects this edge's ACQUIRING->LOCKED promotion, add a LOCKED guard: when `clockState == LOCKED`, call `displayClockEdge.fetch_add(1, std::memory_order_relaxed)`. Per Assumption A3 / Open Question 1, this counts only edges where the module is ALREADY locked at the increment site, so the very first locked beat flashes on the next edge (clean handoff, no flash colliding with acquiring->locked). The audio thread ONLY increments -- never reads, never resets. Do NOT use an atomic<bool> mailbox the widget clears (read-modify-write race -- see Anti-Patterns).

    Touch nothing else in process(): no change to the state machine, EMA period, phase reset, or any other display atomic.
  </action>
  <verify>
    <automated>cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series" && grep -q 'std::atomic<int> displayClockEdge' src/AnalogLFO.cpp && grep -E 'displayClockEdge\.fetch_add\(1, *std::memory_order_relaxed\)' src/AnalogLFO.cpp && grep -B2 'displayClockEdge\.fetch_add' src/AnalogLFO.cpp | grep -q 'clockState == LOCKED' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `src/AnalogLFO.cpp` declares `std::atomic<int> displayClockEdge{0};` among the display atomics
    - The increment is `displayClockEdge.fetch_add(1, std::memory_order_relaxed)` guarded by `clockState == LOCKED`, placed inside the `clockTrigger.process(...)` edge block AFTER the ACQUIRING->LOCKED promotion
    - No `displayClockEdge` read, store, or reset anywhere in `process()` (audio thread is write-only on this field)
    - No other edits inside `process()` / the edge block
    - Compile gate `make RACK_DIR=../Rack-SDK` exits 0 (run at end of Task 3 once all edits land; safe to run here too)
  </acceptance_criteria>
  <done>displayClockEdge atomic exists; LOCKED-guarded fetch_add present in the edge block; audio thread is write-only on it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add flash envelope (widget step) + flash-modulated drawPillText (render)</name>
  <read_first>
    - src/AnalogLFO.cpp lines 850-866 (animation state members), 868-895 (step body), 1161-1197 (drawPillText two-pass render)
    - 21-RESEARCH.md: Pattern 2 (per-frame envelope), Pattern 3 (two-pass emissive text + flash modulation), Pitfall 2 (0.92x decay / epsilon snap), Pitfall 3 (hot color on BOTH passes), Anti-Patterns ("Reading the counter in drawLayer instead of step()")
    - 21-UI-SPEC.md: Color/Glow Flash Contract table, Decay Curve section, white-hot target (1.0, 0.93, 0.78)
    - 21-CONTEXT.md: D-01 (color shift + glow bloom, not alpha), D-03 (peak punchy, exact constants deferred to gate)
  </read_first>
  <files>src/AnalogLFO.cpp</files>
  <action>
    Widget state + envelope (step): Add two widget member fields beside `breathePhase`/`blinkPhase` (~lines 850-866): `int prevClockEdge = 0;` and `float flashIntensity = 0.f;`. In `WaveformDisplay::step()`, inside the existing `if (module) { ... }` block (after the existing fade timers), load `module->displayClockEdge` with `std::memory_order_relaxed`; if it differs from `prevClockEdge`, set `flashIntensity = 1.f` and update `prevClockEdge`. Then apply the locked decay `flashIntensity *= 0.92f` (ANIM-02 -- do NOT change this factor) and snap to `0.f` when below `0.001f`. Advance the envelope exactly ONCE per step() (frame-paced, matching breathePhase/blinkPhase) -- never in drawLayer. The widget only READS the atomic and owns all envelope state; never writes the atomic.

    Render (drawPillText): Extend the signature with a trailing defaulted parameter `float flash = 0.f` so every other caller is unchanged. Modulate BOTH the glow pass and the sharp pass (lines 1188-1196). Color: lerp the three ember floats `(0.91, 0.365, 0.149)` toward the white-hot target `(1.0, 0.93, 0.78)` by `flash`, and apply the SAME lerped color to both the glow and sharp `nvgFillColor` calls (Pitfall 3 -- not just the glow). Glow bloom: lerp `nvgFontBlur` from `2.0` toward a peak (~4.0-5.0) by `flash`, and lerp the glow-pass alpha multiplier from `0.3` toward a stronger peak (~0.6-0.9) by `flash`, both times alpha. Keep the sharp-pass alpha at full `alpha` (D-01: brightness from color + glow, NOT alpha). Leave the pill fill (line 1180) and stroke (line 1184) ember/unchanged (D-01 scopes flash to text/glow; Open Question 2 recommends keeping the pill calm). At `flash == 0.f` the render MUST be byte-for-byte identical to the current steady-state output (zero regression for the badge at rest and for all other callers).

    The peak constants (white-hot hue, peak blur, peak glow alpha) are GATE-TUNED per D-03 -- use the UI-SPEC starting values; do not block on a specific magnitude.
  </action>
  <verify>
    <automated>cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series" && grep -q 'float flashIntensity' src/AnalogLFO.cpp && grep -q 'int prevClockEdge' src/AnalogLFO.cpp && grep -q 'flashIntensity \*= 0.92f' src/AnalogLFO.cpp && grep -Eq 'void drawPillText\(.*float flash = 0\.f' <(tr -d '\n' < src/AnalogLFO.cpp) && grep -q 'displayClockEdge.load(std::memory_order_relaxed)' src/AnalogLFO.cpp && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Widget declares `int prevClockEdge = 0;` and `float flashIntensity = 0.f;` beside existing animation state
    - `step()` reads `displayClockEdge` (relaxed), sets `flashIntensity = 1.f` on counter change, applies `flashIntensity *= 0.92f`, snaps to 0 below `0.001f`; the atomic is read-only in the widget
    - `drawPillText` signature ends with `float flash = 0.f`; every other existing caller is unmodified
    - Both the glow pass and the sharp pass take the ember->white-hot lerped color; glow blur and glow alpha bloom with `flash`; sharp-pass alpha stays full `alpha`; pill fill/stroke unchanged
    - At `flash == 0.f` the rendered output is identical to current steady state (verify by reasoning over the lerp endpoints, not visually)
    - Compile gate `make RACK_DIR=../Rack-SDK` exits 0 (run at end of Task 3)
  </acceptance_criteria>
  <done>flashIntensity envelope advances once per step() with the locked 0.92x decay; drawPillText modulates color + glow bloom on both passes via a defaulted flash param with zero change at flash==0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire flash into SYNC LOCKED branch + compile gate</name>
  <read_first>
    - src/AnalogLFO.cpp lines 1314-1386 (drawTextOverlays incl. clockState load at 1332 and SYNC block 1369-1380)
    - 21-RESEARCH.md: Pitfall 5 (do not edit ACQUIRING branch), Pitfall 6 (stale install / no worktree), Code Examples (applying flash in the SYNC block)
    - 21-CONTEXT.md: D-02 (LOCKED-only, ACQUIRING blink untouched)
    - 21-VALIDATION.md: Manual-Only Verifications (the visual UAT behaviors)
    - Project memory vcv_build_install_workflow.md: build on MAIN tree (not a worktree), stale-install flush + relaunch
  </read_first>
  <files>src/AnalogLFO.cpp</files>
  <action>
    In the SYNC badge block (lines 1371-1380), `clockState` is already loaded at line 1332 and in scope. Leave the existing `if (clockState == AnalogLFO::ACQUIRING) { ... blink ... }` branch byte-for-byte unchanged (D-02 / Pitfall 5 -- the ACQUIRING 2Hz blink must not be touched). Restructure so the LOCKED case (the non-ACQUIRING path) passes `flashIntensity` as the new trailing `flash` argument to `drawPillText`, while the ACQUIRING path passes `0.f` (or omits it, using the default). The badge must flash ONLY when LOCKED; ACQUIRING keeps blink-only with no flash. Do not alter `syncFadeAlpha` gating, position (`rightColX`, `topY + pillLabelSize`), font size, or alignment.

    Then run the compile gate on the MAIN tree (NOT a git worktree -- `../Rack-SDK` will not resolve in a worktree and the build fails): `make RACK_DIR=../Rack-SDK`. It must exit 0. Resolve any warnings/errors introduced by these edits. Do NOT run `make install` or relaunch Rack in this task -- installation + the in-Rack visual UAT happen at the human-verify checkpoint (Task 4).
  </action>
  <verify>
    <automated>cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series" && grep -A8 'clockState == AnalogLFO::ACQUIRING' src/AnalogLFO.cpp | grep -q 'blinkPhase' && grep -A12 'if (syncFadeAlpha > 0.001f)' src/AnalogLFO.cpp | grep -q 'flashIntensity' && make RACK_DIR=../Rack-SDK 2>&1 | tail -5; test -f plugin.dylib && echo PASS_COMPILE</automated>
  </verify>
  <acceptance_criteria>
    - The ACQUIRING branch (`blinkPhase` 2Hz blink) is unchanged; no flash applied during ACQUIRING
    - The LOCKED path passes `flashIntensity` into `drawPillText`'s `flash` param; ACQUIRING passes default/0.f
    - SYNC badge position, size, alignment, and `syncFadeAlpha` gating are unchanged
    - `make RACK_DIR=../Rack-SDK` exits 0 and produces `plugin.dylib` (run on the main tree, not a worktree)
    - No other display element (BPM stack, ratio/Hz/swing pills, border glow, scanlines) is modified
  </acceptance_criteria>
  <done>Flash wired into the LOCKED branch only; ACQUIRING blink intact; clean compile on the main tree producing plugin.dylib.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Install + in-Rack visual acceptance gate (tune peak constants)</name>
  <what-built>
    A per-edge white-hot flash on the SYNC badge while LOCKED: each clock beat re-arms `flashIntensity` to 1.0; the badge text + glow lerp ember -> white-hot with a glow bloom, decaying via `flashIntensity *= 0.92f` (~200ms) back to steady ember. ACQUIRING still blinks at 2Hz with no flash. Compile gate passed on the main tree.
  </what-built>
  <how-to-verify>
    1. Install the freshly built plugin (run on the MAIN tree, per project build memory):
       `make install RACK_DIR=../Rack-SDK`
       Then sync the fresh `plugin.dylib` into the extracted install dir:
       `~/Library/Application Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/`
       and FULLY QUIT + relaunch Rack (stale-install flush -- otherwise Rack shows the old behavior, Pitfall 6).
    2. Patch a clock source (e.g. a CLOCK / square LFO) into the CLK input. Let it lock.
    3. Confirm (success criterion #1): once LOCKED, the SYNC badge visibly FLASHES white-hot on each clock edge, clearly distinct from the steady ember badge at a glance.
    4. Confirm (success criterion #2): the flash decays smoothly back to steady ember over ~200ms with NO abrupt transition / strobe -- "spark cooling to ember."
    5. Confirm D-02 separation: while ACQUIRING (just after patching, before lock) the badge BLINKS at 2Hz with NO per-edge flash; while free-running (no clock) the badge is hidden / shows no flash.
    6. Confirm scope: no other display element changed (BPM stack, ratio/Hz/swing pills, border breathing glow, scanlines, corner brackets all as in Phase 20.1).
    7. TUNE (D-03): judge the peak magnitude. If the flash is too weak or too harsh, adjust ONLY the gate-tuned constants in `drawPillText` -- white-hot target hue `(1.0, 0.93, 0.78)`, peak blur (~4-5), peak glow alpha (~0.6-0.9) -- rebuild, reinstall, relaunch, recheck. Do not change the locked `0.92f` decay or the LOCKED-only trigger.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting the final tuned peak constants), or describe what looks wrong (too weak/harsh, wrong color, flash during ACQUIRING, other element changed) so it can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio thread -> GUI thread | `std::atomic<int> displayClockEdge` crosses `process()` (audio) to `WaveformDisplay::step()` (UI). Single-writer (audio increments), single-reader (widget loads); lock-free, no shared mutable animation state crosses. Not a security boundary -- in-process only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | (V2-V6 input/auth/crypto/data) | accept | N/A -- the only input is a clock voltage already validated by the existing `dsp::SchmittTrigger`; no untrusted/parsed input, no network, no auth, no crypto, no persisted/serialized state (verified: animation fields never touched by dataToJson/dataFromJson). Feature is an in-process render animation. ASVS V2-V6 do not apply. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | N/A -- this phase installs NO external packages. All code uses the already-linked VCV Rack SDK, bundled NanoVG, and stdlib headers already #included. No package legitimacy gate required (RESEARCH.md Package Legitimacy Audit: not applicable). |

Security enforcement is enabled, but per RESEARCH.md Security Domain this phase has no security surface. The single cross-thread atomic is a correctness concern (race-free via the monotonic single-writer counter, addressed in Task 1 / Pitfall 1), not a security one.
</threat_model>

<verification>
Automated (compile gate is the only automated signal -- no test suite exists for this plugin):
- `make RACK_DIR=../Rack-SDK` exits 0 and produces `plugin.dylib` (Task 3)
- Source assertions per task (`displayClockEdge` atomic + LOCKED-guarded fetch_add; `flashIntensity`/`prevClockEdge` + `*= 0.92f` decay; `drawPillText(..., float flash = 0.f)`; flash wired into the LOCKED branch only; ACQUIRING blinkPhase branch unchanged)

Manual (in-Rack visual UAT -- Task 4 human-verify, per 21-VALIDATION.md Manual-Only Verifications):
- LOCKED: visible per-edge white-hot flash distinct from steady ember (ANIM-01 / success criterion #1)
- Smooth ~200ms exponential decay, no strobe (ANIM-02 / success criterion #2)
- ACQUIRING blinks at 2Hz with no flash; free-running shows no flash (D-02)
- No regression to any other display element (scope = SYNC badge only)
</verification>

<success_criteria>
- `std::atomic<int> displayClockEdge` declared and incremented only on LOCKED clock edges (audio write-only)
- `WaveformDisplay` owns `prevClockEdge` + `flashIntensity`; envelope re-arms to 1.0 on counter change and decays `*= 0.92f` per frame, snapping to 0 below 0.001 (ANIM-02)
- `drawPillText` modulates color (ember -> white-hot) + glow bloom on both passes via a defaulted `float flash` param; byte-for-byte unchanged at `flash == 0`; brightness from color+glow not alpha (D-01)
- Flash wired into the SYNC LOCKED branch only; ACQUIRING 2Hz blink untouched (D-02)
- Clean compile on the main tree; in-Rack visual UAT approved with final tuned peak constants (D-03)
- ANIM-01 and ANIM-02 both satisfied; no other display element changed
</success_criteria>

<output>
Create `.planning/phases/21-animated-sync-badge/21-01-SUMMARY.md` when done.
</output>

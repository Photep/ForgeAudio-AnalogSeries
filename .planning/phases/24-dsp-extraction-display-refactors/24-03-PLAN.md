---
phase: 24-dsp-extraction-display-refactors
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - src/AnalogLFO.cpp
autonomous: true
requirements: [CLEAN-04, CLEAN-03, CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "all five animations (breathe, blink, scanline, fade, flash) advance by clamped wall-clock dt instead of fixed 1/60 ticks"
    - "the SYNC badge flash decays via forge::flashDecay (ANIM-02 0.92 preserved by equivalence, not re-tuned)"
    - "ratio + BPM pills fade out over the same 200ms ramp as the SYNC badge via a widget-side cache + alpha gate"
    - "drawZeroCrossing and the scanlineImage member are deleted; the unreachable isStill is resolved"
    - "the plugin builds clean against ../Rack-SDK"
  artifacts:
    - path: "src/AnalogLFO.cpp"
      provides: "dt-driven step(); cachedRatioIdx/cachedPeriod fade cache; dead-code removed"
      contains: "forge::clampFrameDt"
  key_links:
    - from: "WaveformDisplay::step()"
      to: "APP->window->getLastFrameDuration()"
      via: "forge::clampFrameDt(...) once per frame"
      pattern: "forge::clampFrameDt"
    - from: "drawBpmStack / ratio pill"
      to: "cachedRatioIdx, cachedPeriod"
      via: "alpha gate replaces the ratioIdx>=0 / period sentinel early-returns"
      pattern: "cachedRatioIdx"
---

<objective>
Land the three GUI-widget cleanups on `WaveformDisplay`, now that the fill is off the audio thread (24-02):
- CLEAN-04 (D-03/D-04): convert every frame-paced animation in `step()` to clamped wall-clock dt via `forge::clampFrameDt(APP->window->getLastFrameDuration())` and `forge::flashDecay` (from 24-01) — frame-rate-independent at 144 Hz, feel-identical at 60fps.
- CLEAN-03 (D-05): cache the last valid `ratioIdx`+period widget-side and alpha-gate the ratio/BPM pills so they fade out in sync with the SYNC badge instead of popping off on disconnect. No atomic/DSP change.
- CLEAN-01/CLEAN-02 (D-07): delete dead `drawZeroCrossing` + `scanlineImage`; resolve the unreachable `isStill`.

Purpose: finishes the display-layer hygiene; behavior stays feel-identical, just frame-rate-independent and symmetric.
Output: src/AnalogLFO.cpp widget edits; no DSP behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dsp-extraction-display-refactors/24-RESEARCH.md
@.planning/phases/24-dsp-extraction-display-refactors/24-PATTERNS.md
@src/dsp/Anim.hpp

<interfaces>
<!-- Contracts consumed. From 24-01 + the existing widget. -->
From src/dsp/Anim.hpp (24-01):
  - `inline float forge::clampFrameDt(float raw);`   (NAN/neg → 0, caps at 1/30)
  - `inline float forge::flashDecay(float intensity, float dt);`  (intensity * pow(0.92, dt*60))

dt conversion table (24-RESEARCH.md lines 250-256):
  - :388 breathePhase += 2π·0.2/60   → += 2π·0.2f * dt
  - :392 blinkPhase   += 2π·2/60     → += 2π·2f * dt
  - :396 scanlineScrollPhase += 1/60 → += dt
  - :402 fadeSpeed = 1/(0.2·60)      → fadeSpeed = dt / 0.2f
  - :429 flashIntensity *= 0.92f     → flashIntensity = forge::flashDecay(flashIntensity, dt)
  Wrap/clamp guards (:389,:393,:397, the rack::math::clamp ramps :410-419) are amplitude-preserving — LEAVE them.

CLEAN-03 sites:
  - :740-742 drawBpmStack reads displayRatioIndex/displaySmoothedPeriod then `if (ratioIdx<0||period<=0) return;`
  - :873 drawTextOverlays reads `int ratioIdx = displayRatioIndex.load(relaxed);`
  - :891 ratio pill gate `if (ratioFadeAlpha > 0.001f && ratioIdx >= 0)`
  Audio thread keeps publishing -1 on disconnect (no atomic change — "hold the atomics" rejected).

CLEAN-01/02 sites: :373 `int scanlineImage = -1;` (unused); :529 `void drawZeroCrossing(...)` (no caller);
  :974 `bool isStill = (rate <= 0.001f);` unreachable (RATE_PARAM min 0.01f) → :975 dimFactor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLEAN-04 — convert step() animations to clamped wall-clock dt</name>
  <files>src/AnalogLFO.cpp</files>
  <read_first>
    - src/AnalogLFO.cpp lines 386-434 (the full step() body: the five accumulators + wrap guards + the fade ramps + the flash decay)
    - src/dsp/Anim.hpp (clampFrameDt + flashDecay signatures — from 24-01)
    - 24-RESEARCH.md Pattern 3 (lines 247-278, dt conversion table + the dt source line) + Pitfall 1 (lines 333-337) + Pitfall 5 (lines 355-357, feel- not bit-identity)
    - 24-PATTERNS.md "Widget step() dt conversion" bullet
  </read_first>
  <action>
    At the top of `WaveformDisplay::step()` add `float dt = forge::clampFrameDt((float)APP->window->getLastFrameDuration());` (GUI-thread-only call — never the audio thread). Then convert the five sites per the dt table: `breathePhase += 2.f*(float)M_PI*0.2f * dt;` (:388), `blinkPhase += 2.f*(float)M_PI*2.f * dt;` (:392), `scanlineScrollPhase += dt;` (:396), `float fadeSpeed = dt / 0.2f;` (:402), and replace `flashIntensity *= 0.92f;` (:429) with `flashIntensity = forge::flashDecay(flashIntensity, dt);`. Keep the ANIM-02 "do NOT change this factor" intent — the 0.92 now lives inside forge::flashDecay, preserved by mathematical equivalence (D-03), so update the adjacent comment to point at flashDecay rather than deleting the rationale. Leave EVERY wrap/clamp guard untouched: `if (breathePhase > 2π) ...` (:389), `if (blinkPhase > 100π) ...` (:393), `fmodf(scanlineScrollPhase, 4.f)` (:397), the four `rack::math::clamp(target - alpha, -fadeSpeed, fadeSpeed)` ramps (:410-419), and the `if (flashIntensity < 0.001f) flashIntensity = 0.f;` snap (:430). Do not reorder; only the increment/decay expressions change.
  </action>
  <acceptance_criteria>
    - `grep -c "forge::clampFrameDt" src/AnalogLFO.cpp` == 1 (dt computed once at step() top).
    - `grep -c "getLastFrameDuration" src/AnalogLFO.cpp` == 1.
    - `grep -c "forge::flashDecay" src/AnalogLFO.cpp` == 1 (decay routed through the helper).
    - No frame-paced increments remain: `grep -c "/ 60.f" src/AnalogLFO.cpp` == 0 AND `grep -v '^[[:space:]]*//' src/AnalogLFO.cpp | grep -c "flashIntensity \*= 0.92"` == 0.
    - `make` builds the plugin against ../Rack-SDK; `make test` green (Anim helpers already pinned in 24-01).
  </acceptance_criteria>
  <verify>
    <automated>make && make test && grep -c "forge::clampFrameDt" src/AnalogLFO.cpp | grep -qx 1 && grep -c "/ 60.f" src/AnalogLFO.cpp | grep -qx 0</automated>
  </verify>
  <done>step() computes one clamped dt and advances breathe/blink/scanline/fade/flash by it; the 0.92 decay is preserved via forge::flashDecay; wrap/clamp guards unchanged; plugin + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: CLEAN-03 — widget-side ratio/BPM cache + alpha-gated fade-out</name>
  <files>src/AnalogLFO.cpp</files>
  <read_first>
    - src/AnalogLFO.cpp lines 366-384 (fade-alpha members — where cachedRatioIdx/cachedPeriod go)
    - src/AnalogLFO.cpp lines 399-431 (step() module block — where to refresh the cache while clocked)
    - src/AnalogLFO.cpp lines 739-768 (drawBpmStack — the sentinel early-return at :742 + its ratioIdx/period reads)
    - src/AnalogLFO.cpp lines 854-895 (drawTextOverlays — the :873 ratioIdx read + the :891 ratio pill gate)
    - 24-RESEARCH.md Pattern 4 (lines 280-297) + 24-PATTERNS.md "Pill fade-out cache (CLEAN-03)" bullet
  </read_first>
  <action>
    Add widget members near the fade alphas (~:366-370): `int cachedRatioIdx = -1;` and `float cachedPeriod = 0.f;`. In `step()` (inside the existing `if (module)` block), refresh the cache ONLY while genuinely clocked: read `ri = module->displayRatioIndex.load(relaxed)` and `per = module->displaySmoothedPeriod.load(relaxed)`, and `if (ri >= 0 && per > 0.f) { cachedRatioIdx = ri; cachedPeriod = per; }` — so on disconnect (atomics → -1) the cache holds the last valid content while the pill fades. Then: (a) ratio pill gate at :891 — change `if (ratioFadeAlpha > 0.001f && ratioIdx >= 0)` to use `cachedRatioIdx >= 0` and draw `AnalogLFO::RATIO_LABELS[cachedRatioIdx]`; (b) drawBpmStack — change its signature to accept the cached idx + period (e.g. `drawBpmStack(vg, fontHandle, alpha, cachedRatioIdx, cachedPeriod)`), drop the `displayRatioIndex/displaySmoothedPeriod` atomic loads at :740-741 and the `if (ratioIdx<0||period<=0) return;` sentinel at :742 (the `bpmFadeAlpha > 0.001f` gate at the call site :929 now drives visibility); update the call at :930 to pass `cachedRatioIdx, cachedPeriod`. Make NO atomic/DSP change — the audio thread still publishes -1 (D-05; "hold the atomics" rejected). Leave the Hz and Swing overlays alone (already correct). Guard against `cachedRatioIdx` indexing RATIO_LABELS/RATIO_TABLE out of range (it is >=0 and <15 once set, but keep the existing isX1/`ratioIdx==7` logic consistent with the cached value).
  </action>
  <acceptance_criteria>
    - `grep -c "cachedRatioIdx" src/AnalogLFO.cpp` >= 3 (member + refresh + ratio pill + bpm stack usages).
    - `grep -c "cachedPeriod" src/AnalogLFO.cpp` >= 2.
    - The drawBpmStack sentinel is gone: `grep -v '^[[:space:]]*//' src/AnalogLFO.cpp | grep -c "ratioIdx < 0 || period <= 0.f"` == 0.
    - drawBpmStack no longer self-loads the atomics: the `displaySmoothedPeriod.load` inside drawBpmStack is removed (period now passed in).
    - `make` builds the plugin against ../Rack-SDK; `make test` green.
  </acceptance_criteria>
  <verify>
    <automated>make && make test && grep -v '^[[:space:]]*//' src/AnalogLFO.cpp | grep -c "ratioIdx < 0 || period <= 0.f" | grep -qx 0</automated>
  </verify>
  <done>The widget caches the last valid ratioIdx/period and draws ratio + BPM pills behind alpha gates so they fade with the SYNC badge; no atomic/DSP change; plugin + tests green. (Visual symmetry confirmed in 24-04 UAT.)</done>
</task>

<task type="auto">
  <name>Task 3: CLEAN-01/CLEAN-02 — delete dead code and resolve isStill</name>
  <files>src/AnalogLFO.cpp</files>
  <read_first>
    - src/AnalogLFO.cpp line 373 (`int scanlineImage = -1;` — declared, unused)
    - src/AnalogLFO.cpp lines 529-... (the full `drawZeroCrossing` definition — confirm no caller before deleting)
    - src/AnalogLFO.cpp lines 970-976 (the `isStill` / dimFactor site)
    - 24-RESEARCH.md phase_requirements rows for CLEAN-01/02 + Pitfall/Open-Q2 (delete isStill, do NOT lower RATE_PARAM min)
    - 24-PATTERNS.md "Dead-code / isStill (CLEAN-01/02)" bullet
  </read_first>
  <action>
    Mechanical (D-07): (1) Delete the entire `drawZeroCrossing(NVGcontext*)` definition starting at :529 — first confirm zero callers (`grep -n drawZeroCrossing src/AnalogLFO.cpp` shows only the definition). (2) Delete the unused `int scanlineImage = -1;` member at :373 (and its now-stale "used by Plan 03" comment) — confirm no reads (`grep -n scanlineImage src/AnalogLFO.cpp` shows only the declaration). (3) Resolve the unreachable `isStill` at :974: delete `bool isStill = (rate <= 0.001f);` and simplify :975 to `float dimFactor = module->isBypassed() ? 0.25f : 1.f;` (REQUIREMENTS forbids lowering the RATE_PARAM min; `rate <= 0.001f` can never be true with min 0.01f, so the condition is pure dead weight). Do not touch the surrounding bypass-dim behavior otherwise.
  </action>
  <acceptance_criteria>
    - `grep -c "drawZeroCrossing" src/AnalogLFO.cpp` == 0.
    - `grep -c "scanlineImage" src/AnalogLFO.cpp` == 0.
    - `grep -c "isStill" src/AnalogLFO.cpp` == 0.
    - `grep -c "isBypassed() ? 0.25f : 1.f" src/AnalogLFO.cpp` == 1 (dimFactor simplified, bypass dim preserved).
    - `make` builds the plugin clean against ../Rack-SDK (no unused-variable / undefined-reference warnings introduced); `make test` green.
  </acceptance_criteria>
  <verify>
    <automated>make && make test && for s in drawZeroCrossing scanlineImage isStill; do grep -c "$s" src/AnalogLFO.cpp | grep -qx 0 || exit 1; done</automated>
  </verify>
  <done>drawZeroCrossing, scanlineImage, and isStill are gone; dimFactor preserves only the reachable bypass dim; plugin builds clean and tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All edits are GUI-thread-only widget changes (animation math, draw-layer cache, dead-code removal). No new cross-thread data, no I/O, no untrusted input. The audio↔GUI seqlock boundary (24-02) is unchanged here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering (out-of-range index) | cachedRatioIdx → RATIO_LABELS/RATIO_TABLE[15] | mitigate | Cache refreshed only when `ri >= 0`; alpha gate draws only when `cachedRatioIdx >= 0`; values originate from the same audio-published index already bounds-valid today. |
| T-24-03-02 | Denial of Service (first-frame jump) | getLastFrameDuration() NAN | mitigate | dt routed through forge::clampFrameDt (isfinite guard, 24-01) — NAN/neg → 0, capped at 1/30 (Pitfall 1). |
| T-24-03-03 | (external) | — | accept | No network/auth/persistence/untrusted-input surface; ASVS V2-V6 not applicable. No package installs → no supply-chain row. |
</threat_model>

<verification>
- `make` builds the plugin clean against ../Rack-SDK; `make test` stays green (CLEAN-04 dt/decay math already pinned by 24-01's test_anim.cpp).
- No frame-paced `/ 60.f` increments or `*= 0.92f` decays remain; dt is computed once via forge::clampFrameDt.
- drawZeroCrossing / scanlineImage / isStill fully removed (grep == 0).
- CLEAN-03 fade symmetry and overall animation feel are visual — verified in the 24-04 manual in-Rack UAT (D-06).
</verification>

<success_criteria>
step() is frame-rate-independent via clamped wall-clock dt (badge decay feel-identical at 60fps), ratio/BPM pills fade symmetrically with the SYNC badge via the widget cache, and the dead code + unreachable isStill are gone — with the plugin building clean and `make test` green.
</success_criteria>

<output>
Create `.planning/phases/24-dsp-extraction-display-refactors/24-03-SUMMARY.md` when done.
</output>

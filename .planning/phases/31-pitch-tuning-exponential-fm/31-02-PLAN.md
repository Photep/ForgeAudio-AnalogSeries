---
phase: 31-pitch-tuning-exponential-fm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dsp/VcoCore.hpp
autonomous: true
requirements: [PITCH-04]

must_haves:
  truths:
    - "D-11: `forge::kVcoNyquistGuardFrac` is `0.495f` and its comment states the settled PITCH-04 policy — `min(freq, 0.5 x sampleRate x 0.99)` — with the derived ceilings, not a placeholder pointing at a future phase"
    - "D-10: the comment states the policy is a HARD CLAMP — frequency pins at the ceiling and the oscillator keeps sounding, so under deep FM peaks flatten out rather than going silent"
    - "D-12: `forge::kVcoMaxDeltaPhase` is still `0.5` and is still a `double`, and its eight-line wrap-correctness comment still explains why it is a different KIND of constant"
    - "The `kVcoMaxDeltaPhase` rationale's arithmetic is TRUE at the new constant: the coupled-rate increment maximum is now 0.495, which 0.5 clears by roughly one percent, not the two percent the comment claimed at 0.49"
    - "The existing suite is bit-stable in its assertions: every non-comment reference to the constant across `src/` and `tests/` is symbolic, so the value change propagates without a test edit"
  artifacts:
    - "src/dsp/VcoCore.hpp — `constexpr float kVcoNyquistGuardFrac = 0.495f;` with settled rationale"
    - "src/dsp/VcoCore.hpp — corrected margin arithmetic in the `kVcoMaxDeltaPhase` justification comment"
    - "src/dsp/VcoCore.hpp — the two `-21609.00` historical measurements annotated as pre-Phase-31 observations, their numbers unchanged"
  key_links:
    - "`kVcoNyquistGuardFrac` -> `const float maxFreq = kVcoNyquistGuardFrac * safeRate;` (the only non-comment use in this header) -> `if (freq > maxFreq) freq = maxFreq;`"
    - "`kVcoNyquistGuardFrac` -> `tests/test_vco_core.cpp:347` (broken-control mirror) and `:736` (`expectedMaxFreq`) — both symbolic, so both follow the value automatically"
  prohibitions:
    - "D-12: `kVcoMaxDeltaPhase`'s VALUE and TYPE must not change; `grep -c 'constexpr double kVcoMaxDeltaPhase = 0.5;'` stays 1"
    - "CR-01: the two frequency-guard lines must not be reordered — the ceiling first, the negated floor last as the final writer"
    - "D-13: no low-end frequency floor is added; the negated `if (!(freq > 0.f)) freq = 0.f;` stays exactly as written"
    - "Pitfall 8: the recorded `-21609.00` measurement literals must NOT be recomputed or search-and-replaced — they are historical observations, not current expectations"
    - "The pitch expression at the `exp2_taylor5` call site is NOT touched by this plan (that is 31-03's, and splitting the edits keeps each commit self-consistent)"
    - "No frozen header edited: `src/dsp/RackCompat.hpp`, `src/dsp/Waveshape.hpp`, `src/dsp/LfoCore.hpp` and `src/dsp/FROZEN.sha256` must be absent from this plan's diff"
    - "`src/AnalogLFO.cpp` must be absent from this plan's diff (D-16)"
    - "No `inline constexpr` and no in-class `static constexpr` is introduced (the construct that got v2.0.0 rejected)"
    - "`struct VcoCore {` and the `step` signature line each stay on ONE line with their opening brace, and the full signature is never quoted in a comment on a line containing a brace (check_canary.sh `[2b/5]` matcher is UNANCHORED)"
---

<objective>
Retire the provisional Nyquist constant and land PITCH-04's real policy.

`src/dsp/VcoCore.hpp:84` carries a placeholder value with a comment that names Phase 31 as the
phase that replaces it. This plan is that replacement: the value becomes `0.495f` per D-11 and
the comment becomes the settled rationale, including D-10's decision that the clamp is a hard
ceiling the oscillator keeps sounding at.

One non-obvious consequence has to land in the same commit. The `kVcoMaxDeltaPhase`
justification comment argues that `0.5` was chosen because it clears the coupled-rate increment
maximum "by roughly two percent". At `0.495` that maximum rises and the margin becomes roughly
one percent. The constant must not move (D-12 is explicit and its own comment tells this phase
so), but leaving the arithmetic claim standing reproduces exactly the false-comment class that
plan 30-08 existed to remove.

Purpose: PITCH-04's policy stops being provisional, and no comment in the file is falsified by
the change.
Output: one constant value, three comment regions, zero behavioral edits to the guard sequence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-CONTEXT.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-PATTERNS.md
@src/dsp/VcoCore.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set kVcoNyquistGuardFrac to 0.495f and replace its placeholder comment with the settled PITCH-04 rationale (D-10 / D-11)</name>
  <files>src/dsp/VcoCore.hpp</files>
  <read_first>
    - `src/dsp/VcoCore.hpp` in full. Specifically: lines 20-29 (the source-shape contract and the unanchored-matcher trap), 41-64 (the binding C++11 rules), 78-95 (the constants block and the eight-line `kVcoMaxDeltaPhase` comment that tells this phase to leave that constant alone), and 200-234 (the sanitised rate, the ceiling, the negated floor and the CR-01 ordering narrative).
    - `.planning/phases/31-pitch-tuning-exponential-fm/31-RESEARCH.md` §Nyquist Policy — the derived ceilings and crossover volts at 44.1 / 48 / 96 / 192 kHz, and the measured blast-radius table naming every site the change touches.
    - `.planning/phases/31-pitch-tuning-exponential-fm/31-PATTERNS.md` §`src/dsp/VcoCore.hpp` Pattern 1 — the namespace-scope plain `constexpr` idiom the new value must keep.
  </read_first>
  <action>
Change the value of `forge::kVcoNyquistGuardFrac` from its current provisional value to
`0.495f`, keeping it a namespace-scope plain `constexpr float` in the same constants block.

Replace its trailing marker and forward-pointing clause with the settled rationale. Because
the acceptance criteria negative-grep the placeholder word, write the new rationale as
WHOLE-LINE comments above the declaration rather than as a long trailing comment, and keep any
trailing comment on the declaration line short and free of the removed marker word.

<!-- planner-discipline-allow: PROVISIONAL -->
<!-- planner-discipline-allow: 0.49f -->
The words to remove are the marker `PROVISIONAL` and the clause promising that Phase 31 will
replace the constant. The value being replaced is `0.49f`.

The new rationale must state, in the house style (measured figures, named decisions, an
explicit prohibition where one exists):

  - the policy: clamp the oscillator frequency to `kVcoNyquistGuardFrac * sampleRate`, which is
    `0.5 x sampleRate x 0.99` — half the sample rate with a one-percent guard band (D-11,
    `.planning/research/STACK.md:122`);
  - the derived ceilings, which are what makes the choice auditable: 21829.5 Hz at 44100,
    23760.0 Hz at 48000, 47520.0 Hz at 96000. Above human hearing, so the clamp is inaudible
    in normal use;
  - the crossover volts the ceiling corresponds to off C4, which is where 1V/oct tracking
    intentionally stops: about +6.3826 V at 44100, +6.5049 V at 48000, +7.5049 V at 96000 —
    and that Phase 31's TEST-02 gate DERIVES these from this constant rather than hardcoding
    them (D-21), so moving the constant moves the gate;
  - D-10's decision: this is a HARD clamp. Frequency pins at the ceiling and the oscillator
    keeps sounding. Under deep FM (1.0 oct/V at full attenuverter means a +/-5 V audio-rate
    modulator swings +/-5 octaves) the clamp fires on most cycles and peaks flatten out at the
    top. That flattening is the chosen sound, not a defect. Amplitude fade above the threshold
    was considered and rejected because it adds a gain stage that collides with Phase 34's
    OUT-01..03; pitch fold-back was rejected because it is a deliberate effect, not the guard
    PITCH-04 asks for;
  - D-13's counterpart at the low end: no floor is added. Extreme negative pitch freezes the
    phase and the output becomes effectively DC, which is honest — PITCH-04 speaks only to the
    top end;
  - that this constant is a Nyquist POLICY bound on the FREQUENCY and remains a different kind
    of constant from the wrap-correctness bound on the phase increment declared below it, which
    D-12 leaves untouched.

Also update the file banner. The banner currently lists "the non-provisional Nyquist policy" as
work Phase 31 still owes. That clause is false the moment this task lands, so amend the banner
sentence to say the Nyquist policy is settled at `0.495f` (PITCH-04 / D-11) while the
volt-domain summation of coarse, fine and FM before the single exponential is still Phase 31's
remaining pitch work, landed by the plan that follows this one. Keep the banner sentence
truthful at THIS commit; 31-03 rewrites it again when the summation lands.

Do not touch the sanitised-rate ternary, the ceiling line, the negated floor line, their order,
or the `tel.freqHz` assignment. Do not touch `kVcoFreqC4` or `kVcoMaxDeltaPhase`. Do not touch
the pitch expression — that is 31-03's.
  </action>
  <verify>
    <automated>make guards && make strict && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'constexpr float kVcoNyquistGuardFrac = 0.495f;' src/dsp/VcoCore.hpp` returns `1`
    - `grep -c '= 0\.49f' src/dsp/VcoCore.hpp` returns `0`
    - `grep -c 'PROVISIONAL' src/dsp/VcoCore.hpp` returns `0`
    - `grep -c 'constexpr double kVcoMaxDeltaPhase = 0.5;' src/dsp/VcoCore.hpp` returns `1` (D-12 — value and type unchanged)
    - `grep -cE 'inline constexpr' src/dsp/VcoCore.hpp` returns `0`
    - `grep -v '^[[:space:]]*//' src/dsp/VcoCore.hpp | grep -c 'static constexpr'` returns `0`
    - `grep -c '^struct VcoCore {$' src/dsp/VcoCore.hpp` returns `1` (source-shape contract)
    - `grep -c 'float step(const VcoInputs& in) {' src/dsp/VcoCore.hpp` returns `1` — exactly one, so the signature was not quoted into a comment (the `[2b/5]` matcher is unanchored)
    - `grep -n 'if (freq > maxFreq) freq = maxFreq;' src/dsp/VcoCore.hpp` reports a line number strictly LESS than `grep -n 'if (!(freq > 0.f)) freq = 0.f;' src/dsp/VcoCore.hpp` (CR-01 ordering intact)
    - `grep -c '21609' src/dsp/VcoCore.hpp` returns `1` — the historical measurement literal is unchanged in value
    - `grep -ci 'hard clamp\|pins at the ceiling\|keeps sounding' src/dsp/VcoCore.hpp` returns at least `1` (D-10 recorded)
    - `grep -c '21829' src/dsp/VcoCore.hpp` returns at least `1` (the derived 44.1 kHz ceiling recorded)
    - `make guards` exits 0 and prints `guard suite: PASS`
    - `make strict` exits 0 and prints `strict C++11 gate: PASS`
    - `make test` exits 0 with 0 failed, and both the case count and the assertion count are IDENTICAL to the counts captured from a `make test` run taken before this task's edit (record both numbers in the summary)
    - `git diff --name-only` lists only `src/dsp/VcoCore.hpp`
  </acceptance_criteria>
  <done>PITCH-04's Nyquist policy is settled at `0.495f` with derived ceilings and D-10's hard-clamp decision recorded in the source, the existing suite is unchanged in case and assertion count, and no guard moved.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the kVcoMaxDeltaPhase margin arithmetic and annotate the historical measurements (D-12 / Pitfall 8)</name>
  <files>src/dsp/VcoCore.hpp</files>
  <read_first>
    - `src/dsp/VcoCore.hpp` lines 236-262 — the "WHY 0.5 AND NOT kVcoNyquistGuardFrac" paragraph whose arithmetic this task corrects, and the WR-01 decoupling paragraph above it which stays as written.
    - `src/dsp/VcoCore.hpp` lines 203-234 — the CR-01 narrative containing the recorded `-21609.00` measurement.
    - `.planning/phases/31-pitch-tuning-exponential-fm/31-RESEARCH.md` §Nyquist Policy, the blast-radius table row for lines 249-250, and §Common Pitfalls Pitfall 8 — the rule that a recorded past measurement is history, not an expectation.
  </read_first>
  <action>
<!-- planner-discipline-allow: roughly two percent -->
Correct the arithmetic in the paragraph that justifies keeping the phase-increment bound at
`0.5`. It currently reasons that at a coupled rate the guarded frequency yields an increment
of "0.49 plus float rounding" and that `0.5` "clears that maximum by roughly two percent".
Restate it with the numbers that are true at the new constant: at a coupled rate
(`sampleTime == 1 / sampleRate`) the guarded frequency now yields an increment of `0.495` plus
float rounding, so `0.5` clears that maximum by roughly one percent rather than two.

Keep every conclusion the paragraph reaches, because none of them changes: `0.5` still clears
the maximum, so the bound still cannot fire on a legitimate coupled-rate input and cannot move
samples; every existing measurement stays bit-identical for that reason; and any bound strictly
below `1.0` still satisfies the single-subtract wrap. Add one sentence recording that the margin
narrowed from about two percent to about one percent when PITCH-04's policy moved, that D-12
forbids widening the bound in response, and that a future phase raising the guard fraction
further must re-check this specific margin rather than assume it.

Then annotate — do not recompute — the recorded measurement literals. The CR-01 narrative in
this file records an observed `tel.freqHz` figure from a reproduction run performed under the
previous guard fraction. Add a short parenthetical to that narrative stating the figure was
measured under the pre-Phase-31 guard fraction and is a historical observation rather than a
current expectation. The digits themselves must not change: a diff that alters a number inside
a paragraph beginning "MEASURED" is the exact defect Pitfall 8 names.

Finally, confirm by reading rather than by assumption that `kVcoMaxDeltaPhase`'s declaration,
type, value and its own eight-line comment survived Task 1 and this task untouched, and record
that confirmation in the summary.
  </action>
  <verify>
    <automated>make guards && make strict && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'roughly two percent' src/dsp/VcoCore.hpp` returns `0`
    - `grep -ci 'roughly one percent\|about one percent' src/dsp/VcoCore.hpp` returns at least `1`
    - `grep -c '0.495' src/dsp/VcoCore.hpp` returns at least `2` (the declaration plus the corrected increment-maximum arithmetic)
    - `grep -c 'constexpr double kVcoMaxDeltaPhase = 0.5;' src/dsp/VcoCore.hpp` returns `1`
    - `grep -c '21609' src/dsp/VcoCore.hpp` returns `1` and the surrounding line still contains `tel.freqHz` — the literal is annotated, not rewritten
    - `grep -ci 'pre-Phase-31\|pre-phase-31' src/dsp/VcoCore.hpp` returns at least `1`
    - `grep -c 'DO NOT SWAP' src/dsp/VcoCore.hpp` returns `1` — the CR-01 prohibition is still in caps and still present
    - `grep -c 'if (!(freq > 0.f)) freq = 0.f;' src/dsp/VcoCore.hpp` returns `1` (D-13 — the floor is unchanged and there is exactly one)
    - `make guards` exits 0; `make strict` exits 0; `make test` exits 0 with 0 failed and unchanged case/assertion counts
    - `git diff --stat src/dsp/VcoCore.hpp` shows only comment-region changes for this task (zero changes to any line ending in `;` other than the constant already changed in Task 1) — state the line-level evidence in the summary
  </acceptance_criteria>
  <done>No comment in `src/dsp/VcoCore.hpp` makes an arithmetic claim the new constant falsifies, the recorded historical measurements survive with their digits intact, and `kVcoMaxDeltaPhase` is provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `VcoInputs.sampleRate` (injected by the Rack shell or the harness) -> the Nyquist ceiling | a non-positive or NaN rate has no meaningful Nyquist limit; the sanitising ternary upstream of the multiply is the boundary control (WR-06) |
| `VcoCore::step` -> `outputs[OUTPUT].setVoltage` -> every downstream module in the user's patch | an unbounded or non-finite sample propagates through the whole patch, so the ceiling and floor are patch-level containment, not cosmetics |
| `src/dsp/VcoCore.hpp` -> `src/dsp/FROZEN.sha256` / the shipped LFO | the VCO header is NOT frozen, but it sits one include away from three headers that are |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-03 | Denial of Service | `VcoCore::step` frequency ceiling | high | mitigate | `kVcoNyquistGuardFrac * safeRate` ceiling retained and tightened to a settled policy; research MEASURED that with no ceiling `pitchCV = +10` drives the accumulator to phase 1,014,986 and the output to -8,655,011 V while every sample stays finite, so a finiteness test cannot see it — the magnitude bound in `tests/test_vco_core.cpp` is what observes it, and it still runs |
| T-31-11 | Denial of Service | guard-line ORDER inside `step(...)` | critical | mitigate | CR-01 is pinned by an acceptance criterion comparing the two guards' line numbers, by the in-source `DO NOT SWAP` prohibition, and by scenario four of `tests/test_vco_core.cpp`; the measured consequence of the swap is `|out| = 1.476e38 V` with a non-finite sample |
| T-31-12 | Denial of Service | `kVcoMaxDeltaPhase` wrap-correctness bound | high | mitigate | Value and type pinned by acceptance criteria (D-12); the narrowed margin is DOCUMENTED rather than absorbed by widening the bound, and nothing in `VcoInputs` couples `sampleRate` to `sampleTime` (WR-01, measured) so the two guards stay independent |
| T-31-04 | Tampering | frozen shared headers / shipped LFO goldens | critical | mitigate | `check_frozen.sh` byte-pins `RackCompat.hpp`, `Waveshape.hpp`, `LfoCore.hpp`, `MathConst.hpp` and runs inside `make guards` after every header edit; six `.f32` LFO goldens replay inside `make test`; the plan's diff-name assertion fails if `FROZEN.sha256` or `src/AnalogLFO.cpp` appears |
| T-31-13 | Tampering | comments that outlive the arithmetic they assert | medium | mitigate | Task 2 exists solely for this: a false comment is the failure mode plan 30-08 was created to remove, and the acceptance criteria negative-grep the falsified claim rather than trusting review |
| T-31-SC | Tampering | package-manager installs | low | accept | Zero registry packages installed this phase; `31-RESEARCH.md` §Package Legitimacy Audit records an empty table because every symbol is in-tree or from the pinned local `../Rack-SDK` |
| — | Spoofing / Repudiation / Information Disclosure | — | n/a | accept | An offline Rack-free DSP header has no identity, audit-log or confidentiality surface |
</threat_model>

## Artifacts this phase produces

Phase-wide symbol surface, with the owning plan named. Later plans consume these and must not
redefine them.

**`src/dsp/VcoCore.hpp` — namespace-scope plain `constexpr` inside `namespace forge`:**
- `kVcoMaxPitchVolts` = `64.f` — NEW (31-03)
- `kVcoNyquistGuardFrac` — existing name, value `0.49f` -> `0.495f` (**this plan**)
- `kVcoFreqC4` — existing, value UNCHANGED, comment updated (31-03)
- `kVcoMaxDeltaPhase` — existing, UNCHANGED (D-12)

**`src/dsp/VcoCore.hpp` — inside `step(...)`:** local `float pitchVolts` (31-03). No new
`VcoInputs` field and no new `Telemetry` field. `VcoInputs::coarse`, `::fine`, `::fmVolts`,
`::fmAtten`, `::fmConnected` change from declared-and-unread to READ (31-03).

**`src/AnalogVCO.cpp` — enum entries:** `ParamId::COARSE_PARAM`, `ParamId::FINE_PARAM`,
`ParamId::FM_ATTEN_PARAM`, `InputId::FM_INPUT` (all 31-04).

**`res/AnalogVCO.svg` — four new `10 x 10` `#2a2a30` marker rects (31-04):**
`x="15.32" y="55"`, `x="40.72" y="55"`, `x="66.12" y="55"`, `x="40.72" y="95"`.

**`tests/test_vco_pitch.cpp` — NEW FILE (31-05), every helper in ONE anonymous namespace:**
`SAMPLE_RATES[]`, `kTrackingToleranceCents`, `kEstimatorMinSamplesPerCycle`, `pitchBase()`,
`estimateFreqRising()`, `expectedFreqHz()`, `centsError()`, `windowSamples()`,
`clampCeilingVolts()`, `estimatorCeilingVolts()`, `topTestVolts()` (31-05);
`runBlockOn<CoreT>()` and `DeliberatelyMultiplicativeFmCore` (31-06); `kPitchLooseBoundV` (31-07).

**`tests/test_vco_pitch.cpp` — `TEST_CASE` names, i.e. the `-tc` selectors the gate uses:**
`*v/oct tracking*` (31-05), `PITCH-02 COARSE` and `PITCH-03 FINE` (31-06),
`*exponential FM*` (31-06), `*Nyquist*` (31-07), `*hostile pitch*` (31-07).

**`tests/check_includes.sh` (31-01):** `VCO_SIDE_ALLOW` gains `"tests/test_vco_pitch.cpp"`;
the `[2/7]` exclusion anchored; `[6/7]` gains the `nc5` comment-evasion control.

**`tests/test_vco_core.cpp` (31-07):** `DeliberatelyBrokenSharedStateCore::step(...)` mirror
brought in step with the new pitch block.

**`.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md` — NEW (31-08).**

**Deliberately NOT produced:** no `Telemetry` field (D-22 declined it), no new `VcoInputs`
field, no `depthScale` constant (D-06), no low-end frequency floor (D-13), no Makefile or CI
wiring, no permanent UBSan target or gate (D-24), no `FROZEN.sha256` change.

<verification>
- `make guards` exits 0 after EVERY edit to this header, not only at the end (Pitfall 5 — the `[2b/5]` source-shape matcher hard-fails on formatting with a message that reads like a DSP error)
- `make strict` exits 0 — the C++11 `-pedantic-errors` gate over `src/*.cpp`, which reaches this header through both `src/AnalogVCO.cpp` and `src/vco_compile_canary.cpp`
- `make test` exits 0 with the same case count, the same assertion count and 0 failures as the pre-edit baseline; the six LFO goldens replay byte-identical inside that run
- Every non-comment reference to `kVcoNyquistGuardFrac` in `src/` and `tests/` is confirmed symbolic by `grep -rn 'kVcoNyquistGuardFrac' src tests` — no Hz literal was introduced anywhere
</verification>

<success_criteria>
- `kVcoNyquistGuardFrac` is `0.495f` with the settled PITCH-04 rationale, the derived ceilings, the derived crossover volts and D-10's hard-clamp decision recorded in the source
- No comment in the header asserts arithmetic the new value falsifies, and no recorded historical measurement had its digits changed
- `kVcoMaxDeltaPhase`, the CR-01 ordering, the negated floor and the pitch expression are all provably untouched
- `make guards`, `make strict` and `make test` all green with unchanged test counts
</success_criteria>

<output>
Create `.planning/phases/31-pitch-tuning-exponential-fm/31-02-SUMMARY.md` when done.
Record: the pre-edit and post-edit `make test` case and assertion counts side by side, the
before/after text of both comment regions, and the `grep -rn 'kVcoNyquistGuardFrac' src tests`
output proving every live reference is symbolic.
</output>

---
phase: 31-pitch-tuning-exponential-fm
plan: 08
type: execute
wave: 6
depends_on: ["31-04", "31-07"]
files_modified:
  - .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md
autonomous: true
requirements: [PITCH-01, PITCH-02, PITCH-03, PITCH-04, PITCH-05, FM-01, FM-02, FM-03, TEST-02]

must_haves:
  truths:
    - "TEST-02 is a HARD GATE: every tracking case is green AND its selector is proven to have matched a non-zero number of cases, because a selector that matches nothing also exits zero"
    - "The full local gate is green together on one tree: the headless suite, the guard suite, the C++11 strict gate and a real plugin link against the local SDK"
    - "The prohibitions are verified over the whole phase diff, not per plan: no frozen header, no shipped-module source, no libm exponential or power function in `src/`, exactly one exponential in the pitch chain, no in-class constant, no sanitizer wiring"
    - "The three-operating-system CI matrix and the Windows link leg are observed green BY COMMIT HASH, never by run name and never by recency, and the leg's OWN step conclusion is read rather than only its job's"
    - "D-18: this phase's own measured tracking figures are collected in one place, and neither prior research figure is presented as the expected magnitude"
    - "D-24: the shipped module's identical latent undefined behavior is recorded as deferred, pointed at no phase, marked unfixed by decision and marked a guardrail event"
  artifacts:
    - ".planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md — the phase's deferred register, including the shipped-module finding and the status of every inherited item"
    - "31-08-SUMMARY.md — the full local gate transcript, the per-selector matched counts, the measured-figures roll-up and the CI observation record with its commit hash"
  key_links:
    - "each `-tc` selector -> the case it must match -> its reported matched count, which is what distinguishes a gate that passed from a gate that never ran"
    - "the pushed commit hash -> the CI run located by hash equality -> the toolchain job's conclusion AND its Windows-leg step's own conclusion"
  prohibitions:
    - "Do not read a CI conclusion before confirming the run's head commit hash equals the pushed hash — locating a run by name or by recency is the failure mode this project has already recorded twice"
    - "Do not accept a job conclusion as sufficient: a step that fail-fasts upstream is reported as skipped, which scans as not-red in a job summary"
    - "Do not adopt a permanent repository-wide sanitizer gate, target or CI step (D-24) — it would turn the shipped module red"
    - "Do not mark any requirement complete in this plan; the phase-completion flow owns that. Record readiness instead"
    - "Do not edit `src/`, `res/`, `tests/`, the Makefile or any workflow file in this plan"
    - "Do not soften, rewrite or delete any figure recorded by an earlier plan in this phase; roll them up as observed"
    - "Do not resolve deferred items this phase did not actually address — in particular, the shared clamp helper's not-a-number transparency is only HALF closed, and the half about the morph and character inputs stays pointed at its own phase"
---

<objective>
Close the phase on evidence rather than on a green feeling.

TEST-02 is this phase's exit criterion, and the phase-specific constraint is that it must be a hard
gate rather than a closing nicety. This plan makes it one: every tracking case runs, every selector
is proven to have matched cases, and the four local gates plus the off-machine matrix are recorded
together on one tree with their numbers written down.

It also writes the phase's bookkeeping, and there is real substance in it. The shipped Analog LFO
carries the identical latent undefined behavior this phase hardened the VCO against — measured, not
suspected — and fixing it is a guardrail event nobody has signed off. That has to be recorded
pointed at no phase, with the reasoning attached, so a later phase does not rediscover it cold and
so nobody wires a repository-wide sanitizer gate that would immediately turn the live module red.
One inherited item is only half closed by this phase, and saying so is part of the job.

Purpose: the phase's claims are checkable by someone who was not here.
Output: a verified gate transcript and a deferred register.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-CONTEXT.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-VALIDATION.md
@.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-03-SUMMARY.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-05-SUMMARY.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-06-SUMMARY.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The full local gate, with TEST-02 enforced as a hard gate and every prohibition verified over the whole phase diff</name>
  <files>(verification only — no repository file is modified)</files>
  <read_first>
    - `.planning/phases/31-pitch-tuning-exponential-fm/31-VALIDATION.md` §Per-Task Verification Map and §Non-Vacuity Requirements — the row set this gate has to discharge and the six requirements that must each be represented.
    - All four earlier summaries in this phase — the recorded counts, figures and selector counts this gate compares against.
    - `.planning/STATE.md` §Accumulated Context, the Phase-29 entry beginning "P-2 CORRECTED and widened" — the measured fact that the ENTIRE local gate returned success on a commit that could not link. That is why a real link is part of this task and why the off-machine matrix is task 2's.
    - `Makefile` — the four targets and exactly what each one does and does not cover.
  </read_first>
  <action>
Run the whole local gate on the current tree and record every number.

**The four gates.** In order: the headless suite; the guard suite; the C++11 strict syntax gate; and
a real plugin link against the local SDK. Record for each the exit status and the salient output —
for the suite, the case count, the assertion count and the failure count; for the guard suite, each
section's line; for the strict gate and the link, their completion.

**TEST-02 as a hard gate.** Run each of the phase's selectors individually and record BOTH the exit
status AND the number of test cases the run reports as executed:

  - the tracking selector, expected to match two cases (the primary output tier and the secondary
    telemetry tier);
  - the coarse selector and the fine selector, one case each;
  - the exponential-FM selector, expected to match two cases (the invariant and its permanent
    negative control);
  - the Nyquist selector and the hostile-pitch selector, one case each.

A selector that matches nothing exits successfully, so the matched count is the assertion, not the
exit status. If any count is zero or differs from the expectation, the gate FAILS and you stop and
report — a mismatched count means either a case name drifted or a case is missing, and both are
findings.

Also record the whole-suite case count against the recorded pre-phase baseline. The phase adds nine
cases to one new translation unit, so the expected total is the baseline plus nine. Compute the
actual rather than trusting the expectation, and if it differs, reconcile it explicitly by naming
which cases account for the difference.

**Prohibitions over the whole phase diff.** Derive the phase's diff by comparing the current tree
against the commit immediately preceding this phase's first commit, and verify:

  - none of the frozen shared headers appears, and the frozen manifest does not appear;
  - the shipped module's source file does not appear;
  - with whole-line comments stripped, no file under `src/` contains libm's base-2 exponential or
    power function;
  - with whole-line comments stripped, the VCO core header contains exactly ONE call to the frozen
    polynomial;
  - with whole-line comments stripped, no file under `src/` contains an inline constant-variable
    declaration or an in-class constant declaration;
  - the Makefile and every workflow file contain no sanitizer flag;
  - the sample-rate and sample-time hostile grids in the existing test file are unchanged;
  - the two recorded historical measurement literals still carry their original digits;
  - the phase diff's file list matches exactly the set the plans declared, with nothing extra.

**Roll up the measured figures.** Collect into one table in the summary every figure this phase
measured itself: the two tracking tiers' worst absolute cents per rate with the volt at which each
occurred; the coarse and fine cases' worst cents per rate; the measured hundred-cent shifts at the
fine range ends; the FM identity grid with its summed volts; the negative control's per-point
mismatch counts including the integer point's zero; the observed telemetry frequency, peak magnitude
and crossing count at each side of the clamp boundary; the computed reachable-envelope total and its
ratio to the bound; and the two sanitizer transcripts. State, in one sentence, that these are this
phase's own measurements and that the two contradictory figures in the prior-milestone research
documents are cited nowhere in the delivered code or tests.

Then state, requirement by requirement for all nine identifiers, which case or which source
assertion discharges it, and note that the identifiers are READY to mark rather than marking them
here.
  </action>
  <verify>
    <automated>make test && make guards && make strict && make</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; the case count equals the recorded pre-phase baseline plus 9 (or the difference is reconciled case by case in the summary)
    - `make guards` exits 0 and prints `guard suite: PASS`; `make strict` exits 0 and prints `strict C++11 gate: PASS`; `make` completes and produces a linked plugin binary
    - Each of the six selectors exits 0 AND reports a non-zero matched case count equal to its expectation (2, 1, 1, 2, 1, 1); every count is written into the summary
    - `git diff --name-only <pre-phase-commit>..HEAD | grep -cE 'AnalogLFO\.cpp|RackCompat\.hpp|Waveshape\.hpp|LfoCore\.hpp|MathConst\.hpp|DriftEngine\.hpp|FROZEN\.sha256'` returns `0`
    - `grep -rv '^[[:space:]]*//' src/ | grep -c 'std::exp2\|std::pow'` returns `0`
    - `grep -v '^[[:space:]]*//' src/dsp/VcoCore.hpp | grep -c 'exp2_taylor5('` returns `1`
    - `grep -rc 'inline constexpr' src/` returns `0` for every file, and `grep -rv '^[[:space:]]*//' src/ | grep -c 'static constexpr'` returns `0`
    - `grep -rc 'fsanitize' Makefile .github/` returns `0` for every file
    - `git diff <pre-phase-commit>..HEAD -- tests/test_vco_core.cpp | grep -c 'HOSTILE_RATES\|HOSTILE_TIMES'` returns `0` (D-15 — the timing grids are untouched)
    - `grep -c '21609' src/dsp/VcoCore.hpp` returns `1` and `grep -c '21609' tests/test_vco_core.cpp` returns `1` — both historical literals intact
    - The phase diff's file list is exactly: `tests/check_includes.sh`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `tests/test_vco_pitch.cpp`, `tests/test_vco_core.cpp` plus files under `.planning/`; anything else is reported as a finding
    - The measured-figures table in the summary contains at least one figure for every row of the validation strategy's verification map, and all six of its non-vacuity requirements are named with the case that implements each
  </acceptance_criteria>
  <done>All four local gates are green on one tree, every selector is proven to have matched the cases it names, every prohibition holds across the whole phase diff, and the phase's own measurements are collected in one place.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the three-operating-system matrix and the Windows link leg green by commit hash</name>
  <files>(verification only — no repository file is modified)</files>
  <read_first>
    - `.planning/STATE.md` §Accumulated Context, the three Phase-30 entries about closing on observed CI evidence: the one beginning "Phase 30 closed on OBSERVED CI evidence, not local green", the one beginning "The plan's own CI-run selector was unsatisfiable", and the one beginning "The three-OS matrix gap is discharged by ACCOUNTING for it". Read all three before writing a single query: the first says the leg's OWN step conclusion must be read, the second records that a selector matching a run NAME returns nothing because the leg is a JOB inside a differently-named workflow, and the third says the macOS-versus-other case-count gap must be accounted for rather than assumed.
    - `.github/workflows/` — the workflow file, its job names and its step names, so the query targets what actually exists rather than what a plan guessed.
    - `31-08-SUMMARY.md` as written so far — the local gate is a PRECONDITION for this task, recorded as such, because a prior phase measured that exact combination green on code that could not link.
  </read_first>
  <action>
Push the phase's commits to the remote tracking branch — this repository's established workflow — and
then observe the resulting run.

Before pushing: confirm the working tree is clean, confirm every plan commit for this phase is
present, and record the exact commit hash you are about to push. If pushing the current branch is not
already this repository's established workflow, stop and surface that rather than pushing.

Then observe, in this order and with these rules:

  1. Locate the run by HEAD COMMIT HASH EQUALITY. Query the runs and select the one whose head commit
     hash equals the hash you pushed. Never select by run name, never by branch, never by recency.
     Compare the returned hash to the pushed hash and print both before reading anything else — a
     prior phase's plan selected by run name, got an empty result, and its own guard then failed for
     a reason unrelated to CI health.
  2. Read the toolchain job's conclusion. Record it, and record that it is NOT sufficient on its own.
  3. Extract, across all jobs in that run, the STEP whose name identifies the Windows leg
     reproduction — the compile-and-full-link against the Rack library — and read that STEP's OWN
     conclusion. A step that fail-fasts upstream is reported as skipped, which reads as not-red in a
     job summary. Record the step name verbatim and its own conclusion.
  4. Account for the matrix's case-count gap rather than assuming it. Record the case counts reported
     on each operating system, and account for any difference by naming the cases that are gated to
     one platform. If the difference is anything other than that accounting, it is a finding: it would
     mean cases were silently dropped on some platform, which would include this phase's new ones.
     Confirm explicitly that all of this phase's nine new cases ran on all three legs.
  5. If the run is red, capture the failing job, the failing step and its log excerpt, and stop. A red
     Windows leg on this phase most plausibly means a construct the local syntax-only gate cannot
     see — the class that got a previous version rejected — and that is a finding to report, not to
     iterate on silently.

Record everything in the summary: the pushed hash, the located run's hash, the run identifier, the
job conclusion, the step name and its own conclusion, and the per-platform case counts with the gap
accounted for.
  </action>
  <verify>
    <automated>git status --porcelain | grep -cx '' ; git rev-parse HEAD</automated>
  </verify>
  <acceptance_criteria>
    - The working tree was clean before the push and the pushed commit hash is recorded in the summary
    - The located run's head commit hash is recorded alongside the pushed hash and the two are IDENTICAL; the summary states that the run was located by hash equality and not by name or recency
    - The toolchain job's conclusion is recorded as success, and the summary explicitly states that the job conclusion alone is not sufficient
    - The Windows leg reproduction step's name is recorded verbatim and its OWN conclusion is recorded as success
    - The case counts reported on each of the three operating systems are recorded, and any difference is accounted for by naming the platform-gated cases; the summary states affirmatively that all nine of this phase's new cases ran on all three legs
    - If the run is red, the failing job, the failing step and a log excerpt are recorded and the plan reports a finding rather than continuing
  </acceptance_criteria>
  <done>The off-machine matrix and the Windows link leg are observed green on the exact commit this phase produced, located by hash, with the leg's own step conclusion read and the matrix's case-count gap accounted for rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Write the phase's deferred register, including the shipped module's identical latent undefined behavior (D-24)</name>
  <files>.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md</files>
  <read_first>
    - `.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md` in full — the format to follow (found during, observation, why it is worth a note, why it is not fixed here, resolve at) and the current status of all six items, including the one plan 31-01 resolved.
    - `.planning/phases/31-pitch-tuning-exponential-fm/31-CONTEXT.md` `<deferred>` — the six items this phase inherits or creates, and the explicit instruction that one of them was superseded and had to be decided rather than left contradicted.
    - `31-03-SUMMARY.md` — the sanitizer transcript, which is the evidence behind the shipped-module finding.
    - `src/dsp/LfoCore.hpp` lines 178-190 and `src/AnalogLFO.cpp` around line 320 — read them so the finding names the two real call sites rather than paraphrasing a research document. Read only; neither file may be edited.
  </read_first>
  <action>
Create this phase's deferred register in the same format the previous phase used.

**Item — the shipped module's identical latent undefined behavior (D-24).** This is the substantive
one. Record: that the shipped module's shell reads an unsanitised cable voltage from its FM input and
its core feeds that value, scaled, straight into the same frozen float-to-int cast and left shift
this phase hardened the VCO against; the two file-and-line references, read directly rather than
paraphrased; that this was MEASURED in this phase, with the sanitizer transcript in the 31-03 summary
as the evidence; that it is a GUARDRAIL EVENT requiring operator sign-off and a golden
re-verification, because the fix would mean editing a byte-pinned header consumed by a live,
library-published module; that it is UNFIXED BY DECISION and pointed at NO PHASE; and the direct
consequence, which is the reason this entry exists at all — a permanent repository-wide sanitizer
gate cannot be adopted while this stands, because it would turn the shipped module red, so this
phase's use of the sanitizer was deliberately a one-shot probe.

**Item — the shared clamp helper's not-a-number transparency, HALF closed.** The inherited item was
pointed at this phase or a later one, in the plan that adds the morph and character control-voltage
inputs. Record honestly what happened: this phase honoured that item's binding constraint for the new
pitch-volt bound — the guard is local to the VCO core, uses the negated-comparison idiom rather than
the shared helper, and is bit-identical to nothing it replaced — but the morph and character clamps
themselves still call the not-a-number-transparent helper and are still inert against that input
class. Those inputs remain unreachable by a cable until their control-voltage inputs land, so that
half stays pointed at the phase that adds them, with the same constraint attached: a fix must be
local to the VCO core, never an edit to the shared helper.

**Item — the hostile-timing grid extension.** Restate that it remains pointed at the phase whose
oversampled inner loop is the first real source of exotic timing, that this phase deliberately did
not extend it, and that this phase's hostile-input work covered a different input class — the pitch
and FM fields.

**Item — the guard script's unanchored exemption filter.** Record that it was RESOLVED in this phase
by plan 31-01, with a cross-reference to the resolution block appended to the previous phase's
register, and note the reason this phase owned it: a decision in this phase made this the next phase
to touch that script, which is exactly the condition the item's own resolution clause named.

**Item — per-instance seed entropy and patch persistence.** Restate unchanged: still pointed at the
later phases that own shell wiring and patch state, with the requirement that any deserialised seed
be re-validated the same way, because a degenerate pair is a hang while opening a patch rather than
a failing test.

**Item — the coarse control's octave snap.** Record it as deferred, with the reasoning: the
requirement says continuously and this phase honoured that literally; a snap is a new capability
needing a menu item, a persisted flag and patch serialization the module does not have; the natural
home is the panel phase or a later increment. Note that the project's out-of-scope list mentions an
octave snap but that the entry was written about the shipped module, where sub-audio rates made it
meaningless, and is not a ruling on the VCO.

**Item — amplitude fade near the ceiling.** Record that it was considered and rejected for this
phase's clamp decision because it introduces a gain stage that collides with a later phase's output
stage, and that if the flattened-peak sound proves harsh under deep FM during that phase's audition,
that phase owns the output stage and could revisit it.

**Item — display precision.** Record the deliberate divergence: the tooltip UNITS are what the
decision fixed, the digit count is not, this phase left the default and matched the shipped module,
which sets no precision anywhere, and if exactness ever matters it is one line per parameter on the
pointer the declaration returns.

**Item — the canary's field-count margin narrowed.** Record it as a note for the next phase that adds
a POD field: the shell now feeds all but one of the core's floating-point input fields, so the
canary's remaining unique field coverage is a single field. The canary is still not redundant, for
the reason written into the shell's own comment, but a phase that wires the last field must keep the
canary feeding every field a runtime value or the constant-fold guard silently loses its margin.

**Item — the documentation gate's CI wiring.** Restate that the reviewed todo remains deferred to the
release phase, which owns CI and the release, and that it stays visible on every guard run through
its existing exemption entry.

Finish with a short measured-figures section repeating this phase's own tracking numbers from task 1,
so a reader of the register alone can see what was measured without opening four summaries.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md && make test && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md` exists and contains at least nine numbered items
    - The shipped-module item names both real file-and-line references, is marked a guardrail event, is marked unfixed by decision, is pointed at NO phase, and states the consequence that a permanent repository-wide sanitizer gate cannot be adopted
    - `grep -ci 'guardrail event' .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md` returns at least `1`
    - `grep -ci 'unfixed by decision' .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md` returns at least `1`
    - The clamp-helper item is recorded as HALF closed, states which half this phase honoured and which half remains, and carries the constraint that any fix stays local to the VCO core
    - The unanchored-exemption item is recorded as resolved with a cross-reference to the previous phase's register, and `grep -c 'RESOLVED' .planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md` still returns at least `2`
    - The hostile-timing item is restated as still pointed at its own phase, and the register states that this phase did not extend it
    - `git diff --name-only` for this task lists only the new register file
    - `make test` and `make guards` still exit 0 — this task changes no code
  </acceptance_criteria>
  <done>The phase has a deferred register a later phase can act on without rediscovering anything, the shipped-module finding is recorded with its evidence and its consequence, and no inherited item is marked resolved that this phase did not actually resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local green -> shippable | A prior phase MEASURED the entire local gate returning success on a commit that could not link. The real link and the off-machine matrix are the only things that cross this boundary honestly |
| a CI run -> the commit it actually tested | Selecting a run by name or recency crosses this boundary on faith. Hash equality is the only check that does not |
| a job conclusion -> the leg's real result | A step that fail-fasts upstream reports as skipped, which reads as not-red. The step's own conclusion is the boundary control |
| a `-tc` selector -> the cases it actually ran | The boundary between a passing gate and a gate that never ran is invisible unless the matched count is asserted |
| this phase's evidence -> a later phase's assumptions | An unrecorded finding is rediscovered cold. The deferred register is the boundary artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-20 | Tampering | a `-tc` selector matching zero cases | high | mitigate | Every selector's matched count is asserted against an exact expectation and written into the summary; a mismatch stops the gate as a finding rather than being reconciled silently |
| T-31-28 | Tampering | closing the phase on local evidence alone | high | mitigate | The local gate is recorded as a PRECONDITION, not as the gate; the matrix run is located by head-commit-hash equality, the job conclusion is recorded as insufficient, and the Windows leg's own step conclusion is read separately |
| T-31-29 | Tampering | silently dropped test cases on a platform | medium | mitigate | The per-platform case counts are recorded and any gap is accounted for by naming the platform-gated cases; an unaccounted gap is a finding, because it would mean this phase's new cases did not run everywhere |
| T-31-04 | Tampering | frozen shared headers and the shipped module | critical | mitigate | Verified over the WHOLE phase diff rather than per plan: no frozen header, no frozen manifest, no shipped-module source, exactly one exponential in the pitch chain, no libm exponential or power function under `src/`, no inline or in-class constant declaration |
| T-31-15 | Tampering | a permanent repository-wide sanitizer gate | high | mitigate | Asserted absent from the build file and every workflow, and the reason is recorded as a deferred item so a later phase does not add one and turn the live module red |
| T-31-30 | Repudiation | an unrecorded finding being rediscovered without its reasoning | medium | mitigate | The deferred register names the evidence, the decision, the owner phase and the consequence for each item, and explicitly refuses to mark half-closed items closed |
| T-31-SC | Tampering | package-manager installs | low | accept | Zero registry packages across the whole phase; `31-RESEARCH.md` §Package Legitimacy Audit records an empty table and the gate verifies the build file and workflows are unchanged |
| — | Spoofing / Information Disclosure | — | n/a | accept | No identity or confidentiality surface; the only credential-bearing action is a read of a public CI run through the existing tool |
</threat_model>

## Artifacts this phase produces

Phase-wide symbol surface, with the owning plan named.

**`src/dsp/VcoCore.hpp` constants:** `kVcoMaxPitchVolts` = `64.f` NEW (31-03);
`kVcoNyquistGuardFrac` `0.49f` -> `0.495f` (31-02); `kVcoFreqC4` UNCHANGED with an updated comment
(31-03); `kVcoMaxDeltaPhase` UNCHANGED (D-12). Inside `step(...)`: local `float pitchVolts` (31-03).
No new `VcoInputs` field, no new `Telemetry` field.

**`src/AnalogVCO.cpp` enum entries (31-04):** `ParamId::COARSE_PARAM`, `ParamId::FINE_PARAM`,
`ParamId::FM_ATTEN_PARAM`, `InputId::FM_INPUT`.

**`res/AnalogVCO.svg` (31-04):** four new `10 x 10` `#2a2a30` rects at `x="15.32" y="55"`,
`x="40.72" y="55"`, `x="66.12" y="55"`, `x="40.72" y="95"`.

**`tests/test_vco_pitch.cpp` helpers, all in ONE anonymous namespace:** `SAMPLE_RATES[]`,
`kTrackingToleranceCents`, `kEstimatorMinSamplesPerCycle`, `pitchBase()`, `estimateFreqRising()`,
`expectedFreqHz()`, `centsError()`, `windowSamples()`, `clampCeilingVolts()`,
`estimatorCeilingVolts()`, `topTestVolts()` (31-05); `runBlockOn<CoreT>()`, `kPitchLooseBoundV`,
`DeliberatelyMultiplicativeFmCore` (31-06).

**`tests/test_vco_pitch.cpp` invariants and `-tc` selectors:** 1 derived-boundary self-check, 2 and 3
the tracking tiers under `*v/oct tracking*` (31-05); 4 under `*COARSE*`, 5 under `*FINE*`, 6 and 7
under `*exponential FM*` (31-06); 8 under `*Nyquist*`, 9 under `*hostile pitch*`, plus the
compile-time accumulator-type assertion (31-07). Nine new cases in total.

**`tests/check_includes.sh` (31-01):** `VCO_SIDE_ALLOW` gains `"tests/test_vco_pitch.cpp"`; the
`[2/7]` exclusion anchored; `[6/7]` gains the `nc5` comment-evasion control.

**`tests/test_vco_core.cpp` (31-07):** `DeliberatelyBrokenSharedStateCore::step(...)` mirror brought
in step with the new pitch block.

**`.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md` — NEW (**this plan**).**

**Deliberately NOT produced:** no `Telemetry` field (D-22), no new `VcoInputs` field, no
depth-scaling constant (D-06), no low-end frequency floor (D-13), no second tolerance or loose-bound
constant, no spectral or alias assertion, no extension of the hostile timing grids (D-15), no
Makefile or CI wiring, no permanent UBSan target or gate (D-24), no `FROZEN.sha256` change, no
requirement marked complete by this plan.

<verification>
- `make test`, `make guards`, `make strict` and a real `make` link are all green on ONE tree, with their numbers recorded
- Each of the six selectors reports its exact expected matched case count; the counts are the gate, not the exit statuses
- Every prohibition holds across the whole phase diff, verified with comment-stripped greps rather than by review
- The CI run is located by head-commit-hash equality, the toolchain job's conclusion and the Windows leg step's own conclusion are both recorded as success, and the per-platform case-count gap is accounted for
- The deferred register exists with the shipped-module finding recorded pointed at no phase, and with the one half-closed inherited item marked half closed rather than resolved
</verification>

<success_criteria>
- TEST-02 is discharged as a hard gate: every tracking case green and every selector proven to have matched cases
- All four local gates green together, plus the off-machine matrix and the Windows link leg observed green on this phase's exact commit by hash
- Every prohibition verified over the whole phase diff; no frozen header, no shipped-module source, no sanitizer wiring, exactly one exponential in the pitch chain
- This phase's own measured figures collected in one table, with neither prior research figure presented as the expected magnitude
- A deferred register that a later phase can act on, including the shipped module's identical latent undefined behavior with its evidence, its guardrail status and its consequence
</success_criteria>

<output>
Create `.planning/phases/31-pitch-tuning-exponential-fm/31-08-SUMMARY.md` when done.
Record: the four gate transcripts with their counts; the six selector matched counts; the whole-diff
prohibition sweep results; the measured-figures table; the requirement-to-evidence mapping for all
nine identifiers with a readiness note; and the CI observation record with the pushed hash, the
located run's hash, the job conclusion, the leg step's name and its own conclusion, and the
per-platform case counts.
</output>

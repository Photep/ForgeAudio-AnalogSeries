---
phase: 31-pitch-tuning-exponential-fm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/check_includes.sh
  - .planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "D-23: `make guards` exits 0 with `tests/test_vco_pitch.cpp` named in `VCO_SIDE_ALLOW` BEFORE that file exists — the recurring Phase-30 landmine is disarmed by pre-registration carrying its own rationale, not by a gate-time discovery"
    - "Deferred item 5 (WR-05): `[6/7]` reports a HIT on a fixture whose real directive is a genuine Rack SDK include and whose trailing comment carries the exempted shim path — the evasion the Phase-30 reviewer reproduced against the live function body"
    - "`[2/7]` still ignores a fixture whose only include is the exempted `dsp/RackCompat.hpp` — the exemption still works in the direction it exists for (D-14 mandates the shim)"
    - "`[1/7]` still fires on both the one-hop and the two-hop synthetic VCO-leak fixtures"
  artifacts:
    - "tests/check_includes.sh — one exact-path `VCO_SIDE_ALLOW` entry `\"tests/test_vco_pitch.cpp\"`, plus its own rationale paragraph in the comment block above the array"
    - "tests/check_includes.sh — the `detect_rack_sdk_includes` exclusion anchored to a whole `grep -n` output line"
    - "tests/check_includes.sh — a fifth `[6/7]` negative control fixture that must FIRE"
    - ".planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md — item 5 carries a RESOLVED block naming this plan"
  key_links:
    - "`VCO_SIDE_ALLOW` -> the `[[ \"${rel}\" == \"${a}\" ]]` match at tests/check_includes.sh:294 — a QUOTED RHS, so the exemption is literal exact-path (no glob, no substring, no basename) and removes exactly one file from the LFO-side scan set"
    - "the anchored exclusion -> `detect_rack_sdk_includes`, the SAME function `[2/7]` calls and `[6/7]` validates — a control that exercises different code than the guard proves nothing"
  prohibitions:
    - "No frozen header edited; `src/dsp/FROZEN.sha256` must not appear in this plan's diff"
    - "`src/AnalogLFO.cpp` must not appear in this plan's diff (D-16)"
    - "The `VCO_SIDE_ALLOW` match must not become a glob, a substring test or a basename test — exact-path only"
    - "No entry is REMOVED from `VCO_SIDE_ALLOW`; no existing `[6/7]` control is deleted, renamed away or weakened"
    - "No `GUARD_WIRING_EXEMPT` entry added or removed — wiring `tests/check_docs.sh` into CI stays Phase 36's"
    - "No `src/` file and no `tests/*.cpp` file is touched by this plan"
---

<objective>
Disarm the guard landmine this phase would otherwise hit on its first run, and pay off the
one deferred item that becomes this phase's to own the moment it touches this script.

D-23 requires the `VCO_SIDE_ALLOW` addition for `tests/test_vco_pitch.cpp` to be an explicit
plan task carrying its own rationale, because in Phase 30 the analogous edit for
`tests/test_vco_core.cpp` was made at gate time and was flagged for not having been
operator-checkpointed. Phase 29 proved pre-registration works: `src/AnalogVCO.cpp` was added
to the allowlist before the file existed and `make guards` was green on that file's first run.

Deferred item 5 was pointed at "the next phase that touches `tests/check_includes.sh`".
That is this phase. CONTEXT.md's `<deferred>` block flags the contradiction explicitly and
requires the planner to fold the fix in or re-own the deferral. **Decision: fold it in.**
Restating the deferral with a new owner would leave the item pointed at a condition this
plan satisfies, which is the same "false comment" class plan 30-08 existed to remove.

Purpose: `make guards` stays green through this phase, and the `[2/7]` exemption is pinned
to the width its own banner claims.
Output: three edits to one guard script, plus a RESOLVED record on the deferred item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-CONTEXT.md
@.planning/phases/31-pitch-tuning-exponential-fm/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-register tests/test_vco_pitch.cpp in VCO_SIDE_ALLOW with its own rationale (D-23)</name>
  <files>tests/check_includes.sh</files>
  <read_first>
    - `tests/check_includes.sh` lines 236-332 — the derived-scan-set comment block, the `VCO_SIDE_ALLOW` array at 279-285, and the exact-path match at 293-295. Read the whole block before editing: the banner states that ADDING to this list is the dangerous edit, and that prose is part of the pattern.
    - `.planning/phases/31-pitch-tuning-exponential-fm/31-PATTERNS.md` section `tests/check_includes.sh (guard script)` — the literal edit and the argument for why it weakens no detector.
    - `.planning/STATE.md` §Accumulated Context, the two Phase-30 entries that begin "tests/test_vco_core.cpp had to be registered in check_includes.sh [1/7] VCO_SIDE_ALLOW" and "All four items flagged for the phase gate resolved" — the record this task exists to not repeat.
  </read_first>
  <action>
Add exactly one entry to the `VCO_SIDE_ALLOW` array: the tab-indented, double-quoted exact
path `"tests/test_vco_pitch.cpp"`, placed last, after the existing `"tests/test_vco_core.cpp"`
entry. Keep the array's existing five entries byte-unchanged and keep the tab indentation the
other entries use.

Add a matching bullet to the "The VCO-side exemptions, each for a stated reason:" comment
block above the array, immediately after the existing `tests/test_vco_core.cpp` bullet. The
prose block is part of the pattern, not optional. The bullet must carry, in its own words:

  - that this is Phase 31's TEST-02 pitch/FM suite (plans 31-05 / 31-06 / 31-07), named in
    31-CONTEXT.md decision D-23;
  - that it is the same KIND of file as the two entries above it — a VCO-side test
    translation unit whose entire purpose is to drive `forge::VcoCore` through
    `tests/VcoBlockDriver.hpp`, which is an include this section would otherwise flag by
    construction;
  - that it is not an LFO translation unit and links into no LFO build graph;
  - that the entry is added BEFORE the file exists, deliberately, following the Phase-29
    precedent for `src/AnalogVCO.cpp`, so `make guards` is green on the new file's first run
    rather than red the way `tests/test_vco_core.cpp` made it in Phase 30;
  - that the entry changes the LFO-side scan set by exactly one file and that file is VCO
    code, and that the match is exact-path because the comparison's right-hand side is
    quoted.

Do not change the `case` statement at 289-291, the `find` invocation, the closure walk, or
the match expression itself. This task adds one array element and one comment paragraph.
  </action>
  <verify>
    <automated>bash tests/check_includes.sh && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"tests/test_vco_pitch.cpp"' tests/check_includes.sh` returns `1`
    - `grep -c 'tests/test_vco_pitch.cpp' tests/check_includes.sh` returns `2` (one array entry, one comment mention) — if it returns more, the rationale was duplicated
    - `bash tests/check_includes.sh` exits 0 and its `[1/7]` line still reports `zero VCO includes`
    - `make guards` exits 0 and prints `guard suite: PASS`
    - `grep -c '"src/vco_compile_canary.cpp"\|"src/AnalogVCO.cpp"\|"tests/VcoBlockDriver.hpp"\|"tests/test_vco_harness.cpp"\|"tests/test_vco_core.cpp"' tests/check_includes.sh` returns `5` — no pre-existing entry was removed or reworded
    - `grep -c '\[\[ "${rel}" == "${a}" \]\]' tests/check_includes.sh` returns `1` — the match is still a quoted literal comparison, not a glob or substring test
    - `git diff --name-only` lists only `tests/check_includes.sh`
    - `git diff --name-only | grep -c 'FROZEN.sha256\|AnalogLFO.cpp'` returns `0`
  </acceptance_criteria>
  <done>The new test TU is exempt by exact path with its rationale recorded in the script, and `make guards` is green before the file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor the [2/7] exemption filter and add the fifth [6/7] control that proves the evasion is caught (deferred item 5 / WR-05)</name>
  <files>tests/check_includes.sh, .planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md</files>
  <read_first>
    - `tests/check_includes.sh` lines 155-170 — `detect_rack_sdk_includes`, whose second stage is the unanchored line-level exclusion. Note that the first stage is `grep -nE`, so every line handed to the exclusion is prefixed with `<lineno>:`.
    - `tests/check_includes.sh` lines 505-600 — the whole `[6/7]` section: `nc_lfo_leak.cpp`, the two-hop `nc2` fixtures, and the two `[2/7]` exemption controls `VcoNcRackSdkProbe.hpp` (`nc3_hits`, must fire) and `VcoNcRackShimProbe.hpp` (`nc4_hits`, must stay silent). The new control must follow the same shape: a `cat > "${TMP}/..." <<'EOF'` fixture, a call to the SAME `detect_rack_sdk_includes` function, an `if`/`else` with an `echo "  OK: ..."` and a `note_fail` carrying a diagnostic that explains what a silent control would mean.
    - `.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md` item 5 — the reviewer's description of the evasion and the two-part fix it prescribes (anchor the exclusion; add the evasion shape as a third `[6/7]` control that must produce a hit). Item 1 in the same file shows the RESOLVED-block format to copy.
  </read_first>
  <action>
Two edits to `tests/check_includes.sh`, then one record.

**Edit A — anchor the exclusion.** In `detect_rack_sdk_includes`, the exclusion stage is
currently a bare `grep -vE` for the shim's include directive with no anchors, so any output
line containing that text ANYWHERE is discarded — including a line whose real directive is a
genuine SDK include and whose trailing comment merely quotes the shim path. Replace the
pattern with one anchored to a whole `grep -n` output line: it must require the line-number
prefix (`^[0-9]+:`), then optional whitespace, then the shim include directive, then only
optional trailing whitespace and an optional trailing comment (`//...` or `/*...`), then
end-of-line. After the change, a line whose first directive is an SDK include can no longer
satisfy the exclusion, because the anchored pattern requires the shim directive to be the
first non-whitespace content after the line-number prefix.

Add a short comment above the changed line recording: the shape that evaded it (a real SDK
include with the exempted path quoted in a trailing comment), that the Phase-30 reviewer
reproduced the evasion against the live function body, that neither pre-existing `[6/7]`
fixture covered the shape because both are single-directive lines, and that the anchor allows
the `grep -n` line-number prefix on purpose because the exclusion runs downstream of
`grep -nE`.

**Edit B — the fifth `[6/7]` control.** After the existing `nc4_hits` block, add a fixture
named `VcoNcRackSdkCommentEvasionProbe.hpp` whose single include line is a genuine Rack SDK
include followed on the SAME LINE by a trailing comment that quotes the exempted shim's
include directive verbatim. Run it through `detect_rack_sdk_includes` into `nc5_hits` and
require a HIT: on a hit print an `OK:` line naming this as the WR-05 evasion shape; on no hit
call `note_fail` with a message stating that the exemption is swallowing whole lines rather
than whole directives, that `[2/7]`'s PASS is therefore meaningless for any line carrying the
shim path in a comment, and that this is deferred item 5 regressing.

Keep the existing four controls and their variable names (`nc_hits`, `nc2_direct`, `nc2_hits`,
`nc3_hits`, `nc4_hits`) untouched. Keep the closing `NOTE:` lines that state these controls
are what make the audit validated rather than merely green.

**The record.** Append a `**RESOLVED — <today's date>, plan 31-01 Task 2.**` block to item 5
of `.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md`, in the same
format item 1 uses. State: which phase resolved it and why this phase owned it (D-23 made
this phase touch the script, which is exactly the condition the item's own "Resolve at" named);
both halves of the prescribed fix landed; the new control's fixture name; and that the four
Info findings recorded in the same item are still only recorded, not planned — `30-REVIEW.md`
remains their record.
  </action>
  <verify>
    <automated>bash tests/check_includes.sh && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check_includes.sh` exits 0
    - `bash tests/check_includes.sh | grep -c 'VcoNcRackSdkCommentEvasionProbe'` returns at least `1` — the new control FIRED and printed its hit; a silent control would print nothing
    - `bash tests/check_includes.sh | grep -c 'OK: \[2/7\] detector ignores the one exempted path'` returns `1` — the exemption still works in the direction it exists for
    - `bash tests/check_includes.sh | grep -c 'TWO-HOP violation detected'` returns `1` — the pre-existing controls still fire
    - `grep -c 'nc5_hits' tests/check_includes.sh` returns at least `2` (assignment plus test)
    - `grep -cE "grep -vE '\^\[0-9\]\+:" tests/check_includes.sh` returns at least `1` — the exclusion is anchored to the `grep -n` line prefix
    - `make guards` exits 0 and prints `guard suite: PASS`
    - `grep -c 'RESOLVED' .planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md` returns at least `2` (item 1's existing block plus item 5's new one)
    - `git diff --name-only | grep -c 'FROZEN.sha256\|AnalogLFO.cpp\|src/'` returns `0`
  </acceptance_criteria>
  <done>The `[2/7]` exemption is pinned to whole directives rather than whole lines, a control proves the evasion is now caught, and deferred item 5 carries a RESOLVED block naming this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source tree -> guard scan set | `[1/7]` derives its LFO-side scan set as everything under `src/`, `tests/`, `tools/` MINUS a named allowlist; an allowlist edit moves that boundary |
| VCO header include line -> `[2/7]` Rack-free detector | one exact-path exemption exists for the repo's own Rack-free shim; the exemption is the only place the detector can be widened |
| guard script -> CI toolchain-gate job | `[7/7]` asserts every `tests/check_*.sh` is invoked by CI; a guard nobody runs is a guard plus a false belief |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-05 | Tampering | `tests/check_includes.sh` `VCO_SIDE_ALLOW` | high | mitigate | Entry is exact-path against a QUOTED right-hand side (line 294), so no glob/substring/basename widening is possible; the entry changes the scan set by exactly one file and that file is VCO code; acceptance criteria assert the pre-existing five entries are unmoved |
| T-31-09 | Tampering | `detect_rack_sdk_includes` exclusion stage | high | mitigate | Task 2 anchors the exclusion to a whole `grep -n` line so a real SDK include can no longer hide behind a trailing comment; the new `[6/7]` control must FIRE on that exact shape, so the fix is validated by an observed hit rather than by inspection |
| T-31-10 | Tampering | `[6/7]` negative controls | medium | mitigate | The four existing controls are preserved by name; acceptance criteria assert two of them still print their hits, so a fix that quietly disabled a control fails this plan |
| T-31-04 | Tampering | frozen shared headers / shipped LFO | critical | mitigate | This plan touches no `src/` file; `check_frozen.sh` byte-pins the four shared headers and runs inside `make guards`; a diff-name assertion fails the task if `FROZEN.sha256` or `src/AnalogLFO.cpp` appears |
| T-31-SC | Tampering | package-manager installs | low | accept | This phase installs zero registry packages. `31-RESEARCH.md` §Package Legitimacy Audit records an empty table with the reason: every symbol is in-tree or from the locally pinned `../Rack-SDK`. No npm/PyPI/crates registry is contacted, so the `[ASSUMED]`/`[SUS]` checkpoint protocol has nothing to gate |
| — | Spoofing / Repudiation / Information Disclosure | — | n/a | accept | No identity, audit-log or confidentiality surface exists in a guard shell script run locally and in CI |
</threat_model>

## Artifacts this phase produces

Phase-wide symbol surface, with the owning plan named. Later plans consume these and must not
redefine them.

**`src/dsp/VcoCore.hpp` — namespace-scope plain `constexpr` inside `namespace forge`:**
- `kVcoMaxPitchVolts` = `64.f` — NEW (31-03)
- `kVcoNyquistGuardFrac` — existing name, value `0.49f` -> `0.495f` (31-02)
- `kVcoFreqC4` — existing, value UNCHANGED, comment updated (31-03)
- `kVcoMaxDeltaPhase` — existing, UNCHANGED (D-12)

**`src/dsp/VcoCore.hpp` — inside `step(...)`:** local `float pitchVolts` (31-03). No new
`VcoInputs` field and no new `Telemetry` field. `VcoInputs::coarse`, `::fine`, `::fmVolts`,
`::fmAtten`, `::fmConnected` change from declared-and-unread to READ (31-03).

**`src/AnalogVCO.cpp` — enum entries (ID churn is still free; nothing has shipped):**
`ParamId::COARSE_PARAM`, `ParamId::FINE_PARAM`, `ParamId::FM_ATTEN_PARAM`,
`InputId::FM_INPUT` (all 31-04).

**`res/AnalogVCO.svg` — four new `10 x 10` `#2a2a30` marker rects (31-04):**
`x="15.32" y="55"`, `x="40.72" y="55"`, `x="66.12" y="55"`, `x="40.72" y="95"`.

**`tests/test_vco_pitch.cpp` — NEW FILE (31-05), every helper in ONE anonymous namespace:**
`SAMPLE_RATES[]`, `kTrackingToleranceCents`, `kEstimatorMinSamplesPerCycle`, `pitchBase()`,
`estimateFreqRising()`, `expectedFreqHz()`, `centsError()`, `windowSamples()`,
`clampCeilingVolts()`, `estimatorCeilingVolts()`, `topTestVolts()` (31-05);
`runBlockOn<CoreT>()` and `DeliberatelyMultiplicativeFmCore` (31-06); `kPitchLooseBoundV` (31-07).

**`tests/test_vco_pitch.cpp` — `TEST_CASE` names, i.e. the `-tc` selectors the gate uses:**
`*v/oct tracking*` (31-05, primary OUTPUT tier + secondary `tel.freqHz` tier),
`PITCH-02 COARSE` and `PITCH-03 FINE` (31-06), `*exponential FM*` (31-06),
`*Nyquist*` (31-07), `*hostile pitch*` (31-07).

**`tests/check_includes.sh` (this plan):** `VCO_SIDE_ALLOW` gains `"tests/test_vco_pitch.cpp"`;
`detect_rack_sdk_includes` exclusion anchored; `[6/7]` gains the `nc5` control fixture
`VcoNcRackSdkCommentEvasionProbe.hpp`.

**`tests/test_vco_core.cpp` (31-07):** `DeliberatelyBrokenSharedStateCore::step(...)` mirror
brought in step with the new pitch block.

**`.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md` — NEW (31-08).**

**Deliberately NOT produced:** no `Telemetry` field (D-22 declined it), no new `VcoInputs`
field, no `depthScale` constant (D-06), no low-end frequency floor (D-13), no Makefile or CI
wiring, no permanent UBSan target or gate (D-24), no `FROZEN.sha256` change.

<verification>
- `bash tests/check_includes.sh` exits 0; all seven sections report OK
- `make guards` exits 0 (`check_frozen.sh`, `check_includes.sh`, `check_canary.sh`)
- `make test` unchanged from baseline (72 cases / 2,616,112 assertions / 0 failed) — this plan touches no test TU
- The five `[6/7]` controls all behave: three must fire (one-hop leak, two-hop leak, genuine SDK include), one must stay silent (the exempted shim), and the new comment-evasion fixture must fire
</verification>

<success_criteria>
- `tests/test_vco_pitch.cpp` is exempt by exact path with a rationale paragraph in the script, before the file exists
- The `[2/7]` exemption is anchored to whole directives and the evasion shape is proven caught by a control that fires on every run
- Deferred item 5 carries a RESOLVED block naming this plan; deferred item 6 is untouched and still points at Phase 32 (D-15)
- `make guards` green; no `src/` file, no `tests/*.cpp` file, no `FROZEN.sha256` in the diff
</success_criteria>

<output>
Create `.planning/phases/31-pitch-tuning-exponential-fm/31-01-SUMMARY.md` when done.
Record: the exact allowlist entry text, the before/after exclusion patterns, the new control's
printed hit line verbatim, and the `make guards` output.
</output>

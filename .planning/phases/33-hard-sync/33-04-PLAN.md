---
phase: 33-hard-sync
plan: 04
type: tdd
wave: 3
depends_on: ["33-02"]
files_modified:
  - tests/test_vco_core.cpp
autonomous: true
nyquist_compliant: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "SYNC-01 / D-01 / D-03: a master rising edge through the real `forge::VcoInputs` boundary resets the phase to the fractional overshoot; a master that never reaches the high threshold resets nothing; `syncConnected` false resets nothing however hostile the voltage."
    - "SYNC-02 / D-01: `phase` is NEVER exactly 0 after a reset — including on the case where the sub-sample fraction would compute to exactly 1 from a master sample landing on 1.0 V, which is the snap-to-zero landmine reached by arithmetic rather than by choice."
    - "D-09: SC-3's at-most-one-event-per-sample clause is discharged by ASSERTING THE DETECTOR'S STRUCTURAL CEILING — a Schmitt trigger reading one voltage per sample can observe at most one rising edge per sample by construction. Every observable edge fires exactly once, missed edges are missed IDENTICALLY at 44.1 / 48 / 96 kHz, and the output stays bounded and finite throughout."
    - "D-09: the limitation is named BEFORE the gate is written against it, which is the register item 6 move applied ahead of time rather than as a rescue."
    - "D-12: the phase's NEW DIVISOR is exercised with a hostile sync population — a not-a-number cable voltage, equal consecutive samples, an exact 1.0 V sample, both infinities, and one legitimate control edge. The instance must be finite AFTER the hostile input is WITHDRAWN, not merely during it."
    - "D-12 / D-02: the previous-voltage store's invariant is asserted directly — it holds the immediately preceding sample's voltage on every branch — rather than inferred from `f` being finite after the fact. The stated choice that neither the store nor the trigger is reset on a sample-rate change is ASSERTED, not inherited."
    - "CORE-03: two interleaved cores with sync voltages inside the drive window each reproduce their solo block bit-exactly, and the permanent positive control still fails, so the new per-instance state sits inside the window the invariant covers."
  artifacts:
    - tests/test_vco_core.cpp
  key_links:
    - "`tests/test_vco_core.cpp:1224` already asserts the phase accumulator stays in range, so the not-a-number poisoning surfaces there — which is why the hostile grid is the natural home for the new-divisor RED, and why a WITHDRAWAL phase must be added: the existing first-bad-step index cannot distinguish bad-during from bad-forever."
    - "`estimateFreqRising` counts rising zero crossings and is blind under about two samples per cycle. It must NOT be used for any sync high-note claim; D-09's structural-ceiling assertion is the instrument used instead."
    - "`tests/VcoBlockDriver.hpp` needs no change: D-02's raw-volts boundary means the per-sample input functor already carries a master voltage. The driver must never be templated or subclassed with `tests/BlockDriver.hpp`, which feeds the shipped LFO's bit-exact golden leg."
---

<objective>
Assert SYNC-01 behaviorally through the real POD boundary: that a master edge resets the phase where D-01 says it does, that the detector's structural ceiling is what SC-3's multiple-event clause actually means, that the phase's new divisor cannot poison the accumulator, and that the new per-instance state sits inside the CORE-03 window.

Purpose: D-02 put detection in the core precisely so these claims are assertable headlessly. A shell-side trigger would have handed the core an already-decided boolean and no test could ever have seen a mis-detected edge — the same vacuity shape the register records for the shell-side morph mix. This plan is where that design choice is cashed in.

Output: four `TEST_CASE`s in `tests/test_vco_core.cpp` covering detection, the structural ceiling, the hostile-sync grid with a withdrawal phase, and the interleave invariant extended to carry sync.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-RESEARCH.md
@.planning/phases/33-hard-sync/33-PATTERNS.md
@.planning/phases/33-hard-sync/33-VALIDATION.md
@.planning/phases/33-hard-sync/33-02-SUMMARY.md
@src/dsp/VcoCore.hpp
@tests/VcoBlockDriver.hpp
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`.

New symbols:
- `forge::VcoInputs::syncVolts`, `forge::VcoInputs::syncConnected`
- `forge::VcoCore::syncTrig`, `forge::VcoCore::prevSyncVolts`
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection`
- `forge::MorphBlep::addPastStep(float, float)` — conditional on the 33-05 measurement
- `AnalogVCO::SYNC_INPUT`
- `make audition`, `AUDITION_BIN`, `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: `HOSTILE_SYNC[]`, a master-generator helper local to this TU, and the four `"vco sync: ..."` `TEST_CASE`s named in its tasks.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SYNC-01 detection and the fractional-overshoot reset, through the real POD boundary (SYNC-01 / D-01 / D-03)</name>
  <files>tests/test_vco_core.cpp</files>
  <behavior>
    - a ±5 V falling-saw master whose wrap crosses the high threshold upward fires a reset exactly once per master cycle, at all three sample rates
    - the same master with `syncConnected` false fires no reset at all — the telemetry flag is false on every sample of the block
    - a master that peaks below the high threshold fires no reset; a master that dips below the low threshold and rises again re-arms and fires again — the hysteresis band is observed, not assumed
    - on every reset sample the phase equals one minus the recorded sub-sample fraction, times the recorded phase increment, to double precision
    - on every reset sample the phase is STRICTLY greater than zero
    - a master constructed so that a sample lands exactly on the high threshold still leaves the phase strictly greater than zero — the guard's strict upper bound is what makes that true, and the case says so
  </behavior>
  <read_first>
    - tests/test_vco_core.cpp lines 76-200 — the TU shape, the neutral-base helper idiom, `runInterleaveCheck`, and the analysis-helper banner convention
    - tests/test_vco_core.cpp lines 160-190 and line 594 — `estimateFreqRising` and its CORRECTED premise: it counts rising zero crossings and is blind under about two samples per cycle. Do not use it for any sync claim
    - tests/VcoBlockDriver.hpp lines 9-18 and 40-64 — the never-template-or-subclass prohibition, the four seed literals, and the per-sample input functor that already carries a master voltage
    - src/dsp/VcoCore.hpp as landed by plan 33-02 — the sync block, the guard on the fraction, the reset expression, and the three recording-only telemetry floats
    - .planning/phases/33-hard-sync/33-RESEARCH.md § "Master generation for the sync sub-grid" — the exactly-representable dyadic increment and the falling-saw polarity whose wrap is a RISING jump
  </read_first>
  <action>
Add a `TEST_CASE` named `"vco sync: (SYNC-01 / D-01 / D-03) a master rising edge resets the phase to the fractional overshoot"` to `tests/test_vco_core.cpp`.

Add a file-local master generator to the anonymous namespace: a functor or small struct producing a ±5 V FALLING saw from a phase accumulator advanced by an exactly-representable dyadic increment, so the master's period is exact and the wrap fraction is known to the test. The falling ramp re-arms the trigger by crossing the low threshold downward mid-cycle and fires it at the wrap, which is the Forge saw's own polarity and models what an operator actually patches. Give the helper a banner in this file's established style: what it produces, why the increment is dyadic, and that the generator KNOWS the true wrap fraction, which later plans use as an oracle.

Subcase A — the reset fires and lands where D-01 says. Drive the core through `forge::VcoBlockDriver` with the master voltage supplied by the per-sample input functor and `syncConnected` true, at 44.1, 48 and 96 kHz. Count resets from the telemetry flag and `CHECK` the count equals the number of master cycles in the block. On each reset sample, recompute the expected phase from the recorded fraction and the recorded increment and `CHECK` the core's accumulator matches to double precision.

Subcase B — the gate. Repeat with `syncConnected` false and `CHECK` the reset count is exactly zero at all three rates. Then repeat with `syncConnected` true but a master scaled so its peak sits below the high threshold, and `CHECK` the count is exactly zero. Then a master that crosses both thresholds and `CHECK` the count is exactly the cycle count — the three together are what evidence the hysteresis band rather than assuming it. Record the threshold literals in the case comment and name the two shipped sites that already use them, so the convention is visible as inherited.

Subcase C — the never-zero claim, both ways. `CHECK` on every reset sample that the accumulator is strictly greater than zero. Then construct the adversarial master: one whose sample sequence puts a value exactly on the high threshold at a crossing, so the sub-sample fraction would compute to exactly one. `CHECK` the accumulator is still strictly greater than zero, `CAPTURE` the recorded fraction on that sample, and comment that this is the snap-to-zero landmine reached BY ARITHMETIC rather than by choice — a gate output idling at exactly that level or a quantised CV does it routinely — and that the guard's STRICT upper bound plus its fallback of zero is what stops it. Name the alternative fallback that must never be used and why.

Every subcase `CAPTURE`s the sample rate and the master frequency. Use the accumulate-then-assert idiom rather than asserting per sample: this suite is already past two and a half million assertions and a per-sample form at these block lengths would add millions more.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco sync: (SYNC-01 / D-01*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed, with a NON-ZERO assertion count recorded in the SUMMARY
    - The selector's matched-case count is confirmed non-zero before the result is read: a selector matching zero cases also exits 0 and prints SUCCESS
    - Non-vacuity is MEASURED: temporarily force the sub-sample fraction's fallback to one in the working tree and confirm subcase C goes RED with a distinct signature; restore and confirm green. Record both signatures in the SUMMARY
    - `estimateFreqRising` is not called by any new case: `grep -n 'estimateFreqRising' tests/test_vco_core.cpp` shows no occurrence inside the new `TEST_CASE`s — record the line numbers of all occurrences and which case each belongs to
    - `tests/VcoBlockDriver.hpp` is unchanged: `git diff --stat tests/VcoBlockDriver.hpp` produces no output
    - `git diff --name-only` lists only `tests/test_vco_core.cpp` and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>SYNC-01's reset, its gate, its hysteresis band and its never-zero guarantee are asserted through the real POD boundary at three sample rates, with the exactly-on-threshold landmine covered by its own adversarial master.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The detector's structural ceiling, and the new divisor's hostile population with a withdrawal phase (D-09 / D-12)</name>
  <files>tests/test_vco_core.cpp</files>
  <behavior>
    - driving a master far faster than the sample rate, every edge the detector can OBSERVE fires exactly once and no sample fires twice — the ceiling is structural, since one voltage per sample can carry at most one rising transition
    - the set of missed edges is IDENTICAL in count and in position at 44.1, 48 and 96 kHz relative to the master's own cycle index, so the limitation is a property of the instrument rather than a rate-dependent defect
    - throughout, every returned sample is finite and within the outer measured bound, and the phase accumulator stays in range
    - for every member of a hostile sync population, the instance returns finite samples DURING the hostile input and, decisively, KEEPS returning finite samples after the hostile input is withdrawn
    - the previous-voltage store equals the immediately preceding sample's voltage on every branch, including unpatched samples, and survives a sample-rate change without being reset
  </behavior>
  <read_first>
    - tests/test_vco_core.cpp lines 1153-1239 — scenario four's hostile grid: the named array with one physical case per entry, the accumulate-then-assert idiom, the first-bad-step index, and the phase-range assertion at :1224 that a not-a-number fraction would break
    - tests/test_vco_core.cpp lines 133-134 and 680-790 — the two measured output tiers and the provenance block that justifies each number per scenario
    - src/dsp/RackCompat.hpp lines 44-58 — the trigger's exact state machine: it returns true only from the low-to-high arm, and all three of its comparisons are false for a not-a-number
    - .planning/phases/33-hard-sync/33-RESEARCH.md § Common Pitfalls 5, 6 and 7 — the reachability analysis for the zero divisor, the exact not-a-number path to the accumulator, and the fraction leaving its range under a band-limited master
    - .planning/phases/33-hard-sync/33-CONTEXT.md D-09 — the structural ceiling is asserted, not worked around; inferring the master's rate is rejected by name
  </read_first>
  <action>
Add two `TEST_CASE`s.

CASE ONE — `"vco sync: (SYNC-01 / D-09) the detector's structural ceiling, named before it is gated"`.
Open the case comment by NAMING THE LIMITATION FIRST, before any assertion: a Schmitt trigger reading one voltage per sample can observe at most one rising edge per sample, by construction, so a master faster than the sample rate has edges the VCO cannot see. State that this is what SC-3's more-than-one-event-per-sample clause is discharged AS, and that the alternative — inferring the master's rate from edge timings to fire multiple events per sample — is rejected because the VCO sees a voltage, not a phase, and period estimation is the clock machinery the shipped LFO uses at clock rates, which is the wrong instrument at audio rate. Say that naming the ceiling before writing the gate is the register item 6 move applied ahead of time rather than as a rescue after a failure.
Then assert three things over a master swept from below the sample rate to several times above it, at all three rates:
  1. the observed reset count never exceeds one per sample, and equals the number of master wraps whenever the master is slow enough that every wrap is separated by at least one sample
  2. the missed-edge rule is IDENTICAL across the three rates when expressed against the master's own cycle index — compute the missed set at each rate and `CHECK` the three descriptions agree, so the limitation is shown to be structural rather than a rate-dependent defect
  3. every sample is finite, the magnitude stays within the outer measured bound, and the phase accumulator stays in range throughout

CASE TWO — `"vco sync: (D-12) the new divisor cannot poison the phase accumulator"`.
Add a named `HOSTILE_SYNC[]` array to the anonymous namespace following scenario four's discipline exactly: every entry carries a trailing comment naming the PHYSICAL case it models. The population must contain, at minimum: a steady voltage well above the high threshold held constant across samples (the stale-store zero-divisor case), a quiet not-a-number, an exact high-threshold value, positive infinity, negative infinity, and one legitimate control edge that MUST fire so the case cannot pass by never detecting anything.
Drive each entry through the core and assert with the accumulate-then-assert idiom: all samples finite, magnitude within the outer bound, accumulator in range, and a first-bad-step index captured for diagnosis.
Then add the WITHDRAWAL PHASE, which is the half that distinguishes this from the existing grid: after the hostile drive, continue driving the SAME instance with a wholly legitimate master and wholly legitimate timing for a further block, and assert every one of those samples is finite and the accumulator is in range. Comment that the first-bad-step index alone cannot distinguish bad-during from bad-forever, and that this defect is invisible during the hostile sample and only appears after recovery — a hostile-input test that only asserts finiteness during the hostile drive would book coverage it does not have.
Add a subcase asserting the STORE INVARIANT DIRECTLY rather than inferring it: drive a block, and on every sample confirm the core's stored previous voltage equals the voltage supplied on the immediately preceding sample, including on samples where the jack is unpatched and on the first sample after construction. Then change the sample rate mid-block and confirm the store and the trigger state are NOT reset — asserting the stated choice rather than inheriting it — and that the invariant still holds across the transition.
Add a subcase for the out-of-range fraction: drive a BAND-LIMITED master (the falling saw with a two-point residual applied at its wrap, so the samples around the wrap carry the residual) and confirm the recorded fraction is always within its guarded range and the accumulator never leaves its own. Comment that the raw threshold crossing alone would put the fraction inside zero-to-one, but a band-limited master's residuals push it outside, and that the industry reference guards exactly this on both ends.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco sync: (SYNC-01 / D-09*" -s 2>&1 | tail -4` and `./build-test/test -tc="vco sync: (D-12*" -s 2>&1 | tail -4` each report 1 test case, 1 passed, 0 failed with a non-zero assertion count
    - Non-vacuity for the legitimate control edge: the case records the observed reset count for that entry and it is greater than zero — a hostile grid in which nothing ever fires asserts nothing about sync
    - The withdrawal phase is proved able to fail: temporarily remove the negated lower comparison from the fraction guard in the working tree and confirm CASE TWO goes RED specifically in the withdrawal block, with a signature DIFFERENT from any red produced during the hostile block. Restore and confirm green. Record both signatures
    - The missed-edge agreement across rates is recorded as three concrete descriptions in the SUMMARY, not as a boolean
    - `grep -c 'HOSTILE_SYNC' tests/test_vco_core.cpp` is at least 2 (the definition and at least one use), and every array entry has a trailing comment — confirmed by quoting the array in the SUMMARY
    - `tests/VcoBlockDriver.hpp` unchanged; `git diff --name-only` lists only `tests/test_vco_core.cpp` and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>SC-3's multiple-event clause is discharged by an assertion of the detector's structural ceiling with the limitation named first, and the new divisor's three landmines are each pinned including the one that is only visible after the hostile input is withdrawn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CORE-03 — the interleave invariant extended to carry sync (CORE-03)</name>
  <files>tests/test_vco_core.cpp</files>
  <behavior>
    - two differently-seeded cores driven interleaved sample by sample, with DIFFERENT sync masters on the two drives, each reproduce their solo block bit-exactly at all three sample rates
    - the existing permanent positive control still fails the same check, so the invariant is known to be able to detect shared state
    - the helper's solo block is still required to match the driver's own run bit-exactly before anything else is asserted
  </behavior>
  <read_first>
    - tests/test_vco_core.cpp lines 170-200 — `runInterleaveCheck`, its templated core type, its mismatch counts and its solo-equality precondition
    - tests/test_vco_core.cpp lines 1529-1560 and around 1646 — the interleave invariant and the permanent deliberately-broken-shared-state positive control
    - src/dsp/VcoCore.hpp as landed by plan 33-02 — the per-instance state block whose banner now claims the sync state sits inside this window; this task is what makes that claim true
    - .planning/STATE.md § Accumulated Context — the Phase 30 lessons that a control running its own copy of the loop proves nothing, and that the validity-first precondition must fire before anything below it
  </read_first>
  <action>
Extend the existing interleave invariant so the two drives carry SYNC VOLTAGES, and do it without duplicating the helper.

Give the two interleaved drives genuinely different masters — different master frequencies, and one of them with the jack unpatched for part of the block — so the two instances take different sync branches at different samples. That difference is the point: if the trigger state or the previous-voltage store were shared, the two instances would diverge from their solo blocks, and this is the only check that would see it.

Keep the existing structure exactly: the helper stays templated over the core type so the positive control exercises byte-identically the drive loop the real check uses; the solo-block equality precondition stays FIRST so a helper that quietly stopped overwriting the injected timing would fail there rather than passing everything below it; the permanent positive control stays in place and must still fail the same check.

Update the invariant's comment to record what changed and why: the per-instance state this phase adds is a trigger and a voltage store, absence of sharing is exactly what a source-text grep proves badly, and the new state only sits inside the window this invariant covers if the DRIVES carry sync. State that a version of this check whose drives left the jack unpatched would cover the new members with nothing while still reporting green.

Re-run the positive control and record its mismatch figures. If those figures differ from the ones already recorded in the file, report the difference rather than re-typing the constant: the register already notes that the control's figures are a saturated metric and that a change's effect must be measured on the output rather than inferred from numbers that cannot move upward.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0
    - `./build-test/test -tc="*interleave*" -s 2>&1 | tail -4` reports a non-zero matched case count and 0 failures; the mismatch counts for both instances are recorded in the SUMMARY at all three sample rates
    - The permanent positive control still fails the underlying check: its recorded mismatch figures are reproduced in the SUMMARY and compared against the figures already written in the file, with any difference REPORTED rather than absorbed
    - Non-vacuity is MEASURED for the new state specifically: temporarily make the previous-voltage store a shared static in the working tree and confirm the interleave check goes RED with a non-zero mismatch count on both instances. Restore and confirm green. Record the mismatch figures from the probe
    - The two drives are genuinely different: the SUMMARY records the two master frequencies and the sample window over which one jack is unpatched
    - The whole suite's case and assertion totals are recorded as a before/after pair against plan 33-02's recorded totals
    - `git diff --name-only` lists only `tests/test_vco_core.cpp` and does not list `src/AnalogLFO.cpp`; `tests/VcoBlockDriver.hpp` unchanged
  </acceptance_criteria>
  <done>The CORE-03 window provably covers the sync trigger and the previous-voltage store, proved by a shared-static probe producing a real red rather than by inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headless harness → `forge::VcoInputs` | The only caller that can reach the core's guards with a hostile value; the shell conditions nothing on the sync path by decision |
| one `forge::VcoCore` instance → another | CORE-03's polyphony-ready guarantee; absence of shared state is what a source grep proves badly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Denial of Service | permanent poisoning of `VcoCore::phase` via a not-a-number sub-sample fraction | high | mitigate | Task 2 CASE TWO's withdrawal phase asserts the instance is finite AFTER the hostile input is removed, and the guard's removal is proved to produce a red specifically in that block |
| T-33-05 | Denial of Service | zero divisor reached through a stale previous-voltage store | medium | mitigate | Task 2's store-invariant subcase asserts the invariant DIRECTLY on every branch and across a sample-rate change, rather than inferring it from a finite result |
| T-33-06 | Denial of Service | the fraction reaching exactly one and snapping the phase to zero | medium | mitigate | Task 1 subcase C drives an adversarial master that puts a sample exactly on the high threshold and asserts the accumulator is still strictly positive |
| T-33-11 | Denial of Service | the fraction leaving its range under a band-limited master | medium | mitigate | Task 2's band-limited-master subcase asserts the recorded fraction stays inside the guarded range and the accumulator never leaves its own |
| T-33-07 | Denial of Service | unbounded output amplitude reaching the host under hostile sync | medium | mitigate | Task 2 asserts the outer measured bound on every hostile entry; the tighter musical tier is re-derived for sync in plan 33-08 rather than assumed here |
| T-33-16 | Information Disclosure / EoP | cross-voice bleed from shared sync state once polyphony lands | medium | mitigate | Task 3 puts the trigger and the store inside the interleave window and proves the check can fail via a shared-static probe |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- `make test && make strict && make guards` all exit 0
- Every new case's selector is confirmed to have matched a non-zero case count before its result is read
- Each of the four claims has a measured non-vacuity probe producing a DISTINCT red, with both signatures recorded
- `tests/VcoBlockDriver.hpp` unchanged; `src/AnalogLFO.cpp` absent from the diff; the six LFO goldens byte-identical
</verification>

<success_criteria>
SYNC-01 is evidenced behaviorally at three sample rates through the real POD boundary; SC-3's multiple-event clause is discharged by an honest statement of the detector's ceiling made before the gate was written; the new divisor's three landmines are pinned including the after-withdrawal one; and the new per-instance state is inside a CORE-03 window proved able to fail.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-04-SUMMARY.md` when done
</output>

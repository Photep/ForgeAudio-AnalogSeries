---
phase: 33-hard-sync
plan: 11
type: execute
wave: 6
depends_on: ["33-07", "33-08", "33-09", "33-10"]
files_modified:
  - .planning/phases/33-hard-sync/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
nyquist_compliant: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "The full local gate is green — test, strict, guards, frozen, canary, the six shipped-LFO goldens, and a real plugin link — and is recorded as a PRECONDITION rather than as the gate, because this project has measured that exact combination green on code that could not link."
    - "The CI toolchain gate is observed green BY HASH EQUALITY on the exact commit, and only the reproduction STEP's own conclusion counts: a step that fail-fasts upstream reports skipped, which scans as not-red in a job summary. The run is located by hash, never by recency."
    - "The three-platform case and assertion deltas are measured on BOTH sides per leg, because a per-leg before-and-after delta is evidence that every new case ran everywhere while an absent platform guard in a new file is only an argument."
    - "SYNC-01 and SYNC-02 are ticked only against named test cases the gate locates independently, each with a NON-ZERO matched count — a selector matching zero cases also exits 0 and prints success, so exit status cannot distinguish a gate that passed from one that never ran."
    - "PITCH-04 is re-ticked ONLY where sync was observed FIRING behind the claim (D-12), closing Phase 31's deferred item 11. If the firing counts are not non-zero, it is not re-ticked and the reason is recorded."
    - "The LFO guardrail is discharged with the KIND of evidence stated: all frozen paths at zero changed lines over the whole phase diff, the manifest byte-identical, the six goldens byte-identical, and `src/AnalogLFO.cpp` absent from the phase diff. Any operator attestation is recorded only if actually given, never inferred."
    - "D-07's residual carried-forward phantom, the full contract audit of the band-limiting header, and every other item this phase opened are filed in the phase's deferred register with a Resolve-at, rather than carried in prose."
  artifacts:
    - .planning/phases/33-hard-sync/deferred-items.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "This plan ticks requirements; no earlier plan in this phase does. Every earlier plan's acceptance criteria say so explicitly, following the Phase 31 precedent where four consecutive plans declined a false green."
    - "The CI-run selector must match on the commit hash, not on a job or workflow name: a prior phase's selector matched a job name against a RUN name and returned nothing, so its own presence check failed regardless of CI health."
    - "This phase is NOT a tag or a library resubmission. The standing rule that neither may be cut on local evidence alone still binds and is Phase 36's to satisfy."
---

<objective>
Close Phase 33 on observed evidence: a green full local gate recorded as a precondition, the CI toolchain gate observed green by hash equality on the exact commit, every requirement re-verified against a named case with a non-zero matched count, and every item this phase opened filed with a Resolve-at.

Purpose: this project has measured a full green local gate on code that could not link, and has three separate records of a gate whose mechanism did not match the prose it encoded. The phase gate exists to make the difference between "nothing was red" and "the right things were measured" visible.

Output: the phase's deferred register, the requirement status updates that the evidence supports, and the roadmap's phase entry marked complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-VALIDATION.md
@.planning/phases/33-hard-sync/33-05-SUMMARY.md
@.planning/phases/33-hard-sync/33-07-SUMMARY.md
@.planning/phases/33-hard-sync/33-08-SUMMARY.md
@.planning/phases/33-hard-sync/33-09-SUMMARY.md
@.planning/phases/33-hard-sync/33-10-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.

New symbols:
- `forge::VcoInputs::syncVolts`, `forge::VcoInputs::syncConnected`
- `forge::VcoCore::syncTrig`, `forge::VcoCore::prevSyncVolts`
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection`
- `forge::MorphBlep::addPastStep(float, float)` — conditional on the 33-05 measurement
- `AnalogVCO::SYNC_INPUT`
- `make audition`, `AUDITION_BIN`, `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: `.planning/phases/33-hard-sync/deferred-items.md` and the requirement/roadmap status updates.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The full local gate as a precondition, and the CI toolchain gate observed by hash</name>
  <files>.planning/phases/33-hard-sync/deferred-items.md</files>
  <read_first>
    - .planning/STATE.md § Accumulated Context — the Phase 29 record that the entire local gate returned success on a deliberately broken commit; the Phase 30 and 31 records of how the CI run must be located and which conclusion counts; and the Phase 30 record of a run selector that could never match
    - .planning/phases/33-hard-sync/33-VALIDATION.md § "Sampling Rate" — what the phase gate requires beyond the local suite
    - .github/workflows/ — the workflow, its jobs, and the exact name of the reproduction step whose own conclusion is the gate
    - every 33-NN-SUMMARY.md in this phase — the recorded case and assertion totals per plan, needed for the per-leg delta accounting
  </read_first>
  <action>
Run the full local gate and record every result with its numbers, not as a checklist of ticks:
  - the test suite, recording total cases, total assertions and failures
  - the strict C++11 compile over every plugin translation unit
  - the guard suite, and separately each guard script, including the include-direction audit run both with and without a real software development kit present
  - the frozen-manifest check, recording the entry count and confirming the manifest byte-identical by direct byte comparison rather than by counting diff markers
  - the compile canary, recording the runtime-live field count
  - the six shipped-LFO goldens replaying byte-identical, with the matched case count
  - a real plugin link against the software development kit
Record the whole set as a PRECONDITION, and write beside it the reason this project insists on the word: that exact combination has been measured green on code that could not link.

Then push the phase's commits and observe the CI toolchain gate. The observation protocol is binding and each clause has its own recorded failure behind it:
  - locate the run by HASH EQUALITY against the pushed commit, never by recency and never by name. Compare every candidate run's head hash to the pushed hash before reading any conclusion
  - read the job's conclusion, and record it — but do NOT treat it as the gate
  - the GATE is the reproduction step's OWN conclusion. A step that fail-fasts upstream reports skipped, which scans as not-red in a job summary
  - record every step's individual conclusion, and confirm none is skipped
  - if the repository's branch policy requires it, reach CI on a branch rather than moving the default branch, and record which branch and why
Then measure the three-platform case and assertion deltas on BOTH sides, per leg: the totals before this phase and after it, on each platform. Confirm the increment is identical on every leg, and confirm the standing platform-gated gap is unchanged. Account for any difference by naming the specific platform-guarded cases that explain it — a delta accounted for by naming the cases is evidence; a delta explained by expectation is not.

If any of this is red, STOP and report. Do not proceed to the requirement re-verification on a red gate.
  </action>
  <verify>
    <automated>make test && make strict && make guards && make</automated>
  </verify>
  <acceptance_criteria>
    - Every local gate result is recorded with its numbers: case count, assertion count, failure count, frozen entry count, canary field count, LFO golden matched-case count
    - The frozen manifest is confirmed byte-identical by direct byte comparison, and the result of that comparison is quoted
    - The CI run is identified by its commit hash, and the SUMMARY records the pushed hash and the run's head hash side by side as equal
    - The reproduction step's OWN conclusion is recorded as success, and every step's individual conclusion is listed with none skipped
    - The three-platform deltas are recorded per leg on both sides, the increment is identical across legs, and the standing platform-gap is unchanged and accounted for by named cases
    - `git diff --name-only` over the WHOLE phase does not list `src/AnalogLFO.cpp`, and shows zero changed lines on every frozen path
    - If any result is red, the SUMMARY carries a STOP-AND-REPORT section and no requirement is ticked
  </acceptance_criteria>
  <done>The phase's evidence is observed rather than assumed: a green local gate recorded as a precondition, and a CI gate located by hash whose own reproduction step reports success.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify every requirement against a named case with a non-zero matched count</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the sync requirement wording and the phase mapping table
    - .planning/ROADMAP.md § "Phase 33" — the goal and the three success criteria, verbatim
    - .planning/phases/33-hard-sync/33-09-SUMMARY.md — the recorded sync-firing counts, which decide whether the pitch requirement can be re-ticked
    - .planning/STATE.md § Accumulated Context — the Phase 31 record that a selector matching zero cases also exits 0 and prints success, and the Phase 32 record that all nine requirement identifiers were re-verified against a named case with a non-zero matched count rather than ticked from summary prose
  </read_first>
  <action>
For each requirement this phase owns, LOCATE the evidence independently rather than reading it out of a summary: find the test case by searching the test tree, run it as a selector, and confirm the matched case count is NON-ZERO before reading its result. Record for each: the requirement, its wording, the case title, the file and line, the matched case count, the assertion count, and the result.

Cover:
  - the hard-sync input requirement, against the detection and reset cases
  - the sub-sample and band-limiting requirement, against the spectral sub-grid gate, the snap-to-zero comparison, and the time-domain continuity and anti-circularity cases — naming which case carries which clause of the requirement, because this requirement has two clauses and they are evidenced by different instruments
  - the pitch requirement's re-confirmation, and its condition: it is re-ticked ONLY if the recorded sync-firing counts are non-zero on the cells that carry the claim. If they are not, do NOT re-tick it, and record the reason in exactly the form the earlier phases used

Then walk the phase's three success criteria one at a time, in the roadmap's own words, and for each record which case or measurement discharges it and where. The third criterion has two halves — a bounded per-sample step and correct handling of the multiple-event case — and each half needs its own named evidence; the second half is discharged by the assertion of the detector's structural ceiling, and the SUMMARY must say so explicitly rather than letting the criterion appear satisfied by association.

Update the requirement mapping table's status entries for exactly the requirements the evidence supports. Update the roadmap's phase entry: mark the phase complete in the phase list, and fill in the phase's plan count and plan list. Do not edit any other phase's entry, and use scoped replacements rather than rewriting either file.

If any requirement's evidence is weaker than its wording, say so in the status rather than ticking it — this project has a named lesson for each of the three ways that has gone wrong before.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - Every requirement this phase owns has a recorded case title, file, line, matched case count and assertion count, and every matched case count is non-zero
    - The sub-sample and band-limiting requirement's two clauses are separately attributed to separately named cases
    - The pitch requirement's re-tick decision is recorded together with the sync-firing counts it rests on; if the counts are zero anywhere that carries the claim, the requirement is NOT re-ticked and the reason is written
    - Each of the phase's three success criteria has named evidence, and the third criterion's two halves are separately evidenced
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` were edited with scoped replacements: `git diff --stat` shows a small changed-line count on each, and the diff is quoted in the SUMMARY
    - No other phase's roadmap entry changed: `git diff .planning/ROADMAP.md` touches only this phase's entry and the phase list line for this phase
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0
  </acceptance_criteria>
  <done>Every requirement and every success criterion is either ticked against evidence the gate located itself, or left un-ticked with the reason recorded.</done>
</task>

<task type="auto">
  <name>Task 3: File the phase's deferred register, with a Resolve-at on every item</name>
  <files>.planning/phases/33-hard-sync/deferred-items.md</files>
  <read_first>
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md — the register's format: numbered items, each with its evidence, its rejected alternatives and an explicit Resolve-at
    - .planning/phases/33-hard-sync/33-CONTEXT.md § Deferred Ideas — every item carried into this phase with its Resolve-at, several of which are restated unchanged
    - every 33-NN-SUMMARY.md in this phase — the findings each plan recorded for the register
    - .planning/phases/33-hard-sync/33-RESEARCH.md § Open Questions — the three questions research left open and its recommendation on each
  </read_first>
  <action>
Create `.planning/phases/33-hard-sync/deferred-items.md` in the preceding phase's format: numbered items, each with the evidence behind it, the alternatives rejected, and an explicit Resolve-at.

It must contain, at minimum:
  - D-07's residual carried-forward phantom: the window width, the measured diagnostic figure plan 33-05 recorded, why per-site cancellation is impossible without restructuring the band-limiting header, and the accepted disposition
  - the full contract audit of the band-limiting header, carried forward from this phase's own rejected option — the header now enforces four of its advertised claims and the rest are unaudited. Resolve at the phase that adds a third call site
  - the cosine-crossfade third audition leg, with its Resolve-at as the first operator verdict on smear that is equivocal
  - applying the audition renderer to the preceding phase's morph pair, now near-free
  - the slope-correction kernel on the sync path, as the documented FIRST escalation if a sync threshold proves unreachable
  - the kernel-order question, restated unchanged as an operator decision with an impact assessment and never a silent implementation choice
  - the narrow-pulse reach refinement, restated unchanged
  - the spectral column's cross-toolchain portability, now carrying this phase's sync rows as well
  - the shipped module's shared latent undefined behavior, restated unchanged and still deliberately unowned, with its binding consequence: no permanent repository-wide sanitizer gate
  - the two live tripwires carried from the preceding phase
  - the second, differently-slugged plugin directory in the operator's installation
  - anything each plan's SUMMARY recorded for the register that is not already listed above

Then record this phase's own measured figures in a closing section, exactly as the preceding phase's register does: the placement measurement's table, the snap-to-zero margins, the anti-circularity margin distribution, the re-derived output tier, and the audition pair's properties. A later phase should be able to reach every number this phase measured without opening a plan summary.

Finally, write the resume material the next session needs: what is complete, what the operator gate still owes, and the one-line statement that the operator gate has NOT yet been answered — because plan 33-12 is what answers it and until then the phase is not closed.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-hard-sync/deferred-items.md && grep -c 'Resolve at' .planning/phases/33-hard-sync/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - The register exists and every numbered item carries an explicit Resolve-at: the count of Resolve-at lines equals the count of numbered items — record both numbers
    - All eleven named item classes from the action are present, plus every item recorded in a plan SUMMARY for the register; enumerate the mapping in the SUMMARY
    - The closing measured-figures section carries the placement table, the snap margins, the anti-circularity distribution, the re-derived tier and the audition pair's properties
    - Items restated unchanged are recorded as restated unchanged, not silently re-litigated
    - The resume material states plainly that the operator gate is not yet answered
    - `make test` exits 0 with 0 failures; `git diff --name-only` for this task lists only the register file
  </acceptance_criteria>
  <done>Every open thread this phase touched is filed with a Resolve-at and every number it measured is reachable without opening a plan summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local green → shipping correctness | This project has measured a full green local gate on code that could not link; the boundary is crossed only by the CI link leg |
| a gate's prose → a gate's mechanism | Three recorded instances of a gate whose regular expression or selector was wider or narrower than the rule it encoded |
| evidence → a ticked requirement | A selector matching zero cases exits 0 and prints success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Tampering | toolchain-divergent undefined behavior or ODR defect invisible on the development host | high | mitigate | Task 1 observes the CI toolchain gate by hash equality and reads the reproduction step's OWN conclusion, with every step's conclusion listed and none skipped |
| T-33-36 | Repudiation | a requirement ticked from summary prose rather than from located evidence | high | mitigate | Task 2 locates every case independently by searching the test tree and requires a non-zero matched count before any result is read |
| T-33-37 | Repudiation | a success criterion appearing satisfied by association rather than by named evidence | medium | mitigate | Task 2 walks each criterion in the roadmap's own words and requires named evidence for each half of the two-part criterion |
| T-33-15 | Spoofing | a frozen shared header or the shipped module's source entering the phase diff | high | mitigate | Task 1 asserts zero changed lines on every frozen path over the WHOLE phase diff, the manifest byte-identical by direct comparison, and the shipped module's source absent |
| T-33-38 | Repudiation | an operator attestation inferred from a general remark rather than given | high | mitigate | The guardrail is discharged by automated evidence with the KIND of evidence stated; any attestation is recorded only if actually given, and plan 33-12 owns the asking |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- `make test && make strict && make guards && make` all exit 0, recorded with numbers rather than ticks
- The CI toolchain gate is observed green by hash equality, with the reproduction step's own conclusion recorded and no step skipped
- Every requirement and every success criterion has independently located evidence with a non-zero matched count
- Zero changed lines on every frozen path over the whole phase diff; the shipped module's source absent
- The deferred register exists with a Resolve-at on every item
</verification>

<success_criteria>
Phase 33 stands on observed evidence: a green local gate recorded as a precondition rather than as proof, a CI gate located by hash whose reproduction step's own conclusion is success, requirements ticked only against cases the gate found itself, and every open thread filed with a Resolve-at.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-11-SUMMARY.md` when done
</output>

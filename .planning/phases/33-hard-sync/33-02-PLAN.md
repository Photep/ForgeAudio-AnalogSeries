---
phase: 33-hard-sync
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/dsp/VcoCore.hpp
autonomous: true
nyquist_compliant: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "SYNC-01 / D-02: `forge::VcoInputs` gains `syncVolts` and `syncConnected`; `forge::VcoCore` owns the `SchmittTrigger`, the previous-voltage store and the sub-sample solve. Raw volts cross the POD boundary so the whole sync path is assertable headlessly through `tests/VcoBlockDriver.hpp`."
    - "D-03: the hysteresis thresholds are `0.1f, 1.0f`, verbatim what `LfoCore.hpp:137` and `ClockTracker.hpp:111` already use — one convention across the plugin, inherited rather than invented."
    - "SYNC-01 / D-01: on a rising edge the core solves `f = (1.0f - prevSyncVolts) / (in.syncVolts - prevSyncVolts)` and resets `phase = (1 - f) * deltaPhase`. The reset NEVER snaps to exactly 0."
    - "D-01 / D-12: `f` is guarded with the negated-comparison pair whose upper bound is STRICT and whose fallback is 0, never 1 — `f == 1` exactly IS the snap-to-zero landmine and is reachable from any master sample landing on 1.0 V. `forge::clamp` is rejected by name because both of its comparisons are false for a not-a-number."
    - "D-12: `phase` carries NO guard of its own today; a not-a-number cannot fire the trigger but CAN be stored as `prev`, so the next finite crossing computes a not-a-number `f` and poisons the accumulator permanently. The guard on `f` is the fix and is load-bearing, not defensive."
    - "D-02 / Pattern 3: `prevSyncVolts` is stored UNCONDITIONALLY every sample, whatever `syncConnected` says. That invariant is the only thing that makes a zero divisor unreachable in steady state, so the divide-by-zero is a state-management defect, not an arithmetic one."
    - "D-02 discretion, resolved and stated: neither `prevSyncVolts` nor `syncTrig` is reset on a sample-rate change, because a rate change does not alter which sample was the previous one and resetting would break the invariant the guard rests on."
    - "D-05: `jump` is the difference of two `morphedWave` values — the naive value at the pre-reset advanced phase, and ONE extra call at the post-reset phase. The sign is `after - before`; `research/STACK.md:124` writes the same quantity NEGATED and that expression must not be transcribed. Phase 32 D-05 bleed normalization is satisfied by CONSTRUCTION."
    - "D-05: this is a REASONED departure from Phase 32's compute-it-don't-measure-it through-line — a reset lands at an ARBITRARY phase so there is no closed form — and must not be 'restored' to an analytic magnitude for consistency."
    - "D-07: the sync block sits strictly between the wrap and the `p` snapshot, so `p`, the naive sample and the single `blep.step` call all see the POST-reset phase; a site the reset jumped over is behind the post-reset phase and structurally cannot fire."
    - "D-06: the seam call is DELIBERATELY ABSENT from this plan. Plan 33-05 measures the placement convention against this exact ordering before plan 33-06 lands it. A plan that writes the seam call here and measures afterwards has skipped the phase's central question."
    - "CORE-03: `syncTrig` and `prevSyncVolts` are per-instance members held by value beside `blep`, never static and never shared."
  artifacts:
    - src/dsp/VcoCore.hpp
  key_links:
    - "`VcoCore.hpp:60-62` currently asserts the sync fields are deliberately ABSENT and names Phase 33 as their owner. That paragraph self-contradicts the moment the fields land and is corrected in the same commit — an explicit task, not a gate-time discovery."
    - "`morph`/`character` are conditioned at `:597-602`, AFTER the wrap at `:540`. D-05's two `morphedWave` calls need conditioned values, so the conditioning block moves ABOVE the sync block."
    - "The `check_canary.sh [2b/5]` step matcher is UNANCHORED: a sync comment in this header must not quote the `step` signature on a line that also contains an opening brace, or `make guards` hard-fails with unrelated compile errors."
    - "`forge::SchmittTrigger` comes from the FROZEN `RackCompat.hpp:44-58` and is CALLED, never edited. Its UNINITIALIZED handling is load-bearing and is what makes the first sample after construction non-firing."
---

<objective>
Land hard-sync detection, the guarded sub-sample solve, the fractional-overshoot reset and the jump computation inside `forge::VcoCore` — and deliberately stop short of the seam call, so plan 33-05 can measure the placement convention against the real core before plan 33-06 pins it.

Purpose: this is SYNC-01 in full and the input-validation half of SYNC-02. It is also where the phase's new divisor and its three guard landmines live: `f == 1` exactly (reachable from any master sample landing on 1.0 V, and it snaps the phase to zero through the front door), a not-a-number cable voltage that cannot fire the trigger but can poison `phase` one sample later, and a divide-by-zero that is unreachable only while the previous-voltage store is genuinely the previous voltage.

Output: `src/dsp/VcoCore.hpp` with two new POD fields, two new per-instance members, three new recording-only telemetry floats, the sync block, and the corrected banner paragraphs. The core's behavior after this plan is exactly measurement leg `none`: reset applied, sync BLEP withheld.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-RESEARCH.md
@.planning/phases/33-hard-sync/33-PATTERNS.md
@.planning/phases/33-hard-sync/33-01-SUMMARY.md
@src/dsp/VcoCore.hpp
@src/dsp/RackCompat.hpp
@src/dsp/LfoCore.hpp
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`.

New symbols:
- `forge::VcoInputs::syncVolts` (float), `forge::VcoInputs::syncConnected` (bool)
- `forge::VcoCore::syncTrig` (`forge::SchmittTrigger`), `forge::VcoCore::prevSyncVolts` (float)
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection` (recording-only floats; `::syncFired` already exists and is POPULATED, not added)
- `forge::MorphBlep::addPastStep(float, float)` — CONDITIONAL on the plan 33-05 measurement selecting candidate (b)
- `AnalogVCO::SYNC_INPUT` enum member
- `make audition` target, `AUDITION_BIN` Makefile variable, output directory `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: `syncVolts`, `syncConnected`, `syncTrig`, `prevSyncVolts`, `tel.syncFrac`, `tel.syncJump`, `tel.syncCorrection`, and the population of the already-declared `tel.syncFired`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The POD fields, the per-instance state, the telemetry, and the two self-contradicting banner paragraphs (D-02 / CORE-03)</name>
  <files>src/dsp/VcoCore.hpp</files>
  <behavior>
    - `forge::VcoInputs` carries `syncVolts` (float, defaulting to 0) and `syncConnected` (bool, defaulting to false), each with one trailing comment naming the owning phase, matching the `fmVolts` / `fmConnected` precedent exactly
    - `forge::VcoCore` carries `SchmittTrigger syncTrig;` and `float prevSyncVolts = 0.f;` by value, beside `blep`
    - `Telemetry` carries three new recording-only floats and the already-declared `syncFired` is left in place rather than duplicated
    - the header still compiles under BOTH `-std=c++11 -pedantic-errors` and `-std=c++17`, and `make guards` still perturbs it successfully
  </behavior>
  <read_first>
    - src/dsp/VcoCore.hpp lines 40-99 — the source-shape contract, the unanchored-`step`-matcher trap, the naming landmine, the sync-fields-deliberately-ABSENT paragraph at :60-62, the two-standard C++11 rules, and the include hygiene note
    - src/dsp/VcoCore.hpp lines 228-294 — `VcoInputs`, the per-instance state block with its "ALL THREE members" sentence, and the `Telemetry` struct with `syncFired` already declared at :285
    - src/dsp/RackCompat.hpp lines 44-58 — `forge::SchmittTrigger`: its three-state machine, its default thresholds, its `reset()`, and why the UNINITIALIZED state is load-bearing
    - src/dsp/LfoCore.hpp lines 134-145 — the shipped LFO's `resetConnected` gate (the precedent to COPY) and its cosine crossfade plus `phase = 0.0` snap (the body to REJECT)
    - .planning/phases/33-hard-sync/33-PATTERNS.md § `src/dsp/VcoCore.hpp` — every analog quoted at its line range
  </read_first>
  <action>
Part A — the two POD fields. Add `syncVolts` and `syncConnected` to `forge::VcoInputs`, placed after the FM group so the file's own grouping stays readable. Give each an NSDMI default and one trailing `//` comment naming Phase 33, exactly matching the `fmVolts` / `fmConnected` shape. Do not reorder or re-comment any existing field: Phase 31 D-05 forbids churning POD field semantics. Update the struct's own header comment, which currently says the field set covers the near-term Phase 30/31 needs and that sync is deferred to Phase 33.

Part B — the per-instance state. Add `SchmittTrigger syncTrig;` and `float prevSyncVolts = 0.f;` by value inside `struct VcoCore`, immediately beside `blep`, so the CORE-03 window covers them. Extend the block's banner sentence that currently counts "ALL THREE members" to the new count, and extend the paragraph that says the CORE-03 proof is behavioral to name the sync state explicitly: `tests/test_vco_core.cpp`'s two-instance interleave invariant must drive SYNC VOLTAGES for the new state to sit inside the window the invariant covers, and plan 33-04 is what makes that true. Say that in the comment so the claim does not outrun its evidence.

Part C — the telemetry. `Telemetry::syncFired` already exists at `:285` and is reserved for this phase — POPULATE it in Task 2, do not add a parallel field. Add three recording-only floats beside it:
  - one carrying the guarded sub-sample fraction actually used this sample
  - one carrying the jump magnitude the sync seam was handed
  - one carrying the correction value the sync seam deposited into `inject` this sample, in the pre-multiply domain (i.e. before the `5.f *` at the return)
Each gets one trailing comment naming its consumer. The third one's comment must state its purpose precisely, because it is what makes D-14's second audition leg reconstructible from the SAME PASS: under the past-edge placement the sync correction deposits nothing into `pending`, so it is purely additive per sample and the withheld leg is the full leg minus five times this value, exactly. Note in the same comment that this shortcut is SPECIFIC to that placement and that plan 33-06 must re-state it if the measurement selects a different candidate. Extend the `Telemetry` banner's "NOT part of the audio path" sentence to cover the three new members.

Part D — the two self-contradicting paragraphs, which are a task, not a discovery. Rewrite `VcoCore.hpp:60-62`: it currently asserts the sync fields are deliberately ABSENT and names Phase 33 as the phase that adds them. Replace it with the landed statement — which fields exist, that raw volts cross the boundary by decision (D-02), that the core owns detection because that is what makes SYNC-01 assertable headlessly, and that a shell-side trigger would hand the core an already-decided boolean so no test could ever see a mis-detected edge. Do not leave a forward reference pointing at work this commit performs.

SOURCE-SHAPE CONSTRAINT, binding on every comment this plan writes into this file: the `check_canary.sh [2b/5]` step matcher is UNANCHORED. Do not quote the `step` function signature in any comment on a line that also contains an opening brace, or `make guards` hard-fails while perturbing the COMMENT and reports unrelated compile errors. The `struct VcoCore` declaration line and the `step` signature line must each stay on ONE line with their opening brace.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make strict` exits 0 (C++11, `-pedantic-errors`) and `make test` exits 0 with 0 failures
    - `make guards` exits 0; `bash tests/check_canary.sh` completes without the "could not perturb src/dsp/VcoCore.hpp" error and its `[2b/5]` section prints a field count
    - `grep -n 'syncVolts\|syncConnected' src/dsp/VcoCore.hpp` shows both fields declared inside `struct VcoInputs` with NSDMI defaults
    - `grep -c 'syncFired' src/dsp/VcoCore.hpp` shows the existing declaration is still the only declaration — no parallel telemetry field was added
    - The stale forward reference is gone: `grep -n 'deliberately ABSENT' src/dsp/VcoCore.hpp` returns no match
    - `bash tests/check_frozen.sh` exits 0 with 15 entries unchanged; `git diff --stat src/dsp/Waveshape.hpp src/dsp/DriftEngine.hpp src/dsp/RackCompat.hpp src/dsp/MathConst.hpp` produces no output
    - `git diff --name-only` lists only `src/dsp/VcoCore.hpp` and does not list `src/AnalogLFO.cpp`
    - The six shipped-LFO goldens replay byte-identical: `./build-test/test -tc="*golden*" -s 2>&1 | tail -3` reports 0 failures with a non-zero matched case count
  </acceptance_criteria>
  <done>The POD carries the two sync fields, the core carries the two per-instance members and three recording-only telemetry floats, and both banner paragraphs that would have contradicted this commit are corrected in it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The sync block — detection, the guarded solve, the reset and the jump (SYNC-01 / D-01 / D-03 / D-05 / D-12)</name>
  <files>src/dsp/VcoCore.hpp</files>
  <behavior>
    - with `syncConnected` false, no reset ever occurs however hostile `syncVolts` is
    - with `syncConnected` true, a master crossing 1.0 V upward from below 0.1 V fires exactly one reset; a master that peaks at 0.9 V fires none
    - after a reset, `phase` equals `(1 - f) * deltaPhase` and is strictly greater than zero for every reachable `f`
    - a master sample landing exactly on 1.0 V computes `f == 1.0f`, is caught by the STRICT upper bound, and lands on the fallback so `phase` is `deltaPhase` rather than zero
    - a not-a-number `syncVolts` does not fire the trigger, is still stored, and on the next finite crossing produces a not-a-number `f` that is caught by the negated lower comparison and never reaches `phase`
    - `prevSyncVolts` equals the previous sample's `syncVolts` on every sample and every branch, including samples where the jack is unpatched
    - `tel.syncFired` is true exactly on samples where a reset occurred and false otherwise
  </behavior>
  <read_first>
    - src/dsp/VcoCore.hpp lines 505-655 — the deltaPhase guard, the advance and single-subtract wrap at :536-540, the `p` snapshot at :542, the 53-line morph/character comment block and its negated pair at :544-602, the single `morphedWave` call at :608, and the single `blep.step` call at :645
    - src/dsp/Waveshape.hpp lines 158-216 — `morphedWave`, and the `1/(1 + bleedIntensity)` divide at :212 that makes D-05's normalization automatic
    - .planning/phases/33-hard-sync/33-RESEARCH.md § "Architecture Patterns" (the eight-step sample walk, Patterns 1 through 4) and § "Code Examples" (deriving `f` and the reset, with both guards)
    - .planning/phases/33-hard-sync/33-RESEARCH.md § Common Pitfalls 1, 2, 4, 5, 6 and 7 — each has an exact trigger condition that belongs in a comment
    - .planning/research/STACK.md lines 118-160 — the Q3 hard-sync answer and the never-snap-to-zero landmine. NOTE: its jump expression is SIGN-INVERTED relative to `MorphBlep`'s documented convention; read it to know what not to transcribe
  </read_first>
  <action>
Land the sync block in `forge::VcoCore::step`, strictly BETWEEN the single-subtract wrap and the `p` snapshot. Nothing else in `step` moves except the conditioning block described in Part A.

Part A — move the conditioning. The `morph`/`character` negated pair currently sits AFTER the `p` snapshot. D-05's two `morphedWave` calls need conditioned values, so move the whole conditioning block — the pair and its full comment block, unedited except as Part D requires — to sit ABOVE the sync block, i.e. between the wrap and the sync block. Do not weaken, reorder or re-word the pair itself. Add one sentence to its comment recording that the block MOVED in this plan and why: the sync path calls the frozen waveshaper twice more, before the point the block used to sit at.

Part B — detection. Gate on `in.syncConnected` as an outer condition, exactly mirroring the shipped LFO's `resetConnected` gate, then call `syncTrig.process(in.syncVolts, 0.1f, 1.0f)`. Use those two literals verbatim: they are what `LfoCore.hpp:137` and `ClockTracker.hpp:111` both use and what the project's own stack research recommends, so this is inherited, not invented. Do NOT copy the LFO's body — its cosine crossfade and its `phase = 0.0` snap are exactly what SYNC-02 and the stack research forbid, and the comment must say so by name so a reader who follows the precedent to its source does not follow it too far.

Part C — the guarded sub-sample solve, and its comment. Compute the fraction as the HIGH threshold minus the stored previous voltage, over this sample's voltage minus the stored previous voltage. Then guard it with the negated-comparison pair: a negated lower comparison against zero, and a STRICT upper comparison against one, either failing sending the fraction to the fallback value ZERO.
Three properties, each of which the comment must state and plan 33-04 pins:
  - the negated lower comparison is TRUE for a not-a-number, so a not-a-number fraction lands on the fallback and never reaches `phase`, which carries no guard of its own — the accumulator's own guard is on the increment, not on the accumulator
  - the upper bound is STRICT so a resulting phase of exactly zero is unreachable by arithmetic
  - the fallback is ZERO and any fallback except ONE would do; ONE is the snap-to-zero landmine itself, and it is reachable from any master sample landing exactly on the high threshold, which a gate output idling at that level or a quantised CV does routinely
Give this guard the documentation depth this file gives the morph/character pair: what it protects, why it is here rather than in the frozen header, the exact trigger condition for each of the three landmines, and the named test that would go red. Reject the NaN-transparent comparison-ladder helper BY NAME in the comment, with its reason — both of its comparisons are false for a not-a-number, so it is inert against precisely the input class this guard exists to stop.

Part D — the reset and the jump. In order:
  1. take the pre-reset naive value by calling the frozen `morphedWave` at the fully advanced phase with the conditioned `morph`, the conditioned `character`, and a zero bleed argument
  2. overwrite the phase accumulator with the fractional overshoot: one minus the guarded fraction, times `deltaPhase`, in double
  3. take the post-reset value with ONE extra `morphedWave` call at the new phase — this is the whole extra cost D-05 authorises, paid only on samples where sync actually fires
  4. compute the jump as AFTER minus BEFORE
  5. set `tel.syncFired`, `tel.syncFrac` and `tel.syncJump`
The sign in step 4 is load-bearing and the comment must carry the reconciliation: `MorphBlep`'s documented convention is value-after minus value-before, while the project's own stack research writes the same quantity in the opposite order; transcribing the research expression verbatim inverts the correction from pulling the stepped sample toward the band-limited midpoint into pushing it away. Also record D-05's two decisions in the comment: that the pre-reset value REUSES the sample this step was going to compute anyway rather than probing both sides, and that the bleed normalization is satisfied BY CONSTRUCTION because a difference of two `morphedWave` outputs is already through the frozen divide — applying a second correction factor would over-correct by exactly that factor.
State in the comment that this is a REASONED departure from Phase 32's compute-it-don't-measure-it through-line, with its reason: every morph site is a known discontinuity with a closed form, whereas a reset lands at an arbitrary phase, so deriving the jump would be approximating a whole waveform rather than characterising a known edge. Say explicitly that it must not be "restored" to an analytic form for consistency.

Part E — the unconditional store. After the whole block, on every path and every branch, store this sample's `syncVolts` into `prevSyncVolts`. The comment must state the invariant — the store is the voltage the trigger saw on the immediately preceding sample — and the reachability analysis that makes it load-bearing: when the trigger returns true its previous state was LOW, so the previous sample did not satisfy the high comparison, so the denominator is strictly positive BY CONSTRUCTION and the zero divisor is unreachable while the invariant holds. Therefore a divide-by-zero here would be a STATE-MANAGEMENT defect, not an arithmetic one: a store gated on `syncConnected`, or skipped on any branch, or left stale across a connect or rate transition, re-opens it.
Resolve the standing discretion item in the same comment and state the choice rather than inheriting it: neither `prevSyncVolts` nor `syncTrig` is reset on a sample-rate change, because a rate change does not alter which sample was the previous one, and resetting either would break the very invariant the guard rests on. Name plan 33-04 as where that choice is asserted rather than merely written down.

Part F — the seam is DELIBERATELY ABSENT. Do NOT call `addStep`, `addPastStep` or any other `MorphBlep` entry point from the sync block in this plan. Write a boundary paragraph in its place saying so, with the reason: `addStep`'s contract is future-facing while a Schmitt-detected edge is always in the past, three placement candidates are on the table, and plan 33-05 measures them against THIS ordering before plan 33-06 pins the winner. Set the third telemetry float to zero here and name the plan that will populate it. State that the core's behavior as of this commit is exactly measurement leg `none` — reset applied, sync correction withheld — and that this is what makes the 33-05 probe's bit-exactness check against this core possible.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures and `make strict` exits 0
    - `make guards` exits 0, including `check_canary.sh` completing its perturbation of `src/dsp/VcoCore.hpp` without a "could not perturb" error
    - The threshold literals are the in-house pair: `grep -c '0.1f, 1.0f' src/dsp/VcoCore.hpp` outputs `1`
    - The seam is genuinely absent from this commit: `grep -n 'addStep\|addPastStep' src/dsp/VcoCore.hpp` returns no match outside comment lines — verified with `grep -vE '^[[:space:]]*(//|\*)' src/dsp/VcoCore.hpp | grep -c 'addStep'` outputting `0`
    - `blep.step` is still called exactly once per sample: `grep -vE '^[[:space:]]*(//|\*)' src/dsp/VcoCore.hpp | grep -c 'blep.step'` outputs `1`
    - The conditioning block precedes the sync block: the line number of the first `if (!(morph > 0.f))` is LESS than the line number of the first `syncTrig.process`, and both are less than the line number of `const float p = (float)phase;` — record all three line numbers in the SUMMARY
    - The comparison-ladder helper was not introduced on the new path: `grep -vE '^[[:space:]]*(//|\*)' src/dsp/VcoCore.hpp | grep -c 'clamp('` equals its value on the pre-task commit; record both numbers
    - The six shipped-LFO goldens replay byte-identical; `bash tests/check_frozen.sh` exits 0 with 15 entries unchanged
    - `git diff --name-only` lists only `src/dsp/VcoCore.hpp` and does not list `src/AnalogLFO.cpp`
    - With `syncConnected` left false, a 4096-sample block at 44.1 kHz is bit-identical to the pre-task binary's output for the same inputs — 0 differing samples by direct float equality, recorded in the SUMMARY
  </acceptance_criteria>
  <done>SYNC-01's detection, sub-sample solve, guarded fraction, fractional-overshoot reset, jump computation and unconditional store are landed, with every landmine's exact trigger condition written beside the guard that stops it, and the seam call deliberately withheld for the 33-05 measurement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-07's ordering claim, written as the claim it actually is, plus the residual phantom named (D-07)</name>
  <files>src/dsp/VcoCore.hpp</files>
  <behavior>
    - the post-reset phase is strictly less than `deltaPhase`, and `MorphBlep`'s site loop only fires sites within `dt` AHEAD of the phase it is given, so a site the reset jumped over is behind it and cannot fire — the suppression rule is discharged by the ORDERING, with no new predicate and nothing cached
    - the residual case the ordering does NOT cover — the previous sample's `pending`, deposited for sites the reset then skipped — is named in the source with its window width, rather than left for a reader to discover
  </behavior>
  <read_first>
    - src/dsp/MorphBlep.hpp lines 380-390 and 469-541 — the recompute-never-cache rule, the nine-site union, the site loop's crossing test and its wrap adjustment
    - src/dsp/MorphBlep.hpp line 233 — `pending` is a SCALAR SUM, which is why retroactive per-site cancellation is impossible without recording per-site contributions
    - .planning/phases/33-hard-sync/33-RESEARCH.md § "Pattern 4 — D-07's suppression, and where it actually bites", and § Open Questions item 1
    - .planning/phases/33-hard-sync/33-CONTEXT.md D-07 and the `<specifics>` paragraph on suppression being the compact-support instinct applied to sync
  </read_first>
  <action>
Write D-07's rule into `src/dsp/VcoCore.hpp` as the ORDERING CLAIM it actually is, and name the residual it does not cover. Do not implement a per-site predicate: the ordering already provides what such a predicate would assert, so adding one would be a rule that gates nothing — which this project treats as worse than no rule.

The comment must carry four things, each stated as a claim a test can be pointed at:
  1. the mechanism — after the reset the phase is one minus the fraction times `deltaPhase`, which is strictly less than `deltaPhase`; the band-limiter's site loop fires only sites within one sample-increment AHEAD of the phase it is handed, and a jumped-over site is BEHIND that phase, so it cannot fire. Cite the site loop and its wrap adjustment by file and line
  2. why suppression is the safe direction rather than a nicety — a free-run correction placed for an edge the waveform JUMPED OVER rather than crossed is new broadband energy, not a filter, and the register already MEASURED the general form of that mistake as a 30 dB regression when a step-shaped correction was added to a signal with no step
  3. the residual phantom, in full and without softening: at the PREVIOUS sample the loop fired every site within one increment ahead and deposited both halves; sites the reset then jumped over were never traversed, so both halves are phantom, and the second half is still sitting in the accumulator when this sample drains it. Give the window width — one minus the fraction, times the phase increment — and give the order of magnitude at a high note. State that the accumulator is a scalar sum so per-site cancellation is impossible without restructuring the band-limiting header, and that the DISPOSITION taken is to accept and document rather than restructure, with the two reasons: the effect is partly self-cancelling because the pre-reset value is taken PAST the jumped-over site so the jump already contains that site's step with the opposite sign, and restructuring the header this phase is otherwise only hardening is out of proportion
  4. the recompute-never-cache constraint — the site geometry already moves with character and will move per sample once a later phase's drift writes the spread fields, so nothing about the suppression may be cached across samples

Record the phantom as an item for the phase's deferred register in the plan SUMMARY, so plan 33-11 files it with a Resolve-at rather than losing it. Note in the SUMMARY that plan 33-05's grid should carry a diagnostic column reporting it, so a later phase inherits a number rather than an argument.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0
    - The rule is stated as an ordering claim and no per-site suppression predicate was added: the diff for this task adds no new loop, no new array and no new member — `git diff -U0 src/dsp/VcoCore.hpp | grep -c '^+[^+/ ]'` outputs `0` for this task's commit (comment-only change), recorded in the SUMMARY
    - The comment cites the site loop by file and line, and the citation resolves: the referenced line in `src/dsp/MorphBlep.hpp` contains the crossing test
    - `bash tests/check_canary.sh` exits 0 — no comment added by this task quotes the `step` signature on a line that also contains an opening brace
    - The deferred-register item for the residual `pending` phantom is written into the plan SUMMARY with a proposed Resolve-at
    - `git diff --name-only` lists only `src/dsp/VcoCore.hpp`; `bash tests/check_frozen.sh` exits 0 with 15 entries unchanged
  </acceptance_criteria>
  <done>D-07 is discharged honestly as an ordering claim with its mechanism cited, its safe-direction argument recorded, and the one residual it does not cover named with its window width and its accepted disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rack SYNC jack → `forge::VcoInputs::syncVolts` | An arbitrary cable voltage, including a not-a-number produced by an upstream module, crosses here untrusted |
| `forge::VcoInputs` → `forge::VcoCore::step` | The headless harness builds the POD directly with no shell in the way, so it is the only caller that can reach the new guards with a hostile value |
| `forge::VcoCore` → frozen `Waveshape::morphedWave` | A float-to-int cast of a non-finite phase or morph is undefined behavior inside a byte-pinned header that cannot be edited |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Denial of Service | permanent poisoning of `VcoCore::phase` via a not-a-number sub-sample fraction | high | mitigate | Task 2 Part C: the negated lower comparison is true for a not-a-number so the fraction lands on the fallback and never reaches the accumulator, which carries no guard of its own. Pinned by plan 33-04's poisoned-instance case with a withdrawal phase |
| T-33-05 | Denial of Service | division by zero in the sub-sample solve when the previous-voltage store is stale | medium | mitigate | Task 2 Part E: the store is unconditional on every sample and every branch, which is what makes the zero divisor unreachable; the guard on the result covers the transitions where the invariant is broken. Both the invariant and the rate-change choice are asserted by plan 33-04 |
| T-33-06 | Denial of Service | the sub-sample fraction reaching exactly one, snapping the phase to zero through the front door | medium | mitigate | Task 2 Part C: the upper bound is STRICT and the fallback is zero, never one. Reachable from any master sample landing exactly on the high threshold, which is not a measure-zero event for a gate output or a quantised CV |
| T-33-11 | Denial of Service | the fraction leaving the zero-to-one range once the master is band-limited, driving a negative or oversized phase | medium | mitigate | Task 2 Part C's pair bounds both sides. Corroborated by VCV Fundamental, which gates the same quantity on both ends for the same reason |
| T-33-08 | Tampering | toolchain-divergent undefined behavior invisible on the Apple-clang development host | high | mitigate | `make strict` on every task; the CI MinGW **link** leg observed green on the exact commit at the phase gate (plan 33-11) |
| T-33-12 | Tampering | a guard written with a NaN-transparent comparison ladder, which is inert against exactly the input class it exists to stop | high | mitigate | The negated-comparison idiom is mandated at every new guard and the ladder helper is rejected BY NAME in the source with its reason; the comment-stripped occurrence count is compared against the pre-task baseline rather than against zero |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- `make test && make strict && make guards` all exit 0
- `check_canary.sh` perturbs `src/dsp/VcoCore.hpp` successfully and `[2b/5]` still reports a field count
- The seam call is verifiably absent from the comment-stripped source
- The conditioning block precedes the sync block, which precedes the `p` snapshot — all three line numbers recorded
- The six shipped-LFO goldens replay byte-identical; all 15 frozen paths unchanged; `src/AnalogLFO.cpp` absent from the diff
</verification>

<success_criteria>
`forge::VcoCore` detects a master rising edge, solves the sub-sample instant behind a guard whose three landmines each have their trigger condition written beside them, resets to a fractional overshoot that can never be exactly zero, and computes the jump from one extra frozen-waveshaper call — with the seam call deliberately withheld so the placement convention is decided by measurement rather than by assumption.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-02-SUMMARY.md` when done
</output>

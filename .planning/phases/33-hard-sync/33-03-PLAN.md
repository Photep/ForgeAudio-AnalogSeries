---
phase: 33-hard-sync
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - src/AnalogVCO.cpp
  - src/vco_compile_canary.cpp
  - res/AnalogVCO.svg
autonomous: true
nyquist_compliant: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "D-18: `src/AnalogVCO.cpp` gains a SYNC jack and assigns `syncVolts` and `syncConnected` into the POD with NO arithmetic — its THIS FILE DOES NO DSP banner holds, and both fields cross unconditionally exactly as the FM pair does."
    - "D-18: `res/AnalogVCO.svg` gains ONE jack position so the operator has something to patch — Phase 30's D-07 rule that every visible control does something continues. The SVG stays a throwaway replaced wholesale in Phase 35; no Forge Noir design language is applied here."
    - "D-18: `src/AnalogLFO.cpp` remains absent from this phase's diff, as in Phases 30, 31 and 32."
    - "D-02 / register item 15: `src/vco_compile_canary.cpp` feeds BOTH new `VcoInputs` fields runtime-derived values from non-overlapping bit slices of the loop counter, so `check_canary.sh [2b/5]` keeps proving what the shell does not."
    - "D-02 / the trap the register does not name: `syncConnected` must VARY and be TRUE on some iterations. A constant-false flag folds the entire sync branch away at -O3 while [2b/5] still reports PASS on the field's presence — the canary would then cover the new code path with nothing."
    - "The hand-maintained field-count sentence at `src/AnalogVCO.cpp:320-331` is updated in the same commit that moves both of its numbers — a plan task with its own rationale, not a gate-time discovery."
  artifacts:
    - src/AnalogVCO.cpp
    - src/vco_compile_canary.cpp
    - res/AnalogVCO.svg
  key_links:
    - "`src/AnalogVCO.cpp` and `src/vco_compile_canary.cpp` are both already registered in `tests/check_includes.sh` `VCO_SIDE_ALLOW`, so this plan needs no guard-allowlist edit — verified, not assumed."
    - "Both new TUs' contents join `make strict`, the plugin link and the CI MinGW compile-and-link loop with ZERO Makefile or CI wiring, because both globs already cover `src/*.cpp`."
    - "`check_frozen.sh`'s `FROZEN_EXPECTED_ENTRIES` stays at 15 for a modified `res/` asset, as it did in Phase 30 for a new one."
---

<objective>
Wire the SYNC jack through the Rack shell, feed both new POD fields runtime-derived values in the compile canary, and give the panel a jack position to patch — so the phase's DSP is reachable from Rack and the canary keeps proving the C++11/ODR property it exists for.

Purpose: the operator cannot audition sync without a jack, and `check_canary.sh [2b/5]` silently stops proving anything the moment a `VcoInputs` field is fed a literal. The specific hazard here is not the field count but the flag: a constant-false `syncConnected` lets `-O3` fold the whole sync branch away before the MinGW linker is ever consulted, while the gate still reports PASS.

Output: `src/AnalogVCO.cpp` with a fifth input and two POD assignments, `src/vco_compile_canary.cpp` with two non-overlapping runtime-derived feeds, `res/AnalogVCO.svg` with one more jack rect, and both hand-maintained count sentences corrected in the same commit that moves them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-RESEARCH.md
@.planning/phases/33-hard-sync/33-PATTERNS.md
@.planning/phases/33-hard-sync/33-02-SUMMARY.md
@src/AnalogVCO.cpp
@src/vco_compile_canary.cpp
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`.

New symbols:
- `forge::VcoInputs::syncVolts`, `forge::VcoInputs::syncConnected`
- `forge::VcoCore::syncTrig`, `forge::VcoCore::prevSyncVolts`
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection`
- `forge::MorphBlep::addPastStep(float, float)` — conditional on the 33-05 measurement
- `AnalogVCO::SYNC_INPUT` enum member
- `make audition` target, `AUDITION_BIN`, `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: `AnalogVCO::SYNC_INPUT`, the two shell POD assignments, the two canary feeds, and one additional `res/AnalogVCO.svg` rect.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The SYNC jack in the shell, and the field-count sentence it moves (D-18)</name>
  <files>src/AnalogVCO.cpp</files>
  <behavior>
    - `AnalogVCO` exposes a fifth input; the enum gains its member APPENDED before the length sentinel so every existing index is stable
    - `process()` assigns the jack's voltage and connected state into the POD unconditionally, with no `if`, no zeroing and no arithmetic
    - the panel widget places a port at the new jack position and the module still constructs, registers and processes
    - the file's hand-maintained field-count sentence states the counts this commit produces, not the ones it replaced
  </behavior>
  <read_first>
    - src/AnalogVCO.cpp in full — the THIS FILE DOES NO DSP banner, the `InputId` enum at :102-107, the `configInput` calls at :186-188, the unconditional-FM-crossing paragraph and the two FM POD assignments at :302-318, the field-count sentence at :320-345, and the widget block at :401-406
    - src/dsp/VcoCore.hpp as landed by plan 33-02 — the two new POD fields and their comments
    - .planning/phases/33-hard-sync/33-PATTERNS.md § `src/AnalogVCO.cpp` — the FM jack quoted at all five of the places it is wired
    - .planning/STATE.md § Accumulated Context — the Phase 31 decision that both FM fields cross unconditionally and that the shipped LFO's zeroing ternary is named in the source as the anti-pattern
  </read_first>
  <action>
Wire the SYNC jack through all five places the FM jack is wired, copying each shape exactly.

1. Append a `SYNC_INPUT` member to the `InputId` enum, BEFORE the length sentinel. Appending rather than inserting keeps every existing index stable, which is this file's convention.
2. Add a `configInput` call for it beside the existing three, with a short display name.
3. In `process()`, add two POD assignments beside the FM pair: the jack's voltage into `syncVolts`, and its connected state into `syncConnected`. No conditional, no zeroing, no arithmetic. The banner paragraph at `:302-312` already argues why both FM fields cross unconditionally and names the shipped module's zeroing ternary as the anti-pattern; extend that paragraph by one sentence covering the sync pair, and make the sentence say what is DIFFERENT about it rather than repeating the FM argument: the core's gate on the connected flag does something strictly stronger than substituting zero — it does not run the detector at all — and, decisively, it is what keeps a mis-detected edge visible to a headless test. A shell-side trigger would hand the core an already-decided boolean and no test could ever see the detection go wrong.
4. Add the port widget at a free position on the jack row, using the same stock port widget and the same millimetre-to-pixel helper as the existing four. Place it on the existing jack row so the throwaway panel stays legible; the physical form and final placement are Phase 35's call and this file should say so in one clause.
5. Update the hand-maintained field-count sentence at `:320-331`. This phase moves BOTH of its numbers: the count of fields the shell feeds and the count of fields the canary feeds. Restate both from the landed source rather than by arithmetic on the old sentence, and re-check the claim it carries about which single field only the canary reaches — that claim is what keeps `check_canary.sh [2b/5]`'s argument honest, and it must state what is true after this commit. Do NOT weaken the paragraph that follows it, which argues the canary's real value is structural rather than arithmetic; that argument is unchanged and is the reason the narrowing count does not matter.

Do not add any computation to this file. If the task appears to need one, that is a sign it belongs in `forge::VcoCore` — Phase 31's D-17 and this file's own banner both say so.
  </action>
  <verify>
    <automated>make strict && make test && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make strict` exits 0 (C++11, `-pedantic-errors`) over all four `src/*.cpp` translation units
    - `make test` exits 0 with 0 failures; `make guards` exits 0
    - `make` (the real plugin link against `../Rack-SDK`) exits 0 and produces `plugin.dylib`
    - The jack is wired in all five places: `grep -c 'SYNC_INPUT' src/AnalogVCO.cpp` outputs `4` (enum, `configInput`, two POD-assignment reads, widget) — record the actual number and the line numbers in the SUMMARY, and if it differs, say which of the five places uses a different spelling rather than adjusting the criterion
    - The shell does no arithmetic on the new fields: the two new assignment lines contain no operator other than `=` — confirmed by quoting both lines in the SUMMARY
    - The field-count sentence states the post-commit counts: `grep -n 'VcoInputs DSP fields' src/AnalogVCO.cpp` shows a sentence whose numbers match the field counts actually fed by `src/AnalogVCO.cpp` and `src/vco_compile_canary.cpp` after Task 2; re-check this criterion after Task 2 lands
    - `git diff --name-only` does not list `src/AnalogLFO.cpp`; `bash tests/check_frozen.sh` exits 0 with 15 entries unchanged
    - The six shipped-LFO goldens replay byte-identical
  </acceptance_criteria>
  <done>The SYNC jack exists in the shell, both POD fields cross unconditionally with no arithmetic, and the file's own coverage sentence states the counts this commit produced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The compile canary's two runtime-derived feeds, and the fold-away trap the register does not name (D-02 / register item 15)</name>
  <files>src/vco_compile_canary.cpp</files>
  <behavior>
    - both new `VcoInputs` fields are assigned from non-overlapping bit slices of the runtime loop counter, never from a literal and never from an NSDMI default
    - the connected flag VARIES across iterations and is TRUE on some of them, so the sync branch is genuinely emitted at `-O3`
    - `check_canary.sh [2b/5]` reports the new, higher count of runtime-live fields and still exits 0
  </behavior>
  <read_first>
    - src/vco_compile_canary.cpp in full — the LOAD-BEARING banner with its measured constant-fold anecdote at :89-103, the nine existing feeds at :104-112, the runtime-derived trip count and its rationale, and the telemetry read at the end
    - tests/check_canary.sh — section `[2b/5]` in full: how it enumerates `VcoInputs` fields and how it decides a field is runtime-live
    - .planning/phases/33-hard-sync/33-RESEARCH.md § Common Pitfalls, Pitfall 9 — the exact fold-away trap and the suggested bit slices
    - .planning/phases/33-hard-sync/33-PATTERNS.md § `src/vco_compile_canary.cpp`
  </read_first>
  <action>
Add two feeds beside the nine existing ones, in the same style and the same alignment.

Derive `syncVolts` from a bit slice of the loop counter that does not collide with any slice already in use, mapped to a bipolar volt range that STRADDLES both hysteresis thresholds — some iterations must sit below the low threshold and some at or above the high threshold, so the trigger's state machine actually transitions across the loop rather than idling in one state. Derive `syncConnected` from a single bit test of a different, non-colliding slice, in exactly the shape the existing connected flag uses — a bit test, never a literal.

Before writing them, ENUMERATE the bit positions the nine existing feeds occupy and record that enumeration in the SUMMARY. The two naming groups currently overlap deliberately, so "non-overlapping" must be checked against the actual slices rather than assumed from the suggested positions.

Extend the LOAD-BEARING banner by one paragraph. It must name the trap the register does not: if the connected flag were fed a constant false, the entire sync branch would fold away at `-O3` and the canary would cover the new code path with NOTHING, while the gate still reported PASS on the field's presence. The flag must therefore vary AND be true on some iterations, and the voltage must cross both thresholds. State that this is a stronger requirement than the existing "no literals" rule, and that it is the reason this feed is not merely mechanical.

Do not change the loop trip count, the seeds, the telemetry read, or the POD construction idiom — the copy-and-assign form is required because the POD has non-static data member initializers and is therefore not an aggregate under C++11.
  </action>
  <verify>
    <automated>make strict && make guards && make test</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check_canary.sh` exits 0 and its `[2b/5]` section reports a runtime-live field count exactly two higher than the count it reported on the pre-task commit — record BOTH numbers in the SUMMARY
    - `make strict` exits 0; `make guards` exits 0; `make test` exits 0 with 0 failures
    - No literal feed was introduced: every new assignment's right-hand side references the loop counter — quote both new lines in the SUMMARY
    - The bit slices are provably non-overlapping: the SUMMARY carries the enumerated bit positions of all eleven feeds
    - The branch is genuinely emitted at `-O3`: compile the canary TU alone at `-O3` and confirm the emitted object references the sync path — record the command and its output in the SUMMARY. A `[2b/5]` PASS alone is explicitly NOT accepted as evidence for this criterion, because that is the exact false green the new banner paragraph describes
    - `git diff --name-only` lists only `src/vco_compile_canary.cpp` for this task and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>Both new POD fields are runtime-live in the canary, the connected flag varies and is true on some iterations, and the fold-away trap is written into the banner so a later editor cannot re-introduce it by "simplifying" the feed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One jack position on the throwaway panel, and the full local gate (D-18)</name>
  <files>res/AnalogVCO.svg</files>
  <behavior>
    - the panel SVG gains exactly one more placeholder rect, matching the eleven existing ones in size and fill, at a free position on the jack row
    - `check_frozen.sh` still reports 15 entries and the plugin still builds, installs and registers
  </behavior>
  <read_first>
    - res/AnalogVCO.svg in full — all fourteen lines; there are no groups, ids or classes and none should be introduced
    - src/AnalogVCO.cpp as landed by Task 1 — the widget position chosen for the SYNC port, so the rect and the port agree
    - .planning/phases/33-hard-sync/33-PATTERNS.md § `res/AnalogVCO.svg` — the jack-row geometry and the half-width offset between a widget centre in millimetres and the rect's x
    - .planning/phases/33-hard-sync/33-CONTEXT.md D-18 — the SVG is a throwaway replaced wholesale in Phase 35; physical form and placement are Phase 35's call
  </read_first>
  <action>
Add exactly one placeholder rect to `res/AnalogVCO.svg` at the position matching Task 1's widget centre, with the same width, height and fill as the eleven existing ones. Derive the rect's x from the widget centre by subtracting the half-width, exactly as the existing jack rects do; confirm the derivation against two existing pairs before writing it rather than trusting the arithmetic once.

Do NOT apply the Forge Noir design language, add labels, groups, ids, classes or styling. This asset is deliberately ugly and is replaced wholesale in Phase 35; making it pretty here creates work that phase must undo, and Phase 30's operator sign-off already records the unlabelled panel as expected rather than as a defect.

Then run the full local gate for this plan and record every result in the SUMMARY:
  - `make test`, `make strict`, `make guards`
  - `make` against the real Rack SDK, confirming the plugin links
  - `bash tests/check_frozen.sh`, confirming 15 entries and `FROZEN.sha256` byte-identical by direct byte comparison rather than by counting diff markers
  - the six shipped-LFO goldens replaying byte-identical
  - `git diff --name-only` for the whole plan, confirming it lists exactly the three files this plan owns and does not list `src/AnalogLFO.cpp`
  - `tests/check_includes.sh`, confirming `src/AnalogVCO.cpp` and `src/vco_compile_canary.cpp` are still matched by their existing `VCO_SIDE_ALLOW` entries and that NO new allowlist entry was needed — this plan adds no new translation unit, and that is worth stating because plans 33-10's renderer does and pays for it explicitly
  </action>
  <verify>
    <automated>make test && make strict && make guards && make</automated>
  </verify>
  <acceptance_criteria>
    - The SVG gained exactly one rect: `grep -c '<rect' res/AnalogVCO.svg` equals its pre-task value plus 1 — record both numbers
    - The new rect's width, height and fill are byte-identical to an existing jack rect's, and its x is the widget centre minus the half-width — quote the new line and one existing jack line side by side in the SUMMARY
    - No group, id, class or style attribute was introduced: `grep -cE '<g |id=|class=|style=' res/AnalogVCO.svg` outputs `0`
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0; `make` links `plugin.dylib`
    - `bash tests/check_frozen.sh` exits 0 and reports 15 entries; `FROZEN.sha256` is byte-identical to its pre-plan state by `cmp`
    - The six shipped-LFO goldens replay byte-identical with a non-zero matched case count
    - `git diff --name-only` for the whole plan lists exactly `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, and does not list `src/AnalogLFO.cpp`
    - `bash tests/check_includes.sh` exits 0 with no new `VCO_SIDE_ALLOW` entry added — confirmed by `git diff --stat tests/check_includes.sh` producing no output
  </acceptance_criteria>
  <done>The panel carries a jack the operator can patch, the whole local gate is green, and the plan is confirmed to have needed no guard-allowlist change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rack SYNC jack → `AnalogVCO::process` → `forge::VcoInputs` | An arbitrary cable voltage crosses here; the shell marshals it and validates nothing, by decision |
| Apple-clang `-O3` development build → x86 MinGW/GCC shipping link | A branch folded away locally leaves the link gate nothing to resolve; this is the property the canary exists to preserve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-13 | Tampering | the compile canary silently stops covering the sync branch because the connected flag folds to a constant | high | mitigate | Task 2: the flag is derived from a bit slice of the runtime loop counter, varies across iterations and is true on some; the emitted object is inspected at `-O3` and a `[2b/5]` PASS is explicitly refused as evidence on its own |
| T-33-08 | Tampering | toolchain-divergent undefined behavior or ODR defect invisible on the development host | high | mitigate | `make strict` over all four `src/*.cpp` translation units on every task; the CI MinGW **link** leg observed green on the exact commit at the phase gate (plan 33-11). `make strict` is `-fsyntax-only` and never links |
| T-33-14 | Tampering | the shell acquiring arithmetic and becoming a second, untested DSP site | medium | mitigate | Task 1 forbids any operator other than assignment on the two new lines, and the criterion quotes both lines; the core's own guards cover the values regardless of what the shell does |
| T-33-15 | Spoofing | a frozen shared header or the shipped LFO source entering this phase's diff | high | mitigate | `check_frozen.sh` at 15 entries with `FROZEN.sha256` compared by bytes, the six LFO goldens replayed byte-identical, and `src/AnalogLFO.cpp` asserted absent from `git diff --name-only` on every task |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- `make test && make strict && make guards && make` all exit 0
- `check_canary.sh [2b/5]` reports exactly two more runtime-live fields than before, with both numbers recorded
- The sync branch is confirmed emitted at `-O3` by object inspection, not by the gate's own PASS
- The SVG gained exactly one rect with no styling introduced
- 15 frozen entries unchanged, `FROZEN.sha256` byte-identical, six LFO goldens byte-identical, `src/AnalogLFO.cpp` absent from the diff
</verification>

<success_criteria>
The SYNC jack is patchable in Rack, the shell computes nothing, the compile canary genuinely covers the new branch at `-O3` rather than merely naming its fields, and both hand-maintained count sentences state what is true after this commit.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-03-SUMMARY.md` when done
</output>

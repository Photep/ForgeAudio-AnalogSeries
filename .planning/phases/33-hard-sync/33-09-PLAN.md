---
phase: 33-hard-sync
plan: 09
type: tdd
wave: 5
depends_on: ["33-06"]
files_modified:
  - tests/test_vco_pitch.cpp
autonomous: true
nyquist_compliant: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "D-12: PITCH-04 is re-confirmed with SYNC AS A THIRD INPUT CLASS on Phase 31's existing case — extreme pitch times extreme FM times hostile sync — closing Phase 31's deferred item 11, which marked the requirement complete on two of three input classes."
    - "D-12: the requirement is re-ticked ONLY WHERE SYNC IS OBSERVED FIRING behind the claim. The observation is an accumulated non-zero count of the sync telemetry flag across the drive, asserted in the same case that makes the pitch claim — the Phase 31 non-vacuity discipline, where four consecutive plans declined to tick a requirement their own frontmatter claimed."
    - "D-12: a dedicated separate case was REJECTED because it would split one requirement's evidence across two files; folding the claim into the hostile grid was REJECTED because that grid asserts finiteness and boundedness rather than pitch accuracy, and is the wrong instrument for PITCH-04's claim."
    - "D-12: the phase's NEW DIVISOR is exercised here as well as in the core suite, because a new division appearing behind an input field is what moved the hostile grid into the preceding phase — and that rationale was itself a correction of a falsified one, so it is applied deliberately rather than inherited."
    - "The rising-zero-crossing frequency estimator is blind under about two samples per cycle and MUST NOT carry any sync high-note claim; where the estimator cannot see, the claim is not made."
  artifacts:
    - tests/test_vco_pitch.cpp
  key_links:
    - "Phase 31 deferred item 11 is the debt this plan closes; the phase gate in plan 33-11 is where PITCH-04's status is re-stated, and only if this plan's evidence is non-vacuous."
    - "Band-limiting already made the estimator's narrow-pulse tracking worse at high pitch — a sampling limitation of the estimator, not a DSP defect. Sync must not be blamed for it, and the case must not use the estimator where it is blind."
---

<objective>
Close Phase 31's deferred item 11 by re-confirming PITCH-04 with sync as a third input class, and re-tick it only where sync is observed firing behind the claim.

Purpose: PITCH-04 was marked complete on extreme pitch and extreme FM. Sync did not exist then and could not have been covered. This phase adds a third input class and a new divisor behind a new POD field, so the requirement's evidence has to be re-made rather than inherited. The discipline that matters is the one Phase 31 established four times over: a requirement is ticked only where the control is observed FIRING behind the claim.

Output: PITCH-04's third input class in `tests/test_vco_pitch.cpp`, with sync observed firing, and the pitch claim stated only where the instrument can see.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-RESEARCH.md
@.planning/phases/33-hard-sync/33-PATTERNS.md
@.planning/phases/33-hard-sync/33-06-SUMMARY.md
@.planning/phases/33-hard-sync/33-04-SUMMARY.md
@src/dsp/VcoCore.hpp
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`.

New symbols:
- `forge::VcoInputs::syncVolts`, `forge::VcoInputs::syncConnected`
- `forge::VcoCore::syncTrig`, `forge::VcoCore::prevSyncVolts`
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection`
- `forge::MorphBlep::addPastStep(float, float)` — conditional on the 33-05 measurement
- `AnalogVCO::SYNC_INPUT`
- `make audition`, `AUDITION_BIN`, `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: the `"vco pitch: (PITCH-04 / D-12) ..."` `TEST_CASE` and its sync-firing observation.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PITCH-04's third input class, with sync observed firing (D-12)</name>
  <files>tests/test_vco_pitch.cpp</files>
  <behavior>
    - the existing extreme-pitch times extreme-FM case gains a sync axis, so all three input classes are driven together rather than pairwise
    - across the whole grid the output is finite, the magnitude stays within the outer measured tier, and the phase accumulator stays in range
    - the pitch claim is made only on the cells where the frequency instrument can actually see, and the exclusion is stated on the instrument's known blindness rather than on which cells failed
    - the accumulated count of sync-firing samples is non-zero on every cell where sync is patched, and that count is ASSERTED, not merely captured
  </behavior>
  <read_first>
    - tests/test_vco_pitch.cpp around line 2138 — the existing PITCH-04 Nyquist case, its structure and its naming convention
    - tests/test_vco_pitch.cpp around line 2448 — the hostile pitch/FM standing regression case, which is the case this plan extends with a third axis
    - tests/test_vco_pitch.cpp around line 1929 — the negative-control title convention, where a case's own title states that passing means the control DETECTED the defect
    - tests/test_vco_core.cpp lines 160-190 and line 594 — the frequency estimator and its CORRECTED premise: it counts rising zero crossings and is blind under about two samples per cycle
    - src/dsp/VcoCore.hpp as landed by plan 33-06 — the sync block and its telemetry
    - .planning/phases/33-hard-sync/33-CONTEXT.md D-12 — both rejected alternatives and why each is the wrong instrument
  </read_first>
  <action>
Extend the existing hostile pitch and FM case in `tests/test_vco_pitch.cpp` with a SYNC axis, so the three input classes are driven together. Do not add a separate case: splitting one requirement's evidence across two files is the alternative D-12 rejects by name, and the reason belongs in the case comment.

The sync axis must carry, at minimum: an unpatched control (so the pre-existing evidence is preserved as a row rather than replaced), a legitimate master at a musical rate, a master far above the sample rate, a master idling at a steady voltage above the high threshold, a not-a-number cable voltage, and a master whose sample lands exactly on the high threshold.

Assert with the accumulate-then-assert idiom across the whole grid: every sample finite, magnitude within the outer measured tier, and the phase accumulator in range. Capture a first-bad-cell index for diagnosis.

THE NON-VACUITY HALF, which is the point of the plan. On every cell where sync is patched with a master that should fire, accumulate the count of samples on which the sync telemetry flag was true and ASSERT that count is greater than zero. Write into the comment what this assertion is for, in the file's own idiom: a hostile grid in which sync never fires would report green while covering the sync path with nothing, and this project has four consecutive plans on record declining to tick a requirement whose frontmatter already claimed it. Record the observed firing counts per cell in the SUMMARY.

THE PITCH CLAIM, stated only where the instrument can see. Make the tracking assertion on the subset of cells where the slave's frequency is high enough above the estimator's blindness threshold for the estimator to be valid, and state that restriction on the ESTIMATOR'S KNOWN LIMITATION — before enumerating which cells qualify — rather than on which cells produced awkward numbers. Say in the comment that band-limiting already made the estimator's narrow-pulse tracking worse at high pitch, that the cause was measured to be the estimator's sampling rather than the DSP, and that sync must not be blamed for it. Where the estimator is blind, make NO pitch claim and say so.

Add the new-divisor rows explicitly, with a one-line comment each naming the physical case: the steady-voltage stale-store case, the not-a-number cable, and the exactly-on-threshold sample. State in the case comment why the divisor is exercised here as well as in the core suite — a new division appearing behind a new input field is the rationale that moved a hostile grid into the preceding phase, and that rationale was itself a correction of a falsified one, so it is applied deliberately rather than inherited.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco pitch: (PITCH-04*" -s 2>&1 | tail -4` reports a non-zero matched case count, 0 failures, and a non-zero assertion count
    - The sync-firing counts are recorded per cell in the SUMMARY and every patched cell that should fire has a count greater than zero
    - The firing assertion is proved able to fail: temporarily set the sync jack unpatched for the whole grid in the working tree and confirm the case goes RED on the firing assertion specifically. Restore and confirm green. Record the failure signature
    - The estimator-based pitch claim's restriction is stated on the estimator's blindness in the source ABOVE the enumeration of qualifying cells — record both line numbers
    - `make strict` exits 0; `make guards` exits 0
    - `git diff --name-only` lists only `tests/test_vco_pitch.cpp` and does not list `src/AnalogLFO.cpp`
    - No requirement is ticked by this task; the re-tick decision belongs to the phase gate and depends on this evidence being non-vacuous
  </acceptance_criteria>
  <done>PITCH-04's three input classes are driven together, sync is observed firing behind the claim rather than assumed, and the pitch assertion is confined to where its instrument can actually see.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State what the re-confirmation does and does not establish, in the source</name>
  <files>tests/test_vco_pitch.cpp</files>
  <behavior>
    - the case's own title and banner state which requirement it discharges, which deferred debt it closes, and which claim it deliberately does not make
    - the suite's case and assertion totals are recorded so the phase gate can account for the per-leg deltas
  </behavior>
  <read_first>
    - tests/test_vco_pitch.cpp as landed by Task 1 — the extended case
    - .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md — item 11, the debt being closed, in its own words
    - .planning/STATE.md § Accumulated Context — the Phase 31 entries recording four consecutive plans declining to mark a requirement complete, and the reason each gave
  </read_first>
  <action>
Write the case's banner so the evidence is readable without the planning tree.

It must state, in this order:
  - which requirement this case re-confirms and which phase originally ticked it
  - the exact debt being closed: the requirement was marked complete on two of three input classes because the third did not exist yet, and this is the phase that adds it
  - what the case asserts: finiteness, the outer magnitude tier, the accumulator range, and tracking accuracy on the subset where the estimator is valid
  - what it deliberately does NOT assert, and why: no pitch claim where the estimator is blind; no claim about the sync correction's spectral quality, which lives in the spectral grid; and no claim about the per-sample step across a reset, which lives in the time-domain continuity gate. Naming the neighbours is what stops a later reader treating this case as broader than it is
  - the non-vacuity condition in one sentence: this case's pitch claim stands only while its sync-firing counts are non-zero, and the firing assertion is what enforces that

Then run the whole suite and record the case and assertion totals as a before/after pair against plan 33-08's recorded totals, per leg, so the phase gate can prove every new case ran on every platform rather than inferring it.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0
    - The banner contains all five items in the stated order — enumerate which line range covers which item in the SUMMARY
    - The three neighbouring claims are named by their case titles, and each named title resolves: running each as a selector reports a non-zero matched case count
    - The whole suite's case and assertion totals are recorded as a before/after pair
    - `git diff --name-only` lists only `tests/test_vco_pitch.cpp` and does not list `src/AnalogLFO.cpp`; the six LFO goldens replay byte-identical
  </acceptance_criteria>
  <done>The re-confirmation states its own scope and its own limits in the source, so no later reader can take it for more than it evidences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rack SYNC jack → `forge::VcoInputs::syncVolts` → the new divisor | A new division appearing behind a new input field; hostile cable voltages cross here |
| a requirement's tick → the evidence behind it | A requirement ticked on a case where the control never fired is a false green, which this project has four recorded instances of declining |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Denial of Service | permanent poisoning of the phase accumulator via a not-a-number sub-sample fraction, reached through the pitch suite's own hostile drives | high | mitigate | Task 1's sync axis carries the not-a-number cable and the exactly-on-threshold sample, and asserts the accumulator stays in range across the whole grid |
| T-33-05 | Denial of Service | zero divisor reached through a steady voltage against a stale store | medium | mitigate | Task 1's steady-voltage row exercises exactly that case behind the guard the core landed |
| T-33-30 | Repudiation | ticking a requirement on a case where the control under test never fired | high | mitigate | Task 1 asserts a non-zero accumulated sync-firing count per patched cell, and the assertion is proved able to fail by unpatching the jack |
| T-33-31 | Repudiation | attributing an instrument's known blindness to the DSP under test | medium | mitigate | The pitch claim is restricted on the estimator's stated limitation before the population is enumerated, and the prior measurement showing the cause is the estimator's sampling is recorded in the banner |
| T-33-08 | Tampering | toolchain-divergent behavior invisible on the development host | high | mitigate | `make strict` at both task gates; the CI MinGW **link** leg observed green on the exact commit at the phase gate (plan 33-11) |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- `make test && make strict && make guards` all exit 0
- Sync-firing counts are non-zero on every patched cell that should fire, asserted rather than captured, and the assertion is proved able to fail
- The pitch claim's restriction is stated on the estimator's blindness before the population is enumerated
- No requirement is ticked by this plan; the decision belongs to the phase gate
- `src/AnalogLFO.cpp` absent from the diff; the six LFO goldens byte-identical
</verification>

<success_criteria>
PITCH-04's third input class exists, sync is observed firing behind every cell that carries the claim, and the case states its own limits in the source — so the phase gate can re-tick the requirement on evidence rather than on frontmatter.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-09-SUMMARY.md` when done
</output>

---
phase: 33-hard-sync
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/dsp/MorphBlep.hpp
  - tests/test_morph_blep.cpp
autonomous: true
nyquist_compliant: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "D-04 / CR-01: a negative or not-a-number `morph` reaching `MorphBlep::step` cannot index `W[segment]` outside the `float[5]`. The lower clamp is landed AND the entry conditioning is landed; each is proved load-bearing by its own revert-one-only probe producing a DIFFERENT red."
    - "D-04 / CR-02: a non-finite `character` produces no non-finite correction, including at the three sites whose width is a literal `0.f` and therefore never reach `morphBlepCharFactor`'s not-a-number trap."
    - "D-04 third item: a non-finite `jump` handed to `addStep` is REJECTED at the entry gate and leaves `inject`/`pending` exactly `0.0f`; a poisoned-instance trace shows the instance still finite after the hostile value is withdrawn."
    - "D-04: every guard uses the negated-comparison idiom written negated FIRST; no clamp ladder and no NaN-transparent helper is introduced on any of the three paths."
    - "D-04: the ASan reproduction of CR-01 is a SCOPED ONE-SHOT PROBE — no sanitizer target is added to the Makefile, `make test`, `make guards` or CI (register item 12)."
    - "The pinned seam equivalence in case five parts A and B is UNMOVED: a driven morph site at s = 0.5 and `addStep(0.5, 2)` still produce the same +0.250000 / -0.250000 split."
  artifacts:
    - src/dsp/MorphBlep.hpp
    - tests/test_morph_blep.cpp
  key_links:
    - "This plan is the OPERATOR-SCHEDULED PREREQUISITE (deferred item 27, decided 2026-08-27). No `addStep`/`addPastStep` seam work in any later plan of this phase may begin until it is green."
    - "D-04's third item is what makes the phase safe: plan 33-06 feeds `jump` a computed difference of two `morphedWave` values (D-05), which is the first caller that can put a non-finite value on that path."
    - "The four frozen shared headers are NOT touched: `Waveshape.hpp`, `DriftEngine.hpp`, `RackCompat.hpp`, `MathConst.hpp` stay byte-identical and `FROZEN.sha256` needs no bump."
---

<objective>
Close `32-REVIEW.md` CR-01 and CR-02 in `src/dsp/MorphBlep.hpp`, plus the third defect found during the Phase 33 discussion — `addStep` accepts any `jump` — before this phase adds the second `MorphBlep` call site.

Purpose: `blep.step` has exactly ONE call site today (`VcoCore.hpp:645`) whose arguments are conditioned two lines above it, which is the only reason CR-01 is latent rather than live. `(int)NaN` measures a benign `0` on this arm64 host and `INT_MIN` under x86 `cvttss2si` on the MinGW/Linux builds that actually ship — the invisible-on-Apple-clang class that got v2.0.0 rejected from the VCV Library. The `jump` hole is the identical permanent-poison mode plan 32-05 measured for a `+infinity` `dt`, and D-05 of this phase is what makes it reachable.

Output: three guards in `src/dsp/MorphBlep.hpp`, each carrying the RED that found it in its comment; three permanent regression cases in `tests/test_morph_blep.cpp`; a one-shot ASan report recorded in the SUMMARY and in the header banner, with no sanitizer target added anywhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-RESEARCH.md
@.planning/phases/33-hard-sync/33-PATTERNS.md
@.planning/phases/33-hard-sync/33-VALIDATION.md
@src/dsp/MorphBlep.hpp
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`.

New symbols:
- `forge::VcoInputs::syncVolts` (float), `forge::VcoInputs::syncConnected` (bool)
- `forge::VcoCore::syncTrig` (`forge::SchmittTrigger`), `forge::VcoCore::prevSyncVolts` (float)
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection` (recording-only floats; `::syncFired` already exists and is POPULATED, not added)
- `forge::MorphBlep::addPastStep(float, float)` — CONDITIONAL on the plan 33-05 measurement selecting candidate (b)
- `AnalogVCO::SYNC_INPUT` enum member
- `make audition` target, `AUDITION_BIN` Makefile variable, output directory `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`
- New `TEST_CASE`s: `"morph blep: (D-04 / CR-01) ..."`, `"morph blep: (D-04 / CR-02) ..."`, `"morph blep: (D-04 third item) ..."`, `"morph blep: (D-06) addPastStep is the addStep identity"`, `"vco sync: (SYNC-01 / D-01 / D-03) ..."`, `"vco sync: (SYNC-01 / D-09) ..."`, `"vco sync: (D-12) ..."`, `"vco sync: (CORE-03) ..."`, `"vco sync: (SC-3 / D-10) ..."`, `"vco spectrum: (D-06 / D-11) ..."`, `"vco spectrum: (SYNC-02 / D-01) snap ..."`, `"vco pitch: (PITCH-04 / D-12) ..."`

THIS PLAN produces: the three guards in `src/dsp/MorphBlep.hpp` and the three `(D-04 ...)` `TEST_CASE`s in `tests/test_morph_blep.cpp`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — reproduce all three defects before touching the header (D-04)</name>
  <files>tests/test_morph_blep.cpp</files>
  <behavior>
    - CR-01: `MorphBlep::step` called with `morph = -0.25f` computes `segment = -1` and writes `W[-1]`, one float BEFORE the array. Reproduced under AddressSanitizer as a stack-buffer-underflow at the `W[segment] += 1.f - frac;` line.
    - CR-01, the shipping-toolchain half: with `morph` a quiet not-a-number, `(int)(morph * 4.f)` is undefined; the case records the value this host produces so the divergence is stated rather than assumed.
    - CR-02: driven over a 200-point population of `character` values in which the hostile members are non-finite, `MorphBlep::step` returns a non-finite correction on a MEASURED, RECORDED count of those points (the register's figure is 16 of 200 — reproduce and record the count this header produces, do not copy the figure).
    - D-04 third item: `addStep(0.5f, jump)` with `jump` in {+infinity, -infinity, quiet NaN} leaves `pending` non-finite; the NEXT `step()` call with a perfectly legitimate `dt`, `morph` and `character` returns a non-finite value, and so does every call after it — the instance is poisoned FOREVER after the hostile value has been withdrawn.
  </behavior>
  <read_first>
    - src/dsp/MorphBlep.hpp in full — especially `morphBlepCharFactor` (:177-186), the sign-convention banner (:188-212), `addStep` and its entry gate (:238-262), the drain-then-`dt`-guard preamble (:273-306), the `segment` clamp and the `W[5]` write (:308-334), and the nine-site union
    - tests/test_morph_blep.cpp lines 984-1008 — the pinned seam equivalence (case five parts A and B) that must not move
    - tests/test_morph_blep.cpp lines 1036-1055 — subcase B, the entry-gate rejection case; this is the EXACT template for the `jump` population case
    - tests/test_morph_blep.cpp lines 1057-1076 — subcase C, the poisoned-instance `dt` narrative; this is the model for the `jump` RED
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md lines 616-659 — item 27, both defects as originally written with their reachability evidence and the minimum fix
    - .planning/phases/33-hard-sync/33-PATTERNS.md § `src/dsp/MorphBlep.hpp` and § `tests/test_morph_blep.cpp`
  </read_first>
  <action>
Add three `TEST_CASE`s to `tests/test_morph_blep.cpp`, following this file's own naming convention of putting the discharged decision in the title:

1. `"morph blep: (D-04 / CR-01) a negative or not-a-number morph cannot index W outside the float[5]"`
2. `"morph blep: (D-04 / CR-02) a non-finite character produces no non-finite correction at the literal-zero-width sites"`
3. `"morph blep: (D-04 third item) a non-finite jump is rejected by addStep and the instance recovers"`

Write each case against the CURRENT header and OBSERVE IT RED before any header edit. Record the exact failing assertion counts and the measured values in the plan SUMMARY.

Case 1 — CR-01. Drive `forge::MorphBlep::step` with a `forge::Waveshape` constructed the same way the existing cases construct it, a legitimate `dt` of `1.0/44100.0`, a legitimate `character` of `0.5f`, and `morph` drawn from `{-0.25f, -1.f, -1e30f, quiet NaN}`. Assert the returned correction is finite for every point, and assert the correction equals what the same call returns for `morph = 0.f` — because a negative `morph` has no meaning and the only defensible behavior is the clamped-at-zero one. Add a `CAPTURE` for the value of `(int)(morph * 4.f)` this host produces for the NaN point and write it into the case comment together with the sentence that x86 `cvttss2si` yields `INT_MIN` for the same expression, so a reader sees why an arm64-green run proves nothing.

Case 1's ASan half is a SCOPED ONE-SHOT PROBE and is NOT part of this TU. Write a standalone throwaway `main()` into the session scratchpad directory (NOT into the repository), which constructs a `forge::MorphBlep` and a `forge::Waveshape` and calls `step` once with `morph = -0.25f`. Compile it once with `clang++ -std=c++17 -O0 -g -fsanitize=address -fno-omit-frame-pointer -Isrc` and run it once. Copy the ASan report's first six lines — the `stack-buffer-underflow` line, the WRITE-of-size line and the top frame naming `MorphBlep::step` with its `MorphBlep.hpp` line number — into the plan SUMMARY verbatim. Do NOT add an ASan target to the `Makefile`, to `GUARD_SCRIPTS`, to `TEST_CXXFLAGS` or to any CI workflow: register item 12 forbids a permanent repo-wide sanitizer gate because the shipped Analog LFO carries shared latent undefined behavior that is deliberately unowned.

Case 2 — CR-02. Build a 200-point population over `character` in which most points are legitimate values swept across `[0,1]` and a stated subset are non-finite (quiet NaN, +infinity, -infinity), with `morph` held at the square centre `0.75f` where `W[3]` is live and at the pulse centre `1.0f`. Count how many of the 200 points return a non-finite correction, `CAPTURE` the count, and `CHECK` it is zero. Record the observed RED count in the SUMMARY. In the case comment state the mechanism from item 27: the hard-square term multiplies a weight by `(1.f - c)`, so a zero weight times a non-finite `c` is still non-finite, and three sites carry a width that is a literal zero, so `morphBlepCharFactor`'s own trap returns full authority and never sees them.

Case 3 — the `jump` guard, in two subcases mirroring subcases B and C of case five.
Subcase "rejection": for each `jump` in {+infinity, -infinity, quiet NaN}, construct a FRESH `forge::MorphBlep`, call `addStep(0.5f, jump)`, and `CHECK(b.inject == 0.0f)` and `CHECK(b.pending == 0.0f)` with exact float equality, never a tolerance — the shape used by the existing `xAhead` rejection subcase.
Subcase "recovery": construct one `forge::MorphBlep`, call `addStep(0.5f, positive infinity)` once, then call `step` twenty times with an entirely legitimate `dt`, `morph` and `character` — the hostile value WITHDRAWN. `CHECK` every one of the twenty returns is finite, and `CHECK` the instance's `pending` is finite after each. The point of the withdrawal phase is that this defect is invisible during the hostile sample and only appears after recovery; say so in the comment.
  </action>
  <verify>
    <automated>make build-test/test && ./build-test/test -tc="morph blep: (D-04*" -s 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The selector `-tc="morph blep: (D-04*"` matches exactly 3 test cases and reports a NON-ZERO failure count — this task's success is an observed RED, and the SUMMARY records the exact `assertions:` line
    - The selector's matched-case count is confirmed non-zero before any conclusion is drawn from it: `./build-test/test -tc="morph blep: (D-04*" -s 2>&1 | grep -E 'test cases:' | grep -vq ' 0 ' ` exits 0 (a selector matching zero cases also exits 0 and prints SUCCESS, so exit status alone cannot distinguish a gate that failed from one that never ran)
    - The ASan report is reproduced once and its `stack-buffer-underflow` line plus the `MorphBlep::step` frame with its `src/dsp/MorphBlep.hpp` line number are quoted verbatim in the SUMMARY
    - `git status --porcelain` shows no new or modified file under `Makefile`, `.github/`, or `tests/check_*.sh` — the ASan probe left no repository artifact
    - `git diff --name-only` lists only `tests/test_morph_blep.cpp`
    - `git diff --name-only` does not list `src/AnalogLFO.cpp`, and does not list any of `src/dsp/Waveshape.hpp`, `src/dsp/DriftEngine.hpp`, `src/dsp/RackCompat.hpp`, `src/dsp/MathConst.hpp`
    - The measured CR-02 non-finite count over the 200-point population is recorded in the SUMMARY as a number produced by this run, not copied from the register
  </acceptance_criteria>
  <done>All three defects are reproduced against the unmodified header, with the CR-01 out-of-bounds write additionally evidenced by a one-shot ASan report that leaves no repository artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — the three guards, each carrying the RED that found it (D-04)</name>
  <files>src/dsp/MorphBlep.hpp</files>
  <behavior>
    - `MorphBlep::step` conditions `morph` and `character` into `[0,1]` with the negated comparison FIRST, immediately after the `dt` guard and before any arithmetic that consumes them
    - `segment` is clamped from BELOW as well as from above, so `W[segment]` is in bounds even if the conditioning above it is later moved
    - `addStep` rejects a non-finite `jump` at the SAME early return as its existing `xAhead` gate, before `inject` or `pending` is touched
    - every finite input produces bit-identical output to the pre-fix header — no existing measurement in the suite moves
  </behavior>
  <read_first>
    - src/dsp/MorphBlep.hpp lines 273-334 — the drain-then-guard preamble, the `dt` guard's comment block (the exact depth and shape every new guard must match), the `segment` clamp and the `W[5]` write
    - src/dsp/MorphBlep.hpp lines 238-262 — `addStep`'s banner and its `xAhead` entry gate
    - src/dsp/VcoCore.hpp lines 544-602 — the worked negated-comparison example for `morph`/`character`, and the 53-line comment block that is the documentation depth this project holds a two-line guard to
    - src/dsp/RackCompat.hpp line 97 — `forge::clamp`, the NaN-transparent helper that must not be used for any of these three guards
    - .planning/phases/33-hard-sync/33-PATTERNS.md § "Negated-comparison guards for hostile numeric input" and § "Comment density is a project convention, not decoration"
  </read_first>
  <action>
Land three guards in `src/dsp/MorphBlep.hpp`. All three are written with the negated comparison FIRST, because that ordering is what makes a not-a-number land on the fallback branch. Do not introduce a comparison-ladder helper on any of them; the header's existing `dt` guard already records why, and the reason is identical here.

Guard A — the CR-02 entry conditioning. Immediately AFTER the existing `dt` guard's early return and BEFORE the weight algebra begins, shadow the two parameters with conditioned locals using exactly the `VcoCore.hpp:597-602` pair shape: negated lower comparison assigning the floor, then a plain upper comparison assigning the ceiling, for `morph` and then for `character`. Every expression below must consume the conditioned values, including the `character < 0.001f` early-return threshold, `morph * 4.f`, and the bleed ring. Verify by reading that no later line still reads the raw parameters.
Record in the comment: the DECISION to condition rather than early-return, and its reason — conditioning makes the header's behavior for a hostile `morph` identical to what the one shipped caller already produces, so bit-identity is preserved and no recorded figure in the suite can move, whereas an early return would change the correction on a sample the caller has already conditioned. Record the measured RED from Task 1 (the observed non-finite count over the 200-point population) and name `tests/test_morph_blep.cpp`'s `(D-04 / CR-02)` case as the test that found it.

Guard B — the CR-01 lower clamp. Add a lower bound on `segment` beside the existing upper one, so the index is bounded on both sides before `W[segment]` and `W[segment + 1]` are written.
Record in the comment: that this is DEFENCE IN DEPTH beneath Guard A rather than a duplicate of it — Guard A makes a negative `segment` unreachable from the parameters, and this clamp makes the array write safe even if a later editor moves, narrows or removes the conditioning; that the defect was an ASan-reproduced stack-buffer-underflow; and the toolchain half — the float-to-int cast of a not-a-number measures a benign zero on the arm64 development host and yields `INT_MIN` under the x86 instruction the MinGW and Linux shipping builds use, which is the invisible-on-Apple-clang class that got version 2.0.0 rejected from the VCV Library.

Guard C — the `jump` finiteness gate in `addStep`. Extend the existing single early return so a non-finite `jump` returns before `inject` or `pending` is touched. Use a closed-form negated test that needs no new include — the header includes no `<cmath>` today and must not acquire one for this. State the chosen form's coverage explicitly in the comment: which of {quiet NaN, +infinity, -infinity} each clause catches.
Record in the comment: that this is the identical permanent-poison mode plan 32-05 measured for a `+infinity` `dt` — one bad sample and every later sample returns a not-a-number even after the input recovers; that Phase 33's D-05 is what makes the path live, because the sync seam feeds `jump` a computed difference of two `morphedWave` values rather than an analytic constant; and that `tests/test_morph_blep.cpp`'s `(D-04 third item)` case is the test that found it, with its withdrawal phase named as the half that distinguishes "bad during" from "bad forever".

Also update `addStep`'s banner: its documented contract currently says `jump` is already scaled by whatever weights the caller owns, and says nothing about finiteness. Add the finiteness clause to the contract paragraph so the advertised contract and the enforced contract agree — that gap is the substance of the review finding.

Do NOT edit any of `src/dsp/Waveshape.hpp`, `src/dsp/DriftEngine.hpp`, `src/dsp/RackCompat.hpp`, `src/dsp/MathConst.hpp`. If any part of this task appears to require one, STOP: that is a guardrail event and must be surfaced to the operator with impact, remediation options and a recommendation before proceeding.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; the three `(D-04 ...)` cases that were RED in Task 1 are now green
    - `make strict` exits 0 (C++11, `-pedantic-errors`) and `make guards` exits 0
    - `bash tests/check_frozen.sh` exits 0 and reports 15 entries unchanged; `git diff --stat src/dsp/Waveshape.hpp src/dsp/DriftEngine.hpp src/dsp/RackCompat.hpp src/dsp/MathConst.hpp` produces no output
    - `git diff --name-only` does not list `src/AnalogLFO.cpp`
    - The six shipped-LFO goldens replay byte-identical: `./build-test/test -tc="*golden*" -s 2>&1 | tail -3` reports 0 failures with a non-zero matched case count
    - The header acquired no new `#include`: `git diff -U0 src/dsp/MorphBlep.hpp | grep -c '^+#include'` outputs `0`
    - The negated idiom is present at all three sites and no NaN-transparent helper was introduced: `grep -vE '^[[:space:]]*(//|\*)' src/dsp/MorphBlep.hpp | grep -c 'clamp('` equals its value on the pre-task commit (record both numbers in the SUMMARY; a bare zero-count is meaningless here because this header deliberately names the helper it forbids)
    - Bit-identity for legitimate inputs is MEASURED, not asserted: drive a 4096-sample block through `forge::VcoCore` at 44.1 kHz at morph 0.75 / character 0.5 against the pre-task binary and record 0 differing samples by direct float equality in the SUMMARY
  </acceptance_criteria>
  <done>All three defects are closed with negated-comparison guards, each comment carrying the measured RED that found it, and every finite-input measurement in the suite is unmoved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove each guard bites on its own, and re-pin the seam equivalence</name>
  <files>tests/test_morph_blep.cpp</files>
  <behavior>
    - reverting ONLY Guard A produces a red that is DIFFERENT from reverting only Guard B, which is different again from reverting only Guard C — so green after a three-part fix is evidence that each part is load-bearing rather than that one of them is
    - the pinned seam equivalence is unmoved: a driven morph site at `s = 0.5` and `addStep(0.5, 2)` still produce the same `+0.250000 / -0.250000` split, and events still compose by summation
    - a legitimate `jump` of any finite magnitude, including one far outside the musical range, still reaches `inject`/`pending` — the new gate rejects non-finiteness, not largeness
  </behavior>
  <read_first>
    - tests/test_morph_blep.cpp lines 956-1076 — case five in full: the seam-equivalence subcases A and B, the entry-gate rejection subcase, and the poisoned-instance subcase
    - src/dsp/MorphBlep.hpp as landed by Task 2 — the three guards and their comments
    - .planning/STATE.md § Accumulated Context — the Phase 30 lesson that green after a multi-part fix is not evidence that either part bites, and that each guard was proved load-bearing by a revert-one-only probe producing a DIFFERENT red
  </read_first>
  <action>
Part A — the three revert-one-only probes. For each guard in turn, temporarily revert ONLY that guard in the working tree, run `make test`, and record the resulting failure signature: which `TEST_CASE`s fail, how many assertions, and the first failing expression. Restore the guard immediately and confirm the suite is green again before moving to the next. The three signatures must be DIFFERENT from one another — if two of them produce the same red, one of the guards is not load-bearing and that finding must be reported rather than resolved by re-typing an assertion. Write all three signatures into the plan SUMMARY, and write a one-line reference to this probe into each guard's comment block naming which case goes red when that specific guard alone is removed.

Part B — the seam-equivalence re-pin. Do not edit case five parts A and B. Instead confirm by running them that the `+0.250000 / -0.250000` split and the summation-composition assertions are unchanged, and record in the SUMMARY that they were re-run rather than re-derived. If any of them moves, STOP and report: the guards were supposed to be bit-identical for finite inputs and a moved split falsifies that.

Part C — the largeness/non-finiteness distinction. Add a subcase to the `(D-04 third item)` case asserting that a finite but very large `jump` — use a magnitude several orders above anything the sync seam can produce, and a matching negative — is ACCEPTED by `addStep` and deposits a non-zero, finite value into both `inject` and `pending`. Comment that this is what stops a later editor "tightening" the gate into a magnitude bound: the guard's job is to keep a not-a-number and an infinity out of the accumulator, and a bound on magnitude would silently discard legitimate corrections at high character.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - Three DIFFERENT revert-one-only failure signatures are recorded in the SUMMARY, each naming the failing `TEST_CASE` and its assertion count; no two signatures are identical
    - `./build-test/test -tc="morph blep: (D-04*" -s 2>&1 | tail -4` reports 3 test cases, 3 passed, 0 failed
    - `./build-test/test -tc="*seam*" -s 2>&1 | tail -4` reports a non-zero matched case count and 0 failures
    - `make strict` exits 0; `make guards` exits 0; `bash tests/check_canary.sh` exits 0
    - `git diff --name-only` for this plan lists exactly `src/dsp/MorphBlep.hpp` and `tests/test_morph_blep.cpp`, and does not list `src/AnalogLFO.cpp`
    - The full suite's case and assertion totals are recorded in the SUMMARY as a before/after pair so later plans can account for their own deltas
  </acceptance_criteria>
  <done>Each of the three guards is proved individually load-bearing by its own distinct red, the pinned seam equivalence is confirmed unmoved, and the phase's scheduled prerequisite is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `forge::VcoInputs` → `forge::VcoCore::step` | Every field is an arbitrary Rack cable voltage or host timing value and is treated as hostile |
| `forge::VcoCore` → `forge::MorphBlep` | The header's banner claims caller-independence; this plan makes the enforced contract match the advertised one |
| Apple-clang dev host → x86 MinGW/GCC shipping toolchain | Undefined behavior that is benign on arm64 and destructive on x86 crosses here silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering / EoP | `MorphBlep::step` `W[segment]` write, `src/dsp/MorphBlep.hpp:319-332` | high | mitigate | Task 2 Guard B clamps `segment` from below as well as above; Task 2 Guard A makes a negative index unreachable from the parameters; Task 1 reproduces the out-of-bounds write under a one-shot ASan probe; Task 3 proves each guard bites alone |
| T-33-02 | Tampering | float-to-int cast of a non-finite `morph` in `MorphBlep::step` | high | mitigate | Task 2 Guard A conditions `morph`/`character` with the negated pair before any cast, mirroring `VcoCore.hpp:597-602` |
| T-33-03 | Denial of Service | permanent poisoning of `MorphBlep::pending` via a non-finite `jump` at `addStep` | high | mitigate | Task 2 Guard C rejects a non-finite `jump` at the entry gate before per-instance state is touched; Task 1's withdrawal-phase trace proves the poisoning is permanent, not transient |
| T-33-08 | Tampering | toolchain-divergent undefined behavior invisible on the development host | high | mitigate | `make strict` (C++11, `-pedantic-errors`) on every task; the CI MinGW **link** leg observed green on the exact commit at the phase gate (plan 33-11). `make strict` is `-fsyntax-only` and never links, so it cannot substitute |
| T-33-10 | Tampering | adopting a permanent repo-wide sanitizer gate that the shipped LFO's unowned latent UB would immediately trip | medium | mitigate | The ASan reproduction is a scoped one-shot probe compiled and run outside the repository; no Makefile target, no `GUARD_SCRIPTS` entry, no CI step is added (register item 12) |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source. `33-RESEARCH.md` § Package Legitimacy Audit records the audit table as not applicable with that finding stated, not omitted |
</threat_model>

<verification>
- `make test && make strict && make guards` all exit 0 at the end of the plan
- The three `(D-04 ...)` cases were observed RED against the unmodified header before any guard landed, with the counts recorded
- Each guard has its own distinct revert-one-only red recorded
- `tests/check_frozen.sh` reports all 15 frozen paths unchanged and `FROZEN.sha256` byte-identical
- The six shipped-LFO goldens replay byte-identical
- No sanitizer target exists in `Makefile`, `GUARD_SCRIPTS` or `.github/workflows/`
</verification>

<success_criteria>
The operator-scheduled prerequisite is closed: `src/dsp/MorphBlep.hpp` defends `morph`, `character` and `jump` as well as it already defends `dt`, each guard carrying its own measured RED and its own proof of biting, and no later plan in this phase is blocked from adding the second call site.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-01-SUMMARY.md` when done
</output>

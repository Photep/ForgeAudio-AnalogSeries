---
phase: 33-hard-sync
plan: 06
type: execute
wave: 4
depends_on: ["33-05", "33-04", "33-03"]
files_modified:
  - src/dsp/MorphBlep.hpp
  - src/dsp/VcoCore.hpp
  - tests/test_morph_blep.cpp
autonomous: true
nyquist_compliant: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "SYNC-02 / D-06: the seam call landed in `forge::VcoCore` is the one plan 33-05 MEASURED and DECIDED, implemented exactly as its recorded decision states — never the one a reader would reach for by default."
    - "D-06: if the winner is the past-edge residual, it lands as a NAMED additive entry point on `forge::MorphBlep` rather than as a pre-scaled argument to the existing seam. Both forms are numerically identical; the named form is chosen for legibility, because a pre-scaled argument at the call site is opaque and invites a later 'simplification' straight back into the candidate the measurement rejected."
    - "D-06: the `[0,1]` contract of the existing seam is EXTENDED additively, never reinterpreted. The existing seam's behavior, its entry gate and its pinned residual split are untouched."
    - "D-06: the new entry point's own non-circular check is the numerical identity — the named past-edge call and the pre-scaled call to the existing seam produce identical accumulator pairs. That identity is what makes the header change safe rather than merely plausible."
    - "SYNC-02 / D-08: BLEP only on the sync path — no slope-correction kernel. The step is what buzzes and what clicks; the slope kernel is the documented FIRST escalation if a sync alias threshold proves unreachable, ahead of any kernel-order change, which is an operator decision with an impact assessment and never a silent choice."
    - "D-07: the seam call sits inside the sync block, between the wrap and the phase snapshot, so the single band-limiter call still sees the post-reset phase and is still called exactly once per sample."
    - "D-04: the seam accepts the computed jump only because plan 33-01 guarded it. This is the caller D-04's third item was written for."
    - "The recording-only correction telemetry is populated here, and its stated shortcut — that the withheld audition leg is the full leg minus the correction, exactly — is RE-STATED against the leg actually selected rather than inherited from the recommendation."
  artifacts:
    - src/dsp/MorphBlep.hpp
    - src/dsp/VcoCore.hpp
    - tests/test_morph_blep.cpp
  key_links:
    - "This is the SECOND `MorphBlep` call site. It is only safe because plan 33-01 closed the three guards first — that ordering was an operator decision taken on 2026-08-27 and is the reason this plan is late in the phase rather than early."
    - "The pinned seam equivalence in `tests/test_morph_blep.cpp` case five parts A and B must remain unmoved by this plan; if it moves, the additive extension was not additive."
    - "Plan 33-07 must RE-ANCHOR the 33-05 probe's bit-exactness gate from the no-correction leg to the leg landed here, or that gate silently becomes a comparison of two different things."
---

<objective>
Land the placement convention that plan 33-05 measured, in the shipped core, with the header extension it requires and the identity assertion that makes the extension checkable.

Purpose: `MorphBlep`'s seam is future-facing and a Schmitt-detected edge is always in the past. The measurement decides which recoverable half to take; this plan implements exactly that and nothing else. The prediction going in is that the naive reading — calling the existing seam with the sub-sample fraction as a forward position — is worse than applying no correction at all, which is precisely why the decision was routed through an instrument.

Output: the seam call in `src/dsp/VcoCore.hpp`; if the measurement selected the past-edge residual, a named additive entry point in `src/dsp/MorphBlep.hpp` with the forfeited-half paragraph its neighbours' documentation depth demands; and the numerical identity pinned in `tests/test_morph_blep.cpp`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-RESEARCH.md
@.planning/phases/33-hard-sync/33-PATTERNS.md
@.planning/phases/33-hard-sync/33-05-SUMMARY.md
@.planning/phases/33-hard-sync/33-01-SUMMARY.md
@.planning/phases/33-hard-sync/33-02-SUMMARY.md
@src/dsp/MorphBlep.hpp
@src/dsp/VcoCore.hpp
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`.

New symbols:
- `forge::VcoInputs::syncVolts`, `forge::VcoInputs::syncConnected`
- `forge::VcoCore::syncTrig`, `forge::VcoCore::prevSyncVolts`
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection`
- `forge::MorphBlep::addPastStep(float, float)` — landed by THIS PLAN if and only if the 33-05 measurement selected the past-edge candidate
- `AnalogVCO::SYNC_INPUT`
- `make audition`, `AUDITION_BIN`, `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: `forge::MorphBlep::addPastStep` (conditional), the seam call inside `forge::VcoCore::step`, and the `"morph blep: (D-06) ..."` identity `TEST_CASE`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The additive past-edge entry point, with the forfeited half stated so it is not "fixed" later (D-06)</name>
  <files>src/dsp/MorphBlep.hpp</files>
  <behavior>
    - the new entry point deposits exactly the after-edge half of the two-point residual into the current-sample accumulator and deposits NOTHING forward, because the residual's support is exhausted at this sample
    - its entry gate is the same negated shape as the existing seam's, rejecting a position outside its documented range and a non-finite magnitude before per-instance state is touched
    - the existing seam's behavior, its gate and its pinned residual split are byte-unchanged
    - if plan 33-05 selected a candidate other than the past-edge residual, this task lands NO header change at all and records that outcome
  </behavior>
  <read_first>
    - .planning/phases/33-hard-sync/33-05-SUMMARY.md — the recorded decision and the three conditions' outcomes. THIS IS THE INPUT TO THIS TASK. If it declares no winner, STOP and report rather than choosing
    - src/dsp/MorphBlep.hpp lines 188-212 — the sign convention, the residual's two branches and its symmetric support, and the canonical-form reconciliation
    - src/dsp/MorphBlep.hpp lines 213-262 — the accumulator members, the delay-buffer rejection paragraph, and the existing seam with its 19 lines of banner for a 6-line function
    - src/dsp/MorphBlep.hpp as landed by plan 33-01 — the three guards, so the new entry point's gate matches them rather than inventing a fourth shape
    - .planning/phases/33-hard-sync/33-RESEARCH.md § "The additive past-edge entry point" — the body and the asymmetry paragraph it must carry
  </read_first>
  <action>
FIRST: read plan 33-05's recorded decision and restate it at the top of the plan SUMMARY in one sentence. Everything below is conditional on it.

IF the measurement selected the PAST-EDGE residual:
Add a named additive entry point to `forge::MorphBlep`, placed directly beneath the existing seam and mirroring its gate exactly. It takes the edge's position BEHIND this sample and the signed jump, deposits the after-edge half of the residual into the current-sample accumulator, and deliberately leaves the forward-owed accumulator untouched.
Choose the NAMED entry point over the numerically identical trick of calling the existing seam with a pre-scaled magnitude, and record the reason in the SUMMARY and in the banner: both forms produce the same two accumulator values, but a pre-scaled argument at the call site is opaque and invites a later editor to "simplify" it straight back into the candidate the measurement rejected — which the measurement says is worse than applying no correction at all.
The banner must state THE ASYMMETRY so it is not "fixed" later, at the documentation depth this header holds its six-line functions to:
  - the existing seam's edge is AHEAD, so both halves of the two-sample residual are still deliverable
  - this entry point's edge is BEHIND — it happened between the previous sample and this one — and the residual is nonzero on a symmetric interval around the edge, so the pre-edge half belonged on a sample that HAS ALREADY BEEN EMITTED
  - that half is FORFEITED, deliberately and permanently. Recovering it needs a one-sample output delay buffer, which Phase 32 rejected on two grounds: a latency the module would have to declare, and a VCO that silently delays by a sample desyncing against every other oscillator in the patch
  - the MEASURED cost of the forfeit, from plan 33-05's own table, quoted per ratio region rather than as one number
  - why a minimum-phase kernel would not pay this cost, and that it is forbidden by name for this milestone — recorded as the explanation for why the constraint bites HERE specifically, not as a proposal
  - that the `[0,1]` contract of the existing seam is EXTENDED additively and not reinterpreted; the existing seam is untouched
  - the numerical identity with the pre-scaled call, written out in prose so a reader can check it without running anything, and a pointer to the test case that pins it
Give it the same gate shape as the guards plan 33-01 landed: negated comparison first, rejecting an out-of-range position and a non-finite magnitude before either accumulator is touched. Do not invent a fourth guard idiom and do not add an include.

IF the measurement selected a different candidate:
Land NO header change. Record in the SUMMARY which candidate won, that no header change was required, and that this vindicates the register's standing claim that the hard-sync seam needs no header change — noting the claim is recovered as TRUE only under the specific winning candidate and must not be cited as licence for any other.

In either branch: do NOT edit any of the four frozen shared headers. If any part of this task appears to require one, STOP — that is a guardrail event and must be surfaced to the operator with impact, remediation options and a recommendation.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0
    - The SUMMARY opens with plan 33-05's decision restated in one sentence, and every subsequent claim in the SUMMARY is consistent with it
    - The existing seam is byte-unchanged: `git diff -U0 src/dsp/MorphBlep.hpp` shows no modification inside the existing seam's body or gate — quote the diff hunk headers in the SUMMARY
    - The header acquired no new include: `git diff -U0 src/dsp/MorphBlep.hpp | grep -c '^+#include'` outputs `0`
    - The new entry point, if landed, has a banner of at least fifteen comment lines carrying all seven items listed in the action — enumerate which line range covers which item in the SUMMARY
    - The pinned seam equivalence is unmoved: `./build-test/test -tc="*seam*" -s 2>&1 | tail -4` reports a non-zero matched case count and 0 failures, and the recorded residual split values are reproduced in the SUMMARY
    - `bash tests/check_frozen.sh` exits 0 with 15 entries unchanged; `git diff --stat` for the four frozen headers produces no output
    - `git diff --name-only` does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The header carries exactly the extension the measurement requires — or none, if none was required — with the forfeited half stated at the depth that stops a later editor from "restoring" it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The seam call in the core, and D-08 recorded as a decision rather than an omission (SYNC-02 / D-06 / D-08)</name>
  <files>src/dsp/VcoCore.hpp</files>
  <behavior>
    - the sync block deposits the measured correction through the seam selected by plan 33-05, on sync samples only
    - the recording-only correction telemetry carries the deposited value in the pre-multiply domain, so the withheld audition leg is reconstructible from the same pass
    - the single band-limiter call is still the single band-limiter call, still after the sync block, still seeing the post-reset phase
    - no slope-correction kernel is added on the sync path
  </behavior>
  <read_first>
    - .planning/phases/33-hard-sync/33-05-SUMMARY.md — the decision, expressed as the exact seam call
    - src/dsp/VcoCore.hpp as landed by plan 33-02 — the sync block in full, including the boundary paragraph that says the seam is deliberately absent and names this plan as the one that lands it
    - src/dsp/MorphBlep.hpp as landed by Task 1 — the entry point being called and its gate
    - .planning/phases/33-hard-sync/33-CONTEXT.md D-08 — BLEP only, the slope kernel as the documented first escalation, and both reasons it is not taken now
    - .planning/research/STACK.md lines 118-130 — the Q3 answer, whose jump expression is sign-inverted relative to the seam's convention; read it to confirm the landed sign is the seam's and not the research document's
  </read_first>
  <action>
Replace plan 33-02's boundary paragraph in the sync block with the seam call the measurement selected, and with the record of why it is that call.

The comment must carry, at the depth this file documents its guards:
  - WHICH candidate was selected, and the three decision conditions' outcomes with their figures, so the choice is auditable from the source without opening the planning tree
  - the losing candidates named, with the measured margin against each, including the specific finding that the naive reading — the existing seam called with the sub-sample fraction as a forward position — was measured against the alternative of applying NO correction at all, and what that comparison said
  - the mechanism for that result, in one paragraph: under this phase's ordering the naive output has already stepped between the previous sample and this one, so this sample is post-edge, and a correction placed as if the edge were ahead pushes it FURTHER past the new value instead of pulling it back toward the band-limited midpoint. This is the register's already-falsified over-correction premise in a new costume — a step-shaped correction on the wrong side of a step is new broadband energy, not a filter
  - the SIGN, with its reconciliation: the seam's documented convention is value-after minus value-before, and the project's own stack research writes the same quantity negated. Name the mutation probe from plan 33-05 that measured what transcribing the research expression would cost
  - D-08, as a DECISION with both of its reasons: the step is what buzzes and what clicks, the slope kernel is optional for this milestone by the project's own stack research, and it is the documented FIRST escalation if a sync alias threshold proves unreachable — ahead of any kernel-order change, which is an operator decision with an impact assessment and never a silent implementation choice. Give both reasons it is not taken now: the seam carries a value jump only, so a slope seam would be a second header change; and the slope difference across the reset has no closed form, so it would need a numerical derivative of the crossfade at two phases — new transcendental cost and a fresh cross-library divergence risk on a path this milestone has kept free of one

Populate the recording-only correction telemetry with the value deposited into the current-sample accumulator this sample, in the pre-multiply domain. RE-STATE its reconstruction shortcut against the leg ACTUALLY SELECTED rather than inheriting the recommendation's wording: if the selected leg deposits nothing forward, the correction is purely additive per sample and the withheld leg is the full leg minus the recorded value scaled by the output multiplier, exactly; if the selected leg DOES deposit forward, say so and say that plan 33-10's renderer must reconstruct from both halves instead. Whichever is true, write the true one.

Do not move the phase snapshot, do not add a second band-limiter call, and do not touch the conditioning block plan 33-02 moved. Keep the `struct` declaration line and the step signature line each on one line with their opening brace, and do not quote the step signature in any comment on a line that also contains an opening brace.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0, including `check_canary.sh` perturbing `src/dsp/VcoCore.hpp` without error
    - The band-limiter is still called exactly once per sample: `grep -vE '^[[:space:]]*(//|\*)' src/dsp/VcoCore.hpp | grep -c 'blep.step'` outputs `1`
    - The seam is called exactly once, inside the sync block: the comment-stripped occurrence count of the selected entry point in `src/dsp/VcoCore.hpp` is `1`, and its line number is between the wrap line and the phase-snapshot line — record all three line numbers
    - No slope-correction kernel was added on the sync path: the comment-stripped diff for this task adds no call to any slope-correction entry point — enumerate every function called from the sync block in the SUMMARY
    - The sign is the seam's convention, confirmed by quoting the landed expression in the SUMMARY beside the seam's documented convention
    - The output changes only on and around sync samples: drive a block with the jack unpatched and confirm 0 differing samples against the pre-task binary by direct float equality; drive the same block with sync active and record the number of differing samples and the largest difference
    - `bash tests/check_frozen.sh` exits 0 with 15 entries unchanged; `git diff --name-only` does not list `src/AnalogLFO.cpp`
    - The six shipped-LFO goldens replay byte-identical
  </acceptance_criteria>
  <done>The measured placement is landed in the shipped core, its full decision record is in the source, and the slope-kernel exclusion is written as a decision with both of its reasons rather than as a silent omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the header extension's own non-circular identity (D-06)</name>
  <files>tests/test_morph_blep.cpp</files>
  <behavior>
    - the named past-edge call and the numerically identical pre-scaled call to the existing seam produce IDENTICAL accumulator pairs, for a spread of positions and magnitudes
    - the new entry point rejects an out-of-range position and a non-finite magnitude without touching per-instance state, using the same exact-equality assertion shape the existing rejection case uses
    - a legitimate call deposits a non-zero value into the current-sample accumulator and exactly zero forward
    - if no header change was landed, this task instead pins the pre-scaled call's identity against the analytic residual, so the claim is still checkable
  </behavior>
  <read_first>
    - tests/test_morph_blep.cpp lines 984-1008 — the pinned seam equivalence, which is the exact shape this identity assertion must take
    - tests/test_morph_blep.cpp lines 1036-1055 — the entry-gate rejection case with its exact-equality accumulator assertions
    - tests/test_morph_blep.cpp as landed by plan 33-01 — the three `(D-04 ...)` cases, so the new case's naming and structure match
    - src/dsp/MorphBlep.hpp as landed by Task 1 — the entry point and its banner
  </read_first>
  <action>
Add a `TEST_CASE` named `"morph blep: (D-06) the past-edge entry point is exactly the pre-scaled seam call"` to `tests/test_morph_blep.cpp`.

Subcase "identity": over a spread of edge positions across the open unit interval and a spread of signed magnitudes including one far outside the musical range, construct two fresh band-limiter instances, call the named past-edge entry point on one and the pre-scaled form of the existing seam on the other, and `CHECK` both accumulator values are equal — using the same tolerance shape the existing pinned-equivalence subcase uses, and stating in the comment why equality here is the header change's own non-circular check: the extension is safe because it is provably the same arithmetic the pinned seam already performs, not because it looks right.

Subcase "nothing owed forward": `CHECK` the forward-owed accumulator is exactly zero after a legitimate past-edge call, with exact float equality. Comment that this is the property the reconstruction shortcut for the withheld audition leg rests on — plan 33-10's renderer subtracts a per-sample recorded value and that subtraction is exact only while nothing is owed forward.

Subcase "gate": for each of an out-of-range position and each of a non-finite magnitude, construct a fresh instance, call the entry point, and `CHECK` both accumulators are exactly zero — the same population and the same exact-equality shape as the existing rejection subcase. Comment that the gate exists for the same reason the existing one does: a value that reached per-instance state would poison every following sample.

If Task 1 landed NO header change, write this case against the pre-scaled call instead: assert that the pre-scaled call produces exactly the after-edge residual in the current-sample accumulator and exactly zero forward, derived in the comment from the seam's own arithmetic. The claim being pinned is the same; only its subject changes.

Do not edit the existing pinned-equivalence subcases. Re-run them and record that they are unmoved.
  </action>
  <verify>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures; `make strict` exits 0; `make guards` exits 0
    - `./build-test/test -tc="morph blep: (D-06)*" -s 2>&1 | tail -4` reports 1 test case, 1 passed, 0 failed with a non-zero assertion count
    - The identity is proved able to fail: temporarily perturb the new entry point's coefficient in the working tree and confirm the identity subcase goes RED. Restore and confirm green. Record the perturbation and the resulting failure signature
    - The forward-owed accumulator assertion uses exact float equality, not a tolerance — quote the assertion in the SUMMARY
    - The existing pinned-equivalence subcases are unmoved: their recorded residual split values are reproduced in the SUMMARY, and `git diff -U0 tests/test_morph_blep.cpp` shows no modification inside them
    - The whole suite's case and assertion totals are recorded as a before/after pair against plan 33-05's recorded totals
    - `git diff --name-only` lists only `tests/test_morph_blep.cpp` for this task and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The header extension carries its own non-circular check — it is provably the same arithmetic the already-pinned seam performs — and the forward-owed-nothing property the audition reconstruction depends on is pinned with exact equality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `forge::VcoCore` → `forge::MorphBlep` | The SECOND call site. Every guard plan 33-01 landed exists for this boundary; the jump handed across it is a computed difference rather than an analytic constant |
| measured decision → landed implementation | A landed seam that is not the measured one makes the whole 33-05 measurement decorative; the source carries the decision record so the two are auditable together |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03 | Denial of Service | permanent poisoning of the forward-owed accumulator via a non-finite jump from the new call site | high | mitigate | Plan 33-01's guard on the existing seam plus Task 1's identical gate on the new entry point; Task 3's gate subcase pins both with exact-equality accumulator assertions |
| T-33-21 | Repudiation | landing a placement convention other than the measured one, making the measurement decorative | high | mitigate | Task 1 reads the recorded decision as its first act and restates it; Task 2 writes the decision record and all three conditions' outcomes into the source; the acceptance criteria require every claim to be consistent with the restated decision |
| T-33-22 | Tampering | a later editor "simplifying" the past-edge call back into the rejected candidate | medium | mitigate | The named entry point is chosen over the numerically identical pre-scaled argument precisely to make the simplification visible; the banner states the measured cost of the rejected candidate; Task 3 pins the identity so the two forms cannot silently diverge |
| T-33-23 | Tampering | the additive extension turning out not to be additive, moving the pinned residual split | medium | mitigate | The existing seam is asserted byte-unchanged by diff inspection and the pinned equivalence is re-run with its values reproduced |
| T-33-08 | Tampering | toolchain-divergent behavior invisible on the development host | high | mitigate | `make strict` on every task; the CI MinGW **link** leg observed green on the exact commit at the phase gate (plan 33-11) |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- `make test && make strict && make guards` all exit 0
- The landed seam is the measured one, restated at the top of the SUMMARY and recorded in the source with all three conditions' outcomes
- The existing seam is byte-unchanged and its pinned split is reproduced unmoved
- The band-limiter is still called exactly once per sample, after the sync block
- 15 frozen entries unchanged; `src/AnalogLFO.cpp` absent from the diff; the six LFO goldens byte-identical
</verification>

<success_criteria>
The shipped core band-limits its hard-sync reset using the placement the measurement selected, the header extension carries the forfeited-half argument that stops it being "fixed" later, and the extension's own arithmetic is pinned against the seam it extends.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-06-SUMMARY.md` when done
</output>

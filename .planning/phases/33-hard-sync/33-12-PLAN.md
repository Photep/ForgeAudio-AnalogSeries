---
phase: 33-hard-sync
plan: 12
type: execute
wave: 7
depends_on: ["33-11"]
files_modified:
  - .planning/phases/33-hard-sync/.continue-here.md
autonomous: false
nyquist_compliant: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "D-17 precedent 1: a BLOCKING resume marker is written BEFORE the operator gate is presented. A checkpoint-pending summary otherwise makes a resumed session walk straight past the gate — observed in this project, and the reason the preceding phase shipped one."
    - "D-17 precedent 2: the FULL expected-results block is presented BEFORE the operator replies, so an absence of complaint is an absence of complaint rather than an absence of exposure."
    - "D-17 precedent 3: perceptual coverage the script cannot evidence is REFUSED, not booked. The preceding phase's reply — that it was hard to remember what the old audio sounded like — was correct and useful, and the half it left open was recorded as unevidenced rather than allowed to stand in. The renderer built in this phase exists so that this time the script CAN evidence it."
    - "D-17 precedent 4: the session protocol names the plugin DIRECTORY as well as the module — the Analog VCO under Forge Audio Analog Series — because the operator's installation carries a second, differently-slugged plugin declaring the same brand string."
    - "D-17 precedent 4, the install half: the installed plugin is refreshed by a WHOLE-TREE synchronisation of the distribution directory, never by copying the binary and the resource directory alone. A stale install is a stale plugin VERSION, not a stale binary, and the naive refresh satisfies a hash assertion while the host keeps reading the old manifest."
    - "SYNC-01 / SYNC-02: the operator hears hard sync in Rack, with the A/B pair in the room as a reference, and the verdict is recorded VERBATIM and split honestly into what it discharges and what it does not."
  artifacts:
    - .planning/phases/33-hard-sync/.continue-here.md
  key_links:
    - "The install-freshness proof requires measuring the BEFORE state, not only matching the after state to source — the whole-directory flush caught a live stale install in the preceding phase that a manifest check would have passed."
    - "The audition pair is generated on demand and is not committed, so it must be rendered in the session rather than assumed present."
    - "No requirement is ticked by this plan; plan 33-11 already ticked what the automated evidence supports, and this gate can only ADD an operator observation or record a defect."
---

<objective>
Put the built module in front of the operator with a reference in the room, present the full expected-results block before asking, and record the verdict verbatim — split honestly into what it discharges and what it does not.

Purpose: buzzy-versus-smeared and click-versus-no-click have no automated instrument. The preceding phase's audition asked whether an improvement was audible and supplied nothing to compare against, so that half of the question was unanswerable by construction. Every piece of apparatus this phase built for the audition exists so that this time it is answerable.

Output: a blocking resume marker written before the gate, a verified-fresh install, a rendered A/B pair, and the operator's verdict recorded verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hard-sync/33-CONTEXT.md
@.planning/phases/33-hard-sync/33-VALIDATION.md
@.planning/phases/33-hard-sync/33-10-SUMMARY.md
@.planning/phases/33-hard-sync/33-11-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Phase 33 creates the following symbols. Anything named here is NEW in this phase and is not drift from an existing contract.

New files: `tools/render_sync_ab.cpp`, `.planning/phases/33-hard-sync/deferred-items.md`, `.planning/phases/33-hard-sync/.continue-here.md`.

Modified files: `src/dsp/MorphBlep.hpp`, `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `src/vco_compile_canary.cpp`, `res/AnalogVCO.svg`, `Makefile`, `tests/check_includes.sh`, `tests/test_morph_blep.cpp`, `tests/test_vco_core.cpp`, `tests/test_vco_spectrum.cpp`, `tests/test_vco_pitch.cpp`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.

New symbols:
- `forge::VcoInputs::syncVolts`, `forge::VcoInputs::syncConnected`
- `forge::VcoCore::syncTrig`, `forge::VcoCore::prevSyncVolts`
- `forge::VcoCore::Telemetry::syncFrac`, `::syncJump`, `::syncCorrection`
- `forge::MorphBlep::addPastStep(float, float)` — conditional on the 33-05 measurement
- `AnalogVCO::SYNC_INPUT`
- `make audition`, `AUDITION_BIN`, `build-test/audition/`
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` entry `"tools/render_sync_ab.cpp"`
- Test-only: `SyncPlacementProbe`, `SyncCell`, `SYNC_GRID`, `measureSyncCellDb`, `makeMasterSaw`, `HOSTILE_SYNC[]`, `kSyncResetDeltaBoundV`, `kSyncAntiCircularityMarginV`

THIS PLAN produces: `.planning/phases/33-hard-sync/.continue-here.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The blocking resume marker, the whole-tree install flush proved fresh, and the rendered pair (D-17.1 / D-17.4)</name>
  <files>.planning/phases/33-hard-sync/.continue-here.md</files>
  <read_first>
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/ — the preceding phase's own resume marker, for its wording and its placement
    - .planning/STATE.md § Accumulated Context — the Phase 30 record of the stale-install false negative reached THROUGH the safeguard, and the preceding phase's record that the whole-directory flush caught a live stale install that a manifest check would have passed
    - .planning/phases/33-hard-sync/33-10-SUMMARY.md — the render command, the file naming scheme, the render points and the banked expected-results material
    - .planning/phases/33-hard-sync/33-11-SUMMARY.md — the gate results and the requirement statuses, needed for the expected-results block
  </read_first>
  <action>
Part A — the blocking resume marker, FIRST, before anything else in this plan. Write `.planning/phases/33-hard-sync/.continue-here.md` stating in unmistakable terms that a blocking operator checkpoint is OPEN, that it has not been answered, that the phase is NOT complete until it is, and exactly what a resumed session must do — present the expected-results block and ask, not proceed. The reason this file exists is that a checkpoint-pending summary otherwise makes a resumed session walk straight past the gate; that has happened in this project, and the preceding phase shipped this same marker for the same reason. Say so in the file, so a future reader does not delete it as clutter.

Part B — build and install with the WHOLE-TREE flush, and prove freshness by measuring the BEFORE state. Build the plugin and produce the distribution directory. Then, BEFORE replacing anything:
  - record what is currently installed: the manifest's declared version and its module entries, the binary's modification time and hash, the exported module symbols, and the resource directory's contents
  - state whether the current install is stale, and in what respect
Then synchronise the WHOLE distribution directory over the installed one. Never copy the binary and the resource directory alone: a stale install is a stale plugin VERSION rather than a stale binary, and the naive refresh satisfies a hash assertion while the host keeps reading the old manifest — which is exactly how a previous phase reached a false negative THROUGH its own safeguard.
Then record the AFTER state on the same five properties and confirm each changed to what the source produces. A match between the after state and the source is not sufficient on its own; the before-and-after pair is what proves freshness.
Also check the two install hazards that sit outside the flush: any packaged plugin archive anywhere under the host's directory that could be re-extracted over the flush at launch, and any installation of this plugin under a different architecture directory.

Part C — render the audition pair. Run the render target and confirm the pair exists, with the file names, sizes and clipped-sample counts recorded. The pair is generated on demand and is not committed, so it must be produced in this session rather than assumed present.

Part D — assemble the expected-results block from plan 33-10's banked material plus plan 33-11's gate results. Do NOT present it yet; Task 2 presents it. Confirm it contains all seven of the banked items plus the named list of coverage the pair cannot evidence.
  </action>
  <verify>
    <automated>make && make audition && test -f .planning/phases/33-hard-sync/.continue-here.md && echo "marker present"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/33-hard-sync/.continue-here.md` exists and states that a blocking checkpoint is open, that the phase is not complete, and what a resumed session must do
    - The marker was written BEFORE the install and render steps — record the ordering in the SUMMARY
    - The install's BEFORE state is recorded on all five properties, and a verdict on staleness is stated
    - The AFTER state is recorded on the same five properties and each changed to what the source produces
    - The refresh was a whole-directory synchronisation: quote the command used and confirm it is not a copy of the binary and resource directory alone
    - Both out-of-flush hazards were checked and the results recorded
    - The rendered pair exists with file names, sizes and clipped-sample counts recorded
    - The assembled expected-results block contains all seven banked items and the named list of what the pair cannot evidence
    - `make` and `make audition` both exit 0
  </acceptance_criteria>
  <done>The gate is guarded by a blocking marker, the installed plugin is proved fresh by a before-and-after measurement, and the reference pair is in the room.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The operator gate — full expected results first, then ask (D-17.2 / D-17.3 / D-17.4)</name>
  <files></files>
  <read_first>
    - .planning/phases/33-hard-sync/33-10-SUMMARY.md — the banked expected-results material, all seven items plus the named list of what the pair cannot evidence
    - .planning/phases/33-hard-sync/33-11-SUMMARY.md — the gate results, the measured worst-case output excursion, and the requirement statuses
    - .planning/phases/33-hard-sync/33-05-SUMMARY.md — the measured figure for how small the correction's own spectral improvement is, which must be stated before the operator listens
    - .planning/STATE.md § Current Position — how the preceding phase's verdict was split, and why an unanswered question is recorded as unevidenced
  </read_first>
  <action>
Run the operator session and record it. Present the `<what-built>` paragraph, then the FULL expected-results block from `<how-to-verify>` — all seven items, in order, in full, BEFORE asking anything. Only then put the five numbered questions, and wait.

Do not compress the expected-results block, do not summarise item 4 (the honest statement that the correction's own measured improvement is small), and do not drop item 7 (what this session cannot evidence). Each of those three omissions has a recorded cost in this project: a compressed block turns an absence of complaint into an absence of exposure, an over-promised difference produces a reported defect that is not there, and an unnamed limit gets booked as coverage.

Ask Q5 as its own question and wait for its own answer. A general remark about the session is not an answer to it.
  </action>
  <what-built>
Hard sync for the Analog VCO. The module gains a SYNC input; a rising edge on it resets the oscillator's phase to a sub-sample fractional overshoot and places a band-limited correction at the master's wrap fraction, reusing the anti-aliasing machinery rather than the shipped LFO's slow crossfade. Alongside it, a command that renders a matched A/B pair of audio files so the improvement has a reference to be judged against.
  </what-built>
  <how-to-verify>
PRESENT THE FULL EXPECTED-RESULTS BLOCK BEFORE ASKING ANYTHING. The operator answers with it in front of them, so an absence of complaint is an absence of complaint rather than an absence of exposure. Present, in this order and in full:

  1. WHICH MODULE, named unambiguously: the **Analog VCO**, under the plugin directory **Forge Audio Analog Series**. The browser carries more than one Forge Audio entry because a second, older, differently-slugged plugin declares the same brand string; the distinction is the module NAME and the plugin directory, not the brand heading.
  2. THE INSTALL PROOF: the before-and-after properties recorded in Task 1, so the operator can see what changed rather than being told it did.
  3. THE AUDIO PAIR: the exact command, where the files land, which filename is which leg, and the render points in plain language.
  4. THE EXPECTED DIFFERENCE, stated honestly and BEFORE listening — including the measured figure for how small the correction's own spectral improvement is. An operator told to expect a dramatic difference and hearing a subtle one reports a defect that is not there.
  5. THE EXPECTED IN-RACK BEHAVIOR: what the panel looks like (still deliberately unlabelled and ugly, replaced in a later phase), what the SYNC jack does, and the expected timbre — sharp and buzzy rather than smeared — with no click at reset instants.
  6. THE EXPECTED LEVELS: the module's output is still unconditioned, with the measured worst-case excursion from the phase gate quoted, so a level above five volts is expected rather than a surprise.
  7. WHAT THIS SESSION CANNOT EVIDENCE, named: anything about the shipped Analog LFO unless the operator is asked about it separately and answers separately, and anything the rendered pair is not able to show.

THEN ask, as separate numbered questions:

  Q1. Listening to the rendered pair: does the shipped leg read as SHARP AND BUZZY rather than smeared, compared against the withheld leg? Answer only about what you can hear in the pair.
  Q2. Listening to the rendered pair: is there a CLICK at the reset instants in either leg? If so, which leg and at which render point?
  Q3. In Rack: patch an oscillator into the SYNC jack of the Analog VCO under Forge Audio Analog Series and sweep the slave's pitch. Does it behave as hard sync — locking to the master and buzzing as the slave's pitch rises — with no dropouts and nothing obviously wrong?
  Q4. In Rack: does anything about the module's level, stability or behavior look wrong to you?
  Q5. Separately and explicitly: open the shipped **Analog LFO** in the same session. Is it visually and audibly unchanged? A general remark about the session is NOT an answer to this question; if you do not answer it, no guardrail attestation is recorded.

Answer in your own words. Anything you are unsure about, say so — an unsure answer is more useful here than a confident one, and this gate is built to record the difference.
  </how-to-verify>
  <verify>
    <human-check>The operator has answered Q1 through Q5 in their own words, with the full expected-results block presented before the first question.</human-check>
  </verify>
  <acceptance_criteria>
    - All seven expected-results items were presented in full BEFORE the first question was asked — the SUMMARY records the order and confirms none was compressed or omitted
    - Item 4's honest statement that the measured improvement is small was presented with its figure, before listening
    - Q5 was asked as its own question and its answer (or its absence) is recorded separately
    - The operator's reply is captured verbatim for Task 3
  </acceptance_criteria>
  <resume-signal>Answer Q1 through Q5, or describe issues</resume-signal>
  <done>The operator has heard hard sync in Rack with the matched reference pair in the room, with the full expected results in front of them before they answered.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdict verbatim, split it honestly, and clear the marker (D-17.3)</name>
  <files>.planning/phases/33-hard-sync/.continue-here.md</files>
  <read_first>
    - .planning/STATE.md § Current Position — how the preceding phase's qualified verdict was recorded and split, and the explicit refusal to read a general remark as an answer to a specific question
    - .planning/phases/33-hard-sync/33-11-SUMMARY.md — what the automated evidence already discharged, so the operator's answer is added to it rather than substituted for it
  </read_first>
  <action>
Record the operator's reply VERBATIM in the plan SUMMARY, as a quoted block, before any interpretation.

Then split it, question by question:
  - for each question, state what the answer DISCHARGES and what it does not. An answer that does not address a question leaves that question UNEVIDENCED, and unevidenced is recorded as unevidenced rather than as passed
  - if any answer is equivocal on the smear question, record it as equivocal and note that the register carries a cheap escalation for exactly that case — a third audition leg rendering the design this phase's requirement forbids by name, which the renderer can produce as a configuration rather than a build
  - if the operator did not answer the shipped-module question separately, record that NO guardrail attestation was given and that the guardrail is discharged for this phase by AUTOMATED evidence only, naming which evidence. Do not infer an attestation from a general remark; that inference is the fabrication this protocol exists to refuse
  - if the operator reports a defect, record it verbatim, classify it, and file it — do not fix it inside the checkpoint

Do not tick any requirement in this plan. Plan 33-11 already ticked what the automated evidence supports; this gate can only ADD an operator observation or record a defect. If the operator's answer would change a requirement's status, say so explicitly and leave the change to a follow-up rather than making it here.

Then delete `.planning/phases/33-hard-sync/.continue-here.md`, and record in the SUMMARY that it was deleted BECAUSE the checkpoint it guarded is answered — not as cleanup.

Finally, record for the next phase: which of this phase's deferred items the operator's answer moves, and whether the audition apparatus is now the instrument the next phase's audition-gated value should be decided on.
  </action>
  <verify>
    <automated>test ! -f .planning/phases/33-hard-sync/.continue-here.md && echo "marker cleared" && make test</automated>
  </verify>
  <acceptance_criteria>
    - The operator's reply is quoted VERBATIM in the SUMMARY as a block, before any interpretation
    - Each of the five questions has an explicit verdict: discharged, unevidenced, or a recorded defect — with none left implicit
    - If the shipped-module question was not answered separately, the SUMMARY states that no attestation was recorded and names the automated evidence discharging the guardrail instead
    - No requirement status was changed by this plan: `git diff --stat .planning/REQUIREMENTS.md` produces no output for this plan's commits
    - The resume marker is deleted and the SUMMARY records the reason as the checkpoint being answered
    - `make test` exits 0 with 0 failures
    - The next-phase notes record which deferred items the answer moves
  </acceptance_criteria>
  <done>The operator's verdict is on record verbatim, split into what it evidences and what it does not, with no coverage booked that was not given.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built plugin → the operator's installation | A stale install is a stale plugin VERSION; the manifest is the check that passes on a stale install |
| the operator's words → recorded coverage | A general remark read as an answer to a specific question is fabricated coverage, which this protocol refuses by name |
| module browser entry → the module under test | The operator's installation carries a second, differently-slugged plugin declaring the same brand string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-39 | Spoofing | auditioning a stale installed plugin and attributing the result to this phase's code | high | mitigate | Task 1 records the BEFORE state on five properties, refreshes by whole-directory synchronisation, records the AFTER state on the same five, and checks the two hazards that sit outside the flush |
| T-33-40 | Spoofing | auditioning the wrong module, because two plugins declare the same brand string | medium | mitigate | The gate names the plugin DIRECTORY as well as the module name, and the expected-results block states why the brand heading is not the distinction |
| T-33-38 | Repudiation | inferring an operator attestation from a general remark | high | mitigate | Task 2 asks the guardrail question separately and states that a general remark is not an answer; Task 3 records the absence of an answer as an absence rather than as a pass |
| T-33-41 | Repudiation | booking perceptual coverage the session could not evidence | high | mitigate | The full expected-results block precedes the questions, and Task 3 records each question as discharged, unevidenced or defective with none left implicit |
| T-33-42 | Denial of Service | a resumed session walking past an open blocking checkpoint | high | mitigate | Task 1 writes the blocking resume marker BEFORE the gate is presented and Task 3 deletes it only because the checkpoint is answered |
| T-33-SC | Tampering | package-manager supply chain | low | accept | This phase installs no npm/PyPI/crates package and vendors no third-party source; `33-RESEARCH.md` records the audit as not applicable with that finding stated |
</threat_model>

<verification>
- The blocking resume marker exists before the gate is presented and is deleted only after it is answered
- The install is proved fresh by a before-and-after measurement on five properties, not by matching the after state to source
- The full expected-results block is presented before any question is asked
- The verdict is recorded verbatim and split question by question, with unevidenced recorded as unevidenced
- No requirement status is changed by this plan
</verification>

<success_criteria>
The operator hears hard sync in Rack with a matched reference pair in the room, answers with the full expected results already in front of them, and the reply is recorded verbatim and split honestly — so this phase's perceptual claims rest on exposure rather than on the absence of a complaint.
</success_criteria>

<output>
Create `.planning/phases/33-hard-sync/33-12-SUMMARY.md` when done
</output>

---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 04
type: execute
wave: 3
depends_on: ["32-03"]
files_modified:
  - src/dsp/MorphBlep.hpp
  - src/vco_compile_canary.cpp
autonomous: true
nyquist_compliant: true
requirements: [CORE-02, AA-01, AA-02, AA-03, AA-04, AA-05, MORPH-01]
must_haves:
  truths:
    - "CORE-02 / D-12: band-limiting lives in ONE new additive header, src/dsp/MorphBlep.hpp, holding kernels and site logic together; it CALLS the frozen src/dsp/Waveshape.hpp and never edits it. research/STACK.md:61's suggestion to put the kernels in RackCompat.hpp is REJECTED and must not be revisited."
    - "D-01: jump magnitudes are ANALYTIC and character-aware — closed form, no probing. The pristine character-independent alternative is overridden BY DECISION, not by oversight, and must not be restored."
    - "D-03 / D-03b: ONE unified factor k = max(0, 1 - w/(2*dt))^2 scales polyBLEP value-step corrections and polyBLAMP slope-break corrections alike; its COMPACT SUPPORT is the load-bearing property, and the 2*dt cutoff is read off the kernel's own 2-sample support rather than fitted."
    - "D-04: a FIXED union of nine sites is evaluated every sample and positions are recomputed per sample from the live Waveshape fields, never cached. The square's HARD step sits at 0.5f and its SOFT tanh edge at dutySq — different positions, split into separate entries."
    - "D-05: corrections ride through the bleed normalization by folding 1/(1+bleedIntensity) into the weight vector once; the normalization is linear, so this is EXACT."
    - "D-07: overlapping edges are each placed at their own sub-sample position and SUMMED into the accumulator, never overwritten, and there is NO duty floor."
    - "D-13: a pending-residual accumulator delivers the next-sample half at ZERO added latency; it is not a one-sample output delay buffer."
    - "D-14: the Phase 33 sync seam is DESIGNED here and not built here — addStep accepts an externally supplied sub-sample position and value jump and feeds the SAME accumulator as the morph sites. All state is per-instance: no static, no global mutable voice state (CORE-03)."
    - "D-15 / P-14: MorphBlep does NOT rely on its caller — it carries its own negated-comparison dt guard, and divides by dt only, never by an edge width."
    - "AA-05: table-free, Rack-free, closed-form; C++11-strict; no minBLEP and no oversampling in v2.0."
  artifacts:
    - src/dsp/MorphBlep.hpp
    - src/vco_compile_canary.cpp
  key_links:
    - "P-9: check_canary.sh [5/5] strips comment lines first, so the commented placeholder at src/vco_compile_canary.cpp:56 does NOT satisfy the check once the header exists — the include must be ACTIVATED in the same commit as the header."
    - "src/dsp/MorphBlep.hpp needs NO check_includes.sh or check_canary.sh edit: it is already pre-wired at check_includes.sh lines 90, 261, 337 and check_canary.sh lines 414, 452-453, 466."
    - "No FROZEN.sha256 bump is required or permitted in Phase 32."
---

<objective>
Create `src/dsp/MorphBlep.hpp` — the phase's single genuinely-new subsystem — and activate its include in the compile canary in the same commit.

Purpose: CORE-02 names one header holding kernels and site logic together. Everything this phase's iteration budget is spent on lives here: the exact 2-point polyBLEP and polyBLAMP residuals, the analytic character-aware jump table derived against the frozen `Waveshape`, the compact-support D-03 factor, the split-source crossing test, and the pending accumulator that gives D-07 and D-13 for free.

Output: one new `src/dsp/` header, one two-line canary edit, and a green `make strict` / `make guards` / `make test`.
</objective>

<!-- planner-discipline-allow: static -->
<!-- planner-discipline-allow: extern -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md
@src/dsp/Waveshape.hpp
@src/dsp/VcoCore.hpp
@src/vco_compile_canary.cpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — members `float pending`, `float inject`; methods `void reset()`, `void addStep(float xAhead, float jump)`, `float step(const Waveshape& wv, double phase, float p, double dt, float morph, float character)`
- `forge::VcoCore::blep` — a `forge::MorphBlep` held BY VALUE beside `wave`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: `src/dsp/MorphBlep.hpp` in full — `forge::morphBlepCharFactor` and `struct forge::MorphBlep` with `pending`, `inject`, `reset()`, `addStep()` and `step()` — plus the activated canary include.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The header skeleton, the D-03 factor, the D-14 sync seam, and the canary include — one commit</name>
  <files>src/dsp/MorphBlep.hpp, src/vco_compile_canary.cpp</files>
  <read_first>
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "Pattern 1: The exact 2-point kernels and the sub-sample convention" in full, § "Pattern 5: The D-03 character factor" in full including both calibration tables, § "Code Examples — The header skeleton", and § Common Pitfalls P-1, P-9, P-11, P-14
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md § "src/dsp/MorphBlep.hpp (NEW — DSP utility header, per-sample transform)" in full
    - src/dsp/Waveshape.hpp lines 1-33 — the banner shape, the include-hygiene line, and the struct/NSDMI shape
    - src/dsp/VcoCore.hpp lines 1-110 — the two-standard C++11 rule block at :55-80 (quote the list, do not restate it loosely), the include-with-trailing-comment idiom at :82-90, the namespace-scope plain-constexpr rule at :94-96, and the per-instance-state comment at :243-249
    - src/dsp/VcoCore.hpp lines 349-364 — the negated-comparison guard idiom and the paragraph rejecting the comparison-ladder helper BY NAME
    - src/vco_compile_canary.cpp lines 19-56 — the GROWTH RULE at :23-27 and the pre-staged growth point at :54-56
    - tests/check_canary.sh lines 396-430 — section [5/5], which strips comment lines FIRST
  </read_first>
  <action>
Create `src/dsp/MorphBlep.hpp` and activate the canary include in the SAME commit. P-9 is why: `tests/check_canary.sh` [5/5] strips comment lines before it looks for the include, so the commented placeholder at `src/vco_compile_canary.cpp:56` stops satisfying the check the instant the header exists. Landing the header alone turns `make guards` red.

HEADER STRUCTURE. Open with `#pragma once`, the `// src/dsp/MorphBlep.hpp` path line, then the banner. Include `"dsp/Waveshape.hpp"` with the trailing comment reused verbatim from `src/dsp/VcoCore.hpp:89`. Do NOT include `dsp/RackCompat.hpp` — its `check_includes.sh` [2/7] exemption is exact-path to `VcoCore.hpp` and does not cover this file, and nothing here needs it. Add an angle-bracket standard header ONLY if a symbol from it is actually used; the recommended body uses none. `check_includes.sh` [3/7] requires every include to be either angle-bracket or a quoted path beginning with `dsp/`.

BANNER CONTENT, in the house voice, carrying each of these as its own paragraph:

(1) WHAT THIS FILE IS: the morph-aware band-limiting layer (CORE-02 / AA-01..05). It CALLS `forge::Waveshape::morphedWave`'s world — its crossfade weights, its bleed ring, its per-shape branch positions, its character-deformation widths — and never edits it.

(2) THE D-01 FROZEN-INTERNALS DEPENDENCY, which is the trade this whole design rests on. This header duplicates knowledge of `src/dsp/Waveshape.hpp`'s internals. That duplication is safe ONLY because `Waveshape.hpp` is byte-pinned by `tests/check_frozen.sh` and cannot drift underneath it. If that pin is ever lifted, every magnitude, position and width below must be re-derived. State the rejected alternative and why: probing `morphedWave` at phase plus-or-minus epsilon is AA-04's most literal reading and would auto-handle character, bleed and crossfade weights with zero derivation, but `morphedWave` computes all five shapes per call and probing both sides of the site set is roughly eight extra transcendental-heavy calls per sample.

(3) THE D-01 OVERRIDE, RECORDED SO IT IS NOT "RESTORED". `research/STACK.md:40` recommends pristine, character-independent jump magnitudes and calls magnitude-matching a v2.1 nicety. That recommendation is OVERRIDDEN BY DECISION, not by oversight.

(4) A FALSIFIED PREMISE, CORRECTED IN PLACE. `research/STACK.md:40`'s supporting argument — that erring toward over-correction is the safe direction, because character is a lowpass-ish coloration so extra correction only adds harmless high-frequency rolloff — is FALSIFIED BY MEASUREMENT. At character 1 / C6 / morph at the square centre the naive alias floor is -60.1 dB and full-authority correction measures -29.9 dB: a 30 dB REGRESSION. The argument fails because the injected residual is a STEP-shaped correction added to a signal that has no step; it is new broadband energy, not a filter. Keep the sentence, replace the premise, and say the old reason was falsified — the house pattern.

(5) A SECOND FALSIFIED SOURCE, NAMED. `research/STACK.md:100-104`'s polyBLAMP snippet is WRONG for this design: it returns a quartic where the 2-point form is cubic, and it folds `dt` into the wrong place. It is neither the 2-point form used here nor the DAFx-16 four-point form. Do not copy it. The forms below are derived from first principles as the once- and twice-integrated 2-sample triangular basis and were cross-validated numerically.

(6) THE C++11 TWO-STANDARD CONTRACT, quoted as a list from `src/dsp/VcoCore.hpp:55-80` rather than restated loosely: no C++17 inline variables, no compile-time-conditional statement form, no structured bindings, no attribute-bracket annotations, no nested-namespace definition syntax, no auto return-type deduction, no generic lambdas; no standard-library clamping helper; no pi macro; any constant table must be namespace-scope or function-local, NEVER an in-class constant table indexed at runtime — that exact class got v2.0.0 rejected from the VCV Library; and zero Rack-SDK includes.

(7) P-11: the residuals below are chains of multiply-add, so `-ffp-contract=off` is load-bearing for THIS file specifically. Do not add fast-math, do not drop the flag, and never capture a fixture with different flags.

(8) THE DEFERRED FIRST REFINEMENT, so the iteration budget has somewhere cheap to go: an optional narrow-pulse "reach" factor using a rational tanh approximation is MEASURED at plus 1.3 dB at the single worst grid point and about plus 0.1 dB mean. It is deliberately NOT shipped in the first iteration, because it would add the only division by an edge width in this file. Try it first if the pulse threshold at C8 is missed.

THE D-03 FACTOR. Define `inline float morphBlepCharFactor(float w, float dt)` at namespace scope inside `namespace forge`. Body: `twoDt = dt + dt`; `u = twoDt - w`; if `!(u > 0.f)` return `0.f`; `t = u / twoDt`; return `t * t`. That is algebraically `max(0, 1 - w/(2*dt))^2`. Document above it:
- the limits D-03 binds it to: `w = 0` (a true hard step) gives exactly 1, so every hard site keeps full authority at every character; `w` much greater than `dt` gives exactly 0, NOT asymptotically — and the EXACTNESS is the load-bearing property (P-1: a factor that merely decays never stops correcting, and a residual correction on an edge that is already several samples wide injects more alias energy than it removes).
- WHY THE CUTOFF IS NOT A TUNED CONSTANT: the 2-sample kernel's support IS two samples, so an edge already at least two samples wide lies entirely inside the kernel's own support and is already band-limited on the sample grid. `w = 2*dt` is read off the kernel, not fitted.
- THE CALIBRATION EVIDENCE as numbers: over 6 characters by 4 notes by 5 morph centres, the worst regression versus the naive path was -60.4 dB at full authority, -42.7 dB with a reciprocal-linear factor, -36.6 dB with a sinc-Pade fit, -29.8 dB with a reciprocal-quadratic factor, and -1.7 dB with this compact-support form, whose mean improvement of 7.3 dB is also the highest. The exponent 2 rather than 1 was chosen on the continuity argument — both sit on the same measured plateau, but exponent 1 has a slope discontinuity at the cutoff, which under audio-rate MORPH and CHARACTER modulation is a per-sample step in the correction gain.
- THE GUARD: the comparison is written NEGATED so a not-a-number `dt` lands on the zero branch. A comparison-ladder helper is rejected here for the same reason `forge::VcoCore` rejects it by name: both of its comparisons are false for a not-a-number, so it is inert against exactly the input class this guard exists to stop.
- THE DIVISOR NOTE (D-15 / P-14): this divides by `dt` ONLY. There is no division by an edge width anywhere in this header.

THE STRUCT. Define `struct MorphBlep` with two NSDMI float members, `pending = 0.f` (the residual owed to the NEXT sample, D-13) and `inject = 0.f` (the residual owed to THIS sample, supplied externally, D-14). Add `void reset() { pending = 0.f; inject = 0.f; }`. Document, in the shape of `src/dsp/VcoCore.hpp:243-249`'s per-instance-state comment, that both members are INSTANCE state and not static or shared — which is literally what CORE-03 asserts and what D-14 binds: no static, no global mutable voice state, or the polyphony-ready guarantee breaks.

Explain D-13 in the same block: a 2-sample polyBLEP corrects the sample AFTER the edge as well as the one containing it, and this accumulator delivers that second half at the top of the following `step()`. State the rejected alternative and both of its costs: a one-sample output delay buffer is simpler bookkeeping with no carried state, but it adds a sample of latency the module would have to declare — a VCO that silently delays by a sample desyncs against every other oscillator in the patch — and it complicates Phase 33, which needs to act on the current sample. State that the accumulator composes for free with D-07: several corrections `+=` into the same slot instead of fighting over it.

THE D-14 SYNC SEAM. Define `void addStep(float xAhead, float jump)`. Body: if `!(xAhead >= 0.f) || xAhead > 1.f` return; `u = 1.f - xAhead`; `inject += jump * 0.5f * u * u`; `pending += jump * (-0.5f) * xAhead * xAhead`. Document that `xAhead` is the edge's position in samples relative to THIS output sample — 0 means the edge lands immediately after this sample, 1 means it lands on the next sample — and that `jump` is `value_after - value_before`, already scaled by whatever weights the caller owns. State the boundary: Phase 32 implements NO sync behavior and adds NO sync fields to `forge::VcoInputs`. The seam exists so Phase 33 plugs in rather than reopening the one header this phase spends its entire iteration budget stabilising and whose per-shape alias thresholds it has just pinned. Note that the entry gate is written with the negated comparison first so a not-a-number `xAhead` is rejected rather than accumulated.

THE SIGN CONVENTION, stated once, unambiguously, above the two kernels' first use: the jump at a site is `h = value_after - value_before`, signed; a corrected value-step sample is `naive + h * r(x)` and a corrected slope-break sample is `naive + slopeChangePerSample * R(x)`, where the BLEP residual `r(x)` is `(x+1)^2/2` on the sample BEFORE the edge and `-(x-1)^2/2` on the sample AFTER it, and the BLAMP residual `R(x)` is `(x+1)^3/6` and `(1-x)^3/6` on the same two sides, both zero outside. Record the sanity check that fixes the sign: at the edge itself the naive value has already jumped, `r(0)` is `-1/2`, so the corrected sample is the pre-edge value plus half the jump — the band-limited midpoint. Record the canonical-form equivalence too: the widely published two-branch function returns twice this residual and is applied at half the jump; the two agree to 7e-7 at `dt = 0.094`, and the corrected saw's harmonic gain matches a squared sinc to 0.01 dB over twelve harmonics.

Declare `float step(const Waveshape& wv, double phase, float p, double dt, float morph, float character);` inside the struct and define it out of line as an `inline` member below the struct, in this task returning only the drained accumulator: set `now = inject + pending`, zero both members, and return `now`. Task 2 fills the body. Add a one-line comment marking that as the Task 2 growth point so the intermediate state is deliberate rather than mistaken for finished work.

THE CANARY. In `src/vco_compile_canary.cpp`, uncomment the pre-staged `#include "dsp/MorphBlep.hpp"` at line 56 and rewrite the two comment lines at :55-56 to record that the growth point has been TAKEN by Phase 32 rather than leaving a stale instruction pointing at work already done. Update the GROWTH RULE paragraph at :23-27 the same way: `dsp/MorphBlep.hpp` is no longer "the next one due", it is carried; the rule itself stands unchanged for the phase that adds the next header. Add no other change to that file — D-17 adds no `forge::VcoInputs` field, so the runtime-derived field block at :91-99 needs no addition. State that finding in a sentence rather than silently skipping it.
  </action>
  <verify>
    <automated>make strict && make guards && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make strict` exits 0 and prints `strict C++11 gate: PASS`
    - `make guards` exits 0 and prints `guard suite: PASS`; `bash tests/check_canary.sh` prints `OK: dsp/MorphBlep.hpp is carried into both gates by the canary`
    - `bash tests/check_includes.sh` prints `OK: src/dsp/MorphBlep.hpp — no Rack include` and `OK: src/dsp/MorphBlep.hpp — siblings and standard headers only`
    - `make test` exits 0 with an unchanged case count from this plan's starting baseline
    - `grep -c 'morphBlepCharFactor' src/dsp/MorphBlep.hpp` is at least 2
    - `grep -cE 'if \(!\(u > 0\.f\)\) return 0\.f;' src/dsp/MorphBlep.hpp` outputs `1`
    - `grep -c 'void addStep(float xAhead, float jump)' src/dsp/MorphBlep.hpp` outputs `1`
    - `grep -v '^[[:space:]]*//' src/vco_compile_canary.cpp | grep -c '#include "dsp/MorphBlep.hpp"'` outputs `1` — the ACTIVE include, not the placeholder
    - `grep -cE '^[[:space:]]*(static|extern)[[:space:]]' src/dsp/MorphBlep.hpp` outputs `0` — no file-scope mutable state (CORE-03 / D-14)
    - `git status --porcelain src/dsp/FROZEN.sha256` is empty and `git diff --name-only` lists neither `src/dsp/Waveshape.hpp`, `src/dsp/RackCompat.hpp` nor `src/AnalogLFO.cpp`
    - Both files appear in ONE commit (`git show --name-only HEAD` lists `src/dsp/MorphBlep.hpp` and `src/vco_compile_canary.cpp`)
  </acceptance_criteria>
  <done>The header exists with its banner, the compact-support D-03 factor, the per-instance accumulator and the D-14 sync seam; the canary carries it into both the C++11 strict gate and the CI MinGW link leg; every guard is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The per-sample step() body — weight algebra, live geometry, the nine-site union, and the split-source crossing test</name>
  <files>src/dsp/MorphBlep.hpp</files>
  <behavior>
    - with `morph = 0.5` (saw centre) and `character = 0`, the only non-zero magnitude is site 1's `+2` at phase 0, its width is 0 and its factor is exactly 1 at every character — the saw's soft reset does NOT soften the wrap (P-4)
    - with `morph = 0.75` (square centre) and `character = 0.5`, site 4's hard step sits at `0.5f` and site 5's soft edge sits at `dutySq = 0.5 + c*(0.04 + squareDutySpread)` — two DIFFERENT positions, never merged
    - with `morph = 0` (sine centre) and `character = 0.5`, the weight vector is non-zero at index 4 (the narrow pulse bleeds in at full left weight) and the predicted jump at phase 0 is 0.014851, matching the frozen path's measured 0.014853
    - with `character = 1` and a triangle-centre morph at a low note, `morphBlepCharFactor` returns exactly 0 for the triangle sites, so no correction is emitted where the corner is already wider than the kernel's support
    - `step()` returns the drained accumulator and nothing else when `dt` is zero, negative, subnormal-to-zero or not-a-number
  </behavior>
  <read_first>
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "Pattern 2: The crossing test — forward lookahead with a split source of truth" in full including the two-row table and the three anti-patterns, § "Pattern 3: The site map" in full including the nine-row table and the six derived quantities, § "Pattern 4: Weight algebra — replicate, do not approximate", § "Code Examples — The per-sample body", and § Common Pitfalls P-2, P-3, P-4, P-12, P-14
    - src/dsp/Waveshape.hpp in full — every line the site table derives from: `progressiveCurve` at :36-38, the triangle's `valley` at :60-61 and `roundAmount` at :71, the saw's soft reset at :91-97, the square's branch at :104 and its `duty`/`edgeWidth` at :108-110, the pulse's branch at :128 and its `maxEdge`/`edgeWidth`/`sharpness` at :134-139, and `morphedWave`'s segment/frac/pulseDuty at :164-172, its crossfade at :178-185 and its bleed ring plus normalization at :187-213
    - .planning/research/PITFALLS.md:190 — the in-class constant-table trap that got v2.0.0 rejected
  </read_first>
  <action>
Fill in `forge::MorphBlep::step`. Four labelled sections, in this order.

PREAMBLE. `float now = inject + pending;` then zero both members. `const float fdt = (float)dt;` then the D-15 guard: if `!(fdt > 0.f)` return `now`. Comment it as MorphBlep refusing to rely on its caller (P-14), written negated so a not-a-number `dt` lands here too, and never a comparison-ladder helper.

SECTION A — WEIGHT ALGEBRA, REPLICATED, NOT APPROXIMATED (AA-01 / D-05 / P-12). Compute, mirroring `Waveshape`'s own expressions and its own early-return threshold exactly: `c` is `0.f` when `character < 0.001f` and `character * character` otherwise; `scaled = morph * 4.f`; `segment = (int)scaled` then bounded above by 3 (mirroring the frozen minimum-of-3); `frac = scaled - (float)segment`; `pulseFrac` is `scaled - 3.f` when `scaled > 3.f` and `0.f` otherwise; `pulseDuty = 0.50f - 0.45f * min(pulseFrac, 1.f)`. Build `float W[5]` zeroed, then: if `segment == 3` set `W[4] = 1.f` (the frozen direct-duty special case at `:179-182`), else `W[segment] += 1.f - frac` and `W[segment + 1] += frac`. Then, gated on the SAME `character >= 0.001f` comparison the frozen bleed block at `:188` uses: `eb = 0.04f + wv.bleedSpread` floored at zero; `bi = c * eb` floored at zero; `W[(segment - 1 + 5) % 5] += bi * (1.f - frac)`; `W[(segment + 2) % 5] += bi * frac`; then `norm = 1.f / (1.f + bi)` and multiply all five weights by it.

Comment this section with two things. First, D-05: folding the bleed normalization into `W` ONCE is EXACT, not an approximation, because the normalization is linear; applying corrections at full magnitude after normalization would over-correct by exactly `(1 + bleedIntensity)`, and precisely when `character > 0`, which is precisely when the bleed sites exist. Second, the bleed-ring finding, with its numbers, because nothing in AA-01's wording points at it: at `morph = 0` and `character = 1` the segment is 0 and `frac` is 0, so the left weight is 1 and the ring's left index is 4 — the NARROW PULSE bleeds in at full intensity inside what the user hears as a pure sine, a step of about 0.077 in a plus-or-minus-1 wave, roughly -22 dB, and MEASURED as a genuine -23 dB alias floor at C9 / character 1. An implementation that band-limits only "the shapes being crossfaded" is silently wrong at every morph position. Record the verification: at `morph = 0`, `character = 0.5` the predicted jump at phase 0 is 0.014851 and the frozen path measures 0.014853.

Note P-12 explicitly: the `< 0.001f` threshold and the `>= 0.001f` gate must be the frozen code's exact comparisons, or the weight vector disagrees with the naive path in a band the spectral grid sweeps straight through.

SECTION B — GEOMETRY, RECOMPUTED EVERY SAMPLE (D-04). From the live `wv` fields, never cached: `dutySq = 0.5f + c * (0.04f + wv.squareDutySpread)`; `wSq = 2.f * (c * 0.08f)`; `lo = min(pulseDuty, 1.f - pulseDuty)`; `capPl = min(0.08f, lo * 0.8f)`; `wPl = 2.f * (c * capPl / (1.f + wv.pulseEdgeSpread))`; `valley = 0.5f + c * (0.10f + wv.triAsymmetrySpread) * 0.5f`; `wTri = 0.5f * (c * 0.35f)`; `triBrk = 2.f / valley + 2.f / (1.f - valley)`; `hardSq = W[3] * 2.f * (1.f - c)`; `hardPl = W[4] * 2.f * (1.f - c)`. Use ternary comparisons for the two minima rather than library helpers, so the file stays free of standard-library calls (AA-05).

Comment the width derivations, since they are what the D-03 factor consumes: a hyperbolic-tangent edge of half-width `edgeWidth` has unit slope at its centre and swings 2, so its equivalent ramp width is `2 * edgeWidth` — verified against the frozen square at `duty + 0.05` measuring -0.5546; and the triangle's two rounding half-widths sum to `roundAmount / 2`, so its equivalent width is `0.5 * roundAmount`. Comment the "recompute, never cache" rule with its forward consequence: `dutySq`, `valley` and `wPl` all move with `character` NOW, and move again PER SAMPLE once Phase 34's drift starts writing the spread fields — a cached site table would desynchronise silently then.

SECTION C — THE FIXED NINE-SITE UNION (D-04 / AA-01 / AA-02). Build four FUNCTION-LOCAL arrays of nine elements each — `pos`, `mag`, `wid` and a `kind` selector. Function-local `const` is mandatory: an in-class constant table indexed at runtime is a DECLARATION only under C++11, so runtime indexing odr-uses it and the MinGW linker fails with an undefined reference. That exact class got v2.0.0 rejected from the VCV Library. The nine entries, in order:

1. position `0.f`, magnitude `W[2] * 2.f + hardSq + hardPl`, width `0.f`, value-step. This is the coincident wrap: the saw's `1 - 2*phase` reset, the square's rectangle flip and the pulse's rectangle flip all land here.
2. position `0.f`, magnitude `W[3] * 2.f * c`, width `wSq`, value-step. The square's soft edge, whose hyperbolic-tangent zero-crossings sit at 0 and at `duty`.
3. position `0.f`, magnitude `W[4] * 2.f * c`, width `wPl`, value-step. The pulse's soft edge at 0.
4. position `0.5f`, magnitude `-hardSq`, width `0.f`, value-step. THE SQUARE'S HARD STEP, at `0.5f` and NOT at `dutySq`.
5. position `dutySq`, magnitude `W[3] * (-2.f) * c`, width `wSq`, value-step. The square's soft edge at its duty.
6. position `pulseDuty`, magnitude `-hardPl`, width `0.f`, value-step.
7. position `pulseDuty`, magnitude `W[4] * (-2.f) * c`, width `wPl`, value-step. Coincident with entry 6 because the frozen pulse derives both from the same duty.
8. position `0.f`, magnitude `W[1] * (-triBrk)`, width `wTri`, SLOPE-BREAK. The triangle's peak.
9. position `valley`, magnitude `W[1] * (+triBrk)`, width `wTri`, SLOPE-BREAK. The triangle's valley.

Comment entries 4 and 5 with P-2 in full, because the spectral gate cannot see this bug and a later agent will otherwise "simplify" it back. The frozen square derives its hard rectangle from `phase < 0.5f` but centres its hyperbolic tangent on `duty / 2` with half-width `duty / 2`, so the soft edge sits at `duty` — a DIFFERENT place. D-04's prose says "the square duty edge", singular; the code has two. If the hard-step correction is placed at `duty`, then for every sample landing between `0.5` and `duty` the naive path has already flipped while the correction still thinks the edge is ahead, and it injects about half the jump. MEASURED: the output magnitude envelope rises from 1.1047 to 1.96 — from plus-or-minus 5.52 V to plus-or-minus 9.78 V — at EVERY sample rate, while the spectral metric shows 0.0 dB difference across the whole grid. The damage peaks near `character` 0.71, mid-knob. The pulse does NOT split, because the frozen pulse derives both its branch and its soft edge from the same duty.

Comment entry 1 with P-4, as a falsified premise corrected in place. D-03's corollary states that as character rises the saw wrap's effective step shrinks and its correction shrinks with it. IT DOES NOT. The curved saw evaluates to 1 at phase 0 before the reset is applied, and the reset blends FROM a reset value of 1 TOWARD the curved saw, so at phase 0 both are 1 — the wrap jump is `+2.000000` at character 0, 0.25, 0.5, 0.75 and 1.0, measured to six decimals. The saw site therefore has width 0 and factor exactly 1 at every character, which is why the saw is the only shape whose correction is character-independent, improving about 9 dB at every character. What the reset ACTUALLY does is force the slope to zero just after phase 0 — a first-derivative break sitting on top of a value step — whose slope-break magnitude is about `4*dt*dt/6` against the value step's roughly 1, i.e. about three and a half orders of magnitude smaller at `dt = 0.02`. Ignore it. D-03's CONCLUSION survives; only its stated premise is wrong, and the corrected premise is recorded here so no later phase inherits the falsified one.

SECTION D — PLACEMENT (D-07 / D-13 / P-3). Loop `i` from 0 to 8 over the arrays. Skip when the magnitude is exactly `0.f` — that is D-04's "magnitudes fall to zero when a shape carries no weight", and it is what makes a fixed site union cost nothing at morph positions where a shape is absent.

THE CROSSING TEST, and it must be written exactly this way:
- SIDE comes from the FLOAT: `if (!(pos[i] > p)) d += 1.0;`, where `p` is the SAME float handed to `Waveshape::morphedWave` this sample. The frozen branches compare against that float with a strict comparison, and if the correction's side decision disagrees with the frozen branch — which it will whenever the site and the phase differ by less than about 6e-8 — the correction is applied on the WRONG SIDE and the error is the FULL jump.
- DISTANCE comes from the DOUBLE: `double d = (double)pos[i] - phase;` before the side adjustment, where `phase` is the double accumulator. It advances by exactly the phase increment each sample, so the distance decreases by exactly `dt` and each site fires EXACTLY ONCE PER CYCLE. Deriving the distance from the float makes the per-sample interval tiling ragged at the unit-in-last-place level, and a ragged tiling either misses an edge or double-fires it — both half-jump errors.
- Then `const float s = (float)(d / dt);` and skip when `!(s <= 1.f)`, negated so a not-a-number `s` is skipped rather than fired.

Record the measurement that settles it: a pure-double side test produced a systematic full-amplitude spike, with the envelope rising from 1.957 to 2.145, whenever the phase grid resonated with a site position — reproduced at `dt = 0.0005` with a pulse duty of 0.374, exactly 748 samples per edge. Record the three rejected shortcuts: recomputing the second half at the next sample from the then-current phase (valid for a fixed-frequency saw, wrong here, because under audio-rate MORPH and FM the jump magnitude, the site position and `dt` have all moved by then — the accumulator uses ONE consistent set of values for both halves); a one-sample output delay buffer (D-13); and widening the fire gate above 1 to "catch misses" (it creates double-fires on the following sample instead).

THE KERNELS. Compute `k = morphBlepCharFactor(wid[i], fdt)` and skip when it is exactly `0.f`. Let `u = 1.f - s`. For a value-step site: `h = mag[i] * k`, then `now += h * 0.5f * u * u` and `pending += h * (-0.5f) * s * s`. For a slope-break site: `g = mag[i] * fdt * k` — the `fdt` converts a slope expressed per unit phase into a slope per sample — then `now += g * u * u * u * (1.f/6.f)` and `pending += g * s * s * s * (1.f/6.f)`.

Both branches use `+=`, never assignment. Comment that with D-07 and AA-03 together: at C8 a 5-percent-duty pulse is about 0.57 samples wide and at C9 about 0.26 samples wide, so both of its edges land inside one sample; each gets its own sub-sample position and the two opposite-sign corrections SUM. When the duty is narrower than `dt` they partially cancel, and that cancellation is the PHYSICALLY CORRECT band-limited answer — a pulse narrower than a sample genuinely carries less energy. Flooring the effective duty at `dt` was rejected: it introduces a sample-rate-dependent timbre change the frozen `Waveshape` knows nothing about, so the naive and band-limited paths would stop being the same waveform, which would also invalidate D-08's before-and-after comparison.

Return `now`.
  </action>
  <verify>
    <automated>make strict && make guards && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make strict` exits 0 and prints `strict C++11 gate: PASS`
    - `make guards` exits 0 and prints `guard suite: PASS`
    - `make test` exits 0 with an unchanged case count (nothing calls `step()` yet — plan 32-06 wires it)
    - A standalone C++11 pedantic syntax probe compiles the header cleanly: `T=$(mktemp -d); printf '#include "dsp/MorphBlep.hpp"\nint main(){return 0;}\n' > "$T/mb.cpp"; c++ -std=c++11 -pedantic-errors -fsyntax-only -Wall -Wextra -Isrc "$T/mb.cpp" && echo PROBE_OK` prints `PROBE_OK`
    - `grep -v '^[[:space:]]*//' src/dsp/MorphBlep.hpp | grep -c 'std::'` outputs `0` — every expression is addition, subtraction, multiplication, division and comparison (AA-05, table-free closed form)
    - `grep -cE '^[[:space:]]*(static|extern)[[:space:]]' src/dsp/MorphBlep.hpp` outputs `0` (CORE-03 / D-14: no file-scope mutable state)
    - `grep -cE 'if \(!\(pos\[i\] > p\)\)' src/dsp/MorphBlep.hpp` outputs `1` — the float-sourced SIDE decision
    - `grep -cE '\(double\)pos\[i\] - phase' src/dsp/MorphBlep.hpp` outputs `1` — the double-sourced DISTANCE
    - `grep -cE 'if \(!\(fdt > 0\.f\)\) return now;' src/dsp/MorphBlep.hpp` outputs `1`
    - Comment-stripped source contains exactly one `0.5f` site position and one `dutySq` site position on separate array elements: `grep -v '^[[:space:]]*//' src/dsp/MorphBlep.hpp | grep -c 'dutySq'` is at least 2
    - `git status --porcelain src/dsp/FROZEN.sha256` is empty; `git diff --name-only` lists neither `src/dsp/Waveshape.hpp`, `src/dsp/RackCompat.hpp` nor `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>`forge::MorphBlep::step` computes the replicated weight vector, the live geometry, the fixed nine-site union and the two residuals, with the float-side / double-distance split and the square's hard-versus-soft position split each carrying the measurement that makes them non-negotiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `forge::VcoCore::step` -> `forge::MorphBlep::step` | The caller supplies `phase`, `p`, `dt`, `morph` and `character`. `dt` derives from `forge::VcoInputs::sampleTime`, which Rack does not sanitise and which the headless harness may set to anything. |
| `forge::MorphBlep` -> the frozen `forge::Waveshape` field set | This header reads `wv.bleedSpread`, `wv.squareDutySpread`, `wv.pulseEdgeSpread` and `wv.triAsymmetrySpread` and duplicates knowledge of the frozen internals; the byte pin is what makes that safe. |
| Phase 33 -> `MorphBlep::addStep` | An externally supplied `(sub-sample position, value jump)` pair enters the same accumulator as the morph sites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-02 | Denial of Service | division by `dt` in `morphBlepCharFactor` and in the sub-sample position | high | mitigate | Negated-comparison guards at both divisors: `if (!(u > 0.f)) return 0.f;` and `if (!(fdt > 0.f)) return now;`. MorphBlep does not rely on its caller (P-14). There is NO division by an edge width anywhere; the optional pulse-reach refinement that would add one is deliberately deferred. |
| T-32-04 | Tampering | out-of-bounds indexing of `W[5]` and the four nine-element site arrays | medium | mitigate | `segment` is bounded above by 3 mirroring the frozen minimum, so `W[segment + 1]` cannot exceed index 4; ring indices use `% 5` on a provably non-negative sum; the site arrays are fixed at nine elements with a literal loop bound of nine and no runtime-computed index. |
| T-32-05 | Denial of Service | in-class constant table odr-used at runtime -> MinGW undefined reference | high | mitigate | Site arrays are FUNCTION-LOCAL `const`, never in-class constants indexed at runtime. Gated by `make strict` locally and by the CI MinGW compile-and-link leg, which the canary include activated in Task 1 is what carries this header into. This is the exact class that got v2.0.0 rejected from the VCV Library. |
| T-32-16 | Tampering (signal integrity) | a full-amplitude single-sample spike invisible to the spectral gate | high | mitigate | The square's hard step is placed at `0.5f` and its soft edge at `dutySq` as SEPARATE site entries (P-2), and the crossing test takes its side from the float and its distance from the double (P-3). Both defects measure 0.0 dB on the spectral metric and are caught only by the output-magnitude envelope asserted in plan 32-08. |
| T-32-17 | Tampering | `addStep` accepting an out-of-range or non-finite sync event from Phase 33 | medium | mitigate | The entry gate is `if (!(xAhead >= 0.f) || xAhead > 1.f) return;` — negated first, so a not-a-number is rejected rather than accumulated into per-instance state that would then poison every following sample. |
| T-32-18 | Elevation of Privilege (voice isolation) | shared mutable band-limiting state across voices | high | mitigate | Both accumulator members are NSDMI struct fields held by value; the header contains no file-scope `static` or `extern`, asserted by grep, and the behavioral proof is `tests/test_vco_core.cpp`'s interleave invariant plus its permanent shared-state positive control. |
| T-32-12 | Tampering | the shipped Analog LFO's golden bit-stability | high | mitigate | No frozen header is edited; `src/dsp/FROZEN.sha256` is unbumped; `src/AnalogLFO.cpp` is absent from the diff; `make test` replays the six `.f32` goldens on every run. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. Every dependency is in-tree, frozen, or standard library, so no `[ASSUMED]`/`[SUS]` install checkpoint applies. |
</threat_model>

<verification>
- `make strict` green — the C++11 pedantic gate now covers `src/dsp/MorphBlep.hpp` through the activated canary include, with zero Makefile or CI wiring added.
- `make guards` green with `check_canary.sh` [5/5] reporting the header carried, `check_includes.sh` [2/7] and [3/7] reporting it Rack-free and sibling-only, and `check_frozen.sh` reporting the 15-entry manifest unchanged.
- `make test` green with an unchanged case count — nothing calls `step()` until plan 32-06.
- A standalone `-std=c++11 -pedantic-errors -fsyntax-only` probe compiles the header on its own.
- No frozen header edited, no `FROZEN.sha256` bump, `src/AnalogLFO.cpp` absent from the diff.
</verification>

<success_criteria>
- `src/dsp/MorphBlep.hpp` exists with `forge::morphBlepCharFactor` and `struct forge::MorphBlep` (`pending`, `inject`, `reset`, `addStep`, `step`).
- The nine-site union, the split-source crossing test, the compact-support factor and the two residuals are implemented with their measured justifications written beside them.
- Three falsified premises are corrected in place in the source: the over-correction-is-safe argument, the wrong polyBLAMP snippet, and the saw soft-reset corollary.
- The canary carries the header into both C++11 gates in the same commit that created it.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-04-SUMMARY.md` when done. Record: the header's line count; the guard output lines proving [5/5], [2/7] and [3/7] cover it; the standalone C++11 probe result; and the three falsified premises corrected, each with the source line where the correction now lives.
</output>

---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 06
type: execute
wave: 4
depends_on: ["32-04", "32-03"]
files_modified:
  - src/dsp/VcoCore.hpp
  - tests/test_vco_spectrum.cpp
autonomous: true
nyquist_compliant: true
requirements: [CORE-02, MORPH-01, AA-01]
must_haves:
  truths:
    - "CORE-02 / MORPH-01: forge::VcoCore holds a forge::MorphBlep BY VALUE beside its Waveshape and swaps its single morphedWave call at :484 for naive-plus-correction; the frozen Waveshape is still CALLED, never edited, and the surrounding pitch/Nyquist/accumulator sequence does not move."
    - "P-3: the SAME float p handed to Waveshape::morphedWave is handed to MorphBlep::step, and the double phase accumulator supplies the distance — that identity is load-bearing, not stylistic."
    - "CORE-03 / D-14: the MorphBlep member is per-instance state held by value, never static and never shared."
    - "D-15 corrected rationale, recorded in place: src/dsp/VcoCore.hpp:446's claim that Phase 32's oversampled inner loop is the obvious future caller decoupling sampleTime from sampleRate is FALSIFIED by AA-05, which forbids oversampling in v2.0. The conclusion survives on the real reason — this phase introduces division by dt."
    - "T-32-01: the MORPH CV jack declared in plan 32-02 makes VcoCore.hpp:361-362's claim that the comparison-ladder helper is still the right tool for morph and character FALSE; the morph and character conditioning is hardened with the negated-comparison idiom and the sentence is corrected in place."
    - "D-08 inversion: tests/test_vco_spectrum.cpp's naive-equals-core tombstone is INVERTED in place into a proof that the divergence is EXACTLY the MorphBlep correction and nothing else moved."
  artifacts:
    - src/dsp/VcoCore.hpp
    - tests/test_vco_spectrum.cpp
  key_links:
    - "tests/check_canary.sh [2b/5]'s step matcher is UNANCHORED: the `struct VcoCore` line and the `float step(...)` line must each stay on ONE line with their opening brace, and the full step signature must never be quoted in a comment on a line that also contains a brace."
    - "forge::VcoCore::blep is the seam Phase 33's hard sync plugs into through MorphBlep::addStep."
---

<objective>
Wire `forge::MorphBlep` into `forge::VcoCore::step` at the single `morphedWave` call site, harden the morph and character conditioning against the CV jack plan 32-02 just added, correct two falsified sentences in place, and invert the D-08 baseline tombstone into a proof that the core and the naive mirror now differ by EXACTLY the correction.

Purpose: this is the one line of the phase where band-limiting becomes audible. Everything before it built apparatus; everything after it measures the result.

Output: a `forge::MorphBlep blep;` member, a two-line call-site change, two corrected premises, and an inverted tombstone.
</objective>

<!-- planner-discipline-allow: Approx -->
<!-- planner-discipline-allow: oversampled inner loop -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md
@src/dsp/VcoCore.hpp
@src/dsp/MorphBlep.hpp
@tests/test_vco_spectrum.cpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — `float pending`, `float inject`, `void reset()`, `void addStep(float, float)`, `float step(const Waveshape&, double, float, double, float, float)`
- `forge::VcoCore::blep` — a `forge::MorphBlep` held BY VALUE beside `wave`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror` (gaining `lastNaive`, `lastP`, `lastDeltaPhase`, `lastMorph`, `lastCharacter`), `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: `forge::VcoCore::blep` and the five recording members on `NaiveVcoCoreMirror`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hold MorphBlep by value, swap the :484 call site, harden morph and character, and correct two falsified sentences</name>
  <files>src/dsp/VcoCore.hpp</files>
  <behavior>
    - `forge::VcoCore::step` returns `5.f * (morphedWave(p, morph, character, 0.f) + blep.step(wave, phase, p, deltaPhase, morph, character))` — the correction is additive and the frozen call is unchanged
    - a not-a-number `morph` or `character` arriving in `forge::VcoInputs` lands at `0.f` rather than passing through into the frozen float-to-int cast
    - every existing `tests/test_vco_core.cpp` and `tests/test_vco_pitch.cpp` case that does not assert on output magnitude stays green — the pitch chain, the Nyquist guard, the interleave independence and the spread divergence are all untouched
    - two `forge::VcoCore` instances stepped interleaved still reproduce their solo blocks bit-exactly, because the new member is per-instance
  </behavior>
  <read_first>
    - src/dsp/VcoCore.hpp in full — the banner's source-shape contract at :34-43, the C++11 rule block at :55-80, the includes at :82-90, the per-instance-state comment at :243-249, the comparison-ladder rejection paragraph at :357-364, the falsified sentence at :440-446, and the call site at :476-492
    - src/dsp/MorphBlep.hpp in full — the `step` signature, the accumulator semantics and the per-instance constraint
    - src/dsp/Waveshape.hpp lines 158-172 — `morphedWave`'s `(int)scaled` cast, the undefined behavior a not-a-number `morph` would reach
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "The VcoCore.hpp:484 call site" and § Common Pitfalls P-3
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md § "src/dsp/VcoCore.hpp (MODIFIED — DSP core, request-response)" — the source-shape hard constraint and the comment-density pattern
    - .planning/STATE.md § Accumulated Context — the Phase 30 decision recording that the canary's step matcher is UNANCHORED and that quoting the full signature in a comment on a line containing a brace makes `make guards` hard-fail with unrelated errors
  </read_first>
  <action>
Four edits to `src/dsp/VcoCore.hpp`, none of which may move the pitch, Nyquist or accumulator sequence.

EDIT 1 — THE INCLUDE. Add `#include "dsp/MorphBlep.hpp"` beside the existing three, with a trailing comment naming the symbol it supplies in the same shape as the `Waveshape.hpp` line at :89. Include-what-you-use is the rule this file's own comment at :84-87 states.

EDIT 2 — THE MEMBER. Add `MorphBlep blep;` immediately after `Waveshape wave;` in the per-instance state block at :243-249, and extend that block's existing comment rather than writing a second one. The extension must say: all three members — the double phase accumulator, the `Waveshape` and now the `MorphBlep` — are INSTANCE state, not static and not shared, which is literally what CORE-03 asserts and what D-14 binds by name. Name the consequence: two cores stepped interleaved must not see each other, and the band-limiting accumulator is now part of what that claim covers, so `tests/test_vco_core.cpp`'s interleave invariant and its permanent shared-state positive control are the behavioral proof rather than a grep.

EDIT 3 — THE CALL SITE AT :476-492. Keep `p`, `morph` and `character` computed once, keep `tel.displayPhase = p;` where it is, keep the `x5` where it is, and keep the D-13 "returned UNCONDITIONED" paragraph intact — Phase 34 still owns the output stage. Replace the single naive assignment with a named `naive` local holding the frozen call's result, then a `sample` local holding `naive + blep.step(wave, phase, p, deltaPhase, morph, character)`.

Comment the new line with the three facts that make it correct, at the comment density this file already uses:
- THE SAME `p`. The float handed to `MorphBlep::step` is the SAME float handed to `Waveshape::morphedWave`, and the double `phase` accumulator is handed over separately. That split is load-bearing (P-3), not stylistic: the correction's SIDE decision must agree with the frozen branch's own strict float comparison — disagreement costs the FULL jump — while its DISTANCE must come from the double, because the double advances by exactly the phase increment and makes each site fire exactly once per cycle. MEASURED: a pure-double side test produced a systematic full-amplitude spike, the envelope rising from 1.957 to 2.145, whenever the phase grid resonated with a site position.
- ADDITIVE, NOT REPLACING. `morphedWave` is still CALLED, exactly once, exactly as before (CORE-02). The correction is a separate additive term. `bleedLfo` stays `0.f` — Phase 34 passes the real layer-0 state.
- WHAT THE CORRECTION IS. Morph-aware polyBLEP at the value-step sites and polyBLAMP at the triangle's slope corners, scaled by the morph AND bleed weights and by the characterized jump, all of it closed-form and table-free (AA-01..05). Point at `src/dsp/MorphBlep.hpp` for the derivation rather than restating it here.

EDIT 4 — THE TWO FALSIFIED SENTENCES, CORRECTED IN PLACE (the house rule: keep the conclusion, replace the premise, and say the old reason was falsified).

(4a) At :440-446, the paragraph explaining why the increment bound cannot be inferred from the frequency guard ends with a claim that Phase 32's oversampled inner loop is the obvious future caller that decouples `sampleTime` from `sampleRate` on purpose. THAT PREMISE IS FALSIFIED: AA-05 forbids oversampling in v2.0 by name, so no such loop exists in this phase and none will be added. The CONCLUSION is untouched — nothing in `forge::VcoInputs` couples the two fields, the MEASURED 16.7 increment and 314,880 phase at `sampleRate = 44100` with `sampleTime = 1/1000` and `pitchCV = +6` still stands, and the bound is still required. Replace the falsified sentence with the real reason the decoupled case matters now: the two fields are independent POD fields that any caller may set independently, the headless harness does exactly that in scenario four of `tests/test_vco_core.cpp`, and Phase 32 has just introduced division by `dt` — in the character factor and in the sub-sample position — so a decoupled, zero, subnormal or non-finite `sampleTime` now reaches arithmetic that did not exist before. Say in the comment that the old reason was falsified, and by what.

(4b) At :357-364, the paragraph rejecting the comparison-ladder helper by name ends with a parenthetical stating that the helper is still the right tool for morph and character further down, where the inputs are already finite. THAT PREMISE IS NOW FALSE: plan 32-02 declared a MORPH CV jack, Rack does not sanitise cable voltages, and `morph` is therefore no longer guaranteed finite. Replace it with the corrected statement AND change the code to match: condition `morph` and `character` with the NEGATED-COMPARISON PAIR, negated line first, exactly as the pitch-volt bound above them is written, instead of the comparison-ladder helper. State what the guard protects: the frozen `Waveshape::morphedWave` computes `morph * 4.f` and casts the result to `int`, and a float-to-int cast of a not-a-number is undefined behavior; the frozen header cannot change, so the bound is LOCAL to this core, on this side of the call — the same argument and the same placement as the `kVcoMaxPitchVolts` bound ahead of the frozen exponential. State why the core guards even though plan 32-02's shell also does: the core must not rely on its caller, and the headless harness builds `forge::VcoInputs` directly with no shell in the way. State the bit-identity: for every finite input the negated pair returns exactly what the ladder returned, so no existing measurement can move; the two differ only for a not-a-number, where the ladder was inert. Note that `character` is hardened alongside `morph` even though its CV jack is Phase 34's (CHAR-01), because the guard is the same shape for the same reason and splitting them would leave a half-guarded pair.

HARD CONSTRAINTS while editing. The `struct VcoCore {` line and the `float step(...) {` line must each stay on ONE line together with their opening brace — `tests/check_canary.sh` [2b/5] line-matches both patterns to build a perturbed copy, and Allman braces, a wrapped parameter list or an added exception specification make `make guards` hard-fail with "could not perturb src/dsp/VcoCore.hpp", which is a guard error, not a DSP error. The step matcher is UNANCHORED, so never quote the full step signature verbatim in a comment on a line that also contains a brace: the canary then perturbs the COMMENT and the guard fails with unrelated compile errors. Keep the abbreviated form the banner already uses.
  </action>
  <verify>
    <automated>make strict && make guards && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make strict` exits 0 and prints `strict C++11 gate: PASS`
    - `make guards` exits 0 and prints `guard suite: PASS` — in particular `check_canary.sh` does NOT report "could not perturb src/dsp/VcoCore.hpp"
    - `grep -c 'MorphBlep blep;' src/dsp/VcoCore.hpp` outputs `1`
    - `grep -c 'blep.step(wave, phase, p, deltaPhase, morph, character)' src/dsp/VcoCore.hpp` outputs `1`
    - `grep -c 'wave.morphedWave(p, morph, character, 0.f)' src/dsp/VcoCore.hpp` outputs `1` — still exactly one call into the frozen header
    - `grep -cE 'if \(!\(morph > 0\.f\)\)' src/dsp/VcoCore.hpp` outputs `1` and `grep -cE 'if \(!\(character > 0\.f\)\)' src/dsp/VcoCore.hpp` outputs `1`
    - The falsified premise is REPLACED rather than deleted: `grep -c 'AA-05 forbids oversampling' src/dsp/VcoCore.hpp` is at least 1, and `grep -ciE 'falsified' src/dsp/VcoCore.hpp` is at least 2 (one for each of the two corrected sentences)
    - `./build-test/test -tc="vco core: two-instance independence*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed — the new member did not break CORE-03
    - `./build-test/test -tc="vco pitch*" -s 2>&1 | tail -4` reports 0 failed — the pitch chain is untouched
    - `make -j4` exits 0 and produces `plugin.dylib`
    - `git status --porcelain src/dsp/FROZEN.sha256` is empty; `git diff --name-only` lists neither `src/dsp/Waveshape.hpp`, `src/dsp/RackCompat.hpp` nor `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>`forge::VcoCore` band-limits its output through a per-instance `forge::MorphBlep`, conditions morph and character NaN-safely, and carries two corrected premises in place of two falsified ones — with the frozen `Waveshape` still called exactly once and never edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invert the D-08 baseline tombstone — the divergence IS the correction, exactly</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <behavior>
    - for every grid point, `forge::VcoCore`'s sample equals `5.f * (mirror's pre-scale naive value + a locally held forge::MorphBlep's correction for the same phase, p, dt, morph and character)` BIT-EXACTLY — the two paths differ by the correction and by nothing else
    - at least 12 of the 15 grid points at each sample rate now show at least one differing sample, so the naive-equals-core claim is genuinely false
    - the saw-centre grid point at character 0 shows differing samples, and the sine-centre point at character 0 shows NONE — a sine at character 0 has no discontinuity and no bleed ring, so its correction is exactly zero
  </behavior>
  <read_first>
    - tests/test_vco_spectrum.cpp in full — `NaiveVcoCoreMirror`, its banner, and the tombstone case with its inversion contract naming this plan
    - src/dsp/VcoCore.hpp lines 470-493 as edited by Task 1 — the exact order of the phase update, the `p` cast, the conditioning pair, the frozen call and the correction
    - src/dsp/MorphBlep.hpp — the `step` signature and its argument order
    - .planning/STATE.md § Accumulated Context — the Phase 30 decision recording that the Phase-29 silence tombstone was INVERTED in place, same slot, still the same case count, and OBSERVED red against a silenced core
  </read_first>
  <action>
Extend `NaiveVcoCoreMirror` with five public recording members, set on every `step()` and documented as RECORDING ONLY — they change no arithmetic and are not a second divergence: `float lastNaive` (the pre-scale value the frozen call returned), `float lastP`, `double lastDeltaPhase`, `float lastMorph`, `float lastCharacter`. Mirror Task 1's conditioning change too: the mirror's morph and character conditioning must move to the negated-comparison pair, because the mirror-maintenance rule in its own banner requires it to track the real core's guard sequence, and a mirror that quietly kept the ladder would differ from the core in TWO things rather than the one its banner promises.

Then INVERT the tombstone case IN PLACE — same slot, same grid, same seeds, same three sample rates. Rename it to `"vco spectrum: the core now DIVERGES from NaiveVcoCoreMirror by EXACTLY the MorphBlep correction (D-08 inversion, was the baseline-validity tombstone)"`.

For each of the 45 grid points (three rates by five morph centres by three characters), drive 4096 samples of `forge::VcoCore` through `forge::VcoBlockDriver` and 4096 samples of `NaiveVcoCoreMirror` through the same local loop, in lockstep. After each mirror step, call a LOCALLY HELD `forge::MorphBlep` — one per grid point, reset between points — as `localBlep.step(mirror.wave, mirror.phase, mirror.lastP, mirror.lastDeltaPhase, mirror.lastMorph, mirror.lastCharacter)`, and form the reconstruction `5.f * (mirror.lastNaive + correction)` with the two operations in exactly that order. Then:

- `CHECK` the reconstruction equals the core's sample with a direct float `!=` comparison, never an approximate one, and count mismatches. This is the strong claim: the divergence is the correction and nothing else moved. `-ffp-contract=off` is what makes it bit-reproducible, and the comment must say so.
- Count grid points where the core and the mirror differ on at least one sample, and `CHECK` that count is at least 12 of 15 at each rate.
- `CHECK` specifically that the saw-centre point at character 0 has a non-zero differing count, and that the sine-centre point at character 0 has a differing count of exactly zero. Comment the second one: a sine at character 0 has no discontinuity of its own AND no bleed ring (the bleed block is gated on `character >= 0.001f`), so its correction is exactly zero and the two paths must remain identical there. That is the control that keeps the divergence assertion from being satisfied by noise.
- Keep the non-vacuity `REQUIRE` from the original case: at least 90 percent of the core's samples are non-zero and the block is not constant.

Rewrite the case banner to record the inversion rather than to erase it, in the Phase-29-tombstone voice: this case was, until this plan, the assertion that the mirror and the core were BIT-IDENTICAL — the proof that the D-08 baseline is a faithful copy. Plan 32-06 landed `forge::MorphBlep` at the call site, so that claim became false BY DESIGN, and the case was inverted in the same slot rather than deleted. State what the inverted form buys that the old one did not: it proves the baseline mirror is STILL a faithful copy of everything except the correction, which is the precondition the no-regression invariant in plan 32-07 rests on. State the standing warning: if the reconstruction mismatch count ever becomes non-zero, something OTHER than `MorphBlep` changed in `forge::VcoCore::step` and the naive baseline has silently stopped being a baseline — stop and report it rather than loosening the comparison.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco spectrum: the core now DIVERGES*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output shows a reconstruction mismatch count of `0` at all three sample rates
    - The `-s` output shows the diverging-point count at 12 or more of 15 at each rate, the saw-centre character-0 point with a non-zero differing count, and the sine-centre character-0 point with a differing count of exactly `0`
    - `grep -c 'D-08 inversion' tests/test_vco_spectrum.cpp` outputs `1`
    - Comment-stripped source uses no approximate comparator: `grep -v '^[[:space:]]*//' tests/test_vco_spectrum.cpp | grep -c 'Approx'` outputs `0`
    - The doctest case count is unchanged from this plan's starting baseline — the tombstone was INVERTED in place, not deleted and not added alongside
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_spectrum.cpp` for this task, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The baseline tombstone is inverted in place and now proves the core and the naive mirror differ by exactly the `MorphBlep` correction, with a zero-correction control at the sine centre keeping the divergence claim honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `forge::VcoInputs` -> `forge::VcoCore::step` | Every field is externally supplied. `morph` now carries a patch-cable voltage through plan 32-02's jack; `sampleTime` and `sampleRate` are set independently by the harness and by Rack. |
| `forge::VcoCore` -> the frozen `forge::Waveshape` | The frozen header casts `morph * 4.f` to `int`; it cannot change, so the bound must sit on this side of the call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | the frozen `(int)(morph * 4.f)` cast reached through the new MORPH CV jack | high | mitigate | Negated-comparison conditioning pair for BOTH `morph` and `character` inside `forge::VcoCore::step`, negated line first so a not-a-number lands at `0.f`. The comparison-ladder helper's parenthetical claiming it is still adequate here is corrected in place, because the CV jack falsified it. Bit-identical for every finite input, so no existing measurement can move. |
| T-32-02 | Denial of Service | division by `dt` newly reachable from `forge::VcoInputs::sampleTime` | high | mitigate | `MorphBlep` carries its own negated `dt` guard and does not rely on this caller (P-14); the corrected `:440-446` paragraph records that this phase is what put a divisor behind that field. Plan 32-09 extends the hostile-timing grid to the classes that now matter. |
| T-32-18 | Elevation of Privilege (voice isolation) | the new `blep` member | high | mitigate | Held BY VALUE beside `wave` inside the per-instance state block; no static, no global. The interleave invariant and its permanent shared-state positive control in `tests/test_vco_core.cpp` are re-run as an acceptance criterion of this plan. |
| T-32-19 | Tampering (evidence integrity) | the D-08 naive baseline silently ceasing to be a baseline | high | mitigate | The inverted tombstone reconstructs the core's sample from the mirror plus a locally held `MorphBlep` and requires bit-exact agreement. A non-zero mismatch means something other than `MorphBlep` changed in `step()`, and the standing instruction is to stop and report rather than loosen the comparison. |
| T-32-20 | Denial of Service | `make guards` hard-failing on the source-shape contract | medium | mitigate | The `struct VcoCore` and `float step(...)` lines stay on one line with their brace; the full step signature is never quoted in a comment on a line containing a brace, because `check_canary.sh` [2b/5]'s matcher is unanchored and would perturb the comment instead. |
| T-32-12 | Tampering | the shipped Analog LFO's golden bit-stability | high | mitigate | No frozen header edited, `FROZEN.sha256` unbumped, `src/AnalogLFO.cpp` absent from the diff, and `make test` replays the six `.f32` goldens on every run. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. |
</threat_model>

<verification>
- `make strict`, `make guards`, `make test` and `make -j4` all green.
- `check_canary.sh` does not report a perturbation failure on `src/dsp/VcoCore.hpp`.
- The CORE-03 interleave invariant and its positive control are re-run and green with the new member in place.
- The pitch suite is green — the pitch, Nyquist and accumulator sequence did not move.
- The reconstruction mismatch count is 0 at all three sample rates.
- `src/AnalogLFO.cpp` absent from the diff; `FROZEN.sha256` unbumped.
</verification>

<success_criteria>
- `forge::VcoCore` holds `MorphBlep blep;` per instance and returns `5.f * (naive + correction)`.
- `morph` and `character` are conditioned with the negated-comparison pair; the ladder-is-still-fine parenthetical is corrected in place.
- The `:446` oversampled-inner-loop premise is replaced with the real reason, and the replacement says the old reason was falsified.
- The D-08 tombstone is inverted in the same slot and proves the divergence is exactly the correction.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-06-SUMMARY.md` when done. Record: the reconstruction mismatch counts per rate; the diverging-point counts; the sine-centre control's zero count; the doctest case and assertion counts before and after; and the exact source lines where each of the two falsified premises was replaced.
</output>

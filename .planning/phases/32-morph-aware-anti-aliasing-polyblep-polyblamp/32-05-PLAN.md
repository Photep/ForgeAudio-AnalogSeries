---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 05
type: tdd
wave: 4
depends_on: ["32-04"]
files_modified:
  - tests/test_morph_blep.cpp
autonomous: true
nyquist_compliant: true
requirements: [AA-02, AA-03, AA-04, AA-05]
must_haves:
  truths:
    - "AA-04 / D-01: the BLEP/BLAMP magnitude is the CHARACTERIZED jump — asserted by probing the FROZEN Waveshape at each site and comparing against what MorphBlep emits, so the analytic derivation is verified against the code it was derived from rather than against itself."
    - "D-03: morphBlepCharFactor is exactly 1 at zero width, exactly 0 at and beyond twice dt, monotone decreasing between, and continuous in slope at the cutoff."
    - "D-03b: ONE factor governs value-step and slope-break sites alike; the triangle's rounded peaks and the saw's soft reset are fast-but-continuous, and the factor is what makes them self-limiting."
    - "P-4 corrected premise, made permanent: the saw's soft reset does NOT soften the wrap — the jump is +2.000000 at every character, so the saw site's width is 0 and its factor is 1 always."
    - "AA-03 / D-07: at a 5-percent duty and a C9 phase increment both pulse edges fall inside one sample; each is placed at its own sub-sample position and the two opposite-sign corrections SUM into the accumulator rather than overwriting."
    - "D-13: the two halves of one correction sum to the full band-limited step across the pair of samples, at zero added latency."
    - "D-14: addStep feeds the SAME accumulator as the morph sites, so several events and several sites compose by summation; Phase 32 implements no sync behavior."
    - "D-15 / P-14: a zero, negative, subnormal or non-finite dt reaching MorphBlep::step returns the drained accumulator and nothing else."
    - "P-3: the crossing test's SIDE decision comes from the float p and its DISTANCE from the double accumulator. Taking the side from the double injects a systematic FULL-AMPLITUDE spike wherever the phase grid RESONATES with a site position — MEASURED at dt = 0.0005 with pulseDuty = 0.374, exactly 748 samples per edge. The spectral gate measures 0.0 dB difference for this defect, so it needs its own behavioral regression case."
  artifacts:
    - tests/test_morph_blep.cpp
  key_links:
    - "This TU is already exempt from check_includes.sh [1/7] — plan 32-01 pre-registered tests/test_morph_blep.cpp in VCO_SIDE_ALLOW before the file existed."
    - "Probing morphedWave is REJECTED for the runtime path on cost grounds (D-01) and is exactly right here: a test may spend transcendentals the audio thread cannot."
    - "The resonant-tiling case is the P-3 counterpart of Task 2 Part D's square-split probe: both defects are invisible to the spectral metric and visible only on the output envelope, so both get a dedicated case rather than relying on a general sweep."
---

<objective>
Assert, unit by unit, that `forge::MorphBlep`'s magnitudes are the actual characterized jumps of the frozen `forge::Waveshape`, that the D-03 factor hits its stated limits exactly, that overlapping edges sum, and that hostile timing reaches no divisor.

Purpose: the spectral gate in plan 32-07 measures a broadband average and is provably blind to two of this phase's three most likely defects — it shows 0.0 dB difference for a mis-placed square step that swings the output to plus-or-minus 9.78 V. Unit assertions are where those defects are visible. AA-04 in particular is a claim about magnitudes, and a spectral floor can be met with the wrong ones.

Output: `tests/test_morph_blep.cpp` — the AA-02/AA-03/AA-04 unit suite over `forge::morphBlepCharFactor` and `forge::MorphBlep`.
</objective>

<!-- planner-discipline-allow: Approx -->
<!-- planner-discipline-allow: DOCTEST_CONFIG_IMPLEMENT -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md
@src/dsp/MorphBlep.hpp
@src/dsp/Waveshape.hpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — `float pending`, `float inject`, `void reset()`, `void addStep(float, float)`, `float step(const Waveshape&, double, float, double, float, float)`
- `forge::VcoCore::blep`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: `tests/test_morph_blep.cpp` with `probeJump`, `probeSlopeBreak` and `driveOneSite`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The TU, and morphBlepCharFactor's exact limits and hostile inputs (D-03 / AA-04 / D-15)</name>
  <files>tests/test_morph_blep.cpp</files>
  <behavior>
    - `morphBlepCharFactor(0.f, dt)` is EXACTLY 1.0f at every dt in the tested set — a true hard step keeps full authority at every character
    - `morphBlepCharFactor(w, dt)` is EXACTLY 0.0f for every `w >= 2*dt`, not merely small — the exactness is the load-bearing property
    - the factor is strictly monotone decreasing over `w` in the open interval `(0, 2*dt)`
    - the factor is continuous in value AND in first difference at the cutoff: the numerical slope just inside the cutoff is within 1e-4 of zero, which the squared form gives and the un-squared form does not
    - a zero, negative, subnormal or not-a-number `dt` returns exactly 0.0f
    - a not-a-number `w` returns exactly 0.0f
  </behavior>
  <read_first>
    - src/dsp/MorphBlep.hpp in full — `morphBlepCharFactor` and its documentation block, and `struct MorphBlep`
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "Pattern 5: The D-03 character factor" including both calibration tables, and § Common Pitfalls P-1, P-14
    - tests/test_vco_core.cpp lines 76-112 — the TU/include shape and the neutral-base helper idiom
    - tests/test_vco_core.cpp lines 735-760 — the hostile-grid array idiom and the named-array convention
  </read_first>
  <action>
Create `tests/test_morph_blep.cpp` as a doctest TU that does NOT define the doctest impl macro. Include `"doctest.h"`, then `"dsp/MorphBlep.hpp"` and `"dsp/Waveshape.hpp"`, then `<cmath>` and `<limits>`, each with a trailing comment naming the one symbol it supplies. This TU is already exempt from `check_includes.sh` [1/7] — plan 32-01 pre-registered it before the file existed.

Write a file banner stating what this suite is and, more importantly, what it is FOR relative to the spectral gate: the alias-floor gate in `tests/test_vco_spectrum.cpp` measures a broadband average and is PROVABLY BLIND to two of this phase's three most likely defects. Give the number: placing the square's hard step at its soft edge's position swings the output envelope from plus-or-minus 5.52 V to plus-or-minus 9.78 V at every sample rate while showing 0.0 dB spectral difference across the whole grid. Unit assertions are where a wrong magnitude, a wrong site position and a wrong side decision are visible. State the standing organising principle this repository writes every suite against: each case is written against a SPECIFIC MEASURED trap that would otherwise have made it vacuous.

Add a `TEST_CASE` named `"morph blep: morphBlepCharFactor hits D-03's limits EXACTLY, is monotone between them, and is inert against hostile dt (D-03 / D-15)"`.

Part A — THE TWO EXACT LIMITS. Over `dt` in {1.0/44100, 1.0/48000, 1.0/96000, 0.02, 0.0949, 0.1897, 0.25, 0.495}: `CHECK(morphBlepCharFactor(0.f, dt) == 1.0f)` with a bit-exact float comparison, never an approximate one. Then over `w` in {2*dt, 2.5f*dt, 10.f*dt, 1.f}: `CHECK(morphBlepCharFactor(w, dt) == 0.0f)`, again bit-exact. Comment both with P-1: the EXACTNESS of the zero is what makes this factor safe. A factor that merely decays — a reciprocal-linear form, a reciprocal-quadratic form, a sinc-Pade fit — still returns roughly 0.05 to 0.3 on edges that are already several samples wide, and that residual step-shaped correction is broadband INJECTED energy, not a filter. Record the measured worst regressions versus the naive path: -60.4 dB at full authority, -42.7, -36.6 and -29.8 dB for the three decaying forms, against -1.7 dB for this one.

Part B — MONOTONICITY AND THE SLOPE ARGUMENT. For `dt = 0.02`, walk `w` from 0 to `2*dt` in 200 steps and `CHECK` each value is strictly less than the previous one. Then `CHECK` the numerical first difference just inside the cutoff is within 1e-4 of zero. Comment that this is the whole reason the exponent is 2 rather than 1: both sit on the same measured plateau (mean improvement 7.3 versus 7.4 dB, identical -1.7 dB worst regression), but the un-squared form has a SLOPE DISCONTINUITY at the cutoff, and under audio-rate MORPH and CHARACTER modulation that discontinuity is a per-sample STEP in the correction gain. Say that this assertion is what would fail if a later agent "simplified" the square away.

Part C — HOSTILE INPUTS (D-15 / P-14). Over `dt` in {0.f, -1.f/44100.f, the smallest positive subnormal float, positive infinity, negative infinity, a quiet not-a-number} and `w` in {0.f, 0.01f, a quiet not-a-number}: `CHECK` the returned value is exactly 0.0f in every case where the guard should fire, and in all cases `CHECK` the result is finite. `CAPTURE` `dt` and `w` on every point. Comment that this asserts the guard's SHAPE, not just its effect: the comparison is written negated so a not-a-number lands on the fallback branch, and a comparison-ladder helper would be inert here because both of its comparisons are false for a not-a-number (Phase 30 deferred item 3 / CR-02).
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="morph blep: morphBlepCharFactor*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - `grep -c 'DOCTEST_CONFIG_IMPLEMENT' tests/test_morph_blep.cpp` outputs `0`
    - Comment-stripped source uses no approximate comparator: `grep -v '^[[:space:]]*//' tests/test_morph_blep.cpp | grep -c 'Approx'` outputs `0` — the two limit assertions are bit-exact float comparisons
    - `make guards` exits 0; `bash tests/check_includes.sh` prints an OK line for `[1/7]` with the new file already exempt
    - `make strict` exits 0
    - `git diff --name-only` lists only `tests/test_morph_blep.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The D-03 factor's two exact limits, its monotonicity, its slope continuity at the cutoff and its inertness against hostile `dt` are all asserted, each with the measurement that makes it non-negotiable written beside it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The characterized jumps, probed against the FROZEN Waveshape (AA-04 / AA-02 / P-4)</name>
  <files>tests/test_morph_blep.cpp</files>
  <behavior>
    - at character 0, the probed value jump of `morphedWave` across phase 0 is `+2.000000` at the saw centre, at the square centre and at the pulse centre — matching site 1's analytic magnitude to six decimals
    - at character 0, the probed value jump across `0.5` at the square centre is `-2.000000`, and across the pulse duty at the pulse centre is `-2.000000`
    - at character 0, the probed SLOPE break of `morphedWave` at phase 0 at the triangle centre is `-8.000000` and at the valley is `+8.000000` — matching sites 8 and 9's `triBrk`
    - the saw wrap's probed jump is `+2.000000` to six decimals at character 0, 0.25, 0.5, 0.75 AND 1.0 — the P-4 corrected premise, made permanent
    - at the sine centre with character 0.5, the probed jump across phase 0 is 0.0148 within 1e-4, the bleed ring's own step inside what the user hears as a pure sine
    - at character 1 the square's probed jump across `0.5` differs from its probed jump across `dutySq`, proving the two positions are genuinely distinct
  </behavior>
  <read_first>
    - src/dsp/Waveshape.hpp in full — every branch the probes bracket
    - src/dsp/MorphBlep.hpp — the nine-site table and its documented magnitudes
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "Pattern 3: The site map" including the six derived quantities, § "Pattern 4: Weight algebra", and § Common Pitfalls P-2, P-4, P-12
    - tests/test_vco_core.cpp lines 114-149 — the analysis-helper banner shape: state why the helper is structurally sound rather than heuristic, over what grid it was validated, and name the simplification that must not be reintroduced
  </read_first>
  <action>
Add two helpers to the anonymous namespace of `tests/test_morph_blep.cpp`:

`double probeJump(const forge::Waveshape& wv, float sitePhase, float morph, float character, float eps)` — return `morphedWave(sitePhase + eps, ...) - morphedWave(wrapped(sitePhase - eps), ...)`, where `wrapped` adds 1 when the argument goes negative so a site at phase 0 is probed across the wrap. Use `eps = 1e-4f`.

`double probeSlopeBreak(const forge::Waveshape& wv, float sitePhase, float morph, float character, float eps)` — return the one-sided slope after the site minus the one-sided slope before it, each estimated from a two-point difference over `eps` in units of phase, with the same wrap handling.

Banner both helpers with the structural argument and the trap: `eps` must be large enough that the difference is not float noise and small enough that no OTHER site falls inside the bracket. State the measured envelope that makes `1e-4` correct — at character 1 the narrowest feature in the frozen header is the pulse's soft edge, and the narrowest site separation on the tested grid is far wider than `2*eps` — and state that PROBING IS REJECTED FOR THE RUNTIME PATH on cost grounds (D-01: `morphedWave` computes all five shapes per call, and probing both sides of the site set is roughly eight extra transcendental-heavy calls per sample) and is exactly right HERE, because a test may spend transcendentals the audio thread cannot. Close with the do-not-simplify line the house helpers all carry.

Add a `TEST_CASE` named `"morph blep: the site magnitudes ARE the characterized jumps of the frozen Waveshape (AA-04 / D-01)"`, in four lettered parts, each `CAPTURE`ing morph, character, the site phase and the probed value.

Part A — HARD SITES AT CHARACTER 0, where every soft site's magnitude is zero by construction so each shape isolates cleanly. At morph 0.50 (saw centre) `CHECK` the probed jump across phase 0 equals `+2.0` within 1e-3. At morph 0.75 (square centre) `CHECK` it equals `+2.0` across phase 0 and `-2.0` across `0.5`. At morph 1.00 (pulse centre, where the frozen duty is 0.05) `CHECK` it equals `+2.0` across phase 0 and `-2.0` across 0.05. Comment that these four numbers are exactly what site entries 1, 4 and 6 predict — `W[2]*2`, `-W[3]*2*(1-c)` and `-W[4]*2*(1-c)` with `c = 0` — so this part asserts the ANALYTIC table against the code it was derived from, which is the whole of AA-04.

Part B — SLOPE BREAKS AT CHARACTER 0 (AA-02). At morph 0.25 (triangle centre) `CHECK` `probeSlopeBreak` at phase 0 equals `-8.0` within 1e-2 and at phase 0.5 equals `+8.0` within 1e-2. Comment that `triBrk` is `2/valley + 2/(1-valley)`, which at `c = 0` gives `valley = 0.5` and therefore 8, and that the triangle contributes NO value jump at all — without a slope-break correction its alias floor is 15.0 dB worse at C8 / character 0, which is why AA-02 exists as a separate requirement from AA-01.

Part C — THE P-4 CORRECTED PREMISE, MADE PERMANENT. At morph 0.50, over character in {0.f, 0.25f, 0.5f, 0.75f, 1.0f}, `CHECK` the probed jump across phase 0 equals `+2.0` within 1e-3 at EVERY character, and `CAPTURE` all five. Banner it with the correction in full: D-03's stated corollary claims the saw's soft reset shrinks the wrap's effective step as character rises. IT DOES NOT. The curved saw evaluates to 1 at phase 0 before the reset is applied, and the reset blends FROM a reset value of 1 TOWARD the curved saw, so both are 1 there. The saw site's width is therefore 0 and its factor is exactly 1 at every character, which is why the saw is the only shape whose correction is character-independent — about 9 dB of improvement at every character, while the naive saw floor barely moves (-15.6 dB at character 0, -14.7 dB at character 1). D-03's CONCLUSION survives — do not correct the reset separately, its slope-break magnitude is about three and a half orders of magnitude under the value step — only the stated premise was wrong. State that a "self-limiting" correction here would silently under-correct the one shape whose alias floor never improves with character, and that this case is what would catch it.

Part D — THE BLEED RING AND THE SQUARE'S TWO POSITIONS. At morph 0.00 (sine centre) with character 0.5, `CHECK` the probed jump across phase 0 equals `0.0148` within 1e-4, and `CAPTURE` it. Comment that a sine has no discontinuity of its own: this entire step is the bleed ring, because at `morph = 0` the segment is 0 and `frac` is 0, so the left weight is 1 and the ring's left index is 4 — the NARROW PULSE bleeds in at full intensity inside what the user hears as a pure sine. Give the closed form the number comes from — `bleedIntensity * 2 * (1-c) / (1 + bleedIntensity)` — and note the frozen path measures 0.014853 against a predicted 0.014851. Then, at morph 0.75 with character 1.0, probe across `0.5` and separately across `dutySq = 0.5 + c*(0.04 + squareDutySpread)` computed from the same `Waveshape` instance's fields, `CAPTURE` both, and `CHECK` they differ by more than 0.1. Comment with P-2: the frozen square flips its rectangle at `0.5f` but centres its hyperbolic tangent on `duty/2` with half-width `duty/2`, so the soft edge sits at `duty` — a DIFFERENT position. Merging them into one site produces single-sample full-amplitude spikes, MEASURED as an envelope rising from 1.1047 to 1.96 at every sample rate, with 0.0 dB spectral difference. This assertion is one of the two places that defect is visible at all.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="morph blep: the site magnitudes*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output shows five saw-wrap captures at character 0, 0.25, 0.5, 0.75 and 1.0, each within 1e-3 of `+2.0`
    - The `-s` output shows the sine-centre bleed capture within 1e-4 of `0.0148`
    - The `-s` output shows the two square positions captured separately with a difference above 0.1
    - `grep -c 'probeJump' tests/test_morph_blep.cpp` is at least 4 and `grep -c 'probeSlopeBreak' tests/test_morph_blep.cpp` is at least 2
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_morph_blep.cpp`, and does not list `src/dsp/Waveshape.hpp` or `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>Every site magnitude in `MorphBlep`'s analytic table is asserted against a direct probe of the frozen `Waveshape`, the saw's character-independence is pinned at five characters, and the square's two distinct positions are proven distinct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The accumulator, overlapping edges, the sync seam, hostile timing, and the P-3 resonant-tiling regression (D-13 / D-07 / AA-03 / D-14 / D-15 / P-3)</name>
  <files>tests/test_morph_blep.cpp</files>
  <behavior>
    - driving one site across the two samples that straddle it, the two emitted halves sum to the full band-limited step: at `s` approaching 0 the first sample carries half the jump and the second carries none; at `s` approaching 1 the first carries none and the second carries minus half the jump
    - the pending accumulator is drained exactly once — a third `step()` at the same inputs with the site now behind emits no residue from the previous firing
    - at a 5-percent duty with a phase increment of 0.19 (C9 at 44.1 kHz), BOTH pulse edges fire inside one sample and the emitted correction is the SUM of two opposite-sign contributions, strictly smaller in magnitude than either one alone
    - `addStep` with a value jump and a sub-sample position produces the same two-half split as a morph site with the same jump, and two `addStep` calls in one sample compose by summation rather than by replacement
    - `addStep` rejects a negative, greater-than-one, or not-a-number sub-sample position without touching either accumulator member
    - `step()` with `dt` zero, negative, subnormal, infinite or not-a-number returns exactly the drained accumulator and leaves both members at zero
    - two `MorphBlep` instances stepped interleaved never see each other's accumulators
    - at RESONANT phase increments — every `dt` that divides a site position by an exact integer, including the measured `dt = 0.0005` with `pulseDuty = 0.374` at exactly 748 samples per edge — the band-limited value `naive + correction` stays inside the pre-scale musical envelope of 1.11, and each site still fires exactly once per cycle
    - taking the side decision from the double accumulator instead of the float `p` makes that envelope assertion FAIL, because the correction lands on the wrong side of the edge and injects the FULL jump rather than half of it
  </behavior>
  <read_first>
    - src/dsp/MorphBlep.hpp in full
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "Pattern 2: The crossing test" including the two limiting rows and the three anti-patterns, § "Pattern 1" sign convention, and § Common Pitfalls P-3, P-14
    - tests/test_vco_core.cpp lines 935-1075 — the interleave invariant's five measured non-vacuity requirements and the validity-first `REQUIRE` habit
    - tests/test_vco_core.cpp lines 735-826 — the hostile-timing array idiom and the accumulate-don't-assert convention
  </read_first>
  <action>
Add a helper `float driveOneSite(forge::MorphBlep& b, const forge::Waveshape& wv, double startPhase, double dt, float morph, float character)` to the anonymous namespace: advance a local double phase by `dt`, apply the single-subtract wrap, cast to float, and call `b.step(wv, phase, p, dt, morph, character)` — mirroring exactly the order `forge::VcoCore::step` uses, so the helper cannot drift from the real call sequence. Banner it saying so, and saying that the SAME float `p` handed to the correction is the one that would be handed to `morphedWave` — that identity is load-bearing (P-3).

Add three `TEST_CASE`s.

CASE ONE, named `"morph blep: the pending accumulator delivers the second half at zero latency, and drains exactly once (D-13)"`. At morph 0.50 and character 0 (the isolated saw wrap, magnitude `+2`, width 0, factor 1), choose `dt = 0.02` and two start phases that put the wrap at a sub-sample position near 0 and near 1 respectively. For each: run `driveOneSite` twice and `CAPTURE` both emitted values. `CHECK` the near-zero case emits close to `+1.0` on the first sample and close to `0.0` on the second, and the near-one case emits close to `0.0` on the first and close to `-1.0` on the second, each within 0.05. Then run a THIRD step and `CHECK` it emits exactly `0.0f` — the accumulator drained once and left nothing behind. Comment with the sign-convention sanity check: at the edge itself the naive value has already jumped and the residual is minus one half, so the corrected sample is the pre-edge value plus half the jump, the band-limited midpoint. Also record why the second half is CARRIED rather than recomputed at the next sample: under audio-rate MORPH and FM the jump magnitude, the site position and `dt` have all moved by then, so the accumulator's one consistent set of values is strictly more robust — and a one-sample output delay buffer was rejected because a VCO that silently delays by a sample desyncs against every other oscillator in the patch.

CASE TWO, named `"morph blep: overlapping pulse edges SUM rather than overwrite at a narrow duty (AA-03 / D-07)"`. At morph 1.00 (the frozen duty is 0.05) and character 0, with `dt = 0.19` — the phase increment of C9 at 44.1 kHz, where the 5-percent pulse is about 0.26 samples wide — sweep the start phase so that the sample straddles BOTH pulse edges. `REQUIRE` first, as the non-vacuity precondition, that a start phase exists on the swept grid where both edges lie inside one sample; a case that never actually overlaps would pass for free. Then `CAPTURE` the emitted correction and `CHECK` its magnitude is strictly less than the magnitude either edge would contribute alone at the same sub-sample position — the partial cancellation D-07 calls the physically correct band-limited answer, because a pulse narrower than a sample genuinely carries less energy. Comment that flooring the effective duty at `dt` was rejected: it would guarantee the pulse never thins out at the top of the keyboard but would introduce a sample-rate-dependent timbre change the frozen `Waveshape` knows nothing about, so the naive and band-limited paths would stop being the same waveform — which would also invalidate D-08's before-and-after comparison. Also `CHECK` the emitted value is finite at every point on the sweep.

CASE THREE, named `"morph blep: the D-14 sync seam feeds the SAME accumulator, and hostile dt reaches no divisor (D-14 / D-15)"`, in three parts.

Part A — THE SEAM. Call `addStep(xAhead, jump)` on a fresh instance for `xAhead` in {0.f, 0.25f, 0.5f, 0.75f, 1.f} at `jump = 2.f`, and `CHECK` the resulting `inject` and `pending` match the same two-half split a morph site of the same jump produces at the equivalent sub-sample position, within 1e-5. Then call `addStep` TWICE in one sample with two different jumps and `CHECK` both members hold the SUM rather than the last value — that is what makes several sync events and several morph sites compose (D-07). State the Phase 32 boundary in a comment: this seam is DESIGNED here and BUILT in Phase 33; this phase implements no sync behavior, adds no sync fields to `forge::VcoInputs`, and never snaps a reset to exactly zero.

Part B — THE SEAM'S ENTRY GATE. Call `addStep` with `xAhead` in {-0.1f, 1.1f, a quiet not-a-number} and `CHECK` both accumulator members are still exactly `0.0f` afterwards. Comment that the gate is written with the negated comparison first so a not-a-number is REJECTED rather than accumulated into per-instance state, which would then poison every following sample.

Part C — HOSTILE `dt` (D-15 / P-14). Prime the accumulator with a known `addStep`, then call `step()` with `dt` in {0.0, -1.0/44100.0, the smallest positive subnormal double, positive infinity, negative infinity, a quiet not-a-number}. `CHECK` each call returns exactly the primed value and leaves both members at exactly `0.0f`. `CAPTURE` `dt` on every point. Comment with the corrected D-15 rationale IN FULL, because this is the assertion that carries it: Phase 31's D-15 pointed the hostile-timing work at Phase 32 on the grounds that "Phase 32's oversampled inner loop is the first real source of exotic timing". THAT PREMISE IS FALSIFIED — AA-05 forbids oversampling in v2.0, so no such loop exists or will exist. The conclusion survives on better evidence: this phase introduces division by `dt`, in the D-03 factor and in the sub-sample position, so a zero, subnormal or non-finite `sampleTime` now reaches arithmetic that did not exist before. Note the measured scope limit too: the shipped formulation divides by `dt` ONLY — there is no division by an edge width anywhere in the header — and MorphBlep guards it itself rather than trusting its caller.

Part D — PER-INSTANCE STATE (CORE-03 / D-14). Prime two `MorphBlep` instances with different `addStep` values, drive them interleaved one call each for 64 iterations, and `CHECK` each reproduces bit-exactly what it produced running alone. Compare with a direct float inequality, never an approximate comparator: independence is a bit-exactness claim or it is nothing. `REQUIRE` first that the two solo streams are distinguishable, or "interleaved equals solo" is satisfied for free by two identical signals.

CASE FOUR, named `"morph blep: RESONANT phase increments do not spike the envelope - the side decision comes from the float p, not the double phase (P-3)"`. This is the P-3 counterpart of Task 2 Part D's square-split probe, and it exists for the same reason: the defect it catches measures 0.0 dB on the spectral metric and is visible only on the output envelope.

Add a local helper that, for a given `morph`, `character` and `dt`, walks a double phase from 0 for enough samples to cover at least 40 full cycles, applying the single-subtract wrap and casting to a float `p` in EXACTLY the order `forge::VcoCore::step` uses, and at each sample forms the band-limited value as `wv.morphedWave(p, morph, character, 0.f)` plus `blep.step(wv, phase, p, dt, morph, character)`. Return the maximum absolute value, the count of samples where the emitted correction was non-zero, and the count of full cycles walked.

THE RESONANT GRID, and every number in it must be computed rather than typed where a formula exists, so the divisor relation is exact by construction rather than by luck:
- `morph = 0.82f`, which drives the frozen pulse duty to `0.50f - 0.45f * 0.28f`, i.e. 0.374. `CAPTURE` the computed duty and `REQUIRE` it is within 1e-6 of 0.374, so the row is pinned to the geometry RESEARCH measured rather than to a morph value that might drift.
- the literal measured point: `dt = 0.0005`, which divides that duty exactly 748 times. `REQUIRE` that `duty / dt` rounds to an integer within 1e-9, so the resonance is asserted to actually exist rather than assumed.
- the generalisation, so the case is not a single lucky point: `dt = duty / N` for `N` in {373, 500, 748, 1000, 1871} and `dt = 0.5f / N` for `N` in {2, 3, 7, 64, 1000} — the second family targets the square's hard site at 0.5 the same way. Skip any `dt` at or above 0.5, which `forge::kVcoMaxDeltaPhase` makes unreachable anyway.
- a few-unit-in-last-place neighbourhood around each resonant `dt`, walking `nextafter` outward two steps in each direction, because the defect is a TIE at the unit-in-last-place level and the exact divisor is where the float `p` and the double `phase` disagree about which side of the edge the sample is on.
- each of the above at `character` in {0.f, 0.5f, 1.f}. Character 0 is the cleanest isolation, because every soft site's magnitude is zero and only the two hard pulse sites carry weight.

ASSERT, in this order:
- NON-VACUITY FIRST: `REQUIRE` that the correction was non-zero on at least one sample per cycle walked, so a configuration where no site ever fired cannot pass for free; and `REQUIRE` the cycle count is at least 40.
- THE PROPERTY: `CHECK` the returned maximum is at or below a named local constant `kMorphBlepPreScaleEnvelope = 1.11f`. Give that constant its own provenance comment: it is the pre-scale form of the analytic ceiling `tests/test_vco_core.cpp` derives — the sine path is monotone with range about -1.05 to +1.11, the other four shapes are each bounded by 1, the crossfade is a linear interpolation and the bleed step is a convex combination — and it is the same number that becomes 5.55 V after the unconditioned five-times output scale. It is declared locally rather than shared across translation units on purpose; note that if the two ever disagree, `tests/test_vco_core.cpp`'s is the authority.
- `CHECK` every sampled value is finite.
- `CAPTURE` the maximum, the duty, `dt`, `duty / dt`, the character and the firing count on every row.

BANNER IT with the measurement and with the sensitivity, because a reader will otherwise "simplify" the grid to round numbers and delete the whole point:
- WHAT THIS CATCHES: deriving the crossing test's SIDE decision from the double `phase` instead of from the float `p`. The frozen branches compare against `p` with a strict comparison, so whenever the site and the phase differ by less than about 6e-8 the two disagree, the correction is applied on the WRONG SIDE, and the error is the FULL jump rather than half of it. MEASURED as a systematic plus-or-minus-1.0-amplitude spike, with the prototype's envelope rising from 1.957 to 2.145, whenever the phase grid resonates with a site position.
- WHY THESE SPECIFIC NUMBERS: 0.0005 and 0.374 are not arbitrary and are not "a low note with a narrow pulse". They are the exact pair at which the prototype reproduced the defect, and they are exact BECAUSE 0.374 divided by 0.0005 is exactly 748 — the sub-sample distance lands on a whole number of samples every cycle, which is precisely the condition that puts the float and the double on opposite sides of the comparison. Rounding either value away destroys the resonance and turns this case green against a broken implementation. Do NOT replace the computed `duty / N` grid with hand-typed decimals for the same reason.
- WHY IT IS A SEPARATE CASE: the spectral gate in `tests/test_vco_spectrum.cpp` shows 0.0 dB difference for this defect, exactly as it does for a merged square site. The output envelope is the only place either one is visible, and a general magnitude sweep can miss a resonance that only occurs on a measure-zero set of phase increments. This case goes looking for that set instead of hoping to land in it.
- THE COMPANION ANTI-PATTERN: do not "fix" a suspected miss by widening the fire gate above `s = 1`. That trades a missed edge for a double-fired edge on the following sample, and both are single-sample full-amplitude clicks. The tiling is exact to about 1e-16 when the distance comes from the double.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="morph blep: *" -s 2>&1 | tail -4` reports exactly 5 test cases, 5 passed, 0 failed (this plan's four plus Task 1's one)
    - The `-s` output for case one shows the third step emitting exactly `0` at both start phases
    - The `-s` output for case two shows the overlapping-edge `REQUIRE` satisfied and the summed correction strictly smaller than either single contribution
    - The `-s` output for case three Part C shows all six hostile `dt` values returning the primed value with both members at zero
    - `./build-test/test -tc="morph blep: RESONANT*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output for case four shows the computed duty captured within 1e-6 of `0.374`, and a `duty / dt` capture of `748` on the literal measured row
    - The `-s` output for case four shows a per-row maximum at or below `1.11` on every row of the resonant grid, at all three character values, with a non-zero firing count on every row
    - `grep -c 'kMorphBlepPreScaleEnvelope' tests/test_morph_blep.cpp` is at least 2 and `grep -c '748' tests/test_morph_blep.cpp` is at least 1
    - SENSITIVITY, PROVED BY DETECTING: with `src/dsp/MorphBlep.hpp`'s side line temporarily changed from the float comparison to the equivalent double comparison against `phase`, `./build-test/test -tc="morph blep: RESONANT*"` reports at least 1 failed assertion, and the observed maximum is recorded. Revert the probe before committing and confirm `git status --porcelain src/dsp/MorphBlep.hpp` is empty
    - Comment-stripped source uses no approximate comparator: `grep -v '^[[:space:]]*//' tests/test_morph_blep.cpp | grep -c 'Approx'` outputs `0`
    - The doctest summary reports at least 90 test cases (phase baseline before plan 32-01 was exactly 81) with 0 failed
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_morph_blep.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The accumulator's two-half delivery, single drain, overlapping-edge summation, sync seam, entry gate, hostile-`dt` inertness, per-instance isolation and resonant-tiling envelope are each asserted with a non-vacuity precondition ahead of the value, and the resonant case is proved to DETECT a double-sourced side decision rather than merely to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> `forge::MorphBlep::step` | The suite calls the seam directly with no `VcoCore` in the way, which is exactly the coverage a driver-routed test cannot give. |
| Phase 33 -> `addStep` | The seam accepts an externally supplied sub-sample position and value jump. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-02 | Denial of Service | division by `dt` inside `MorphBlep` | high | mitigate | Case three Part C drives all six hostile `dt` classes straight into `step()` with no driver in the way and asserts the drained-accumulator return. The bypass IS the coverage — routing it through a driver would silently delete the scenario while leaving it green (the plan-30-08 lesson). |
| T-32-17 | Tampering | `addStep` accepting an out-of-range or non-finite event | medium | mitigate | Case three Part B asserts both accumulator members are untouched after three rejected calls, so a bad event cannot poison per-instance state. |
| T-32-16 | Tampering (signal integrity) | a defect invisible to the spectral metric | high | mitigate | Task 2 Part D asserts the square's two positions are genuinely distinct, and Task 2 Part A asserts every hard magnitude against a direct probe of the frozen header. Both defects measure 0.0 dB spectrally. |
| T-32-31 | Tampering (signal integrity) | a full-amplitude spike at RESONANT phase increments from a double-sourced side decision (P-3) | high | mitigate | Task 3 case four drives the exact measured resonance (`dt = 0.0005`, duty 0.374, 748 samples per edge) plus a computed `duty / N` and `0.5 / N` grid and a unit-in-last-place neighbourhood around each, and bounds the pre-scale envelope at 1.11. Its sensitivity is PROVED by temporarily sourcing the side from the double and observing the failure, not inferred from a green run. A general magnitude sweep can miss a resonance that occurs on a measure-zero set of increments; this case goes looking for that set. |
| T-32-18 | Elevation of Privilege (voice isolation) | shared band-limiting state across voices | high | mitigate | Case three Part D drives two instances interleaved and requires bit-exact reproduction of their solo streams, with a distinguishability `REQUIRE` first so the property is not satisfiable by two identical signals. |
| T-32-14 | Repudiation (evidence integrity) | a unit case that cannot fail | high | mitigate | Every case carries a non-vacuity `REQUIRE` ahead of its value assertion: the overlap case requires the overlap to actually occur, the interleave case requires the two streams to be distinguishable, and the probe helpers carry their validated `eps` envelope. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. |
</threat_model>

<verification>
- `make test` green with the case count risen to at least 90 and 0 failures.
- Every AA-04 magnitude is asserted against a probe of the FROZEN header rather than against a restatement of `MorphBlep`'s own table.
- The P-4 corrected premise is pinned at five character values.
- Hostile `dt` is driven straight into `MorphBlep::step` with no driver in the way.
- The P-3 resonant-tiling case is observed FAILING against a temporarily double-sourced side decision, and the probe is reverted with `src/dsp/MorphBlep.hpp` confirmed unmodified before the commit.
- `make guards` and `make strict` green; `src/AnalogLFO.cpp` absent from the diff.
</verification>

<success_criteria>
- `tests/test_morph_blep.cpp` exists with five test cases covering D-03's limits, AA-04's characterized magnitudes, AA-02's slope breaks, AA-03/D-07's overlapping edges, D-13's accumulator, D-14's seam, D-15's hostile timing and P-3's resonant tiling.
- Each case carries a non-vacuity precondition and the measured figure that makes its threshold non-negotiable.
- The two defects the spectral metric cannot see — a merged square site and a double-sourced side decision — each have a dedicated case, and the second one's sensitivity is measured rather than argued.
- No `src/` file is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-05-SUMMARY.md` when done. Record: the five saw-wrap probe values at the five characters; the two square positions and their difference; the sine-centre bleed step; the overlapping-edge summed and single-contribution magnitudes; the resonant grid's computed duty, its `duty / dt` integer, the per-row maxima at all three character values, and the observed maximum under the temporary double-sourced-side probe; and the doctest case and assertion counts before and after.
</output>

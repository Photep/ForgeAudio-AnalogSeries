---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 11
type: execute
wave: 8
depends_on: ["32-10"]
files_modified: []
autonomous: false
nyquist_compliant: true
requirements: [MORPH-01, MORPH-02, AA-05, TEST-03]
must_haves:
  truths:
    - "D-16 / Phase 30 D-07: every visible control does something, so an in-Rack check is honest. The MORPH CV jack and attenuverter declared this phase exist partly so the operator can actually audition the hardest case the alias floor has to survive."
    - "The spectral gate bounds alias ENERGY; it does not bound audible artefacts at a crossfade seam. That judgement is perceptual and is the one manual verification in this phase's validation strategy."
    - "The install must be flushed as a WHOLE PLUGIN DIRECTORY, not as a dylib plus res/: a stale install is a stale plugin VERSION, and refreshing only the binary satisfies a hash check while Rack keeps reading an old manifest."
    - "The shipped Analog LFO must be visually and audibly UNCHANGED in the same session — the milestone guardrail, checked by the operator rather than only by the golden replay."
    - "The expected results are presented in full BEFORE the operator answers, so an absence of complaint is an absence of complaint and not an absence of exposure."
  artifacts: []
  key_links:
    - "This session is the perceptual counterpart of invariant 6 in tests/test_vco_core.cpp — same control surface, same sample rates, same modulation rates. Neither substitutes for the other."
    - "A stale pre-rename plugin directory under the old slug may still put a SECOND Forge LFO in the browser; the guardrail subject must be pinned rather than inferred."
---

<objective>
Put the band-limited VCO in front of the operator with a cable in the MORPH CV jack, and get a judgement on the two things no headless assertion can make: whether the audio-rate morph sweep sounds clean through the segment boundaries, and whether the shipped Analog LFO is unchanged.

Purpose: Phase 30's D-07 rule and this phase's D-16 both point here. The spectral gate proves the alias floor sits below a pinned threshold; it says nothing about zipper noise at a crossfade seam, and the phase's whole audible claim is that the oscillator got cleaner.

Output: an operator verdict, recorded verbatim, plus the observations that go with it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-07-SUMMARY.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-10-SUMMARY.md
@src/AnalogVCO.cpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — `float pending`, `float inject`, `void reset()`, `void addStep(float, float)`, `float step(const Waveshape&, double, float, double, float, float)`
- `forge::VcoCore::blep`
- `AnalogVCO::MORPH_ATTEN_PARAM` (ParamId), `AnalogVCO::MORPH_CV_INPUT` (InputId)
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- Test-only in `tests/test_vco_core.cpp`: `kMusicalBoundV`, `kHostileBoundV`, `DeliberatelyBrokenSharedStateCore::blep`, invariant 6
- `res/AnalogVCO.svg` — two additional 10x10 marker rects at (25.48, 75) and (55.96, 75)

THIS PLAN produces: no code artifact. It produces the operator verdict.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build, flush the WHOLE plugin directory, and pin the install's identity before asking anyone to listen</name>
  <files></files>
  <read_first>
    - .planning/STATE.md § Accumulated Context — the Phase 30 record that a stale install is a stale PLUGIN VERSION and not a stale binary, including the measured false negative: an extracted install that was a complete previous-version LFO-only plugin, whose manifest carried ONE module entry, so refreshing only `plugin.dylib` and `res/` satisfied the hash assertion while Rack kept reading a one-module manifest
    - .planning/STATE.md § Operator Next Steps — the stale pre-rename plugin directory under the old slug, which puts a SECOND Forge LFO in the module browser and is why the last guardrail sign-off had an inferred rather than pinned subject
    - src/AnalogVCO.cpp — the ten controls and their coordinates, so the session script names the right knobs
    - plugin.json — the module list and version
  </read_first>
  <action>
Build and install, then PIN what is installed before anyone is asked to judge it.

BUILD. Run `make clean` then `make -j4` and `make dist` against `../Rack-SDK`, using the relative SDK path — never a worktree copy.

FLUSH THE WHOLE DIRECTORY, not the binary. Synchronise the ENTIRE extracted plugin directory from `dist/` into the Rack user plugins directory, replacing everything: the manifest, `res/` and the binary together. Executing a binary-only refresh is the exact false negative Phase 30 measured — the hash assertion passes while Rack reads an old manifest, so a newly added module never appears and the session tests the previous version.

PIN THE INSTALL'S IDENTITY, and record every one of these in the SUMMARY before the checkpoint runs:
- the installed `plugin.json`'s version string and its module count, read from the INSTALLED file rather than from the repository
- the presence and modification time of the installed `res/AnalogVCO.svg`, and its rect count
- the installed binary's exported model symbols, read with `nm`, confirming both the LFO model and the VCO model are present
- the SHA-256 of the installed binary compared against the freshly built one

PIN THE GUARDRAIL'S SUBJECT. List every Forge plugin directory currently installed in Rack, with each one's slug, version and module list. If a stale directory under a previous slug is present, say so explicitly in the session script and tell the operator WHICH of the two LFOs in the browser is the shipped one under test. Do not delete anything — removing a plugin from the operator's Rack installation is not an executor's call. Record it as an outstanding housekeeping item instead, as STATE already does.

If any of these checks disagrees with the freshly built artefact, STOP and report rather than proceeding to the checkpoint. A perceptual verdict on the wrong binary is worse than no verdict.
  </action>
  <verify>
    <automated>make clean && make -j4 && make dist && ls -la dist/</automated>
  </verify>
  <acceptance_criteria>
    - `make -j4` and `make dist` both exit 0 and `dist/` contains the extracted plugin directory
    - The INSTALLED `plugin.json` reports the same version string as the repository's and lists 2 modules
    - The installed `res/AnalogVCO.svg` exists and `grep -c '<rect'` on it outputs `12`
    - `nm` on the installed binary lists both the LFO model symbol and the VCO model symbol
    - The installed binary's SHA-256 equals the freshly built one's
    - Every installed Forge plugin directory is enumerated in the SUMMARY with its slug, version and module list
  </acceptance_criteria>
  <done>The operator is about to listen to the artefact this phase actually built, and that fact is pinned by manifest, asset, symbol and hash rather than assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator in-Rack UAT — the audio-rate MORPH sweep, and the shipped LFO unchanged</name>
  <files></files>
  <read_first>
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md § "Manual-Only Verifications" — the single manual row and its stated test instructions
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-07-SUMMARY.md — the measured alias floors, which are the expected-results figures presented before the operator answers
    - .planning/STATE.md § Accumulated Context — the Phase 30 UAT record, and specifically why the expected-results block is presented IN FULL before the reply is taken
    - src/AnalogVCO.cpp — the ten controls, so the script names them exactly as the panel does
  </read_first>
  <action>
Run the operator session and record it. Present the `<what-built>` summary and then the EXPECTED-RESULTS block in full — the measured per-shape alias floors from `32-07-SUMMARY.md`, the fact that the roadmap's earlier target figure was corrected because 2-sample polyBLEP's own ceiling sits well above it, and the plain statement that the narrow-pulse end at the top of the keyboard will still be the roughest and that this is the technique's ceiling rather than a defect. Only then walk the operator through the nine numbered steps in `<how-to-verify>`, in order, waiting at each.

Record the operator's reply VERBATIM. Do not paraphrase it into a verdict, do not invent an observation, and do not omit a minor one. If no observation is raised, say so explicitly and note that the expected-results block was presented in full beforehand, so the absence is an absence of complaint rather than an absence of exposure — the Phase 30 UAT record makes that distinction by name.

Route every observation: an artefact that contradicts an assertion this phase makes becomes a gap-closure finding and the phase does NOT close on it; an artefact that is out of scope goes into `deferred-items.md` with an owner. Then re-run `make test`, `make strict` and `make guards` so the recorded verdict is attached to a green tree.
  </action>
  <what-built>
Band-limited morph-aware anti-aliasing. `src/dsp/MorphBlep.hpp` now applies polyBLEP at the value-step discontinuities (the saw wrap, the square's two edges, the variable-width pulse's two edges) and polyBLAMP at the triangle's slope corners, scaled by the morph and bleed weights and by the characterized jump so CHARACTER's edge softening auto-scales the correction. `forge::VcoCore` calls it beside the unchanged frozen `Waveshape`. The MORPH knob gained a CV jack and a bipolar attenuverter so the sweep can be driven at audio rate. Measured alias-floor improvements are in `32-07-SUMMARY.md`; the headless bound and finiteness assertions are in `tests/test_vco_core.cpp` invariant 6.
  </what-built>
  <how-to-verify>
PRESENT THE EXPECTED RESULTS IN FULL BEFORE TAKING THE REPLY. An absence of complaint only means something if the operator was told what to expect. Present, from `32-07-SUMMARY.md`, the measured per-shape improvement at C8 and the fact that the roadmap's earlier target figure was corrected because 2-sample polyBLEP's own ceiling sits well above it. State plainly that the oscillator will still not sound pristine at the very top of the keyboard on the narrow pulse, that this is the technique's ceiling and not a defect, and that the escalation path is recorded and deliberately not taken in v2.0.

Then, step by step:

1. Open Rack. Confirm the module browser shows the Analog VCO under Forge Audio. If TWO Forge LFOs appear, use the one named in the SUMMARY's plugin enumeration — the other is a stale install under a previous slug.
2. Add an Analog VCO. Patch its OUT to an audio interface or scope. Set COARSE to 0, FINE to 0, CHARACTER to 0, MORPH fully counter-clockwise.
3. Play or patch a V/OCT around C7, then C8, then C9 (about 2.1 kHz, 4.2 kHz and 8.4 kHz). At each, sweep MORPH slowly by hand through all five shapes. Listen for the change this phase is about: the harsh, gritty, metallic ringing that sat under the tone before should be substantially reduced on the saw, square and triangle regions. The narrow-pulse end will still be the roughest.
4. Repeat step 3 with CHARACTER at 12 o'clock and then fully clockwise. Confirm the tone gets softer and darker with CHARACTER rather than louder or more brittle, and that nothing clicks, spits or drops out as CHARACTER moves.
5. THE HARD CASE. Patch an LFO or a second oscillator into MORPH CV. Turn the MORPH DEPTH attenuverter fully clockwise. Sweep the modulator from a slow rate up through audio rate — roughly 50 Hz, then 500 Hz, then 2 kHz — while holding a note around C7 and again around C9. Listen for zipper noise, stepping, clicks or discontinuities as the morph crosses the boundaries between shapes. This is the case the phase exists to survive.
6. Turn the MORPH DEPTH attenuverter to fully counter-clockwise and confirm the modulation inverts rather than stopping — the control is bipolar by design.
7. Sweep the attenuverter through its centre and confirm the modulation passes smoothly through zero with no jump.
8. THE GUARDRAIL. In the same session, add the shipped Analog LFO. Confirm it looks exactly as it always has and sounds exactly as it always has — its panel, its knobs, its rates, its shapes, its display. Nothing in this phase should have touched it.
9. Report anything you hear that the expected-results block did not prepare you for, however small, and say if anything sounds worse than before rather than better.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the audio-rate MORPH sweep is free of zipper noise and boundary artefacts at C7 and C9, that the alias reduction is audible on the saw, square and triangle regions, that the MORPH attenuverter is bipolar and passes smoothly through zero, and that the shipped Analog LFO is visually and audibly unchanged.</human-check>
    <automated>make test && make strict && make guards</automated>
  </verify>
  <acceptance_criteria>
    - The expected-results block, including the measured per-shape figures and the corrected roadmap target, is presented in full BEFORE the operator's reply is taken, and that presentation is recorded in the SUMMARY
    - The operator's reply is recorded VERBATIM, including any observation, however minor, and including an explicit note if no observation was raised
    - No observation is invented, paraphrased into a verdict, or omitted
    - Any reported artefact is logged as a finding with the step that produced it, and routed — to a gap-closure plan if it contradicts an assertion, or to `deferred-items.md` if it is out of scope
    - `make test`, `make strict` and `make guards` are re-run after the session and all exit 0, so the verdict is attached to a green tree
  </acceptance_criteria>
  <resume-signal>Type "approved" to record the phase as operator-verified, or describe what you heard and where.</resume-signal>
  <done>The operator has heard the band-limited oscillator under audio-rate morph modulation and the shipped LFO in the same session, and their verdict plus any observations are recorded verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artefact -> installed artefact -> what the operator hears | A stale install makes a perceptual verdict describe the previous version. |
| VCO output jack -> audio interface -> speakers and ears | This is the one point in the phase where an out-of-range voltage reaches physical equipment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-29 | Repudiation (evidence integrity) | a verdict recorded against a stale install | high | mitigate | The whole plugin directory is flushed, and the install's identity is pinned by manifest version, module count, asset rect count, exported symbols and binary hash before the checkpoint runs. Phase 30 MEASURED the binary-only refresh producing a false negative that passed its own hash assertion. |
| T-32-03 | Tampering (signal integrity) | out-of-range output reaching speakers or an interface | high | mitigate | The single outer magnitude bound pinned in plan 32-08 is asserted across every scenario including the audio-rate MORPH grid, at 10.0 V against Rack's plus-or-minus 12 V norm, and `make test` is re-run after the session so the verdict attaches to a green tree. The session script asks the operator to start at a low monitoring level implicitly by beginning at CHARACTER 0 and a mid keyboard note. |
| T-32-12 | Tampering | the shipped Analog LFO | high | mitigate | Step 8 checks the shipped module visually and audibly in the SAME session, which is the operator-side counterpart of the golden byte-lock. Step 1 pins WHICH of two possible LFO instances is the one under test, so the guardrail's subject is pinned rather than inferred. |
| T-32-30 | Repudiation (evidence integrity) | an absence of complaint mistaken for an absence of problems | high | mitigate | The expected-results block is presented in full before the reply is taken, and the reply is recorded verbatim with an explicit note when no observation was raised. Observations are never invented. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem across the entire phase; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. The plugin build adds no dependency to `plugin.json`. |
</threat_model>

<verification>
- The installed artefact is proven identical to the freshly built one by manifest, asset, symbol and hash before any listening happens.
- The operator's verdict is recorded verbatim, along with the expected-results block that preceded it.
- `make test`, `make strict` and `make guards` are green after the session.
- Any reported artefact is routed to a gap-closure plan or to the deferred register, never absorbed silently.
</verification>

<success_criteria>
- The plugin is installed by whole-directory flush and its identity is pinned five ways.
- The operator has auditioned the audio-rate MORPH sweep at C7 and C9 through every segment boundary, at three modulation rates, at three CHARACTER settings.
- The shipped Analog LFO is confirmed unchanged in the same session, with the subject pinned rather than inferred.
- The verdict and every observation are recorded verbatim.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-11-SUMMARY.md` when done. Record: the five install-identity checks with their values; the enumerated Forge plugin directories with slugs and versions; the expected-results block exactly as presented; the operator's reply verbatim; and the routing decision for every observation raised.
</output>

---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 07
type: execute
wave: 5
depends_on: ["32-06"]
files_modified:
  - tests/test_vco_spectrum.cpp
autonomous: true
nyquist_compliant: true
requirements: [TEST-03, AA-01, AA-02, AA-03]
must_haves:
  truths:
    - "TEST-03: an alias-floor spectral invariant asserts high-note aliasing stays below per-shape thresholds pinned from THIS repository's own measurement, at C7, C8 and C9 (operator decision 2026-08-01)."
    - "D-08 closed: the tombstone landed in plan 32-03 is INVERTED in place into the live gate, so the phase's RED is on the record and the GREEN replaces it in the same slot."
    - "D-09: thresholds are per (morph region, note, character) and each carries its measured justification in the test; a single global threshold and a global-floor-with-pulse-carve-out are both rejected."
    - "D-09 anti-circularity: pinning a threshold from the implementation's own output is circular unless bounded, so the gate also carries a no-regression invariant over ALL 90 cells and a minimum-improvement assertion over the named large-margin cells."
    - "D-11: 48 kHz and 96 kHz run the same assertions on the same note as the 44.1 kHz C8 row, because a correction scaled wrongly by dt fails RATE-DEPENDENTLY and is invisible at a single rate."
    - "P-5: the roadmap's former 'approximately -60 dB' is a target and not a reachable floor; ROADMAP SC-4 was corrected on 2026-08-01 and the plans are consistent with the corrected text."
    - "Thresholds are NEVER softened to match what the implementation produced; a shortfall escalates with impact and options, per the deferred-ideas escalation path."
  artifacts:
    - tests/test_vco_spectrum.cpp
  key_links:
    - "measureCellDb's useMirror=false branch drives the corrected forge::VcoCore and its useMirror=true branch drives NaiveVcoCoreMirror through the identical code path, which is what makes the no-regression delta a like-for-like comparison."
    - "The escalation path if a threshold is unreachable is the deferred pulse-reach refinement documented in src/dsp/MorphBlep.hpp's banner, then an operator decision on kernel order — never a quiet loosening, and never minBLEP or oversampling (AA-05)."
---

<objective>
Close the measure→pin loop: measure the real `forge::MorphBlep` across all 90 cells, pin the D-09 per-shape thresholds from those numbers, and invert plan 32-03's tombstone into the live alias-floor gate (TEST-03).

Purpose: this is the phase's exit criterion and the second half of D-08. The RED is already on the record; this plan makes the GREEN, in the same slot, against thresholds derived from this repository's own measurement rather than from prose or from a prototype.

Output: a re-pinned `SPECTRUM_GRID` threshold column with per-cell provenance, the live gate at C7/C8/C9, a no-regression invariant over every cell, and a minimum-improvement assertion that makes the pinned thresholds non-circular.
</objective>

<!-- planner-discipline-allow: Approx -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-03-SUMMARY.md
@tests/test_vco_spectrum.cpp
@src/dsp/MorphBlep.hpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor`, `struct forge::MorphBlep` (`pending`, `inject`, `reset`, `addStep`, `step`), `forge::VcoCore::blep`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: the re-pinned `SPECTRUM_GRID.thresholdDb` column and the live TEST-03 gate.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The measure pass — record the corrected alias floor for all 90 cells, as a repeatable protocol</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <read_first>
    - tests/test_vco_spectrum.cpp in full — `SPECTRUM_GRID`, `measureCellDb`, the naive baseline case and the tombstone from plan 32-03
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-03-SUMMARY.md — the recorded 90-cell naive baseline and the observed failing count
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "D-08 baseline and D-09 threshold evidence" — the prototype's corrected column and the five readings the planner must honour
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Assumptions Log entry A3 — the prototype's numbers are expected to reproduce within about 1 dB, and D-08 exists precisely to catch drift
  </read_first>
  <action>
Extend the D-08 baseline case in `tests/test_vco_spectrum.cpp` — do not add a second case — so that every one of the 90 cells is measured TWICE in the same pass: once with `useMirror = true` (the naive baseline) and once with `useMirror = false` (the corrected `forge::VcoCore`). `CAPTURE` both values plus the delta for every cell, and rename the case to `"vco spectrum: the naive and corrected alias floors, recorded per shape, note and character (D-08 measure->pin loop)"`.

Write, immediately above the case, THE REPEATABLE MEASURE-TO-PIN PROTOCOL as a numbered comment block. It is the artifact this phase's deliberate iteration budget is spent through, and it must be re-runnable by a later phase without re-deriving anything:

1. Run `./build-test/test -tc="vco spectrum: the naive and corrected alias floors*" -s` and read the per-cell captures.
2. For each gated cell, the pinned threshold is the measured CORRECTED value plus a 3 dB margin, rounded OUTWARD (toward the less negative value). The margin is 3 dB because that is roughly twice the largest measured cross-toolchain and cross-block-length variation this suite has seen, and because a tighter margin turns a legitimate float-ordering difference into a red build.
3. Compare each measured corrected value against the prototype figure recorded in `32-VALIDATION.md`. A difference beyond about 1 dB is a FINDING about the implementation, not a bookkeeping update: record it, name which side moved and why, and do not silently adopt the new number.
4. Never adjust a threshold to accommodate a measured shortfall. A shortfall escalates — see Task 2's escalation rule.
5. Re-run steps 1 to 4 after ANY change to `src/dsp/MorphBlep.hpp` or to `forge::VcoCore::step`'s call site.

Keep the two structural sanity assertions from plan 32-03 (sine at character 0 below -140 dB; the C9 pulse at character 0 above -5.0 dB on the NAIVE side) and add one more that is independent of any threshold: for every cell, `CHECK` the corrected alias peak is FINITE and below `0.0` dB — an alias louder than the fundamental would mean the correction is injecting rather than removing energy, and that is checkable without any pinned number.

Keep the per-cell D-10 leakage `REQUIRE` in front of every measurement.

Do NOT change the threshold column in this task. Task 2 pins it, from the numbers this task records.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco spectrum: the naive and corrected alias floors*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output contains 90 naive captures, 90 corrected captures and 90 delta captures
    - Every corrected capture is finite and below `0.0` dB
    - The doctest case count is unchanged from this plan's starting baseline — the case was EXTENDED, not duplicated
    - `grep -c 'MEASURE-TO-PIN PROTOCOL' tests/test_vco_spectrum.cpp` outputs `1`
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_spectrum.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>Both alias floors are recorded for all 90 cells in one pass, and the repeatable measure-to-pin protocol is written into the test file rather than living only in a plan.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the D-09 thresholds and invert the tombstone into the live TEST-03 gate</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <read_first>
    - tests/test_vco_spectrum.cpp — the measured captures from Task 1 and the tombstone case with its inversion contract naming this plan
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md § "Threshold Policy (D-09)" and the operator-decision paragraph in § "Spectral Construction"
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-5 and P-6, and § Open Questions items 1, 2 and 4
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md § Deferred Ideas — the escalation path: the narrow pulse-reach refinement first, then an operator decision on kernel order; broad escalation is v2.1 oversampling and explicitly NOT minBLEP
    - .planning/ROADMAP.md § "Phase 32" Success Criterion 4 as it reads NOW (corrected 2026-08-01)
  </read_first>
  <action>
Re-pin every `thresholdDb` in `SPECTRUM_GRID` from Task 1's measured corrected column plus 3 dB, rounded outward. Rewrite each `provenance` string to name the measurement rather than the prototype — it must state the measured corrected value, the sample rate and the note, and the margin applied. Where a re-pinned value differs from the plan-32-03 provisional by more than 1 dB, the provenance string must ALSO name the prototype figure and say which side moved; that difference is a finding about the implementation and belongs in the SUMMARY too.

Then INVERT the tombstone case IN PLACE — same slot, same 45 gated cells. Rename it `"vco spectrum: TEST-03 - the alias floor stays below its per-shape pinned threshold at C7, C8 and C9 (D-09, was the D-08 RED tombstone)"`. Replace the `failing >= kNaiveFailuresFloor` assertion with `CHECK(failing == 0)` measured on the CORRECTED core, and flip the five named large-margin subset assertions from `naiveDb > threshold + 5.0f` to `correctedDb <= cell.thresholdDb`. Keep every per-cell `CAPTURE` and keep the per-cell D-10 leakage `REQUIRE`.

Rewrite the case banner to record the inversion rather than erase it, carrying:
- the D-08 history: this case was, until this plan, the tombstone asserting that the NAIVE core failed this gate, and plan 32-03's SUMMARY holds the verbatim RED transcript. It was inverted here in the same slot rather than deleted, so the phase's RED and its GREEN occupy one place in the file.
- P-5, stated as a number rather than as an apology: the roadmap's former "approximately -60 dB" is a TARGET and is not reachable by 2-sample polyBLEP, whose spectral multiplier is a squared sinc that reaches only about -8 dB at Nyquist. DAFx-16 independently reports the same ceiling for a FOUR-point polyBLAMP. ROADMAP SC-4 was corrected on 2026-08-01 to per-shape measured thresholds; this table IS that gate.
- P-6, stated as the reason the third index exists: a threshold indexed by region and note alone cannot be both red-on-naive and green-on-corrected, because the triangle at C8 improves by 15.0 dB at character 0 and by exactly 0.0 dB at character 1 where the corner is already 7.7 samples wide and the D-03 factor correctly returns zero.
- THE ANTI-SOFTENING RULE, written as an instruction to the next agent: a threshold is NEVER adjusted to accommodate a measured shortfall. If a gated cell misses, the escalation is, in order — (1) enable the deferred narrow-pulse reach refinement documented in `src/dsp/MorphBlep.hpp`'s banner, which is the cheap place the iteration budget is meant to go; (2) if that is insufficient, STOP and surface to the operator with the measured shortfall, an impact assessment and a recommendation, because raising the kernel order to four points is an operator decision and not a silent implementation choice. Oversampling and minBLEP are forbidden in v2.0 by AA-05 and remain forbidden; the broad escalation path is v2.1 oversampling, explicitly not minBLEP.

IF ANY GATED CELL MISSES ITS THRESHOLD while executing this task, follow that escalation rule literally: do not loosen the number, do not proceed to Task 3, and report the shortfall with both options and a recommendation.

Finally, add the ANTI-CIRCULARITY assertion in the same case, because a threshold pinned from the implementation's own output proves nothing on its own: for each of the five named large-margin cells, `CHECK(naiveDb - correctedDb >= 8.0f)` — the correction must deliver at least 8 dB of measured improvement over the naive path at those cells. `CAPTURE` each delta. Comment that this is the assertion a threshold table cannot fake: it compares two measurements of the same apparatus and does not consult any pinned number.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco spectrum: TEST-03*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output shows `failing := 0` over the 45 gated cells and shows the five improvement deltas each at or above 8.0 dB
    - `grep -c 'kNaiveFailuresFloor' tests/test_vco_spectrum.cpp` outputs `0` — the tombstone's constant is gone because the tombstone was inverted
    - `grep -c 'was the D-08 RED tombstone' tests/test_vco_spectrum.cpp` outputs `1`
    - Every `provenance` string in `SPECTRUM_GRID` names a measured value; `grep -c 'PROVISIONAL' tests/test_vco_spectrum.cpp` outputs `0`
    - The doctest case count is unchanged from this plan's starting baseline — the tombstone was INVERTED in place
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_spectrum.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>TEST-03 is live at C7, C8 and C9 against per-shape thresholds pinned from this repository's own measurement, the D-08 tombstone is inverted in its own slot, and an anti-circularity improvement assertion sits beside the pinned numbers.</done>
</task>

<task type="auto">
  <name>Task 3: The no-regression invariant over all 90 cells, and the D-11 cross-rate equivalence</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <read_first>
    - tests/test_vco_spectrum.cpp — the measure pass and the live gate from Tasks 1 and 2
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "D-08 baseline and D-09 threshold evidence" reading 4 (the only regressions are about 1.5 dB, all at C9 on the sine row) and the § Alternatives table showing worst regressions of -60.4, -42.7, -36.6 and -29.8 dB for the rejected factors
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "Pattern 5" — the D-03 factor is what the no-regression invariant directly asserts the purpose of
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md § "Threshold Policy" recommended threshold form
  </read_first>
  <action>
Add a `TEST_CASE` named `"vco spectrum: band-limiting never makes any cell WORSE than the naive path (the D-03 compact-support invariant)"`.

Walk all 90 cells — gated, diagnostic AND cross-rate regression, with no exclusions — and for each `CHECK(correctedDb <= naiveDb + 2.0f)`. `CAPTURE` both values and the delta on every cell, and additionally `CAPTURE` a running worst regression across the whole grid.

Banner it with the argument that makes it the single most valuable assertion in this file, and give the numbers: this is the assertion that would have caught EVERY rejected alternative in the phase's own design search. A character factor without COMPACT SUPPORT still returns a small non-zero correction on edges that are already several samples wide, and that residual step-shaped correction is broadband INJECTED energy, not a filter. MEASURED worst regressions versus the naive path: -60.4 dB at full authority, -42.7 dB with a reciprocal-linear factor, -36.6 dB with a sinc-Pade fit, -29.8 dB with a reciprocal-quadratic factor, against -1.7 dB for the shipped compact-support form. State the tolerance's provenance: 2.0 dB, against a measured worst regression of about 1.5 dB confined to the sine row at C9 where the naive floor is already between -23 and -48 dB, so the margin is real but tight enough that any of the rejected forms fails immediately. Say plainly that this invariant, not the threshold table, is what asserts what the D-03 factor is FOR — and that a future refactor cannot use it to trade one shape against another.

Then add a `TEST_CASE` named `"vco spectrum: the D-11 cross-rate regression - the same note lands within 3 dB at 44.1, 48 and 96 kHz"`.

For each of the fifteen morph-by-character combinations, take the corrected alias peak at the C8 cells at 44100 (K = 389), 48000 (K = 357) and 96000 (K = 179), `CAPTURE` all three, and `CHECK` the 48 kHz and 96 kHz values are each within 3.0 dB of the 44.1 kHz value ABOVE it — that is, neither higher rate may be more than 3 dB worse. Note the asymmetry deliberately: a higher sample rate legitimately produces a LOWER alias floor (96 kHz carries 11 harmonics below Nyquist against C8's 5 at 44.1 kHz), so the assertion is one-sided by design and a two-sided band would fail on correct behavior.

Banner it with D-11's reason in full: a correction scaled wrongly by `dt` fails RATE-DEPENDENTLY, and that is the most likely way this implementation goes subtly wrong, so a single-rate gate would never see it. State that 44.1 kHz is the BINDING assertion and the other two are regression, that the three rows deliberately land on the same note so the comparison is like with like, and that this mirrors the Phase 31 lesson that the most natural version of a test can be bit-exactly vacuous.

Add one further assertion to the same case, because it is the cheapest possible direct test of `dt`-scaling: for the saw-centre cell at character 0, `CHECK` the corrected value at 96 kHz is at least as good as (no worse than) the corrected value at 44.1 kHz. The saw is the one shape whose correction is character-independent, so it isolates the `dt` scaling from every character effect.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco spectrum: band-limiting never makes any cell WORSE*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - `./build-test/test -tc="vco spectrum: the D-11 cross-rate regression*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output shows a worst-regression capture across all 90 cells, and it is at or below `2.0` dB
    - The `-s` output shows fifteen cross-rate triples with both higher-rate values within 3.0 dB above the 44.1 kHz value
    - The doctest summary reports at least 91 test cases (phase baseline before plan 32-01 was exactly 81) with 0 failed
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_spectrum.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>Every cell in the grid is proven no worse than the naive path within 2 dB, and the same note is proven to behave consistently across all three sample rates — the two assertions that catch a wrongly-scaled or non-compact correction independently of any pinned threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured evidence -> pinned threshold | A number pinned from the implementation's own output is circular unless an independent invariant bounds it. |
| 44.1 kHz assertion -> 48 and 96 kHz | A `dt`-scaling defect is invisible at a single rate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-15 | Tampering | threshold provenance and circular pinning | high | mitigate | Every threshold carries a measured provenance string; the no-regression invariant over all 90 cells and the 8 dB minimum-improvement assertion over the five named cells both compare two measurements and consult no pinned number. The anti-softening rule is written into the case banner with a two-step escalation that ends at the operator. |
| T-32-11 | Repudiation (evidence integrity) | the gate's own noise floor | high | mitigate | The per-cell D-10 leakage `REQUIRE` runs in front of every measurement, in both the measure pass and the live gate. |
| T-32-21 | Tampering (signal integrity) | a `dt`-scaled correction that is wrong at one rate only | high | mitigate | The D-11 cross-rate case asserts the same note within 3 dB one-sided at all three rates, plus a saw-centre isolation assertion that removes every character effect from the comparison. |
| T-32-22 | Tampering (signal integrity) | a correction that INJECTS alias energy rather than removing it | high | mitigate | The no-regression invariant over all 90 cells at a 2.0 dB tolerance. Measured, it fails immediately for every non-compact factor considered during design: worst regressions of -60.4, -42.7, -36.6 and -29.8 dB. |
| T-32-12 | Tampering | the shipped Analog LFO's golden bit-stability | high | mitigate | No `src/` file is touched by this plan; `make test` replays the six `.f32` goldens on every run; `src/AnalogLFO.cpp` is absent from the diff. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. |
</threat_model>

<verification>
- `make test` green with the case count risen to at least 91 and 0 failures.
- `failing == 0` over the 45 gated cells at C7, C8 and C9.
- Worst regression across all 90 cells at or below 2.0 dB.
- Five named cells each showing at least 8 dB of measured improvement over the naive path.
- Cross-rate triples within 3 dB one-sided at all fifteen morph-by-character combinations.
- `make guards` and `make strict` green; no `src/` file changed by this plan.
</verification>

<success_criteria>
- `SPECTRUM_GRID`'s threshold column is pinned from this repository's measurement, with per-cell provenance and no remaining provisional entries.
- TEST-03 is live and green at C7, C8 and C9; the D-08 tombstone is inverted in its own slot.
- The no-regression invariant and the cross-rate regression are both asserted and both independent of the pinned thresholds.
- Any measured shortfall was escalated rather than absorbed; no threshold was loosened.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-07-SUMMARY.md` when done. Record: the full 90-cell naive-versus-corrected table with deltas; every re-pinned threshold and any cell that moved more than 1 dB from the plan-32-03 provisional, with which side moved; the five improvement deltas; the worst regression across the grid; and the fifteen cross-rate triples.
</output>

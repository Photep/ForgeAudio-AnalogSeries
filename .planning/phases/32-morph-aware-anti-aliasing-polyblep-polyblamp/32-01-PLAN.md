---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/check_includes.sh
  - tests/test_vco_spectrum.cpp
autonomous: true
nyquist_compliant: true
requirements: [TEST-03]
must_haves:
  truths:
    - "D-08: the spectral apparatus and the naive baseline exist BEFORE any band-limiting, so the alias-floor gate can be observed genuinely RED against the naive core."
    - "D-10: alias energy is measured with integer cycles per block and a rectangular window, so leakage is exactly zero and the gate measures the DSP rather than its own window."
    - "D-10 self-check: the gate asserts its own achieved bin error implies a leakage floor at least 10 dB below any threshold it will assert."
    - "The naive path stays callable as a test-only mirror (CONTEXT discretion, PATTERNS: the tests/test_vco_core.cpp:349-418 in-repo pattern), with NO flag and NO second entry point under src/."
    - "P-8: both new test TUs are pre-registered in check_includes.sh VCO_SIDE_ALLOW BEFORE they exist, the Phase-29 precedent."
  artifacts:
    - tests/test_vco_spectrum.cpp
    - tests/check_includes.sh
  key_links:
    - "NaiveVcoCoreMirror::step mirrors forge::VcoCore::step line for line and is REQUIREd bit-identical to it in this plan; that REQUIRE inverts in plan 32-06."
    - "binCentredPitchCV feeds forge::VcoBlockDriver unchanged — the driver's unconditional sampleTime overwrite must never become conditional."
---

<objective>
Build the Phase 32 spectral apparatus and the test-only naive mirror, and pre-register both new test translation units with the include guard — all before one line of band-limiting exists (D-08).

Purpose: D-08's ordering is the phase's objective iteration metric. A gate written against already-passing code proves nothing, so the DFT helper, the bin construction (D-10), the leakage self-check and the naive path this phase measures against must all land first. This plan builds the apparatus; plan 32-03 runs the baseline sweep and records the RED.

Output: `tests/test_vco_spectrum.cpp` (new) carrying an exact-integer-cycle radix-2 FFT, the alias-peak classifier, the bin-centred `pitchCV` solver with its own leakage self-check, and `NaiveVcoCoreMirror`; plus two `VCO_SIDE_ALLOW` entries in `tests/check_includes.sh`.

Pre-planning note: the D-02 (ROADMAP SC-2 "characterized") and D-06 (REQUIREMENTS AA-01 "morph and bleed weights") document edits are ALREADY COMMITTED (462b473), as is the SC-4 per-shape-threshold correction (7fc7634). No task in this phase re-applies them.
</objective>

<!-- planner-discipline-allow: Approx -->
<!-- planner-discipline-allow: DOCTEST_CONFIG_IMPLEMENT -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — members `float pending`, `float inject`; methods `void reset()`, `void addStep(float xAhead, float jump)`, `float step(const Waveshape& wv, double phase, float p, double dt, float morph, float character)`
- `forge::VcoCore::blep` — a `forge::MorphBlep` held BY VALUE beside `wave` (per-instance, CORE-03 / D-14)
- `AnalogVCO::MORPH_ATTEN_PARAM` (ParamId enum entry), `AnalogVCO::MORPH_CV_INPUT` (InputId enum entry)
- Test-only, anonymous namespace in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `SpectrumCell`
- `res/AnalogVCO.svg` — two additional 10x10 marker rects at (25.48, 75) and (55.96, 75)

THIS PLAN produces: `tests/test_vco_spectrum.cpp` (FFT helper, alias classifier, bin solver, `NaiveVcoCoreMirror`) and the two `VCO_SIDE_ALLOW` entries.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-register both new VCO-side test TUs in check_includes.sh VCO_SIDE_ALLOW</name>
  <files>tests/check_includes.sh</files>
  <read_first>
    - tests/check_includes.sh — read §[1/7] in full, lines 254-345: the denylist derivation, the six existing exemptions, the Phase-31 D-23 rationale-comment precedent at :300-316, and the standing "ADDING TO THE EXEMPTION LIST IS THE DANGEROUS EDIT HERE" warning at :320-322
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md § "tests/check_includes.sh (MODIFIED — guard script)"
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-8
  </read_first>
  <action>
Add exactly two entries to the `VCO_SIDE_ALLOW` array in `tests/check_includes.sh` §[1/7]: `"tests/test_vco_spectrum.cpp"` and `"tests/test_morph_blep.cpp"`. Both are added BEFORE the files exist — that is the Phase-29 precedent the existing `tests/test_pitch` entry's comment already records, and it is the whole point of doing this as a planned task rather than as a gate-time discovery (Phase 31 D-23 / P-8).

Extend the rationale-comment block that ends at line 316 with a new paragraph covering both files, in the same voice as the `tests/test_vco_pitch.cpp` paragraph immediately above it. The paragraph must state, in words: (a) both are VCO-side test translation units whose entire purpose is to drive `forge::VcoCore` and `forge::MorphBlep`, includes this section flags BY CONSTRUCTION; (b) `tests/test_vco_spectrum.cpp` is Phase 32's TEST-03 alias-floor suite and `tests/test_morph_blep.cpp` is its AA-02/AA-03/AA-04 unit suite; (c) neither is an LFO translation unit and neither links into any LFO build graph; (d) both entries are added BEFORE the files exist, deliberately, because the entry two paragraphs above was instead added reactively after Phase 30's TU had already turned this section red; (e) the match compares against a QUOTED right-hand side, so it is exact-path — no glob, no substring, no basename, and no sibling path is exempted along with it.

Note explicitly in the same paragraph, because it prevents a future editor from "completing" the wiring: `src/dsp/MorphBlep.hpp` itself needs NO guard edit. It is already pre-wired at `check_includes.sh` lines 90, 261 and 337 and at `check_canary.sh` lines 414, 452-453 and 466. The only other guard-side edit this phase makes is activating the canary include, which belongs to plan 32-04.

Do not touch any other section, do not reorder the existing six entries, and do not widen any match from `==` to a glob.
  </action>
  <verify>
    <automated>make guards</automated>
  </verify>
  <acceptance_criteria>
    - `make guards` exits 0 and prints `guard suite: PASS`
    - `bash tests/check_includes.sh` prints an OK line for section `[1/7]` and exits 0
    - `grep -c 'tests/test_vco_spectrum.cpp' tests/check_includes.sh` is at least 1 and `grep -c 'tests/test_morph_blep.cpp' tests/check_includes.sh` is at least 1
    - The `VCO_SIDE_ALLOW` array contains exactly 8 quoted entries (was 6)
    - `git diff --stat tests/check_includes.sh` shows insertions only, zero deletions of existing array entries
    - `git diff --name-only` does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The two new test TUs are exempt from the [1/7] LFO-side scan by exact path, the exemption carries its written rationale, and `make guards` is green before either file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The exact-integer-cycle spectral apparatus (D-10) with its own leakage self-check</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <behavior>
    - `fftRadix2` of a pure real cosine at an integer cycle count K over N = 4096 puts all energy at bins K and N-K, with every other bin at float-noise level (verified by an in-test synthetic control, not by inspection)
    - `aliasPeakDb` on that synthetic returns a value below -140 dB, because a perfect single harmonic has no alias energy
    - `aliasPeakDb` on a synthetic with a deliberate spur injected at a known non-harmonic bin returns that spur's amplitude in dB, to within 0.1 dB — the classifier is validated by DETECTING, never by a green run
    - `binCentredPitchCV(sr, K, N)` returns a `pitchCV` whose resulting `deltaPhase` sits within 2e-3 bins of K/N at all three sample rates
    - the implied rectangular-window leakage `20*log10(binErrorInBins)` is at least 10 dB below -62 dB (the tightest threshold this suite will ever assert) at every grid row
  </behavior>
  <read_first>
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Validation Architecture, in full — "The spectral construction (D-10), concretely", the test-frequency table, the alias metric, and the bin-centre placement table
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-7
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md § "Spectral Construction (D-10) — pinned parameters"
    - tests/test_vco_core.cpp lines 76-150 — the TU/include shape, the `SAMPLE_RATES` and `coreBase()` idiom, and `estimateFreqRising`'s helper-banner shape (state why the helper is structurally sound, over what grid it was validated, and name the simplification that must not be reintroduced)
    - tests/test_vco_core.cpp lines 466-499 — the driving pattern and the non-vacuity `REQUIRE` that precedes the value assertion
    - tests/VcoBlockDriver.hpp lines 49-64 — the unconditional `sampleTime`/`sampleRate` overwrite that constrains the bin-centre construction to bisecting `pitchCV`
    - src/dsp/VcoCore.hpp lines 284-493 — `step()` in full, for the exact pitch/guard/accumulate sequence the solver must replicate
  </read_first>
  <action>
Create `tests/test_vco_spectrum.cpp` as a doctest TU that does NOT define the doctest impl macro (`tests/main.cpp` owns it). Include `"doctest.h"`, then `"VcoBlockDriver.hpp"`, then `<vector>`, `<complex>`, `<cmath>`, `<cstddef>`, each with a trailing comment naming the one symbol it supplies — the `tests/test_vco_core.cpp:78-86` idiom.

Write a file banner in the house voice covering: what this suite is (TEST-03's alias-floor gate and the D-08 naive baseline); the D-10 gate-correctness argument in full — integer cycles per block means the rectangular window is EXACT, leakage is exactly zero, every harmonic and every folded alias lands dead on a bin centre, so "alias energy" is simply the magnitude at non-harmonic bins with no guard bands and no window coefficients; and the coprimality argument, which is the whole justification for the bin classification: with `N = 4096 = 2^12`, `gcd(K, N) = 1` for EVERY odd `K`, harmonic `n` sits at bin `nK`, a folded alias lands on `nK mod N` reflected, and an alias collides with a true harmonic only if `N` divides `(n +/- m)K`, which for coprime `K` requires `|n +/- m| >= 4096` — i.e. never, for any harmonic the waveform actually carries. Choosing `N` a power of two therefore reduces D-10's coprimality requirement to "pick an odd number". State that the test frequencies sit at BIN CENTRES rather than at exact equal-tempered notes, that this is irrelevant to aliasing behavior, and that a later agent must not "fix" it.

In an anonymous namespace, define:

1. `constexpr int kSpectrumN = 4096;` and `constexpr double SAMPLE_RATES[] = {44100.0, 48000.0, 96000.0};`.

2. `void fftRadix2(std::vector<std::complex<double>>& a)` — an in-place iterative radix-2 decimation-in-time complex FFT in `double`: bit-reversal permutation, then `log2(n)` butterfly stages with twiddles from `cos`/`sin`. libm is available here because this is `tests/`, not `src/` (the D-18 precedent); say so in the helper banner. Assert `n` is a power of two with a `REQUIRE`-free precondition comment plus an early return on a non-power-of-two size.

3. `double aliasPeakDb(const std::vector<float>& block, int K, int* aliasBinOut, double* aliasRmsDbOut)` — copy the block into a complex vector, `fftRadix2`, then compute `20*log10(max over i in [1, N/2] excluding H of |X_i| / |X_K|)` where `H = { n*K : 1 <= n <= (N/2 - 1)/K }`. Bin 0 is EXCLUDED and the banner must say why: the narrow pulse legitimately carries DC and the DC blocker is Phase 34's (OUT-02). Report the winning bin through `aliasBinOut` and the alias RMS through `aliasRmsDbOut` — the peak is the gate, the RMS is the diagnostic. Guard `|X_K| == 0` by returning a large negative sentinel and document it.

4. `float binCentredPitchCV(double sr, int K, double* achievedBinErrorOut)` — solve for the `pitchCV` that puts `deltaPhase` on the bin centre `K / kSpectrumN`. Replicate `VcoCore::step`'s own chain exactly: `freq = forge::kVcoFreqC4 * forge::exp2_taylor5(pitchVolts)`, then the Nyquist ceiling `forge::kVcoNyquistGuardFrac * sr`, then the negated floor, then `deltaPhase = (double)freq * (double)dt` with `dt = (float)(1.0 / sr)` — the value `forge::VcoBlockDriver` injects. Bisect `pitchCV` over `[-2.f, +8.f]` for 60 iterations, then scan +/-4096 ULPs around the resulting float with `std::nextafterf` and keep the candidate minimising `|deltaPhase - K/N|`. Write the achieved error IN BINS (`|deltaPhase - K/N| * kSpectrumN`) out through `achievedBinErrorOut`. RESEARCH measured 2.3e-4 to 1.5e-3 bins with this method; state those two figures in the banner as the expected envelope.

5. `double impliedLeakageDb(double binError)` — `20*log10(binError)`. This is the rectangular-window leakage into the adjacent bin at a fractional bin offset. Corroborate it in the banner against RESEARCH's two MEASURED points: 1.5e-3 bins gives -56.5 dB and 2.3e-4 bins gives -73 dB. The banner must state the failure this exists to stop, in D-10's own words: without this self-check the gate can pass by measuring `forge::exp2_taylor5`'s output granularity rather than the DSP, which is D-10's stated failure mode wearing a different costume.

Then write a single `TEST_CASE` named `"vco spectrum: the DFT apparatus is validated by DETECTING a planted spur, and its own leakage floor sits 10 dB under the tightest threshold this suite asserts (D-10)"` containing three parts:

Part A, the POSITIVE CONTROL. Synthesise `kSpectrumN` samples of `cos(2*pi*K*i/N)` at `K = 389`, run `aliasPeakDb`, and `CHECK` the result is below -140 dB. A perfect single harmonic has no alias energy; this proves the classifier does not manufacture a floor.

Part B, the DETECTION CONTROL — the load-bearing half, and it must never be deleted. Take the same synthetic and add a spur of amplitude `1e-3` at a NON-harmonic bin (use bin `K + 1`, which is not a multiple of `K`). `CHECK` that `aliasPeakDb` now returns `-60.0` dB within `0.1` dB and that the reported alias bin is exactly `K + 1`. Banner it with the same argument `tests/check_includes.sh` §[6/7] and `tests/test_vco_core.cpp` invariant 5 carry: a detector that has only ever been observed green is indistinguishable from one that cannot fail.

Part C, the LEAKAGE SELF-CHECK. For each of the six grid rows in RESEARCH's test-frequency table — `(44100, 97)`, `(44100, 195)`, `(44100, 389)`, `(44100, 777)`, `(48000, 357)`, `(96000, 179)` — call `binCentredPitchCV`, `CAPTURE` the achieved bin error and the implied leakage in dB, `CHECK` the achieved error is below `2e-3` bins, and `CHECK` that `impliedLeakageDb` is at least 10 dB below `-62.0` dB (the tightest threshold this suite will ever assert, per the D-09 sine row at C7). Also `CHECK` each `K` is odd, with a one-line comment pointing at the coprimality argument in the banner — that assertion is the mechanical form of the whole bin classification.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0; the doctest summary reports at least 82 test cases (baseline before this plan is exactly 81) with 0 failed
    - `./build-test/test -tc="vco spectrum: *" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed — a selector matching zero cases also exits 0 and prints `Status: SUCCESS!`, so the case COUNT is the gate, not the exit status
    - The `-s` output for that case shows the six `achievedBinError` captures, each below `2e-3`
    - `grep -c 'DOCTEST_CONFIG_IMPLEMENT' tests/test_vco_spectrum.cpp` outputs `0`
    - `make guards` exits 0 (the [1/7] exemption from Task 1 now covers a file that actually exists)
    - `git diff --name-only` does not list `src/AnalogLFO.cpp`, `src/dsp/Waveshape.hpp` or `src/dsp/RackCompat.hpp`
  </acceptance_criteria>
  <done>The spectral apparatus exists, is proven to detect a planted spur at a known amplitude and bin, and asserts its own noise floor sits 10 dB below the tightest threshold this phase will pin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NaiveVcoCoreMirror, REQUIREd bit-identical to the live core</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <behavior>
    - `NaiveVcoCoreMirror` driven over 4096 samples at all three sample rates, at five morph positions and three character values, reproduces `forge::VcoCore`'s block BIT-EXACTLY (float `!=`, never `doctest::Approx`) — because at this point in the phase the live core IS naive
    - the two blocks are non-trivial: at least 90 percent of samples are non-zero and the block is not constant
  </behavior>
  <read_first>
    - tests/test_vco_core.cpp lines 259-418 — `DeliberatelyBrokenSharedStateCore` in full: the banner shape, the `>>> THE DELIBERATE DEFECT, AND THE ONLY ONE. <<<` marker, the mirror-maintenance rule at :303-314, and the "IF ANY OF THOSE FIGURES EVER MOVES ... STOP AND REPORT IT RATHER THAN UPDATING THE NUMBER" clause at :344-347
    - src/dsp/VcoCore.hpp lines 284-493 — the pitch/guard/accumulate/waveshape/scale sequence to mirror line for line
    - tests/test_vco_core.cpp lines 1040-1057 — the validity-first `REQUIRE` idiom (assert the fixture tests what it claims BEFORE asserting the result)
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md § "Naive-path oracle pattern"
  </read_first>
  <action>
Add `struct NaiveVcoCoreMirror` to the anonymous namespace of `tests/test_vco_spectrum.cpp`. It mirrors `forge::VcoCore` field for field and line for line: a per-instance `forge::DriftEngine drift`, a per-instance `forge::Waveshape wave`, a per-instance `double phase`, `seed(...)` and `setSpreadSeed(...)` performing the same D-11 five-coefficient copy, and a `float step(const forge::VcoInputs&)` reproducing the volt-domain pitch summation, the `forge::kVcoMaxPitchVolts` bound with the negated comparison FIRST as the NaN catcher, the single `forge::exp2_taylor5` off `forge::kVcoFreqC4`, the sanitised rate, the ceiling-then-negated-floor order, the `forge::kVcoMaxDeltaPhase` bound, the single-subtract wrap, and one call into `wave.morphedWave(p, morph, character, 0.f)` scaled by 5.

State the divergence in a banner in the same voice and with the same marker as `DeliberatelyBrokenSharedStateCore`'s: this mirror's ONE deliberate divergence is that it applies NO band-limiting correction — it is the permanent D-08 NAIVE BASELINE and it must stay naive forever, because the no-regression invariant in plan 32-07 compares the corrected core against it. State the containment argument too: it lives in this TU's anonymous namespace, has internal linkage, appears in no header and in no shipped build graph, and must NEVER be moved under `src/`. State the mirror-maintenance rule: every future change to `forge::VcoCore::step`'s pitch/guard/accumulate sequence must be mirrored here, and if mirroring one ever MOVES a figure this file records, that is a finding about the real core, not a bookkeeping update.

Add a `TEST_CASE` named `"vco spectrum: NaiveVcoCoreMirror is bit-identical to the live forge::VcoCore (D-08 baseline validity) - THIS CASE INVERTS IN PLAN 32-06"`. For each of the three sample rates, over morph in {0.00, 0.25, 0.50, 0.75, 1.00} and character in {0.0, 0.5, 1.0}, drive 4096 samples of `forge::VcoCore` through `forge::VcoBlockDriver` and 4096 samples of `NaiveVcoCoreMirror` through a local loop that injects `sampleTime` and `sampleRate` identically and uses the SAME four seed literals the driver defaults to (`0x1234`, `0x5678`, `0x9E3779B9`, `0x7F4A7C15` — copied verbatim, never invented, and never a pair of zeros: a degenerate Xoroshiro seed emits an all-zero stream and makes the normal distribution's rejection loop never terminate, which in Rack is a hang on patch load). Count differing samples with a direct float `!=`, never `doctest::Approx` — Approx's `epsilon(0)` still applies a relative-scaling margin and is not a bit-exact comparator.

Assert non-vacuity FIRST: `REQUIRE` that at least 90 percent of the core's samples are non-zero and that the block is not constant, so "identical" cannot be satisfied by two blocks of silence. Then `CAPTURE` the differing count and `CHECK(differing == 0)`.

Write the inversion contract into the case name AND into a comment above the assertion, in the shape Phase 29's silence tombstone used and Phase 30 honoured: THIS CASE IS A TOMBSTONE. It is true only for exactly as long as `forge::VcoCore::step` applies no correction. Plan 32-06 lands `MorphBlep` at the `VcoCore.hpp:484` call site and MUST invert this case in place — same slot, same grid, same seeds — into an assertion that the two now DIVERGE, and that they diverge by exactly the correction `forge::MorphBlep::step` returns. Deleting the case instead of inverting it silently removes the only proof that the baseline is a faithful copy of the core.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0; the doctest summary reports at least 83 test cases (baseline 81) with 0 failed
    - `./build-test/test -tc="vco spectrum: NaiveVcoCoreMirror*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output for that case shows `differing := 0` at all three sample rates
    - `grep -c 'NaiveVcoCoreMirror' tests/test_vco_spectrum.cpp` is at least 3, and `grep -rc 'NaiveVcoCoreMirror' src/` finds no matches (containment: the mirror never reaches src/)
    - Comment-stripped source uses no approximate comparator: `grep -v '^[[:space:]]*//' tests/test_vco_spectrum.cpp | grep -c 'Approx'` outputs `0`
    - `make guards && make strict` both exit 0
    - `git diff --name-only` does not list `src/AnalogLFO.cpp` and `git status --porcelain src/dsp/FROZEN.sha256` is empty
  </acceptance_criteria>
  <done>A test-only naive mirror exists, is proven bit-identical to the live core over a non-trivial grid, and carries an explicit inversion contract naming plan 32-06 as the plan that must flip it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> `forge::VcoCore::step` | The suite constructs `forge::VcoInputs` directly and may supply any float, including hostile timing. Nothing sanitises it upstream. |
| `tests/` -> `src/` include direction | A new test TU that reaches a VCO header changes the `check_includes.sh` [1/7] LFO-side scan set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-08 | Tampering | `tests/check_includes.sh` VCO_SIDE_ALLOW | high | mitigate | Add exactly two exact-path entries; the array grows 6 -> 8 and is asserted at that count. Never widen a match from `==` to a glob or a basename; the `[6/7]` negative controls prove the exemption's width. |
| T-32-09 | Denial of Service | `NaiveVcoCoreMirror` seeding | high | mitigate | Reuse the four proven non-degenerate seed literals verbatim. A `(0,0)` Xoroshiro seed is a fixed point emitting an all-zero stream, which makes the normal distribution's rejection loop never terminate — in Rack a hang on patch load, not a failing test. |
| T-32-10 | Repudiation (evidence integrity) | the spectral classifier | high | mitigate | `aliasPeakDb` is validated by DETECTING a planted spur at a known amplitude and bin, never by a green run. A classifier proven only green is indistinguishable from one that cannot fail — the standing posture of `check_frozen.sh` [3/3], `check_includes.sh` [6/7] and `check_canary.sh` [4/5]. |
| T-32-11 | Repudiation (evidence integrity) | the gate's own noise floor | high | mitigate | `impliedLeakageDb` is asserted at least 10 dB below the tightest threshold the suite will pin. Without it the gate can pass by measuring `forge::exp2_taylor5`'s output granularity instead of the DSP (D-10 / P-7). |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | This phase installs ZERO packages in any ecosystem. `32-RESEARCH.md` § Package Legitimacy Audit records `(none)` in every column; every dependency is in-tree, frozen, or standard library. No install checkpoint applies because there is no install step. |
</threat_model>

<verification>
- `make test` green with the case count risen from 81 to at least 83 and 0 failures.
- `make guards` green: `check_frozen.sh` (no frozen file touched, `FROZEN.sha256` unbumped), `check_includes.sh` (both new exemptions exact-path), `check_canary.sh` (untouched).
- `make strict` green — no `src/` file changed, so this is a regression check rather than a new gate.
- The spectral classifier is observed detecting a planted spur, and the bin solver's achieved error is recorded per grid row.
- `src/AnalogLFO.cpp` is absent from this plan's diff.
</verification>

<success_criteria>
- `tests/test_vco_spectrum.cpp` exists with `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb` and `NaiveVcoCoreMirror`.
- `tests/check_includes.sh` `VCO_SIDE_ALLOW` holds exactly 8 entries including both new test files, with a written rationale.
- The naive mirror is proven bit-identical to `forge::VcoCore` over a 45-point grid at three rates, with the inversion contract written into the case name.
- No frozen header is edited; no `FROZEN.sha256` line moves.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-01-SUMMARY.md` when done. Record: the six measured `achievedBinError` values and their implied leakage in dB; the doctest case and assertion counts before and after; and the exact `VCO_SIDE_ALLOW` array size.
</output>

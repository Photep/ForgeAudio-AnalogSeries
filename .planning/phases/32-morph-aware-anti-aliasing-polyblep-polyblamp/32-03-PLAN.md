---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - tests/test_vco_spectrum.cpp
autonomous: true
nyquist_compliant: true
requirements: [TEST-03]
must_haves:
  truths:
    - "D-08: the alias floor of today's deliberately-aliased oscillator is RECORDED per shape, per note and per character before any band-limiting exists — the threshold is set from measurement rather than inherited."
    - "D-08: the alias-floor gate is OBSERVED FAILING against the naive core, with the failure transcribed verbatim, and is then landed as a tombstone that asserts the failing reality until plan 32-07 inverts it."
    - "D-09: thresholds are per (morph region, note, character) and each carries its measured justification in the test; a single global threshold and a global-floor-with-pulse-carve-out are both rejected."
    - "D-11: 44.1 kHz is the binding assertion; 48 and 96 kHz land on the SAME note (C8) so the cross-rate regression compares like with like."
    - "Operator decision 2026-08-01: the gate asserts on C7, C8 AND C9. C6 is retained as a diagnostic row. The roadmap's former 'approximately -60 dB' is a target, not a reachable floor (P-5)."
  artifacts:
    - tests/test_vco_spectrum.cpp
  key_links:
    - "SPECTRUM_GRID's thresholdDb column is PROVISIONAL here (32-RESEARCH prototype + 3 dB) and is re-pinned in plan 32-07 from this repo's own measurement of the real forge::MorphBlep."
    - "measureCellDb drives NaiveVcoCoreMirror in this plan and forge::VcoCore in plan 32-07 through the same code path, so the naive-versus-corrected delta is a like-for-like comparison."
---

<objective>
Record the naive alias floor across the full D-09/D-11 grid, then observe the alias-floor gate FAILING against that naive core and land it as a tombstone (D-08).

Purpose: D-08's three payoffs, in the phase's own words — the threshold is set from measurement rather than inherited; the phase gets an objective iteration metric instead of ear-guessing, which is what its deliberate iteration budget is for; and the RED is genuine, because the gate provably fails before `MorphBlep` lands rather than being written against already-passing code. This plan is the "measure" half of the measure→pin loop; plan 32-07 is the "pin" half and runs the same code.

Output: `SPECTRUM_GRID` (90 cells), `measureCellDb`, the recorded naive baseline, and a tombstone case asserting the naive core fails the gate — with the inversion contract naming plan 32-07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-01-SUMMARY.md
@tests/test_vco_spectrum.cpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — members `float pending`, `float inject`; methods `void reset()`, `void addStep(float xAhead, float jump)`, `float step(const Waveshape& wv, double phase, float p, double dt, float morph, float character)`
- `forge::VcoCore::blep` — a `forge::MorphBlep` held BY VALUE beside `wave`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`, and the D-08 baseline plus tombstone cases.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 90-cell SPECTRUM_GRID and the recorded naive baseline (D-08 / D-09 / D-11)</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <behavior>
    - `measureCellDb` on the sine cell at character 0 returns an alias peak below -140 dB at every note — a pure sine at character 0 has no discontinuity of its own and no bleed ring, so there is nothing to alias
    - `measureCellDb` on the 5-percent pulse at C9 / character 0 returns an alias peak above -5 dB — the naive worst case, and the proof the apparatus is looking at a genuinely aliased signal
    - every cell's achieved bin error implies a leakage floor at least 10 dB below that cell's own threshold, asserted per cell before the alias value is read
  </behavior>
  <read_first>
    - tests/test_vco_spectrum.cpp in full — `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `NaiveVcoCoreMirror` and the banner from plan 32-01
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md § "Spectral Construction (D-10) — pinned parameters" and § "Threshold Policy (D-09)" — the naive-to-corrected matrix is the source of the provisional threshold column
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "D-08 baseline and D-09 threshold evidence" and § Common Pitfalls P-5, P-6
    - tests/test_vco_pitch.cpp lines 1127-1170 — the `struct Row` / `GRID` / `role`-string table idiom, and the lettered comment sections stating what each group proves
    - tests/test_vco_pitch.cpp lines 1195-1205 — the skip-not-clip idiom
  </read_first>
  <action>
Add to the anonymous namespace of `tests/test_vco_spectrum.cpp`:

`struct SpectrumCell { double sr; int K; const char* note; float morph; const char* region; float character; float thresholdDb; const char* tier; const char* provenance; };`

and `static const SpectrumCell SPECTRUM_GRID[]` with 90 entries, laid out in lettered comment sections in the `tests/test_vco_pitch.cpp` GRID style, each section stating what the group proves:

(a) THE GATED GRID — 44100 Hz at K = 195 (C7), K = 389 (C8) and K = 777 (C9), each crossed with the five shape centres morph = 0.00 sine, 0.25 triangle, 0.50 saw, 0.75 square, 1.00 pulse, each crossed with character = 0.00, 0.50, 1.00. Forty-five cells, `tier` = "gated". Note in the section comment that the operator settled this on 2026-08-01: the gate asserts on C7, C8 AND C9, superseding the undefined "top two octaves", and ROADMAP SC-4 has already been edited to say so.

(b) THE DIAGNOSTIC ROW — 44100 Hz at K = 97 (C6), same fifteen morph-by-character cells, `tier` = "diagnostic". These are `CAPTURE`d and never `CHECK`ed. The section comment must say why C6 earns its runtime: it is where the naive path is ALREADY clean at high character (the square measures -60.1 dB naive at character 1), so it is the row that would expose an over-correcting factor as a REGRESSION rather than as a miss.

(c) THE D-11 CROSS-RATE REGRESSION — 48000 Hz at K = 357 and 96000 Hz at K = 179, both landing on C8, same fifteen cells each, `tier` = "regression". The section comment must state D-11's reason in full: a correction scaled wrongly by `dt` fails RATE-DEPENDENTLY and is invisible at a single rate, which is the most likely way this implementation goes subtly wrong; and the two extra rates deliberately land on the SAME note as the 44.1 kHz C8 row so the comparison is like with like.

Fill `thresholdDb` from `32-VALIDATION.md`'s § Threshold Policy matrix, CORRECTED column, plus a 3 dB margin, rounded outward (toward the less negative value). Set every `provenance` string to name the source and the fact that it is provisional — the exact form: PROVISIONAL, from the 32-RESEARCH prototype's corrected column plus 3 dB; re-pinned in plan 32-07 from this repository's own measurement of the real `forge::MorphBlep`. Where the matrix has no row for a note or character, interpolate from the nearest measured character and say so in the provenance string; do not invent a number without saying where it came from.

Write the section header comment for the whole table carrying the two facts that stop a later agent softening it. First, P-5: the roadmap's former "approximately -60 dB" is UNREACHABLE and that is a property of 2-sample polyBLEP itself, not of this implementation — the kernel multiplies the spectrum by a squared sinc, which is only about -8 dB at Nyquist, and DAFx-16 independently reports the same ceiling for a FOUR-point polyBLAMP. Second, P-6: a threshold indexed by region and note ALONE cannot be both red-on-naive and green-on-corrected, because the triangle at C8 improves by 15.0 dB at character 0 and by exactly 0.0 dB at character 1 — where the corner is already 7.7 samples wide and the D-03 factor correctly returns zero. That is why the third index exists.

Then add:

`double measureCellDb(const SpectrumCell& cell, bool useMirror, double* aliasRmsDbOut, double* binErrorOut)` — resolve `binCentredPitchCV(cell.sr, cell.K, binErrorOut)`, build a constant `forge::VcoInputs` at that pitch with `cell.morph` and `cell.character`, drive `2 * kSpectrumN` samples (the first `kSpectrumN` is the discarded warm-up block — the pending accumulator and the phase must reach steady state, and with `deltaPhase` on the bin centre the following block is exactly periodic), and return `aliasPeakDb` over the second block. When `useMirror` is true, drive `NaiveVcoCoreMirror` through a local loop injecting `sampleTime` and `sampleRate` exactly as `forge::VcoBlockDriver` does; when false, drive `forge::VcoCore` through `forge::VcoBlockDriver`. Both branches use the same four proven non-degenerate seed literals. Banner the helper stating that BOTH branches exist from the start so plan 32-07's corrected measurement runs the identical code path — a comparator whose two sides run different loops proves nothing about the difference, which is the `check_includes.sh` [6/7] argument and the `runInterleaveCheck` template argument in `tests/test_vco_core.cpp:170-180`.

Add a `TEST_CASE` named `"vco spectrum: the NAIVE alias floor, recorded per shape, note and character (D-08 baseline)"`. Walk all 90 cells with `useMirror = true`. For every cell: `CAPTURE` the rate, `K`, note, region, character, tier, the achieved bin error, the implied leakage in dB, the alias peak in dB and the alias RMS in dB. Before reading the alias value, `REQUIRE` that `impliedLeakageDb(binError)` is at least 10 dB below `cell.thresholdDb` — the D-10 self-check, per cell, so the gate can never pass by measuring `forge::exp2_taylor5`'s output granularity.

Assert only the two structural sanity properties, both with wide measured margins so they are robust across toolchains:
- every sine cell at character 0.00 returns below -140 dB (a sine at character 0 has no discontinuity and no bleed ring; RESEARCH measured -150.7 dB)
- the morph = 1.00 cell at K = 777 (C9), character 0.00 returns above -5.0 dB (RESEARCH measured -0.3 dB; this is the proof the apparatus is looking at a genuinely aliased signal rather than at silence)

Assert nothing else here. This case's job is to RECORD, and the recorded figures are what plan 32-07 pins against.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco spectrum: the NAIVE alias floor*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - `sizeof(SPECTRUM_GRID) / sizeof(SPECTRUM_GRID[0])` is 90, asserted in the case with a `REQUIRE`
    - The `-s` output contains 90 `aliasPeakDb` captures, and every `impliedLeakage` capture is at least 10 dB below its cell's threshold
    - `grep -c 'PROVISIONAL' tests/test_vco_spectrum.cpp` is at least 1
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_spectrum.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The naive alias floor is recorded for all 90 cells with per-cell leakage self-checks, and the grid table carries provisional thresholds with written provenance and the P-5/P-6 reasoning that stops them being softened.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the alias-floor gate RED against the naive core, then land it as a tombstone (D-08)</name>
  <files>tests/test_vco_spectrum.cpp</files>
  <read_first>
    - tests/test_vco_spectrum.cpp — `SPECTRUM_GRID` and `measureCellDb` from Task 1
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § "D-08 baseline and D-09 threshold evidence" — the naive-to-corrected matrix, and reading 4 (the only regressions are about 1.5 dB, all at C9 on the sine row)
    - .planning/phases/30-vcocore-skeleton-module-registration/30-CONTEXT.md and .planning/STATE.md § Accumulated Context — the Phase 29 silence TOMBSTONE and the Phase 30 decision that INVERTED it in place, same slot, and observed it red against a silenced core
    - tests/test_vco_core.cpp lines 1077-1106 — the positive-control banner shape ("IF THIS CASE EVER GOES GREEN, INVARIANT 4 ABOVE IS MEANINGLESS")
  </read_first>
  <action>
This task is executed in two observable steps and committed once, green.

STEP ONE — OBSERVE THE RED. Write the alias-floor gate in its FINAL form: a `TEST_CASE` that walks every `tier == "gated"` cell and `CHECK`s `measureCellDb(cell, /*useMirror=*/false, ...) <= cell.thresholdDb`. Build and run it against the current, naive `forge::VcoCore`. It MUST fail. Capture the doctest output VERBATIM — the failing cell list, the reported and expected dB values, and the summary line's failed-assertion count — and paste that transcript into the plan SUMMARY under a heading naming it the D-08 RED evidence. Do NOT commit this state. If it does not fail, STOP and report: either the thresholds were mis-transcribed or `measureCellDb`'s `useMirror = false` branch is not reaching the live core, and both are findings, not bookkeeping.

STEP TWO — INVERT IT IN PLACE. Convert the same case, in the same slot, into the tombstone. Rename it `"vco spectrum: TOMBSTONE - the NAIVE core FAILS the Phase 32 alias-floor gate (D-08 RED evidence) - INVERTS IN PLAN 32-07"`. Keep the same walk and the same per-cell `CAPTURE`s. Change the assertions to:

- a counter `failing` over the 45 gated cells, incremented where `naiveDb > cell.thresholdDb`, with `CHECK(failing >= kNaiveFailuresFloor)`. Set `kNaiveFailuresFloor` to the count OBSERVED in step one MINUS 5, so the assertion is robust to a cell flipping across a threshold by a decibel or two on a different toolchain. Record the exact observed count in the comment above the constant, alongside the instruction that the observed number is a measurement and the constant is a floor derived from it — do not "tighten" the floor to the observed count later.
- an explicitly NAMED large-margin subset, asserted cell by cell, where RESEARCH shows at least 8 dB of expected improvement and the naive value therefore misses by a wide margin: 44100/K=389/morph 0.50 saw/character 0.00; 44100/K=389/morph 0.75 square/character 0.00; 44100/K=389/morph 0.25 triangle/character 0.00; 44100/K=389/morph 1.00 pulse/character 0.00; 44100/K=777/morph 0.50 saw/character 0.00. For each, `CHECK(naiveDb > cell.thresholdDb + 5.0f)`. These five are what make the tombstone specific rather than statistical, and the 5 dB cushion is what makes it robust.

Write the inversion contract into the case banner in the Phase-29-tombstone voice, carrying all of:
- WHY THIS CASE EXISTS: D-08 requires the gate to be observed failing against the naive core before `MorphBlep` lands. A gate written against already-passing code proves nothing, and this is the phase's objective iteration metric.
- WHAT PLAN 32-07 MUST DO: invert this case IN PLACE — same slot, same grid, same cells — into `CHECK(failing == 0)` against the real `forge::MorphBlep`, and flip the five named subset assertions to `CHECK(correctedDb <= cell.thresholdDb)`. Deleting the case instead of inverting it silently removes the phase's only recorded RED.
- THE STANDING WARNING: if this case ever goes green while `forge::VcoCore::step` is still naive, the gate has stopped being able to see aliasing at all, and every threshold below it is decoration.
- THE ANTI-SOFTENING CLAUSE: a threshold that turns out to be unreachable is escalated per plan 32-07's documented procedure — it is NEVER quietly loosened, and the corrected column is never edited to match whatever the implementation happened to produce.

Also record, in the same banner, the observed count and the five subset margins as numbers, with the standing instruction from `tests/test_vco_core.cpp:344-347`: if any of those figures ever moves when this case is touched, stop and report it rather than updating the number.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures AFTER step two; the SUMMARY carries the verbatim failing transcript from step one
    - `./build-test/test -tc="vco spectrum: TOMBSTONE*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output shows `failing` at or above `kNaiveFailuresFloor` and shows all five named subset cells exceeding their threshold by more than 5 dB
    - `grep -c 'INVERTS IN PLAN 32-07' tests/test_vco_spectrum.cpp` outputs `1`
    - `grep -c 'kNaiveFailuresFloor' tests/test_vco_spectrum.cpp` is at least 2
    - The doctest summary reports at least 85 test cases (phase baseline before plan 32-01 was exactly 81) with 0 failed
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_spectrum.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The alias-floor gate has been observed failing against the naive core with its transcript recorded, and the failure is now pinned by a tombstone that names plan 32-07 as the plan obliged to invert it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> `forge::VcoCore::step` | The suite builds `forge::VcoInputs` directly; nothing sanitises the values it chooses. |
| measured evidence -> pinned threshold | A threshold copied from prose rather than from measurement makes every later assertion decoration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11 | Repudiation (evidence integrity) | the gate's own noise floor | high | mitigate | Per-cell `REQUIRE` that `impliedLeakageDb(binError)` sits at least 10 dB below that cell's threshold, BEFORE the alias value is read (D-10 / P-7). |
| T-32-14 | Repudiation (evidence integrity) | a gate that has never been observed red | high | mitigate | Step one runs the gate in final form against the naive core and transcribes the failure verbatim; step two pins that failure as a tombstone with a named inversion owner. The five large-margin subset cells make it specific rather than statistical. |
| T-32-15 | Tampering | threshold provenance | high | mitigate | Every `thresholdDb` carries a `provenance` string naming its source and its provisional status. The anti-softening clause forbids editing the corrected column to match whatever the implementation produced; unreachable thresholds escalate per plan 32-07's procedure. |
| T-32-09 | Denial of Service | RNG seeding in `measureCellDb` | high | mitigate | Both branches reuse the four proven non-degenerate seed literals verbatim. A `(0,0)` Xoroshiro seed emits an all-zero stream and makes the normal distribution's rejection loop never terminate — in Rack a hang on patch load. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. No install checkpoint applies. |
</threat_model>

<verification>
- `make test` green with the case count risen to at least 85 and 0 failures.
- The D-08 RED transcript is recorded verbatim in the plan SUMMARY, with the failing cell list and the reported-versus-expected dB values.
- All 90 naive baseline figures are captured in `-s` output and reproduced in the SUMMARY as a table.
- `make guards` and `make strict` green; no `src/` file changed by this plan.
- `src/AnalogLFO.cpp` absent from the diff; `src/dsp/FROZEN.sha256` unbumped.
</verification>

<success_criteria>
- `SPECTRUM_GRID` holds 90 cells across 45 gated, 15 diagnostic and 30 cross-rate regression entries, each with a threshold and a written provenance.
- The naive alias floor is recorded for every cell with a per-cell D-10 leakage self-check.
- The alias-floor gate has been observed RED against the naive core and landed as a tombstone naming plan 32-07 as its inversion owner.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-03-SUMMARY.md` when done. Record: the verbatim step-one RED transcript; the full 90-cell naive baseline as a table; the observed `failing` count and the derived `kNaiveFailuresFloor`; and the five subset cells with their measured margins.
</output>

---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 09
type: execute
wave: 6
depends_on: ["32-08"]
files_modified:
  - tests/test_vco_core.cpp
autonomous: true
nyquist_compliant: true
requirements: [MORPH-01, MORPH-02, AA-05]
must_haves:
  truths:
    - "D-15: the hostile-timing grid stays in Phase 32 on a CORRECTED rationale. The old premise — that Phase 32's oversampled inner loop is the first real source of exotic timing — is FALSIFIED, because AA-05 forbids oversampling in v2.0. The surviving reason is that this phase introduces division by dt, so a zero, subnormal or non-finite sampleTime now reaches arithmetic that did not exist before."
    - "D-15: the grid is extended to plus and minus infinity, subnormal and very-large-finite sampleRate and sampleTime, and the guards use the negated-comparison idiom, never a comparison-ladder helper."
    - "MORPH-01 / MORPH-02 / P-13: audio-rate MORPH sweeping through segment boundaries is the hardest case this phase's band-limiting has to survive, and it is asserted finite and bounded at three sample rates, three notes and three morph modulation rates."
    - "D-13 consequence, stated rather than hidden: a MODULATED morph legitimately exceeds the static-input envelope, because the pending accumulator delivers the second half of a correction computed with the previous sample's weight vector — which is the design, chosen because recomputing at the next sample would use a moved jump magnitude, a moved site position and a moved dt."
  artifacts:
    - tests/test_vco_core.cpp
  key_links:
    - "Scenario four calls core.step(in) DIRECTLY on a caller-built forge::VcoInputs; the bypass IS the coverage, because both drivers overwrite sampleTime and sampleRate unconditionally. Routing it through a driver would silently delete the whole scenario while leaving it green."
    - "The audio-rate MORPH case is the headless counterpart of plan 32-10's operator UAT: the same control surface, driven at the same rates."
---

<objective>
Extend scenario four's hostile-timing grid to the input classes this phase's new divisors made reachable, correct the falsified premise that pointed the work here, and add the audio-rate MORPH sweep that MORPH-01 and MORPH-02 are actually about.

Purpose: Phase 31's D-15 deferred the hostile-timing grid to Phase 32 for a reason that turned out to be false. The item stays, because the real reason is stronger — this is the phase that put a division by `dt` behind `forge::VcoInputs::sampleTime`. And MORPH-02's whole point, per D-16, is that audio-rate morph sweeping through segment boundaries is the hardest case the alias floor has to survive; nothing asserts it yet.

Output: an extended hostile grid with a corrected rationale, and a new invariant over audio-rate MORPH modulation.
</objective>

<!-- planner-discipline-allow: Approx -->
<!-- planner-discipline-allow: oversampled inner loop -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md
@tests/test_vco_core.cpp
@src/dsp/MorphBlep.hpp
@src/dsp/VcoCore.hpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor`, `struct forge::MorphBlep` (`pending`, `inject`, `reset`, `addStep`, `step`), `forge::VcoCore::blep`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- Test-only in `tests/test_vco_core.cpp`: `kMusicalBoundV`, `kHostileBoundV`, `DeliberatelyBrokenSharedStateCore::blep`, and invariant 6 (audio-rate MORPH)
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: the extended `HOSTILE_RATES` / `HOSTILE_TIMES` arrays and invariant 6.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the hostile-timing grid to the classes this phase's divisors made reachable, on the corrected D-15 rationale</name>
  <files>tests/test_vco_core.cpp</files>
  <behavior>
    - every combination of the extended `HOSTILE_RATES` and `HOSTILE_TIMES` arrays with the existing hostile pitch values produces output that is finite, inside the outer magnitude bound, with the phase in the unit interval, a non-negative telemetry frequency and a Nyquist-bounded telemetry frequency
    - a plus-or-minus infinite or subnormal `sampleTime` reaches `forge::MorphBlep::step`'s `dt` guard and returns the drained accumulator rather than dividing
    - the per-sample assertion count does not explode: results are accumulated across the whole run and asserted once per configuration
  </behavior>
  <read_first>
    - tests/test_vco_core.cpp lines 669-827 — scenario four in full: the coverage-gap banner at :674-684, the four-plus-one assertion rationale at :695-728, the two arrays at :735-751, the accumulate-don't-assert idiom at :770-807 and the five named `CHECK`s
    - tests/test_vco_core.cpp lines 742-744 — the falsified premise sentence sitting directly above `HOSTILE_TIMES`
    - .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md — item 6, whose rationale D-15 corrects
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-14 — the measured finding that hostile timing reaches exactly one divisor
    - src/dsp/MorphBlep.hpp — the `dt` guard at the top of `step` and `morphBlepCharFactor`'s guard
  </read_first>
  <action>
Two changes to scenario four, both inside the existing block. The surrounding structure is unchanged: still a DIRECT `core.step(in)` with NO driver in the way, still the accumulate-don't-assert idiom, still the same five named `CHECK`s.

CHANGE ONE — CORRECT THE FALSIFIED PREMISE IN PLACE. The comment directly above `HOSTILE_TIMES` explains that the decoupled entries are "the shape Phase 32's oversampled inner loop will produce naturally". That premise is FALSIFIED: AA-05 forbids oversampling in v2.0 by name, so no such loop exists in this phase and none will be added. Keep the conclusion — the decoupled entries stay, and they are still the right entries — replace the premise, and say in the comment that the old reason was falsified and by what. The corrected premise: `sampleTime` and `sampleRate` are independent POD fields that any caller may set independently, this scenario is the caller that does so, and Phase 32 has now put a DIVISION BY `dt` behind `sampleTime` — in `forge::morphBlepCharFactor` and in the sub-sample position inside `forge::MorphBlep::step` — so these values now reach arithmetic that did not exist when the grid was written. Note the measured scope limit alongside it: the shipped formulation divides by `dt` ONLY; there is no division by an edge width anywhere in `src/dsp/MorphBlep.hpp`, and the optional refinement that would add one is deliberately deferred.

CHANGE TWO — EXTEND THE TWO ARRAYS, in place, keeping every existing entry and its inline reason.

`HOSTILE_RATES` gains: positive infinity, negative infinity, the smallest positive subnormal float, and a very-large-finite value (use `1e30f`). Give each a one-clause reason on its own line in the same style as the four existing entries — the infinities are what a mis-wired host or an uninitialised process-args block delivers; the subnormal is what an arithmetic underflow upstream produces; the very-large-finite value is the class that is neither infinite nor small and therefore passes a naive finiteness check while making the Nyquist ceiling meaningless.

`HOSTILE_TIMES` gains the same four classes, plus a negative subnormal. Note in the comment which of them is the NEW one relative to `sampleRate`: a subnormal or zero `sampleTime` is the entry that reaches the new divisor, and it is the whole reason D-15 kept this item in Phase 32.

Do not widen the hostile pitch array — it is already `{0.f, 10.f}` and pitch is not what this scenario is about. Keep `nHostile` at 20000, the step count both the code reviewer and the verifier reproduced at.

State in the banner the consequence of the extension for the assertion budget: the configuration count rises from 48 to the new product, and the accumulate-don't-assert idiom is what keeps that from adding millions of assertions to a suite already carrying more than 2.6 million. Record the new configuration count as a number.

Confirm that every guard reached on the extended grid is the negated-comparison form — in `forge::VcoCore::step`, in `forge::morphBlepCharFactor` and at the top of `forge::MorphBlep::step` — and never a comparison-ladder helper, which is transparent to a not-a-number. State it in the comment rather than leaving it to be re-derived.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco core: output magnitude*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - `grep -c 'infinity()' tests/test_vco_core.cpp` is at least 2 and `grep -c 'denorm_min()' tests/test_vco_core.cpp` is at least 2
    - `HOSTILE_RATES` has 8 entries and `HOSTILE_TIMES` has 11 entries, each with an inline reason
    - `grep -c '1e30f' tests/test_vco_core.cpp` is at least 2
    - The corrected premise is present and names what was falsified: `grep -c 'AA-05 forbids oversampling' tests/test_vco_core.cpp` is at least 1
    - The `-s` output shows the five named assertions still present and passing at every configuration, with `firstBadStep` captured as `-1` throughout
    - The doctest case count is unchanged from this plan's starting baseline; the assertion count rises by the extended configuration count times five
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_core.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The hostile-timing grid covers infinite, subnormal and very-large-finite timing on both fields, the falsified rationale that pointed the work here is replaced by the real one in place, and the five named assertions hold across the widened configuration set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invariant 6 — audio-rate MORPH sweeping through segment boundaries stays finite and bounded (MORPH-01 / MORPH-02)</name>
  <files>tests/test_vco_core.cpp</files>
  <behavior>
    - at 44.1, 48 and 96 kHz, at notes near 2093, 4186 and 8372 Hz, with morph modulated sinusoidally over its full range at 50, 500 and 2000 Hz and character at 1.0, every one of 20000 samples is finite
    - the observed magnitude stays inside the outer bound at every one of the 27 configurations
    - the observed magnitude at the worst configuration EXCEEDS the static-input musical figure, and that excess is recorded with its mechanism rather than hidden
    - the morph modulation genuinely crosses every segment boundary, asserted before the value assertions rather than assumed
  </behavior>
  <read_first>
    - tests/test_vco_core.cpp lines 1-75 — the file banner's invariant list, which this plan appends invariant 6 to without renumbering anything
    - tests/test_vco_core.cpp lines 501-611 — invariant 2's scenario structure, the `INFO` labelling idiom and the capture-on-pass habit, plus `kMusicalBoundV` and `kHostileBoundV` as pinned by plan 32-08
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-13 — the measured grid and the measured worst figure
    - src/dsp/MorphBlep.hpp — the pending accumulator and D-13's one-consistent-set-of-values argument
    - src/AnalogVCO.cpp as edited by plan 32-02 — the shell mix this headless case is the counterpart of
  </read_first>
  <action>
Append invariant 6 to `tests/test_vco_core.cpp` as a new `TEST_CASE` named `"vco core: audio-rate MORPH sweeping through every segment boundary stays finite and bounded (MORPH-01 / MORPH-02)"`, and add a matching one-line entry to the numbered invariant list in the file banner — appended, never renumbering the five that exist.

Drive, through `forge::VcoBlockDriver`, the cross product of: the three `SAMPLE_RATES`; three pitch values placing the fundamental near 2093, 4186 and 8372 Hz (derive them from `forge::kVcoFreqC4` rather than hardcoding volts, so the case moves with the constant); and three morph modulation rates of 50, 500 and 2000 Hz. Hold character at 1.0 — the value at which every component-spread coefficient and the whole bleed ring are live. Run 20000 samples per configuration. Modulate morph as a full-range sinusoid over the unit interval, which is what a patched audio-rate LFO into the MORPH CV jack at a full attenuverter produces.

Assert NON-VACUITY FIRST, in two parts, because this case is trivially green if the modulation does not actually do anything:
- `REQUIRE` that the morph functor's own output crosses all four segment boundaries at 0.25, 0.50, 0.75 and 1.00 within the block at every configuration. Count the crossings and `CAPTURE` the count. Comment that segment boundaries are the whole point: D-16 pulled MORPH's CV into this phase precisely because audio-rate morph sweeping through them is the hardest case the alias floor has to survive.
- `REQUIRE` that the output block is not constant and that at least 90 percent of its samples are non-zero.

Then assert, accumulating rather than asserting per sample (48-plus configurations at 20000 steps would otherwise add millions of assertions to a suite already past 2.6 million):
- every sample is finite
- the observed maximum magnitude is at or below `kHostileBoundV`
- `CAPTURE` the observed maximum for every configuration, and `CAPTURE` the worst across the whole grid

Then add the assertion that keeps the case honest, and write its reasoning into the banner because it is the one number a reader will trip over: the worst configuration's magnitude EXCEEDS the static-input `kMusicalBoundV`, and that is EXPECTED rather than a defect. `CHECK` that it does, with the observed figure recorded, so the excess is pinned rather than discovered later and mistaken for a regression. The mechanism, stated in full: the pending accumulator deliberately delivers the second half of a correction computed with the PREVIOUS sample's weight vector, site position and phase increment. That is D-13's design and it was chosen over recomputing at the next sample precisely because, under audio-rate MORPH and FM, all three of those have moved by then — so one consistent set of values for both halves is strictly more robust even though it costs a slightly larger transient at a swept boundary. Record the researcher's measured envelope for this grid as the expected order of magnitude and record this run's own figure beside it.

Close the banner with the link to the operator check: this case is the headless counterpart of plan 32-10's in-Rack UAT — same control surface, same three sample rates, same modulation rates — and the two are deliberately not substitutes. This one bounds the numbers; the operator judges whether it sounds like zipper noise at a crossfade seam.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco core: audio-rate MORPH*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The `-s` output shows a segment-boundary crossing count of at least 4 at every one of the 27 configurations
    - The `-s` output shows 27 per-configuration maxima plus one grid-wide worst, all at or below `kHostileBoundV`
    - The `-s` output shows the grid-wide worst exceeding `kMusicalBoundV`, matching the `CHECK` that asserts it
    - The file banner's numbered invariant list contains six entries and the first five are unrenumbered
    - The doctest summary reports at least 93 test cases (phase baseline before plan 32-01 was exactly 81) with 0 failed
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_core.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>Audio-rate MORPH through every segment boundary is asserted finite and bounded across 27 configurations, with the boundary crossings proven to happen and the expected excess over the static-input figure pinned and explained rather than left to be rediscovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-built `forge::VcoInputs` -> `forge::VcoCore::step` | Scenario four bypasses both drivers, so `sampleTime` and `sampleRate` arrive exactly as the caller set them. This is the only route by which hostile timing reaches the core at all. |
| Rack patch cable -> MORPH CV -> the crossfade weight vector | An audio-rate voltage now moves the weight vector every sample, and the pending accumulator carries a correction across that movement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-02 | Denial of Service | division by `dt` newly reachable from `forge::VcoInputs::sampleTime` | high | mitigate | The extended grid drives plus and minus infinity, subnormal and very-large-finite values on BOTH timing fields straight into `core.step(in)` with no driver in the way, and asserts finiteness, the magnitude bound, the phase range and both frequency properties at every configuration. The bypass IS the coverage. |
| T-32-24 | Denial of Service | audio-rate modulation driving an unbounded transient through the pending accumulator | high | mitigate | Invariant 6 bounds 27 configurations at three rates, three notes and three modulation rates, with the boundary crossings proven to occur first. The measured excess over the static-input figure is pinned and explained rather than absorbed. |
| T-32-03 | Tampering (signal integrity) | out-of-range output damaging downstream gear or speakers | high | mitigate | Every configuration in both tasks asserts the single outer bound plan 32-08 pinned, with no exceptions. |
| T-32-23 | Repudiation (evidence integrity) | a falsified rationale inherited by a later phase | medium | mitigate | The premise directly above the hostile-timing arrays is replaced in place with the real reason and says what was falsified, and plan 32-10 records the correction in the phase's deferred register so no later phase inherits the old one. |
| T-32-25 | Repudiation (evidence integrity) | a modulation case that never modulates | high | mitigate | Invariant 6 `REQUIRE`s the counted segment-boundary crossings and the block's non-triviality BEFORE any value assertion, which is the same validity-first habit invariant 4 uses. |
| T-32-12 | Tampering | the shipped Analog LFO's golden bit-stability | high | mitigate | No `src/` file is touched by this plan; `make test` replays the six `.f32` goldens on every run; `src/AnalogLFO.cpp` is absent from the diff. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. |
</threat_model>

<verification>
- `make test` green with the case count risen to at least 93 and 0 failures.
- Scenario four's five named assertions hold across the widened configuration set, with `firstBadStep` at `-1` throughout.
- Invariant 6's boundary-crossing precondition is satisfied at all 27 configurations.
- `make guards` and `make strict` green; no `src/` file changed by this plan.
- `src/AnalogLFO.cpp` absent from the diff; `FROZEN.sha256` unbumped.
</verification>

<success_criteria>
- `HOSTILE_RATES` and `HOSTILE_TIMES` cover infinite, subnormal and very-large-finite values on both fields, each entry carrying its own reason.
- The D-15 rationale is corrected in place where it is used, not only in the register.
- Invariant 6 exists and bounds audio-rate MORPH across 27 configurations with a proven-non-vacuous modulation.
- The expected excess over the static-input figure is asserted and explained.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-09-SUMMARY.md` when done. Record: the new configuration count for scenario four and the assertion-count delta; the 27 invariant-6 maxima with the grid-wide worst; the boundary-crossing counts; and the exact source line where the falsified premise was replaced.
</output>

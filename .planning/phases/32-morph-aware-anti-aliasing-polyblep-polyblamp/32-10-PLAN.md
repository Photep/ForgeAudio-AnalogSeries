---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 10
type: execute
wave: 7
depends_on: ["32-02", "32-05", "32-07", "32-09"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md
autonomous: true
nyquist_compliant: true
requirements: [AA-05, CORE-02, TEST-03, MORPH-01, AA-01, AA-02, AA-03, AA-04]
must_haves:
  truths:
    - "AA-05: the whole phase compiles under -std=c++11 -pedantic-errors and links on the CI MinGW leg; nothing is table-driven, nothing is Rack-coupled, and no minBLEP or oversampling exists anywhere in the diff."
    - "CORE-02: src/dsp/Waveshape.hpp and src/dsp/RackCompat.hpp take ZERO edits and src/dsp/FROZEN.sha256 is unbumped — no manifest entry moved."
    - "The milestone guardrail holds: src/AnalogLFO.cpp is absent from the ENTIRE phase diff, and all six shipped LFO goldens replay bit-exact."
    - "Local green is a PRECONDITION and never sufficient: Phase 29 measured make test, make strict, make guards and check_canary.sh all returning exit 0 on a commit that could not link. The CI toolchain-gate's win-x64 link leg must be observed green on the exact commit BY SHA, never by run name and never by recency."
    - "Every falsified premise this phase corrected is recorded in the deferred register with the correction, so no later phase inherits the old reason: D-15's oversampling premise, P-4's saw soft-reset premise, research/STACK.md:40's over-correction-is-benign argument, research/STACK.md:100-104's wrong polyBLAMP snippet, and D-04's literal site-map wording."
    - "A requirement is marked complete only where this phase asserts behavior for it — the four-consecutive-plans discipline Phase 31 set, which exists so no PANEL-03-style false green is booked."
  artifacts:
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "The gate stands on the OWN CONCLUSION of the win-x64 link-leg STEP, not on the job's conclusion: a step that fail-fasts upstream reports 'skipped', which scans as 'not red' in a job summary."
    - "src/dsp/MorphBlep.hpp is the first new VCO header since Phase 29, so this is the first CI run where the canary carries a genuinely new header into the MinGW link leg."
---

<objective>
Close Phase 32's automated evidence: the full local gate, the CI toolchain-gate observed by SHA, the requirement traceability, and the deferred register carrying every falsified premise this phase corrected.

Purpose: this phase added a new `src/dsp/` header that reaches the CI MinGW link leg through the compile canary, and the in-class-constant class that got v2.0.0 rejected from the VCV Library surfaces only at link time on that toolchain. Local green is the precondition, not the evidence.

Output: a green local gate, an observed CI link leg pinned by commit hash, updated requirement status, and `deferred-items.md`.
</objective>

<!-- planner-discipline-allow: oversampled inner loop -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.

New symbols:
- `forge::morphBlepCharFactor(float w, float dt) -> float`
- `struct forge::MorphBlep` — `float pending`, `float inject`, `void reset()`, `void addStep(float, float)`, `float step(const Waveshape&, double, float, double, float, float)`
- `forge::VcoCore::blep`
- `AnalogVCO::MORPH_ATTEN_PARAM` (ParamId), `AnalogVCO::MORPH_CV_INPUT` (InputId)
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- Test-only in `tests/test_vco_core.cpp`: `kMusicalBoundV`, `kHostileBoundV`, `DeliberatelyBrokenSharedStateCore::blep`, invariant 6
- `res/AnalogVCO.svg` — two additional 10x10 marker rects at (25.48, 75) and (55.96, 75)

THIS PLAN produces: `deferred-items.md` and the requirement/roadmap status updates.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The full local gate, the guardrail assertions, and requirement traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the nine Phase 32 requirement rows and the traceability table
    - .planning/ROADMAP.md § "Phase 32" — the five success criteria as they read NOW, including the SC-2 and SC-4 corrections already committed
    - .planning/STATE.md § Accumulated Context — the Phase 29 record that the ENTIRE local gate returned exit 0 on a commit that could not link, and the four-consecutive-plans discipline Phase 31 set on marking requirements complete
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-01-SUMMARY.md through 32-09-SUMMARY.md — every measured figure this task reports
  </read_first>
  <action>
Run and record the full local gate, then update traceability. Every figure goes into the SUMMARY as a number, not as a verdict.

THE GATE, in order:
- `make test` — record the case count and the assertion count, and compare both against the phase's starting baseline of 81 cases and 2,618,053 assertions. State the delta and account for it: which plan added which cases.
- `make strict` — the C++11 pedantic gate over the `src/*.cpp` glob, now covering `src/dsp/MorphBlep.hpp` through the canary. Record the translation-unit count.
- `make guards` — record each script's own verdict, and specifically quote the lines proving `check_frozen.sh` sees an unchanged 15-entry manifest, `check_includes.sh` [1/7] covers the two new test files by exact path, [2/7] and [3/7] cover `src/dsp/MorphBlep.hpp`, and `check_canary.sh` [5/5] reports the header carried and [5b/5] reports the naming rule satisfied.
- `make -j4` against `../Rack-SDK` — a real link, not a syntax check. Record that `plugin.dylib` was produced.

THE GUARDRAIL ASSERTIONS, measured over the WHOLE phase diff rather than one commit:
- `git diff --name-only <pre-phase-32-commit>..HEAD` must not list `src/AnalogLFO.cpp`, `src/dsp/Waveshape.hpp`, `src/dsp/RackCompat.hpp` or any other line in `src/dsp/FROZEN.sha256`. Record the full file list.
- `src/dsp/FROZEN.sha256` must be byte-identical to its pre-phase state, asserted by reading BYTES rather than by counting diff markers (the Phase 30 lesson).
- The six shipped LFO goldens must replay bit-exact — confirm the golden cases are among the passing cases in `make test` and that none was skipped. On this platform three of them are gated behind an Apple-only guard; account for that gap by number rather than by assertion, as Phases 30 and 31 did.

TRACEABILITY. Mark a requirement complete ONLY where this phase asserts behavior for it. Working from the SUMMARYs:
- MORPH-01, MORPH-02, AA-01, AA-02, AA-03, AA-04, AA-05, CORE-02 and TEST-03 each have named assertions across plans 32-02, 32-05, 32-07, 32-08 and 32-09. For each, name in the SUMMARY the exact test case and file that discharges it. If any requirement has no named assertion, DO NOT mark it — record it as outstanding and say which plan was expected to cover it. That is the Phase 31 discipline: four consecutive plans there declined to tick a requirement their own frontmatter claimed, on the grounds that the plan asserted no behavior for it, and the alternative is a PANEL-03-style false green.
- Update `.planning/REQUIREMENTS.md`: tick the checkbox for each discharged ID and set its traceability-table status to Complete. Use scoped edits; never rewrite the file.
- Update `.planning/ROADMAP.md` § "Phase 32": fill the `**Plans:**` count and the plan list with all eleven plans and their one-line objectives, and tick each. Use scoped edits; a whole-file write destroys every phase entry outside the diff window.

Do not edit ROADMAP SC-2 or SC-4 — both corrections are already committed (462b473 and 7fc7634) and re-applying them would produce a spurious diff.
  </action>
  <verify>
    <automated>make test && make strict && make guards && make -j4</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures and a case count of at least 92 against the pre-phase baseline of exactly 81
    - `make strict` exits 0 and prints `strict C++11 gate: PASS`
    - `make guards` exits 0 and prints `guard suite: PASS`
    - `make -j4` exits 0 and produces `plugin.dylib`
    - `git diff --name-only <pre-phase>..HEAD` lists none of `src/AnalogLFO.cpp`, `src/dsp/Waveshape.hpp`, `src/dsp/RackCompat.hpp`
    - `git diff --name-only <pre-phase>..HEAD -- src/dsp/FROZEN.sha256` is empty and the file's SHA-256 is unchanged from its pre-phase value
    - `.planning/REQUIREMENTS.md` shows every discharged Phase 32 ID ticked and Complete, and every ID with no named assertion left Pending with a written reason
    - `.planning/ROADMAP.md` § "Phase 32" lists eleven plans with one-line objectives and a matching `**Plans:**` count
    - Both `.planning` edits are scoped replacements, verified by `git diff --stat` showing changes confined to the Phase 32 rows
  </acceptance_criteria>
  <done>The full local gate is green and recorded as numbers, the milestone guardrail is asserted over the whole phase diff rather than one commit, and requirement status reflects what is actually asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the CI toolchain-gate green on the exact commit, BY SHA</name>
  <files>.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md</files>
  <read_first>
    - .github/workflows/test.yml — the workflow name, the `toolchain-gate` job, and the win-x64 leg reproduction step's exact name
    - .planning/STATE.md § Accumulated Context — the three Phase 30/31 records that govern this task: the CI MinGW link gate is PROVEN to bite; the job's conclusion is never sufficient because a step that fail-fasts upstream reports `skipped`; and a run selector matching the RUN name is unsatisfiable because `toolchain-gate` is a JOB inside the single workflow named `test`, so the run must be located by head-commit equality
    - .planning/phases/30-vcocore-skeleton-module-registration and 31-pitch-tuning-exponential-fm SUMMARYs for the exact `gh` invocation shape used there
  </read_first>
  <action>
Push the phase branch and observe the CI toolchain-gate on the EXACT commit this phase ends at.

LOCATE THE RUN BY HEAD-COMMIT EQUALITY. Capture the local HEAD SHA first. Then list runs with `gh` and select the one whose `headSha` EQUALS that SHA. Never select by run name — `toolchain-gate` is a JOB inside the single workflow named `test`, and a name-based selector returns nothing, which makes the whole check silently unsatisfiable regardless of CI health (that exact defect was found and corrected in Phase 30). Never select by recency either — Phase 31 recorded that the pre-phase commit had its own green run sitting one line above the target in the run list.

READ THE STEP'S OWN CONCLUSION, not the job's. Extract, across all jobs in that run, the step whose name is the win-x64 leg reproduction, and read that STEP's `conclusion`. A job that fail-fasts upstream reports its later steps as `skipped`, which scans as "not red" in a job summary and is exactly the false green this rule exists to stop. Record BOTH the job conclusion and the step conclusion, and treat only the step conclusion as the gate.

Record in the SUMMARY: the SHA, the run id, the job conclusion, the step conclusion, the step's own printed verdict line, and the three-OS matrix case and assertion counts per leg.

ACCOUNT FOR THE MATRIX GAP BY MEASURING BOTH SIDES, as Phase 31 did rather than asserting it is expected. Record the case and assertion counts on macOS, Ubuntu and Windows. The standing gap is exactly three cases and 24,582 assertions, which are the three Apple-gated drift-ON golden cases; the phase's own new cases must appear on EVERY leg. Compute the per-leg BEFORE and AFTER delta and confirm it is identical on all three, which is what proves every new case ran everywhere. An absent platform guard in a new file is only an argument; a per-leg delta is evidence.

WHY THIS RUN MATTERS MORE THAN THE LAST TWO: `src/dsp/MorphBlep.hpp` is the first genuinely new VCO header since Phase 29, and it reaches the MinGW link leg only through the compile canary's include. The failure class that gate exists for — an in-class constant odr-used at runtime producing an undefined reference — is invisible to Apple clang at every optimization level and invisible to every syntax-only gate on every platform. State that in the SUMMARY.

If the step conclusion is anything other than success, STOP AND REPORT with the failing step's log excerpt. Do not proceed to Task 3 and do not treat a green job conclusion as a substitute.
  </action>
  <verify>
    <automated>gh run list --limit 20 --json databaseId,headSha,conclusion,name</automated>
  </verify>
  <acceptance_criteria>
    - The selected run's `headSha` string-equals the local `git rev-parse HEAD` output, verified before any conclusion is read
    - The `toolchain-gate` job's conclusion is `success`
    - The win-x64 leg reproduction STEP's own conclusion is `success`, and its printed verdict line is quoted verbatim in the SUMMARY
    - The three-OS matrix case counts are recorded per leg, with the per-leg BEFORE and AFTER delta identical on all three legs
    - The macOS-versus-others gap is accounted for as exactly three cases and 24,582 assertions, unchanged from before the phase
    - The SUMMARY records both the job conclusion and the step conclusion, and states that only the step conclusion is the gate
  </acceptance_criteria>
  <done>The CI MinGW compile-and-link leg is observed green on the exact commit this phase ends at, located by hash, with the step's own conclusion read rather than the job's, and every new test case proven to have run on all three legs.</done>
</task>

<task type="auto">
  <name>Task 3: The deferred register — every falsified premise this phase corrected, and every refinement it deliberately did not take</name>
  <files>.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md</files>
  <read_first>
    - .planning/phases/31-pitch-tuning-exponential-fm/deferred-items.md — the exact format this file must match: found during, observation, why it is worth a note, why it is not fixed here, resolve at
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md § Deferred Ideas — the eight inherited items and their owners
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-1, P-4, P-5 and § Open Questions items 4 and 5
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-01-SUMMARY.md through 32-09-SUMMARY.md — every finding recorded during execution
  </read_first>
  <action>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md` in the Phase 31 format, with two sections.

SECTION ONE — FALSIFIED PREMISES CORRECTED IN THIS PHASE. One entry each, and every entry must name the source file and line where the correction now lives, so the record and the code cannot drift apart:

1. D-15's own premise. Phase 31's D-15 pointed the hostile-timing grid at Phase 32 because "Phase 32's oversampled inner loop is the first real source of exotic timing". AA-05 forbids oversampling in v2.0, so no such loop exists. The conclusion survives on the real reason: this phase introduced division by `dt`. Corrections landed in `tests/test_vco_core.cpp` above the hostile-timing arrays and in `src/dsp/VcoCore.hpp` in the increment-bound paragraph. Note that the premise appeared in TWO places, not one — that is why two corrections are named.

2. D-03's saw corollary (P-4). The claim that the saw's soft reset shrinks the wrap's effective step with rising character is false: the jump is `+2.000000` at every character, measured to six decimals and asserted permanently at five character values in `tests/test_morph_blep.cpp`. The CONCLUSION survives — do not correct the reset separately, its slope-break magnitude is about three and a half orders of magnitude under the value step — but the premise did not. Correction landed in `src/dsp/MorphBlep.hpp` beside site entry 1.

3. `research/STACK.md:40`'s over-correction argument, restated in this phase's own CONTEXT under specific ideas. "Erring toward over-correction is the safe direction" is FALSIFIED BY MEASUREMENT: at character 1 / C6 / the square centre the naive floor is -60.1 dB and full authority measures -29.9 dB, a 30 dB regression, because the injected residual is step-shaped energy added to a signal with no step. Correction landed in `src/dsp/MorphBlep.hpp`'s banner beside the character factor. Record the consequence: the compact-support requirement is not a preference.

4. `research/STACK.md:100-104`'s polyBLAMP snippet — wrong polynomial order and wrong placement of `dt`. Correction landed in `src/dsp/MorphBlep.hpp`'s banner with the derivation that replaces it.

5. D-04's site map as literally worded. "The square duty edge", singular, is one site in the prose and two in the frozen code: a hard step at `0.5f` and a soft edge at the duty. Merging them produces single-sample full-amplitude spikes measuring 0.0 dB spectrally. Correction landed as two separate site entries in `src/dsp/MorphBlep.hpp` and as an assertion in `tests/test_morph_blep.cpp`.

6. `tests/test_vco_core.cpp`'s invariant-1 grid attribution, if plan 32-08 Task 3's measurement resolved it that way. Record the two re-measured tracking errors and where high-note behaviour is now asserted instead.

SECTION TWO — DEFERRED, WITH OWNERS. Carry forward every inherited item from CONTEXT's deferred list with its existing owner unchanged, and add this phase's own:

- The narrow-pulse reach refinement (a rational approximation in place of the exact hyperbolic tangent). MEASURED at plus 1.3 dB at the single worst grid point and about plus 0.1 dB mean. NOT taken, deliberately: it would add the only division by an edge width in the header. Resolve at: the first plan that misses a pulse threshold — it is the documented cheap first step of the escalation, and `src/dsp/MorphBlep.hpp`'s banner points at it.
- Four-point (quintic) polyBLEP and polyBLAMP. Would roughly double the decibel attenuation, moving the saw at C8 from about -25.8 to about -36 dB — still not -60. AA-05 forbids minBLEP and oversampling by name but does not speak to kernel order. Resolve at: an OPERATOR DECISION with an impact assessment, never a silent implementation choice. The broad escalation path remains v2.1 oversampling, explicitly not minBLEP.
- Probed rather than analytic jump magnitudes. The narrow escalation, only if a D-09 threshold proves unreachable AND research attributes it to magnitude error rather than kernel order.
- The shipped LFO's shared latent undefined behavior — carried forward pointed at NO PHASE, unfixed by decision, a guardrail event. The direct consequence still binds in this phase: a permanent repository-wide undefined-behavior sanitizer gate cannot be adopted, so any sanitizer use stays a scoped one-shot probe.
- Hard sync to Phase 33: the seam is `forge::MorphBlep::addStep`, and Phase 33 must never snap the reset to exactly zero.
- Phase 34's binding re-read: once drift writes the component-spread fields per sample, every discontinuity position moves per sample, and D-04's recompute-never-cache rule is what keeps `MorphBlep` correct then. Name the four fields it reads.
- CHARACTER CV and attenuverter to Phase 34; per-instance seed entropy and patch persistence to Phase 34 or 35; the COARSE octave snap and the FM depth knob's affordance to Phase 35; the check-docs CI wiring to Phase 36.

Also carry forward, unresolved, the operator's optional housekeeping item from STATE about the stale pre-rename plugin directory, since it still makes any in-Rack audition's subject inferred rather than pinned.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md && grep -c '^## ' .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md` exists with at least 6 falsified-premise entries and at least 7 deferred entries
    - Every falsified-premise entry names the source file where its correction now lives, and each named file contains that correction (spot-check at least three by grep)
    - Every deferred entry names a resolve-at owner, or states explicitly that it is pointed at no phase and why
    - `grep -c 'Resolve at' .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md` is at least 7
    - The file follows the Phase 31 format: found during, observation, why it is worth a note, why it is not fixed here, resolve at
    - `make test && make strict && make guards` all exit 0 (unchanged by a documentation-only task, and re-run to prove it)
  </acceptance_criteria>
  <done>Every premise this phase falsified is recorded with the source location of its correction, and every refinement it deliberately declined is recorded with an owner — so no later phase inherits a false reason or rediscovers a decided question cold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local toolchain -> the VCV Library toolchain | Apple clang materializes the in-class-constant construct as a per-translation-unit local symbol and links cleanly at every optimization level; MinGW GCC at C++11 does not. Only the CI link leg sees the difference. |
| a green gate -> the claim it stands for | Phase 29 measured the entire local gate returning exit 0 on a commit that could not link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Denial of Service | in-class constant odr-used at runtime -> MinGW undefined reference | high | mitigate | The CI toolchain-gate's win-x64 link leg is observed green on the exact commit BY SHA, with the STEP's own conclusion read rather than the job's. This is the first run where `src/dsp/MorphBlep.hpp` reaches that leg. |
| T-32-12 | Tampering | the shipped Analog LFO's golden bit-stability | high | mitigate | The guardrail is asserted over the WHOLE phase diff: `src/AnalogLFO.cpp`, `src/dsp/Waveshape.hpp` and `src/dsp/RackCompat.hpp` absent, `FROZEN.sha256` byte-identical asserted by reading bytes rather than counting diff markers, and all six goldens replaying inside `make test`. |
| T-32-26 | Repudiation (evidence integrity) | a CI run selected by name or by recency | high | mitigate | The run is located by head-commit equality and the equality is verified before any conclusion is read. A name-based selector is unsatisfiable here and a recency-based one selected the wrong run in Phase 31's own history. |
| T-32-27 | Repudiation (evidence integrity) | a requirement marked complete without an assertion behind it | high | mitigate | A requirement is ticked only where a named test case in a named file discharges it; anything without one stays Pending with a written reason. This is the discipline four consecutive Phase 31 plans applied to their own frontmatter. |
| T-32-23 | Repudiation (evidence integrity) | a falsified premise inherited by a later phase | medium | mitigate | Six falsified premises are recorded in the deferred register, each naming the source file where its correction lives, and at least three are spot-checked by grep so the record and the code cannot drift apart. |
| T-32-28 | Information Disclosure | none applicable | low | accept | This phase performs no file, network, logging or persistence operation. The plugin remains offline and stores nothing; no patch-state serialization was added. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem across the entire phase; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)` in every column, and `plugin.json` gains no dependency. No install checkpoint applies because there is no install step. |
</threat_model>

<verification>
- `make test`, `make strict`, `make guards` and `make -j4` all green, with case and assertion counts recorded against the pre-phase baseline of 81 and 2,618,053.
- The whole-phase diff contains no frozen file and no `src/AnalogLFO.cpp`; `FROZEN.sha256` is byte-identical.
- The CI toolchain-gate job AND its win-x64 link-leg step are both observed `success` on a run whose `headSha` equals the local HEAD.
- The three-OS matrix delta is identical on all three legs.
- Requirement status reflects named assertions only.
</verification>

<success_criteria>
- Every Phase 32 requirement with a named assertion is marked Complete in `.planning/REQUIREMENTS.md`; any without one stays Pending with a written reason.
- `.planning/ROADMAP.md` § "Phase 32" lists all eleven plans; SC-2 and SC-4 are untouched.
- `deferred-items.md` records six falsified premises with their correction locations and at least seven deferred items with owners.
- The CI link leg is observed green by hash, with the step conclusion read rather than the job conclusion.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-10-SUMMARY.md` when done. Record: the four local gate results with counts and deltas; the whole-phase diff file list; the `FROZEN.sha256` hash before and after; the CI SHA, run id, job conclusion, step conclusion and verbatim verdict line; the three-OS matrix counts per leg with the per-leg delta; and the requirement-by-requirement discharge table naming the test case and file for each.
</output>

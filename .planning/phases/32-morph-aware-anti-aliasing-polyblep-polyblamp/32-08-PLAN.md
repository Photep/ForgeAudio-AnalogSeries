---
phase: 32-morph-aware-anti-aliasing-polyblep-polyblamp
plan: 08
type: execute
wave: 5
depends_on: ["32-06"]
files_modified:
  - tests/test_vco_core.cpp
autonomous: true
nyquist_compliant: true
requirements: [AA-01, AA-03]
must_haves:
  truths:
    - "The tests/test_vco_core.cpp oracle is brought back in step with the real core: its ONE deliberate divergence stays the shared static phase accumulator, and the band-limiting correction plus the hardened morph and character conditioning are mirrored, exactly as plans 30-08 and 31-07 mirrored their own changes."
    - "The output-magnitude bound is RE-DERIVED FROM MEASUREMENT, never assumed: corrections are additive and bipolar, so the pre-Phase-32 reasoning about morphedWave alone no longer covers the output."
    - "Operator decision 2026-08-01: two explicit tiers, NESTED rather than partitioned. The loose OUTER bound rises to one number, approximately 10.0 V, with the P-10 measured table as provenance and with NO per-scenario exceptions anywhere in the suite; the tight assertion is layered on top of it wherever the measurement entitles a scenario to it, which is the static-input musical range."
    - "P-10: at every musically reachable phase increment the corrected envelope stays INSIDE the existing 1.1047 figure - the corrections consistently REDUCE the excursion at edges. The bound is exceeded only at the Nyquist ceiling with the narrow pulse at low character, which is the deliberate D-07 overlapping-edge case at literally Nyquist."
    - "The hostile tier is EXERCISED, not decorative: a scenario reproducing the P-10 worst point asserts the observed maximum actually exceeds the musical bound."
  artifacts:
    - tests/test_vco_core.cpp
  key_links:
    - "DeliberatelyBrokenSharedStateCore's banner rule binds: keep it in step with the real core, or it differs in more than the one field it promises - and worse, goes on PASSING, because a drifting stand-in is invisible precisely when the drift is inert."
    - "The magnitude bound is the only assertion in the suite that can see the two defects the spectral metric measures at 0.0 dB (the square's merged position and a double-sourced side decision)."
---

<objective>
Bring `tests/test_vco_core.cpp` back in step with the band-limited core: mirror the correction into the oracle, and re-derive the output-magnitude bound from measurement into the operator's two explicit tiers.

Purpose: the magnitude envelope is the only place two of this phase's three most likely defects are visible at all — a merged square site and a double-sourced side decision both measure 0.0 dB spectrally while swinging the output to plus-or-minus 9.78 V. This plan is what makes that envelope a real gate rather than a round number.

Output: an updated oracle with its control figures re-observed, a two-tier bound with measured provenance, and a new scenario that exercises the hostile tier instead of leaving it decorative.
</objective>

<!-- planner-discipline-allow: Approx -->

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-CONTEXT.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md
@.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md
@tests/test_vco_core.cpp
@src/dsp/VcoCore.hpp
@src/dsp/MorphBlep.hpp
</context>

<artifacts_this_phase_produces>
New files: `src/dsp/MorphBlep.hpp`, `tests/test_vco_spectrum.cpp`, `tests/test_morph_blep.cpp`, `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/deferred-items.md`.

Modified files: `src/dsp/VcoCore.hpp`, `src/AnalogVCO.cpp`, `res/AnalogVCO.svg`, `src/vco_compile_canary.cpp`, `tests/check_includes.sh`, `tests/test_vco_core.cpp`.

New symbols:
- `forge::morphBlepCharFactor`, `struct forge::MorphBlep` (`pending`, `inject`, `reset`, `addStep`, `step`), `forge::VcoCore::blep`
- `AnalogVCO::MORPH_ATTEN_PARAM`, `AnalogVCO::MORPH_CV_INPUT`
- Test-only in `tests/test_vco_spectrum.cpp`: `NaiveVcoCoreMirror`, `fftRadix2`, `aliasPeakDb`, `binCentredPitchCV`, `impliedLeakageDb`, `SpectrumCell`, `SPECTRUM_GRID`, `measureCellDb`
- Test-only in `tests/test_morph_blep.cpp`: `probeJump`, `probeSlopeBreak`, `driveOneSite`
- Test-only in `tests/test_vco_core.cpp`: `kMusicalBoundV`, `kHostileBoundV`, and `DeliberatelyBrokenSharedStateCore::blep`
- `res/AnalogVCO.svg` — two additional marker rects

THIS PLAN produces: `kMusicalBoundV`, `kHostileBoundV`, `DeliberatelyBrokenSharedStateCore::blep`, and the P-10 worst-case scenario.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mirror the band-limited path into the :416 oracle, and re-observe its control figures</name>
  <files>tests/test_vco_core.cpp</files>
  <read_first>
    - tests/test_vco_core.cpp lines 259-418 — `DeliberatelyBrokenSharedStateCore` in full: the containment argument, the ONE-FIELD isolation promise, the mirror-maintenance rule at :303-314, the plan-31-07 update record at :316-334, the re-observed figures at :336-347 and the "STOP AND REPORT IT RATHER THAN UPDATING THE NUMBER" clause
    - tests/test_vco_core.cpp lines 1077-1160 — invariant 5, the positive control that drives the broken type through the same helper
    - src/dsp/VcoCore.hpp lines 236-493 as edited by plan 32-06 — the new member, the hardened conditioning pair and the call site
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-PATTERNS.md § "tests/test_vco_core.cpp (MODIFIED — test)" part (a)
  </read_first>
  <action>
Bring `DeliberatelyBrokenSharedStateCore` back in step with `forge::VcoCore`, in obedience to the rule its own banner states: its ONE deliberate divergence is the function-local static phase accumulator, and if any other difference accumulates it stops being the specific control it claims to be — and, worse, goes on PASSING, because a stand-in drifting from what it mirrors is invisible precisely when the drift is inert.

Three mirrored changes:
- add a per-instance `forge::MorphBlep blep;` member beside the per-instance `forge::Waveshape wave;`
- replace the final line's single frozen call with the same two-step form the real core now uses: a named `naive` local from `wave.morphedWave(p, morph, character, 0.f)`, then `naive + blep.step(wave, sharedPhase, p, deltaPhase, morph, character)`, scaled by 5
- replace the comparison-ladder conditioning of `morph` and `character` with the same negated-comparison pair the real core now uses

Note in the mirrored comment WHY the `blep` member is per-instance even in a deliberately-broken control: the defect this type demonstrates is a SHARED PHASE ACCUMULATOR, and giving it a second shared thing would blur what invariant 5 proves. Keep the `>>> THE DELIBERATE DEFECT, AND THE ONLY ONE. <<<` marker on the static, and extend the banner's update record with a Phase 32 paragraph in the same voice as the existing plan-30-08 and plan-31-07 paragraphs.

Then RE-OBSERVE the control's captured figures — the per-instance mismatch counts and their total at each of the three sample rates — by running invariant 5 with `-s` and reading them off. Three outcomes, and the banner's own instruction governs which applies:
- UNCHANGED: record that they are unchanged, exactly as plans 30-08 and 31-07 did, and say why the addition was inert at this control's own inputs.
- MOVED, WITH A KNOWN MECHANISM: this is the outcome to expect, because the correction is a function of the SHARED accumulator, so the interleaved runs now diverge from the solo runs through a second route as well. Record the OLD figures and the NEW figures side by side, name the mechanism in the banner, and state explicitly that the numbers were re-measured rather than recomputed. Do NOT delete the old figures — they are the record of what the control measured before band-limiting existed.
- `totalMismatch` at or below zero at any rate: STOP AND REPORT. A green invariant 5 means the helper has stopped being able to see shared state at all, which makes invariant 4 meaningless regardless of its own verdict. That is a finding about the harness, not a bookkeeping update.

Also re-run invariant 4 — the real core's interleave independence — and confirm it still measures 0 of 1024 mismatches on both instances at all three rates, and that the two solo blocks are still distinguishable. The `blep` member is per-instance, so this must hold; capturing it is what proves it rather than assuming it.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco core: independence positive control*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed, and the `-s` output shows `totalMismatch` above 0 at all three sample rates
    - `./build-test/test -tc="vco core: two-instance independence*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, and the `-s` output shows `mismatchA := 0` and `mismatchB := 0` at all three rates
    - `grep -c 'forge::MorphBlep blep;' tests/test_vco_core.cpp` outputs `1`
    - `grep -c 'blep.step(wave, sharedPhase, p, deltaPhase, morph, character)' tests/test_vco_core.cpp` outputs `1`
    - `grep -cE 'if \(!\(morph > 0\.f\)\)' tests/test_vco_core.cpp` outputs `1` and `grep -cE 'if \(!\(character > 0\.f\)\)' tests/test_vco_core.cpp` outputs `1`
    - `grep -rc 'DeliberatelyBrokenSharedStateCore' src/` finds no matches — containment holds
    - The doctest case count is unchanged from this plan's starting baseline
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_core.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The oracle mirrors the band-limited core in everything but its one deliberate defect, its control figures are re-observed and recorded against the old ones, and both independence invariants are confirmed with the new per-instance member in place.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the output-magnitude bound from measurement into two explicit tiers, and exercise the hostile tier</name>
  <files>tests/test_vco_core.cpp</files>
  <read_first>
    - tests/test_vco_core.cpp lines 501-667 — invariant 2 in full: the bound provenance paragraph at :504-515, `kLooseBoundV` at :525, scenario one at :528-555, scenario two with its load-bearing `> 5.1f` assertion at :557-611, and scenario three at :613-666
    - tests/test_vco_core.cpp lines 669-827 — scenario four's banner and its five named assertions
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Common Pitfalls P-10 in full, including the eight-row measured table and the recommendation
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-VALIDATION.md § "Per-Task Verification Map", the "Output bound" row
    - src/dsp/VcoCore.hpp lines 112-146 and 205-216 — `kVcoNyquistGuardFrac` at 0.495 and `kVcoMaxDeltaPhase` at 0.5, the two constants that decide which phase increments are reachable
  </read_first>
  <action>
This task MEASURES FIRST and PINS SECOND. Do not write a number before reading one.

STEP ONE — MEASURE. Temporarily raise `kLooseBoundV` to a value that cannot fire (for example 100.0f), run invariant 2 with `-s`, and read off the observed `maxAbs` for every scenario at every sample rate: scenario one (the sweep), scenario two (the fixed worst case at morph 0 / character 1), scenario three (hostile V/OCT at pitchCV +10 and +14) and scenario four (the hostile-timing grid). Additionally run a one-shot probe over morph in {0.90, 0.95, 1.00} crossed with character in {0.00, 0.05, 0.10, 0.20} at pitchCV +10 — the P-10 worst region, where the guarded frequency pins the phase increment at 0.495 and the 5-percent pulse's two edges fall inside one sample — and record the maximum. Record every figure; they are this task's whole provenance.

STEP TWO — PIN TWO TIERS, replacing the single `kLooseBoundV`. The two tiers are NESTED, not partitioned, and that structure is what honours the operator's instruction literally.

`kHostileBoundV` is the PHASE-WIDE OUTER BOUND and applies to EVERY scenario in the suite with NO exceptions — including the audio-rate MORPH sweep plan 32-09 adds. Pin it at 10.0f.

`kMusicalBoundV` is an ADDITIONAL, TIGHTER assertion layered on top of the outer bound, and it applies only to the scenarios whose step-one measurement supports it: coupled timing, musical pitch range, inputs held CONSTANT across the block. Pin it at 5.55f unless the measurement says otherwise, and decide each scenario's membership FROM step one's figures rather than by assumption — record the decision and the number behind it for every scenario. Comment the structure explicitly so no reader mistakes it for a carve-out: there is exactly ONE outer bound and it has no exceptions; the second number is a stricter claim asserted where it is demonstrably true, and asserting it nowhere would throw away the tightest thing this suite knows.

`kMusicalBoundV`'s derivation paragraph replaces the one at :504-515 and must be rewritten in the same voice — analytic ceiling, then numerical confirmation, then the "a REAL bound, not a round number" close — carrying:
- the ANALYTIC part, which SURVIVES unchanged: for character at or above 0.001 the sine path is a cubic whose derivative is strictly positive, so it is monotone on the unit interval with range roughly -1.05 to +1.11; triangle, saw, square and pulse are each bounded by 1; the morph crossfade is a linear interpolation and cannot exceed the larger of its two shapes; and the bleed step is a convex combination, which cannot raise a maximum.
- WHY IT NO LONGER COVERS THE OUTPUT ON ITS OWN, which is the part that changed: corrections are ADDITIVE and BIPOLAR, so a bound derived from `morphedWave` alone says nothing about `naive + correction`. The bound must be MEASURED, not assumed.
- the MEASUREMENT, which is better than expected and worth stating plainly: at every musically reachable phase increment the corrected envelope stays INSIDE the pre-Phase-32 figure of 1.1047 — 5.523 V — because the corrections consistently REDUCE the excursion at edges. Record this task's own measured maxima beside the P-10 table's, per rate and per scenario.

`kHostileBoundV`'s provenance paragraph must carry:
- the OPERATOR DECISION of 2026-08-01, by name: ONE outer number with NO per-scenario exceptions anywhere in the suite, on D-09's own reasoning that an exception invites a second exception. 10.0 V is comfortably inside Rack's plus-or-minus 12 V norm.
- the MEASURED table from P-10, reproduced as rows: 5.523 V at a phase increment of 0.0005, 5.506 V at 0.0200, 5.523 V at 0.0949, 0.1897 and 0.2500, 5.829 V at 0.4000, 9.198 V at 0.4950 (the guarded Nyquist ceiling) and 6.150 V at 0.5000 (the increment bound).
- WHY THE HEADROOM EXISTS AT ALL, in one sentence: the tighter figure is exceeded only where the guarded frequency is pinned at the ceiling and the morph sits at the 5-percent pulse with character near zero — the deliberate D-07 overlapping-edge case at literally Nyquist, where the naive path is already meaningless — and where an input is MODULATED across the block, because the pending accumulator deliberately delivers the second half of a correction computed with the previous sample's weight vector (D-13).
- WHY THIS IS NESTING AND NOT AN EXCEPTION: every scenario asserts the outer bound; the scenarios that additionally assert the tighter one say so in their own `INFO` label, along with the measured figure that entitles them to. A reader never has to infer which assertions apply.

STEP THREE — KEEP AND RE-DERIVE THE NON-VACUITY ASSERTIONS.
- Scenario two keeps `CHECK(maxAbs > 5.1f)`. Confirm from step one that it still exceeds 5.1 V and record the figure; if it does not, STOP AND REPORT, because that assertion is what makes the musical bound exercised rather than merely satisfied.
- Add SCENARIO FIVE, the load-bearing scenario for the hostile tier, driven through `forge::VcoBlockDriver` at all three rates over the morph-by-character grid from step one at pitchCV +10, with an `INFO` label naming it the P-10 Nyquist-ceiling overlapping-edge worst case and naming its tier. `CAPTURE` the observed maximum and assert BOTH: it is at or below `kHostileBoundV`, AND it is strictly greater than a pinned exercise floor set from step one's observed maximum minus a 1.5 V cushion. Comment that the second assertion is what stops `kHostileBoundV` from being decoration: without it, a tier no scenario ever approaches proves nothing, which is exactly the criticism the existing banner levels at a sweep-only bound test. If step one's observed maximum does NOT exceed `kMusicalBoundV`, do not invent a scenario to make it — record the finding, state that the hostile tier is currently unexercised and why, and surface it in the SUMMARY for the phase gate.
- Scenarios one, three and four keep every existing assertion; only the constant they compare against changes, from `kLooseBoundV` to `kHostileBoundV`, plus the additional tighter assertion wherever step one's figures entitle the scenario to it. Do not touch scenario four's other four named assertions — `allFinite`, `phaseInRange`, `freqNonNegative` and `freqNyquistBounded` — and do not merge any of them; the banner at :695-728 explains why none is redundant with its neighbours.

Add one sentence to invariant 2's banner recording what this bound is now FOR, because it acquired a second job this phase: it is the ONLY assertion in the suite that can see the two defects the spectral metric measures at 0.0 dB — placing the square's hard step at its soft edge's position, and deriving the sub-sample side decision from the double accumulator. Both swing the envelope to about plus-or-minus 9.78 V while showing no spectral difference at all.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco core: output magnitude*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - `grep -c 'kMusicalBoundV' tests/test_vco_core.cpp` is at least 4 and `grep -c 'kHostileBoundV' tests/test_vco_core.cpp` is at least 5
    - No live use of the old single bound remains: `grep -v '^[[:space:]]*//' tests/test_vco_core.cpp | grep -c 'kLooseBoundV'` outputs `0` — the single bound is replaced by the two tiers, not supplemented (a historical mention inside a comment is permitted and expected)
    - `grep -cE 'CHECK\(maxAbs > 5\.1f\)' tests/test_vco_core.cpp` outputs `1` — scenario two's load-bearing assertion is intact
    - The `-s` output shows scenario five's captured maximum, and it satisfies both the `kHostileBoundV` ceiling and the pinned exercise floor
    - The `-s` output shows scenario four still asserting `allFinite`, `phaseInRange`, `freqNonNegative` and `freqNyquistBounded`
    - The doctest case count is unchanged from this plan's starting baseline — scenario five is a block inside invariant 2, not a new case
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_core.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The output bound is two measured tiers with written provenance, the musical tier is tighter than the number it replaced, and the hostile tier is exercised by a scenario that reproduces the P-10 worst point rather than left as headroom nobody reaches.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the narrow-pulse sampling limit and correct invariant 1's forward-reference in place</name>
  <files>tests/test_vco_core.cpp</files>
  <read_first>
    - tests/test_vco_core.cpp lines 422-499 — invariant 1 in full, and specifically the grid-provenance paragraph at :436-442 stating that the grid stops at pitchCV +2 because of measured sampling loss (-24.53 percent at +3.5 and -46.89 percent at +4.0 at 44.1 kHz) and calling that "Phase 32's" to own
    - tests/test_vco_core.cpp lines 114-149 — `estimateFreqRising` and its banner, including the measured two-sign-changes-per-cycle argument and its named failure mode
    - .planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-RESEARCH.md § Validation Architecture — the note that the spectral suite is where high-note behaviour is now asserted
  </read_first>
  <action>
Invariant 1's grid-provenance paragraph makes a forward reference that Phase 32 has now arrived at: it records measured errors of -24.53 percent at pitchCV +3.5 and -46.89 percent at +4.0, attributes them to sampling loss in a deliberately unband-limited oscillator, and says "Phase 32 owns it". Resolve it BY MEASUREMENT rather than by assumption, and do not leave a stale forward reference in the file plan 30-08 exists to keep clean.

STEP ONE — MEASURE. Run a one-shot probe reproducing the two recorded points exactly: pitchCV +3.5 and +4.0 at morph 1.00, character 0.0, at 44.1 kHz, over the same 250 ms window, through `forge::VcoBlockDriver` against the now band-limited core, and read the relative error from `estimateFreqRising`. Record both figures beside the pre-Phase-32 ones.

STEP TWO — DECIDE FROM THE MEASUREMENT, and write the outcome into the paragraph in place.
- If both points now track within 1 percent, EXTEND `PITCHES` to include +3.5 and +4.0 and rewrite the paragraph to say band-limiting restored the pulse's body at those notes, with both before-and-after figures recorded.
- If they do not — which is the expected outcome, because the failure is in the MEASUREMENT and not in the DSP — leave the grid where it is and CORRECT THE ATTRIBUTION in place, in the house voice: keep the conclusion (the grid stops at +2), replace the premise, and say the old reason was falsified. The corrected premise: `estimateFreqRising` counts RISING ZERO CROSSINGS, and at morph 1.00 the 5-percent pulse's positive region falls under about two samples above pitchCV +2, so the sampler steps over it. Band-limiting reduces the ALIAS ENERGY that narrow region radiates; it does not and cannot restore a zero-crossing counter's ability to resolve a sub-two-sample feature, because that is a property of the estimator's sampling, not of the waveform's spectrum. So this is NOT Phase 32's to fix, and the sentence claiming it was is corrected here. Name where the high-note behaviour IS now asserted: `tests/test_vco_spectrum.cpp` gates the alias floor at C7, C8 and C9 — 2099, 4188 and 8367 Hz — which is above the pitch range this grid stops at, and it does so spectrally rather than by counting crossings.

In either outcome, record both measured figures in the paragraph and add the standing instruction the file's other historical figures carry: these are OBSERVATIONS of a reproduction run, not a current expectation to recompute when a constant moves.

Do not touch `estimateFreqRising` itself, do not loosen the 1 percent tolerance, and do not soften the D-16 "NOT the TEST-02 tracking gate" label, which appears twice by design.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with 0 failures
    - `./build-test/test -tc="vco core: naive pitch tracks*" -s 2>&1 | tail -4` reports exactly 1 test case, 1 passed, 0 failed
    - The forward reference is RESOLVED rather than left standing: `grep -ciE 'falsified|corrected here' tests/test_vco_core.cpp` is at least 1 in invariant 1's grid-provenance paragraph, and that paragraph records both re-measured tracking errors
    - `grep -c 'rising zero crossings\|RISING ZERO CROSSINGS' tests/test_vco_core.cpp` is at least 1 — the corrected premise names the estimator, not the DSP
    - `grep -c 'test_vco_spectrum.cpp' tests/test_vco_core.cpp` is at least 1 — the corrected paragraph names where high-note behaviour is now asserted
    - `grep -c 'NOT the TEST-02 tracking gate' tests/test_vco_core.cpp` is at least 2 — the D-16 label is intact
    - `grep -cE 'CHECK\(relErr < 0\.01\)' tests/test_vco_core.cpp` outputs `1` — the tolerance is unchanged
    - The doctest case count is unchanged from this plan's starting baseline
    - `make guards && make strict` both exit 0
    - `git diff --name-only` lists only `tests/test_vco_core.cpp`, and does not list `src/AnalogLFO.cpp`
  </acceptance_criteria>
  <done>The narrow-pulse sampling limit is re-measured against the band-limited core, the "Phase 32 owns it" forward reference is resolved from that measurement, and the corrected paragraph points at the suite that now asserts high-note behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `forge::VcoCore` -> the module output jack -> downstream gear | An out-of-range voltage leaves the plugin and reaches whatever is patched after it. |
| test oracle -> the claim it stands for | A stand-in that has drifted from what it mirrors is invisible precisely when the drift is inert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03 | Tampering (signal integrity) | out-of-range output damaging downstream gear or speakers | high | mitigate | Two measured tiers: the musical tier tightens from 6.0 V to 5.55 V, and the hostile tier is pinned at 10.0 V from the P-10 table, comfortably inside Rack's plus-or-minus 12 V norm. Scenario five exercises the hostile tier so it is not headroom nobody reaches. |
| T-32-16 | Tampering (signal integrity) | a full-amplitude spike invisible to the spectral gate | high | mitigate | This bound is the only assertion in the suite that can see a merged square site or a double-sourced side decision; both measure 0.0 dB spectrally and about plus-or-minus 9.78 V on the envelope. The banner now records that as the bound's second job. |
| T-32-19 | Repudiation (evidence integrity) | the positive control drifting from the real core | high | mitigate | The oracle is brought back in step with all three of plan 32-06's changes, its control figures are re-observed against the recorded old ones rather than recomputed, and the stop-and-report rule governs a moved figure. |
| T-32-18 | Elevation of Privilege (voice isolation) | the new per-instance `blep` breaking CORE-03 | high | mitigate | Invariant 4 is re-run and required to show 0 of 1024 mismatches on both instances at all three rates, with the distinguishability precondition intact. |
| T-32-23 | Repudiation (evidence integrity) | a stale forward reference claiming a later phase owns a resolved issue | medium | mitigate | Invariant 1's "Phase 32 owns it" attribution is resolved by measurement and corrected in place, keeping the conclusion and replacing the premise — the same discipline plan 30-08 applied to its own scope sentence. |
| T-32-12 | Tampering | the shipped Analog LFO's golden bit-stability | high | mitigate | No `src/` file is touched by this plan; `make test` replays the six `.f32` goldens on every run; `src/AnalogLFO.cpp` is absent from the diff. |
| T-32-SC | Tampering (supply chain) | package installs | low | accept | Zero packages installed in any ecosystem; `32-RESEARCH.md` § Package Legitimacy Audit records `(none)`. |
</threat_model>

<verification>
- `make test` green with an unchanged case count and 0 failures — every change in this plan is inside an existing case.
- Invariant 5 still detects shared state; invariant 4 still measures 0 of 1024 on both instances at all three rates.
- Both bound tiers are pinned from figures measured in this plan, with scenario two's `> 5.1f` and scenario five's exercise floor both satisfied.
- Scenario four's five named assertions are intact and unmerged.
- `make guards` and `make strict` green; `src/AnalogLFO.cpp` absent from the diff.
</verification>

<success_criteria>
- The oracle mirrors the band-limited core in everything but its one deliberate defect, with its control figures re-observed and both old and new recorded.
- `kLooseBoundV` is replaced by `kMusicalBoundV` and `kHostileBoundV`, each with measured provenance and each named in the scenarios that use it.
- Scenario five reproduces the P-10 worst point and proves the hostile tier is exercised.
- Invariant 1's forward reference is resolved by measurement and corrected in place.
</success_criteria>

<output>
Create `.planning/phases/32-morph-aware-anti-aliasing-polyblep-polyblamp/32-08-SUMMARY.md` when done. Record: the oracle's old and new control figures per rate with the mechanism if they moved; every scenario's measured maximum per rate from the step-one measure pass; the two pinned tier values with their provenance; scenario five's observed maximum and pinned exercise floor; and the two re-measured narrow-pulse tracking errors beside the pre-Phase-32 ones.
</output>

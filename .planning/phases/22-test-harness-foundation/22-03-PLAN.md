---
phase: 22-test-harness-foundation
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/dsp/ClockTracker.hpp
  - src/dsp/DriftEngine.hpp
  - src/dsp/LfoCore.hpp
  - src/AnalogLFO.cpp
  - tests/golden/freerun_44100.f32
  - tests/golden/freerun_48000.f32
  - tests/golden/freerun_96000.f32
  - tests/golden/freerun_seeds.txt
  - tests/test_extraction.cpp
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "A driveable forge::LfoCore takes a POD forge::Inputs and returns an output voltage, orchestrating ClockTracker + RatioTable + Swing + DriftEngine + Waveshape with the exact inline per-sample order"
    - "The extracted core reproduces the still-live inline DSP bit-for-bit on identical inputs/seeds at 44.1/48/96 kHz (extraction-correctness gate) before the inline copy is deleted"
    - "Golden reference blocks are captured from the validated core and committed; the plugin shell #includes dsp/LfoCore.hpp, delegates process() to core.step(), and the inline DSP copies are DELETED in the same change (no two live definitions)"
    - "DriftEngine re-implements Xoroshiro128Plus drift bit-identically with the exact 4-OU→jitter→DC RNG draw order; bleedLfo = post-update ouLayers[0].state, retained (not zeroed) when drift < 0.001"
  artifacts:
    - path: "src/dsp/LfoCore.hpp"
      provides: "forge::Inputs POD + LfoCore::step(Inputs)->float orchestrator + seed() + spread-seed setter"
      contains: "struct Inputs"
    - path: "src/dsp/DriftEngine.hpp"
      provides: "OU layers + jitter + DC OU + initComponentSpread, injected RNG + sampleTime, returns bleedLfo"
      contains: "OULayer"
    - path: "src/dsp/ClockTracker.hpp"
      provides: "EMA + outlier-rejection + 3-state FSM; step(clkV, dt, connected)"
      contains: "smoothedPeriod"
    - path: "tests/golden/freerun_48000.f32"
      provides: "Reference output block at 48k, raw little-endian float32, captured from the validated core"
    - path: "src/AnalogLFO.cpp"
      provides: "Thin-er shell that #includes dsp/LfoCore.hpp and delegates; inline DSP deleted"
      contains: "dsp/LfoCore.hpp"
  key_links:
    - from: "src/AnalogLFO.cpp process()"
      to: "forge::LfoCore::step"
      via: "pack params/inputs into forge::Inputs, call core.step(in)"
      pattern: "core\\.step"
    - from: "src/dsp/LfoCore.hpp"
      to: "src/dsp/{ClockTracker,RatioTable,Swing,DriftEngine,Waveshape}.hpp"
      via: "orchestration mirroring process()"
      pattern: "DriftEngine|ClockTracker|Waveshape"
    - from: "src/dsp/Waveshape.hpp morphedWave"
      to: "src/dsp/DriftEngine.hpp bleedLfo output"
      via: "post-drift-update ouLayers[0].state passed as bleedLfo"
      pattern: "bleedLfo"
---

<objective>
Extract the stateful DSP (clock tracker, drift engine) and the `LfoCore` orchestrator, then wire the plugin shell to delegate to it — executing the D-08 capture ordering so the golden baseline is proven against the still-live inline DSP before that inline copy is deleted.

Purpose: TEST-02 (full core extraction effectively lands here per D-04) + D-08 (golden-output regression baseline). This is the load-bearing "extraction changed nothing" proof: the extracted core must reproduce the inline DSP bit-for-bit.
Output: `src/dsp/ClockTracker.hpp`, `src/dsp/DriftEngine.hpp`, `src/dsp/LfoCore.hpp`; a thinned `src/AnalogLFO.cpp` that delegates `process()` to `core.step()` with the inline DSP deleted; committed golden `.f32` files + a provenance sidecar; and `tests/test_extraction.cpp` containing the extraction-correctness gate.

⚠ CROSS-PHASE FLAG (D-04 — surface to the human, do NOT silently re-scope): REQUIREMENTS.md lists TEST-02's full-extraction owner as Phase 24, but the full core extraction lands HERE. Record this in the SUMMARY so the human can confirm updating the requirement-ownership table (Phase 24 then becomes shell-thinning to ~20 lines + CLEAN-01..05 + the already-done bleed lift, NOT core extraction). If, while implementing, the executor finds this materially changes Phase 24's planned scope beyond the ownership note, flag it rather than guessing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-test-harness-foundation/22-RESEARCH.md
@.planning/phases/22-test-harness-foundation/22-PATTERNS.md
@src/AnalogLFO.cpp
@src/dsp/RackCompat.hpp
@src/dsp/Waveshape.hpp
@src/dsp/RatioTable.hpp
@src/dsp/Swing.hpp

<interfaces>
<!-- forge::Inputs POD — copy verbatim from RESEARCH.md L186-204; each field maps to its params[]/inputs[]/ProcessArgs source. -->
forge::Inputs fields (RESEARCH.md L186-204): rate, ratioScaled, morph, character, drift, phaseOffset,
  fmCV, fmAtten, fmConnected, clkVoltage, clkConnected, resetVoltage, resetConnected, swingIndex,
  sampleTime (INJECTED — never read from a global).

LfoCore surface the shell + tests consume:
  void seed(uint64_t s0, uint64_t s1);          // drift RNG
  void setSpreadSeed(uint64_t s0, uint64_t s1); // reproduces initComponentSpread (Open Q2)
  float step(const forge::Inputs& in);          // returns output voltage

Waveshape boundary (from Plan 02): morphedWave(phase, morph, character, bleedLfo) — LfoCore passes the
  post-drift-update ouLayers[0].state as bleedLfo (Pitfall 3).
</interfaces>

<notes>
- Include hygiene (Pitfall 1): every src/dsp/*.hpp includes ONLY `<cmath> <cstdint> <array> <random> <algorithm>`. No rack.hpp/plugin.hpp/rack/*.
- ODR (Pitfall 4): inline/struct-member everything. NEVER ship a state where both the inline DSP and the core are live and reachable — delete the inline copy in the SAME change that wires delegation.
- Bit-identity discipline (D-07/D-08): preserve the exact RNG draw ORDER in DriftEngine (4× OU, then 1× jitter, then 1× DC). Preserve the exp2_taylor5 FM path (Pitfall 2), OnePole snap + per-sample setLambda reconfig (Pitfall 5), and the drift<0.001 skip semantics (Pitfall 3).
- STAYS IN THE SHELL (do NOT pull into core): paramQuantities[...]->getScaledValue(), all display atomics, updateDisplayBuffer, dataToJson/dataFromJson (jansson), and params[]/inputs[]/outputs[] array I/O (PATTERNS.md L189).
- Build workflow (memory): after wiring, `make` must build the plugin .dylib unchanged and it must still load. Do not perturb make/dist/install.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract ClockTracker.hpp and DriftEngine.hpp</name>
  <files>src/dsp/ClockTracker.hpp, src/dsp/DriftEngine.hpp</files>
  <read_first>
    - src/AnalogLFO.cpp L407-544 (processClockInput — the FSM, EMA, outlier rejection, fast-track re-acquire) and L40 (ClockState enum)
    - src/AnalogLFO.cpp L94-105 (OULayer + drift members), L198-216 (initComponentSpread + spreadRng), L606-629 (OU layer theta/sigma/weight init), L726-761 (per-sample OU/jitter/DC step) and L598-599 (random_device seed — module side only)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L88-109 (ClockTracker lift + what becomes inputs/outputs + what stays in shell) and L134-163 (DriftEngine lift, RNG draw order, drift-low skip, bleedLfo output, spread-seed)
    - src/dsp/RackCompat.hpp (forge::SchmittTrigger, Timer, OnePole, Xoroshiro128Plus)
  </read_first>
  <behavior>
    - ClockTracker: lock at 120 BPM after >=4 clean edges (state→LOCKED, smoothedPeriod ≈ 0.5s); a single outlier edge is rejected (smoothedPeriod unchanged); after a sustained >3× tempo jump it re-acquires (does NOT stay LOCKED at the stale period forever — the existing fast-track path). Voltage is injected as `float clkV`, never read from inputs[].
    - DriftEngine: with a fixed seed, two engines step()'d over identical dt/drift produce bit-identical OU/jitter/DC output (deterministic). RNG draw order is exactly 4× OU then 1× jitter then 1× DC. step() returns bleedLfo = ouLayers[0].state AFTER the update. When drift < 0.001, OU layers are NOT stepped and ouLayers[0].state is RETAINED (bleedLfo = last value, not zero). setSpreadSeed reproduces initComponentSpread spread coefficients via the separate spreadRng.
  </behavior>
  <action>
    Create `src/dsp/ClockTracker.hpp` (`#pragma once`, allowed includes, `namespace forge`). Lift the `processClockInput` FSM verbatim from L407-544 into `ClockTracker::step(float clkV, float dt, bool connected) -> {state, smoothedPeriod, edgeFired, resetWanted}`. Replace the two Rack reads — `inputs[CLK_INPUT].isConnected()` (L408) and `inputs[CLK_INPUT].getVoltage()` (L446) — with the injected `connected`/`clkV` params. Use `forge::SchmittTrigger`/`forge::Timer` (replacing L121-122). The ratio index (`paramQuantities[RATE_PARAM]->getScaledValue()`, L509-512) is computed by the shell/RatioTable and passed in, NOT read here. The phase-reset/crossfade writes (L456-458, L528-532) and `resetBlanking.trigger` belong to LfoCore orchestration — ClockTracker only SIGNALS "reset wanted this edge". Display atomics (L421, L441, L484, L541) STAY IN THE SHELL — do not include them.
    Create `src/dsp/DriftEngine.hpp` (`#pragma once`, allowed includes incl. `<random>`, `namespace forge`). Lift `OULayer` (L94-99) and the 4-layer + dcOffsetOU theta/sigma/weight init (L606-629) verbatim. Lift the per-sample OU/jitter/DC step (L731-761) into `step(float dt, float drift, bool isClocked, float sqrtDt) -> {deltaPhaseMul, dcOffsetV, bleedLfo}` using a member `forge::Xoroshiro128Plus rng` + `std::normal_distribution<float>{0,1}`. PRESERVE the exact RNG draw order (4× OU, then 1× jitter, then 1× DC — order is part of the bit-identical stream). After the OU loop, return `ouLayers[0].state` (post-update) as `bleedLfo`. Replicate the drift<0.001 skip (L759-761): do NOT step OU layers, leave ouLayers[0].state retained, return it as bleedLfo (do NOT zero it). Lift `initComponentSpread` (L198-216) using a SEPARATE `forge::Xoroshiro128Plus spreadRng` (L199-200); expose `setSpreadSeed(s0,s1)` and `seed(s0,s1)`. The core MUST accept explicit seeds (no random_device inside src/dsp/).
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L 'rack' src/dsp/ClockTracker.hpp src/dsp/DriftEngine.hpp` lists both files (zero rack tokens); `grep -c 'random_device' src/dsp/DriftEngine.hpp` returns 0.
    - DriftEngine.hpp steps 4 OU layers then jitter then DC in that order; step() returns a bleedLfo equal to ouLayers[0].state after update.
    - ClockTracker.step takes injected `clkV`/`connected` and contains no `inputs[` reference.
    - `make test` passes the ClockTracker lock/recover and DriftEngine determinism/draw-order behavior tests (added in test_dsp_units.cpp or a new case).
  </acceptance_criteria>
  <done>ClockTracker + DriftEngine lifted verbatim, RNG injected with exact draw order, bleedLfo retained at low drift, zero rack/random_device coupling, behavior-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build LfoCore.hpp orchestrator and the extraction-correctness gate</name>
  <files>src/dsp/LfoCore.hpp, tests/test_extraction.cpp</files>
  <read_first>
    - src/AnalogLFO.cpp L659-852 (process() — the orchestration mirror, exact per-sample order)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L167-190 (LfoCore orchestration sequence steps 1-14, what stays in shell)
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L186-204 (forge::Inputs POD verbatim), L256-266 (Pitfall 2 exp2 / Pitfall 3 bleed), L274-278 (Pitfall 5 OnePole snap + per-sample setLambda), L471-493 (golden capture procedure & the step-2 cross-validation gate)
    - src/dsp/{Waveshape,RatioTable,Swing,DriftEngine,ClockTracker,RackCompat}.hpp (the pieces LfoCore wires)
  </read_first>
  <behavior>
    - LfoCore.step(Inputs) reproduces the inline process() per-sample sequence (PATTERNS.md L173-188): clock+reset → dual-mode freq → freqSlew (OnePole) → drift clamp → driftSlew with per-sample setLambda reconfig → FM via forge::exp2_taylor5 → deltaPhase=freq*sampleTime (double) → DriftEngine.step modifies deltaPhase + yields dcOffsetV + bleedLfo → Swing warp → phase accumulate+wrap → morph/character CV+clamp (+characterSpread) → phaseOffset at readout → Waveshape::morphedWave(p,morph,character,bleedLfo) → 5*sample + 3ms cosine anti-click crossfade + dcOffsetV after crossfade.
    - Extraction gate: for a fixed (s0,s1) drift seed + fixed spread seed, LfoCore output equals the inline-DSP output BIT-FOR-BIT over a sample block at 44.1/48/96 kHz, for both a free-run scenario (drift on) and a drift=0 scenario.
  </behavior>
  <action>
    Create `src/dsp/LfoCore.hpp` (`#pragma once`, allowed includes, `namespace forge`). Define the `forge::Inputs` POD verbatim from RESEARCH.md L186-204. Implement `LfoCore::step(const Inputs&) -> float` mirroring the L659-852 per-sample order exactly (PATTERNS.md L173-188 lists all 14 steps). LANDMINES: (Pitfall 2) FM uses `forge::exp2_taylor5(fmPitch)` not std::exp2; (Pitfall 5) the driftSlew `setLambda(1.f/slewTau)` per-sample lazy reconfig (L695-705) must be reproduced inside step, and OnePole keeps its snap; (Pitfall 3) pass DriftEngine's post-update bleedLfo into `morphedWave`. Expose `seed(s0,s1)`, `setSpreadSeed(s0,s1)`. Do NOT pull shell-only concerns into the core (paramQuantities scaled-value, display atomics, updateDisplayBuffer, JSON, params/inputs/outputs arrays — PATTERNS.md L189).
    Create `tests/test_extraction.cpp` (`#include "doctest.h"`, no impl macro) implementing the D-08 step-2 EXTRACTION-CORRECTNESS GATE (RESEARCH.md L493): while the inline DSP is STILL LIVE in AnalogLFO.cpp, this test drives the extracted `LfoCore` AND a reference computed the inline way on identical inputs/seeds and asserts bit-exact equality. Because the shell is not yet refactored, capture the inline reference by temporarily exposing or replicating the inline path under the same seeds (document the method in the test). This is the real "did extraction preserve behavior" check; the permanent golden replay (Plan 04) pins it thereafter.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `src/dsp/LfoCore.hpp` defines `struct Inputs` and `LfoCore::step(const Inputs&)`, with `seed`/`setSpreadSeed`; `grep -L 'rack' src/dsp/LfoCore.hpp` lists the file.
    - LfoCore FM path calls `forge::exp2_taylor5` (not `std::exp2`); `grep -c 'std::exp2' src/dsp/LfoCore.hpp` returns 0.
    - `tests/test_extraction.cpp` asserts the extracted core matches the inline DSP bit-exact at 44.1/48/96 kHz for drift-on AND drift=0, and passes under `make test`.
  </acceptance_criteria>
  <done>LfoCore orchestrates the full per-sample chain; the extraction-correctness gate proves the core reproduces the still-live inline DSP bit-for-bit at all three rates.</done>
</task>

<task type="auto">
  <name>Task 3: Capture goldens, wire the shell to delegate, and delete the inline DSP</name>
  <files>src/AnalogLFO.cpp, tests/golden/freerun_44100.f32, tests/golden/freerun_48000.f32, tests/golden/freerun_96000.f32, tests/golden/freerun_seeds.txt</files>
  <read_first>
    - src/AnalogLFO.cpp (the whole shell — process() L659-852, the inline DSP being deleted, dataToJson/dataFromJson L632-657 stay)
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L471-493 (golden capture procedure + D-08 ordering: validate, write golden, THEN delete inline) and L235-237 (storage format: raw little-endian float32, sidecar .txt, tracked in git)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L255-257 (shell extract-in-place: include, delegate, delete inline in the SAME change — Pitfall 4)
    - .planning/research/PITFALLS.md L184-201 (Pitfall 6: extract-pure-function-first, output-preserving discipline)
  </read_first>
  <action>
    With the extraction gate (Task 2) GREEN, freeze the goldens: run the validated `LfoCore` over the canonical free-run scenario (base.rate/morph/character/drift per RESEARCH.md L486, fixed seeds) at 44100/48000/96000 and write each output block as raw little-endian float32 to `tests/golden/freerun_<rate>.f32`. Write `tests/golden/freerun_seeds.txt` documenting the exact (s0,s1) drift seed, spread seed, scenario params, sample count, and the canonical capture OS (bit-exact gate is canonical-OS-only per Pitfall 6 / Open Q1). These files are TRACKED in git (not gitignored).
    THEN wire the shell: in `src/AnalogLFO.cpp` `#include "dsp/LfoCore.hpp"`, add a `forge::LfoCore core` member, seed it from the existing `random_device`-derived seeds in the module's init (mirroring L598-599) and set the spread seed from the persisted `spreadSeed0/1`. Refactor `process()` to pack `params[]`/`inputs[]`/`ProcessArgs` into a `forge::Inputs` and call `core.step(in)`, then write `outputs[]`, display atomics, and run `updateDisplayBuffer` as before. DELETE the inline DSP copies (computeMorphedWave/compute*, processClockInput, the inline OU step, the inline orchestration that LfoCore now owns) IN THIS SAME CHANGE — never ship two live definitions (Pitfall 4). Keep in the shell: `dataToJson`/`dataFromJson` (L632-657, jansson), display atomics, `updateDisplayBuffer`, and the params/inputs/outputs I/O. NOTE: this begins delegation; the ~20-line pass-through shell-thinning finishes in Phase 24 (D-04) — the shell may still be larger than 20 lines after this plan, that is expected.
  </action>
  <verify>
    <automated>make test && make 2>&1 | tail -3 && ls tests/golden/*.f32 | wc -l | grep -q 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three golden `.f32` files exist under `tests/golden/` plus `freerun_seeds.txt` documenting seeds/scenario/canonical-OS; the `.f32` files are committed (not gitignored).
    - `src/AnalogLFO.cpp` contains `#include "dsp/LfoCore.hpp"` and `core.step(`; the inline `computeMorphedWave`/`processClockInput` definitions are DELETED (`grep -c 'float computeMorphedWave' src/AnalogLFO.cpp` returns 0).
    - `make` builds the plugin .dylib and it loads unchanged (no duplicate-symbol error; behavior preserved per the golden gate).
    - `make test` stays green (extraction gate + existing units).
  </acceptance_criteria>
  <done>Goldens captured from the validated core and committed; shell delegates process() to core.step(); inline DSP deleted in the same change; plugin builds + loads unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Build/refactor of pure DSP into a Rack-free core plus a thin delegation in the shell. No network, no auth, no untrusted runtime input in this phase's scope (patch-load JSON hardening is BUG-04 → Phase 23). The real boundary is float-output identity between the inline DSP and the extracted core.

| Boundary | Description |
|----------|-------------|
| inline DSP → extracted core → shipped plugin | Output must be bit-identical; a silent divergence ships a behavior regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-I | Integrity | extracted core diverging from shipped DSP | mitigate | D-08 extraction-correctness gate asserts core == inline bit-exact at 3 rates BEFORE the inline copy is deleted; goldens are frozen from the validated core; verbatim primitives + preserved RNG draw order + exp2/OnePole/bleed landmine handling. |
| T-22-03-D | Denial of service | duplicate-symbol / two live DSP definitions | mitigate | Delete inline DSP in the SAME change that wires delegation (Pitfall 4); `make` build verifies no duplicate-symbol link error and the plugin still loads. |
| T-22-03-SC | Tampering | supply chain | accept | No package-manager install in this plan; doctest already vendored+pinned (T-22-SC, Plan 01). |

No high-severity threats reach the ASVS L1 block threshold — offline DSP-extraction tooling, no runtime network/auth/untrusted-input surface added.
</threat_model>

<verification>
- `make test` green: extraction-correctness gate proves core == inline bit-exact at 44.1/48/96 kHz (drift on + drift=0).
- `make` builds the plugin .dylib unchanged and it loads (no two live DSP definitions).
- `grep -L 'rack' src/dsp/*.hpp` lists every dsp header (zero rack includes).
- Three golden `.f32` + sidecar `.txt` committed under `tests/golden/`.
- Inline `computeMorphedWave`/`processClockInput` deleted from `AnalogLFO.cpp`.
</verification>

<success_criteria>
- TEST-02: the full Rack-independent DSP core (`src/dsp/*.hpp`) exists, zero `rack/` includes, and is consumed by the plugin shell which delegates `process()` to it.
- D-08: the extracted core is proven to preserve inline-DSP behavior bit-for-bit, and golden baselines are frozen for permanent regression replay in Plan 04.
- D-04 cross-phase consequence surfaced in the SUMMARY for human confirmation of the Phase 24 / TEST-02 ownership update.
</success_criteria>

<output>
Create `.planning/phases/22-test-harness-foundation/22-03-SUMMARY.md` when done. Include the D-04 / TEST-02-ownership flag for human confirmation.
</output>

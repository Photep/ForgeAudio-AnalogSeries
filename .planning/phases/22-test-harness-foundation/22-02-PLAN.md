---
phase: 22-test-harness-foundation
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/dsp/RackCompat.hpp
  - src/dsp/Waveshape.hpp
  - src/dsp/RatioTable.hpp
  - src/dsp/Swing.hpp
  - tests/test_dsp_units.cpp
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "src/dsp/RackCompat.hpp re-implements the 6 Rack primitives bit-identically (Xoroshiro128Plus, SchmittTrigger, Timer, PulseGenerator, OnePole, exp2_taylor5) plus a clamp shim"
    - "Waveshape, RatioTable, and Swing pure-math headers exist with zero rack/ includes and compile under the Rack-free test target"
    - "The computeMorphedWave bleed dependency on ouLayers[0].state is lifted to an explicit float bleedLfo parameter (D-05)"
  artifacts:
    - path: "src/dsp/RackCompat.hpp"
      provides: "forge:: re-implementations of Rack SDK primitives, bit-identical to shipped"
      contains: "namespace forge"
    - path: "src/dsp/Waveshape.hpp"
      provides: "sine/tri/saw/square/pulse/morph wave math with explicit bleedLfo param"
      contains: "bleedLfo"
    - path: "src/dsp/RatioTable.hpp"
      provides: "RATIO_TABLE[15] + RATIO_LABELS + shouldReset(ratioIdx, beatCount), shaped for BEATS_PER_ALIGN[15] (P23)"
      contains: "RATIO_TABLE"
    - path: "src/dsp/Swing.hpp"
      provides: "SWING_FRACTIONS[6] + swingPhaseMultiplier(phase, swingFrac, isClocked)"
      contains: "SWING_FRACTIONS"
  key_links:
    - from: "src/dsp/Waveshape.hpp"
      to: "src/dsp/RackCompat.hpp"
      via: "forge::clamp / forge::exp2_taylor5 where the inline code used rack equivalents"
      pattern: "forge::"
    - from: "tests/test_dsp_units.cpp"
      to: "src/dsp/{Waveshape,RatioTable,Swing}.hpp"
      via: "#include via -Isrc"
      pattern: "dsp/"
---

<objective>
Extract the Rack SDK primitive shims and the three PURE (stateless math) DSP headers into `src/dsp/`. These are the leaf dependencies the stateful headers and the orchestrator (Plan 03) build on. Lift the bleed-LFO coupling out of the wave math now (D-05) so Waveshape stays pure.

Purpose: TEST-02 (scaffold) — pure DSP logic begins moving into a Rack-independent header-only core (`src/dsp/*.hpp`) with zero `rack/` includes. RackCompat.hpp is the bit-identity foundation that makes the later golden gate a bit-exact check.
Output: `src/dsp/RackCompat.hpp`, `src/dsp/Waveshape.hpp`, `src/dsp/RatioTable.hpp`, `src/dsp/Swing.hpp`, and a `tests/test_dsp_units.cpp` that compiles them under the Rack-free target and sanity-checks them.

This plan does NOT yet wire the plugin shell or delete inline DSP (that is Plan 03). The inline copies in `AnalogLFO.cpp` stay live and untouched here — the new headers are added alongside.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-test-harness-foundation/22-RESEARCH.md
@.planning/phases/22-test-harness-foundation/22-PATTERNS.md
@src/AnalogLFO.cpp

<interfaces>
<!-- The verbatim re-implementations the executor must COPY from RESEARCH.md (do not re-derive from the SDK). -->
<!-- RESEARCH.md L293-396 contains the full source for all of RackCompat.hpp. -->

forge:: primitives required in src/dsp/RackCompat.hpp (RESEARCH.md L293-396):
  - Xoroshiro128Plus      (L293-319) — uint64_t state[2], seed(s0,s1) shifts a bad seed, operator(), min()/max()
  - SchmittTrigger        (L327-340) — LOW/HIGH/UNINITIALIZED; UNINITIALIZED handling is load-bearing
  - Timer                 (L342-348)
  - PulseGenerator        (L350-357) — trigger() keeps the LONGER of existing/new
  - OnePole               (L359-370) — == TExponentialFilter<float>; out=(out==y)?in:y snap is load-bearing (Pitfall 5); setLambda/setTau
  - clamp                 (L372)     — std::clamp wrapper
  - exp2Floor + exp2_taylor5 (L374-393) — bit-identical FM path (Pitfall 2); polyHorner order VERIFIED in RESEARCH.md L396

Wave-math entry point Waveshape.hpp must expose (PATTERNS.md L52-56):
  inline float morphedWave(float phase, float morph, float character, float bleedLfo);
  // AnalogLFO.cpp:369  bleedIntensity *= (1.f + ouLayers[0].state * 0.2f)  becomes
  //                    bleedIntensity *= (1.f + bleedLfo * 0.2f)
</interfaces>

<notes>
- Include hygiene (Pitfall 1 / TEST-02): every `src/dsp/*.hpp` includes ONLY `<cmath> <cstdint> <array> <random> <algorithm>` — never `rack.hpp`, `plugin.hpp`, or `rack/*`.
- ODR (Pitfall 4): every free function in these headers must be `inline` or a struct member.
- All three math headers wrap their content in `namespace forge`.
- Inline copies in AnalogLFO.cpp are NOT modified in this plan. Two definitions co-exist (inline + new header) but the new headers are only consumed by the test, never by the shell yet — no ODR clash because the shell does not include them yet.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-implement Rack primitives verbatim into RackCompat.hpp</name>
  <files>src/dsp/RackCompat.hpp</files>
  <read_first>
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L293-396 (the VERBATIM source for every primitive — copy from here) and L396 (polyHorner order verification)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L193-207 (RackCompat type→source map + include hygiene)
    - src/AnalogLFO.cpp L101 (rack::random::Xoroshiro128Plus rng), L121-122 (SchmittTrigger/Timer members), L140-141 (TExponentialFilter freqSlew/driftSlew), L167 (PulseGenerator resetBlanking), L715 (dsp::exp2_taylor5) — the call sites being shimmed
  </read_first>
  <behavior>
    - Xoroshiro128Plus seeded with a fixed (s0,s1) produces a reproducible uint64 stream; same seed → identical stream; min()==0, max()==UINT64_MAX so std::normal_distribution accepts it.
    - SchmittTrigger from UNINITIALIZED: process(>=highThreshold) returns true once then stays HIGH; process(<=lowThreshold) goes LOW with no spurious trigger.
    - OnePole: at steady state where computed y == out, output snaps exactly to in (granularity snap, Pitfall 5) — repeated process() at constant in converges and stays exactly at in.
    - exp2_taylor5(0.f) == 1.f exactly; exp2_taylor5(1.f) within tight tolerance of 2.f (it is the Rack polynomial approximation, not libm exp2).
  </behavior>
  <action>
    Create `src/dsp/RackCompat.hpp` with `#pragma once`, the allowed includes only (`<cmath> <cstdint> <algorithm>`; add `<random>`/`<array>` only if used), wrapped in `namespace forge`. COPY the implementations VERBATIM from RESEARCH.md L293-396 (do not re-derive from the SDK): `Xoroshiro128Plus`, `SchmittTrigger`, `Timer`, `PulseGenerator`, `OnePole`, `clamp`, `exp2Floor`, `exp2_taylor5`. Preserve the exact bit tricks in `exp2Floor` (int32 reinterpret) and the Horner loop order in `exp2_taylor5` (RESEARCH.md confirms it is bit-identical to the SDK `polyHorner`). The `OnePole::process` MUST keep `out = (out == y) ? in : y;` — this snap-to-input is load-bearing for golden bit-identity (Pitfall 5). Add a source-provenance comment citing the SDK header each primitive mirrors.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L 'rack' src/dsp/RackCompat.hpp` lists the file (zero `rack` tokens).
    - `src/dsp/RackCompat.hpp` contains `namespace forge` and all six primitives + `clamp` + `exp2_taylor5`.
    - `OnePole::process` contains the `(out == y) ? in : y` snap.
    - `make test` compiles RackCompat.hpp (via test_dsp_units.cpp from Task 4) and the primitive behavior tests pass.
  </acceptance_criteria>
  <done>RackCompat.hpp re-implements all 6 primitives + clamp bit-identically, zero rack includes, compiled and behavior-tested under make test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract Waveshape.hpp with the explicit bleedLfo lift (D-05)</name>
  <files>src/dsp/Waveshape.hpp</files>
  <read_first>
    - src/AnalogLFO.cpp L194-388 (progressiveCurve, computeSine/Triangle/Saw/Square/Pulse, computeMorphedWave — lift verbatim) and L369 specifically (the ouLayers[0].state bleed coupling)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L38-59 (Waveshape lift instructions, spread-member dependency, the bleed lift, ODR rule)
    - src/dsp/RackCompat.hpp (for forge::clamp if the wave math clamps)
  </read_first>
  <behavior>
    - morphedWave(phase, morph, character, bleedLfo) over a full phase cycle [0,1) stays within [-1, 1] pre-scale for every (morph, character) in [0,1]×[0,1].
    - Shape identity at morph segment boundaries: morph=0.0 ≈ sine, 0.25 ≈ triangle, 0.5 ≈ saw, 0.75 ≈ square, 1.0 ≈ narrow pulse (spot-check a representative phase).
    - bleedLfo enters ONLY at the line that was `bleedIntensity *= (1.f + ouLayers[0].state * 0.2f)`, now `*= (1.f + bleedLfo * 0.2f)` — passing bleedLfo=0 reproduces the no-drift bleed path.
  </behavior>
  <action>
    Create `src/dsp/Waveshape.hpp` (`#pragma once`, allowed includes only, `namespace forge`). Lift verbatim from `src/AnalogLFO.cpp` L194-388: `progressiveCurve` (L194-196), `computeSine` (L222-232), `computeTriangle` (L234-260), `computeSaw` (L262-278), `computeSquare` (L280-301), `computePulse` (L303-329), `computeMorphedWave` (L331-388). Carry the spread members these read (`triAsymmetrySpread`, `sawCurvatureSpread`, `squareDutySpread`, `pulseEdgeSpread`, `bleedSpread` — L240, L268, L286, L316, L365) as fields of a `Waveshape` struct (or a passed `Spread` POD) — they are produced by spread init / DriftEngine, not by the wave math. THE LIFT (D-05 / Pitfall 3): change the signature to `inline float morphedWave(float phase, float morph, float character, float bleedLfo)` and replace `AnalogLFO.cpp:369` `bleedIntensity *= (1.f + ouLayers[0].state * 0.2f)` with `bleedIntensity *= (1.f + bleedLfo * 0.2f)`. Every free function `inline` (Pitfall 4). Use `forge::clamp` where the inline math used `rack::math::clamp`. Do NOT alter the math otherwise — bit-identity to the inline copy is the load-bearing requirement.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `src/dsp/Waveshape.hpp` exposes `morphedWave(float phase, float morph, float character, float bleedLfo)`; no `ouLayers` reference appears anywhere in the file.
    - `grep -c 'ouLayers' src/dsp/Waveshape.hpp` returns 0.
    - `grep -L 'rack' src/dsp/Waveshape.hpp` lists the file (zero rack tokens).
    - The Waveshape bounds + shape-identity tests in test_dsp_units.cpp pass under `make test`.
  </acceptance_criteria>
  <done>Waveshape.hpp lifts the wave math verbatim with bleedLfo as an explicit parameter, zero rack/ouLayers coupling, tested in [-1,1] bounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract RatioTable.hpp and Swing.hpp (pure tables + math)</name>
  <files>src/dsp/RatioTable.hpp, src/dsp/Swing.hpp</files>
  <read_first>
    - src/AnalogLFO.cpp L42-65 (RATIO_TABLE/RATIO_LABELS), L503-533 (beat-align / shouldReset decision), L67-75 (SWING_FRACTIONS), L763-773 (swing warp)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L63-84 (RatioTable lift + shape-for-P23) and L113-130 (Swing lift, isClocked gate, >0.5001f fast path)
  </read_first>
  <behavior>
    - RatioTable: RATIO_TABLE and RATIO_LABELS are length 15. shouldReset(ratioIdx, beatCount): for a division ratio (RATIO_TABLE[idx] < 1), reset only when beatCount >= round(1/ratio); for ratios >= 1 it resets every beat. (Do NOT implement the P23 x1.5/÷1.5 fix — preserve current behavior.)
    - Swing: swingPhaseMultiplier(phase, swingFrac, isClocked) returns 1.0 when isClocked is false (free-run never warps); returns 1.0 when swingFrac <= 0.5001 (straight); for swingFrac=0.66 clocked, first-half (phase<0.5) and second-half multipliers match the inline 0.5/swingFrac and 0.5/(1-swingFrac) values.
  </behavior>
  <action>
    Create `src/dsp/RatioTable.hpp` (`#pragma once`, allowed includes, `namespace forge`). Lift `RATIO_TABLE[15]` and `RATIO_LABELS[15]` verbatim from `src/AnalogLFO.cpp` L43-59. Lift the division-aware reset decision (L520-524) into `inline bool shouldReset(int ratioIdx, int beatCount)`. SHAPE FOR P23 (D-04 / deferred — do NOT implement the fix here): structure `shouldReset` so a per-ratio `BEATS_PER_ALIGN[15]` table could replace the `round(1/RATIO_TABLE[idx])` divisor math later without an API change. Leave a comment marking where `BEATS_PER_ALIGN` will slot in.
    Create `src/dsp/Swing.hpp` (`#pragma once`, allowed includes, `namespace forge`). Lift `SWING_FRACTIONS[6]` verbatim (L68-75) and the warp into `inline double swingPhaseMultiplier(double phase, float swingFrac, bool isClocked)` mirroring L767-773: preserve the `isClocked` gate (PHASE-04: free-run never warps — return 1.0) and the `> 0.5001f` straight-mode fast path (return 1.0). Use double precision for the multiplier as the inline code does. Do NOT change behavior (the P23 free-run swing-desync fix lands in Phase 23; only SHAPE the API to receive it).
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `src/dsp/RatioTable.hpp` defines length-15 `RATIO_TABLE` and `RATIO_LABELS` and an `inline bool shouldReset(int, int)`; a comment marks the future `BEATS_PER_ALIGN[15]` slot.
    - `src/dsp/Swing.hpp` defines length-6 `SWING_FRACTIONS` and `swingPhaseMultiplier(...)` returning 1.0 when `!isClocked` or `swingFrac <= 0.5001f`.
    - `grep -L 'rack' src/dsp/RatioTable.hpp src/dsp/Swing.hpp` lists both files (zero rack tokens).
    - RatioTable + Swing unit tests in test_dsp_units.cpp pass under `make test`.
  </acceptance_criteria>
  <done>RatioTable.hpp and Swing.hpp lift the tables + reset/warp math verbatim, shaped for P23 fixes without behavior change, both rack-free and tested.</done>
</task>

<task type="auto">
  <name>Task 4: Add unit tests exercising the three pure headers + RackCompat</name>
  <files>tests/test_dsp_units.cpp</files>
  <read_first>
    - tests/main.cpp (impl TU — test_dsp_units.cpp must NOT redefine the impl macro)
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L590 (waveshape scaffold scope — full ranges are P23) and L597-606 (candidate tolerances)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L223-233 (test scaffold expectations)
  </read_first>
  <action>
    Create `tests/test_dsp_units.cpp` (`#include "doctest.h"`, NO impl macro; `#include "dsp/RackCompat.hpp"`, `dsp/Waveshape.hpp`, `dsp/RatioTable.hpp`, `dsp/Swing.hpp` via `-Isrc`). Add doctest TEST_CASEs covering the `<behavior>` blocks of Tasks 1-3: (a) RackCompat — Xoroshiro same-seed reproducibility, SchmittTrigger UNINITIALIZED→HIGH trigger-once, OnePole steady-state snap, exp2_taylor5(0)==1; (b) Waveshape — morph×character sweep stays in [-1,1] pre-scale over a phase cycle (this is the scaffold; FULL ranges are Phase 23 per RESEARCH.md L590), shape identity at the 5 morph boundaries; (c) RatioTable — length-15 tables, shouldReset division behavior; (d) Swing — straight at swingFrac=0.5, isClocked=false ⇒ 1.0, 0.66 warp values. Use the candidate tolerances from RESEARCH.md L597-606 where approximate comparisons are needed.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E 'test cases:.*passed|SUCCESS' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dsp_units.cpp` includes the four dsp headers and contains TEST_CASEs for RackCompat, Waveshape, RatioTable, and Swing; it does NOT define the doctest impl macro.
    - `make test` compiles and runs green with all new unit cases passing (exit 0).
    - The Waveshape sweep asserts every grid point stays in [-1, 1] pre-scale.
  </acceptance_criteria>
  <done>test_dsp_units.cpp exercises all four new headers and passes under make test, proving the pure core compiles and behaves Rack-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Build-only extraction of pure-math C++ headers into a test-compiled core. No network, no auth, no untrusted runtime input. The only boundary is correctness/integrity of the extracted code vs the shipped DSP — guarded by behavior tests here and the golden gate in Plan 03.

| Boundary | Description |
|----------|-------------|
| inline DSP → extracted core | Float-output identity must be preserved; a silent divergence is the real risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-I | Information disclosure / Integrity | extracted DSP headers diverging from shipped math | mitigate | Copy primitives VERBATIM from RESEARCH.md (not "equivalently"); preserve OnePole snap and exp2_taylor5 bit tricks; behavior tests here, and the bit-exact golden gate in Plan 03, catch any divergence. |
| T-22-02-SC | Tampering | supply chain | accept | No package-manager install in this plan; doctest already vendored+pinned in Plan 01 (T-22-SC). |

No high-severity threats. Build-only, offline tooling with no runtime input surface — ASVS L1 high block-threshold not reached.
</threat_model>

<verification>
- `make test` green with all new unit cases passing.
- `grep -L 'rack' src/dsp/*.hpp` lists every dsp header (zero rack includes — TEST-02 / Pitfall 1).
- Waveshape exposes `bleedLfo` and references no `ouLayers` (D-05 lift verified).
- Inline DSP in `AnalogLFO.cpp` is unchanged; plugin build (`make`) still succeeds.
</verification>

<success_criteria>
- TEST-02 (scaffold): RackCompat + Waveshape + RatioTable + Swing exist under `src/dsp/`, zero `rack/` includes, consumed by the Rack-free test target.
- The bleed-LFO lift (D-05) is complete in the wave math, ready for LfoCore to supply `bleedLfo` in Plan 03.
- The pure leaf headers are in place for the stateful headers + orchestrator in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/22-test-harness-foundation/22-02-SUMMARY.md` when done.
</output>

---
phase: 22-test-harness-foundation
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - tests/BlockDriver.hpp
  - tests/test_invariants.cpp
  - tests/test_golden.cpp
  - .github/workflows/test.yml
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "A headless tests/BlockDriver.hpp drives forge::LfoCore over sample blocks, injecting sampleTime=1/sampleRate per sample and seeding deterministically"
    - "Invariant tests assert ±5V output bounds, free-run frequency accuracy, phase continuity at reset, and fixed-seed determinism, parametrized over 44.1/48/96 kHz"
    - "The golden replay test pins the extracted core against the committed reference blocks (bit-exact on the canonical OS, epsilon-tolerant for drift-on elsewhere)"
    - "A GitHub Actions matrix (ubuntu/macos/windows) runs make test on every push, Rack-free (no RACK_DIR, no SDK fetch, no xvfb)"
  artifacts:
    - path: "tests/BlockDriver.hpp"
      provides: "Block-driver harness over LfoCore with a clockedScenario helper"
      contains: "struct BlockDriver"
    - path: "tests/test_invariants.cpp"
      provides: "TEST-04 invariants parametrized over 3 sample rates"
      contains: "44100"
    - path: "tests/test_golden.cpp"
      provides: "D-08 permanent golden replay regression"
      contains: "golden"
    - path: ".github/workflows/test.yml"
      provides: "3-OS CI matrix running make test"
      contains: "matrix"
  key_links:
    - from: "tests/test_invariants.cpp"
      to: "tests/BlockDriver.hpp"
      via: "BlockDriver.run(nSamples, inputAt) over {44100,48000,96000}"
      pattern: "BlockDriver"
    - from: "tests/test_golden.cpp"
      to: "tests/golden/*.f32"
      via: "loadF32 + replay with the capture seeds"
      pattern: "golden/freerun"
    - from: ".github/workflows/test.yml"
      to: "make test"
      via: "matrix job, unix make / windows direct g++"
      pattern: "make test"
---

<objective>
Stand up the headless block-driver harness and the full invariant suite over the extracted `LfoCore`, pin the golden regression permanently, and wire cross-platform CI — completing the v1.4 safety net.

Purpose: TEST-04 — a headless block-driver harness runs the DSP core over sample blocks and asserts output invariants (±5V bounds, frequency accuracy, phase continuity at reset, fixed-seed determinism) at 44.1/48/96 kHz, establishing the behavioral baseline for later refactors. Plus D-09 (GitHub Actions CI now) and the permanent D-08 golden replay.
Output: `tests/BlockDriver.hpp`, `tests/test_invariants.cpp`, `tests/test_golden.cpp`, and `.github/workflows/test.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-test-harness-foundation/22-RESEARCH.md
@.planning/phases/22-test-harness-foundation/22-PATTERNS.md
@src/dsp/LfoCore.hpp
@tests/golden/freerun_seeds.txt

<interfaces>
<!-- BlockDriver template — RESEARCH.md L422-461. Drives LfoCore; links nothing. -->
BlockDriver: { forge::LfoCore core; double sampleRate; }
  BlockDriver(sr, s0, s1) : seeds core
  std::vector<float> run(int nSamples, std::function<forge::Inputs(int)> inputAt)  // sets in.sampleTime = 1/sr
  static std::function<forge::Inputs(int)> clockedScenario(sr, bpm, base)          // square-wave clock into clkVoltage

Golden replay loader (RESEARCH.md L477-491): loadF32(path) -> std::vector<float>; replay with SAME seeds as capture.
</interfaces>

<notes>
- Candidate tolerances (RESEARCH.md L597-606, Claude's-Discretion — adopt these concrete values):
  - ±5V bounds: [-5.12, +5.12] V with drift; strict ±5.0 V (allow 1e-4 FP slop) without drift.
  - Frequency accuracy (free-run): within ±1.0% of knob Hz over ≥2 s.
  - Phase continuity at reset: |Δout| ≤ 0.5 V per sample across the 3 ms crossfade.
  - Determinism (same platform): 0 (bit-exact).
  - Golden epsilon: 0 (bit-exact) on canonical OS / drift-off everywhere; 1e-5 absolute for drift-on on non-canonical OS.
- Cross-platform split (Pitfall 6 + Open Q1): same-platform same-seed determinism is bit-exact everywhere; the drift-on golden is bit-exact ONLY on the canonical capture OS (std::normal_distribution is not portable across libstdc++/libc++/MSVC). Run drift-on golden bit-exact only on the canonical leg; epsilon-tolerant elsewhere (or restrict the cross-platform bit-exact golden to drift-off blocks).
- Windows CI (RESEARCH.md L516-522): windows-latest has no preinstalled GNU make and defaults to MSVC. Use the direct g++ invocation fallback OR `choco install make` with g++/clang on PATH. Prefer the same compiler family as the canonical golden-capture OS for any bit-exact golden leg.
- doctest impl macro stays solely in tests/main.cpp; these new TUs only `#include "doctest.h"`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BlockDriver.hpp and the TEST-04 invariant suite</name>
  <files>tests/BlockDriver.hpp, tests/test_invariants.cpp</files>
  <read_first>
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L422-461 (BlockDriver template), L580-606 (invariant→test map + candidate tolerances)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L211-213 (BlockDriver — no analog) and L223-225 (invariants expectations)
    - src/dsp/LfoCore.hpp (the step(Inputs)->float boundary the driver consumes)
    - .planning/research/ARCHITECTURE.md L162-176 (meaningful assertions for an LFO over sample blocks)
  </read_first>
  <action>
    Create `tests/BlockDriver.hpp` (`#pragma once`) from RESEARCH.md L422-461: a `struct BlockDriver { forge::LfoCore core; double sampleRate; }` with a constructor that seeds the core, a `run(int nSamples, std::function<forge::Inputs(int)> inputAt)` that injects `in.sampleTime = 1.0/sampleRate` per sample and collects `core.step(in)`, and a static `clockedScenario(sr, bpm, base)` helper emitting a square-wave clock into `clkVoltage`. Links nothing.
    Create `tests/test_invariants.cpp` (`#include "doctest.h"`, no impl macro; `#include "BlockDriver.hpp"`). Implement TEST-04 invariants, each parametrized over {44100, 48000, 96000} (a loop or doctest subcases): (1) ±5V bounds — every output sample in [-5.12,+5.12] with drift, strict ±5.0±1e-4 without; (2) frequency accuracy — measure free-run period via zero-crossings over ≥2 s, assert within ±1.0% of the knob Hz; (3) phase continuity at reset — drive a steady clock via clockedScenario, assert |out[n]-out[n-1]| ≤ 0.5 V across the 3 ms crossfade at each reset; (4) fixed-seed determinism — two identically-seeded BlockDrivers produce bit-identical blocks (drift on), different seeds diverge. Use the tolerances from the notes block.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E 'test cases:.*passed|SUCCESS' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BlockDriver.hpp` defines `struct BlockDriver` with `run(...)` injecting `sampleTime` and a `clockedScenario` helper; it includes `dsp/LfoCore.hpp` and links nothing.
    - `tests/test_invariants.cpp` asserts all four invariants and runs each across 44100/48000/96000 (`grep -E '44100|48000|96000' tests/test_invariants.cpp` matches all three).
    - `make test` passes the full invariant suite at all three rates (exit 0).
  </acceptance_criteria>
  <done>BlockDriver drives LfoCore headless; all four TEST-04 invariants pass at 44.1/48/96 kHz.</done>
</task>

<task type="auto">
  <name>Task 2: Add the permanent golden replay regression (D-08)</name>
  <files>tests/test_golden.cpp</files>
  <read_first>
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L471-493 (golden replay template + capture ordering), L597-606 (golden epsilon values)
    - tests/golden/freerun_seeds.txt (the exact seeds + scenario + canonical OS captured in Plan 03)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L227-229 (test_golden expectations, canonical-OS bit-exact / epsilon split)
  </read_first>
  <action>
    Create `tests/test_golden.cpp` (`#include "doctest.h"`, no impl macro; `#include "BlockDriver.hpp"`). Add a `loadF32(path)` helper (RESEARCH.md L477-482) and TEST_CASEs that, for each rate in {44100,48000,96000}, load `tests/golden/freerun_<rate>.f32`, replay the canonical free-run scenario through a `BlockDriver` seeded with the SAME seeds documented in `freerun_seeds.txt`, and assert the output matches the reference. Use bit-exact (`doctest::Approx(ref[i]).epsilon(0)`) on the canonical OS / drift-off path, and the `1e-5` absolute tolerance for the drift-on path on non-canonical OS — gate the bit-exact-vs-epsilon choice on a compile-time or runtime OS check consistent with the CI split (Pitfall 6 / Open Q1). Document the canonical OS in a comment, sourced from `freerun_seeds.txt`.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E 'test cases:.*passed|SUCCESS' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_golden.cpp` loads all three `tests/golden/freerun_*.f32` and replays with the seeds from `freerun_seeds.txt`.
    - The golden replay asserts bit-exact match on the canonical OS / drift-off, epsilon `1e-5` for drift-on elsewhere.
    - `make test` passes the golden regression on the development (canonical) machine (exit 0).
  </acceptance_criteria>
  <done>The golden replay test pins the extracted core to the committed reference blocks permanently; green on the canonical OS.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the GitHub Actions CI matrix (D-09)</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .planning/phases/22-test-harness-foundation/22-RESEARCH.md L495-522 (the workflow template + the windows-make caveat and direct-g++ fallback)
    - .planning/phases/22-test-harness-foundation/22-PATTERNS.md L239-241 (CI — no analog, template source, MSVC float-codegen note)
    - .planning/phases/22-test-harness-foundation/22-CONTEXT.md D-09 (CI runs on the private repo, Rack-free)
  </read_first>
  <action>
    Create `.github/workflows/test.yml` from RESEARCH.md L495-522: `name: test`, `on: [push, pull_request]`, a `test` job with `strategy: { fail-fast: false, matrix: { os: [ubuntu-latest, macos-latest, windows-latest] } }`, `runs-on: ${{ matrix.os }}`, `actions/checkout@v4`, then run `make test` on non-Windows. For Windows (no preinstalled GNU make, default MSVC), use the direct g++ fallback: `g++ -std=c++17 -O2 -g -Isrc -Itests -Wall -Wextra -ffp-contract=off tests/*.cpp -o test.exe && ./test.exe` (mirroring the Makefile TEST_CXXFLAGS so flags stay consistent). NO `RACK_DIR`, no SDK fetch, no xvfb/display — the core is plain C++17. No dependency caching needed. If the drift-on golden bit-exact assertion is restricted to a canonical OS, ensure the workflow/test logic reflects that split (the test handles the tolerance; the workflow just runs `make test`).
  </action>
  <verify>
    <automated>test -f .github/workflows/test.yml && grep -q 'ubuntu-latest' .github/workflows/test.yml && grep -q 'macos-latest' .github/workflows/test.yml && grep -q 'windows-latest' .github/workflows/test.yml && grep -q 'make test' .github/workflows/test.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` defines a 3-OS matrix (ubuntu/macos/windows-latest) triggered on push + pull_request.
    - Non-Windows legs run `make test`; the Windows leg uses the direct g++ fallback with flags matching the Makefile (incl. `-ffp-contract=off`).
    - The workflow references no `RACK_DIR`, no SDK fetch, no `xvfb`/display (`grep -c 'RACK_DIR\|xvfb' .github/workflows/test.yml` returns 0).
  </acceptance_criteria>
  <done>CI matrix runs make test on ubuntu/macos/windows on every push, Rack-free; ready to verify green on the private repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Test-harness code and a CI workflow over an already-extracted Rack-free core. No network at runtime, no auth, no untrusted runtime input. The CI workflow runs in GitHub Actions on the private repo.

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo build | CI executes `make test` / direct g++ on the committed sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-T | Tampering | CI workflow third-party action | mitigate | Only `actions/checkout@v4` (official GitHub action, version-pinned) is used; no third-party marketplace actions, no secrets, no `RACK_DIR`/SDK fetch. |
| T-22-04-I | Integrity | cross-platform float non-determinism producing false golden failures | mitigate | Documented bit-exact-canonical-OS / epsilon-elsewhere split (Pitfall 6 / Open Q1); `-ffp-contract=off`, no `-ffast-math`; same-platform determinism stays bit-exact everywhere. |
| T-22-04-SC | Tampering | supply chain | accept | No package-manager install; doctest already vendored+pinned (T-22-SC, Plan 01). |

No high-severity threats reach the ASVS L1 block threshold — test/CI tooling on a private repo with no runtime network/auth/untrusted-input surface and no secrets.
</threat_model>

<verification>
- `make test` green on the canonical machine: invariants at 44.1/48/96 kHz + golden replay + prior units all pass.
- `.github/workflows/test.yml` defines the 3-OS matrix running `make test` (windows via direct g++ fallback), Rack-free.
- BlockDriver injects `sampleTime` per sample and seeds deterministically; the determinism invariant is bit-exact same-platform.
- `make` still builds the plugin unchanged.
</verification>

<success_criteria>
- TEST-04 satisfied: the headless BlockDriver harness asserts ±5V bounds, frequency accuracy, phase continuity at reset, and fixed-seed determinism, passing at 44.1/48/96 kHz — the behavioral baseline for later refactors.
- D-08 golden regression is permanent and green on the canonical OS.
- D-09 CI matrix is wired and runs `make test` on every push across three platforms.
</success_criteria>

<output>
Create `.planning/phases/22-test-harness-foundation/22-04-SUMMARY.md` when done.
</output>

---
phase: 26-vcv-library-compliance-packaging
plan: 04
type: execute
wave: 2
depends_on: [26-01, 26-02, 26-03]
files_modified: []
autonomous: true
requirements: [PKG-05]
must_haves:
  truths:
    - "make dist produces a .vcvplugin artifact via the Rack SDK plugin.mk (no hand-rolled packaging)"
    - "The artifact is a zstd-compressed tar (inspected with tar --zstd, not unzip)"
    - "The artifact contains the built binary, plugin.json with populated URLs, res/, and LICENSE"
    - "The artifact contains NO trial/commercial fonts — res/fonts holds only the OFL font + OFL.txt"
  artifacts:
    - path: "dist/"
      provides: "make dist output containing the verified .vcvplugin (build artifact, gitignored)"
  key_links:
    - from: "plugin.json (populated URLs from 26-01)"
      to: "packaged plugin.json inside the .vcvplugin"
      via: "make dist copies plugin.json into dist/<slug>/"
      pattern: "sourceUrl"
---

<objective>
Produce and verify the `.vcvplugin` submission artifact (PKG-05). Run the SDK-provided `make dist`
(no custom packaging), then inspect the resulting zstd-tar to assert it contains the binary, the
populated `plugin.json`, `res/`, and `LICENSE` — and NO trial fonts.

Purpose: The final packaging gate. `.vcvplugin` is a zstd-compressed tar (NOT a zip), so verification
uses `tar --zstd -tvf`, not `unzip`. This runs after the manifest is populated (26-01) and the CI-fix
refactors have landed (26-02, 26-03) so the artifact reflects the final tree state.
Output: a verified dist artifact and a SUMMARY recording the asserted contents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vcv-library-compliance-packaging/26-CONTEXT.md
@.planning/phases/26-vcv-library-compliance-packaging/26-RESEARCH.md
@.planning/phases/26-vcv-library-compliance-packaging/26-PATTERNS.md

<interfaces>
<!-- Packaging facts (RESEARCH §Code Examples / Pitfall 4; ../Rack-SDK/plugin.mk:93-95) -->
make dist  → dist/<slug>/{plugin.<dylib|so|dll>, plugin.json, res/, LICENSE, NOTICES}
           → dist/<slug>-<version>-<arch>.vcvplugin   (zstd-tar; arch e.g. mac-arm64 on Apple silicon)
Inspect (NOT unzip):  tar --zstd -tvf dist/ForgeAudio-AnalogSeries-2.0.0-<arch>.vcvplugin
                 or:  zstd -dc dist/ForgeAudio-AnalogSeries-*.vcvplugin | tar -tvf -

Makefile DISTRIBUTABLES (Makefile:13-16): res, LICENSE*, NOTICES*, presets — LICENSE/NOTICES ship.
res/fonts must hold ONLY the OFL font (JetBrainsMonoNL-Regular.ttf) + OFL.txt — NO trial/commercial fonts
(Barell / FoundationLogo purged in Phase 25). Arch suffix is cosmetic (arch.mk computes ARCH_OS-ARCH_CPU).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the .vcvplugin via make dist</name>
  <files></files>
  <read_first>
    - Makefile (DISTRIBUTABLES L13-16; dist/install come from plugin.mk)
    - plugin.json (confirm populated URLs from 26-01 will be packaged)
    - .planning/phases/26-vcv-library-compliance-packaging/26-RESEARCH.md (§Code Examples PKG-05 build+verify; §Environment Availability — jq/zstd/tar/SDK present)
  </read_first>
  <action>
    Confirm the four dist tools are present (`jq --version`, `zstd --version`, `tar --version`,
    `command -v make`) and that ../Rack-SDK exists. Run `make dist`. This is SDK-provided packaging — do
    NOT author any custom zip/tar script (RESEARCH Don't-Hand-Roll). Capture the exact produced artifact
    filename `dist/ForgeAudio-AnalogSeries-2.0.0-<arch>.vcvplugin` (arch is machine-dependent, e.g.
    mac-arm64) and record it in the SUMMARY for Task 2 to inspect.
  </action>
  <verify>
    <automated>make dist && ls dist/ForgeAudio-AnalogSeries-2.0.0-*.vcvplugin</automated>
  </verify>
  <acceptance_criteria>
    - `make dist` exits 0 using the SDK plugin.mk (no custom packaging script authored)
    - Exactly one `dist/ForgeAudio-AnalogSeries-2.0.0-*.vcvplugin` artifact exists
    - The artifact filename (with resolved arch suffix) is recorded in the SUMMARY
  </acceptance_criteria>
  <done>A single versioned .vcvplugin artifact is produced by make dist.</done>
</task>

<task type="auto">
  <name>Task 2: Verify .vcvplugin layout and asset integrity (PKG-05)</name>
  <files></files>
  <read_first>
    - .planning/phases/26-vcv-library-compliance-packaging/26-RESEARCH.md (§Pitfall 4 zstd-tar-not-zip; §Code Examples PKG-05 assertions)
    - NOTICES (confirm the shipped-asset inventory the artifact must match)
    - plugin.json (the packaged manifest whose URLs must be populated)
  </read_first>
  <action>
    Inspect the artifact with `tar --zstd -tvf dist/ForgeAudio-AnalogSeries-2.0.0-*.vcvplugin` (NOT unzip —
    it is a zstd-compressed tar per Pitfall 4). Assert the listing contains, all under the
    `ForgeAudio-AnalogSeries/` root: the built binary (plugin.dylib/.so/.dll), plugin.json, the res/ tree,
    and LICENSE. Extract and confirm the packaged plugin.json has the three populated URLs (sourceUrl etc.).
    Assert NO trial/commercial font ships: the listing must contain no `Barell` or `FoundationLogo` entry,
    and res/fonts must hold only the OFL font (JetBrainsMonoNL-Regular.ttf) + OFL.txt. Record the full
    listing and each assertion result in the SUMMARY.
  </action>
  <verify>
    <automated>A=$(ls dist/ForgeAudio-AnalogSeries-2.0.0-*.vcvplugin | head -1); L=$(tar --zstd -tvf "$A"); echo "$L" | grep -q 'plugin\.\(dylib\|so\|dll\)' && echo "$L" | grep -q 'plugin.json' && echo "$L" | grep -q 'res/' && echo "$L" | grep -q 'LICENSE' && ! echo "$L" | grep -Eqi 'Barell|FoundationLogo'</automated>
  </verify>
  <acceptance_criteria>
    - `tar --zstd -tvf` lists the binary, plugin.json, res/, and LICENSE under the `ForgeAudio-AnalogSeries/` root
    - The packaged plugin.json contains the three populated GitHub URLs
    - The listing contains NO `Barell` or `FoundationLogo` entry; res/fonts holds only the OFL font + OFL.txt
    - All assertions and the full artifact listing are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The .vcvplugin is verified to contain the binary, manifest (populated URLs), res/, and LICENSE, with no trial fonts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → shipped .vcvplugin | make dist packages the working tree; anything in DISTRIBUTABLES ships to end users |
| shipped fonts/assets → public release | Asset licensing/provenance must be clean for public GPL distribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | .vcvplugin contents (secrets / trial fonts leaking into the artifact) | mitigate | Explicit assertion that the artifact contains no `Barell`/`FoundationLogo` font and res/fonts holds only the OFL font + OFL.txt; no secrets are in DISTRIBUTABLES |
| T-26-06 | Tampering | packaging format correctness | mitigate | Use SDK `make dist` (correct zstd-tar), never a hand-rolled zip; verify with `tar --zstd`, not `unzip`, so a mis-formatted artifact is caught |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; packaging tools (jq/zstd/tar/make) are OS/SDK-provided — no supply-chain surface, no slopcheck required |
</threat_model>

<verification>
- `make dist` produces exactly one `ForgeAudio-AnalogSeries-2.0.0-*.vcvplugin`
- `tar --zstd -tvf` lists binary + plugin.json (populated URLs) + res/ + LICENSE, and no trial fonts
</verification>

<success_criteria>
A verified `.vcvplugin` artifact exists containing the binary, populated manifest, res/, and LICENSE, with
no trial fonts — PKG-05 met. Combined with 26-01 (manifest), 26-02 + 26-03 (CI green), the phase is
submission-ready.
</success_criteria>

<output>
Create `.planning/phases/26-vcv-library-compliance-packaging/26-04-SUMMARY.md` when done.
</output>

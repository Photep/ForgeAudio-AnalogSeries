---
phase: 02-waveform-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AnalogLFO.cpp
  - res/AnalogLFO.svg
  - res/PANEL-SPEC.md
autonomous: true
requirements:
  - OUT-02
  - PTCH-01
  - WAVE-03

must_haves:
  truths:
    - "INV output jack is removed from the module -- only one output jack (OUT) exists"
    - "Rate knob tooltip shows linear Hz values from 0.01 to 20.0 Hz with default ~0.7 Hz"
    - "Morph CV attenuator Trimpot appears on the panel near the Morph CV jack"
    - "Module compiles and loads in VCV Rack without errors after all structural changes"
  artifacts:
    - path: "src/AnalogLFO.cpp"
      provides: "Updated enums, configParams, and widget layout"
      contains: "MORPH_ATTEN_PARAM"
    - path: "res/AnalogLFO.svg"
      provides: "Updated panel with attenuator component and INV label removed"
    - path: "res/PANEL-SPEC.md"
      provides: "Updated component table reflecting new layout"
  key_links:
    - from: "src/AnalogLFO.cpp"
      to: "res/AnalogLFO.svg"
      via: "mm2px coordinates match SVG component circles"
      pattern: "mm2px.*Vec"
    - from: "res/PANEL-SPEC.md"
      to: "src/AnalogLFO.cpp"
      via: "Component table positions match widget code"
---

<objective>
Restructure the module scaffold for Phase 2: remove the inverted output, add the Morph CV attenuator parameter, change the rate knob to linear Hz mapping, and redesign the bottom row panel layout for 3 jacks + 1 Trimpot.

Purpose: The Phase 1 scaffold declared 2 outputs and exponential rate mapping. Phase 2 user decisions require: single output only (INV removed), linear rate 0.01-20Hz, and a new Morph CV attenuator knob. These structural changes must land before the DSP engine (Plan 02) can reference the correct enums and parameters.

Output: Updated AnalogLFO.cpp with correct enums/configParams/widget, updated SVG panel, updated PANEL-SPEC.md -- all three synchronized. Module compiles and loads (with still-empty process()).
</objective>

<execution_context>
@/Users/mrcbrown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrcbrown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-waveform-engine/02-RESEARCH.md
@.planning/phases/02-waveform-engine/02-CONTEXT.md
@src/AnalogLFO.cpp
@res/AnalogLFO.svg
@res/PANEL-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure enums, configParams, and widget layout in AnalogLFO.cpp</name>
  <files>src/AnalogLFO.cpp</files>
  <action>
  Make these changes to src/AnalogLFO.cpp:

  **1. Add MORPH_ATTEN_PARAM to ParamId enum:**
  Add `MORPH_ATTEN_PARAM` after `OCTAVE_PARAM` and before `PARAMS_LEN`. This preserves existing param indices (MORPH=0, CHARACTER=1, DRIFT=2, RATE=3, OCTAVE=4, MORPH_ATTEN=5). The enum must be:
  ```cpp
  enum ParamId {
      MORPH_PARAM,
      CHARACTER_PARAM,
      DRIFT_PARAM,
      RATE_PARAM,
      OCTAVE_PARAM,
      MORPH_ATTEN_PARAM,
      PARAMS_LEN
  };
  ```

  **2. Remove INV_OUTPUT from OutputId enum:**
  Remove the `INV_OUTPUT` line. The enum becomes:
  ```cpp
  enum OutputId {
      OUTPUT,
      OUTPUTS_LEN
  };
  ```

  **3. Update configParam calls in the constructor:**
  - Change Rate from exponential to linear:
    OLD: `configParam(RATE_PARAM, -8.f, 4.f, -3.f, "Rate", " Hz", 2.f, 1.f);`
    NEW: `configParam(RATE_PARAM, 0.01f, 20.f, 0.7f, "Rate", " Hz");`
    This gives linear mapping where the raw value IS the frequency in Hz. Tooltip shows "Rate: 0.70 Hz".
  - Add the Morph CV attenuator:
    `configParam(MORPH_ATTEN_PARAM, 0.f, 1.f, 0.f, "Morph CV", "%", 0.f, 100.f);`
    Default 0 means CV has no effect until user turns it up. Tooltip shows "Morph CV: 0%".
  - Remove the `configOutput(INV_OUTPUT, ...)` line.

  **4. Update the widget constructor:**
  - Add the Trimpot for MORPH_ATTEN_PARAM. Place it beside the Morph CV jack. With INV output removed, redesign the bottom row (y=104.0) to have 4 elements across the panel width:
    - Morph CV attenuator (Trimpot): x=9.0, y=104.0
    - Morph CV jack (PJ301MPort input): x=21.0, y=104.0
    - Drift CV jack (PJ301MPort input): x=35.0, y=104.0
    - Output jack (PJ301MPort output): x=51.0, y=104.0

    These positions give ~12mm spacing between the Trimpot and MCV jack (visually grouped as attenuator+input pair), then ~14mm to DCV, then ~16mm to OUT. The Trimpot (6.05mm dia) and jacks (8.03mm dia) fit comfortably.

    Add this line in the Params section:
    `addParam(createParamCentered<Trimpot>(mm2px(Vec(9.0, 104.0)), module, AnalogLFO::MORPH_ATTEN_PARAM));`

  - Update existing jack positions to the new X coordinates:
    - Morph CV input: change from Vec(11.0, 104.0) to Vec(21.0, 104.0)
    - Drift CV input: change from Vec(24.5, 104.0) to Vec(35.0, 104.0)
    - Output: change from Vec(38.0, 104.0) to Vec(51.0, 104.0)

  - Remove the `addOutput` line for INV_OUTPUT entirely.

  **5. Add cmath include at the top** (needed for Phase 2 DSP, add now to avoid a second touch):
  Add `#include <cmath>` after `#include "plugin.hpp"`.

  **6. Add double phase member variable** (needed for Phase 2 DSP, add now):
  Add `double phase = 0.0;` as a member variable in the AnalogLFO struct, after the enum declarations and before the constructor.
  </action>
  <verify>
  Run `cd "/Users/mrcbrown/Claude/Software/Forge Audio/Analog Series" && make` -- must compile without errors or warnings.
  Verify in the source: grep for INV_OUTPUT (must not exist), grep for MORPH_ATTEN_PARAM (must exist), grep for "0.01f, 20.f, 0.7f" (rate param must be linear).
  </verify>
  <done>
  AnalogLFO.cpp has: MORPH_ATTEN_PARAM in enum, no INV_OUTPUT anywhere, linear rate configParam (0.01-20Hz, default 0.7), Trimpot widget at (9.0, 104.0), updated jack positions, cmath include, double phase member. Compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SVG panel and PANEL-SPEC.md to match new layout</name>
  <files>res/AnalogLFO.svg, res/PANEL-SPEC.md</files>
  <action>
  **SVG changes (res/AnalogLFO.svg):**

  1. **Remove the "INV" label** -- delete the entire `<g transform="translate(50.352, 96.2) scale(0.28)">` group that renders the INV label (the group containing I, N, V letter paths near x=51.5, y=97).

  2. **Update jack label positions to match new jack X coordinates:**
     - "MCV" label: change from centered at x=11.0 to centered at x=21.0. Recalculate startX: the label is 3.164mm wide (11.3 units * 0.28 scale). New startX = 21.0 - 3.164/2 = 19.418. Update the translate to `translate(19.418, 96.2)`.
     - "DCV" label: change from centered at x=24.5 to centered at x=35.0. The label is 2.744mm wide (9.8 * 0.28). New startX = 35.0 - 2.744/2 = 33.628. Update the translate to `translate(33.628, 96.2)`.
     - "OUT" label: change from centered at x=38.0 to centered at x=51.0. The label is 2.744mm wide. New startX = 51.0 - 2.744/2 = 49.628. Update the translate to `translate(49.628, 96.2)`.

  3. **Add an attenuator label near the Trimpot.** This is optional per VCV convention (Trimpots near jacks are often self-documenting), but for clarity with the changed layout, add a small label. Use a tiny dot or marker -- actually, per research recommendation, do NOT add a separate label for the Trimpot. The visual grouping of Trimpot+Jack is self-documenting. Keep it clean.

  4. **Update the components layer** (`<g id="components" style="display:none">`):
     - Add a red circle for the Morph CV attenuator Trimpot: `<circle cx="9.0" cy="104.0" r="3.03" fill="#ff0000"/>` (Trimpot radius = 6.05/2 = 3.025, round to 3.03)
     - Update Morph CV input circle: change cx from 11.0 to 21.0
     - Update Drift CV input circle: change cx from 24.5 to 35.0
     - Update Output circle: change cx from 38.0 to 51.0
     - Remove the Inv Out blue circle entirely (cx=51.5)

  **PANEL-SPEC.md changes (res/PANEL-SPEC.md):**

  1. **Update Section 4 component table** to reflect the new layout:
     - Add row: Morph CV Atten | Trimpot | 9.0 | 104.0 | 6.05 | Param
     - Update Morph CV row: Center X from 11.0 to 21.0
     - Update Drift CV row: Center X from 24.5 to 35.0
     - Update Output row: Center X from 38.0 to 51.0
     - Remove the Inv. Output row entirely

  2. **Update Section 3 layout zones:**
     - Jack labels row: change "MCV, DCV, OUT, INV labels" to "MCV, DCV, OUT labels"
     - Jack row: change "All 4 jacks in a single horizontal row" to "Morph CV attenuator (Trimpot) + 3 jacks in bottom row"

  3. **Update jack spacing note** at the bottom of Section 4 to reflect the new arrangement.

  4. **Update components layer example** in Section 5 to include the Trimpot circle and updated positions.
  </action>
  <verify>
  Open the SVG in a browser to verify no rendering errors.
  Cross-check: every mm2px coordinate in AnalogLFO.cpp must have a matching circle in the SVG components layer, and a matching row in PANEL-SPEC.md Section 4. Count: 6 params + 2 inputs + 1 output = 9 components total.
  Run `make && make install` to verify the module still loads with the updated SVG.
  </verify>
  <done>
  SVG panel has: no INV label, updated MCV/DCV/OUT label positions, Trimpot component circle at (9.0, 104.0), updated jack component circles, no Inv Out circle. PANEL-SPEC.md matches. All three sources of truth (C++, SVG, spec) are synchronized. Module compiles and loads.
  </done>
</task>

</tasks>

<verification>
1. `make` compiles without errors
2. `make install` succeeds
3. Module loads in VCV Rack (visual check -- only one output jack visible, Trimpot visible near MCV jack)
4. grep -c "INV_OUTPUT" src/AnalogLFO.cpp returns 0
5. grep "MORPH_ATTEN_PARAM" src/AnalogLFO.cpp returns the enum entry and configParam line
6. All mm2px coordinates in C++ match component circles in SVG and rows in PANEL-SPEC.md
</verification>

<success_criteria>
- Module compiles and loads with the restructured scaffold
- Single output jack (no INV), Trimpot for morph CV attenuator visible on panel
- Rate knob shows linear Hz values (0.01-20 range, default 0.7)
- All three sources of truth synchronized: C++ widget, SVG components layer, PANEL-SPEC.md table
</success_criteria>

<output>
After completion, create `.planning/phases/02-waveform-engine/02-01-SUMMARY.md`
</output>

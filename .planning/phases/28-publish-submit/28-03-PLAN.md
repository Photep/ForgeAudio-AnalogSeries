---
phase: 28-publish-submit
plan: 03
type: execute
wave: 2
depends_on: [28-02]
files_modified: [.planning/STATE.md]
autonomous: false
requirements: [PUB-02]
user_setup: []

must_haves:
  truths:
    - "Exactly ONE submission issue exists on VCVRack/library titled with the plugin slug ForgeAudio-AnalogSeries"
    - "The issue body carries plugin name, license GPL-3.0-or-later, sourceUrl, manualUrl, version 2.0.0, and the full 40-char commit hash (not a branch name)"
    - "The full 40-char commit hash in the issue matches the tagged v2.0.0 release commit"
    - "The submission issue URL is recorded to STATE.md for all future update comments"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Recorded VCV Library submission issue URL (permanent update channel)"
      contains: "VCVRack/library/issues"
  key_links:
    - from: "submission issue body commit hash"
      to: "tagged v2.0.0 release commit"
      via: "git rev-parse v2.0.0^{commit} equality"
      pattern: "rev-parse v2.0.0\\^\\{commit\\}"
    - from: "issue title"
      to: "plugin slug"
      via: "title == ForgeAudio-AnalogSeries (not display name, not module slug)"
      pattern: "ForgeAudio-AnalogSeries"
---

<objective>
Submit the plugin to the VCV Library by opening EXACTLY ONE issue on
`github.com/VCVRack/library`, titled with the plugin slug `ForgeAudio-AnalogSeries`,
carrying the plugin name, license, sourceUrl, manualUrl, version 2.0.0, and the exact
full 40-char commit hash. Record the resulting issue URL — it is the permanent channel
for all future version updates.

Purpose: PUB-02. VCV's `rack-plugin-toolchain` clones the public sourceUrl at the
submitted commit and builds every platform itself, so the submission MUST pin an
immutable commit hash (a branch/tag name is explicitly forbidden by the README) and
the title MUST be the plugin slug so their tooling keys the thread correctly.

Output: one open issue on VCVRack/library; its URL recorded in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-publish-submit/28-RESEARCH.md
@plugin.json

<facts>
- Title MUST be the plugin slug `ForgeAudio-AnalogSeries` — NOT the display name
  "Forge Audio - Analog Series", NOT the module slug `ForgeAnalogLFO` (RESEARCH Pitfall 6).
- Body fields (from plugin.json, confirmed at plan time):
  name "Forge Audio - Analog Series", license GPL-3.0-or-later, version 2.0.0,
  sourceUrl https://github.com/Photep/ForgeAudio-AnalogSeries,
  manualUrl https://github.com/Photep/ForgeAudio-AnalogSeries/blob/main/docs/index.md.
- Commit hash MUST be the full 40-char `git rev-parse v2.0.0^{commit}` — never `main`,
  `HEAD`, or `v2.0.0` (RESEARCH Pitfall 2).
- EXACTLY ONE issue — it is the permanent thread; all future version bumps are comments
  on it, never a second issue (RESEARCH Pitfall 3).
- `gh` is authed as `Photep`. Email in the body is OPTIONAL (RESEARCH Open Question 2) —
  default to omitting it unless the operator asks to include it.
- No GitHub Release and no .vcvplugin attachment — VCV rebuilds from source.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm slug/manifest and draft the submission body</name>
  <files>.planning/STATE.md</files>
  <read_first>plugin.json; .planning/phases/28-publish-submit/28-RESEARCH.md (VCV Library Submission Mechanism); .planning/STATE.md (recorded v2.0.0 hash)</read_first>
  <action>
    Do a final read of plugin.json (Pitfall 7 — confirm, do NOT edit) and assert
    slug == ForgeAudio-AnalogSeries, module slug == ForgeAnalogLFO, version == 2.0.0,
    license == GPL-3.0-or-later, sourceUrl/manualUrl as listed. Re-capture the exact
    hash `COMMIT=$(git rev-parse v2.0.0^{commit})` and confirm it equals the value
    recorded by plan 28-01. Assert NO existing submission issue for this slug so a
    duplicate is not opened: `gh issue list --repo VCVRack/library --search "ForgeAudio-AnalogSeries in:title"`.
    Draft the issue body containing: plugin name, slug, license, version 2.0.0, sourceUrl,
    manualUrl, and the full 40-char commit hash. Present the drafted title + body for the
    Task 2 gate. Omit email unless the operator opts in.
  </action>
  <acceptance_criteria>
    `jq -r .slug plugin.json` == `ForgeAudio-AnalogSeries`;
    `jq -r .version plugin.json` == `2.0.0`;
    `jq -r .license plugin.json` == `GPL-3.0-or-later`;
    `git rev-parse v2.0.0^{commit}` == the hash recorded in STATE.md (40 chars);
    `gh issue list --repo VCVRack/library --search "ForgeAudio-AnalogSeries in:title"` returns ZERO existing issues (no duplicate).
  </acceptance_criteria>
  <verify>
    <automated>test "$(jq -r .slug plugin.json)" = "ForgeAudio-AnalogSeries" && test "$(jq -r .version plugin.json)" = "2.0.0" && echo MANIFEST-OK</automated>
  </verify>
  <done>Manifest values confirmed unchanged, the 40-char hash matches the tag, no duplicate issue exists, and the body is drafted with the hash embedded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: File the ONE VCV Library submission issue</name>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (Exact command — HUMAN-GATED)</read_first>
  <what-built>Task 1 confirmed the slug/version/license/URLs from plugin.json, re-captured the 40-char v2.0.0 commit hash, verified no duplicate issue exists, and drafted the issue body. The filing command is `gh issue create --repo VCVRack/library --title "ForgeAudio-AnalogSeries" --body "<drafted body containing the full 40-char hash>"`.</what-built>
  <action>After the operator approves the drafted title and body, run `gh issue create --repo VCVRack/library --title "ForgeAudio-AnalogSeries" --body "<drafted body with the full 40-char hash>"` and capture the printed issue URL. File exactly one issue.</action>
  <how-to-verify>
    1. Review the drafted TITLE — it MUST be exactly `ForgeAudio-AnalogSeries` (the slug), not the display name and not the module slug.
    2. Review the BODY — confirm it contains the plugin name, license GPL-3.0-or-later, version 2.0.0, sourceUrl, manualUrl, and the FULL 40-char commit hash (not `main`/`v2.0.0`/`HEAD`).
    3. Confirm this is the ONLY submission issue — a second issue is never opened; future updates are comments here.
    4. On go, run the `gh issue create` command; capture the printed issue URL.
  </how-to-verify>
  <acceptance_criteria>
    `gh issue create` prints an issue URL under `github.com/VCVRack/library/issues/`;
    `gh issue view <url> --repo VCVRack/library --json title --jq .title` == `ForgeAudio-AnalogSeries`;
    `gh issue view <url> --repo VCVRack/library --json body --jq .body` contains the full 40-char hash and does NOT contain a bare branch name where the hash belongs.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the created issue URL, or describe corrections needed before filing</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the submission issue URL to STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md</read_first>
  <action>
    Write the created issue URL and the submitted 40-char commit hash into STATE.md
    (Accumulated Context) as the permanent VCV Library submission record. Note that ALL
    future version updates are comments on THIS issue — never a new issue. This satisfies
    ROADMAP success criterion 4 (issue URL recorded for future update comments).
  </action>
  <acceptance_criteria>
    `grep -F "VCVRack/library/issues" .planning/STATE.md` matches the recorded issue URL;
    STATE.md also contains the submitted 40-char commit hash alongside the URL.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'VCVRack/library/issues' .planning/STATE.md && echo URL-RECORDED</automated>
  </verify>
  <done>STATE.md records the submission issue URL and submitted commit hash as the permanent update channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submitter → VCVRack/library (external repo) | The one issue is the permanent, public comms channel with the Library team |
| declared build ref → actual source | VCV builds from whatever ref we submit; a mutable ref would break reproducibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Tampering | build source ref in the issue body | mitigate | Submit the full 40-char `git rev-parse v2.0.0^{commit}`; Task 1 asserts it matches the recorded tag hash and Task 2 gate confirms no bare branch name is present |
| T-28-09 | Repudiation | duplicate/fragmented submission threads | mitigate | Task 1 asserts zero existing slug-titled issues before filing; exactly one issue is opened and its URL recorded (Task 3) as the sole future channel |
| T-28-10 | Spoofing | wrong title (display name / module slug) mis-keys VCV tooling | mitigate | Task 2 gate verifies title == plugin slug `ForgeAudio-AnalogSeries` exactly |
</threat_model>

<verification>
- Exactly one issue titled `ForgeAudio-AnalogSeries` on VCVRack/library.
- Issue body contains the full 40-char commit hash equal to `git rev-parse v2.0.0^{commit}`.
- STATE.md records the issue URL and the submitted hash.
</verification>

<success_criteria>
One VCV Library submission issue is open, slug-titled, carrying name/license/sourceUrl/
manualUrl/version 2.0.0/full-40-char-hash; its URL is recorded in STATE.md as the
permanent update channel.
</success_criteria>

<output>
Create `.planning/phases/28-publish-submit/28-03-SUMMARY.md` when done — include the
submission issue URL and the submitted 40-char commit hash.
</output>

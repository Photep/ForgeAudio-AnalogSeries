---
phase: 28-publish-submit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [.planning/STATE.md]
autonomous: false
requirements: [PUB-01, PUB-02]
user_setup: []

must_haves:
  truths:
    - "The v2.0.0 release commit is on main so manualUrl /blob/main/ paths resolve"
    - "An annotated tag v2.0.0 exists on the remote pointing at the release commit"
    - "The full 40-char release commit hash is captured and recorded to STATE.md"
    - "docs/index.md and docs/changelog.md are present on main (manualUrl + changelogUrl targets)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Recorded v2.0.0 commit hash for the submission issue"
      contains: "v2.0.0"
  key_links:
    - from: "git tag v2.0.0"
      to: "release commit on main"
      via: "git rev-parse v2.0.0^{commit}"
      pattern: "rev-parse v2.0.0\\^\\{commit\\}"
    - from: "release commit"
      to: "main branch"
      via: "merge-base --is-ancestor"
      pattern: "merge-base --is-ancestor"
---

<objective>
Finalize the release tree WHILE THE REPO IS STILL PRIVATE: merge the release
work onto `main`, create the annotated `v2.0.0` tag on that commit, push both to
the private remote, and capture the exact 40-char commit hash. No public exposure
happens in this plan — this is the pre-flip preparation that a later wave verifies
and exposes.

Purpose: The submitted commit MUST be on `main` (so `manualUrl` = `/blob/main/docs/index.md`
resolves) and MUST be tagged/pushed and reachable before the flip. This closes the
"release commit is on a feature branch" pitfall (RESEARCH Pitfall 1) and produces
the immutable hash PUB-02 requires.

Output: `main` fast-forwarded to include the release commits, tag `v2.0.0` on the
remote, the full commit hash recorded in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-publish-submit/28-RESEARCH.md

<facts>
Verified at plan time (2026-07-10):
- Current branch: chore/finalise-18hp-docs-and-cleanup ; main is at 4b27436.
- `git merge-base --is-ancestor main HEAD` exits 0 → a fast-forward merge of the
  feature branch INTO main is possible (no divergence, no merge commit needed).
- Existing tags: v1.0 v1.1 v1.2 v1.3 — no v2.0.0 yet.
- docs/index.md and docs/changelog.md exist on the feature-branch HEAD.
- plugin.json version is 2.0.0 → tag MUST be v2.0.0 (NOT v1.4; the milestone label
  is internal, VCV requires MAJOR = Rack major = 2).
- Working tree is clean.
</facts>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Choose merge-to-main strategy</name>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (Pitfall 1, Open Question 1)</read_first>
  <decision>How to bring the release commits onto `main` before tagging v2.0.0</decision>
  <context>
    The release + docs work sits on `chore/finalise-18hp-docs-and-cleanup`, not on
    `main`. A verified check (`git merge-base --is-ancestor main HEAD` exits 0) shows
    `main` is an ancestor of the feature branch, so a clean fast-forward is available.
    The chosen commit becomes the tagged, submitted, publicly-browsed release.
  </context>
  <action>Present the two merge strategies to the operator and record the selection; take no git action until a strategy is chosen.</action>
  <options>
    <option id="ff-only">
      <name>Fast-forward main (recommended)</name>
      <pros>Linear history, no synthetic merge commit; verified possible right now; the tag lands on the exact reviewed release commit.</pros>
      <cons>None material — history is already linear.</cons>
    </option>
    <option id="merge-commit">
      <name>Explicit merge commit into main</name>
      <pros>Records an explicit "release merge" node.</pros>
      <cons>Adds a non-content commit; the tagged commit is the merge, not the reviewed release commit; unnecessary given a clean FF is available.</cons>
    </option>
  </options>
  <acceptance_criteria>Operator selects `ff-only` or `merge-commit`; selection recorded in the plan summary before Task 2 runs.</acceptance_criteria>
  <resume-signal>Select: ff-only or merge-commit</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Merge release onto main and push (still PRIVATE)</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (Pitfall 1, Release Tagging)</read_first>
  <action>
    Per the Task 1 decision, bring the feature branch onto `main`: for ff-only,
    `git checkout main` then `git merge --ff-only <feature-branch>`; for merge-commit,
    a standard `git merge --no-ff`. Push `main` to origin. Do NOT flip visibility —
    the repo stays PRIVATE. Confirm the docs the manifest links to are present on
    `main`: `git ls-tree --name-only main docs/index.md docs/changelog.md`. Confirm the
    release commit is now reachable from `main` via `git merge-base --is-ancestor`.
    Record the resolved main HEAD hash in STATE.md as the release-commit candidate.
  </action>
  <acceptance_criteria>
    `git merge-base --is-ancestor $(git rev-parse main) main` exits 0;
    `git ls-tree --name-only main docs/index.md docs/changelog.md` lists BOTH files;
    `git ls-remote origin refs/heads/main` shows the local main hash (push landed);
    repo still PRIVATE (`gh repo view --json visibility --jq .visibility` == `PRIVATE`).
  </acceptance_criteria>
  <verify>
    <automated>git ls-tree --name-only main docs/index.md docs/changelog.md | grep -c 'docs/' # expect 2</automated>
  </verify>
  <done>main contains the release + docs commits and is pushed to the private remote; visibility unchanged (PRIVATE).</done>
</task>

<task type="auto">
  <name>Task 3: Tag v2.0.0 on main, push tag, capture the exact hash</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/phases/28-publish-submit/28-RESEARCH.md (Release Tagging); plugin.json</read_first>
  <action>
    On the pushed `main` commit, create an ANNOTATED tag exactly named `v2.0.0`
    (`git tag -a v2.0.0 -m "Forge Audio Analog Series v2.0.0 — first VCV Library release"`),
    then `git push origin v2.0.0`. Dereference the tag to its commit and capture the
    full 40-char hash: `git rev-parse v2.0.0^{commit}` (an annotated tag's own SHA is
    the tag object, not the commit — always dereference). Do NOT create a GitHub Release
    and do NOT attach any .vcvplugin (VCV rebuilds all platforms from source). Record the
    captured 40-char hash in STATE.md — this is the exact value the submission issue will carry.
  </action>
  <acceptance_criteria>
    `git rev-parse v2.0.0^{commit}` resolves to a 40-char hash;
    `git ls-remote --tags origin v2.0.0` shows the tag on the remote;
    `git merge-base --is-ancestor $(git rev-parse v2.0.0^{commit}) main` exits 0 (tagged commit is on main);
    STATE.md contains the full 40-char hash string.
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse v2.0.0^{commit} | grep -Eq '^[0-9a-f]{40}$' && echo TAG-OK</automated>
  </verify>
  <done>Tag v2.0.0 exists on the remote pointing at the on-main release commit; the full 40-char hash is recorded in STATE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → GitHub remote (still PRIVATE) | Refs/tags pushed here; not yet exposed publicly |
| tag ref → commit identity | Downstream submission pins on the commit, not the mutable tag/branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | submitted release identity | mitigate | Capture and record the dereferenced 40-char hash via `git rev-parse v2.0.0^{commit}`; never propagate a branch/tag name as the build ref (enforced in plan 28-03) |
| T-28-02 | Information Disclosure | premature exposure | accept-by-order | This plan performs NO visibility change; all pushes target the still-PRIVATE remote. The public flip is gated in plan 28-02 behind the purge re-verify |
| T-28-03 | Repudiation | wrong tagged commit (feature branch, missing docs) | mitigate | Assert `merge-base --is-ancestor` (commit on main) and `ls-tree` docs presence before/after tagging, so manualUrl `/blob/main/` cannot 404 |
</threat_model>

<verification>
- `gh repo view --json visibility --jq .visibility` == `PRIVATE` (no exposure this plan).
- `git rev-parse v2.0.0^{commit}` returns a 40-char hash recorded in STATE.md.
- `git ls-tree --name-only main docs/index.md docs/changelog.md` lists both files.
</verification>

<success_criteria>
main includes the release + docs commits and is pushed; tag v2.0.0 exists on the
remote on that commit; the full 40-char hash is recorded in STATE.md; the repo is
still PRIVATE.
</success_criteria>

<output>
Create `.planning/phases/28-publish-submit/28-01-SUMMARY.md` when done — include the
selected merge strategy and the captured 40-char v2.0.0 commit hash.
</output>

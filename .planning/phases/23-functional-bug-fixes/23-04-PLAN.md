---
phase: 23-functional-bug-fixes
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [BUG-02]
must_haves:
  truths:
    - "The CURRENT (unfixed) plugin is built, installed, and the stale install is flushed so Rack loads the fresh dylib"
    - "x1.5 and /1.5 are auditioned in Rack against a clock — current every-beat retrig vs the proposed table cadence"
    - "A dated keep-current-vs-adopt-table decision is appended to STATE.md ### Decisions BEFORE any Wave C code"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "The logged audition decision (project milestone log)"
      contains: "[Phase 23]: x1.5"
  key_links:
    - from: ".planning/STATE.md ### Decisions"
      to: "plan 23-05 (alignment change + regression EXPECTED[15])"
      via: "the logged keep-current | adopt-table decision parameterizes the Wave C work"
      pattern: "DECISION: (keep-current|adopt-table)"
---

<objective>
THE PHASE'S DEFINING GATE (SC3, research Pitfall 7). BUG-02 changes user-audible behavior:
the x1.5 ratio currently retriggers on every clock beat (chopping half an LFO cycle), and /1.5
resets every 2 beats (truncating a third of a cycle). The mathematically-correct fix is a
per-ratio `BEATS_PER_ALIGN` table (/1.5 → every 3 beats, x1.5 → every 2). BUT the every-beat
retrig may be a DESIRABLE groove — only a human listening test can decide. An autonomous agent
must NOT self-decide x1.5 desirability.

This plan is a BLOCKING human-verify checkpoint with NO code change to the DSP. The operator
builds + installs the CURRENT (unfixed) plugin, flushes the stale install, auditions x1.5 and
/1.5 against a clock, and LOGS the keep-current-vs-adopt-table decision to STATE.md `### Decisions`.
Nothing in plan 23-05 (the alignment change + its regression) may land until that decision line
exists.

Note on frontmatter: `depends_on` is INTENTIONALLY empty. This is the Wave-B gate — it depends on
nothing, and plan 23-05 (Wave C) depends on IT. (The structural validator's "Wave > 1 but
depends_on empty" warning is expected and correct for an audition gate.)

Purpose: SC3 — record the audition decision in the milestone log BEFORE the alignment change.
Output: a dated `- [Phase 23]: x1.5/÷1.5 audition — DECISION: ...` entry in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-functional-bug-fixes/23-RESEARCH.md
@.planning/phases/23-functional-bug-fixes/23-PATTERNS.md
@CODE-REVIEW-FINDINGS.md

<interfaces>
<!-- Build/install/flush facts (memory: vcv_build_install_workflow.md). Exact commands the operator runs. -->

Build (from repo root):   make RACK_DIR=../Rack-SDK
Install:                  make install RACK_DIR=../Rack-SDK
SDK path is RELATIVE (../Rack-SDK) — run on the main tree, NOT a worktree (worktree breaks ../Rack-SDK resolution).

Stale-install flush (false-negative trap — Rack loads the EXTRACTED dir, not the archive):
  installed dir: ~/Library/Application Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/
  1. shasum plugin.dylib  (freshly built, repo root)
  2. shasum "<installed dir>/plugin.dylib"
  3. if they DIFFER: cp the fresh plugin.dylib over the installed one AND
     rsync -a dist/ForgeAudio-AnalogSeries/res/ "<installed dir>/res/"
  4. fully QUIT and RELAUNCH Rack (it caches SVGs/dylib at load)
  (A separate stale ForgeAudio/ dir, slug ForgeAudio/module ForgeAudioLFO, is an unrelated
   plugin — does NOT shadow ForgeAudio-AnalogSeries/ForgeAnalogLFO.)

Current behavior to audition (from RatioTable.hpp shouldReset, UNFIXED):
  x1.5 (idx 8): resets EVERY beat (chops 0.5 cycle/beat).   Proposed table: every 2 beats.
  /1.5 (idx 6): resets every 2 beats (truncates 1/3 cycle). Proposed table: every 3 beats.

STATE.md ### Decisions logging convention (existing entries use this exact shape):
  - [Phase N]: <decision text>
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Build + install CURRENT plugin, flush stale install, audition x1.5 / ÷1.5</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/phases/23-functional-bug-fixes/23-RESEARCH.md (Pitfall 7 L240-250 — the BLOCKING audition-before-code gate; Runtime State Inventory L189 stale-install trap)
    - CODE-REVIEW-FINDINGS.md (#2 L38-64 — the x1.5/÷1.5 truncation description + "Before fixing: Audition x1.5 in Rack to confirm the every-beat retrig isn't a desirable groove")
    - .planning/STATE.md (### Decisions section L53-66 — the existing `- [Phase N]:` logging convention this entry must match)
  </read_first>
  <what-built>
    No code change. The current (unfixed) plugin is built and installed for an in-Rack listening
    audition of the x1.5 and ÷1.5 clock-sync alignment behavior.
  </what-built>
  <action>
    HUMAN-IN-THE-LOOP audition (no source edit). The operator: builds + installs the CURRENT
    (unfixed) plugin, flushes the stale install via the hash-compare/relaunch procedure, auditions
    x1.5 and ÷1.5 against a clock, decides keep-current vs adopt-table, and appends the decision to
    `.planning/STATE.md` `### Decisions`. The concrete numbered steps are in <how-to-verify>. The
    ONLY file changed by this plan is `.planning/STATE.md`.
  </action>
  <how-to-verify>
    1. Build the CURRENT plugin: from repo root run `make RACK_DIR=../Rack-SDK`.
    2. Install: `make install RACK_DIR=../Rack-SDK`.
    3. Flush the stale install (CRITICAL — Rack loads the extracted dir, not the archive):
       - `shasum plugin.dylib` (repo root) vs
         `shasum "$HOME/Library/Application Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/plugin.dylib"`
       - if they differ, `cp` the fresh dylib over the installed one and
         `rsync -a dist/ForgeAudio-AnalogSeries/res/ "$HOME/Library/Application Support/Rack2/plugins-mac-arm64/ForgeAudio-AnalogSeries/res/"`
       - fully QUIT and RELAUNCH Rack.
    4. Patch a steady clock into the Analog LFO's CLK input. Set the ratio to x1.5; listen/watch
       the phase dot and output — note the every-beat retrig (half-cycle chop). Then set ÷1.5 and
       note the every-2-beats reset (third-of-a-cycle truncation).
    5. Compare against the PROPOSED table cadence (x1.5 → reset every 2 beats = clean 3 full
       cycles per 2 beats; ÷1.5 → reset every 3 beats = clean 2 full cycles).
    6. DECIDE: keep-current (the every-beat groove is desirable) OR adopt-table (clean alignment).
    7. Append the decision to `.planning/STATE.md` under `### Decisions` as a dated entry exactly
       in the existing convention, e.g.:
       `- [Phase 23]: x1.5/÷1.5 audition — DECISION: adopt-table — rationale: <one line>`
       (or `DECISION: keep-current`). This entry is the gate; plan 23-05 reads it.
  </how-to-verify>
  <verify>
    <automated>grep -nE "\[Phase 23\]: x1.5.*DECISION: (keep-current|adopt-table)" .planning/STATE.md && echo "decision logged" || echo "GATE NOT MET: audition decision missing"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains a line matching `\[Phase 23\]: x1.5` AND
      `DECISION: (keep-current|adopt-table)` under the `### Decisions` section.
    - The decision was recorded AFTER the in-Rack audition of the CURRENT build (operator confirms
      the stale-install hashes matched before listening).
    - No DSP/source file was modified in this plan (the only changed file is STATE.md).
  </acceptance_criteria>
  <done>A dated `- [Phase 23]: x1.5/÷1.5 audition — DECISION: {keep-current|adopt-table}` line exists under STATE.md `### Decisions`; the audition ran against the freshly-flushed CURRENT build; no source/test file changed.</done>
  <resume-signal>Type "logged: adopt-table" or "logged: keep-current" (matching the STATE.md entry), or describe an audition issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Audition gate — listening test + a documentation edit to STATE.md. No code, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-NA | — | STATE.md decision log | accept | No new threats (human audition + doc edit only). The audition uses the CURRENT shipped code path unchanged. |
</threat_model>

<verification>
- `grep -E "\[Phase 23\]: x1.5.*DECISION: (keep-current|adopt-table)" .planning/STATE.md` returns the logged entry.
- No source/test file changed (only `.planning/STATE.md`).
- The hash-flush step was performed (operator confirms fresh dylib loaded) before the audition.
</verification>

<success_criteria>
SC3 satisfied: an in-Rack listening session for x1.5/÷1.5 is completed against the CURRENT build
and the keep-current-vs-adopt-table decision is recorded in the milestone log (STATE.md
`### Decisions`) BEFORE the alignment change and its regression test land. This decision GATES
plan 23-05.
</success_criteria>

<output>
Create `.planning/phases/23-functional-bug-fixes/23-04-SUMMARY.md` when done. Quote the exact
STATE.md decision line and confirm the stale-install hashes matched before the audition. Echo the
decision into the SUMMARY for traceability (research Open Q2 recommendation).
</output>

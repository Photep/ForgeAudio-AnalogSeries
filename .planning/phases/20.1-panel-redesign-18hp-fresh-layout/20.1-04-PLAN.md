---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 04
type: execute
wave: 2
depends_on: ["20.1-03"]
files_modified:
  - src/AnalogLFO.cpp
autonomous: true
requirements: [PANEL-11]
must_haves:
  truths:
    - "The WaveformDisplay fills the larger 81.44x26.00mm box as a deliberate refit (not a stretched artifact) with no clipping or overflow"
    - "All Phase 20 display content (ember trace, dot, pills, scanlines, breathing glow, corner brackets, SYNC badge, BPM stack) is preserved with no regression"
  artifacts:
    - path: "src/AnalogLFO.cpp"
      provides: "WaveformDisplay with px constants re-tuned for the 81.44x26.00mm box"
      contains: "float margin ="
  key_links:
    - from: "WaveformDisplay drawLayer px constants"
      to: "the new box.size (~308x98 px after Plan 03's box.size change)"
      via: "re-tuned margins/font-sizes/insets that read proportional in the larger glass"
      pattern: "box.size"
---

<objective>
Re-tune the handful of hardcoded px constants inside `WaveformDisplay` (`src/AnalogLFO.cpp` ~lines 846-1445) so the +27% wider / +45% taller box (set in Plan 03) reads as a deliberate, polished refit rather than a stretched artifact, with zero regression to Phase 20 display content (PANEL-11, success criteria 4 + 6). Everything proportional to `box.size` already re-flows for free; only the absolute-px margins, corner-bracket inset/size, pill/BPM font sizes, and top/bottom baselines need bumping (D-06, Claude's discretion).

Purpose: After Plan 03 enlarges the box, the proportional center-band math, pill columns (0.11/0.89), phase dot, BPM stack, brackets, and scanlines all re-flow automatically. But constants kept at their 14HP absolute px size (margin=6, brackets 3/5, pill fonts 4.1/3.5/3.2/2.9, BPM 3.5/2.9) would leave content sitting in a ~30px dead band at the bottom and look tiny — the "stretched artifact" the user explicitly warned against. This task bumps only those.
Output: WaveformDisplay px constants re-tuned for the new box; project compiles; Phase 20 content preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-01-PLAN.md

<interfaces>
<!-- Re-tune ONLY the hardcoded px constants below. Everything else in WaveformDisplay is -->
<!-- proportional to box.size and re-flows for free — DO NOT touch it. box.size is PX inside -->
<!-- the widget (~308x98 after Plan 03), NOT mm. Reason in px or fractions, never raw mm. -->

KEEP AS-IS (proportional, re-flows free):
  phaseToX:      box.size.x * 0.20 (centerStart), * 0.60 (centerWidth)   // lines ~908-913
  drawTextOverlays columns:  box.size.x * 0.11 (leftColX), * 0.89 (rightColX)  // ~1321-1322
  drawPhaseDot:  box.size.y * 0.03 (dotRadius)                           // ~1068
  drawBpmStack:  box.size.x * 0.89 (bpmX)                                // ~1221
  drift jitter:  box.size.y * 0.05 / 0.03                                // ~1096/1117
  border/brackets/scanlines: w=box.size.x h=box.size.y                   // proportional

RE-TUNE (hardcoded px — bump per discretion, stay within UI-SPEC 5-8px NanoVG range for fonts):
  valueToY margin:        line ~916   6.f          -> ~8-9f
  drawCornerBrackets:     ~962-963    inset 3.f / size 5.f -> ~4f / ~6f
  pill font sizes:        ~1327-1330  4.1/3.5/3.2/2.9 -> ~+10-20% (e.g. 4.6/4.0/3.6/3.3)
  BPM/CLK font:           ~1220/1238  bpmFontSize 3.5 / clkFontSize 2.9 -> bump in step
  top/bottom baselines:   ~1323-1324  topY 6.f / bottomY box.size.y - 4.f -> re-check no clip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-tune WaveformDisplay px constants for the 81.44x26.00mm box (D-06)</name>
  <files>src/AnalogLFO.cpp</files>
  <read_first>
    - src/AnalogLFO.cpp lines 846-1445 (the WaveformDisplay class — read drawLayer, phaseToX, valueToY, drawCornerBrackets, drawTextOverlays, drawBpmStack, drawBorder; identify the exact current line of each hardcoded constant before editing, since lines may have shifted)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (Code Examples > WaveformDisplay internal layout — the proportional-vs-hardcoded inventory and suggested new values; Pitfall 4 + Pitfall 5 box.size is px)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md (WaveformDisplay re-tune table — exact loci and suggested values; Frozen surface rule)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md (Display internal layout + Typography — fonts stay within 5-8px NanoVG range; Hz format frozen)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-01-PLAN.md (the baseline screenshot reference this re-tune is diffed against)
  </read_first>
  <action>
    Bump ONLY the hardcoded-px constants so the larger glass reads as a deliberate refit. Suggested values (Claude's discretion — tune to read best, but stay in range): valueToY margin (line ~916) from 6.f to ~8-9f so the trace breathes in the taller box; drawCornerBrackets inset/size (~962-963) from 3.f/5.f to ~4f/6f (about +30%); the four pill font sizes (~1327-1330) pillLabelSize 4.1, pillValueSize 3.5, pillSmallSize 3.2, pillMicroSize 2.9 bumped ~10-20% (e.g. 4.6/4.0/3.6/3.3) while staying within the UI-SPEC 5-8px NanoVG character range; bpmFontSize 3.5 and clkFontSize 2.9 (~1220/1238) bumped in step; re-check topY 6.f / bottomY box.size.y - 4.f (~1323-1324) so neither the top pill row nor the bottom BPM stack clips in the taller box.

    Do NOT touch anything proportional (phaseToX 0.20/0.60, leftColX/rightColX 0.11/0.89, dotRadius box.size.y*0.03, bpmX box.size.x*0.89, drift jitter, border/scanlines/brackets w/h math) — it re-flows for free. Reason in PX or fractions of box.size, never raw mm (box.size inside the widget is ~308x98 px after Plan 03, not mm). FROZEN — do NOT touch: WaveformDisplay::step(), the module->display* atomic loads, RATIO_TABLE, swing math, any DSP, the Hz-format contract (1 decimal below 10Hz, integer at/above), or the drawText blur 2.0f values. This is geometry/typography-constant re-tuning only, so all Phase 20 content (ember trace color, 3-layer glow, scanlines, breathing border glow at 0.2Hz, corner brackets, comet trail, phase dot, SYNC badge, BPM stack) is preserved by construction (PANEL-11 criterion 6).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'box.size.x \* 0.20f' src/AnalogLFO.cpp && grep -q 'box.size.x \* 0.60f' src/AnalogLFO.cpp && grep -q '0.11f' src/AnalogLFO.cpp && grep -q '0.89f' src/AnalogLFO.cpp && make RACK_DIR=../Rack-SDK 2>&1 | tail -3 && [ -f plugin.dylib -o -f plugin.so ] && echo DISPLAY_RETUNE_OK</automated>
  </verify>
  <done>The proportional center-band (0.20/0.60) and pill columns (0.11/0.89) are untouched (grep confirms), the px constants (margin, bracket inset/size, pill+BPM font sizes, baselines) are bumped for the larger box, WaveformDisplay::step()/atomics/DSP are unchanged, and `make RACK_DIR=../Rack-SDK` compiles clean. Visual no-regression diff vs the Plan 01 baseline + no-clip/overflow confirmation deferred to Plan 05's in-Rack gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits NanoVG rendering constants (font sizes, margins, insets) in the display widget of an offline desktop audio-plugin faceplate. No input parsing, network, auth, secrets, or user data. The display reads frozen atomic values from the unchanged DSP; no data path changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-04 | — | WaveformDisplay drawLayer px constants | accept | No applicable ASVS category. Per RESEARCH.md Security Domain, re-tuning rendering constants on an offline faceplate introduces no threat surface — no new input, no atomic/DSP change, render-only edit. |

No package installs (T-20.1-SC not applicable — zero npm/pip/cargo/registry packages this phase per RESEARCH.md Package Legitimacy Audit).
</threat_model>

<verification>
- Compile gate: `make RACK_DIR=../Rack-SDK` exits 0 and produces plugin.dylib.
- Source assertions: proportional center-band (0.20/0.60) and pill columns (0.11/0.89) untouched; px constants bumped.
- Frozen surface untouched: step()/atomics/DSP/Hz-format unchanged.
- Visual no-regression + no-clip diff vs Plan 01 baseline deferred to Plan 05.
</verification>

<success_criteria>
The display fills the new 81.44x26.00mm box with readable three-column pills, no clipping/overflow, reading as a deliberate refit, with all Phase 20 content/polish preserved (PANEL-11).
</success_criteria>

<output>
Create `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-04-SUMMARY.md` when done. Record the exact constants changed and their new values, and confirm no proportional math or frozen surface was touched.
</output>

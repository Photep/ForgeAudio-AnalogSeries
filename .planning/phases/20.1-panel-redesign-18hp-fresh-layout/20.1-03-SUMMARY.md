---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 03
subsystem: panel
tags: [panel, widget-placement, forge-noir, 18hp]
requires:
  - "res/AnalogLFO.svg promoted to 18HP (Plan 02)"
provides:
  - "AnalogLFOWidget ctor with 18HP mm anchors and four equal Secondary knobs"
affects:
  - src/AnalogLFO.cpp
tech-stack:
  added: []
  patterns:
    - "mm2px(Vec) anchor literal substitution preserving enum bindings"
    - "Template-type token swap (ForgeKnobUtility -> ForgeKnobSecondary) for visual equality"
key-files:
  created: []
  modified:
    - src/AnalogLFO.cpp
decisions:
  - "D-02: RATE/PHASE upgraded Utility->Secondary so all four secondary knobs are visually equal"
  - "D-09: Hex bolts left in RACK_GRID_WIDTH/box.size.x auto-adapting form (no mm fallback applied)"
metrics:
  duration: "~6 min"
  completed: 2026-06-12
---

# Phase 20.1 Plan 03: 18HP Control Anchor Repositioning Summary

Repositioned every AnalogLFOWidget control anchor from 14HP to the locked 18HP mm table and upgraded RATE/PHASE to ForgeKnobSecondary, delivering the MORPH hero + four equal secondary knob layout with preserved enum bindings — compiles clean.

## What Was Built

- **Display box** moved to pos `(5.00, 19.00)` size `(81.44, 26.00)`; stale "14HP" comment updated to "18HP" (D-04, D-06 geometry only).
- **Main knobs row (y=87):** MORPH hero at `(45.72, 61.00)` unchanged type; CHARACTER `(18.00, 87.00)`, DRIFT `(36.24, 87.00)`, RATE `(54.48, 87.00)`, PHASE `(72.72, 87.00)`.
- **RATE/PHASE upgrade (D-02):** both swapped `ForgeKnobUtility` -> `ForgeKnobSecondary`. Zero `ForgeKnobUtility` instantiations remain; all four secondary knobs now visually equal (13.50mm).
- **Trimpots row (y=108.5):** `7.70 / 18.56 / 29.43 / 40.29 / 51.15`.
- **CV jacks row (y=119.5):** `7.70 / 18.56 / 29.43 / 40.29 / 51.15`.
- **Grouped CLK/RST/OUTPUT (y=119.5):** CLK `62.01`, RST `72.88`, OUTPUT `83.74`.
- **Hex bolts (D-09):** left in `RACK_GRID_WIDTH` / `box.size.x` auto-adapting form. Fallback NOT applied — deferred to Plan 05's 200% gate.

All `addParam`/`addInput`/`addOutput` wrappers, `create*Centered` helpers, and `AnalogLFO::*_PARAM/_INPUT/OUTPUT` enum bindings unchanged — functionality preserved (PANEL-12).

## Verification

- Grep assertions: all required 18HP anchors present (MORPH 45.72/61.00, display 5.00/19.00 + 81.44/26.00, RATE/PHASE Secondary at 54.48/72.72, OUTPUT 83.74/119.50); `ForgeKnobUtility` instantiation count = 0. PASS.
- Compile gate: `make RACK_DIR=../Rack-SDK` exited 0 and produced `plugin.dylib` (162192 bytes). PASS.
- No file deletions in the commit. No enum bindings removed or remapped.
- In-Rack 100%/200% alignment, hex-bolt landing, and functional checks deferred to Plan 05.

## Deviations from Plan

None — plan executed exactly as written. Hex-bolt fallback was correctly NOT applied at this stage.

## Deferred Issues

- Pre-existing `-Wunused-variable` warning on `pillValueSize` (src/AnalogLFO.cpp:1328) inside WaveformDisplay — out of scope for this plan (Plan 04 owns WaveformDisplay internals). Logged, not fixed.

## Commits

- `a5b4a8d` feat(20.1-03): reposition AnalogLFOWidget ctor to 18HP anchors

## Self-Check: PASSED

- FOUND: src/AnalogLFO.cpp (modified, all 18HP anchors present)
- FOUND: commit a5b4a8d
- FOUND: plugin.dylib produced by compile gate

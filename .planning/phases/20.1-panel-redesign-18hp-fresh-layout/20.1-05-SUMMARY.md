---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 05
status: complete
completed: 2026-06-12
requirements: [PANEL-08, PANEL-09, PANEL-10, PANEL-11, PANEL-12]
---

# Summary — Plan 05: Phase acceptance gate (clean rebuild + in-Rack alignment)

## What was done

### Task 1 — Clean rebuild + reinstall (flush stale 14HP art)
- `make clean && make install RACK_DIR=../Rack-SDK` → exit 0, `plugin.dylib` produced.
- Verified production art: `res/AnalogLFO.svg` viewBox `0 0 91.44 128.5` (18HP),
  zero `url(#metal)`/`url(#metalHero)`; dead SVGs absent from regenerated `dist/`.
- **Stale-install pitfall hit and resolved (RESEARCH Pitfall 2):** the installed
  extracted dir `ForgeAudio-AnalogSeries/res/AnalogLFO.svg` still held the OLD 14HP
  art (35434 bytes, dated 07:52) even after `make install` copied the fresh
  `.vcvplugin`. Refreshed the extracted dir from fresh `dist/` (now 58165 bytes,
  18HP viewBox) and removed the dead SVGs there. Confirmed the separate stale
  `ForgeAudio/` plugin dir is an unrelated slug (module `ForgeAudioLFO`, not the
  Analog LFO `ForgeAnalogLFO`) so it does not shadow — left untouched.

### Task 2 — In-Rack alignment + regression + functional gate (D-08)
Human visual gate via VCV Rack at the user's machine. **Approved** after 7
refinement iterations. Final approved render: `verify-18hp-100.png`.

Confirmed: 18HP render with fresh stripped art; MORPH hero + four EQUAL secondary
knobs on their y=87 sockets; trimpots / CV jacks / grouped CLK·RST·OUT on their
rings (OUTPUT inside its ember ring); display fills the 81.44×26.00 box with
readable pills and no clip; no Phase-20 content regression vs the Plan 01 baseline;
all functionality intact. No double-rendered knob bodies.

## Gate-feedback fixes (refinement iterations)

The gate surfaced polish issues beyond the original plan scope; each was fixed,
rebuilt, reinstalled, and re-verified in Rack:

1. `cb6b363` — **Knob bg-centering bug (Phase-19 components):** the `bg` SvgWidget
   (larger than the foreground that sizes the box) was left at framebuffer origin,
   drawing every knob body offset down-right of its socket art (hero 0.81mm,
   secondary 0.755mm, trimpot 0.30mm). Centered each `bg` in its box. Also enlarged
   the trimpot SVGs 1.5× (disc 3.2→4.8mm) to fill their ~5.8mm socket-tick rings.
2. `8c6ec26` — **Knob sweep vs tick art:** large knobs used ±0.83π (±149°), letting
   the pointer overshoot the bright end-ticks at ±135°. Set all large knobs to
   ±0.75π so the pointer lands on the end ticks (trimpots already ±0.75π).
3. `b1f0d32` — **Trimpot scale ticks:** the panel drew a full 360° ring implying
   full rotation. Restyled to the 270° sweep — deleted bottom-dead-center tick,
   orange at top/min/max, grey intermediates — on all 5 attenuverters.
4. `05bd583` — **TDC consistency:** MORPH/DRIFT/RATE had a grey top-dead-center tick
   while CHARACTER/PHASE had orange. Recolored MORPH/DRIFT/RATE tops to orange.
5. `6d458f1` — **MORPH orbit dots:** the 3 decorative ring dots (0/120/240°) put one
   under the display edge. Rotated +90° → one at bottom-dead-center, the other two
   symmetric about the vertical axis (y=52.25), clear of the display.
6. `4b535c7` — **Corner bolts vs OUTPUT:** the bottom-right bolt overlapped OUTPUT's
   ember rings. Moved all 4 bolt art-holes + widgets into the panel corners
   (2.60/88.84, 2.60/125.90), keeping 4-bolt symmetry; bottom-right bolt now 8.18mm
   from OUTPUT (clears r=6.10). Widgets switched to `createWidgetCentered`+`mm2px`
   so they sit exactly on their holes (D-09 alignment fix applied with new coords).

## Verification

- Compile + clean reinstall green; installed/dist art is the 18HP stripped panel;
  dead SVGs gone everywhere.
- Full alignment checklist (PANEL-08..12) confirmed by the user at 100% (and across
  iterative zoom reviews) — every widget on its anchor, four equal secondary knobs,
  grouped clock section, refit display with no clip/overflow, no Phase-20 regression.
- Functionality intact (params, attenuverters, CLK/RST, OUTPUT).
- Proof screenshot `verify-18hp-100.png` is the final approved render. (Note: a
  single consolidated proof was captured; the user reviewed alignment across both
  zoom levels during the iterative gate and approved.)

## Self-Check: PASSED

---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AnalogLFO.cpp
autonomous: true
requirements: [PANEL-08, PANEL-09, PANEL-10, PANEL-12]
must_haves:
  truths:
    - "MORPH hero + four equal-sized secondary knobs are anchored on the single y=87 row (RATE/PHASE upgraded Utility to Secondary)"
    - "Trimpots (y=108.5), CV jacks (y=119.5), and grouped CLK/RST/OUTPUT (62.01/72.88/83.74) are anchored at their 18HP positions"
    - "Display box is repositioned to pos (5,19) size (81.44,26); hex bolts land on the 18HP art bolt-holes"
    - "All param/input/output enum bindings are unchanged so functionality is preserved"
  artifacts:
    - path: "src/AnalogLFO.cpp"
      provides: "AnalogLFOWidget ctor with 18HP mm anchors and Secondary RATE/PHASE"
      contains: "mm2px(Vec(45.72f, 61.00f))"
  key_links:
    - from: "AnalogLFOWidget ctor RATE/PHASE createParamCentered"
      to: "ForgeKnobSecondary"
      via: "template type token swap from ForgeKnobUtility"
      pattern: "createParamCentered<ForgeKnobSecondary>.*RATE_PARAM"
---

<objective>
Reposition every control anchor in the `AnalogLFOWidget` ctor (`src/AnalogLFO.cpp` ~lines 1534-1585) from the 14HP values to the locked 18HP mm anchor table (D-07), upgrade RATE and PHASE knobs from `ForgeKnobUtility` to `ForgeKnobSecondary` so all four secondary knobs are visually equal (D-02), move the display box to its new pos/size (D-06 box geometry only), verify hex-bolt auto-adaptation (D-09), and remove the now-stale 14HP comments (D-04). This delivers PANEL-08 (18HP layout), PANEL-09 (hero + 4 equal secondary), PANEL-10 (trimpots/jacks/clock group), and the placement half of PANEL-12 (functionality preserved via unchanged enum bindings).

Purpose: This ctor is the single locus for all control placement. Every edit is a literal value substitution into the existing `create*Centered<T>(mm2px(Vec(x,y)), module, ENUM)` idiom — the wrappers, helpers, and enum bindings are preserved, which is exactly what keeps functionality intact.
Output: The ctor places all 20 controls + display box on the 18HP anchors with the four equal secondary knobs; the project compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md

<interfaces>
<!-- Forge widget structs already defined in src/AnalogLFO.cpp (~lines 1451-1521). -->
<!-- Use these directly — no codebase exploration needed. RATE/PHASE swap Utility->Secondary only. -->

ForgeKnobHero       : app::SvgKnob  // 18.00mm _bg.svg, MORPH (unchanged)
ForgeKnobSecondary  : app::SvgKnob  // 13.50mm _bg.svg, CHARACTER/DRIFT + RATE/PHASE (new)
ForgeKnobUtility    : app::SvgKnob  // 10.50mm _bg.svg, REMOVED from use (D-02)
ForgeTrimpot        : app::SvgKnob  // 4.20mm, 5 attenuverters
ForgeJackInput      : app::SvgPort  // CV/CLK/RST inputs
ForgeJackOutput     : app::SvgPort  // OUTPUT
ForgeHexBolt        : app::SvgScrew // 4 corner bolts (RACK_GRID_WIDTH/HEIGHT px math, box.size.x auto-adapts)

// Idiom (do not change the wrapper, helper, template-type [except RATE/PHASE], or enum):
//   addParam(createParamCentered<T>(mm2px(Vec(x,y)), module, AnalogLFO::ENUM));
//   addInput(createInputCentered<ForgeJackInput>(mm2px(Vec(x,y)), module, AnalogLFO::ENUM));
//   addOutput(createOutputCentered<ForgeJackOutput>(mm2px(Vec(x,y)), module, AnalogLFO::OUTPUT));
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap all 18HP anchors + display box + RATE/PHASE upgrade + bolt verify</name>
  <files>src/AnalogLFO.cpp</files>
  <read_first>
    - src/AnalogLFO.cpp lines 1523-1601 (the AnalogLFOWidget ctor — current 14HP anchors, the exact lines being edited; read to see current state before editing)
    - src/AnalogLFO.cpp lines 1451-1521 (Forge struct definitions — confirm ForgeKnobSecondary exists and its _bg.svg path; needed for the RATE/PHASE swap)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (Pattern 1 old->new anchor table; Pattern 2 RATE/PHASE swap; Pattern 3 hex-bolt auto-adaptation + Pitfall 3 fallback)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-PATTERNS.md (ctor anchor + RATE/PHASE + hex-bolt analogs with the new Vec table)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md (LOCKED Spacing Scale mm anchor table — the contract)
  </read_first>
  <action>
    Apply the exact old->new mm2px(Vec) substitutions in the ctor. Display box (lines 1538-1539): box.pos to mm2px(Vec(5.00f, 19.00f)), box.size to mm2px(Vec(81.44f, 26.00f)); update the stale "14HP" comment on line 1534 to say 18HP (D-04). Knobs: MORPH (1544) to (45.72f, 61.00f) keep ForgeKnobHero; CHARACTER (1546) to (18.00f, 87.00f); DRIFT (1548) to (36.24f, 87.00f); RATE (1550) to (54.48f, 87.00f) AND change template token ForgeKnobUtility to ForgeKnobSecondary (D-02); PHASE (1552) to (72.72f, 87.00f) AND change ForgeKnobUtility to ForgeKnobSecondary (D-02). Trimpots row y=108.5 (1556-1564): MORPH_ATTEN (7.70f, 108.50f), CHARACTER_ATTEN (18.56f, 108.50f), DRIFT_ATTEN (29.43f, 108.50f), FM_ATTEN (40.29f, 108.50f), PHASE_OFFSET_ATTEN (51.15f, 108.50f). CV jacks row y=119.5 (1568-1576): MORPH_CV (7.70f, 119.50f), CHARACTER_CV (18.56f, 119.50f), DRIFT_CV (29.43f, 119.50f), FM_INPUT (40.29f, 119.50f), PHASE_OFFSET_CV (51.15f, 119.50f). Clock group y=119.5 (1580-1584): CLK_INPUT (62.01f, 119.50f), RESET_INPUT (72.88f, 119.50f), OUTPUT (83.74f, 119.50f).

    CRITICAL: Change ONLY the Vec(x,y) numbers and the two RATE/PHASE template tokens. Do NOT change any addParam/addInput/addOutput wrapper, any create*Centered helper, or any AnalogLFO::*_PARAM/_INPUT/OUTPUT enum binding — preserving the enum bindings is what keeps functionality intact (PANEL-12). Note FM_INPUT remains the 4th CV slot and PHASE_OFFSET_* keep their enum even though grouped; only coordinates move.

    Hex bolts (lines 1529-1532): VERIFY-ONLY by default — do NOT rewrite. The right-side bolts already derive x from box.size.x, which becomes the 18HP width automatically once the promoted SVG loads. Leave them as the RACK_GRID_WIDTH/RACK_GRID_HEIGHT form. Fallback (apply ONLY if Plan 05's 200% gate shows the bolt offset from the art holes at x=4.60/86.84): switch the four bolt anchors to mm2px(Vec(4.60f,4.60f)), mm2px(Vec(86.84f,4.60f)), mm2px(Vec(4.60f,123.90f)), mm2px(Vec(86.84f,123.90f)) per D-09. Do not apply the fallback pre-emptively.

    Remove any now-dead 14HP position constants/comments left in the ctor (D-04). The ForgeKnobUtility struct definition (lines ~1477-1488) MAY remain harmlessly; just remove stale comments referencing it. Do NOT touch WaveformDisplay internals (that is Plan 04), DSP, atomics, or plugin.json.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'mm2px(Vec(45.72f, 61.00f))' src/AnalogLFO.cpp && grep -q 'mm2px(Vec(5.00f, 19.00f))' src/AnalogLFO.cpp && grep -q 'mm2px(Vec(81.44f, 26.00f))' src/AnalogLFO.cpp && grep -q 'createParamCentered<ForgeKnobSecondary>(mm2px(Vec(54.48f, 87.00f))' src/AnalogLFO.cpp && grep -q 'createParamCentered<ForgeKnobSecondary>(mm2px(Vec(72.72f, 87.00f))' src/AnalogLFO.cpp && grep -q 'mm2px(Vec(83.74f, 119.50f))' src/AnalogLFO.cpp && U=$(grep -c 'createParamCentered<ForgeKnobUtility>' src/AnalogLFO.cpp); [ "$U" = 0 ] && make RACK_DIR=../Rack-SDK 2>&1 | tail -3 && [ -f plugin.dylib -o -f plugin.so ] && echo CTOR_OK</automated>
  </verify>
  <done>src/AnalogLFO.cpp ctor contains all 18HP anchors (verified by grep on MORPH 45.72/61.00, display box 5.00/19.00 + 81.44/26.00, RATE/PHASE Secondary at 54.48/72.72, OUTPUT 83.74/119.50). Zero createParamCentered<ForgeKnobUtility> remain. `make RACK_DIR=../Rack-SDK` compiles with zero errors and produces plugin.dylib. All enum bindings unchanged. Hex bolts left in box.size.x form (auto-adapt). In-Rack alignment + functional + bolt verification deferred to Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits compile-time widget placement constants in an offline desktop audio-plugin faceplate. No input parsing, network, auth, secrets, or user data. The only runtime "inputs" are CV voltages handled by the frozen, unchanged DSP (not touched here).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-03 | — | AnalogLFOWidget ctor placement constants | accept | No applicable ASVS category. Per RESEARCH.md Security Domain, repositioning widgets (changing mm2px anchor literals and one template-type token) introduces no threat surface — no new data path, no input handling, enum bindings unchanged. |

No package installs (T-20.1-SC not applicable — zero npm/pip/cargo/registry packages this phase per RESEARCH.md Package Legitimacy Audit).
</threat_model>

<verification>
- Compile gate: `make RACK_DIR=../Rack-SDK` exits 0 and produces plugin.dylib.
- Source assertions: all 18HP anchors present; zero ForgeKnobUtility instantiations; OUTPUT at 83.74/119.50; display box at 5/19 size 81.44/26.
- Enum bindings unchanged (no AnalogLFO::*_PARAM/_INPUT/OUTPUT removed or remapped).
- In-Rack alignment at 100%/200%, hex-bolt landing, and functional check deferred to Plan 05.
</verification>

<success_criteria>
The ctor places MORPH + four equal secondary knobs (y=87), trimpots (y=108.5), CV jacks + grouped clock section (y=119.5), and the resized display box on the locked 18HP anchors, compiles clean, and preserves every enum binding (PANEL-08/09/10/12 placement).
</success_criteria>

<output>
Create `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-03-SUMMARY.md` when done. Record the anchor swaps applied, confirm the RATE/PHASE upgrade, note whether the hex-bolt fallback was needed (it should NOT be at this stage), and the compile result.
</output>

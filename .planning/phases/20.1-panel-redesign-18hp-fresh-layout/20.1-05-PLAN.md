---
phase: 20.1-panel-redesign-18hp-fresh-layout
plan: 05
type: execute
wave: 3
depends_on: ["20.1-02", "20.1-03", "20.1-04"]
files_modified: []
autonomous: false
requirements: [PANEL-08, PANEL-09, PANEL-10, PANEL-11, PANEL-12]
must_haves:
  truths:
    - "The plugin compiles clean and a clean reinstall has flushed all stale 14HP art from dist/ and the installed plugin dir"
    - "Every widget center sits exactly on its 18HP artwork anchor at both 100% and 200% zoom with no visible offset and no double-rendered knob bodies"
    - "All functionality works unchanged (params, attenuverters, CLK/RST, OUTPUT) and there is no Phase 20 display regression"
  artifacts:
    - path: ".planning/phases/20.1-panel-redesign-18hp-fresh-layout/verify-18hp-100.png"
      provides: "Proof screenshot of aligned 18HP panel at 100% zoom"
    - path: ".planning/phases/20.1-panel-redesign-18hp-fresh-layout/verify-18hp-200.png"
      provides: "Proof screenshot of aligned 18HP panel at 200% zoom"
  key_links:
    - from: "installed plugin dir res/AnalogLFO.svg"
      to: "the freshly built stripped 18HP art"
      via: "make clean && make install (flushes stale cache)"
      pattern: "make install"
---

<objective>
Run the phase acceptance gate: clean-rebuild and reinstall the plugin (flushing stale 14HP art from dist/ and the installed plugin dir), load the Analog LFO in VCV Rack, and confirm the full alignment checklist at BOTH 100% and 200% zoom — every widget on its anchor, no double-rendered knob bodies, four equal secondary knobs, grouped clock section, display filling the larger box with no clip/overflow, no Phase 20 regression, and all functionality intact. Capture 100% + 200% screenshots as proof (D-08). This closes PANEL-08 through PANEL-12.

Purpose: This is a manual-visual-fidelity phase — the only automated signal is the compile gate; every alignment/regression criterion is a human in-Rack check. A clean reinstall is MANDATORY first: stale dist/ and the installed plugin dir cache the old art, so without it Rack renders the stale 14HP panel and the whole verification is meaningless.
Output: Clean reinstall green; full alignment checklist confirmed at 100%/200%; proof screenshots committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-VALIDATION.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md
@.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean rebuild + reinstall to flush stale art</name>
  <files>(no source files — build/install only)</files>
  <read_first>
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (Build & verify mechanics; Runtime State Inventory — the installed plugin dir + dist/ cache old art; Pitfall 2 stale install masks the change; Open Question on ForgeAudio vs ForgeAudio-AnalogSeries install dir)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-VALIDATION.md (Full suite command: make clean && make install)
  </read_first>
  <action>
    From the repo root run a clean rebuild and reinstall: `make clean` then `make install RACK_DIR=../Rack-SDK`. This recompiles the ctor + display edits and copies the new res/ (including the stripped 18HP res/AnalogLFO.svg) into the installed plugin dir at ~/Library/Application Support/Rack2/plugins-mac-arm64/. Confirm the build exits 0. Then verify the installed res/AnalogLFO.svg got a fresh timestamp and is the 18HP art (viewBox 91.44x128.5, no url(#metal)/url(#metalHero)), and that the deleted dead SVGs (AnalogLFO-redesign.svg, AnalogLFO-fresh-v1.svg) are NOT present in the installed dir or in regenerated dist/. Per the RESEARCH open question, both ForgeAudio/ and ForgeAudio-AnalogSeries/ may exist in the plugins dir — confirm which got the fresh timestamp (slug is ForgeAudio-AnalogSeries); if a stale ForgeAudio/ copy could shadow it, refresh or remove the stale one so Rack loads the new art. Fully quit VCV Rack before relaunch (Rack caches SVGs at load).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && make clean >/dev/null 2>&1; make install RACK_DIR=../Rack-SDK 2>&1 | tail -5 && grep -q 'viewBox="0 0 91.44 128.5"' res/AnalogLFO.svg && [ ! -f dist/ForgeAudio-AnalogSeries/res/AnalogLFO-redesign.svg ] && [ ! -f dist/ForgeAudio-AnalogSeries/res/AnalogLFO-fresh-v1.svg ] && echo REINSTALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `make clean && make install RACK_DIR=../Rack-SDK` exits 0 (compile gate green).
    - res/AnalogLFO.svg viewBox is "0 0 91.44 128.5" (18HP) and contains zero url(#metal)/url(#metalHero).
    - Neither AnalogLFO-redesign.svg nor AnalogLFO-fresh-v1.svg exists in regenerated dist/ or the installed plugin dir.
    - The installed plugin dir's res/AnalogLFO.svg has a fresh timestamp (newer than the last build); the correct slug dir (ForgeAudio-AnalogSeries) holds it and no stale ForgeAudio/ copy shadows it.
  </acceptance_criteria>
  <done>Clean reinstall is green, the installed/dist art is the 18HP stripped panel, the dead SVGs are gone everywhere, and Rack will load the fresh art on next launch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-Rack alignment + regression + functional gate at 100% and 200% (D-08)</name>
  <read_first>
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-RESEARCH.md (Validation Architecture > Alignment checklist — the acceptance bar; Pitfall 1 over/under-strip warning signs; Pitfall 3 hex-bolt offset fallback)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-VALIDATION.md (Manual-Only Verifications table — PANEL-08..12 instructions)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-UI-SPEC.md (Acceptance / Verification D-08 — the 7 done conditions; LOCKED anchor table for the offset check)
    - .planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-01-PLAN.md (the Plan 01 baseline screenshots to diff Phase 20 content against)
  </read_first>
  <what-built>
    Plans 02-04 produced: the 18HP bezel-stripped production panel (res/AnalogLFO.svg), the ctor repositioned to all 18HP anchors with RATE/PHASE upgraded to equal secondary knobs and the enlarged display box, and the WaveformDisplay px constants re-tuned for the larger glass. Task 1 of this plan clean-reinstalled so Rack will render the new art. This checkpoint is the human visual confirmation that everything lands correctly — it cannot be automated (no headless faceplate-geometry rig exists).
  </what-built>
  <how-to-verify>
    1. Launch VCV Rack 2 (after the clean reinstall + full quit) and add the "Analog LFO" module. Confirm it renders at 18HP width with the fresh panel art.
    2. Set zoom to 100%. Walk the alignment checklist:
       - No double-rendered knob bodies — no hard metal ring that fails to rotate with any live knob (D-01 strip applied). A static metal disc under a knob = strip incomplete -> route back to Plan 02. Missing display glass = over-strip -> route back to Plan 02.
       - MORPH hero centered on (45.72, 61.00).
       - Four secondary knobs CHARACTER/DRIFT/RATE/PHASE visually EQUAL diameter, single row, centered on x=18.00/36.24/54.48/72.72 at y=87.
       - 5 trimpots centered on their y=108.5 art rings (x=7.70/18.56/29.43/40.29/51.15); 5 CV jacks directly below on y=119.5 (same x).
       - Clock group CLK/RST/OUTPUT at x=62.01/72.88/83.74, y=119.5, right of the artwork divider (x about 56.58); OUTPUT sits inside its ember accent ring.
       - 4 hex bolts centered on the art bolt-holes (x=4.60/86.84, y=4.60/123.90).
       - Display fills the 81.44x26.00 box; three-column pills readable, no clip/overflow; SYNC badge top-right; BPM stack bottom-right.
       - Phase 20 regression diff vs the Plan 01 baseline: ember waveform color, 3-layer glow, scanlines, breathing border glow (0.2Hz), corner brackets, comet trail, phase dot — all present and unchanged in style.
    3. Set zoom to 200% and repeat the whole checklist — pay special attention to per-widget offset and the hex-bolt landing (Pitfall 3: if a bolt is offset from its art hole, route back to Plan 03 to apply the mm2px bolt-anchor fallback).
    4. Functional check: turn each knob (responds), patch CV through each attenuverter (modulates), feed a clock into CLK (SYNC -> ACQUIRING blink at 2Hz -> SYNC steady), trigger RST (resets), confirm OUTPUT emits audio.
    5. Capture proof screenshots: save the 100% view to `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/verify-18hp-100.png` and the 200% view to `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/verify-18hp-200.png` (macOS screencapture / Cmd-Shift-4).
  </how-to-verify>
  <acceptance_criteria>
    - PANEL-08: Module renders at 18HP with the fresh stripped art; no double-rendered knob bodies.
    - PANEL-09: Four secondary knobs equal-sized in the single y=87 row, on their art sockets.
    - PANEL-10: Trimpots (y=108.5), CV jacks (y=119.5), and grouped CLK/RST/OUTPUT (62.01/72.88/83.74) each centered on their art rings; OUTPUT inside its ember ring.
    - PANEL-11: Display fills 81.44x26.00mm with readable three-column pills, no clip/overflow, and no Phase 20 content regression vs the Plan 01 baseline.
    - PANEL-12: No visible offset on any widget or hex bolt at BOTH 100% and 200%; every param/attenuverter/CLK/RST/OUTPUT works unchanged.
    - Proof screenshots verify-18hp-100.png and verify-18hp-200.png saved in the phase directory.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the full alignment checklist passes at both 100% and 200%, functionality is confirmed intact, and both proof screenshots are saved. If any check fails, describe the failure and which plan it routes back to (strip issues -> Plan 02, anchor/bolt issues -> Plan 03, display clip/regression -> Plan 04).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan builds, installs, and visually inspects an offline desktop audio-plugin faceplate. No input parsing, network, auth, secrets, or user data. The build runs the local Makefile against the in-repo Rack SDK; functional testing exercises only CV voltages through the frozen, unchanged DSP.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-05 | — | Build/install + in-Rack visual gate | accept | No applicable ASVS category. Per RESEARCH.md Security Domain, building and visually verifying an offline faceplate introduces no threat surface — no input handling, no network, no secrets, frozen DSP. |

No package installs (T-20.1-SC not applicable — zero npm/pip/cargo/registry packages this phase; build uses only the local in-repo Rack SDK at ../Rack-SDK per RESEARCH.md Package Legitimacy Audit).
</threat_model>

<verification>
- Compile + clean reinstall green; installed/dist art is the 18HP stripped panel; dead SVGs gone.
- Full alignment checklist (PANEL-08..12) confirmed by human at BOTH 100% and 200% zoom.
- Functionality intact (params, attenuverters, CLK/RST, OUTPUT); no Phase 20 regression vs Plan 01 baseline.
- Proof screenshots captured.
</verification>

<success_criteria>
The 18HP Analog LFO renders with every control on its artwork anchor at 100% and 200% with no offset, no double knobs, four equal secondary knobs, a grouped clock section, a refit display with no clip/overflow, full preserved functionality, and zero Phase 20 regression — with screenshots as proof (PANEL-08 through PANEL-12).
</success_criteria>

<output>
Create `.planning/phases/20.1-panel-redesign-18hp-fresh-layout/20.1-05-SUMMARY.md` when done. Record the reinstall result, the alignment-checklist outcome at both zooms, whether the hex-bolt mm2px fallback was needed, and the proof screenshot paths.
</output>

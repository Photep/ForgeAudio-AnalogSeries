---
phase: 30-vcocore-skeleton-module-registration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dsp/VcoCore.hpp
  - tests/test_vco_core.cpp
autonomous: true
gap_closure: true
requirements: [CORE-01]

must_haves:
  truths:
    - "D-14 guard closure (CR-01): forge::VcoCore::step() bounds its own output for every member of the hostile timing grid — sampleRate in {-44100, 0, 44100, NaN} x sampleTime in {-1/44100, 0, 1/44100, 1/1000, 999, NaN} x pitchCV in {0, +10}: every returned sample is finite and inside +/-6.0 V, core.phase stays in [0.0, 1.0), and tel.freqHz is never negative"
    - "WR-03 closure: the new coverage case was OBSERVED RED against the unfixed header BEFORE the fix landed, and each of the two guards was OBSERVED independently load-bearing by a revert-one-only probe — neither guard is masked by the other"
    - "D-18a / D-18b non-regression: every pre-existing 'vco core:' and 'vco harness:' assertion value is bit-identical before and after the fix (a doctest -s diff removes or changes zero lines), and all six shipped-LFO goldens replay byte-identical"
  artifacts:
    - "src/dsp/VcoCore.hpp — ceiling-then-floor guard order, a deltaPhase bound, and the namespace constant kVcoMaxDeltaPhase"
    - "tests/test_vco_core.cpp — scenario four (driverless hostile timing) inside the D-18b bound case, plus the mirrored guard sequence in DeliberatelyBrokenSharedStateCore"
    - "build-test/gap-evidence/before-vco-s.txt and after-vco-s.txt — the bit-identity evidence pair (gitignored, never committed)"
  key_links:
    - "scenario four -> forge::VcoCore::step() called DIRECTLY on a caller-built forge::VcoInputs, bypassing tests/VcoBlockDriver.hpp:49-52's unconditional sampleTime/sampleRate overwrite — the bypass IS the coverage gap being closed"
    - "DeliberatelyBrokenSharedStateCore::step() -> mirrors forge::VcoCore::step()'s guard sequence; its banner claims the mirror, so the two must move together or the banner becomes the next false comment"
    - "kVcoMaxDeltaPhase (0.5) -> the single-subtract wrap at the end of step(); the wrap is correct for any bound strictly below 1.0, and 0.5 clears the coupled-rate maximum (0.49) by roughly 2 percent"
---

<objective>
Close the CR-01 gap the operator marked "fix now". The Nyquist guard in `forge::VcoCore::step()`
applies its zero-floor BEFORE its ceiling, so a non-positive `in.sampleRate` makes
`maxFreq = kVcoNyquistGuardFrac * in.sampleRate` negative and the ceiling writes a negative
frequency straight over the value the floor just sanitised. The wrap has no negative branch, so
the accumulator is unbounded downward. Reproduced independently by both the code reviewer and the
verifier at `sampleRate = -44100`, `morph = 0.5`, 20000 steps: `freqHz = -21609.00`,
`phase = -9800.00`, `maxAbs = 1.476e38 V`, `isfinite = 0`.

Close WR-03 in the same plan. Both `tests/VcoBlockDriver.hpp:59-60` and `runInterleaveCheck`
(`tests/test_vco_core.cpp:214-216, 226-228, 241-247`) unconditionally overwrite
`sampleTime`/`sampleRate`, so hostile timing is the one input class `step()` is never exposed to
— while `tests/test_vco_core.cpp:540-548` claims the magnitude bound "is the only invariant in
the suite that can" see a runaway accumulator. It cannot see this one: it only varies `pitchCV`.

Purpose: make the header's own LOAD-BEARING claim true, and leave behind a case that was observed
to bite, so the next reader is not trusting an untested assertion the way this phase did.
Output: a two-part guard in `src/dsp/VcoCore.hpp` and a driverless hostile-timing scenario in
`tests/test_vco_core.cpp`, with bit-identity for every finite positive rate PROVEN mechanically
rather than argued.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-vcocore-skeleton-module-registration/30-REVIEW.md
@.planning/phases/30-vcocore-skeleton-module-registration/30-UAT.md
@.planning/phases/30-vcocore-skeleton-module-registration/30-02-SUMMARY.md

@src/dsp/VcoCore.hpp
@tests/test_vco_core.cpp
@tests/VcoBlockDriver.hpp
</context>

<landmines>
Read these before touching anything. Each was measured in this repository, not theorised.

1. `tests/check_canary.sh [2b/5]`'s step matcher is UNANCHORED. Never write the full `step`
   signature verbatim on a comment line that also carries an opening brace — the canary will
   perturb the COMMENT, splice its ODR wrapper above `struct VcoInputs`, and `make guards`
   hard-fails with nine "unknown type name 'VcoInputs'" errors that have nothing to do with the
   DSP. The header's banner abbreviates the signature for exactly this reason. Keep it abbreviated.
   (Recorded in `30-02-SUMMARY.md`, Decisions Made.)

2. Same-second mtime tie. `build-test/test` depends on `src/dsp/*.hpp` by mtime. If you splice a
   probe, rebuild, and restore the header inside one wall-clock second, `make test` considers the
   binary up to date and reports the OLD behavior — the diagnosis is a stale binary, not a failing
   assertion. Every probe restore in this plan MUST be followed by `touch src/dsp/VcoCore.hpp`
   before the next build. (Recorded in `30-02-SUMMARY.md`, Deviations item 2.)

3. The frozen headers are OFF LIMITS. `src/dsp/RackCompat.hpp`, `src/dsp/Waveshape.hpp`,
   `src/dsp/MathConst.hpp` and `src/dsp/DriftEngine.hpp` are byte-pinned by
   `tests/check_frozen.sh` and consumed by the SHIPPED Analog LFO (live in the VCV Library, issue
   #929, v2.0.1). This plan touches `src/dsp/VcoCore.hpp` and nothing else under `src/dsp/`.
   `forge::clamp` is NOT to be changed — CR-02 is explicitly out of scope for this phase and is
   tracked in `deferred-items.md` by plan 30-09.

4. `src/AnalogLFO.cpp` and `res/AnalogLFO.svg` must not appear in this plan's diff at all.

5. C++11 is binding on `src/dsp/VcoCore.hpp`. No C++17 inline variable, no in-class constant
   indexed at runtime, no standard-library clamp helper, no compile-time-conditional branch form.
   A plain namespace-scope `constexpr` is the idiom this file already uses for `kVcoFreqC4` and
   `kVcoNyquistGuardFrac`; follow it exactly.

6. `make strict` is `-fsyntax-only` and NEVER links. Local green says nothing about link health.
   Plan 30-10 owns the CI observation on the exact pushed commit; do not treat a green local gate
   as sufficient evidence for anything.
</landmines>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capture the bit-identity baseline, add the driverless hostile-timing scenario, and OBSERVE IT RED</name>
  <files>tests/test_vco_core.cpp</files>
  <behavior>
Against the UNFIXED `src/dsp/VcoCore.hpp`, the new scenario must FAIL, and must fail in more than
one way:
- `sampleRate = -44100`, `sampleTime = 1/44100`, `pitchCV = 0` — the accumulator ramps negative;
  `freqNonNegative`, `phaseInRange`, `maxAbs <= 6.0` and `allFinite` all go red.
- `sampleRate = 44100`, `sampleTime = 999`, `pitchCV = 0` — a legitimate rate with a decoupled
  increment; `phaseInRange` goes red on an unbounded upward ramp.
- any config with `sampleTime = NaN` — the increment is NaN, `phase` becomes NaN;
  `phaseInRange` and `allFinite` go red.
Against a correct implementation all four hold at all 48 configs, including the one legitimate
control point (`sampleRate = 44100`, `sampleTime = 1/44100`).
  </behavior>
  <action>
BEFORE editing anything, capture the bit-identity baseline from the UNMODIFIED tree. Run
`mkdir -p build-test/gap-evidence`, then `make build-test/test`, then
`./build-test/test -tc="vco core*,vco harness*" -s > build-test/gap-evidence/before-vco-s.txt 2>&1`.
`build-test/` is gitignored, so this file is evidence that never enters a commit. Confirm it is
non-empty and contains `5.51803` (the fixed worst-case magnitude, captured at all three rates)
before continuing. If it does not, `-s` is not emitting CAPTURE values on this doctest build and
Task 3's proof would be vacuous — stop and say so rather than proceeding on a weaker check.

Now add a FOURTH scenario to the existing D-18b bound case, `TEST_CASE("vco core: output
magnitude stays inside the 6.0 V loose bound (D-18b)")`. Place it AFTER the
`for (double sr : SAMPLE_RATES)` loop closes, at case scope, in its own block: this scenario
supplies its OWN timing and must not inherit the loop's rate. Do NOT add a new `TEST_CASE` — the
file banner numbers its invariants 1 through 5 and promises appends do not renumber, and the case
count staying at 72 keeps the count from being mistaken for the evidence.

Add `#include <limits>` to the include block, for `std::numeric_limits<float>::quiet_NaN()`.

The scenario, in this file's established idiom:

- Three `static const float` arrays at scenario scope. `HOSTILE_RATES` holds -44100, 0, 44100 and
  a quiet NaN. `HOSTILE_TIMES` holds -1/44100, 0, 1/44100, 1/1000, 999 and a quiet NaN.
  `HOSTILE_PITCH_T4` holds 0 and 10. Use that third name verbatim: the identifier `HOSTILE_PITCH`
  is already taken by scenario three inside the same `TEST_CASE`.
- `const int nHostile = 20000;` — the step count both the reviewer and the verifier reproduced at.
- A triple-nested loop over rate, then time, then pitch: 48 configs.
- For each config construct a FRESH `forge::VcoCore core;` and seed it with the proven
  non-degenerate literals, drift pair `0xC0FFEEULL, 0xBADF00DULL` and spread pair
  `0x9E3779B9ULL, 0x7F4A7C15ULL`. Never a pair of zeros: a degenerate Xoroshiro seed is a fixed
  point emitting an all-zero stream, which makes `std::normal_distribution`'s rejection loop never
  terminate — in Rack that is a hang on patch load, not a failing test (T-30-02).
- Build `forge::VcoInputs` by default construction plus field assignment, never a brace value
  list: NSDMIs make it a non-aggregate under C++11, and this file keeps C++11 shape deliberately.
  Set `pitchCV` from the loop, `morph = 0.5f`, `character = 1.f`, then `sampleTime` and
  `sampleRate` from the loop.
- Call `core.step(in)` DIRECTLY inside the inner loop. Do not route through
  `forge::VcoBlockDriver` and do not route through `runInterleaveCheck`. Both overwrite
  `sampleTime` and `sampleRate` unconditionally (`VcoBlockDriver.hpp:49-52`), which is correct for
  them and is precisely why this input class has never reached `step()`.
- Accumulate rather than asserting per sample — the idiom scenario three already uses, and the
  reason is arithmetic: 48 configs at 20000 steps would otherwise add nearly four million
  assertions to a 2.6 million assertion suite. Carry across the inner loop: `allFinite` (every
  returned sample passes `std::isfinite`), `maxAbs` (running max of `std::fabs` of the sample),
  `phaseInRange` (`core.phase >= 0.0 && core.phase < 1.0` after every step — a NaN phase fails
  this, which is intended), `freqNonNegative` (`core.tel.freqHz >= 0.f` after every step), and
  `firstBadStep` (index of the first step violating any of the four, else -1).
- Assert once per config after the inner loop. `CAPTURE` the rate, the time, the pitch, `maxAbs`
  and `firstBadStep`; add an `INFO` naming the scenario as hostile timing driven straight into
  the core with no driver; then `CHECK(allFinite)`, `CHECK(maxAbs <= kLooseBoundV)`,
  `CHECK(phaseInRange)` and `CHECK(freqNonNegative)`.

Write the scenario banner so each of the four assertions says which defect it pins and why it is
not redundant with its neighbours:
- `freqNonNegative` is the CR-01-specific pin, and it is the ONLY one of the four that a
  clamp-order defect can still trip once Task 2's increment bound exists. Without it the increment
  bound would absorb the negative step and leave the output looking healthy while `tel.freqHz`
  hands the shell's future display a negative frequency. Do not delete it as redundant.
- `phaseInRange` is the WR-01 pin: the single-subtract wrap is correct only for an increment
  inside [0, 1), and nothing in `forge::VcoInputs` couples `sampleTime` to `sampleRate`.
- `allFinite` and the magnitude bound are the user-visible consequence, and the reason the header
  called this guard load-bearing in the first place.

Also amend the sentence near `tests/test_vco_core.cpp:540-548` claiming the magnitude bound is the
only invariant in the suite that can see a runaway accumulator. As written it holds only for a
runaway caused by PITCH. Name scenario four as the one covering a runaway caused by TIMING, so
the two read as a pair rather than as one covering the other.

Do NOT touch `DeliberatelyBrokenSharedStateCore` in this task: it must stay unchanged while the
RED observation is taken, or the observation stops describing the shipped core.

Do NOT touch `src/dsp/VcoCore.hpp` in this task. The RED observation must be taken against the
UNFIXED header. That is this repository's standing practice — a new invariant is proven to bite
before it is trusted — and it is the discipline `30-02-SUMMARY.md`'s stub probe established.

Build, run the case alone, and RECORD THE VERBATIM FAILURE OUTPUT: the failing assertion names,
the CAPTURE values, and at minimum one config where `allFinite` is false and one where
`phaseInRange` is false. That transcript goes in the SUMMARY. It is the evidence, not a
formality.

Commit this task RED. State in the commit body that the suite is deliberately red at this commit
and name the task that turns it green. There are no git hooks in this repository, so nothing will
block the commit.
  </action>
  <verify>
    <automated>make build-test/test && if ./build-test/test -tc="vco core: output magnitude*"; then echo "FAIL: expected RED against the unfixed header, got green"; exit 1; else echo "OK: observed RED"; fi</automated>
    <automated>test -s build-test/gap-evidence/before-vco-s.txt && grep -q '5.51803' build-test/gap-evidence/before-vco-s.txt && grep -q 'HOSTILE_PITCH_T4' tests/test_vco_core.cpp && grep -q 'numeric_limits<float>::quiet_NaN' tests/test_vco_core.cpp && grep -q 'include <limits>' tests/test_vco_core.cpp</automated>
    <automated>test "$(./build-test/test -ltc | grep -c 'vco core:')" = "5" && test "$(git diff --name-only HEAD~1 HEAD | tr -d ' ')" = "tests/test_vco_core.cpp" && git status --porcelain src/dsp | grep -q . && exit 1 || exit 0</automated>
  </verify>
  <done>
`./build-test/test -tc="vco core: output magnitude*"` exits NON-ZERO against the unfixed header,
red on `allFinite`, `maxAbs`, `phaseInRange` and `freqNonNegative` across multiple hostile
configs, with the verbatim transcript captured for the SUMMARY.
`build-test/gap-evidence/before-vco-s.txt` exists, is non-empty, and contains `5.51803`. The
`vco core:` case count is unchanged at 5. `src/dsp/` is untouched. The task commit names exactly
one file, `tests/test_vco_core.cpp`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: land the two-part guard fix, mirror it into the positive control, and prove each guard independently load-bearing</name>
  <files>src/dsp/VcoCore.hpp, tests/test_vco_core.cpp</files>
  <behavior>
After the fix, all 48 configs of scenario four pass, `make test` is green at 72 cases, and:
- Reverting ONLY the clamp order (increment bound still present) turns `freqNonNegative` red at
  the negative-rate configs while `allFinite`, `maxAbs` and `phaseInRange` stay green.
- Reverting ONLY the increment bound (clamp order still correct) turns `phaseInRange` red, and
  `allFinite`/`maxAbs` red at the NaN-`sampleTime` and decoupled-`sampleTime` configs.
Both asymmetries are required: they are what proves neither guard is masking the other.
  </behavior>
  <action>
**Part A — the guard in `src/dsp/VcoCore.hpp`.**

Add one namespace-scope plain `constexpr` alongside `kVcoFreqC4` and `kVcoNyquistGuardFrac`:
`constexpr double kVcoMaxDeltaPhase = 0.5;`. It is a `double` because the accumulator is a
`double`. Comment it as the WRAP-CORRECTNESS bound, explicitly distinct from
`kVcoNyquistGuardFrac`'s Nyquist POLICY bound — Phase 31 (PITCH-04) replaces the Nyquist constant
and must not mistake this one for it.

Reorder the two frequency clamps so the ceiling runs FIRST and the NaN-safe floor runs LAST:
`if (freq > maxFreq) freq = maxFreq;` then `if (!(freq > 0.f)) freq = 0.f;`. The floor is then
always the last writer, so a non-positive `in.sampleRate` can no longer reintroduce a negative
frequency. Leave `tel.freqHz = freq;` where it is, immediately after both clamps.

Then bound the increment itself, after `deltaPhase` is computed and before `phase += deltaPhase;`:
a NaN-safe floor `if (!(deltaPhase > 0.0)) deltaPhase = 0.0;` followed by
`if (deltaPhase > kVcoMaxDeltaPhase) deltaPhase = kVcoMaxDeltaPhase;`.

Rewrite the two comment blocks so they state what is now true, in this file's house style — name
the consequence, name the measurement, name the phase that owns the follow-on:

- At the guard: the ORDER is load-bearing. Record the measured failure of the old order
  (`sampleRate = -44100` gives `freqHz = -21609`, `phase = -9800`, `1.476e38 V`, non-finite) and
  say plainly that the two lines must not be swapped back. Keep the existing measured note that
  without any ceiling `pitchCV = +10` reaches phase 1,014,986 and -8,655,011 V while every sample
  stays `std::isfinite` — still true, and still the reason a finiteness test alone cannot see it.
  Record that the new order is bit-identical to the old for every finite POSITIVE `in.sampleRate`:
  with `maxFreq > 0` the two orders agree on all five input classes (NaN, non-positive, in range,
  above the ceiling, positive infinity), and Task 3 proves that mechanically rather than by this
  argument.
- At the accumulate: the single-subtract wrap is correct for any increment inside [0, 1). Say that
  the ceiling above is computed from `in.sampleRate` while the increment is computed from
  `in.sampleTime`, and that NOTHING in `forge::VcoInputs` couples the two — measured at
  `sampleRate = 44100` with `sampleTime = 1/1000` and `pitchCV = +6`: the increment reaches 16.7
  and `phase` reaches 314,880, an unbounded ramp at a perfectly legitimate sample rate. Name Phase
  32's oversampled inner loop as the obvious future caller that decouples them. Then justify the
  constant: 0.5 rather than `kVcoNyquistGuardFrac`, because at a coupled rate the guarded
  frequency yields an increment of 0.49 plus float rounding, so a 0.49 ceiling could fire on a
  legitimate input and move samples; 0.5 clears that by roughly two percent, leaves every existing
  measurement bit-identical, and still satisfies the wrap.
- Correct the stale inline claim that the wrap "is correct ONLY because the guard above bounds
  deltaPhase at kVcoNyquistGuardFrac (0.49) < 1.0". The frequency guard never bounded the
  increment; it bounded `freq`. The increment is now bounded directly, so state that instead.

Respect landmine 1 while writing these comments, and landmine 5 for the constant.

**Part B — the positive control in `tests/test_vco_core.cpp`.**

`DeliberatelyBrokenSharedStateCore::step()` (around lines 332-338) reproduces the old clamp order
and the unbounded increment, while its banner states that everything except the one shared field
mirrors `src/dsp/VcoCore.hpp`, naming the NaN-safe zero test, the Nyquist clamp and the
single-subtract wrap. Leaving it stale would create a second false comment of exactly the class
this plan exists to remove, and would make the control differ from the real core in TWO things
rather than the one field its banner promises.

Mirror the fix into it: ceiling before floor, and the same two-line increment bound using
`forge::kVcoMaxDeltaPhase`. Change NOTHING else. The function-local `static double sharedPhase`
stays exactly as it is — that is the deliberate defect and the entire point of the type. Add one
line to its banner recording that the guard sequence is mirrored deliberately and must be kept in
step with the real core.

The mirror must be behaviorally inert at the control's own inputs (`pitchCV` in [-1, +1] and 0.5,
`sampleTime` equal to `1/sampleRate` at 44.1/48/96 kHz, giving an increment near 0.006 to 0.012),
so invariant 5's captured figures must stay at mismatchA and mismatchB of 512 and a
totalMismatch of 1024. Task 3's diff proves that; do not assert it by eye here.

Commit Parts A and B together as the GREEN commit, referencing Task 1's red commit.

**Part C — the two revert-one-only probes, run AGAINST THE COMMITTED FIX.**

Green is not evidence that either guard bites: the two guards can now cover for each other, which
is structurally how CR-01 hid in the first place. Run each probe, record the verbatim output,
restore with `git checkout -- src/dsp/VcoCore.hpp`, then `touch src/dsp/VcoCore.hpp` before the
next build (landmine 2).

- Probe P1, clamp order only: put the frequency floor back BEFORE the ceiling, leaving the
  increment bound in place. Rebuild, run the D-18b case. Expect `freqNonNegative` RED at the
  negative-rate configs with the other three still GREEN. That asymmetry is the point.
- Probe P2, increment bound only: restore the correct clamp order and instead delete the two
  increment-guard lines. Rebuild, run the D-18b case. Expect `phaseInRange` RED, plus `allFinite`
  and `maxAbs` RED at the NaN-`sampleTime` and decoupled-`sampleTime` configs.

After both probes assert the tree is clean: `git status --porcelain src/dsp` empty, no `.bak`
anywhere in the tree, and `kVcoMaxDeltaPhase` present in the header at its declaration and at its
use site. If either probe does NOT produce the expected red, stop and report it — a guard that
cannot be observed failing is a guard this plan has not actually validated.
  </action>
  <verify>
    <automated>touch src/dsp/VcoCore.hpp && make test</automated>
    <automated>./build-test/test -tc="golden*" && ./build-test/test -tc="vco core*" && ./build-test/test -tc="vco harness*"</automated>
    <automated>make guards && make strict && make guards RACK_DIR=/nonexistent-rack-sdk</automated>
    <automated>awk '/if \(freq > maxFreq\) freq = maxFreq;/{c=NR} /if \(!\(freq > 0\.f\)\) freq = 0\.f;/{f=NR} END{ exit !(c>0 && f>0 && c<f) }' src/dsp/VcoCore.hpp</automated>
    <automated>test "$(grep -c 'kVcoMaxDeltaPhase' src/dsp/VcoCore.hpp)" -ge 2 && test "$(grep -c 'kVcoMaxDeltaPhase' tests/test_vco_core.cpp)" -ge 1 && test "$(grep -c 'static double sharedPhase' tests/test_vco_core.cpp)" = "1"</automated>
    <automated>test "$(grep -v '^[[:space:]]*//' src/dsp/VcoCore.hpp | grep -c 'float step(const VcoInputs& in) {')" = "1" && git status --porcelain src/dsp | wc -l | grep -qx '0' && test -z "$(find . -name '*.bak' -not -path './.git/*')"</automated>
  </verify>
  <done>
`make test` is green at 72 cases with an assertion count above the 2,615,872 baseline. All six
shipped-LFO goldens replay byte-identical. `make guards`, `make guards RACK_DIR=/nonexistent-rack-sdk`
and `make strict` all pass. The ceiling clamp precedes the floor clamp in `src/dsp/VcoCore.hpp`,
`kVcoMaxDeltaPhase` is declared once and used at the accumulate, and the control mirrors the same
sequence with its shared static intact. Both revert-one-only probes were observed producing their
predicted, DIFFERENT reds, and both transcripts are recorded for the SUMMARY. The working tree is
clean and carries no probe residue.
  </done>
</task>

<task type="auto">
  <name>Task 3: prove bit-identity mechanically and prove the shipped LFO is absent from the diff</name>
  <files>build-test/gap-evidence/after-vco-s.txt</files>
  <action>
The operator's constraint is that the fix must be bit-identical for all finite positive sample
rates: the six shipped-LFO goldens and the VCO's own measured values (5.51803 V at 44.1/48/96 kHz;
spread divergence 0.233229 / 0.233235 / 0.233187 V) must not move by one bit. Prove that. Do not
assume it, and do not settle for eyeballing three numbers.

Regenerate the comparison half of the evidence pair:
`./build-test/test -tc="vco core*,vco harness*" -s > build-test/gap-evidence/after-vco-s.txt 2>&1`.

Then diff it against the Task 1 baseline with the doctest summary banners filtered out (those
carry assertion counts, which legitimately grow). The proof obligation is one-directional and
exact: since this plan only ADDS assertions, NO line present in the baseline may be missing or
altered afterwards. Concretely, filtering lines beginning with the doctest banner prefix from
both files, the diff must produce ZERO lines marked as removed-from-baseline. Any such line is a
moved measurement and means the fix is not bit-identical — stop and report rather than adjusting
the threshold.

Record explicitly in the SUMMARY, quoted from `after-vco-s.txt`:
- the fixed-worst-case magnitude at each of the three rates (expected 5.51803 V, unchanged),
- the spread-seed divergence figures (expected 0.233229 / 0.233235 / 0.233187 V, unchanged),
- invariant 4's mismatch counts (expected 0 and 0) and invariant 5's (expected 512, 512, 1024).
The last of those is what proves Task 2 Part B's mirror into the positive control was inert.

Then prove the guardrail held, by reading bytes rather than by trusting the plan:
- `git diff --stat` across this plan's task commits names exactly two files,
  `src/dsp/VcoCore.hpp` and `tests/test_vco_core.cpp`.
- No file under `src/dsp/` other than `VcoCore.hpp` appears, and neither `src/AnalogLFO.cpp`,
  `res/AnalogLFO.svg`, `src/dsp/FROZEN.sha256`, `tests/BlockDriver.hpp` nor anything under
  `tests/golden/` appears.
- `git diff --diff-filter=D --name-only` across the same range is empty: nothing was deleted.
- `bash tests/check_frozen.sh` passes with no digest bump, and `bash tests/check_canary.sh` still
  reports all eight `VcoInputs` DSP fields runtime-live at -O3.

This task produces no source change. Commit the SUMMARY-bound evidence as documentation only if
the executor's workflow requires a commit here; otherwise fold the findings into the plan SUMMARY.
`build-test/gap-evidence/` is gitignored and must never be committed.
  </action>
  <verify>
    <automated>test -s build-test/gap-evidence/after-vco-s.txt && test "$(diff <(grep -v '^\[doctest\]' build-test/gap-evidence/before-vco-s.txt) <(grep -v '^\[doctest\]' build-test/gap-evidence/after-vco-s.txt) | grep -c '^<')" = "0"</automated>
    <automated>grep -q '5.51803' build-test/gap-evidence/after-vco-s.txt && grep -q '0.233229' build-test/gap-evidence/after-vco-s.txt && grep -q '0.233187' build-test/gap-evidence/after-vco-s.txt</automated>
    <automated>bash tests/check_frozen.sh && bash tests/check_canary.sh && ./build-test/test -tc="golden*"</automated>
    <automated>git diff --name-only HEAD~2 HEAD | sort | tr '\n' ' ' | grep -qx 'src/dsp/VcoCore.hpp tests/test_vco_core.cpp ' && test -z "$(git diff --name-only HEAD~2 HEAD | grep -E 'AnalogLFO|FROZEN.sha256|tests/golden/|tests/BlockDriver.hpp')" && test -z "$(git diff --diff-filter=D --name-only HEAD~2 HEAD)"</automated>
  </verify>
  <done>
The baseline-versus-after diff removes or alters ZERO pre-existing assertion lines, so every
measurement the suite records is bit-identical across the fix. 5.51803, 0.233229 and 0.233187
appear unchanged in the after capture, invariant 5 still reports 512/512/1024, and all six
shipped-LFO goldens replay byte-identical. `check_frozen.sh` and `check_canary.sh` pass with no
digest bump and all eight fields runtime-live. The plan's diff names exactly
`src/dsp/VcoCore.hpp` and `tests/test_vco_core.cpp`, deletes nothing, and contains no shipped-LFO
source, panel, driver, golden fixture or frozen-header digest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rack host -> `forge::VcoCore::step()` | `sampleRate` / `sampleTime` arrive from `ProcessArgs`; today Rack always supplies a sane pair, but the seam is public and Phases 31-34 plus v2.1 polyphony will drive it from call sites other than `VcoBlockDriver` |
| Patch cable -> `pitchCV` | unbounded user voltage crosses here; already guarded, re-pinned by scenario four's `pitchCV = 10` column |
| VCO source tree -> shipped-LFO build graph | any leak makes a live, published module regress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Denial of Service | `forge::VcoCore::step()` phase accumulator | high | mitigate | Ceiling-then-floor frequency guard plus a direct bound on the phase increment; pinned by scenario four's 48-config hostile timing grid and by two revert-one-only probes that prove each guard bites independently |
| T-30-11 | Tampering | `tel.freqHz` consumed by Phase 35's display | medium | mitigate | The floor is made last-writer so a non-positive rate can never publish a negative frequency; `freqNonNegative` is asserted at every step of every hostile config |
| T-30-04 | Tampering | VCO code entering the shipped LFO build graph | critical | mitigate | Only `src/dsp/VcoCore.hpp` is touched under `src/dsp/`; `check_frozen.sh` digest unchanged, `check_includes.sh [1/7]` green, six goldens byte-identical, and Task 3 asserts the diff contains no LFO source, panel, driver or fixture |
| T-30-12 | Repudiation | the fix silently moving a measured VCO value | medium | mitigate | Task 3's before/after doctest `-s` diff must remove or alter zero pre-existing assertion lines — bit-identity is proven mechanically rather than argued |
| T-30-02 | Denial of Service | degenerate `(0,0)` RNG seed -> non-terminating rejection loop -> hang on patch load | high | mitigate | Scenario four seeds with the proven non-degenerate literals only; the plan forbids a zero pair explicitly |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed by this plan; no package manager is invoked anywhere in it |
</threat_model>

<verification>
1. `make test` — 72 cases, 0 failed, assertion count above the 2,615,872 baseline.
2. `./build-test/test -tc="golden*"` — 6/6, 49,164 assertions, byte-identical.
3. `make guards` and `make guards RACK_DIR=/nonexistent-rack-sdk` — both exit 0.
4. `make strict` — PASS over four translation units (with the standing caveat that it never links).
5. `bash tests/check_frozen.sh` — no digest bump; `bash tests/check_canary.sh` — all eight
   `VcoInputs` DSP fields runtime-live at -O3.
6. The before/after doctest `-s` diff removes or alters zero pre-existing lines.
7. `git diff --name-only` across the plan names exactly `src/dsp/VcoCore.hpp` and
   `tests/test_vco_core.cpp`.
8. Working tree clean; no `.bak`, no probe residue, nothing under `build-test/gap-evidence/`
   staged or committed.
</verification>

<success_criteria>
- The Nyquist guard's ceiling runs before its NaN-safe floor, and the phase increment is bounded
  directly by `kVcoMaxDeltaPhase`, so `step()` bounds its own output for every config in the
  48-point hostile timing grid.
- The coverage case was observed RED against the unfixed header before the fix landed, and each
  guard was observed independently load-bearing by a revert-one-only probe.
- Bit-identity across the fix is proven by a mechanical diff, not asserted: zero pre-existing
  assertion lines removed or altered, 5.51803 / 0.233229 / 0.233187 unchanged, goldens 6/6
  byte-identical.
- The shipped Analog LFO does not appear anywhere in the diff, and no frozen header was touched.
</success_criteria>

<output>
Create `.planning/phases/30-vcocore-skeleton-module-registration/30-08-SUMMARY.md` when done.

It MUST contain, verbatim rather than paraphrased:
1. The Task 1 RED transcript — the failing assertions and their CAPTURE values, for at least one
   negative-rate config and one NaN-`sampleTime` config.
2. Both Task 2 probe transcripts, with the observed asymmetry stated explicitly: P1 red on
   `freqNonNegative` only, P2 red on `phaseInRange` and finiteness.
3. Task 3's bit-identity result: the removed-line count from the diff (must be 0) and the three
   quoted measured figures.
4. An explicit statement of what this plan did NOT do: `forge::clamp` was not touched (CR-02 is
   out of scope for Phase 30 and is tracked by plan 30-09), no frozen header was edited, and the
   shipped LFO is absent from the diff.
</output>

---
phase: 30-vcocore-skeleton-module-registration
plan: 10
type: execute
wave: 2
depends_on: ["30-08", "30-09"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [CORE-01, CORE-03, PANEL-03]

must_haves:
  truths:
    - "The full local gate is green on the COMBINED tip of 30-08 and 30-09 — not on either plan in isolation — at 72 cases, guards PASS, strict PASS over four translation units, six shipped-LFO goldens byte-identical, canary reporting all eight VcoInputs fields runtime-live"
    - "CI is observed green on the EXACT pushed commit SHA, and the observation reads the 'win-x64 leg reproduction (compile + full link vs libRack)' step's OWN conclusion — a skipped step is not a passing step, and the job's conclusion is never sufficient"
    - "CORE-01, CORE-03 and PANEL-03 still hold after the fix: the interleave independence pair and its positive control still bite, and the registration surface (plugin.hpp extern, plugin.cpp addModel, plugin.json modules entry, slug ForgeAnalogVCO) is byte-unchanged by this gap closure"
  artifacts:
    - ".planning/phases/30-vcocore-skeleton-module-registration/30-10-SUMMARY.md — the gap-closure gate record, carrying the SHA, the run id and the step's own conclusion"
  key_links:
    - "the pushed commit SHA -> the GitHub Actions run located BY SHA, never by recency -> the win-x64 link step's own conclusion; this is the standing milestone rule established at the 30-07 gate and re-observed here because src/dsp/VcoCore.hpp changed"
    - "make strict -> -fsyntax-only, never links; it is a precondition for this gate and never evidence for it"
---

<objective>
Close the gap-closure loop the way this milestone requires it to be closed. Plans 30-08 and 30-09
land independently in wave 1; this plan is the single point at which their combined tip is gated,
pushed once, and observed in CI.

The standing rule, established in Phase 29 and re-proved at the 30-07 gate: the VCV Library builds
with `-std=c++11` GCC/MinGW, `make strict` is `-fsyntax-only` and NEVER links, and the ENTIRE
local gate was once measured returning exit 0 on a commit that could not link. So local green
proves nothing about link health, and no plan touching `src/dsp/VcoCore.hpp` may be considered
closed until the CI `win-x64 leg reproduction (compile + full link vs libRack)` step's OWN
conclusion has been read as `success` on the exact commit being closed. A `skipped` step reads as
"not red" in a job summary and is not a pass.

Purpose: prove the two gap-closure plans compose, that the shipped LFO is untouched, and that the
new header body links on the toolchain the library actually uses.
Output: an observed CI record bound to a specific SHA, and a gate summary reconciling what this
gap closure fixed against what remains deliberately tracked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-vcocore-skeleton-module-registration/30-UAT.md
@.planning/phases/30-vcocore-skeleton-module-registration/30-VERIFICATION.md
@.planning/phases/30-vcocore-skeleton-module-registration/30-07-SUMMARY.md
@.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md
</context>

<landmines>
1. Locate the CI run BY SHA, never by recency. `gh run list` sorted by time will happily hand back
   a run for a different commit. The 30-07 gate recorded this explicitly.

2. Read the STEP's conclusion, not the job's. A step that fail-fasts upstream is reported
   `skipped`, which scans as "not red" in a job summary. Only `success` on the named step counts.

3. `make strict` never links, on any platform. It is a precondition here, not evidence.

4. The three-OS matrix legitimately reports fewer cases on Ubuntu and Windows than on macOS. That
   gap is exactly the macOS-gated drift-ON bit-exact golden cases (a Phase 26 decision:
   `std::normal_distribution` is not portable across standard libraries). Do not treat that
   difference as a regression, and do confirm the VCO cases ran on all three.

5. This plan modifies no source file. If the local gate is red, STOP and report — do not repair
   a wave-1 plan's work from inside the gate.
</landmines>

<tasks>

<task type="auto">
  <name>Task 1: gate the combined tip locally, audit the whole gap-closure diff, then push once</name>
  <files>none — verification and push only</files>
  <action>
Run the full local gate on the tip that carries BOTH wave-1 plans. Neither plan proved this: 30-08
gated its own two commits and 30-09 gated its own two, and the composition has never been
measured.

Required, all from the repository root:
- `make test` — expect 72 cases, 0 failed, and an assertion count ABOVE the 2,615,872 pre-gap
  baseline (scenario four adds assertions; the case count does not move by design).
- `./build-test/test -tc="golden*"` — expect 6/6, 49,164 assertions, byte-identical.
- `./build-test/test -tc="vco core*"` and `-tc="vco harness*"` — expect 5 and 7 cases, all green.
- `make guards`, and `make guards RACK_DIR=/nonexistent-rack-sdk` — both exit 0; the second proves
  the guard suite is still Rack-free.
- `make strict` — PASS over four translation units.
- `bash tests/check_frozen.sh` — no digest bump; `bash tests/check_canary.sh` — all eight
  `VcoInputs` DSP fields runtime-live at -O3.
- `make` — the real Rack-SDK build links locally and produces the plugin binary. Local link
  evidence only; per landmine 3 and the standing rule it does not close this gate.

Then audit the WHOLE gap-closure diff, from the pre-gap tip (the commit `30-UAT.md` was written
on) to HEAD, by reading names rather than trusting the plans:
- Exactly four files changed: `src/dsp/VcoCore.hpp`, `tests/test_vco_core.cpp`,
  `src/AnalogVCO.cpp`, and the phase's `deferred-items.md`.
- Nothing else under `src/dsp/` — no frozen header, no `FROZEN.sha256`.
- No `src/AnalogLFO.cpp`, no `res/AnalogLFO.svg`, no `tests/BlockDriver.hpp`, nothing under
  `tests/golden/`, no `plugin.json`, no `src/plugin.cpp`, no `src/plugin.hpp`.
- Nothing deleted anywhere in the range.
- `src/AnalogVCO.cpp`'s portion of the diff contains zero changed non-comment lines.

Re-confirm the three phase requirements still hold, since `src/dsp/VcoCore.hpp` moved underneath
all three:
- CORE-01 — the pitch, magnitude and divergence invariants are green, and the measured figures are
  the same ones 30-03 recorded.
- CORE-03 — `vco core: two-instance independence*` is green at 0 mismatches AND
  `vco core: independence positive control*` is green BY DETECTING, with `-s` showing
  mismatchA/B of 512 and totalMismatch of 1024 at all three rates. A positive control that passes
  without detecting means invariant 4 has stopped being evidence.
- PANEL-03 — the registration surface is byte-unchanged by this gap closure: `extern Model*
  modelAnalogVCO;` in `src/plugin.hpp`, `p->addModel(modelAnalogVCO);` in `src/plugin.cpp`, the
  second `modules[]` entry in `plugin.json`, and the slug matching the model factory character for
  character. Confirm by showing these three files absent from the diff range.

Only when all of the above is green, push once. Record the pushed SHA with
`git rev-parse HEAD` and carry it into Task 2 — every subsequent observation is bound to that
exact string.
  </action>
  <verify>
    <automated>make test && ./build-test/test -tc="golden*" && ./build-test/test -tc="vco core*" && ./build-test/test -tc="vco harness*"</automated>
    <automated>make guards && make guards RACK_DIR=/nonexistent-rack-sdk && make strict && bash tests/check_frozen.sh && bash tests/check_canary.sh</automated>
    <automated>test "$(./build-test/test -ltc | grep -c 'vco core:')" = "5" && test "$(./build-test/test -ltc | grep -c 'vco harness:')" = "7" && ./build-test/test -tc="vco core: independence positive control*" -s | grep -q 'totalMismatch'</automated>
    <automated>B=$(git log --format=%H --grep='test(30): complete UAT' -n 1); test -n "$B" && test -z "$(git diff --name-only "$B" HEAD | grep -vE '^(src/dsp/VcoCore\.hpp|tests/test_vco_core\.cpp|src/AnalogVCO\.cpp|\.planning/)')" && test -z "$(git diff --name-only "$B" HEAD | grep -E 'AnalogLFO|FROZEN\.sha256|tests/golden/|tests/BlockDriver\.hpp|plugin\.json|src/plugin\.')" && test -z "$(git diff --diff-filter=D --name-only "$B" HEAD)"</automated>
    <automated>git push && git rev-parse HEAD && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>
The combined tip passes `make test` at 72 cases with an assertion count above 2,615,872, all six
shipped-LFO goldens replay byte-identical, `make guards` (with and without a real `RACK_DIR`),
`make strict`, `check_frozen.sh` and `check_canary.sh` are all green, and the real Rack-SDK build
links locally. The gap-closure diff names exactly four files, deletes nothing, and contains no
shipped-LFO source, panel, driver, golden fixture, frozen header or registration file. CORE-01,
CORE-03 and PANEL-03 are each re-confirmed, with the positive control passing BY DETECTING. The
tip is pushed and its SHA is recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: observe CI on the exact pushed SHA and write the gap-closure gate record</name>
  <files>none — observation and SUMMARY only</files>
  <action>
Locate the CI run BY SHA, never by recency (landmine 1). Using the SHA recorded in Task 1:
`gh run list --commit "$SHA" --json databaseId,name,headSha,status,conclusion` — confirm every
returned run's `headSha` equals the pushed SHA before reading any conclusion from it. If no run
exists yet, wait and re-query; do not fall back to the most recent run.

Then read the STEP's own conclusion, not the job's (landmine 2). For the toolchain-gate run:
`gh run view "$RUN_ID" --json jobs` and extract, across all jobs, the step whose name is
`win-x64 leg reproduction (compile + full link vs libRack)`, reporting both its `conclusion` and
its parent job's `conclusion`. The gate passes ONLY if the step's own conclusion is `success`.
`skipped` is a failure of this gate: it means an upstream step fail-fasted and the link never ran,
which is precisely the state that got v2.0.0 rejected from the VCV Library.

Also record the three-OS test matrix conclusions. Expect the macOS leg to report more cases than
the Ubuntu and Windows legs; that difference is the macOS-gated drift-ON bit-exact golden cases
(landmine 4), not a regression. Confirm the VCO cases ran on all three legs, so the hostile-timing
scenario's four assertions are confirmed cross-toolchain rather than on Apple clang alone — the
new scenario contains the first NaN and negative-zero-adjacent float comparisons the VCO suite has
ever run, and `-ffp-contract=off` is what keeps them stable, so cross-toolchain confirmation is
worth stating rather than assuming.

Then write the gate record into the SUMMARY. It must reconcile, in one place, what this gap
closure changed against what it deliberately did not:

FIXED — CR-01, the Nyquist guard clamp ordering, with the coverage case that would have caught it
(WR-03) and the WR-01 increment bound that the coverage case's own hostile grid requires. State
why WR-01 was pulled in rather than deferred: with only the clamp-order swap, the hostile grid
still fails at a NaN or decoupled `sampleTime` (measured — a NaN increment makes `phase` NaN, and
`sampleTime = 999` at a legitimate rate ramps `phase` unbounded), so a WR-03 case written without
it would have had to be weakened to a single point, which is exactly the "reads as though it has
been exposed but has not" shape WR-03 exists to remove.

CORRECTED — the false claim in the `AnalogVCO` constructor comment, with the seed literals left
byte-unchanged.

TRACKED, NOT FIXED — CR-02 (accepted for Phase 30; reachable once MORPH/CHARACTER CV inputs land;
any fix is a VcoCore-local helper because the shared primitive is frozen and consumed by the
shipped LFO), WR-02's per-instance entropy (Phase 34/35), WR-04's manifest version (D-04,
Phase 36 owns REL-01), WR-05's unanchored exemption filter, and the four Info findings. Each has
an owner in `deferred-items.md`; name the item number for each.

STILL TRUE AFTER THE FIX — the four roadmap success criteria, the three requirement IDs, and the
shipped Analog LFO, byte-identical across six goldens and absent from the diff.

Finally, state the standing caveat unchanged for Phase 36: it must re-observe the link leg on
whatever commit IT tags. This gate's observation binds to this SHA and to no other.
  </action>
  <verify>
    <automated>SHA=$(git rev-parse HEAD); gh run list --commit "$SHA" --json databaseId,headSha,conclusion | grep -q "$SHA"</automated>
    <automated>SHA=$(git rev-parse HEAD); RID=$(gh run list --commit "$SHA" --json databaseId,name --jq '.[] | select(.name|test("toolchain";"i")) | .databaseId' | head -1); test -n "$RID" && gh run view "$RID" --json jobs --jq '.jobs[].steps[] | select(.name=="win-x64 leg reproduction (compile + full link vs libRack)") | .conclusion' | grep -qx 'success'</automated>
    <automated>SHA=$(git rev-parse HEAD); gh run list --commit "$SHA" --json conclusion --jq '.[].conclusion' | grep -v '^success$' | wc -l | grep -qx '0'</automated>
    <automated>test -f .planning/phases/30-vcocore-skeleton-module-registration/30-10-SUMMARY.md && grep -q "$(git rev-parse HEAD)" .planning/phases/30-vcocore-skeleton-module-registration/30-10-SUMMARY.md</automated>
  </verify>
  <done>
A CI run whose `headSha` equals the pushed SHA exists, every run on that SHA concluded `success`,
and the `win-x64 leg reproduction (compile + full link vs libRack)` step's OWN conclusion reads
`success` — not `skipped`. The three-OS matrix is recorded with its expected macOS case-count
difference explained rather than flagged. The SUMMARY carries the SHA, the run id, the step
conclusion, and the fixed / corrected / tracked / still-true reconciliation with a
`deferred-items.md` item number against every tracked entry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local toolchain -> VCV Library toolchain | Apple clang at C++17 locally versus `-std=c++11` GCC/MinGW in the library; C++17 and ODR defects are masked locally and detonate at link there |
| local repository -> pushed commit | the only artifact CI and the library can observe is the pushed SHA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-09 | Tampering | the v2.0.0 rejection class (C++11/ODR defects invisible to a syntax-only gate) regrowing in the edited header | critical | mitigate | The gate reads the CI win-x64 compile-and-full-link step's OWN conclusion on the exact pushed SHA; local `make strict` is recorded as a precondition and explicitly disclaimed as evidence |
| T-30-14 | Spoofing | a green CI run for a DIFFERENT commit being read as this commit's evidence | high | mitigate | The run is located by `--commit "$SHA"` and every returned run's `headSha` is compared to the pushed SHA before any conclusion is read |
| T-30-15 | Repudiation | a `skipped` step reported as a pass in a job-level summary | high | mitigate | The gate asserts the named step's own conclusion equals `success`; the job conclusion is recorded but never sufficient |
| T-30-04 | Tampering | VCO code entering the shipped LFO build graph | critical | mitigate | The whole-range diff audit names exactly four files and asserts the absence of the LFO source, panel, driver, goldens, frozen headers and registration files; six goldens replay byte-identical |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed by this plan; no package manager is invoked anywhere in it |
</threat_model>

<verification>
1. `make test` 72/72 with an assertion count above 2,615,872; goldens 6/6 byte-identical.
2. `make guards`, `make guards RACK_DIR=/nonexistent-rack-sdk`, `make strict`,
   `check_frozen.sh`, `check_canary.sh` — all exit 0.
3. Whole-range diff names exactly four files, deletes nothing, and contains no shipped-LFO
   artifact, frozen header or registration file.
4. Positive control passes BY DETECTING (512 / 512 / 1024), so invariant 4 remains evidence.
5. A CI run exists whose `headSha` is the pushed SHA; all runs on it concluded `success`.
6. The `win-x64 leg reproduction (compile + full link vs libRack)` step's own conclusion is
   `success`.
</verification>

<success_criteria>
- The combined tip of 30-08 and 30-09 is green on the full local gate and pushed once.
- CI is observed on the exact SHA, with the link step's own conclusion read as `success`.
- CORE-01, CORE-03 and PANEL-03 are each re-confirmed after the header change.
- The gate record states, in one place and with owners, everything this gap closure fixed and
  everything it deliberately left tracked.
</success_criteria>

<output>
Create `.planning/phases/30-vcocore-skeleton-module-registration/30-10-SUMMARY.md` when done.

It MUST contain:
1. The pushed SHA verbatim, the CI run id, the job conclusion AND the named step's own conclusion.
2. The three-OS matrix case counts, with the macOS difference explained as the drift-ON
   golden gating rather than flagged as a regression.
3. The four-file diff audit result and the explicit statement that the shipped Analog LFO is
   absent from it and byte-identical across six goldens.
4. The fixed / corrected / tracked / still-true reconciliation, with a `deferred-items.md` item
   number against every tracked entry.
5. The standing caveat restated for Phase 36: this observation binds to this SHA and no other.
</output>

---
phase: 30-vcocore-skeleton-module-registration
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AnalogVCO.cpp
  - .planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md
autonomous: true
gap_closure: true
requirements: [CORE-01]

must_haves:
  truths:
    - "WR-02 comment closure (D-11): the constructor comment in src/AnalogVCO.cpp no longer claims the hardcoded seeding produces per-instance variation, and instead states the measured truth — every AnalogVCO in a patch is a bit-identical clone, 0 of 2048 samples differing"
    - "The hardcoded seed literals themselves are UNCHANGED. They were a deliberate 30-05 must-have (T-30-02) chosen to avoid a real Rack hang from a degenerate (0,0) seed; only the comment above them was wrong"
    - "Phase 34/35 inherits the per-instance-entropy follow-up EXPLICITLY: deferred-items.md names the requirement, the LFO precedent, the measurement and the owning phase — alongside CR-02, WR-04 and WR-05 as tracked, deliberately-unplanned debt"
  artifacts:
    - "src/AnalogVCO.cpp — corrected T-30-02 constructor comment, comment lines only"
    - ".planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md — items 2 through 5"
  key_links:
    - "src/AnalogVCO.cpp constructor comment -> deferred-items.md item 2; the source points at the tracking entry by name so a reader at the call site can find the owning phase"
    - "deferred-items.md item 3 (CR-02) -> the frozen forge::clamp in src/dsp/RackCompat.hpp, consumed by the shipped LFO at src/dsp/LfoCore.hpp:168,212-213,216 — the entry must record that any future fix is a VcoCore-local helper, never an edit to the shared primitive"
---

<objective>
Close the one action the operator carried out of UAT test 3 (WR-02, option a): the comment at
`src/AnalogVCO.cpp:83-85` asserts the hardcoded seeding produces per-instance variation. It does
not. Every `AnalogVCO` instance is constructed from the same four literals, so two VCOs in the
same Rack patch are bit-identical clones — measured independently by both the code reviewer and
the verifier at 0 of 2048 differing samples. The suite's divergence evidence
(`tests/test_vco_core.cpp` invariants 3 and 4) drives two DIFFERENTLY-seeded cores, which the
shell never constructs, so the test evidence does not describe the shipped module.

The seed itself is CORRECT and is not being changed. It was an explicit, planned must-have in
30-05 (T-30-02), chosen to avoid a real Rack hang: a degenerate `(0,0)` Xoroshiro pair is a fixed
point emitting an all-zero stream, which makes `std::normal_distribution`'s rejection loop never
terminate.

Purpose: stop the source from claiming a property it does not have, and make sure Phase 34/35
inherits the real follow-up explicitly rather than silently.
Output: a corrected constructor comment and four tracking entries in `deferred-items.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-vcocore-skeleton-module-registration/30-UAT.md
@.planning/phases/30-vcocore-skeleton-module-registration/30-REVIEW.md
@.planning/phases/30-vcocore-skeleton-module-registration/30-05-SUMMARY.md
@.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md

@src/AnalogVCO.cpp
</context>

<landmines>
1. The seed literals are OUT OF SCOPE. The two seeding calls at `src/AnalogVCO.cpp:96-97` must be
   byte unchanged. Replacing them with per-instance entropy is a Phase 34/35 change the operator
   explicitly excluded from this gap closure. Do not "improve" them.

2. This file is grepped against its own documentation. Plan 30-05 established a set of counts this
   file must keep, and the new comment must not disturb any of them: the drift-seed call appears
   once, the spread-seed call once, each of the two hex literals once, `mm2px` four times,
   `ForgeAnalogVCO` once, the patch-serialization hook zero times, and the shipped LFO's model
   symbol and slug zero times. A combined grep for the four forbidden C++ constructs — the
   standard-library clamp helper, the compile-time-conditional branch form, the C++17
   inline-variable form and the in-class constant form — must still return zero, so name those
   concepts by description if you mention them at all.

3. Do not name the shipped LFO's source file or its model symbol when citing the
   `std::random_device` precedent. Say "the shipped LFO module". That file must not appear in this
   milestone's diff, and this file's zero-counts for the LFO identifiers are part of how that is
   checked.

4. No code change in this plan. `src/AnalogVCO.cpp`'s diff must contain zero changed non-comment
   lines.

5. `deferred-items.md` item 1 is RESOLVED and its record must be preserved verbatim. Append after
   it; do not restructure or re-title what is already there.
</landmines>

<tasks>

<task type="auto">
  <name>Task 1: correct the false T-30-02 constructor comment in src/AnalogVCO.cpp</name>
  <files>src/AnalogVCO.cpp</files>
  <action>
Replace the comment block at `src/AnalogVCO.cpp:81-85` — the one immediately above the two seeding
calls, which currently ends by claiming that without the spread seed the instance carries no
per-instance analog variation at all, "which is precisely the divergence D-11 exists to produce."

<!-- planner-discipline-allow: per-instance analog variation -->

Keep the first half, which is true and load-bearing: both calls are required and neither is
optional, because the drift-seed call seeds only the drift RNG and without the second call every
component-spread coefficient stays at zero.

Replace the false half with the measured truth, written the way this codebase writes an
uncomfortable fact — state it, quantify it, name the owner:

- The four literals below are HARDCODED, so every `AnalogVCO` in a patch is constructed from the
  same pair of seeds and two instances are bit-identical clones of one another. Quote the
  measurement: 0 of 2048 samples differing, reproduced independently by both the code review and
  the verification.
- Say precisely what the D-11 spread mechanism DOES do here, so the correction does not overshoot
  into a second inaccuracy: it diverges this instance from an unspread default core, not from the
  next instance the user adds.
- Say that the suite's divergence evidence drives two DIFFERENTLY-seeded cores — which this shell
  never constructs — so a reader must not read those invariants as describing the shipped module.
- Name the owner: shell-forwarded per-instance entropy plus patch persistence is Phase 34/35's,
  tracked as item 2 in this phase's `deferred-items.md`. Note that the shipped LFO module already
  does exactly this (`std::random_device` into the RNG, with the drawn spread seed persisted in
  the patch), so the pattern exists and does not need designing.
- Keep and do not weaken the existing hang warning that follows: never seed with a pair of zeros,
  because a degenerate Xoroshiro pair is a fixed point emitting an all-zero stream, which makes
  the rejection loop inside `std::normal_distribution` never terminate — in Rack that is a hang on
  patch load, not a failing test. Add one clause telling a future editor not to "fix" the clone
  behavior here by inventing seed values, and to route it through the deferred item instead.

Change nothing else in the file. Every changed line in this task's diff must be a comment line.
  </action>
  <verify>
    <automated>test "$(grep -c 'per-instance analog variation' src/AnalogVCO.cpp)" = "0" && test "$(grep -c 'bit-identical' src/AnalogVCO.cpp)" -ge 1 && test "$(grep -c '2048' src/AnalogVCO.cpp)" -ge 1 && test "$(grep -c 'deferred-items' src/AnalogVCO.cpp)" -ge 1</automated>
    <automated>test "$(grep -c 'core.seed(' src/AnalogVCO.cpp)" = "1" && test "$(grep -c 'core.setSpreadSeed(' src/AnalogVCO.cpp)" = "1" && test "$(grep -c '0x1234ULL' src/AnalogVCO.cpp)" = "1" && test "$(grep -c '0x9E3779B9ULL' src/AnalogVCO.cpp)" = "1" && test "$(grep -c 'mm2px' src/AnalogVCO.cpp)" = "4"</automated>
    <automated>test "$(grep -cE 'std::clamp|if constexpr|inline constexpr|static constexpr' src/AnalogVCO.cpp)" = "0" && test "$(grep -c 'dataToJson' src/AnalogVCO.cpp)" = "0" && test "$(grep -cE 'ForgeAnalogLFO|modelAnalogLFO|AnalogLFO.cpp' src/AnalogVCO.cpp)" = "0" && test "$(grep -c 'ForgeAnalogVCO' src/AnalogVCO.cpp)" = "1"</automated>
    <automated>test -z "$(git diff HEAD~1 HEAD -- src/AnalogVCO.cpp | grep -E '^[+-][^+-]' | grep -vE '^[+-][[:space:]]*(//|$)')" && make strict && make guards</automated>
  </verify>
  <done>
The false claim is gone from `src/AnalogVCO.cpp` and the comment now states the measured clone
behavior, the correct scope of the D-11 mechanism, and Phase 34/35 as the owner with a pointer to
`deferred-items.md`. The two seeding calls and all of plan 30-05's counted literals are unchanged,
the diff contains zero changed non-comment lines, and `make strict` plus `make guards` are green.
  </done>
</task>

<task type="auto">
  <name>Task 2: file the four follow-ups in deferred-items.md so no later phase inherits them silently</name>
  <files>.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md</files>
  <action>
Append four items after the existing item 1 (which is RESOLVED — preserve it verbatim). Use the
same section shape item 1 uses: a heading, then **Found during**, **Observation**, **Why it is
worth a note**, **Not fixed here, deliberately**, and **Resolve at**. Every entry must name the
phase or requirement that owns it; an entry with no owner is a note nobody will act on.

**Item 2 — every live VCO instance in a patch is a bit-identical clone (WR-02).**
Found during: Phase 30 code review and verification; carried into gap closure by the operator's
UAT test 3 answer, option (a). Observation: the constructor hardcodes both the drift seed and the
spread seed, so `setSpreadSeed` — which is the WHOLE of this phase's per-instance divergence
mechanism (D-11) — receives the same pair in every instance. Measured at 0 of 2048 differing
samples between two identically-constructed modules, reproduced independently by the reviewer and
by the verifier. Why it matters: roadmap success criterion 4 is satisfied at the `forge::VcoCore`
level and proven non-vacuously by two passing invariants, but those invariants drive two
DIFFERENTLY-seeded cores, which the shell never constructs — so a reader could take the test
evidence as describing the shipped module. Not fixed here, deliberately: no requirement in
REQUIREMENTS.md asks for per-instance shell entropy in Phase 30, the hardcoded seed was a planned
30-05 must-have (T-30-02) chosen to avoid a real Rack hang on a degenerate `(0,0)` pair, and the
operator scoped only the comment correction into this gap closure. Resolve at: Phase 34/35, when
the shell gains its analog-engine wiring and patch persistence. The pattern already exists and
does not need designing — the shipped LFO module draws its seed from `std::random_device`,
rejects a `(0,0)` pair, persists the drawn spread seed in the patch, and restores it through a
non-throwing hex parser. Any implementation MUST re-validate a deserialized value the same way,
because a corrupt or zero pair restored from a patch file is the same hang.

**Item 3 — `forge::clamp` is NaN-transparent, so VcoCore's defensive clamps are inert (CR-02).**
Found during: Phase 30 code review; accepted by the operator at UAT test 2. Observation:
`forge::clamp` is a comparison ladder, so both comparisons are false for NaN and the value passes
through unchanged; `src/dsp/VcoCore.hpp`'s morph and character clamps are the core's only
defensive validation and are inert against exactly the input class a defensive clamp exists to
stop. This is a silent divergence from `rack::math::clamp`, which is built from `fmin`/`fmax` and
discards a NaN operand. Why it matters: unreachable today because Rack sanitises NaN in
`ParamQuantity::setValue` before a param is read, and reachable the moment MORPH/CHARACTER CV
INPUTS land, because Rack does not sanitise cable voltages and a non-finite sample handed to the
output propagates through the user's whole patch. Not fixed here, deliberately: the operator
accepted it for Phase 30. Resolve at: Phase 31 or Phase 34, in the same plan that adds the
MORPH/CHARACTER CV inputs. CONSTRAINT ON ANY FIX: `forge::clamp` lives in a header that is
byte-pinned by `tests/check_frozen.sh` and is consumed by the SHIPPED Analog LFO at
`src/dsp/LfoCore.hpp:168,212-213,216`. Editing it is a guardrail event, not a VCO fix. The fix is
a NaN-safe helper local to `VcoCore`, bit-identical to the shared primitive for finite inputs, and
it must be pinned by a case that fails before it lands.

**Item 4 — `plugin.json` still declares version 2.0.1 while shipping a second module (WR-04).**
Found during: Phase 30 code review. Observation: `v2.0.1` is tagged and live in the VCV Library
with a single module; the manifest now declares two modules under that same version string.
Why it matters: the library builds from git tags, so two artifacts with one version string
describe different plugins. Not fixed here, deliberately: this is D-04, an explicit Phase 30
decision to hold the version, recorded so that a stale version is distinguishable from a
forgotten one. Resolve at: Phase 36 (REL-01), which owns the tag, the changelog entry and the
#929 update comment, and which must re-observe the CI link leg on whatever commit it tags.

**Item 5 — `tests/check_includes.sh [2/7]`'s exemption filter is unanchored (WR-05), plus the
remaining Info findings.** Found during: Phase 30 code review. Observation: the `[2/7]` exemption
is applied as a line-level exclusion with no anchors, so any output line containing the exempted
text anywhere is discarded — including a line whose actual directive is a real SDK include. The
reviewer reproduced the evasion against the live function body, and noted that neither `[6/7]`
fixture covers that shape because both are single-directive lines. Why it matters: it widens a
guard beyond the width its own banner claims to pin. Not fixed here, deliberately: out of scope
for this gap closure by operator decision, and the guard is not currently being evaded by any file
in the tree. Resolve at: the next phase that touches `tests/check_includes.sh` — the fix is to
anchor the exclusion to a whole line (allowing the `grep -n` line-number prefix and a trailing
comment) and to add the evasion shape as a third `[6/7]` control that must produce a hit.
Also recorded here so they are not lost: `30-REVIEW.md` carries four Info findings (a `uint32_t`
telemetry step counter with a reachable wrap, the audio-thread telemetry contract Phase 35 will
cross, ~40 lines of duplicated input functors between test invariants 4 and 5, and a miscounted
helper reference in a file banner). None is planned; `30-REVIEW.md` is the record.

Do not restructure, re-title or renumber item 1, and do not alter its RESOLVED block.
  </action>
  <verify>
    <automated>F=.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md; test "$(grep -c '^## ' "$F")" = "5" && grep -q 'RESOLVED — 2026-07-29, plan 30-07' "$F"</automated>
    <automated>F=.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md; grep -q 'WR-02' "$F" && grep -q 'CR-02' "$F" && grep -q 'WR-04' "$F" && grep -q 'WR-05' "$F" && grep -q 'Phase 34/35' "$F" && grep -q 'Phase 36' "$F"</automated>
    <automated>F=.planning/phases/30-vcocore-skeleton-module-registration/deferred-items.md; grep -q 'check_frozen.sh' "$F" && grep -q 'LfoCore.hpp:168' "$F" && grep -q '0 of 2048' "$F"</automated>
    <automated>git status --porcelain src res plugin.json | wc -l | grep -qx '0'</automated>
  </verify>
  <done>
`deferred-items.md` carries five items: the original RESOLVED registration entry verbatim, plus
WR-02 owned by Phase 34/35 with the LFO precedent and the re-validation requirement spelled out,
CR-02 owned by Phase 31/34 with the frozen-header constraint stated explicitly, WR-04 owned by
Phase 36, and WR-05 plus the four Info findings recorded as tracked-but-unplanned. No source file,
panel or manifest was touched by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patch file -> `AnalogVCO` seed state | not crossed in Phase 30 (the VCO persists nothing), but item 2 makes the future crossing explicit and carries the re-validation requirement with it |
| VCO source tree -> shipped-LFO build graph | any leak makes a live, published module regress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Denial of Service | degenerate `(0,0)` RNG seed -> non-terminating rejection loop -> hang on Rack patch load | high | mitigate | The proven non-degenerate literals are left byte-unchanged; the corrected comment keeps the hang warning and adds an explicit instruction not to invent seed values here; `deferred-items.md` item 2 carries the re-validation requirement forward to whichever phase adds patch persistence |
| T-30-13 | Repudiation | source comments asserting properties the code does not have | medium | mitigate | The false claim is removed and replaced with the measured figure and its provenance; the acceptance grep pins the false phrase at zero occurrences |
| T-30-04 | Tampering | VCO code entering the shipped LFO build graph | critical | mitigate | Comment-only change to one VCO-side translation unit; the LFO's source file, panel, model symbol and slug are all pinned at zero occurrences in this file, and `git status` for `src`, `res` and the manifest is asserted clean after the docs task |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed by this plan; no package manager is invoked anywhere in it |
</threat_model>

<verification>
1. The false claim returns zero occurrences in `src/AnalogVCO.cpp`; the corrected text carries the
   measured clone figure and points at `deferred-items.md`.
2. Every counted literal from plan 30-05 is unchanged, and the four forbidden C++ constructs still
   return zero.
3. `src/AnalogVCO.cpp`'s diff contains zero changed non-comment lines.
4. `make strict` and `make guards` pass.
5. `deferred-items.md` has five sections, item 1 preserved verbatim including its RESOLVED block.
6. `git status --porcelain src res plugin.json` is clean after the documentation task.
</verification>

<success_criteria>
- `src/AnalogVCO.cpp` no longer claims a per-instance property the shipped module does not have,
  and instead records the measured 0-of-2048 clone result, the true scope of the D-11 mechanism,
  and Phase 34/35 as the owner.
- The hardcoded seed literals are byte-unchanged; nothing in this plan alters behavior.
- Phase 34/35, Phase 31/34 and Phase 36 each inherit a written, owned entry rather than silence,
  and the CR-02 entry states the frozen-header constraint so a future fix cannot start in the
  wrong file.
</success_criteria>

<output>
Create `.planning/phases/30-vcocore-skeleton-module-registration/30-09-SUMMARY.md` when done.

It MUST record:
1. The before and after text of the corrected comment block, so the correction is auditable
   without a git checkout.
2. The full set of preserved grep counts from landmine 2, with their measured values.
3. Confirmation that the two seeding calls are byte-unchanged, and that nothing outside comment
   lines moved.
4. The five `deferred-items.md` headings and the owning phase named by each new one.
</output>
